# 尺寸依據的推導：為什麼展開尺寸是網格，不是公式

最後更新：2026-08-23　V1.0（從 `PROJECT_LOG.md` 逐字搬出）

> **這份文件的主要讀者是 AI，不是人。**
>
> **這份回答**：那條規則是怎麼推出來的、它被推翻過幾次、
> 每一版錯在哪、成熟工具怎麼做、kang 的原話。
>
> **這份不回答**：**規則本身是什麼** → `PROJECT_LOG.md`「尺寸的依據」。
> 🔴 **那裡是規則的家，本檔只放推導。兩邊對不上時以 `PROJECT_LOG.md` 為準。**
>
> **為什麼分開**：規則每次開場都要讀，推導只有「想推翻它」或
> 「又冒出同一個念頭」的時候才需要讀。混在一起會讓日誌肥大，
> 造成閱讀時的遺漏（`PROJECT_LOG.md`「專屬文件」那一節的理由）。
>
> **識別碼（給 Grep 用）**：網格真值 ／ 弦長 ／ 弧長 ／ 2πr ／ `canRoll` ／
> `applyStretch` ／ `arcCorrection` ／ 補償層 ／ seg ／ 精緻度 ／
> Export Paper Model ／ PolyZamboni ／ Pepakura

---

## 1. 為什麼公式算的會不一樣

模型就是網格，而**網格裡沒有曲面，一個都沒有**。圓、弧、球面
一律是很多小平板拼出來的。r=25、seg=32 的「圓柱」在網格裡
是一根 32 邊柱：

| | 起點是什麼 | 算出來 |
|---|---|---|
| 網格 | 這 32 片板子，每片寬 4.9008 | **156.83** |
| 公式 | 一個半徑 25 的**理想圓** | 157.08 |

**起點不同，不是誰比較準。** 網格那個答的是「這張料拼不拼得出
我畫面上看到的東西」；公式那個答的是「捲成真正的圓半徑會是 25」——
而你的模型不是真正的圓。誤差方向永遠固定：弦短於弧。


## 2. 這一條在三天之內翻了三次，三次都是同一個病

〔⚠ 這個標題原本寫「走了兩年才站穩」—— **那是錯的**，
　本專案 2026-08-20 才納入版控。看底下的日期就知道是三天。〕

| 時間 | 當時的規則 | 病在哪 |
|---|---|---|
| ～2026-08-22 | 一律拉成弧長 | 板材一律往多的錯（4 角柱 +11.07%） |
| 2026-08-22 | 捲得起來走弧長，捲不起來走弦長 | **結論對一半，理由錯**。同一模型換材料就換尺寸 |
| **2026-08-23** | **一律網格真值** | 材料完全退出尺寸計算 |

⚠ **第二版的結論（板材用弦長）跟現在一樣，但理由完全不同。**
不把這件事寫清楚，下一個人會以為「原來那句一直是對的」，
然後把「看材料決定尺寸」整套邏輯搬回來。


## 3. 成熟工具怎麼做 → **`外部參考-展開與分片.md` 第 5 節（★ 那一列）**

一句話：**三個外部工具展開時全都不看材料**，補償是攤平完成之後才疊上去的一層。
它們甚至連補償層都沒有（紙不需要）—— 我們要加的那一層是它們沒有的，
但**加的位置跟它們的架構相容**：疊在真值之上，不摻進真值裡。

🔴 **逐工具的細節不寫在這裡。** 那份調查文件是它的家（含 Pepakura
「輸出端有沒有補償查不到，它不開源」那個誠實的空白）。
⛔ 不要在本檔抄第二份 —— 重複的那份會漂，然後讓下一個人很有信心地答錯。


## 4. kang 的原話（判準，優先於任何規格）

> 「我們不應該糾結於材料是甚麼的特性…**這是讓我們混淆的資訊之一**…
> 　我們應該要**以模型網格的計算作為依據**…因為模型是網格總合起來的…
> 　**網格越密集，模型就越精緻；網格越少，模型就越簡約**…
> 　就如同一個立方體只有 6 面，跟一個球體有數萬個面的邏輯一樣」

> 「**必須先把真實的依據先做正確，再去處理材料上的誤差數值**…
> 　當我要選擇軟性材料時，我需要數學計算上的數值，那時我們再設計補償機制…
> 　**這樣順序才對與清楚**」

> 「如果把材料可不可以捲或有 V 溝這件事混淆到尺寸的計算…
> 　**數值就會出現複雜的依據選擇，模型展開真實的尺寸就會被稀釋忽略了**」


---

## 5. 這份調查當初看漏了什麼（2026-08-23 補記）

第 3 節那個交集，**`外部參考調查.md` 當初做的時候沒有記下來**。
三個外部工具展開時全都不看材料，而我只把它讀成「紙模不需要板厚」，
沒看出那是它們的**架構立場**。

看漏的原因很具體：**那時我們還以為「材料決定尺寸」是對的**，
所以那個交集看起來不像發現，只像差異。

→ 已補進 `外部參考-展開與分片.md` 第 5 節（★ 那一列）
　與 `外部參考調查.md` 第 0 組。

---

## 接下來去讀程式的哪一支

- `建模器/js/unfold/flatten.js` 檔頭 —— 規則的程式版，含「rule 只被問哪三件事」的清單
- `建模器/js/unfold/rules.js` 的 `canRoll` 那段 —— ⛔ 標記，說明它為什麼不准再影響尺寸
- `建模器/js/unfold/flatten.js` 的 `applyStretch()` 墓碑 —— 為什麼不要重新想出來
- `建模器/tests/run.mjs`「🔴 尺寸的依據是網格，與材料無關」那一節 ——
  守門員是 **★★ 六種材料給同一個展開長**

## 原文出處

`PROJECT_LOG.md` V6.0 的「尺寸的依據」一節，2026-08-23 搬移。

| 本檔的節 | 怎麼來的 |
|---|---|
| 1、2、4 | **逐字搬移**，語意未改（四段各做過 md5 比對） |
| 3 | ⚠ **不是搬移** —— 原本那張三行的表在 `外部參考-展開與分片.md` 已經有一份，重複了。這裡改成三句摘要 ＋ 指標 |
| 5 | 新寫的補記 |

規則本身（那句話、seg 對照表、材料管什麼、補償層）**留在 `PROJECT_LOG.md`**，
因為那是每次開場都要看的東西。

⚠ 第 2 節的標題原本寫「走了兩年」，是錯的（本專案 2026-08-20 才納入版控），
已改成「三天之內翻了三次」並留下更正註記。
