# 外部參考調查：Blender 與 Pepakura

最後更新：2026-08-23　V1.0

> **這份文件的主要讀者是 AI，不是人。**
> 它是「**第 6 期以後所有功能邏輯的依據**」——不再一項一項現推。
> 動任何編輯或展開相關的東西之前，先讀這一份。

---

## 1. 為什麼有這份文件（kang 2026-08-23 定調）

kang 的原話：

> 「我們的建模器..在很多功能的邏輯運用上..有很多缺失...
> 　**因為我們很多都是推論出來的結果**...但是 Blender 有很多實際的文件與程式..
> 　可以提供很多專業邏輯讓我們可以參考運用...我們應該要仔細理解...
> 　**雖然使用的程式語言不同...但是功能的邏輯應該都是相同的**...
> 　我們應該吸取優點來優化我們的建模器」

以及：

> 「『編輯』也牽涉到很多**操作邏輯與介面的搭配**...
> 　這也是目前我們建模器欠缺需要優化的地方」

### 為什麼這是對的：2026-08-23 當天的證據

| 我推論出來的 | 實際 |
|---|---|
| 「把一個頂點旋轉 30 度沒有意義」→ **一行把 gizmo 鎖成只能平移** | 對**點**成立，對**面**完全不成立。梯形、收尖、擠出後轉方向全被那一行鎖死 |
| 「拉面預設沿法向」 | 寫了一個**不存在的開關**（`pushFace()` 沒有任何介面呼叫） |
| 「一個面」＝ 三角形 | 是**共面區域**（方塊是 12 個三角形，使用者看到 6 個面） |
| 「圓柱側面有四邊形」 | 只有 `tube` 與 `roundBox` 有 |
| `buildPrim({type:...})` | 簽名是 `buildPrim(type, params)` |

而**「變換 ＝ 種類 × 方向 × 中心」這三個正交概念**是查了才知道的 ——
**推論給得出一個能動的東西，成熟做法給得出一個對的結構。**

---

## 2. 🔴 兩條界線（動手前必讀）

### 一、Blender 與它的擴充都是 GPL → **讀邏輯，不抄程式碼**

- ✅ **構想與架構不受著作權保護**。讀文件、讀原始碼、理解它**為什麼**那樣設計，
  然後用我們自己的方式重寫 —— 完全合法。
- ⛔ **不可以把 Blender 或它的擴充的原始碼搬進本專案**。GPL 有傳染性，
  搬進來整個建模器就必須以 GPL 釋出。
- 這條也適用於 Blender 內建的 `io_export_paper_model` 與 PolyZamboni（都是 GPL）。

### 二、Pepakura 是商業軟體 → **只讀公開文件與行為**

Pepakura Designer（Tama Software）**不開源**。所以：

- ✅ 手冊、功能說明、操作流程 —— 讀得到，可以學「它決定做什麼、不做什麼」
- ❌ 演算法（重疊偵測、自動分片怎麼解）—— 沒公開，學不到

**演算法層次要去別的地方拿**：兩個開源展開器（見下）＋ 公開論文。

### 三、學邏輯，**不學範圍**

日誌「目標與範圍」自己定的判準：

> 專攻建模與展開，**不做 Blender 那種全能軟體**。
> Blender 做不好、但公司天天要用的事才做。

**每一項調查結果都要過這道篩子：**

> **「這個對板材切割／展開／剖面分切這條路，有沒有用？」**

有才拿。雕塑、動畫、算繪、紙模的黏合片（我們是銑 V 溝或切開再接）——一律不碰。

---

## 3. 研究線一：編輯 → Blender

**優先做這條。** 這是目前卡住的地方，而且第 6 期還沒做的四個動作全靠它。

| # | 要弄清楚的 | 我們現在 | 狀態 |
|---|---|---|---|
| 1 | **BMesh 的結構與設計取捨** | 半邊網格（`mesh.js`） | 未查 |
| 2 | **Transform 三層分解**：種類／方向／中心 | 只有「移動 × 世界 XYZ」 | 已初查（見下） |
| 3 | **選取模式與 active element** | 有過濾器，**沒有「哪一個是主導的」** | 未查 |
| 4 | **Modal operator 的互動模型** | 沒有（我們是按鈕→固定值→再拖） | 已初查 |
| 5 | **各編輯工具的完整定義與邊界情況** | 只有拉點線面、擠出（一種） | 部分 |
| 6 | **拓撲取捨：四邊形 vs 三角形** | `box` 是 12 個三角形 | 已查（見下） |
| 7 | **Undo 的粒度** | 整份文件快照 | 未查 |
| 8 | **操作邏輯與介面的搭配**（kang 特別指出） | 按鈕堆在工具列 | 未查 |

### 已經查到的（2026-08-23，官方手冊）

**⚠ 這些是 Blender 官方文件寫明的事實，不是推論。**
〔用詞更正：`編輯功能設計方向.md` 第 6 節原本寫「調查是二手的」，
那句的範圍只到**操作手感**。官方手冊是第一手來源，而且 Blender 開源，
程式碼本身也讀得到。〕

**① 結構**

| | Blender 預設方塊 | 我們的 `box` |
|---|---|---|
| 頂點 | 8 | 8 |
| **邊** | **12** | **18** |
| **面** | **6 個四邊形** | **12 個三角形** |

多出來的 6 條邊就是三角化的對角線。三角化在 Blender 裡只發生在**算繪時**，
資料層看不到。
→ 我們的 `box(p)` 是用 `THREE.BoxGeometry` 產生的，而 **three.js 只有三角形**。
　`tube`／`roundBox`／`plate`／`extrude` 是手工 `fromFaceList` 建的，所以有四邊形。
　**`mesh.js` 本身完全撐得住 n 邊形**（`roundBox` 有兩個 20 邊形的面）。

**② 變換的三個正交概念**

| 概念 | Blender | 我們 |
|---|---|---|
| **種類** | 移動 `G`／旋轉 `R`／縮放 `S` | **只有移動** |
| **方向**（Transform Orientation） | Global／Local／**Normal**／Gimbal／View／Cursor | **只有世界 XYZ** |
| **中心**（Pivot Point） | Median Point（預設）／Individual Origins／3D Cursor／Active Element／Bounding Box | **沒有這個概念** |

- **擠出預設沿法向**（Normal 方向下 Z ＝ 法向）。斜面尤其明顯 ——
  我們只能沿世界 XYZ 拉，那正是「擠出好像沒用」的根源。
- Individual Origins 在**面模式**下最可預測；頂點模式跟 Median 差不多、
  邊模式常常變形。

**③ 核心工具**

| Blender | 做什麼 | 我們 |
|---|---|---|
| `E` 擠出（Extrude Region） | 按下去幾何立刻長出來，**滑鼠拖著即時預覽**，左鍵確認／右鍵取消；過程中可鎖軸、可**直接打數字** | 部分（按鈕→固定 1cm→再另外拖） |
| `Alt+E` 沿各自法向擠出 | 多個面各自沿自己的法向 | ✘ |
| **`I` 內插（Inset）** | 面內縮一圈小面（**＝擠出＋立刻縮放**） | ✘ |
| **`S` 縮放** | **選頂面 → `S` → 縮小 ＝ 角錐台／梯形，一步** | ✘ |
| `Ctrl+B` 導圓角（Bevel） | | ✘ |
| `Ctrl+R` 環切（Loop Cut） | 加一圈邊（＝細分） | ✘ |
| `M` 合併頂點 | | ✘ |
| `X` 刪除面／邊／點 | | ✘ |

**④ 最大的手感落差：modal**

Blender 是 modal 的（按下去 → 立刻出現 → 拖著看 → 滿意才確認）。
我們是兩步（按鈕產生固定距離 → 再去拖 gizmo）。

⚠ 我當初避開 modal 的理由是「拖曳時每幀重建網格會卡」——
**那個顧慮從來沒實測過，只是推論。** 方塊 16 個面重建一次應該是微秒級。
**要先量再決定。**

---

## 4. 研究線二：展開與分片

我們的展開**已經在生產用了**，所以這條是「驗證與優化」，不是「卡住」。排第二。

### 可用的來源（依可讀深度排序）

| 來源 | 授權 | 能學到 |
|---|---|---|
| **Export Paper Model**（`io_export_paper_model`）**內建在 Blender 裡** | GPL，**原始碼讀得到** | 演算法與資料結構 |
| **PolyZamboni**（Blender 擴充，**專做低多邊形紙模**） | GPL，**原始碼讀得到** | 同上，而且正好是我們要做的那一類 |
| 公開論文（strip-based approximate unfolding 那一系列） | 公開 | 演算法本身 |
| **Pepakura Designer** | **商業，不開源** | 只有行為與工作流程 |

### ⭐ 已經查到、而且直接打到痛點的一條

Export Paper Model 的官方說明：

> The layout of the net can be customized using **Seams**, representing the edges to be cut...
> the export will use all given seams and **add some more cuts if necessary**.

**「用 Seams 定義切割線」＝ 我們的「指定分片」，同一個概念。**
但它多做一件事：**必要時自動再補刀**。

而我們：**重疊偵測只做到「偵測與告知」，自動分片排在原第 7 期、已取消。**
（取消的理由是剖面分切解了同一個需求；但「展開時重疊了怎麼辦」這條路仍然沒解。）

### 要弄清楚的

| # | 項目 | 我們現在 |
|---|---|---|
| 1 | 接縫怎麼定義、**怎麼自動補刀** | 只做使用者標；重疊只偵測不自動解 |
| 2 | **重疊偵測與自動分片的策略** | 佔展開引擎約 70% 工作量，第 3 期只做到偵測 |
| 3 | **展開圖的排版**（片怎麼擺才省料） | 沒做（在待辦裡） |
| 4 | 標示、編號的慣例 | 有接合編號，其餘沒有 |
| 5 | 低多邊形紙模的特殊處理 | 未知 |
| 6 | Pepakura 決定**不做**什麼、為什麼 | 未知 |

⚠ **過篩子**：紙模的**黏合片**對我們沒用（我們是銑 45 度 V 溝或切開再接）；
但**自動補刀、排版、重疊處理**是直接相關的。

---

## 5. 調查結果要怎麼記

每一項寫成五段，缺一不可：

```
Blender／該工具怎麼做
  → 為什麼那樣設計（這一段最重要，也最容易被跳過）
  → 我們現在怎麼做
  → 差在哪
  → 該不該學（過篩子的結論 ＋ 理由）
```

**寫進這份文件對應的章節**，不是寫進 `PROJECT_LOG.md`。
結論確定要做之後，才進待辦與 `規格\`。

⛔ **沒確認過的調查不進文件** —— 免得又長出一份會讓下一個人答錯的東西。

---

## 6. 相關文件

- `PROJECT_LOG.md` —— 目前確定的狀態、鐵律、待辦
- `編輯功能設計方向.md` —— 第 6 期的**定位、範圍、被否決的方向**（動編輯前必讀）
- `規格\建模器-點線面編輯.md` —— 已經做出來的拉點線面與擠出面
- `規格\建模器-展開與分片.md` —— 已經做出來的展開與指定分片
