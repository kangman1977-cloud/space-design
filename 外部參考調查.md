# 外部參考調查：索引、界線、與結論

最後更新：2026-08-23　V2.0（三條研究線全部完成，**優先序等 kang 拍板**）

> **這份文件的主要讀者是 AI，不是人。**
> 它是「**第 6 期以後所有功能邏輯的依據**」——不再一項一項現推。
> 動任何編輯或展開相關的東西之前，先讀這一份。
>
> 🔴 **本檔已改成索引（2026-08-23 起）。** 調查內容在下面三份專屬文件裡。
> 本檔只放**每次都要看的東西**：為什麼有這件事、兩條界線、篩子、記錄格式、
> **三線總結與優先序**。

| 檔 | 裝什麼 | 什麼時候讀 |
|---|---|---|
| **本檔** | 界線、篩子、格式、**結論與優先序** | **每次動編輯或展開之前** |
| `外部參考-Blender編輯.md` | 研究線一：BMesh 結構／變換三層／選取與 active／modal／工具／Undo | 動編輯之前，讀完整 |
| `外部參考-展開與分片.md` | 研究線二：兩個開源展開器的**演算法邏輯**；20 項逐條對照 | 動展開或分片之前，讀完整 |
| `外部參考-Pepakura.md` | 範圍與介面：一個只做展開的商業軟體，決定做什麼／不做什麼 | 討論「這個程式該做到哪」時 |

---

## 1. 為什麼有這份文件（kang 2026-08-23 定調）

kang 的原話：

> 「我們的建模器..在很多功能的邏輯運用上..有很多缺失...
> 　**因為我們很多都是推論出來的結果**...但是 Blender 有很多實際的文件與程式..
> 　可以提供很多專業邏輯讓我們可以參考運用...我們應該要仔細理解...
> 　**雖然使用的程式語言不同...但是功能的邏輯應該都是相同的**...
> 　我們應該吸取優點來優化我們的建模器」

以及：

> 「『編輯』也牽涉到很多**操作邏輯與介面的搭配**...
> 　這也是目前我們建模器欠缺需要優化的地方」

以及調查開始前的補充：

> 「我們要調查的是『**邏輯**』..不是程式語法..因為我們建模器的語法跟 Blender 是不同的..
> 　但『**邏輯**』架構行為是相通的」

### 為什麼這是對的：2026-08-23 當天的證據

| 我推論出來的 | 實際 |
|---|---|
| 「把一個頂點旋轉 30 度沒有意義」→ **一行把 gizmo 鎖成只能平移** | 對**點**成立，對**面**完全不成立。梯形、收尖、擠出後轉方向全被那一行鎖死 |
| 「拉面預設沿法向」 | 寫了一個**不存在的開關**（`pushFace()` 沒有任何介面呼叫） |
| 「一個面」＝ 三角形 | 是**共面區域**（方塊是 12 個三角形，使用者看到 6 個面） |
| 「圓柱側面有四邊形」 | 只有 `tube` 與 `roundBox` 有 |
| `buildPrim({type:...})` | 簽名是 `buildPrim(type, params)` |

而**「變換 ＝ 種類 × 方向 × 中心」這三個正交概念**是查了才知道的 ——
**推論給得出一個能動的東西，成熟做法給得出一個對的結構。**

---

## 2. 🔴 三條界線（動手前必讀）

### 一、Blender 與它的擴充都是 GPL → **讀邏輯，不抄程式碼**

- ✅ **構想與架構不受著作權保護**。讀文件、讀原始碼、理解它**為什麼**那樣設計，
  然後用我們自己的方式重寫 —— 完全合法。
- ⛔ **不可以把 Blender 或它的擴充的原始碼搬進本專案**。GPL 有傳染性，
  搬進來整個建模器就必須以 GPL 釋出。
- 這條也適用於 Blender 內建的 `io_export_paper_model` 與 PolyZamboni（都是 GPL）。

🔴 **落實到文件層面的硬規則（2026-08-23 加）**：

> **這四份文件裡不出現任何一行外部原始碼。**
> 只寫三種東西：①它的資料結構長什麼樣（用我們的詞描述）
> ②它的處理順序與判斷條件（散文，或**我們自己寫的**偽碼）③為什麼那樣設計。
>
> 兩個理由都成立：**GPL 有傳染性**（貼了片段，日後有人照抄進 `js/`，
> 整個建模器就得以 GPL 釋出）；以及**語法本來就對我們沒用**
> （我們是 JS ＋ 半邊網格，Blender 是 C++ ＋ BMesh，抄了也接不上）。
> **在文件端就斷掉，比事後靠紀律安全。**

### 二、Pepakura 是商業軟體 → **只讀公開文件與行為**

Pepakura Designer（Tama Software）**不開源**。所以：

- ✅ 手冊、功能說明、操作流程 —— 讀得到，可以學「它決定做什麼、不做什麼」
- ❌ 演算法（重疊偵測、自動分片怎麼解）—— 沒公開，**學不到，也不要猜**

**演算法層次要去別的地方拿**：兩個開源展開器（見 `外部參考-展開與分片.md`）＋ 公開論文。

### 三、學邏輯，**不學範圍**

日誌「目標與範圍」自己定的判準：

> 專攻建模與展開，**不做 Blender 那種全能軟體**。
> Blender 做不好、但公司天天要用的事才做。

**每一項調查結果都要過這道篩子：**

> **「這個對板材切割／展開／剖面分切這條路，有沒有用？」**

有才拿。雕塑、動畫、算繪、紙模的黏合片（我們是銑 V 溝或切開再接）——一律不碰。

---

## 3. 三個來源的分工（別搞混誰能回答什麼）

| 來源 | 授權／可讀深度 | 只有它給得了的東西 |
|---|---|---|
| **Blender 本體** | GPL，**原始碼讀得到** | 編輯的**結構**：BMesh、變換三層分解、選取與 active、modal 狀態機、拓撲原子操作、Undo |
| **Export Paper Model**（Blender 內建）／**PolyZamboni**（擴充） | GPL，**原始碼讀得到** | 展開的**演算法**：接縫優先序、自動分刀、重疊處理、排版 |
| **Pepakura Designer** | **商業，不開源** | **範圍與介面**：一個只做展開的商業軟體，二十年決定做什麼、不做什麼 |
| 公開論文 | 公開 | 近似展開那一路（⚠ 只做到「知道有哪些」，未讀原文） |

⚠ **官方線上手冊（docs.blender.org）用抓網頁的方式取回來幾乎全是側欄目錄，正文抓不到。**
所以研究線一完全走原始碼 —— 反而更好，**原始碼註解本身就在講「為什麼」，還附 bug 編號與實測數字**。
⚠ **GitHub 的目錄列表 API 被擋，raw 檔案本身沒問題** → 要逐檔取，**路徑猜錯會回空白、不會報錯**。

---

## 4. 🔴 三線總結：結論與建議優先序

> ⚠ **這是建議，不是決定。** 排出來給 kang 拍板，
> **確認之後才進 `PROJECT_LOG.md` 的「待辦」與 `規格\`。**
> 每一項後面括號是出處，去那一份看完整的五段推導。

### 第 0 組　🔴 現在正在給錯答案的（跟功能無關，先修）

| # | 是什麼 | 為什麼排最前面 |
|---|---|---|
| 0-1 | **重疊偵測在 `polys > 3000` 時直接回 `false`，而且不說**（展開#3） | **大模型現在顯示的是「沒問題」**。這是**沉默的漏報**，正是鐵律三「誤報比漏報更糟」的同一家族，而且比誤報更糟——使用者不會知道要懷疑 |
| 0-2 | **展開的重疊警告寫著「第 7 期會做自動分片」，而第 7 期已經取消**（展開#1.4） | 鐵律六「不要寫一個不存在的退路」，只是這次寫在**使用者看得到的訊息**裡 |

### 第 1 組　地基（做了之後，後面很多項變便宜）

| # | 是什麼 | 出處 | 為什麼是地基 |
|---|---|---|---|
| 1-1 | **變換三層分解**：方向加「**法向**」、中心加「**選取元素本身**」；連帶解掉 `_attachEditProxy()` 寫死 translate 那一行 | 編輯§3 | 「擠出好像沒用」「斜面推不動」「拉不出梯形」**根源都是這一個**。而 three.js 的 `TransformControls` 本來就有 `space`，我們只要算得出那個 3×3 |
| 1-2 | **記下初始座標，每幀從初始值重算**（取代現在的增量累加） | 編輯§5 | 價值不是手感是**正確性**。改完之後：取消、鎖軸、換方向、即時預覽**全部變成免費的**。也是 Pepakura 那個「貼合即時預覽」的前提 |
| 1-3 | **子元素多選 ＋ 順序即 active ＋ 網格重建時搬移選取** | 編輯§4 | 物件層**已經有現成寫法可抄**（`ids` 有順序、`active` 取最後一個）。⚠ 三件事必須一起做——多選之後「網格換人就全清掉」不可行 |
| 1-4 | **抽一支帶「停止條件」的走訪函式**（＝ Blender 的 walker ＋ delimiter） | 編輯§2 | 同一個泛洪迴圈我們**已經寫了四次**（`planarRegions`／`componentCount`／`splitPatches`／`boundaryLoops`）。而且環切要用的「找出一圈邊」也靠它 |
| 1-5 | **`preflightRebuild()`：拆掉重建前先預檢**（孤點／重複面／零面積面／非流形） | 編輯§6、展開#11 | 我們唯一改拓撲的路徑就是拆掉重建，而**現在完全沒有檢查**。`extrudeFace` 已經吃過虧（「孤點不會報錯，只會讓尤拉數算錯」）。兩個開源展開器都是「**先算預覽、全部檢查通過才動資料**」 |

### 第 2 組　便宜且直接有用（各自獨立，隨時可插隊）

| # | 是什麼 | 出處 | 備註 |
|---|---|---|---|
| 2-1 | **把既有的體檢接到展開入口**：零長邊／零面積面／不平的 n 邊形／繞向翻轉 | 展開#4 | ⭐ **零件全都在** —— `edit.js` 的 `facePlanarity()`／`degenerateFaces()`／`nonPlanarFaces()` 已經寫好而且測得到，**只是沒有人在展開前叫它們**。學它們的另一半：**訊息要講出「有幾個、該怎麼修」，並且把問題元素標出來** |
| 2-2 | **重疊回饋改成分級**（攤不平／會重疊／乾淨），不要一個布林值 | 展開#2 | 我們現在把「**根本做不出來**」和「**做得出來但圖沒法用**」講成同一件事 |
| 2-3 | **編號上下顛倒會誤讀就加句點**（`69NZMWpbqd`／`80oOxXIl`） | 展開#14 | 十行的事。**下料後的板子會被翻來翻去** |
| 2-4 | **接合編號旁邊加箭頭**，指出配對在哪一片 | Pepakura#2 | 片一多，光有數字要在整張圖上找配對很慢 |
| 2-5 | **`box` 改成手工建的 6 個四邊形**（不要借 `THREE.BoxGeometry`） | 編輯§2 | `mesh.js` 本來就撐得住 n 邊形（`roundBox` 有 20 邊形面），只有 `box` 被 three.js 逼成 12 個三角形。`planarRegions()` 每次重跑泛洪，修的是**建構時就可以不要發生的問題** |
| 2-6 | **隱含切割線要在圖上跟「指定分片」區分開** | 展開#6 | 我們的生成樹**替使用者加了接縫**，而圖上分不出哪幾條是他標的。⚠ 這一項只做「講清楚」，**不改行為** |
| 2-7 | **Undo 一起還原「當時的模式與過濾器」** | 編輯§7 | Blender 每步 undo 都存 selectmode，理由是不能讓 undo 之後顯示的狀態跟他剛才在編輯的不一樣 |
| 2-8 | **貼合（mate）加即時方向預覽** | Pepakura#3 | 現在點完第二個直接貼上去，**貼歪了才知道**。做完 1-2 之後幾乎是順便 |

### 第 3 組　⚠ 要先問 kang 才寫得出規格（**不要自己推**）

| # | 要問的問題 | 為什麼問不出來就不能動 |
|---|---|---|
| 3-1 | 🔴 **「你決定分片位置時，實際上是照什麼判斷的？」** | 兩個開源展開器都有自動分刀，但**約束全部綁著紙模**（面數上限、黏合片、繞軸成圈），對板材一條都不成立。鐵律一：**目的藏在下游** |
| 3-2 | **接縫優先序要不要加「邊長」與「凹凸」權重？** | Export Paper Model 是「長邊優先合併＝優先切在短邊」（接縫總長短、對位誤差少）＋「凹邊懲罰是凸邊兩倍」。前者聽起來對我們也成立，**但會改變現有圖的接縫位置** → 要先給 kang 看對照 |
| 3-3 | **要不要「組裝順序編號」？** | kang 原話有「**依序**讓 CNC 切割後再組裝結合」——「依序」那兩個字可能就是這件事 |
| 3-4 | **外框偏移／刀徑補償該不該在建模器做？** | ⚠ 很可能跟排版一樣，答案是「Illustrator 那一關已經做了」（坑第 23 條同一個形狀） |
| 3-5 | **`pushFace()` 到底要不要接介面？** | 已在待辦。判準是「有沒有真的遇到要推**斜面**」。⚠ 若 1-1 做了，**這一項自動消失**——它會變成「方向＝法向」的一個組合 |

### 第 4 組　✘ 明確不做（**寫下來，免得下一個人重提**）

| 不做什麼 | 理由 |
|---|---|
| **匯入 STL／OBJ 當主力入口** | 那是 Pepakura 的入口。做成一樣的＝做半個 Pepakura，還少了它二十年的圖紙生態。kang 2026-08-23 當場否決 |
| **黏合片（glue flap）** | 我們是銑 45° V 溝或切開再接。過篩子第一個被刷掉的 |
| **展開圖的排料／nesting** | kang 的 DXF **先進 Illustrator 做整體處理**（與「板材尺寸上限檢查」同一個理由，坑第 23 條） |
| **2D 版面編輯、材質貼圖、點陣匯出** | 同上，或紙模專屬 |
| **non-manifold 容忍度 ／ 整套 Euler operators** | 布林走 Manifold 本來就保證二流形；我們的編輯是拆掉重建，沒有中間態。半邊手術「接線錯了不會報錯」 |
| **選取的 flush（往上灌）** | 我們的過濾器互斥（平板只能按按鈕），沒有「三種模式同時開」的需求 |
| **Gimbal／View／Cursor／Parent／Custom 方向；外框中心／3D 游標中心** | 對板材切割沒用 |
| **十幾種滑鼠輸入模式、軌跡球、virtual cursor** | 我們只有移動與少數旋轉縮放 |
| **把 Undo 改成增量式** | 🔴 **Blender 編輯模式也是完整快照。** 增量式會讓每個工具都要寫反向操作——那是 bug 的溫床。**這一條的收穫是「不用改」** |
| **改用相對容許值** | 我們的絕對值（`0.01cm`）是**刻意的**且理由更強：切的是實體板材，0.1mm 是物理尺度（坑 25／26） |
| **近似展開（Mitani-Suzuki 那一路）** | 前提是「可以改變形狀去換可展性」。我們需要精確的走剖面分切，不精確的本來就不做 |

### 第 5 組　🔴 動工前必須再查一輪的（**現在的答案不夠**）

| # | 什麼 | 缺什麼 |
|---|---|---|
| 5-1 | 🔴 **導任意一條邊的圓角（Bevel）** | Blender 的 edge bevel 底層**需要「兩邊形的面」當中間步驟**，而我們的 `fromFaceList` **直接跳過少於 3 個點的面**。→ **Blender 那條路我們走不了，必須另外想一條。** 這是第 6 期剩下四個工具裡最大的未知 |
| 5-2 | **環切（Loop Cut）** | 難的不是切，是「找出一圈邊」。要先有 1-4 那支走訪函式 |
| 5-3 | **Pepakura 到底有沒有板厚補償** | 只查到「大量用在 5～10mm EVA 發泡板」，**沒查到有厚度補償的證據**。⚠ **「沒查到」不等於「沒有」**，它不開源，我驗不了。要據此做決定前**請 kang 或有裝的人實際確認** |
| 5-4 | **Kerfing（在硬板材上開槽讓它彎）** | 2024 年有論文。⭐ 這跟珍珠板／木板**直接相關** —— 是「捲不起來的材料」的**第三條路**（既不是折、也不是切開再接）。目前只知道有這個方向 |

---

## 5. 調查結果要怎麼記

每一項寫成五段，缺一不可：

```
Blender／該工具怎麼做
  → 為什麼那樣設計（這一段最重要，也最容易被跳過）
  → 我們現在怎麼做
  → 差在哪
  → 該不該學（過篩子的結論 ＋ 理由）
```

**寫進三份專屬文件的對應章節**，不是寫進 `PROJECT_LOG.md`，也不是寫回本檔。
結論確定要做之後，才進待辦與 `規格\`。

🔴 **「我們現在怎麼做」那一段必須來自實際讀程式，不可以照日誌抄。**
日誌會過期，程式不會。2026-08-23 那五次推論失誤全是這個病
（`pushFace()` 那個不存在的開關就是這樣寫出來的）。

⛔ **沒確認過的調查不進文件** —— 免得又長出一份會讓下一個人答錯的東西。
查不到就**明寫「查不到」**，不要留白讓人以為查過了（見 5-3）。

---

## 6. 相關文件

- `PROJECT_LOG.md` —— 目前確定的狀態、鐵律、待辦
- `外部參考-Blender編輯.md` —— 研究線一（動編輯前讀完整）
- `外部參考-展開與分片.md` —— 研究線二（動展開或分片前讀完整）
- `外部參考-Pepakura.md` —— 範圍與介面
- `編輯功能設計方向.md` —— 第 6 期的**定位、範圍、被否決的方向**（動編輯前必讀）
- `規格\` —— 一功能一檔的完整規格（先 `Glob 規格\*`）
