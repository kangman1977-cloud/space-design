比例編輯：拉一個點，周圍照影響半徑跟著平滑地動（三顆裡的第一顆）

動到 7 個檔：
  M  建模器/js/core/edit.js      propFalloff / proportionalVerts / 權重
  M  建模器/js/view/select.js    propRadius 狀態、拖曳收範圍內的點、上色權重
  M  建模器/js/view/scene.js     setVertexDots 逐點上色
  M  建模器/index.html           gEditXf 末尾加「影響半徑」欄
  M  建模器/js/main.js           欄位接線＋選取時講「會動到幾個點」
  M  建模器/tests/run.mjs        +26 項（2667 → 2693）
  A  規格/建模器-比例編輯.md      新檔
  M  STATUS.md                   待實測＋真待辦
  M  版控工具/commit-msg.txt

════════════════════════════════════════════════
【動手前檢查表】

【這次要做什麼】
  用「拉點線面」拉的時候，影響半徑內的點跟著動，越遠動得越少。
  ⛔ 不是新的一顆工具 —— 左側直條沒有多一顆。

【依賴哪些事實】
  1. 拖曳整條路只在一個檔四處    【已查證 · select.js:3355/3412/3441/3529 改前行號】
  2. 套變換的核心是一行          【已查證 · edit.js:5025 改前行號】
  3. 這條路有現成測試，改簽名會被擋【已查證 · run.mjs:4828 起，7 處呼叫】
  4. 顯示點是整批一個 Points，寫死白色【已查證 · scene.js:1169-1188 改前行號】
  5. 參數物件進不了編輯模式      【已查證 · select.js:1776 改前行號】

【會動到哪幾個檔】見上面那張表

【怎麼知道做對了】見底下「kang 要實測的」

════════════════════════════════════════════════
【🔴 三題的拍板（kang 2026-09-02）】

  | 題 | 答案 | 一句話理由 |
  |---|---|---|
  | 一、半徑怎麼給 | 乙：上方那排一個數字欄 | 🔴 平板沒有滾輪 |
  | 二、越遠少動多少 | 甲：只做「平滑」一條 | 唯一「點越密越好」的 |
  | 三、看不看得到 | 乙：受影響的點變色 | 畫的就是真的會動的那些點 |

  → 完整推導與量測 → `規格\建模器-比例編輯.md`

════════════════════════════════════════════════
【🔴🔴 這一輪最該記的一件：我被自己的量測騙過去】

  第一版只量了球，量「那一點周圍」的折角 → 「球」曲線 14.8° 大勝
  Blender 預設的「平滑」27.0°，我就照這樣建議 kang。

  ⇒ kang 問「**這個功能只針對弧形還是只有球…一個面有很多點是不是也適用?**」
    去量平板才照出來：**那張表只量了中心那一點**。

  🔴 而同一頁我才剛用「尖角被搬到範圍邊緣去了」刷掉「固定」那一條 ——
     ⛔ 同一個病，我沒有拿去檢查「球」。

  分開量中心／邊緣，再換三種格子密度（平板 100×100、拉 10、半徑 30）：

      平滑   24.9/20.1 → 15.8/15.5 → 8.8/9.4    （點越密，兩端一起變好）
      球      8.9/36.7 →  4.4/47.9 → 2.3/68.9   （中心贏，邊緣爆，越密越糟）

  ⭐ smoothstep 兩端導數都是 0；球在 d=R 的導數是無限大。
  ⇒ **量一個「越小越好」的指標時，要先問「它會不會只是搬家」。**

════════════════════════════════════════════════
【⚠ 量過才決定的兩件】

  ① **直線距離，⛔ 不是沿表面繞過去的**
     管子 外r25/內r20（壁厚 5），半徑一超過壁厚 → 背面一半的點跟著動。
     ⭐ 而那是對的：有厚度的殼，正面凸背面就該跟著凸，否則殼被拉薄。
     改用表面距離 → 背面要繞過帽緣 → 完全不動 → 壁厚被破壞。

  ② **順不順看的是坡度（拉多遠 ÷ 半徑），⛔ 不是半徑本身**
     半徑固定 30：拉 2 → 12.5°／拉 5 → 28.9°／拉 10 → 47.9°

════════════════════════════════════════════════
【四道機械檢查】

  ✅ node 建模器/tests/run.mjs                  2693 通過 0 失敗（+26）
  ✅ node 文件工具/兩端檢查.js                   70 個元件，沒有兩端打架
  ✅ 模組圖真的載入                              解析通過
  ✅ node 文件工具/目次產生器.js --all           全部最新

════════════════════════════════════════════════
【🔴 kang 要實測的（電腦＋平板都要）】

  前置：新增一個「球」（預設 r30、32×16），按「轉成網格」，
       開「拉點線面」，確認「顯示點」是開的。

  1. 🔴 **保命線**：影響半徑留 0，選一個點往上拉
     → 跟以前**完全一樣**，戳出一個尖角。⛔ 這條不過就是既有行為被改壞了。
  2. 影響半徑填 15 → 提示會講「會動到 N 個點」，
     → **畫面上那 N 個點由白轉橘**，越靠近被拉的那點越橘。
     ⚠ 兩個數字要對得起來（橘點數 ＝ 提示講的 N）。
  3. 拉同一個點 → **中間高、旁邊漸漸低**，⛔ 不再是尖角。
  4. 半徑改 30 再拉一次 → 隆起**更寬更緩**；範圍外的點**紋風不動**。
  5. 拉到一半按 Esc → **整片回到原狀**（⛔ 不是只有中間那點回去）。
  6. 拉完在「數值」欄打一個精確數字按 Enter → 整片照那個數字重來。
  7. 選一整圈邊（`選一圈`）再拉 → 影響範圍是那一圈各自往外長的聯集。
  8. 🔴 **平板**：半徑欄位點得到、打得進去、鍵盤收掉之後值有留住。
  9. 欄位打 -5 或亂字元 → **當場變回 0** 並講「只動選到的那些點」，
     ⛔ 不是沉默地吃掉。

  ⚠ 前置參數照 `js/build/prim.js` 的 `PRIM_DEFAULTS.sphere` 抄（r30、32×16）。

════════════════════════════════════════════════
【🔴 `STATUS.md` 行數】

  上一輪 294 → 這一輪（真待辦那段改寫 ＋ 待實測那節填回來）。
  ⛔ 沒有自己動手砍。
