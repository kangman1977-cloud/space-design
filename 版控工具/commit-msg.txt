修：`擠出邊` 上一版是【死的】—— 板子外圈根本點不到。加一顆「含外緣」開關＋摺線標起來

動到 4 個檔：index.html、js/core/edit.js、js/main.js、js/view/select.js

════════════════════════════════════════════════
🔴🔴 【kang 一按就發現：功能是死的】

  他的原話：「如果是面做邊擠出....我驗收後沒辦法...因為面沒有邊...
             如果我是選立方體的邊做邊擠出...也會被擋」

  **權威原因（⛔ 不是推論，查到那一行）**：
  `js/unfold/seam.js` `isMarkable()` **第一行**
      `if (!he.twin || !he.face || !he.twin.face) return false;   // 邊界`
  而畫面上點得到哪些邊走的正是它（`select.js` 用 `markableEdges(mesh)`）。
  ⇒ **平板外圈一條都選不到；方塊的邊全是內部的邊 → 被我擋掉。**
  ⇒ 🔴 **⛔ 沒有任何一條邊按得到。**

  🔴🔴 **這是我的錯，而且是重犯**：動手前檢查表填了 5 條事實，
  **⛔ 沒有一條問「使用者選得到那種邊嗎」** ——
  而鐵律五明文寫著「開清單前問【使用者驗得出這個結果嗎】」。
  ⚠ **沙箱測得過，是因為我直接餵半邊給函式，⛔ 沒有走使用者那條路。**

════════════════════════════════════════════════
【修法：照日誌自己寫下的處方，⛔ 不是我發明的】

  `edit.js` 補洞那支的註解逐字寫著：
  〔要做成精準版就得在 `select.js` **另外加一支**「找最近的邊界邊」，
  　**不能改 `isMarkable` —— 那支被分片與貼合共用**〕

  · `select.js` 加 `includeBoundary` ＋ `_pickableEdges()` / `_nearestPickableEdge()`
    🔴 **預設 false → ⛔ 不開的話現有行為一格都沒變**
  · `index.html` / `main.js` 加一顆 **`含外緣`**（過濾器旁邊，⛔ 不佔左側直條，
    只在「編輯模式＋過濾器吃得到邊」時出現）

  🔴 **kang 知情之後選了「開關」那一案**，而理由⛔ 不是它比較安全：
     **是它讓「哪裡會出事」變得可預期** ——
     另一案（切到「邊」就直接點得到）會連**框選**都把外圈掃進去，
     而**使用者完全不會察覺**。
     ⚠ 而這個專案今天早上才付過那個代價
     （「放寬一個判定範圍，把原本靠打不中相安無事的兩條路撞在一起」）。

════════════════════════════════════════════════
🔴 【「先量再做」當場又擋下兩個 bug】

  ① **摺線⛔ 沒有標 `hard`** —— 裙邊是**先長成跟板子平的**（先長一格再折），
     ⇒ 那條摺線是「共面的邊」⇒ **畫不出來、點不到、按壓平被併掉**
     （沙箱實測：面 2 → **1**）。**⚠ 使用者會看不到要摺哪裡。**
     ⭐ 日誌逐字警告過：「⛔ **日後任何『加共面的線』的功能都要記得標 `hard`**」。
     ✅ 修完：hard **2** 條、**兩條都點得到**、壓平面 3 → **3**（⛔ 沒被吃掉）。
  ② **既有的邊標記⛔ 沒搬** —— 整個重建卻沒帶 `marksOf()`／`applyMarks()`，
     分片線那些會**安靜消失**〔那條規則被違反兩次、兩次都出事〕。✅ 已補。

【怎麼知道做對了】

  ✅ 四道機械檢查全過（測試 **2610**、兩端 ✅、模組圖 OK、目次最新）
  🔴 **我答應的那件事做了**：把外圈的邊**一支一支餵給吃邊的 8 支功能**——
     沿面滑動／平行複製／導角／分離 **4 支都擋下來，而且訊息講得出原因**
     （導角：「外輪廓的邊不能導角（它只有一側有面）」）；
     面上加線／邊上加點 **2 支照做，而那是對的**；多點連接⛔ 不吃邊。
     ⇒ **⛔ 沒有一支會做出怪東西。**
     ⚠ 過程中我自己還呼叫錯一次，差點冤枉「面上加線」—— 訂正後才是上面這個結果。

⚠ **這一版我還沒在線上驗過。**
