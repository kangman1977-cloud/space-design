量測第 2 步：把尺寸畫到 3D 畫面上（工具列多一個「標尺寸」下拉）

kang 2026-08-27 從三個候選裡挑了這一件。動到 7 個檔：
建模器/js/core/measure.js、js/view/scene.js、js/view/select.js、
js/main.js、index.html、tests/run.mjs、規格\建模器-量測.md
（＋ PROJECT_LOG.md、HISTORY.md）。測試 2112 → 2140 項，失敗 0。

【做了什麼】

  工具列「編輯」那組，「顯示點」右邊多一個【標尺寸】下拉，三段：

    總計（預設）　選 32 條邊 → 1 個字在重心：「32 條邊／總長 156.83 cm」
    每一個　　　　選 32 條邊 → 32 個字，每條邊中間各一個：「4.90 cm」
    關　　　　　　一個字都沒有

  邊寫長度、面寫面積（cm²）、點寫座標 —— 三種都是 kang 當場選的。
  右邊面板那一行不受這個下拉影響，兩邊永遠都在。

【🔴 三段可切換是 kang 拍板的，⛔ 不是開關】

  他的原話：「是不是有一個選項可以變換標示..不然一起出現可能會很擠」。
  ⭐ 我原本要自己挑一種寫死（列了三個選項請他選），他給了第四個答案。
  預設「總計」也是他選的 —— 永遠只有一個字，不可能擠。

【⭐ 一行新的數學都沒有 —— 三樣東西全是現成的】

  數字　→ measureSelection()（跟右邊面板【同一個來源】，⛔ 不另算一份）
  畫字　→ select.js _initAxisLabels() 那套 canvas→CanvasTexture→Sprite
  　　　　（gizmo 的 X／Y／Z 三個字就是它，kang 已經實測過）
  重畫　→ _drawEditMark()（十六個寫入點共同的出海口，⛔ 不新開同步鏈）

  ⛔ 不畫在螢幕座標上：index.html 的 #knifeLine 墓碑註解白紙黑字寫著
  「刀具第一版失敗的原因正是畫在螢幕上，一轉視角就對不上」。
  量測的字指的也是模型上的位置 —— ⛔ 不把同一個坑再挖一次。

【🔴 查出一條 ⛔ 不經過 _drawEditMark() 的路】

  clearEditSel() 直接呼叫 view.clearPickMarks() 就回去了。
  ⛔ 沒補的話症狀是【選取已經沒了、數字還賴在畫面上】。
  ⭐ 那裡呼叫的是 refreshMeasureLabels()（讀當下狀態自己算），
  　 ⛔ 不是 clearMeasureLabels() —— ⛔ 不在第二個地方抄「什麼時候該清」。

【機械斷言】

  ★★★ node tests/run.mjs → 通過 2140 失敗 0（新增 28 項）
  ★★  每一條各自的長度加起來 ＝ 總計那個字的 210（兩個數字互相驗）
  ★★  同一個面選兩次 → 只標 1 個字（⛔ 不會兩個字疊在一起）
  ★★  超過上限 → 一個字都不畫、但講得出實際幾個、tooMany 為 true
  ★★  放大 2 倍 → 兩種模式的字都跟著 2 倍（一律世界座標）
  ★   六個面的字各站各的位置（Set 大小 ＝ 6）
  ★   node --check 五個檔全過；新改的三支 0 個外部連線
  ★   HTML id measureMode ↔ main.js 接得上；三個 option 值都對得上程式
  ★   九個新函式的定義與呼叫端逐一對照，全部接得上
  ★   五個常改的文字檔檔尾都是 0a

  ⚠ 「被呼叫但不存在的函式」那道檢查【誤報一個 isMarkable】——
     它只找 ^function f，而 isMarkable 是從 seam.js import 進來的
     （main.js:27）。⛔ 這不是這一輪改壞的，已寫進日誌檔頭那一欄。
     ⛔ 這一輪沒有動那道檢查（它寫在日誌裡，要改得先讓 kang 知道）。

【⚠ 兩個失敗是我的測試寫錯，⛔ 不是程式】

  第一次跑失敗 2 項，都是拿 ring[0] 當「長度 60 的那條邊」——
  那一圈裡兩條 60、兩條 45，第一條是哪一種【沒有保證】（得到 90＝45×2）。
  改成指名 edgeOf(60)。理由已寫進測試的註解裡。

【沙箱驗不了的部分（⏳ 待 kang 實測，桌機七項）】

  字在畫面上長什麼樣、拉遠拉近會不會一樣大、繞到背面看不看得到、
  上限擋下來時橫條講得夠不夠清楚 —— 沙箱一項都驗不了（沒有畫面）。
  🔴 清單在 PROJECT_LOG.md「待辦」那一節（Grep：實測清單：標尺寸）,
  ⛔ 不可以只寫在這裡（坑第 34 條，這條規則已經被違反兩次）。

  🔴 第 4 項（拉遠拉近轉視角）與第 7 項（球全選邊撞上限）最重要：
     前者正是刀具第一版死掉的地方；後者驗上限有沒有安靜地把畫面弄糊。
     ⚠ 200 這個上限是猜的、沒有量過，平板實測再照真實結果調。

  ⏳ 平板版面仍然沒驗 —— 工具列這一輪又多了一格。

【文件】

  規格\建模器-量測.md　第 2 步整節寫進去（介面、三段、各自寫什麼、
  　　　　　　　　　　　⛔ 為什麼不畫在螢幕上、上限、掛在哪、每幀重算）
  　　　　　　　　　　　＋ 識別碼補七個 ＋「去讀哪一支」補三列
  PROJECT_LOG.md　　　 待辦那條標 ⏳ 待實測 ＋ 七項清單；開場「待實測」
  　　　　　　　　　　　改成兩批；三個候選那張表把「量測第 2 步」劃掉；
  　　　　　　　　　　　檔頭覆蓋一條（isMarkable 誤報）。V12.8 → V12.9
  HISTORY.md　　　　　 被擠出檔頭的「mesh.issues 說非流形」逐字搬入
  　　　　　　　　　　　（append 9 行 ＋ 索引 3 行，⛔ 沒有改動任何舊內容）
