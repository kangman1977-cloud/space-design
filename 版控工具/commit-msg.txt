刀具三件結案（kang 驗收 OK）：拖切點就移動 ＋ 吸到角 ＋ 修閉合會多出切點

⛔ 程式一行都沒改。動到 2 個檔：STATUS.md、HISTORY.md

════════════════════════════════════════════════
【這次要做什麼】

  ✅ **kang：「我也驗收OK」**，並另外逐項確認了兩項最容易假結案的：
     · 第 16／17 項：`吸中點` 開著（或按著 Shift）時 **⛔ 不吸角**
     · 第 18 項：相鄰兩點快點兩下 **⛔ 不會多出一個切點**

  → 照日常流程第 6 步結案。

【動到哪裡】

  · `HISTORY.md` 新增第 51 節，**兩塊清單合成一節**
    （同一天、同一個功能的連續兩輪；第二輪的兩件都是他驗收第一輪時當場提的）
    🔴 兩份原清單各自**逐字搬、一字未改**，機械斷言分別寫在小節標題裡：
       清單一 行數 34、md5 85efda36a961652f11d92414c5bd929d
       清單二 行數 23、md5 42da892a66b53e62dc9f893642908268
  · `STATUS.md` 兩塊都移除 → 「⏳ 待實測」清空
  · ⛔ 不動「下一件」—— 這一輪做的兩件都是他臨時提的，本來就不在那兩個候選裡

【🔴🔴 這一輪留下來的三條（權威版寫在第 51 節）】

  一、**⛔ 不要為了新手勢開新模式 —— 先問「現有的手勢用完了嗎」。**
     kang 本來以為移動會跟刪除撞；⭐ 它們⛔ 不會撞：
     **刪除是「點」、移動是「拖」**，而 `TAP_MOVE` 那道界線早就存在。

  二、🔴 **使用者明講的東西，⛔ 不可以被程式「順手改好」。**
     `吸中點` 開著要不要吸角 —— **AI 說「角贏」（理由是美學），
     kang 說「⛔ 不吸角」（理由是規則）**：
     > `吸中點` 是使用者明講「我現在要中點」，⛔ 不是建議。
     ⭐ 而且兩者⛔ 永遠不在同一個位置打架 —— **只有規則要定，⛔ 沒有距離要比**。

  三、🔴 **試失敗的那條路，一定要把自己收乾淨。**
     `knifeCloseLoop()` 先 push 副本才去切，`knifeApply()` 失敗直接 return
     → 副本留下、而且⛔ 沒叫 `updateBar()` → **寫 2 個、實際 3 個**。
     ⚠ **⛔ 不是那一輪造成的**，2026-08-26 就在 —— 閉合失敗本來就少見。

【⚠ 「吸到角」同一天先撤回後做成，⛔ 兩件事不要混】

  撤回的做法：把落點比例設成 0／1 ＝ **插一個新點** → 長度 0 的邊 → 每次都切不下去
  這一次的做法：**回一個既有頂點的索引**，⛔ 完全不插點
  🔴 **測試釘的判準⛔ 不是「有沒有切成功」，是「頂點多了幾個」**
     —— 重用多 1 個、插新點多 2 個，**兩種都切得下去**。

【怎麼知道做對了】

  ✅ 目次產生器 --all → HISTORY **51** 節／6228 行，自我否決通過：正文逐字未變
  ✅ **`STATUS.md` 259 行** —— 🔴 316 → 259，**警報線的超額自己解決了**
     （⛔ 沒有動任何穩定的區塊）
  ⛔ 測試／兩端／模組圖不用跑 —— ⛔ 沒動程式

⚠ **搬移時踩到一個自己的錯，當場修掉**：第一次的 `sed` 範圍多抓了一行空白，
  斷言擋下來了 —— 🔴 **那正是「搬移一律逐字搬 ＋ 機械斷言」在做的事**。
