旋轉成形（Spin／車床）第 1 階段 —— 一條輪廓繞軸轉一圈，⏳ 待 kang 實測

kang 從對照表挑的（他自己正在想這件事）。動到 7 個檔：
  ?? 建模器/js/build/revolve.js（**全新檔**，幾何）
  ?? 規格/建模器-旋轉成形.md（**全新檔**）
  M  建模器/index.html         　 按鈕 ＋ 7 個控制項
  M  建模器/js/main.js          　接線、兩個輪廓入口、標籤與範圍提示
  M  建模器/tests/run.mjs       　新 section（2639 → 2659）
  M  STATUS.md                  　⏳ 待實測十項
  M  外部參考-Blender編輯.md    　`Spin` 那一列 ⭐ → ✅
  M  版控工具/commit-msg.txt

🔴🔴 **`STATUS.md` 撞到警報線了：286 行（上限 276）** —— 報告在最下面。

════════════════════════════════════════════════
【這次要做什麼】

  **一條輪廓繞一根軸轉一圈** → 碗、罩、瓶、燈罩、圓頂、輪圈、環。
  🔴 **這一整類在這之前⛔ 建不出來**：`擠出` 只會沿直線長、⛔ 不會轉。

  ⭐ **它跟 `螺旋` 是同一個地基** —— 螺旋 ＝ 這一支 ＋ 一個
  「每轉一圈往上走多少」的欄位，填 0 就是旋轉成形。
  ⛔ **不要為螺旋另寫一支**〔kang 看過互動圖確認：「原來如此」〕。

════════════════════════════════════════════════
【依賴哪些事實】⛔ 每條都填了出處

  1. 側牆繞向⛔ 不可以照抄 `extrudeFace()`
     【已查證 · 規格\建模器-點線面編輯.md「側牆的繞向」那一節（第一版 χ −62）】
  2. 建網格的骨架：points ＋ faces → fromFaceList
     【已查證 · edit.js `bridgeLoops()`、build/extrude.js `extrudeMany()`】
  3. 鋼筆畫的線躺在地板上，形狀的 (x,y) 對到世界的 (x,z)
     【已查證 · io.js `penPathToWorld()` 檔頭 ＋ build/extrude.js 檔頭】
  4. `不封口` 是整個物件一個旗標，⛔ 不是每條路徑各存一份
     【已查證 · build/prim.js `pen()` 那一段逐字寫著】
  5. 新物件的做法（烘好的網格 ＋ 原點置中）
     【已查證 · main.js `separateSelected()`；io.js:60「{type:'mesh'} 已烘好的網格」】
  6. 板件是開放的單層面，厚度由 shell() 在匯出時才加
     【已查證 · build/ribbon.js 檔頭「⛔ 這裡不可以自己把厚度做進網格」】
  7. `seg` 預設 32 照 PRIM_DEFAULTS.cylinder 抄，⛔ 不自己填
     【已查證 · build/prim.js:46】

════════════════════════════════════════════════
【🔴🔴 三個【量出來】才發現的東西，⛔ 沒有一個是推出來的】

  ① **繞向反了 → 整個模型法向朝內。**
     照直覺寫 `[a,b,c,d]` → 圓柱體積 **−136563.225**（應該是 ＋）。
     ⚠ **那個模型畫面上看起來完全正常**，只有匯出 STL 的列印前檢查才抓得到。
     ✅ 正解 `[d,c,b,a]`；**抓到它的是「體積是不是正的」那道斷言**。

  ② **鋼筆畫的路徑【預設就是封閉的】** —— 當成開放線處理會**少接一段**，
     而那個缺口畫面上很難看出來。
     🔴 而「把第一個點複製到最後」那個偷懶解法會產生**兩個重疊的頂點**
     ⇒ 非流形，**畫面上一模一樣**。
     ✅ 正解是 `revolve()` 自己偵測首尾同點，內部多接一段。

  ③ 🔴🔴 **開清單前問「使用者【產得出】那個輸入嗎」，當場問出一個半殘的功能。**
     要轉出**封閉的實體**，輪廓端點得**剛好**落在軸上（容許值 1 微米），
     ⚠ **而手畫的線幾乎不可能剛好** ⇒ ⛔ 不補的話 kang 永遠只做得出薄殼。
     ✅ 補法照 `變成正圓` 的半徑欄抄：**這一組剛出現時自動填輪廓第一個點的座標**，
     　 而且**打數字蓋得掉**。⚠ 只在「剛出現」那一瞬間填，⛔ 不是每次都填。
     ⭐ **這是鐵律五的第三次應驗**（前兩次：`擠出邊`、鋼筆最少 3 點）。

════════════════════════════════════════════════
【⚠ 動手前更正過的兩件事（⛔ 都在動工前就告訴 kang 了）】

  ① 拍板時的選項寫「軸跟 `切一刀` 一樣打一個座標」—— **錯的**。
     `切一刀` 定的是一個【平面】，一個數字就夠；**軸是一條【線】，要兩個**。
     ⇒ 改成兩個座標欄，**標籤跟著軸變**（繞 Z 顯示 X、Y）。
  ② 選項寫「兩端封起來 ＝ 直接就是封閉的實體」—— **只在輪廓端點碰到軸時成立**。
     ⇒ 改成**看輪廓自己決定**：碰到 → 實體（列印）；沒碰到 → 板件（展開切割）。

════════════════════════════════════════════════
【⛔ 這一輪沒有做的（⛔ 不要當成有）】

  · **只轉半圈 ＋ 兩端封口** —— kang 拍板要做，排**第 2 階段**
  · **`螺旋`**（每轉一圈往上走多少）—— 參數位置留著，⛔ 不是忘了
  · 轉出來是烘好的網格，**參數回不去**（⛔ 不能事後改格數）—— 跟 `分離` 同一個做法

════════════════════════════════════════════════
【怎麼知道做對了】⛔ 要能對答案的量

  ✅ 測試 **2639 → 2659**（+20），全過。守著的是：
     體積 ＝ 正 32 邊形柱（⛔ 不是 πr²h）而且是**正的**／圓錐 ＝ 柱的 1/3／
     χ ＝ 2／**頂點 ＝ 2 個極點 ＋ 32×2**（守「極點只放一個點」）／
     環的面數 ＝ 4×32 且**首尾那點只放一份**／smooth ＝ 2×32／
     **軸搬到 x＝100 體積一模一樣**（守「判準是離軸多遠，⛔ 不是座標正負」）
  ✅ 兩端檢查通過（8 個控制項的 `hidden` **兩端都設了**）
  ✅ 模組圖真的載入一次 —— 解析階段過
  ✅ 目次產生器 --all → 兩份都「已是最新」
  ⚠ **DXF／STL／畫面沙箱驗不了** —— 一定要 kang 實測，清單十項在 `STATUS.md`

════════════════════════════════════════════════
【🔴🔴 停下來報告：`STATUS.md` 286 行，超過 276】

  ⛔ **沒有自己動手砍**（規則：超過就停下來報告，等 kang 決定）。

  **病因是「⏳ 待實測」那 30 行的清單** —— 而那一節在**結案時就會整段搬走**，
  ⇒ **這一次的超線是會自己消失的**。

  可以移出的三塊（若 kang 要現在處理）：
  | 移什麼 | 省幾行 | 權威版在哪 |
  |---|---|---|
  | 甲 「穩定參考」裡**平板／實機**那則 | −8 | 它自己寫著「完整經過在 `版控工具\平板伺服器.py` 檔頭」|
  | 乙 「真待辦」裡**非等比縮放**那則 | −5 | 出處寫著 `io.js` 的 `penPathToWorld()` 檔頭 |
  | 丙 「動手前檢查表」 | −25 | ⛔ **不建議** —— 它是每次要填的表 |

  ⭐ **AI 的建議：這一次⛔ 不動它**，等實測結案時那 30 行自己會走。
