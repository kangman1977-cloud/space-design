回寫：參考線 第 1 階段 十二項全過結案（kang「267都有」）

⛔ 程式一行都沒改。動到 2 個檔：HISTORY.md（搬入）／ STATUS.md

🔴🔴 **這是「結案就搬、STATUS ⛔ 不留摘要」那條新規則第一次生效。**
   STATUS 從 284 → 262 行，而且⛔ 不是靠砍東西 —— 是摘要本來就該搬走。

════════════════════════════════════════════════
【① 結案】

  ✅ 參考線 第 1 階段：線本身 十二項全過
     · 左側直條第 9 顆 `參考線`（排在 `分片` 後面）
     · 控制列一組：X│Y│Z ＋ 下拉 ＋ 位置欄 ＋ 加一條／刪這條／全部清掉
     · 檢視一顆 `顯示參考線`（＝ 藏起來，⛔ 不是刪掉）
     · 線存在 doc.guides ⇒ 存檔與 Undo【免費拿到】
  ⭐ 三個視角【共用】—— 一條螢幕上的線在 3D 裡是一個平面（kang 拍板）

  分工：AI 在線上版驗掉九項（1/3/4/5/8/9/10/11/12），
        kang 驗那三項我驗不到的（2 平板高度／6 上視同一條線／7 側視好不好看）。

【② 🔴 這一輪真正值錢的，⛔ 不是功能，是那段卡住的路】

  症狀：平板走內網 IP —— 按鈕在、提示說「加了」「已經有了」，
        ⛔ 而畫面上一條線都沒有；線上版與電腦 localhost 都正常。

  病因：python -m http.server ⛔ 不送 Cache-Control ⇒ 瀏覽器自己推測
        「≈（現在 − 上次修改）× 10%」⇒ 天天改的 main.js 拿到新的、
        很久沒動的 scene.js 拿到【舊的，連問都不問伺服器】
        ⇒ 新的 main.js ＋ 舊的 scene.js
        ⇒ 按鈕/提示/資料全對，而 syncGuides() 在舊版【根本不存在】
        ⇒ 什麼都不畫，⛔ 而且不會報錯。

  🔴 三條留下來的（全文在 HISTORY.md）：
    一、「原始碼是對的」≠「那台裝置拿到的是這份原始碼」——
        按鈕有反應、畫面沒東西 → 先問【是不是同一份程式】，
        ⛔ 不要先查邏輯。最快的判準：換成線上版試一次。
        ⚠ 那一步花不到一分鐘，而我在它之前燒掉了一整輪。
    二、診斷版問對了問題，但【三個候選答案全在「畫圖」這一層】——
        我從頭到尾沒把「交付」也列成一個候選。
    三、修法要是機制⛔ 不是紀律 ⇒ 伺服器每次都說「不准存」
        （版控工具/平板伺服器.py，已由 kang 在平板實測確認）。

【怎麼知道做對了】

  ✅ 逐字搬 ＋ md5：23 行／450317a96f1a85be4cc872be3050c214，diff 全等
  ✅ 目次產生器 --all（HISTORY.md 已重產 40 節，自我否決通過）
  ✅ 測試 2534 全過／兩端檢查全過（這一輪⛔ 沒動程式，跑來確認沒誤傷）
  ✅ STATUS.md 262 行（警報線 276）

【⏳ 待實測】無

【下一件（已排定）】參考線 第 2 階段：吸附（`吸到參考線`）
  ⭐ kang 最初要的「提升物件對齊排列」到這一步才真的成立。
  要點：掛 objectChange（⛔ 不是 setTranslationSnap，那只吸整數倍）／
        用 align.js 的 worldBounds() 試 min／center／max（⛔ 不可以拿 obj.pos）／
        容許距離用【螢幕像素】。
  ⛔ 動工前讀完 規格/建模器-參考線.md。

  ⚠ 提醒：左側直條的空間【只剩不到一顆】（實測剩 27.5px、一顆要 50.3px）
     —— 第 2 階段只加一顆開關到「吸附」那一組，⛔ 不進直條。
