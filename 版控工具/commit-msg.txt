回寫：吸附十項全過結案（kang 驗收OK）

kang 2026-08-31：「驗收OK」。
⛔ 程式一行都沒改。動到 2 個檔：HISTORY.md（搬入）／ STATUS.md

════════════════════════════════════════════════
【① 結案】

  ✅ 吸附：移動與旋轉各自可自由輸入，那一欄跟著模式換
     移動 `關 1 5 10` ＋ cm 欄／旋轉 `關 15 45 90` ＋ 度欄／
     縮放 `關 0.05`（⛔ 沒有欄位）
  ⭐ 欄位是唯一的真相，按鈕只是捷徑 —— 打 2.5，四顆全不亮
  🔴 三個各自獨立（舊版一個開關管三個）

  量到的：吸附組 298px 寬，跟 `模式`／`視角` 同一列 —— ⛔ 沒把 bar 撐高。
  ⭐ 版面成本 ≈ 0 的原因是【永遠只顯示一組】，⛔ 不是把按鈕做小。

【② 🔴 這一輪最該留下來的三條】

  一、**setMode() ⛔ 沒有叫 updateBar()**
      它只叫 syncModeButtons() 與 updateEditNum()。
      ⇒ 「跟著模式換」只寫在 updateBar() 裡的話，按了模式鈕那一欄不會換，
      而選取一變它又突然換了 —— 看起來是「這東西時靈時不靈」。
      ✅ 掛進 syncModeButtons()：它有【四個】呼叫點，⛔ 不只 setMode()。
      〔鐵律二：性質由兩端決定〕

  二、**第 4 道機械檢查在沙箱走不完整個模組圖**
      沙箱沒有 three 的 addons，它只走到 OrbitControls 就停。
      ⚠ 光憑它⛔ 不敢說沒有重複宣告 —— 那正是 8/28 讓整個網頁掛掉的坑。
      ✅ 補一道：vm.SourceTextModule 把改過的兩支【真的解析一次】
         （node --experimental-vm-modules），⛔ 不解析 import 目標。

  三、**存檔與上傳跑錯順序，而 .bat 的 log 自己照出來了**
      有一次 3-上傳網路.bat 跑了、2-存檔.bat 沒跑到 ——
      git-log-3-push.txt 的「[1] anything not saved yet - should be empty」
      底下列著 5 個改過的檔 ＋ 一個 ?? 新檔，而 [5] push 說 up-to-date。
      ⇒ 推出去的是上一輪的東西。
      ⭐ ⛔ 這不必靠人記得：三個 .bat 的 log 都有「應該是空的」那一欄，
         下次照樣【先讀那一欄】，⛔ 不要只看有沒有跳錯。
      🔴 病因⛔ 不是程式：AI 用滑鼠點 .bat 時 kang 同時動了滑鼠造成誤判，
         他當場說明並改成自己按。

【③ 🔴 一個新做法，值得留】

  **十項只交出去三項。** 上傳後 AI 開線上版用 JS 驅動介面，
  把 1／2／3／4／5／7／8／10 逐項印出「看得到的鈕／欄位值／單位」對答案。
  ⚠ 而【那不叫驗過】：驗得到「介面顯示的值 ＝ 程式裡存的值」，
     ⛔ 驗不到「TransformControls 真的照那個值吸」（要拖曳）。
  ⭐ 切法是【先指出我驗不到哪幾項】，⛔ 不是全驗一遍再請他複驗。

════════════════════════════════════════════════
【怎麼知道做對了】

  ⛔ 這一輪只有文件回寫，沒有可測的量。
  ✅ 逐字搬 ＋ md5 對答案：21 行／b28c519d3c03dd1bbb33b04b5a9279f9，diff 全等
     ⚠ 第一次比對抓錯段（awk 抓到前面同名的「### 原清單（逐字搬」）——
        改用 tail -1 取最後一個才對上。⭐ 又一次「指標用名字⛔ 不用位置」。
  ✅ 目次產生器 --all（HISTORY.md 已重產，自我否決通過：正文逐字未變）
  ✅ STATUS.md 265 行（上限 276）

【⏳ 待實測】無

【下一件（已排定）】參考線 —— 規格已寫好在 規格/建模器-參考線.md，
  ⛔ 動工前讀完那一份，⛔ 不要重新設計。
  ⏳ 那一份裡掛著一個沒解決的量：左側直條變成 9 顆，平板高度夠不夠 ——
     AI 量得出高度、⛔ 量不出手指按不按得到，要請 kang 拿平板看。
