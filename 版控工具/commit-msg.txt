鋼筆 第 3 階段（一）：加點／刪點　＋　併入上一輪未存檔的 classify 驗證

kang 2026-08-29 選了「加點／刪點」先做，刪點選「擬合，盡量讓形狀不變」。
動到 5 個檔：svgPath.js / prim.js / select.js / main.js / index.html
＋ tests/run.mjs。測試 2420 → 2440 通過 0 失敗。

⚠ 【這一個 commit 含兩輪的東西，⛔ 不要以為 PROJECT_LOG 的改動是這一輪的】

  上一輪（做洞的假設驗證）看完差異之後【沒有存檔就往下開工了】，
  所以 PROJECT_LOG.md 那 +27 −2 是【上一輪】的：
  classify() 沙箱實測四項 ＋ 那句「_pen 只吃 paths[0]」的更正。

  🔴 那一段的重點（⛔ 免得被這一輪的訊息蓋過去）：
     ① classify() 【沒有被 export】，外面拿不到 → 「解法現成」一半是假的
     ② 現在的 pen() 餵兩條路徑 = 體積 10000（兩個各自實心），⛔ 沒有洞
     ③ extrudeMany 帶 holes = 體積 6000 且封閉 → 擠出那一半真的是現成的
     ④ ⛔ 不要去改 BUILDERS.pen 的「只吃第一條」—— 它根本沒有那個問題

  ⭐ 教訓：【看完差異 ≠ 已經存檔】。下一輪開工前先確認上一輪落地了。

════════════════════════════════════════════════
【🔴 ① 加點：形狀⛔ 一格都不能變】

  ⛔ 不是「插一個尖角點」（那只要一行，但那一段會被拉直）。
  ✅ 走 de Casteljau 分割 splitCubic() —— 一段曲線【換成兩段曲線】，
     形狀數學上等價。

  ⭐ 使用者要的是【多一個可以調的點】，⛔ 不是改形狀。

  🔴🔴 但「形狀一格都沒變」這句話【是我寫錯的】，AI 上線實測才照出來：

     線上量到 體積 761,513.3 → 761,530.2（差 0.0022%）—— 對不上我的清單。

     ⛔ 病不在 penAddAnchor()（曲線確實等價，⑱ 逐點比對 1e-9），
     ✅ 病在 flattenPenPath() 是「遞迴對半切到夠平」——
        一段切成兩段之後，兩段各自的切法【不會跟原本那一段對齊】。
        實測折線點數 63 → 61／62／64 都出現過。

     ⚠ 我的第一個診斷也是錯的（以為是容許值 tol 造成的）——
        用 tol 0.2／0.05／0.01／0.002 各跑一次，【全部精確不變】。
        真正的變因是【形狀規不規則】：圓每段對稱所以切法剛好一樣，
        不規則的就會差。

  ✅ kang 2026-08-29 拍板：【改措辭，講實話】，⛔ 不調 tol、⛔ 不改核心。
     → 提示、按鈕說明、實測清單第 4 項全部改成
       「曲線還是同一條，體積會差千分之幾」。
     → 測試多一組 ⑲：不規則形狀，守住【差距一定在 0.1% 以內】。
       ⭐ ⑯（圓，精確不變）＋ ⑲（不規則，有差但有上界）
          合起來才是完整的真相 —— 只留 ⑯ 會誤導。

  ⚠ svgPath.js 原本【只有細分沒有分割】（grep 全域 0 個）——
     splitCubic / cubicAt / nearestOnCubic 三支都是新寫的。

【🔴 ② 刪點：擬合，而且值不值得是量過的】

  端點與切線方向固定，只解兩根把手的【長度】α、β（2×2 最小平方閉式解）。
  ⭐ 切線方向一定要沿用原本的，⛔ 不然接點會折一下。

  🔴 光看擬合自己的誤差沒有人判斷得了好壞 ——
     所以測試裡【旁邊放一個「直接接」的對照組】：

       擬合     面積誤差   1.27
       直接接   面積誤差 114.12
       ───────────────────────
       好將近 90 倍（測試門檻設 10 倍，留餘裕）

     〔鐵律三：一個孤零零的數字沒有人能驗〕

  ⚠ 擬合【有可能無解】（兩邊方向幾乎平行）→ 退回「直接接」，
     而且 fitted:false 會一路傳到提示訊息 ——
     ⛔ 安靜地退路的話，使用者會以為形狀本來就該變那麼多。

【③ 介面】

  加點：⛔ 沒有按鈕 —— 在【改點】模式下【點在線上】就是加。
        命中順序：把手 → 錨點 → 線上 → 空白，⛔ 順序不可以動。
        ⭐ 加完直接進入拖曳（使用者多半想把它拉到別的地方）。
  刪點：工具列多一顆【刪點】，只在改點模式下出現。
        ⚠ 沒選到點、或只剩 3 個點 → 變灰（坑第 21 條）。

════════════════════════════════════════════════
【🔴 怎麼知道做對了】

  ✅ 測試 2420 → 2442 通過 0 失敗（新增 22 項）
     ⑯ 加點·圓（精確不變）8 項　⑰ 刪點擬合 vs 直接接 6 項
     ⑱ splitCubic 本人（逐點比對、nearestOnCubic）6 項
     ⑲ 加點·不規則（有差，但上界 0.1%）2 項 ← 實測照出來才補的

  🔴 故意寫錯一次，確認測試抓得到：
     把加點改成「插一個尖角點」（新錨點把手歸零）
     → ⑯ 當場失敗在正確的兩項：
        「面積【精確】不變」「那個面積真的是一個圓」
     ✅ 還原後 md5sum -c 通過（這次先存了，⛔ 不像上一輪降級成 grep）

  ✅ 模組圖真的載入　✅ id 對照：penDel 兩邊都在
  ✅ 目次產生器 --all

════════════════════════════════════════════════
【⚠ 老實記：測試抓到我兩次，而且第一次的診斷也是錯的】

  nearestOnCubic 第一版的「細化」是：每輪把步長除以 4 再試左右兩點。
  測試報「距離 0.108 > 門檻」。

   ⛔ 我的第一個診斷：「門檻沒有物理意義」→ 改門檻、把三輪加到六輪
   🔴 但再跑一次【還是失敗】—— 因為病根不是輪數：
      粗取樣的 t 最多差【半格】≈ 0.021，
      而「步長每輪 /4」能移動的【總距離】只有
      0.0104 ＋ 0.0026 ＋ … ≈ 0.0139 —— 比 0.021 小。
      ⇒ 那種寫法【不管加幾輪都追不上】。

  ✅ 正解是換成【三分搜尋】（縮區間，⛔ 不是往旁邊試一步）：
     每輪區間 ×2/3，40 輪後跟初始誤差無關。改完 t=0.62、距離 0。

  ⭐ 兩件事都記著：
     一、判準要看【位置】⛔ 不看 t（t 差 0.0017 在那條曲線上就是 0.1 cm）
     二、⛔ 不要只調參數就當作修好了 —— 先確定病根是不是參數。

【⏳ 待實測】加點／刪點 十項（清單在 STATUS.md）

【⏭ 還沒做】開放與封閉、做洞（第 3 階段另外兩件，互不相依）
