PROJECT_LOG V3.1：剖面分切的示意圖進版控，並定下參考圖資料夾的做法

PROJECT_LOG.md +23 / -2，新增資料夾 剖面分切參考/（2 張 jpg）。
程式碼一個字都沒動。

【起因】

  kang 把討論剖面分切時畫的兩張示意圖存進專案資料夾，
  原話：「這樣就不用一直說明討論」。

  這是個好習慣，值得變成慣例 —— 有些事情用圖講比用文字快，
  而且**每次重講都可能講出不一樣的版本**。

【做了三件事】

  1. 檔名改成看得懂的
     原本是時間戳（1787364087271.jpg），下次開對話沒人知道哪張是哪張。
     改成：
       1-剖面分切流程.jpg        立體造型 -> 水平切成 N 片 -> 每片一張輪廓
       2-疊層對位三種做法.jpg     定位孔／中心線／外部夾具的側視比較
     對應關係是開圖確認過的，不是照時間猜的。

  2. 「剖面分切」那節最前面加一個指標
     寫明**動工前先開圖，不要只讀文字然後開始猜**。
     文字寫得再清楚，跟看一眼還是不一樣。

  3. 協作規則新增「參考圖資料夾」的做法
     討論過程中畫出來、kang 確認過的示意圖，存成 `XX參考/` 放在根目錄，
     並在日誌對應章節指過去。
     -> 要動某一項之前，先看有沒有對應的參考資料夾，有就先開圖。

【一個容易誤會的地方，已寫進日誌】

  .gitignore 有 `/*.jpg`，看起來像會擋掉這兩張圖。
  但**開頭那個斜線只擋根目錄**，子資料夾裡的圖不受影響。
  步驟 [1] 已確認兩張圖都被認成新檔案（??），會正常進版控。

【檢查】

  [1] PROJECT_LOG.md 修改 ＋ 剖面分切參考/ 兩張新圖。
  [3] +23 / -2。兩行刪除都是同一行改寫：
      版本號 V3.0 -> V3.1、待辦那條加上「動工前先看參考圖」。
      **無內容遺失。**
  [5] 其餘所有檔案的絕對行數與上一個 commit 完全相同。

【下一步】

  對齊／均分 ＋ 正交三視圖。這次不再討論，直接做一版給 kang 試。
