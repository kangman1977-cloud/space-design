吸附：移動與旋轉各自可自由輸入，那一欄跟著模式換

kang 2026-08-31 要求：「（關／1／5／10）...可以增加一個可以手動輸入的數值」
「旋轉與縮放...我都認為需要有可以輸入數值的機制」→ 縮放他當場收回（見下）。

════════════════════════════════════════════════
【這次要做什麼】

  吸附那一組從「四顆固定值、一個開關管三種變換」
  改成「三套、跟著模式換、移動與旋轉可自由輸入」：

    移動模式  吸附  [關][1][5][10]   [  1  ] cm
    旋轉模式  吸附  [關][15][45][90] [ 15  ] 度
    縮放模式  吸附  [關][0.05]        ⛔ 沒有輸入欄

  ⭐ 欄位是唯一的真相，四顆按鈕只是【幫你把數字填進去】。
     打 2.5 進去，四顆全部不亮。
     〔跟 editNum 同一條路：入口分開、動作只走一條〕

  🔴 版面成本 ≈ 0 —— 永遠只顯示一組。
     判準：吸附格距【只在對應的模式裡有意義】，三者不會同時需要。

【依賴哪些事實】

  1. 現在 setSnap(step) 一支【同時】設三件事：
     移動＝參數值、旋轉＝寫死 15 度、縮放＝寫死 0.05，
     後兩者只跟著「開／關」走、跟參數無關。
                                    【已查證 · select.js:1907-1914】

  2. 吸附四顆的家與現行處理：
     HTML 在 index.html:938-944（class="snapBtn" data-s，⛔ 無 hidden 屬性）
     JS  在 main.js:3622-3631（querySelectorAll('.snapBtn')）
                                    【已查證 · 兩處都讀過】

  3. 🔴🔴 setMode() 只叫 syncModeButtons() 與 updateEditNum()，
     ⛔【沒有】叫 updateBar()。
     ⇒ 「跟著模式換」只寫在 updateBar() 裡的話，
        按模式鈕那一欄【不會換】—— 又是鐵律二（兩端）。
                                    【已查證 · main.js:3593-3606】

  4. syncModeButtons() 有【四個】呼叫點（1488／1509／3604 ＋ 1714），
     ⛔ 不只 setMode 一個 —— 選到「點」時 _attachEditProxy() 會自己
     把 gizmo 切回移動，那條路⛔ 不經過 setMode()。
     ⇒ 掛在 syncModeButtons() 裡，四個呼叫點一次涵蓋。
                                    【已查證 · main.js:3615, grep 四處】

  5. 現行 .snapBtn 的隱藏條件是 gizmoOff，
     ⛔ 不是「哪一顆變換工具亮著」（那三顆永遠有一顆亮著）。
     gizmoOff ＝ sel.editMode ? sel.editCount===0 : sel.count===0
                                    【已查證 · main.js:3760-3771】

  6. 「標籤跟著模式換」這個專案已經在做：
     NAME = { translate:'移動', rotate:'旋轉', scale:'縮放' }
     ⇒ 吸附照抄，⛔ 不發明新機制。
                                    【已查證 · main.js:1580, 1594】

  7. 兩端檢查.js 看得懂 $('id').hidden＝… 與同一行的 for-of 陣列，
     ⛔ 看不懂 querySelectorAll 掃一批（一律漏報）。
     ⇒ #snapNum／#snapNumUnit 的 HTML 初始屬性要跟 JS 用同一個機制
        （照 editNum：HTML 寫 hidden、JS 也開關 hidden）。
                                    【已查證 · 文件工具/兩端檢查.js 檔頭】

  8. sel.snapStep 還有兩個下游讀者（擠出的預設距離、它的 title），
     ⇒ 移動那一支的欄位名⛔ 不可以改名。
                                    【已查證 · main.js:1818, 3781】

  ⛔ 沒有【還沒查】的項目。

【會動到哪幾個檔】

  1. 建模器/index.html            吸附組（938-944）
  2. 建模器/js/view/select.js     setSnap 拆三支
  3. 建模器/js/main.js            syncSnapRow／gizmoOff／snapBtn／snapNum
  4. STATUS.md                    待實測回寫 ＋ 四塊已結案摘要移出（見下）
  5. 規格/建模器-參考線.md         🆕 下一輪的規格（這一輪⛔ 不寫程式）
  6. 版控工具/commit-msg.txt      本檔

  ⚠ 4 的移出：**收合／加厚後轉成網格／鋼筆第 3 階段（一）（二）** 四塊。
  ⛔ 這【不是搬】—— 完整經過本來就已經在 HISTORY.md（節名逐一 grep 對過），
  刪掉的只是 STATUS 上的摘要。⭐ 它們帶的兩條通則⛔ 沒有消失：
  「AI 驗過 ≠ kang 驗過」→ STATUS「穩定參考」；
  「指標用名字⛔ 不用位置」→ STATUS「三條鐵律（文件的）」第 2 條。
  理由是 STATUS 上限 276 行，加清單後會爆到 283 —— 落點規則本來就寫
  「本檔替換式，結案就移出」。移完 269 行。

【怎麼知道做對了】

  ⚠ 吸附走的是 three.js TransformControls，⛔ 沙箱驗不到「真的吸住」——
    那一半只有 kang 按得出來。所以機械那一半只能守住【接線】：

  ✅ 四道機械檢查全過（第三道兩端檢查這次是主角）：
     測試 2500 過 0 失敗／目次已是最新／兩端 60 個元件 0 打架／模組圖
  ✅ 🔴 另外自己補一道：用 vm.SourceTextModule 把改過的兩支【真的解析一次】
     ⚠ 理由：第 4 道只走到 OrbitControls 就停（沙箱沒有那個套件），
     光憑它⛔ 不敢說沒有重複宣告 —— 而那正是 2026-08-28 讓整個網頁掛掉的坑。
     結果：main.js ✅／select.js ✅
  ✅ 1-看差異.bat 跑過，逐行看【被刪掉的每一行】—— 全部對得上，⛔ 沒有誤刪
     〔上一輪 #rail 的 CSS 就是在這一步才抓到的〕
  ⏳ kang 實測清單（見 STATUS.md）—— 數字寫成他算得出來的式子

════════════════════════════════════════════════
【🔴 一個行為改變，要講】

  舊：按「關」→ 移動、旋轉、縮放的吸附【全部一起關】
  新：三個【各自獨立】

  理由（kang 同意）：會想要「位置吸得死死的、角度能微微斜」，
  反過來也會 —— 一個總開關做不到。
  ⚠ 沒有相容性負擔：建模器還沒進實際工作流程。

【✘ 這一輪明確不做】

  縮放的自由輸入 —— kang 2026-08-31 當場收回：
  「既然縮放較複雜..這就不要處理...維持目前的機制」
  ⇒ 0.05 維持寫死，只是跟著模式換位置後有了自己的開關。

【⏳ 待實測】見 STATUS.md

【下一件（已排定）】參考線 —— 第二輪。
  左側直條第 9 顆工具 ＋ 控制列一組參數
  ＋ 檢視放「顯示參考線」＋ 吸附放「吸到參考線」。
  ⭐ 先做吸附是因為參考線的吸附要接在這一組上面。
