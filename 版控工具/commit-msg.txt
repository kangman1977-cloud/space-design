日誌減重：三批已結案的實測清單搬進 HISTORY.md —— 只動文件，0 行程式

kang 2026-08-27 同意，開新對話之前做的。測試重跑全過（2048 項）。
動到 2 個檔：PROJECT_LOG.md、HISTORY.md。

【為什麼現在做 —— 有實證，⛔ 不是看檔案大小】

  🔴 今天開場時，AI 讀 PROJECT_LOG.md 被截斷在第 729 行（全檔 2366 行），
     分三次才讀完。

  而日誌自己記著這個病：
    〔改造前本檔 2806 行 / 167KB，AI 讀到第 814 行就被截斷 ——
      規格明明寫在裡面，卻讀不到。〕

  照「什麼時候該整理」那六個徵兆，第 1 個中了，而且很明顯：
    「瘦身完很快又長回去」——今天早上才瘦到 2119，晚上就回到 2366。

  ⛔ 只中一個，所以照規則「順手修那一處就好，不整份重來」：
     只搬今天三批已結案的實測清單，其餘一律不動。

【搬了什麼】

  PROJECT_LOG.md「開新對話時先讀這段」底下的三批清單（286～399 行，共 114 行）：
    選轉角＋選相似 11 項、變成正圓 8 項、破洞在哪裡 7 項
  → HISTORY.md 新節「三批實測清單全過」，逐字，一字未改。目次已加。

  落點規則的依據：
    「已結案的實作流水帳、當初怎麼驗出來的」→ HISTORY.md
    「還沒做完的事、進度、狀態」→ 本檔待辦（結案就移出，不堆積）

  ⚠ 「實測結果的回寫」那條說「清單本身留著當對照」——
     那是指⛔不要刪掉，不是指「留在日誌」。兩條規則的交集就是：
     逐字搬進 HISTORY.md，日誌留摘要 ＋ 指標。

【🔴 日誌只留下一條，而且是刻意的】

  > 只要物件有板厚，破洞就會被 shell() 自動加厚救回來
  > → 3D 列印面板那則紅字不出現 →「指出來」那顆按鈕也不出現。

  ⛔ 不講的話，下一個人會以為按鈕壞了 —— kang 今天實際卡住過。
  這一條符合檔頭那張表的判準：「你以為的 X 其實是錯的」那一類非留不可。

  其餘（每一項的預期數字、對答案的經過、清單第一版寫錯的教訓）
  全部是流水帳，Grep「三批實測清單全過」找得到。

【機械斷言（全過）】

  ★★★ 搬走的 114 行 md5 = e09641fe827f3dc008ddd5ee23008f1d
       搬進 HISTORY.md 之後再算一次 = e09641fe827f3dc008ddd5ee23008f1d
       → 逐字吻合，⛔ 沒有靜默改寫
  ★★★ HISTORY.md 舊的 3539 行 md5 = 2a206c8d164b6bad933abe064471e298
       搬移後 head -3539 的 md5 完全相同 → 純 append
  ★★ PROJECT_LOG.md 沒被動到的兩段（前 285 行、第 400 行之後）md5 均相同
  ★★ 行數：PROJECT_LOG 2366 → 2272（−94）；HISTORY 3539 → 3670
  ★★ 差異檔核對：HISTORY 只有 1 行刪除（就是「最後更新」那一行的日期）
  ★★ 兩檔都過 iconv -f UTF-8 -t UTF-8，檔尾都是 0a
  ★★ node 建模器/tests/run.mjs 全部通過（2048 項，程式一行都沒動）

【開新對話接得上嗎（實際 Grep 過）】

  ✅ Grep 待實測 → 只剩「刀具的平板三項」（卡硬體，不擋新功能）
  ✅ 下一件 ＝ 依特徵全選還沒做完的那些（非流形邊、鬆散元素）
  ✅ 檔頭「最近一輪翻掉了什麼」5 條，編號不重複
