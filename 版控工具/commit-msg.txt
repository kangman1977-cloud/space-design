日誌 V8.5：鐵律「目的藏在下游」補一條但書（只動文件，程式沒動）

只改 PROJECT_LOG.md，一支程式都沒碰。測試項數一個都沒動。

【起因：kang 2026-08-25】
「現在建模器都還在建置開發中...並沒有進入實際工作流程..
  我們要整個建模器都完工後...我才會真實的導入實際工作」

【這句話翻掉了什麼】
我當時正在問他「你實際建模時多常需要量兩點距離」，
拿他的答案去排「加線 vs 量測」的順序。

那個問題他答不出來 —— 實際工作還沒開始。
逼他回答只會得到一個猜的答案，然後我拿它去排順序，
等於在計畫上埋一個地雷。

【🔴 鐵律「目的藏在下游」要補的但書】
那一條有一個沒寫出來的前提：下游要「已經存在」。

剖面分切、指定分片、匯入線稿那幾輪問得出東西，是因為下游當時就在
（Illustrator、CNC、疊層黏合）。
而編輯功能的下游是「用這個建模器建模」，那件事還沒開始發生 ——
不是 kang 不肯講，是還不存在。

⛔ 所以不要問他「這個功能你實際上多常用」。

【🔴 現階段的排序判準】
換成 kang 8/24 自己定的那條：對照表上標 ⭐ 的那約 25 項做齊
（外部參考-Blender編輯.md 第 10 節）。

⛔ 不可以拿「這個實際上大概不常用」去砍範圍或改順序 ——
沒有使用資料的時候，那種話就是「功能邏輯從推論來」的另一張臉。

【⭐ 連帶推論：kang 的實測份量比看起來重】
現階段它是唯一的現實檢查。
回頭看他抓到的東西 —— 12 條邊選不到／預設值寫死 0／
選取有兩份資料會切錯物件／訊息叫人走死路 ——
全部是介面類的，沙箱一個都驗不了。

那不是巧合：資料層有 1606 項數學守著，介面沒有。

【改了哪三處】
  檔頭版本                  V8.4 → V8.5，補一句 kang 的澄清
  鐵律「一、動手之前」       「目的藏在下游」底下加一則但書（完整推導）
  「先讀這段」kang 定的方向   「建模編輯的功能要做齊」底下補時程意義 ＋ 指標

⚠ 三處只有一份完整推導（在鐵律那則），另外兩處是指標，
不抄第二份（落點規則「同一條規則只寫一次」）。

【下一輪（判準換掉之後，順序不變，理由換了）】
在一個面上加一條線（連接兩點 J → 刀具 K）
  · 它是能力缺口（對照表 10.7 節三大缺口之一）
  · 量測第 2 步是方便（面板已經給得出數字），第 4 步是缺口但可以晚
  · 先做加線會讓量測第 4 步變便宜（共用「先點一個、再點一個」的介面）
