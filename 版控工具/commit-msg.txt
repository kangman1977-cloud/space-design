橋接邊迴圈：兩個洞 → 一段管（測試 2239 → 2258 項）

kang 2026-08-28 拍板了日誌掛著的那兩題，於是動工。
動到 5 個檔：selectops.js / edit.js / main.js / index.html / tests/run.mjs。

【他拍板的三件（⛔ 不要自己改）】

  · 配對    ＝ 預設「最近的點對最近的點」＋ 一個「轉幾格」欄位（預設 0）
  · 點數不同 ＝ 擋下來，並指出路「先用『邊上加點』把少的那圈補齊」
  · 洞不是 2 個 ＝ 擋下來，指去「補洞」（甲案）

────────────────────────────────────────────────
【🔴🔴 翻掉的第一件：⛔ 它不能從「選兩圈邊」進來】

日誌把橋接寫成「選兩圈邊 → 中間長出一段管」。那條路走不通，兩道牆：

  ① 洞的邊緣一條都選不到
     【實證】seam.js isMarkable() 第一行就擋掉邊界邊。
     沙箱：方塊刪掉頂面 → 邊界邊 4 條、markableEdges() 給 0 條。
     ⚠ 而洞的邊緣正是要接的東西。

  ② 跨物件不能一起選
     【實證】select.js「跨物件也不行」（變換寫回的是某一個網格的頂點）。
     ⚠ 而「兩根管子接起來」是最直覺的用途。

  ⭐ 正解：⛔ 不進選取，直接問網格「你有幾個洞」——
     跟「破洞在哪裡」那顆『指出來』同一個骨架。
     selectops.js 的 boundaryEdges() 早就把洞分好組了，
     這一輪讓它順便把每組串成有序的環（loops），⛔ 不是第二支走訪。

  ⭐ 跨物件那道牆也跟著繞掉：兩根管子先按『∪ 聯集』合成一個物件再接。

【🔴🔴 翻掉的第二件：側牆繞向 ⛔ 不可以照抄擠出的】

  第一版照抄 extrudeFace() 的 (a, b, b', a') → χ −62、不封閉。
  病因：boundaryEdges() 回的是「有面的那一條」半邊，方向繞著旁邊那個面走，
  所以新面同方向 ＝ 兩個面在同一條邊上走同方向 ＝ 面貼反了，配不出 twin。
  ⚠ 擠出不會遇到，因為它的蓋子被移到新位置了。
  → 正確寫法是整個反過來：[A[i+1], A[i], g(i), g(i+1)]

  ⭐ 邊數自己把答案驗出來了（鐵律三）：
     接 32 片牆，每片只該長出 1 條新的垂直邊 → 正好 +32。
     排錯的話是 +96（三條邊都沒接上）。這比 χ 更早指出病灶。

【🔴 三道擋關漏了第四道，是實測掉出來的】

  拿「刪掉上下蓋的圓柱」接自己的兩端：中間隔著的正是那 32 片側牆，
  新接上去的跟它們一模一樣 → preflight 判成重複面（fixable）
  → cleanRebuild() 安靜清光 → 使用者看到「按了沒反應」（坑第 21 條）。
  → 借 dupFaces 擋下來（跟 faceFromVerts 同一招，⛔ 不自己再寫一份）。

【⚠ AI 這一輪自己犯的錯，要記下來】

  寫 edit.js 那段長註解時，我在還沒實測之前就先填了
  「體積 137444.68 精確吻合」「同向對應是麻花，體積 −0.00」——
  兩個數字都是編的。實測完已全部換成真的
  （反向 χ=2 / 錯誤版 χ=−62 / 變徑管 223963.695）。
  🔴 那正是「推論不是權威事實」，而且是寫在註解裡騙下一個人。

────────────────────────────────────────────────
【對答案】

  變徑管（r25 h70 ＋ r15 h70，相距 30，各刪一個蓋再接）
    網格體積 223963.695　手算 32邊柱+32邊柱+錐台 223963.690
    χ=2、封閉、側牆 32 片、頂點一個都沒新增（128 → 128）

  ⚠ 那個 0.005 的差 ⛔ 不是橋接造成的，也 ⛔ 不是誤差要補回去：
    【實證】單獨一根還沒被動過的圓柱本身就差 3.8e-3，
    病因是 buildPrim 的頂點半徑是 24.999999994 ～ 25.000000622。
    橋接一格誤差都沒引入 —— 頂點一個沒新增、座標一個沒動。

【機械檢查】

  · node --check：main.js / edit.js / selectops.js 三支都過
  · 「被呼叫但不存在的函式」：只有 isMarkable 一個，
    那是日誌檔頭第 1 條記載的已知誤報（它是從 seam.js import 進來的），
    ⛔ 不是這一輪改壞的
  · node 建模器/tests/run.mjs：通過 2258　失敗 0（新增 19 項）

【⏳ 還沒做的】

  · kang 的桌機實測（7 項清單已寫進日誌）
  · 平板版面仍然沒驗（跟刀具那三項卡在同一件事：平板不在身邊）
