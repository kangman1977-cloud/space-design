日誌 V9.8：瘦身 —— 十三輪的經過逐字搬進 HISTORY.md（1113 → 332 行）

不動程式，只動文件結構。測試項數一格都沒動（仍是 1842）。
PROJECT_LOG.md 2783 → 2025 行；HISTORY.md 1752 → 2654 行。

【🔴 要解的問題：日誌三天就長回原樣】
改造前 2806 行 / 167KB，改造後又長到 2783 行 / 182KB —— 位元組還更大。
「開新對話時先讀這段」佔 1113 行（41%），而那一節的定義是「每次從頭讀完」。

病因明確：HISTORY.md 最後一筆停在 2026-08-24。
8/25 之後的十三輪（內縮、導角、outer miter、切一刀、連接兩點、面上加線、
多點連接、邊上加點、顯示點、分離、刀具兩版、量測）一筆都沒搬過去。

⚠ 日誌自己「文件分層」那節寫著：「這條沒守住，一年後就會長回 2806 行」。
   實際上用了三天。

【搬去哪：不是兩個去處，是三個】
盤點才看清楚「先讀這段」混了四種東西：

  流水帳（這一輪怎麼查出來的）      → HISTORY.md          ~817 行
  跨功能的教訓（埋在某個功能底下）   → 本檔「鐵律」那一節    4 條
  功能的用法（哪顆按鈕、怎麼操作）   → 規格\（本來就寫在裡面）
  規則／方向／否決紀錄／現況         → 留在原地不動         291 行

🔴 第二列是坑第 33 條（教訓寫在別的功能底下就等於沒寫）的現行案例。

【🔴 機械斷言：這次是照鐵律做的，不是手寫重組】
「搬移一律逐字搬 ＋ 機械斷言，⛔ 不做順手精簡改寫」——
上一輪就是違反這條把 main.js 吃掉 18 個函式。這次的做法：

  1. 把 84-1196 行切成 22 塊（KEEP / MOVE / 搬去待辦），
     斷言「切完重組 = 原文」→ 1113 行、md5 48e982af6b17，✅ 一個位元組都沒差
  2. MOVE 的十二塊附加到 HISTORY.md，
     斷言「每一塊在 HISTORY 裡逐字出現剛好一次」→ ✅ 12/12
  3. 斷言 HISTORY 舊內容沒被動到 →
     ✅ diff 只有 903 行新增、1 行刪除（就是「最後更新」那個日期）
  4. KEEP 的十塊放回日誌，斷言逐字保留且各一次 → ✅ 10/10
  5. 最後全檔對照：原日誌 2231 行非空，
     ✅ 兩個檔都找不到的只有 45 行，而且全部是我刻意重寫的標題那一塊

【新增：按鈕清單一張表】
「先讀這段」現在用一張表列出工具列「編輯」那組有哪 16 顆按鈕，
每顆一句話，⛔ 用法一律去 規格\建模器-點線面編輯.md。
理由：同一件事寫兩個地方，重複的那份會漂，
而且會讓下一個人「很有信心地答錯」（文件分層第一條鐵律）。

【四條教訓升格進「鐵律」】
  三、做介面 → 🔴「沙箱說可以、kang 說不行」先看介面，不要先懷疑演算法
             → 🔴 使用者說「似乎」「有時候」是狀態依賴，不是他沒操作好
             → 🔴 加新功能時順手檢查舊的錯誤訊息有沒有因此過期
  一、動手前 → 🔴 同一個檔案裡的寫法不一定能照抄，要先看那是什麼元素
               （hidden 對 div 有效、對 SVG 不保證）

⭐ 動 main.js 那道「被呼叫但不存在的函式」檢查也進了協作規則，
   ⛔ 不再只寫在某一輪的敘述底下。

【⚠ 一個順手查出來、但沒有動它的東西】
測試項數那條「A → B」鏈現在跨三個檔（日誌／HISTORY／規格），
⚠ 而且它本來就有四個缺口，不是這次搬壞的：
1309→1338、1367→1385、1537→1548、1548→1574 從來沒被寫成「A → B」。
已在「文件分層」那節寫明驗法與這四個缺口，
⛔ 免得下一個人看到缺口就以為被改壞了。

【⏭ 還沒做】
  3. 拆 規格\建模器-點線面編輯.md（2393 行 / 16 個功能，違反一功能一檔）
     ⚠ 它會是下一個被讀到一半截斷的檔案。

【✅ 存檔前自我檢查】
  五道機械斷言全過（見上）
  現況項數 1842 沒動；⛔ 沒有用整批取代
  節前 1-83 行、節後（尺寸的依據 → 檔尾）逐字未動
  Grep 待實測 仍然只撈到一件事：平板版面
