刀具：按住拖一個切點就移動它（kang 提的，三件他拍板）

⛔ 幾何一行都沒改。動到 2 個檔：js/view/select.js、js/main.js

════════════════════════════════════════════════
【這次要做什麼】

  kang：「下切點後...我可以移動切點...但這會跟『刪除』功能撞一起」

  🔴 **⛔ 不加新模式** —— 因為刪除是「點」、移動是「拖」，**本來就是兩個手勢**。
  ⭐ 這個專案已經解過同一題：**參考線第 3 階段**（點＝選、拖＝挪，⛔ 沒開模式）。

  掉出來的規則（⛔ 前三列都是現況，只有第三列是新的）：

  | 手勢 | 在哪 | 做什麼 |
  |---|---|---|
  | 輕點 | 空白 | 加一顆 |
  | 輕點 | 切點上 | 刪掉它 |
  | **按住拖** | **切點上** | 🔴 **移動它（新）** |
  | 按住拖 | 其他 | 一筆畫 |

【依賴哪些事實】⛔ 每條都填了出處

  1. 「輕點 vs 拖」的門檻已經存在：`TAP_MOVE` 8px、`TAP_TIME` 450ms，
     而且是在 `pointerup` 才判 —— ⛔ 不必新定門檻
                                    【已查證 · js/view/select.js:43-44, 1231】
  2. 一筆畫在 `pointerdown` 開，記在 `this._stroke`
                                    【已查證 · js/view/select.js:885-887】
  3. 一筆畫在 `pointerup` 收：`stroke && dist > TAP_MOVE`
                                    【已查證 · js/view/select.js:1231-1238】
  4. 「游標離哪一顆切點最近」＝ `_knifeHotIndex()`，螢幕 px，
     ⛔ 不隨模型大小成長　　　　　　【已查證 · js/view/select.js:1936-1956】
  5. 切點的真相只有 `knifePicks` 一份，`select.js` ⛔ 不存第二份
                                    【已查證 · js/main.js:965, 146-149】
  6. 🔴 **切點⛔ 不可能自由飄**：`knifePath()` 要求每一個切點都在一條邊上
     —— 所以「移動」＝ 拿新位置重跑 `pickEdgePoint()`
                                    【已查證 · js/core/edit.js:2536 檔頭】
  7. `_stroke` 有 5 個清除點，新的狀態要跟著它們一起清
                                    【已查證 · js/view/select.js:200,866,1163,1178,3298】

【kang 拍板的三件（結果不唯一，⛔ 不是我決定的）】

  ① **拖出模型外 → 彈回原位**（⛔ 不是留在最後一個吸得到的地方）
     ⭐ 而且**當場就彈回去**，⛔ 不等放手 —— 讓他立刻看到「這裡不行」
  ② **移動⛔ 不進 `退一步`** —— 那顆只管刪除，⛔ 不擴大它的意思
  ③ **拖曳中即時重畫整條路徑**

【會動到哪幾個檔】

  · js/view/select.js
      pointerdown：起手落在某顆切點上 → 開 `_knifeMove`，**⛔ 不開 `_stroke`**
      pointermove：超過 `TAP_MOVE` 才算拖 → 每次現算 `pickEdgePoint()` 送出去
      pointerup  ：拖過 → 收工；⛔ 沒拖過 → 什麼都不做，讓它照舊落到「輕點＝刪」
      ⚠ `_knifeMove` 要加進 `_stroke` 那 5 個清除點
  · js/main.js
      `knifeMoving = { i, orig }`；拖曳中直接改 `knifePicks[i]` 再 `drawKnifePicks()`
      吸不到邊 → 當場還原成 `orig`

【怎麼知道做對了】⛔ 要能對答案的量

  · 4 道機械檢查全跑
  · 🔴 **⛔ 不要只驗自己寫的那一支** —— 上一輪的教訓就是這條：
     要真的按過「拖一顆、拖到外面、輕點刪、一筆畫、快點兩下」五條路
  · 🔴🔴 **特別盯「放寬判定會撞到誰」** —— 上一輪就是這樣出事的：
     這次新的分岔吃掉的是「**從既有切點正上方起手的一筆畫**」，
     ⚠ 已當面跟 kang 講過並確認可以接受
