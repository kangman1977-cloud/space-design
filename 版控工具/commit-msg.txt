③ 開放與封閉：甲 加厚成板 ／ 乙 立成牆

kang 2026-08-30 拍板的第 ③ 件。動到 7 個檔（新增 1）：
  新　建模器/js/build/ribbon.js
  改　建模器/js/build/prim.js ／ js/view/select.js ／ js/main.js
  　　js/ui/toolbar.js ／ index.html ／ tests/run.mjs
  文件　STATUS.md

════════════════════════════════════════════════
【動手前檢查表】

【這次要做什麼】
  鋼筆多兩顆開關：`不封口` ＋ 只在它開著時出現的 `往上長`。
  開了之後畫的是【一條線】—— 拿去做【板】（沿線加厚、平躺）
  或【牆】（沿線立起來）。共用同一個數字欄位，標籤跟著換。

【依賴哪些事實】⛔ 每條都填了出處
  1. BUILDERS.pen ＝ 拉直→丟面積0→classify→extrudeMany
                                    【已查證 · prim.js:745-758】
  2. extrudeMany 要封閉輪廓，開放的線走不了  【已查證 · extrude.js:44-101】
  3. takePen()／peekPen() 把 closed:true 寫死
                                    【已查證 · select.js:2934,2937,2961】
  4. flattenPenPath() 本來就支援 closed:false 【已查證 · prim.js:548,569-571】
  5. 板件 ＝ 開放單層面，厚度是 shell() 顯示時才加
                                    【已查證 · prim.js:810-819／mesh.js:634】
  6. miter（角平分線 ÷ cos）現成，內縮已經借用過
                                    【已查證 · mesh.js:655-668／edit.js:3232】
  7. PRIM_SPECS.pen 只有一個欄位、label 寫死；面板照 fields 硬跑
                                    【已查證 · prim.js:119-126／toolbar.js:185-192】
  8. finishPen() 建物件時 kind 寫死 SOLID    【已查證 · main.js:1157-1161】

【會動到哪幾個檔】見上（7 個）

【怎麼知道做對了】
  ⭐ 面積 ＝ 折線長 × 那個數字 —— 甲乙【同一條斷言】
  ⭐ shell(板厚) 之後 體積 ＝ 面積 × 板厚
  ⭐ 封閉那條路的體積一格不變

════════════════════════════════════════════════
【① 甲乙是同一支，⛔ 不是兩套幾何】

  拆開之後是同一個動作：【沿著線，往某個方向掃出一片帶】。
    乙 牆 ＝ 往 +Y 掃，距離就是高度（線在底下）
    甲 板 ＝ 往線的側向掃，距離就是寬度（線在正中間，兩側各半）

  ⇒ 一組四邊形，只換「兩條軌道由誰產生」。
  〔kang 2026-08-25 批准的四動作框架：新功能 ＝ 既有零件換個組合〕

【② 🔴 「封不封口」只有【一個】家】

  ⛔ 沒有動 takePen()／peekPen()／loadPen() —— 一行都沒改。
  旗標放在物件身上（src.open），BUILDERS.pen 生成時把 closed:false
  【蓋上去】。若改成「看每條 path.closed」，就會有兩個地方各記一份
  同一件事（坑第 31 條）。

【③ 甲的轉角一定要 miter】

  每段各自偏移的話，90 度轉角外側【裂開一個 w×w 的缺口】。
  正解跟 shell()／insetFaces() 一樣：沿角平分線推、而且要除以 cos。
  ⭐ 而 miter 讓面積【剛好守恆】：外側多出的楔形與內側少掉的
     都是 (w/2)²·tan(θ/2) —— 所以「面積 ＝ 線長 × 寬度」才會精確。
  【實證】L 形 100＋60、寬 12 → 面積 1920.000000 ＝ 160 × 12（誤差 <1e-9）

【④ 三件「⛔ 不講就跟壞掉分不出來」的事】

  · 開著不封口時按到起點 → ⛔ 不接起來，跳紅字講原因
    （⛔ 不可以安靜地在起點疊一個看不見的重複點）
  · 不封口的物件【種類自動變板件】—— ⛔ 不改的話板厚欄位看不到，
    而且 3D 列印會報「不封閉」（誤報比漏報更糟）
  · 收工的提示講【面積】⛔ 不講體積 —— 開放的網格算不出體積
    〔2026-08-28 那個假數字的教訓〕

【⑤ 面板上也有那兩顆（kang 要求「兩邊都做」）】

  · PRIM_SPECS 多一種宣告 toggles，toolbar.js 照它畫（用現成的 _rowCheck）
  · 數字欄位的標籤走 labelOf(src)：厚度／高度／寬度
  · 面板上改 open 會【連動 obj.kind】—— 性質由兩端決定，只改一端 ＝ 沒改
  · 編輯路徑途中按工具列那兩顆也會寫進物件；按【取消】時
    兩顆開關與種類都會還原（⛔ 不可以只還原路徑）

【⑥ 🔴 看差異時自己抓到、當場修掉的兩件】

  一、updateBar() 裡我把新的一段【插進了舊註解與它在講的那幾行中間】——
      舊註解（「兩個數字欄位一定要帶標籤」）就變成在講我的東西了。
      ⇒ 兩段各自歸位，順便把「標籤會跟著開關換」補進那則舊註解。

  二、🔴 進「編輯路徑」時我會【照那個物件】把工具列兩顆擺好 ——
      那是【程式改的】，⛔ 不是使用者按的。收工不還原的話，
      他下一次畫新東西會莫名其妙變成一道牆，而他從頭到尾沒按過那顆。
      ⇒ penEditing 多留 barOpen／barUp，endPenEdit() 一律還原。
      ⭐ 這跟「鎖角度」不一樣：那顆一直是他自己按的，所以才可以黏著。

【⑦ 🔴🔴 上傳後我自己開線上版驗，撞到一個【真的 bug】並修掉】

  症狀：開著「不封口」畫一條【兩個點的直線】，按收工 →
        東西沒建出來，訊息說「至少要放 3 個點才圍得出一個形狀」。

  🔴 病因：「一條路徑至少 3 個錨點」這個下限【寫死在六個地方】
     （takePen／peekPen／penNewPath／loadPen／penRemoveAnchor
      ＋ main.js 三顆按鈕的 disabled）。
     ⚠ 那個下限對【封閉】是對的（兩個點圍不出面積），
        對【一條線】是胡說 —— 兩個點就是一條完全正常的板／牆。

  ⇒ 改成一個地方回答：sel.penMinPts（不封口 2、封起來 3），
    penRemoveAnchor() 多一個 min 參數。六處全部改成問它。
  ⭐ 話也跟著改：「至少要放 2 個點才有一條線」「再刪就不成一條線」。

  ⚠ 🔴 這一條照出【我的實測清單第 3 項本來會直接失敗】——
     那一項就是叫 kang「只放 2 個點」。
     ⇒ 開清單之前要問「使用者驗得出這個結果嗎」〔鐵律五〕，
       而這一次是【真的去按了才知道】。

════════════════════════════════════════════════
【怎麼知道做對了 —— 實際跑的】

  ✅ node 建模器/tests/run.mjs → 通過 2500　失敗 0（新增一節 30 項）
     其中釘住的：面積＝線長×數字（甲乙）、shell 後體積＝面積×板厚、
     法向朝上（反著畫也是）、弧牆的橫向邊除兩端全 smooth、
     ★★ 封閉那條路體積 16000 一格不變、存讀檔帶得回兩顆開關
  ✅ 模組圖真的載入一次（改過 js/）→ ⛔ 沒有 SyntaxError
  ✅ node 文件工具/目次產生器.js --all
  ✅ 🔴 上傳後開線上版走完四步（⑦ 就是這樣抓到的）：
     ① ?bust ＋ no-store 抓到的五個檔都有新識別字（伺服器是新的）
     ② 逐檔 fetch(cache:'reload')【⛔ 不帶 query】＋ reload
     ③ decodedBodySize vs 伺服器位元組：main/prim/ribbon/toolbar/select
        五個檔【全部一模一樣】→ 跑的確實是新版
        ⚠ 我第一次比錯了：encodedBodySize 是【壓縮後】的，
          而 text().length 是【字元數】—— 兩個本來就不會相等。
          ⇒ 要比就比 decodedBodySize vs TextEncoder 算出來的位元組。
     ④ 實際按過工具列：不封口／往上長的顯隱與標籤（厚度→寬度→高度）全對
  ⚠ **AI 驗過 ≠ kang 驗過** —— 我驗的是數字與訊息，
    你驗的是「這是不是你要的東西」。

【⏳ 待實測】③ 八項（清單在 STATUS.md）　⚠ 平板仍然沒驗

【下一件】🔴 ⛔ 沒有現成的下一件 ——
  ⏳ 待實測清空之後要從 外部參考-Blender編輯.md 第 10 節重挑，而且要問 kang
