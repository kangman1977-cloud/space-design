刀具：游標靠近的那個切點會變大（11 → 19 px）—— kang 提的

動到 3 個檔：建模器/js/view/select.js／建模器/js/view/scene.js／建模器/js/main.js
文件：規格/建模器-點線面編輯.md／STATUS.md

════════════════════════════════════════════════
【動手前檢查表】

【這次要做什麼】
  刀具已經點下的切點，**游標靠近哪一顆、那一顆就畫大一點**。
  🔴 kang 的原話：「滑鼠靠近那一個點…會變得略大…讓我可以準確選到即可」
  ⭐ 跟點邊面、鋼筆的 hover 同一種感覺 —— ⛔ 不換顏色，只變大。

【依賴哪些事實】⛔ 每條都填了出處
  1. 切點是一批 `THREE.Points`，`size: 11`、`sizeAttenuation: false`
     ⇒ **`size` 整批共用**，改它會每一顆都變大
     【已查證 · `scene.js` 的 `_buildLineOverlay()` dots 那一段】
  2. 🔴 **誰在用 `setKnifePreview`**（新鐵律：先問誰在用它）：
     只有 `main.js` 的 `drawKnifePicks()` 與一筆畫預覽兩處
     【已查證 · grep `setKnifePreview|clearKnifePreview`】
  3. 切點的真相是 `main.js` 的 `knifePicks`，元素有 `.world`
     【已查證 · `main.js`】
  4. 觸控放寬的現成值 `EDGE_GRAB_PX_TOUCH`【已查證 · `select.js`】
  5. `sel` 在第 56 行、`drawKnifePicks()` 在 981 行 ⇒ ⛔ 沒有 TDZ 問題
     【已查證 · grep 行號】

【會動到哪幾個檔】見最上面那三個 ＋ 兩份文件。

【怎麼知道做對了】
  ✅ 測試 2560 全過／兩端檢查全過／三個改過的檔 ESM 解析乾淨
  ⚠ **這一輪沙箱測不到任何一項** —— 全部是畫面與滑鼠的事。老實寫在這裡。
  ⏳ 真正的驗證是 kang 那 7 步。

════════════════════════════════════════════════
【① ⭐ 先畫互動圖，他確認之後才接完】

  kang：「你先做一個互動圖..我擔心你會錯意...」
  ⇒ 做了一張上下對照（上排＝現在、下排＝加了之後）的圖，
     他回「**對...就這樣即可**」才把程式接完。
  🔴 **這是這幾輪第二次靠一張圖擋掉誤解** —— ⭐ 值得繼續。

【② ⚠ ⛔ 完全不碰切點的位置】

  它只是**畫得大一點**，`knifePath()` 收到的東西**一格都沒變**。
  ⭐ **⛔ 這一點是刻意跟上一輪那個撤掉的東西區分開的**
  （上一輪動的是**落點**，所以踩到下游的規則）。

【③ 🔴🔴 差點漏掉的陷阱 —— 跟參考線高亮那次【同一個形狀】】

  重畫切點時畫面上那顆放大的會被清掉，
  **而 `select.js` 還記著「剛才亮的是第幾顆」** ——
  下一次移動游標算出同一個數字，「一樣就不重送」判定⛔ 不用送，
  **那顆就再也不會亮回來**。
  ⚠ 而且索引本身也可能**指向不存在的點**（取消最後一點之後）。

  ✅ `drawKnifePicks()` 開頭叫 `sel.resetKnifeHot()` ——
     **切點一有變動一定經過那裡**，所以只有一個呼叫點。
  🔴 **實測第 6 步就是專門驗這一項。**

【④ ⚠ ⛔ 沒有節流，而那是刻意的】

  切點通常只有幾個到十幾個，**⛔ 不隨模型大小成長**〔鐵律四問的正是這件事〕
  —— 跟 `nearestMarkableEdge()`（跟邊數成正比）⛔ 不是同一類。

【⑤ ⚠ 順帶跟 kang 講清楚的一個事實】

  他提到「有時候下錯點，也要能準確選到要刪除的點」——
  🔴 **刀具現在刪點只能「慢慢再點一次 ＝ 取消最後一點」**，
  ⛔ **沒有「挑中間某一點刪掉」的功能**。已當場講明，⛔ 免得他期待落空。
  ⏳ 要不要做「挑任意一點刪」是**另一件事**，他之後決定。

【⏳ 待實測】刀具切點放大 **7 步**（清單在 `STATUS.md`）＋ 平板同一批
  🔴 第 6 步最重要（取消最後一點之後，剩下的還要會亮）。

【⚠ 停下來報告的一件】
  `STATUS.md` 超過 276 警報線。⛔ 沒有自己動手砍。

【下一件】⏳ 等 kang 決定：① 刀具「挑任意一點刪」，
  ② 讓 `多點連接` 更好用，③ 或回對照表那 19 項重挑。
