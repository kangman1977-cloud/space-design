刀具：切點吸到角（kang 收回 09-01 的「不做」）＋ 修「閉合失敗會多出一個切點」

⛔ 動到 4 個檔：js/core/edit.js、js/view/select.js、js/main.js、index.html（＋測試）

════════════════════════════════════════════════
【第一件 · 修 bug：閉合失敗會多出一個切點】

  🔴 **kang 回報，我在線上版重現了，而且照出兩個毛病**：
     · `knifeCloseLoop()` 先 push 一份「起點副本」才去切；
       `knifeApply()` 失敗直接 return，**⛔ 沒有人把副本收回去**
     · 失敗那條路**⛔ 沒有叫 `updateBar()`** →
       工具列寫「切下去（2 點）」而實際有 3 個 —— **兩個數字對不起來**（鐵律三）

  【實證】方塊上放 2 個相鄰邊的切點 → 快點兩下 →
  「第 2 段連不起來：這兩個點是相鄰的」→ **切點數變 3**。

  ⚠ **⛔ 不是「移動切點」那一輪造成的** —— 從 `knifeCloseLoop()`
     寫出來那天（2026-08-26）就在，**閉合失敗本來就少見**。
  ⭐ **通則**：**試失敗的那條路一定要把自己收乾淨**，
     ⛔ 不可以留半成品給下一個動作。

════════════════════════════════════════════════
【第二件 · 刀具「吸到角」—— 🔴 kang 收回他自己 09-01 的「不做」】

  ⚠ **⛔ 這一次的理由跟上一次完全不同，⛔ 不要混在一起讀**：
  · 09-01 撤掉的是**錯的做法**（在角上插一個新點 → 長度 0 的線 → 每次都切不下去）
  · 09-01 拍板不做，用的判準是他自己立的「**⛔ 不要跟多點連接重覆**」
  · 🔴 **今天他收回那個判準**，理由是：
    「某些狀況下...刀具切點還是會切在角點位置」

  ⭐ **新論據（09-01 當時不存在）**：
     **`多點連接` 做不到「一條路徑裡混著『邊上的點』和『角』」** ——
     它只連既有的角。而刀具是**一條路徑，中間可能剛好經過一個角**。
     ⇒ 定位其實⛔ 沒有模糊。

【依賴哪些事實】⛔ 每條都查證過

  1. 正解是**重用既有的角**，⛔ 不是插新點 —— 已實測乾淨
     （ok=true｜頂點 9、面 7、χ=2、體積一格沒變｜長度 0 的邊：0）
                          【已查證 · 規格\建模器-點線面編輯.md「✘ 刀具⛔ 不做吸到角」】
  2. 🔴 `knifePath()` 的 `points` 開頭就是**原本的頂點**，
     而 `rebuildWithInserts()` 回傳的 `remap` **涵蓋原本的索引** ——
     ⇒ 「重用一個既有的角」只要 `order.push(k.v)`，⛔ 完全不碰 `ins`
                          【已查證 · js/core/edit.js:2544, 2472, 2630】
  3. 擋端點那道（`len*s < PLANAR_TOL_CM`）**要留著** ——
     它擋的是「插新點在端點附近」，跟「重用既有的角」是兩件事
                          【已查證 · js/core/edit.js:2565-2572】
  4. `pickEdgePoint()` 目前把落點投影到邊上，回 `{obj,a,b,p,world}`
                          【已查證 · js/view/select.js pickEdgePoint()】
  5. 螢幕距離的容許值已經有一對：`EDGE_GRAB_PX` 14 / `_TOUCH` 26
     —— ⛔ 不新定一個值　　　【已查證 · js/view/select.js:102-103】

【kang 拍板的四件】

  ① **⛔ 不做開關，永遠開著**（上方那一排已經 5 顆了）
  ② 🔴 **`吸中點` 開著（或按著 Shift）→ ⛔ 不吸角**
     ⭐ **他的理由比我的好**：`吸中點` 是使用者**明講「我現在要中點」**，
       ⛔ 不是建議 —— 再把它拉到角上就是**安靜地改掉使用者要的東西**（坑第 11 條）。
     ⚠ 而且兩者**⛔ 永遠不會在同一個位置打架**（中點在正中間、角在兩端）——
       只有「哪一條規則生效」要定，⛔ 沒有距離要比。
  ③ 吸到角之後**還可以再拖走**（拖出半徑就回到邊上）
  ④ `吸中點` 那顆**⛔ 不改名**

【怎麼知道做對了】

  · 4 道機械檢查全跑
  · 🔴 **新增測試**：拿一個既有的角當切點 →
    ok、χ=2、體積精確不變、**長度 0 的邊 ＝ 0**、**頂點只多 1 個**（⛔ 不是 2 個）
  · ⚠ **既有那一項要一起改**：「太靠近角落要擋」那則的**訊息**會變
    （⛔ 不能再說「想從角落切就用多點連接」）—— 🔴 **兩端要一起動**
  · 上線後自己走一遍：拖到角／拖走／吸中點開著時⛔ 不吸角／切下去
