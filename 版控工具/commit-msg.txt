物件模式接上「方向：世界／物件」＋ 順手修掉那一排賴著不走的 bug

動到 5 個檔（＋commit-msg 自己）：程式 3 個、文件 2 個
　程式：建模器/js/view/select.js、建模器/js/main.js、建模器/index.html
　文件：STATUS.md、規格/建模器-點線面編輯.md

════════════════════════════════════════════════
【這次要做什麼】

  kang 的兩個問題（2026-09-01）：
  ① 物件轉了角度，方向能不能跟著一起轉？
  ② 能不能繞點邊面轉，不要繞中心？

  **這一輪只做 ①**（kang 拍板「先接方向」）。② 是下一輪。

  🔴🔴 **查到的事實比他問的更前面：那一排本來就不該出現在物件模式。**
  `toggleEditMode()` 有 `$('gEditXf').hidden = !on`，
  而 **`exitOtherModes()` 收了編輯模式卻沒收那一排**。
  ⇒ 「先進編輯模式 → 直接按貼合」那條路，整排留在畫面上，
     而且按下去完全沒作用。**kang 的截圖二正是貼合亮著。**
  ⭐ 所以他的需求的起點，是一排不該在那裡的按鈕。

════════════════════════════════════════════════
【依賴哪些事實】

  1. 物件模式寫死世界方向          【已查證 · select.js:2337】
  2. `setEditSpace()` 只改 `editSpace`，
     物件模式下什麼都不做           【已查證 · select.js:2895-2903】
  3. 那四顆的 hidden 由 `gizmoOff()` 管，
     物件模式 ＝ 沒選到物件才收      【已查證 · main.js:4087-4089, 4558-4559】
  4. **但外層 `gEditXf` 預設 hidden**，
     只有 `toggleEditMode()`／刀具兩處會開 【已查證 · index.html:929、main.js:1846,1868】
  5. `exitOtherModes()` ⛔ 沒有收 `gEditXf` 【已查證 · main.js:915-921】
  6. 🔴🔴 **這一條我第一版寫錯了，⛔ 是推論**：
     原本寫「貼合／分片那兩條路⛔ 沒呼叫 `updateBar()`」——
     **⛔ 錯**。`setXxxMode()` 每一支都 `_refresh()` → `hooks.onChange`
     → `updateBar()`【已查證 · select.js setEditMode/setMateMode 本體
     ＋ main.js:64 那行 hook】。
     ⭐ **真正的病因**：`updateBar()` **只管那一排裡面的按鈕，
     ⛔ 不管外層容器 `gEditXf`**，而容器只有 `toggleEditMode()` 會設
     ⇒ **裡面收了、殼還在**。
     ⚠ 我當初**只看 `toggleMate()` 表面**就下結論，⛔ 沒追進 `setMateMode()`。
  7. XYZ 三個字的方向**已經**照 `tc.space` 處理，
     ⛔ 這次不用動它                【已查證 · select.js:688-689】
  8. 物件模式拖完是把 node 的 pos/rot/scale
     抄回 obj，**跟 space 無關**     【已查證 · select.js:706-714】
  9. 縮放模式永遠用物件本地軸
     （TransformControls 寫死的）    【已查證 · select.js:683-684】

  ⛔ 沒有一條是【還沒查】。

════════════════════════════════════════════════
【會動到哪幾個檔】

  · `js/view/select.js` — `_refresh()` 物件模式那段；`setEditSpace()`
  · `js/main.js` — 新增 `syncEditXfRow()`；取代散在三處的手動開關
  · `index.html` — ⛔ **⛔ 不是改 title**（title 與按鈕的字都是 JS 動態換的）：
    只加了兩則註解 ＋ 給「中心」那個標籤一個 `id="pvLbl"`
    （⚠ **標籤⛔ 沒有 id 就收不掉** —— 不收會留下孤零零的「中心」兩個字）

  🔴 **`syncEditXfRow()` 是「只有一條路」的做法**〔坑第 31 條〕——
  那一排該長什麼樣只寫這一支，⛔ 不再讓三個地方各自去對齊。

════════════════════════════════════════════════
【怎麼知道做對了】

  ⚠ **鐵律五：先問「kang 驗得出這個結果嗎」** ——
  驗得出來，而且⛔ 不必看座標：**把方塊轉 30 度，按「物件」，
  三根箭頭跟著歪 30 度**，拖一下就是沿歪掉的方向走。

  · 方塊轉 Y 30 度 → 按「物件」→ 紅箭頭（X）不再指世界 X
  · 拖紅箭頭 → 位置的 X 與 Z **兩個都會變**（沿物件的軸走）
  · 按回「世界」→ 紅箭頭回到世界 X，拖它只有 X 變
  · **進編輯模式 → 按貼合** → 那一排**不再賴著不走**（本輪順手修的）
  · 四道機械檢查全過

════════════════════════════════════════════════
🔴🔴 【kang 實測照出來的第二個洞 —— 同一輪一起修】

  **第 8 項⛔ 沒過**：按了「貼合」之後，「方向」那兩顆**還在**，
  而貼合模式下 gizmo 早就 `detach()` 了 ⇒ **按了完全沒作用**。

  **病因**：`gizmoOff()` ⛔ 只抄了 `_refresh()` 掛 gizmo 條件的**前半**——
  真正的條件是 **`node && !this.inPickMode`**。
  ⚠ **⛔ 這不是本輪弄壞的**：以前那一排在物件模式下反正 hidden，
  **看不出來**；物件層級的方向一接上就浮出水面。
  ⭐ **同一個病的第二個受害者是「吸附」那四顆**（一起好了，因為問同一支）。
  ⚠ **它自己的註解就寫著「條件要跟 select.js 一模一樣」而它沒做到**
  —— 兩個地方各記一份同一條規則的下場（坑第 31 條）。

  ⚠ **我開的清單第 8 項【預期值的理由寫錯了】**：
  寫「離開編輯模式所以整排不見」—— ⛔ 錯。離開編輯模式若還選著物件，
  **方向那半應該留著**（物件模式下它有作用）。
  整排該不見的正確理由是「**進了點選模式**」。
  〔鐵律五那條的又一次：開清單之前⛔ 不只要問「驗得出來嗎」，
  　還要問「**我寫的理由站得住嗎**」〕

  ✅ **`exitOtherModes()` 結尾那個 `syncEditXfRow()` 加了又拿掉**：
  多餘（每條路都會經過 `updateBar()`），**而且時機是錯的**
  —— 它跑在 `setMateMode(true)` 之前，那時 `inPickMode` 還是 false。
