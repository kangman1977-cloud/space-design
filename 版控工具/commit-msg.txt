日誌 V8.4：切一刀第 5 項驗過了，八項全通過（只動文件，程式沒動）

只改 PROJECT_LOG.md 與 規格\建模器-量測.md，一支程式都沒碰。
測試項數一個都沒動（1574 → 1606 項那條鏈維持原樣）。

【這一輪解掉什麼】
「切一刀」實測第 5 項（把物件轉個角度再切，位置對不對得上打的數字）
原本卡著，日誌寫的是「kang 沒有辦法量，量測機制他會另外提」。

那個前提已經消失 —— 上一輪的「量測第 1 步」就把它解開了，
而且一行程式都不用改。

【怎麼驗的】
方塊 60×45×40、位置 Y 22.5、繞 Y 轉 −30 度，
切一刀軸 X、打 5 → 新的一圈邊自動選中 → 看編輯造型面板。

🔴 判準要兩個一起看：
     重心 X ＝ 5.00   平面在你打的位置
   ＋ 外框 X ＝ 0.00   整圈確實躺在同一個平面上

⚠ 只看重心會被騙 —— 切歪了的話左右對稱抵消，重心 X 照樣是 5。
外框 X ＝ 0 才是「整圈共面」那句話的機械斷言。

【⭐ 四個數字互相對得起來，那比判準本身更值錢】
  範圍 X ±35.98   手算 ±(30cos30 ＋ 20sin30) ＝ ±35.98
  外框 Y  45.00   方塊高 45，切平面平行 Y 軸
  外框 Z  46.19   截面 z 從 −20.21 到 25.98 ＝ 46.19
  中心 Z −20.21   「最後選的」那條垂直邊，z ＝ −20.21

四條不同的路（範圍提示、量測外框、量測重心、手算），
沒有一個是從另一個推出來的，卻全部吻合。
worldAxisPlane() 的本地／世界換算是對的。
＝ 鐵律三「讓兩個數字互相對得起來，錯誤才會自己現形」。

【💡 解法的形狀值得記】
那一項不是靠「補一個新功能」解開的 ——
既有的面板本來就印得出來，只是沒有人想到拿它去驗。
⛔ 遇到「驗不出來」先查現有面板印了什麼，不要先排一個新功能。

【🔴 留著沒刪的教訓（前提沒了，但它仍然成立）】
當初開這一項時 kang 驗不出來，而它偏偏是唯一「錯了也看不出來」的
那一項 —— 切錯位置形狀完全正常、體積也對（坑第 17 條）。
⛔ 下次開實測清單，每一項都要先問「使用者驗得出這個結果嗎」，
跟「使用者做得出這個輸入嗎」是同一條的兩半。

【改了哪五處】
  檔頭版本            V8.3 → V8.4，補一句順帶解開第 5 項
  「先讀這段」八項表   第 5 列 ⏳ 待驗 → ✅ 已驗
  表下面那一整則       改寫（前提消失，三條教訓搬進新版）
  待辦「任意切線」那則  七項通過、在等量測機制 → 八項全通過
  規格\建模器-量測.md   第 2 節加指標，⛔ 不抄第二份（落點規則）

【⚠ 看 diff 才發現的一個小漂移，順手修掉】
「這張清單一定要放在日誌裡」原本緊接在八項表底下。
中間插進一整則之後，「這張清單」會被讀成四個數字那張表。
→ 改成「上面那張八項實測清單」，指名道姓。
日誌的讀者是 AI，這種漂移正是「讓下一個人很有信心地答錯」那條。

【⏳ 還沒做的（不變）】
  量測第 2 步  把數字畫到 3D 畫面上（只有選到的才顯示）
  量測第 4 步  量兩個元素的距離 ＋ 圓的半徑／弧長／弦長
  平板版面     kang 手邊暫無平板
  下一個大缺口 在一個面上加一條線（連接兩點 J → 刀具 K）
