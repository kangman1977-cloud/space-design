修：`原點搬到⋯` 按下去之後那個字破格（kang 截圖）

動到 2 個檔（＋commit-msg）：建模器/js/ui/toolbar.js、規格/建模器-編輯與組裝.md
⚠ **⛔ 只改一個字串** —— 功能一行都沒動。

════════════════════════════════════════════════
【這次要做什麼】

  ✅ **kang：「驗收OK…但是點完『原點搬到』後的顯示內容破格」**（附截圖）。

  第一版按下去之後那個字是
  **「⋯ 點模型上的一個角／邊／面（再按一次取消）」—— 二十個字**，
  右側面板放不下，**撐破版面**。

════════════════════════════════════════════════
🔴🔴 【判準⛔ 不是「感覺短一點」，是量過的】

  > **這個面板上既有的按鈕最長 7 個字**
  > （`轉成可編輯網格`／`加厚後轉成網格`）⇒ **⛔ 不要超過它。**

  ⚠ **我第二版寫「⋯ 點一個角／邊／面」，那還是 9 個字** ——
  **⛔ 一樣會破**，是量完既有按鈕才發現的。
  ✅ 定案：**「⋯ 請點模型」**（5 字，比最短的 `原點置中` 還短一點）。

  ⭐ **⛔ 資訊不必塞進按鈕**：「點什麼」與「怎麼取消」
  講在 `title` 與按下去跳的提示裡 ——
  按鈕只要短到看得出「**現在在等你點**」就夠了。

【依賴哪些事實】

  1. 面板既有按鈕最長 7 字               【已查證 · 掃 `_rowBtn()` 全部字串】
  2. `title` 與 toast 已經講了「點一個角、一條邊或一個面」與「再按一次取消」
                                        【已查證 · toolbar.js 同一段】

【怎麼知道做對了】

  · kang 按下去 → 那一行**⛔ 不再撐破版面**
  · 功能完全不變（⛔ 只改了顯示的字）
  · 四道機械檢查全過

【⚠ 規則已經寫進規格檔】

  `規格\建模器-編輯與組裝.md`「原點搬到你點的地方」那一節末尾：
  **右側面板的按鈕字⛔ 不可以超過 7 個字**，含實證與那兩個破格的版本。
