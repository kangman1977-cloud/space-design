鋼筆 第 3 階段（二）：做洞

kang 2026-08-30 拍板「② 做洞可以做」＋「第二條路徑兩邊都能畫」。
動到 4 個檔：profile.js / prim.js / select.js / main.js ＋ index.html
＋ tests/run.mjs。測試 2442 → 2449 通過 0 失敗。

⚠ 【③ 開放與封閉（牆／板）這一輪⛔ 沒做】——
   它要全新的幾何建構（開放的線走不了 extrudeMany），
   而做洞已經完整可測。一次塞兩件會讓實測清單變長，
   也違反「一輪一件、實測結案再下一件」。

════════════════════════════════════════════════
【🔴 ① 做洞：三個接點，⛔ 幾何一行都沒新寫】

  ① profile.js 的 classify() 改成 export
     ⛔ 不在 prim.js 重寫一份 —— 判「哪一圈是洞」跟匯入線稿是同一件事。
     ⭐ 相依方向查過：profile.js 沒有 import 回 prim.js，⛔ 不會循環。

  ② pen() 補算 polyArea()
     🔴 classify() 一定要有 area。少了它每一條都會被當成外框，
        外 40×40 ＋ 內 20×20 會得到體積 10000（兩塊疊著），⛔ 不是 6000。

  ③ pen() 把結果組成 { pts, holes } 再餵 extrudeMany()
     ⭐ extrudeMany 帶 holes 本來就做得出洞（2026-08-29 沙箱驗過）。

  ⭐ 判準是【幾何】⛔ 不是繞向也⛔ 不是順序：
     巢狀深度偶數 ＝ 實心、奇數 ＝ 洞。
     測試第 ③ 項就是把順序顛倒過來，答案必須一樣。

【🔴 ② 第二條路徑：kang 要「兩邊都做」，而我讓它只有一條路】

  他選的是「畫的時候能接著畫」＋「事後進編輯路徑也能加」。
  ⚠ 那個選項我標了代價：兩條路做同一件事（坑第 31 條）。

  ✅ 解法：鋼筆存的從「一條路徑」改成
     【一疊已完成的 _penDone ＋ 正在畫/改的那一條 _pen】。

     ⭐ 為什麼這樣分，⛔ 不是存成陣列加索引：
        既有的每一支（_penAddAnchor／penUndo／命中測試／預覽）
        都是對 _pen 操作的 —— 保持「目前這條就叫 _pen」，
        那些【一行都不用改】。要改別條就把兩者交換（_penSwitchTo）。

     ⭐ 於是「加一條」這顆鈕畫的時候與事後按，走的是【同一支】。

  ⚠ 改點模式下【點到哪一條就改哪一條】，⛔ 不必先選哪條。
     命中測試的做法是「暫時把 _pen 換過去再問一次」——
     ⛔ 不複製第二份命中測試，問完一定換回來。

  ⚠ 其他那幾條也要畫出來 —— ⛔ 不畫的話按了「加一條」
     前一條會整個消失，使用者只會以為它不見了（坑第 21 條）。

【③ 介面】

  工具列多一顆【加一條】（鋼筆模式下一直看得到，
  目前這條不到 3 個點就變灰）。
  ⛔ 不必先宣告「這條是不是洞」—— 畫在裡面就是洞，畫在外面就是第二塊。

════════════════════════════════════════════════
【🔴 怎麼知道做對了】

  ✅ 測試 2442 → 2449 通過 0 失敗（新增 7 項，全部可以對答案）
       一條          8000  ＝ 40×40×5
       外 ＋ 內      6000  ＝ 8000 − 2000
       順序顛倒      6000  ← 守「⛔ 不看繞向、⛔ 不看順序」
       兩塊分開     16000
       兩塊 ＋ 洞   14000
       ＋ 兩項「仍然封閉」（不封閉的話 3D 列印會擋、體積也沒有意義）

  🔴 故意寫錯一次，確認測試抓得到：
     把 area 全部給同一個值（＝ 誰包誰判不出來）
     → 當場失敗在正確的三項（外＋內、順序顛倒、兩塊＋洞）
     ✅ 還原後 md5sum -c 通過

  ✅ 模組圖真的載入　✅ 目次產生器 --all

════════════════════════════════════════════════
【🔴 AI 上線先驗，八項驗到七項 ＋ 抓到一個介面 bug】

  ✅ ① 加一條：0/2 個點是灰的、3 個點變可按
  ✅ ② 按下去 → 提示對，而且【第一條還留在畫面上】
  ✅ ③ 收尾提示：「2 條路徑、8 個點」＋「包在裡面的那條已經變成【洞】」
  ✅ ④ 對照組：實心 270811.6 vs 有洞 258108.6 →【洞真的挖掉了】
  ✅ ⑤ 3D 列印：綠色可以列印（有洞⛔ 不代表不封閉）
  ✅ ⑥ 順序顛倒：252429.2 vs 252429.2 →【差 0，精確相同】
  ✅ ⑦ 兩塊分開：94865.2 → 189730.4【正好兩倍 ＝ 相加，⛔ 不互挖】
  ✅ ⑧ 事後加洞：94865.2 → 81997.5

  🔴 抓到一個介面 bug 並當場修掉：
     按了「加一條」之後，按鈕還停在「鋼筆 4」——
     而目前這條明明是 0 個點。
     ⛔ 病因是【按鈕文字有兩個地方在設】（onPenAdd 與 updateBar）——
        坑第 31 條。✅ 統一交給 updateBar()，兩條以上時掛「·N條」。

  ⚠ 老實記：⑥ 第一次量出來【差 5679】，我差點當成 bug。
     ⛔ 病因是我按過「3D 列印」，那個面板讓版面重排 ——
        canvas 從 t:426/h:391.3 變成 t:390.9/h:426.4，
        兩組用了【不同的 rect】去算座標，畫出來的形狀根本不一樣大。
     ⭐ 一氣呵成重測就是差 0。
     🔴 這是同一類錯誤的第三次：【跨階段沿用座標】。

【🔴 順便修掉一個我自己設計壞的實測項】

  清單第 4 項原本寫「自己乘得出來：外框 40×40、內框 20×20」——
  ⛔ 那是錯的：鋼筆是用滑鼠畫的，kang 畫不出剛好 40×40，乘不出來。
  ✅ 改成【對照組比大小】，而且兩種錯法方向剛好相反：
       有洞的比較小 ＝ 對；有洞的比較【大】＝ 第二條被當成疊上去的第二塊。

【⏳ 待實測】做洞 八項（清單在 STATUS.md）

【⏭ 下一件】③ 開放與封閉：甲 加厚成板 ／ 乙 立成牆
   （丙 參考線 kang 已決定排到後面 —— 它要一個「⛔ 不產生網格」的新概念）
