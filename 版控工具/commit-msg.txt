量測讀數改到畫面左下角 —— kang 實測退回「數字擋住 XYZ 拉桿」

kang 實測 0d10829 當場退回顯示位置。動到 6 個檔：
建模器/js/core/measure.js、js/view/select.js、js/main.js、index.html、
tests/run.mjs、規格\建模器-量測.md（＋ PROJECT_LOG.md、HISTORY.md）。
測試 2112 → 2150 項，失敗 0。

【🔴 kang 退回的那件事】

  他的原話：「顯示位置..目前顯示集中在 XYZ 控制軸..這樣很難選」
  　　　　　　「我的意思是顯示的數值座標面積..這些數值..不是選單」

  ⚠ 病因是【設計上必然】，⛔ 不是巧合：
  那串字站在選取的重心上，而 gizmo 掛的就是同一個點
  （兩邊都借 elementCenter()）—— 它們一定會搶同一個位置。
  結果是數字擋住拉桿、拉桿擋住數字。

  → 讀數改到【畫面左下角】，FPS 那塊的右邊（#measureBox）。
  ⭐ 那塊【不指向任何位置】，所以它擋不到任何東西。

【⛔ 這跟刀具第一版那個坑不一樣，要講清楚】

  index.html 的 #knifeLine 墓碑註解寫著「畫在螢幕上，一轉視角就對不上」。
  🔴 判準是【這個東西指不指著模型上的某個地方】：
  　　指著 → 得畫進 3D 場景（刀具的預覽線、'each' 那一套）
  　　不指著 → 可以是固定的 HTML（FPS 那格、這次的讀數）
  ⛔ 不要把這次的改動讀成「所以量測不能畫在螢幕上」。

【⏭ 「每一個」暫時收起來，⛔ 不是不做】

  kang：「每一個先暫時收起來...我思考一下還有甚麼方式可以呈現..
  　　　　會在跟你說想法」

  measureLabels() 的 'each'、scene.js 那一整套 Sprite、以及它們的測試
  【全都留著】，setMeasureMode('each') 也還收得下 ——
  只是目前沒有任何介面在傳它。

  🔴 ⚠ 這代表「寫好了而使用者按不到」（那正是 pushFace() 那個坑），
  所以三個地方都寫著它在等什麼：日誌待辦、index.html 那顆按鈕的註解、
  setMeasureMode() 的註解。⛔ 不可以安靜地留著。

  → 剩兩段就是開關，所以下拉改成按鈕（跟旁邊「顯示點」同一個作風，
  　 平板也比兩選項的下拉好按）。

【左下角那塊寫什麼】

  1 條邊 ／ 長度 60.00 cm ／ 重心 X … Y … Z … cm
  4 條邊（共 4 個頂點）／ 總長 210.00 cm ／ 外框 … ／ 重心 …
  1 個面 ／ 面積 2700.00 cm²　周長 210.00 cm ／ 重心 …
  1 個點 ／ 座標 X … Y … Z … cm　← ⛔ 不寫「重心」，一個點的重心就是它自己

  ⛔ 數字不在這裡另算一份：measureLabels('total') 產生整串字，
  　 而它問的是 measureSelection() —— 跟右邊面板同一個來源。
  ⚠ 沒東西可寫就整塊 hidden，⛔ 不留一個空框。

【⚠ #hud：兩塊包成 flex，⛔ 不是各自 absolute】

  原本 #status 自己 absolute。要讓兩塊並排而且【永遠不重疊】，
  給第二塊寫死一個 left 值辦不到 —— #status 的寬度會隨內容變
  （物件名字一長就撞上去），而那種撞法【只是擠在一起，⛔ 不會有東西報錯】。
  pointer-events:none 一併搬到 #hud，⚠ #status 那段「⛔ 不要加 title」的
  註解也跟著改了（⛔ 不可以讓舊註解變成謊話）。
  加了 flex-wrap：兩塊約 445px，窄畫面換行往上長，⛔ 不會被切掉。

【🔴 分層：select.js ⛔ 不碰 DOM】

  它只把字交給 hooks.onMeasure，由 main.js 的 updateMeasureBox() 寫進 HTML。
  ⭐ 走 hook ⛔ 不是讓 main.js 自己去問，是因為【重畫的時機只有 select.js 知道】
  （選取變動、清空選取、換模式）。

【機械斷言】

  ★★★ node tests/run.mjs → 通過 2150 失敗 0（這一輪淨增 10 項）
  ★★  總計那一塊【逐行驗】：選到幾個／尺寸／外框／重心，四行各驗一次
  ★★  單選一個點 → 兩行，而且第二行是「座標」⛔ 不是「重心」
  ★★  多選面 → 有總面積、⛔ 沒有周長（共用的邊會被算兩次）
  ★★  放大 2 倍 → 兩種模式的數字都跟著（一律世界座標）
  ★   ⛔ 不把整串寫死：最後一行是座標，寫死等於把它也綁進來
  ★   node --check 五個檔全過；CSS 大括號 126/126 配對
  ★   舊 id（measureMode / changeMeasureMode）全檔 0 個殘留
  ★   新 id measureHud / measureBox / hud ↔ main.js 接得上
  ★   esc() ⛔ 沒有跟既有函式撞名；@media 兩個區塊都沒動到 #status
  ★   檔尾 0a、編碼無亂碼

  ⚠ 「被呼叫但不存在的函式」那道檢查照樣誤報 isMarkable
     （它只找 ^function f，而那個是 import 進來的）——
     ⛔ 已知誤報，⛔ 不是這一輪改壞的。日誌檔頭那一欄記著。

【沙箱驗不了的部分（⏳ 待 kang 實測，桌機七項）】

  🔴 清單在 PROJECT_LOG.md「待辦」那一節（Grep：實測清單：標尺寸）。
  ✘ 舊清單（標在元素身上那一版）已作廢 —— 第 3、4、7 項整個不成立。

  🔴 第 2 項與第 7 項最重要：
     第 2 項 ＝ 看 XYZ 拉桿上⛔還有沒有數字疊著（就是這次退回的那件事）；
     第 7 項 ＝ 取消選取之後左下角那塊會不會賴著不走
     　　　　　（clearEditSel() 那條⛔不經過 _drawEditMark() 的路）。

  ⏳ 平板版面仍然沒驗。

【文件】

  規格\建模器-量測.md　第 2 步整節重寫（左下角、#hud、⏭ 'each' 收起來、
  　　　　　　　　　　　⛔ 這跟刀具那個坑不一樣）；識別碼補五個；
  　　　　　　　　　　　「去讀哪一支」四列重排
  PROJECT_LOG.md　　　 待辦那條與實測清單整條換掉（舊清單標 ✘ 作廢）；
  　　　　　　　　　　　檔頭覆蓋一條（標在元素上是錯的）。V12.9 → V13.0
  HISTORY.md　　　　　 被擠出檔頭的「非流形邊比破洞難只對一半」逐字搬入
