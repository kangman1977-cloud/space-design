線上實測回寫：隔一個選一個 六項通過 ＋ 平板版面量到數字 —— 0 行程式

只動 STATUS.md（+42 −1）。⛔ 建模器一行都沒動，測試仍是 2338。

🔴 這一輪的新東西是【驗證方式】，⛔ 不是功能：
   Cowork 內建瀏覽器可以開線上版（GitHub Pages），
   所以「沙箱連畫面都驗不了」那道牆破了一半。

────────────────────────────────────────────────
【① 隔一個選一個：八項裡我自己驗掉六項】

  在 kangman1977-cloud.github.io 線上版實際操作，⛔ 不是沙箱推的。

  1 ✅ 圓柱 選一圈面 → 32 個面
  2 ✅ 隔 2 從 1 → 「32 個面 → 留下 16 個」，
       而且「刪除面」的說明同步變成「把選到的 16 個面拿掉」
  3 ✅ 刪除面 → 畫面上真的是一圈格柵（截圖看過）
       ★ 三角形 260 → 228，差 32 ＝ 16 個四邊形 —— 兩個數字對得起來
  4 ✅ 從第 2 個開始 → 一樣 16 個、一樣 228，格柵整體挪了一格
  5 ✅ 隔 3 → 11 個（32÷3 ＝ 10 餘 2）
  7 ✅ 全選邊 96 條 → 紅字擋下來「有的同時接到 6 個…
       先用『選一圈』或『選一條線』」（⭐ 訊息帶著出路）

  ⏳ 剩第 6 項（五角柱的接縫警告）與第 8 項（一圈邊 32 → 16）要 kang 驗。

  ⚠ ⛔ 這 ⛔ 不等於 kang 驗過 —— 我驗的是「數字與訊息對不對」，
     他要驗的是「這是不是他要的東西」。
  ⭐ 但「按下去該擋而沒擋」那一類的錯，⛔ 已經不必再花他的時間。

【② 平板版面：卡了很久的那一項量到數字了】

  | 視窗              | 工具列高 | 佔畫面高 | 換行 | 被切掉 |
  |------------------|--------|--------|-----|------|
  | 1920×1080 桌機    | 229 px | 21%    | 6   | 否   |
  | 1024×768 平板橫   | 391 px | 51% ←最糟| 10  | 否   |
  | 768×1024 平板直   | 405 px | 40%    | 11  | 否   |

  ⭐「會不會被切掉」有答案了：⛔ 不會（flex-wrap 正常，按鈕全在）。
  🔴「會不會吃掉太多畫面高度」也有答案了：橫向平板吃掉一半。

  ⚠ ⛔ 但這 ⛔ 不等於刀具第 3 項結案 —— 量的是版面，好不好用要 kang 看。
  ⛔ 第 1、2 項（單指畫線／兩指轉視角）仍然只能等真的平板，模擬器驗不了。

────────────────────────────────────────────────
【🔴 ③ 立一條新規則：線上版驗證前一定要強制重抓模組】

  【實證 2026-08-29】第一次開線上版，index.html 是新的（按鈕在），
  而 main.js 是瀏覽器快取裡的舊版
  → 按鈕永遠變灰、title 停在 HTML 寫死那句。

  ⚠ 症狀跟「disabled 條件寫錯」一模一樣，我差點回報成 bug。

  ⭐ ⛔ 沒有停在「應該是快取」那句推論（那正是 8/28 犯過的錯，
     當時被 kang 一句「我不要止血，我要知道原因」擋下來）——
     做法是：
       ① 抓線上的 main.js，確認裡面【有】canChecker  ← 證明伺服器是新的
       ② 逐檔 fetch(..., {cache:'reload'}) 重抓整個模組圖
       ③ 重新載入 → 症狀當場消失          ← 這一步才敢下結論

  🔴 規則寫進 STATUS.md「⏳ 待實測」那一節，
     ⛔ 因為以後每一次用瀏覽器驗線上版都會踩到。

────────────────────────────────────────────────
【文件回寫】
  · STATUS.md（238 行，上限 276）—— 唯一動到的檔

【⏳ 待實測】
  · 隔一個選一個 第 6、8 項（桌機）
  · 刀具三項輔助的平板【第 1、2 項】—— 卡平板不在身邊
    （第 3 項版面已量到數字，但好不好用仍要 kang 看）
