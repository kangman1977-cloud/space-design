刀具：指到哪一個切點就刪哪一個 ＋ 一顆「退一步」（kang 提的，三件都他拍板）

⛔ 幾何一行都沒改。動到 3 個檔：index.html、js/main.js、js/view/select.js

════════════════════════════════════════════════
【這次要做什麼】

  kang 2026-09-01：「下錯點要能選到要刪除的點」。
  ⚠ 現在只刪得掉**最後一點**，而且判定是**世界座標 0.5 公分**。

  ⭐ **缺的那一半上禮拜已經做好了** —— 「游標靠近的切點會變大」那一輪
     算出來的索引（`_knifeHotIndex()`）正好就是「你指的是第幾顆」。

【依賴哪些事實】⛔ 每條都填了出處

  1. 切點只有一份真相 `knifePicks`，`select.js` ⛔ 不存第二份
                                          【已查證 · js/main.js:965, 146-149】
  2. hover 算「游標離哪一顆最近」＝ `_knifeHotIndex()`，比的是**螢幕 px**，
     沿用 `EDGE_GRAB_PX` / `EDGE_GRAB_PX_TOUCH`
                                          【已查證 · js/view/select.js:1936-1956】
  3. 現在的「再點一次取消」只認**最後一點**，判定 `distanceTo < 0.5`（公分）
                                          【已查證 · js/main.js:1541-1549】
  4. 快點兩下（`info.double`）**排在最前面**攔截，⛔ 不可以動它的順序
                                          【已查證 · js/main.js:1524】
  5. 切點一變就 `sel.resetKnifeHot()`，呼叫點只有 `drawKnifePicks()` 一個
                                          【已查證 · js/main.js:987 · select.js:1977】
  6. 上方那一排刀具鈕（`吸中點`／`取消`）⛔ 不佔左側直條最後一顆空間
                                          【已查證 · index.html:787-788】
  7. 鋼筆已有「自己的退一步」先例 `penUndo`，⛔ 不是全域復原
                                          【已查證 · index.html:846 · js/main.js:696】
  8. 切下去之後才 `commit()`；切點本身**⛔ 完全不進復原清單**
                                          【已查證 · js/main.js:1601, 965】

【kang 拍板的三件（結果不唯一，⛔ 不是我決定的）】

  ① **快點兩下維持原樣** —— 不管指到哪一顆，快點兩下照樣是「閉合並切下去」
  ② **刀具那一排加一顆「退一步」**（乙案）—— 照鋼筆 `退一點` 的先例，
     把剛才刪掉的那一顆**插回原來的位置**（⛔ 不是接到最後面），可以連按
  ③ **判定範圍改成螢幕距離** —— 跟「變大」同一個範圍：
     🔴 **看到它變大，按下去就一定刪得掉**（現在的 0.5 公分拉遠就點不到）

【會動到哪幾個檔】

  · index.html      加一顆 `knifeUndoDel`；刀具 title 補「指到哪刪哪」
  · js/view/select.js  按下去時**現算**一次 `_knifeHotIndex()` 塞進 `info.hotIdx`
     🔴 ⛔ 不可以用 `_knifeHotIdx` 那個快取 —— **平板沒有 hover，它永遠是 −1**
  · js/main.js      `knifePick()` 換判定；加 `knifeDeleted` 堆疊與 `undoKnifeDelete()`；
     四個離開刀具的出口都要清掉那個堆疊

【怎麼知道做對了】⛔ 要能對答案的量

  · 4 道機械檢查全跑（動到 js/ 與 index.html → 模組圖、兩端、測試、目次）
  · 新增測試：刪中間一顆 → 剩幾個、前後接得起來、退一步插回**原索引**
  · 實測清單交給 kang（數字寫成他自己數得出來的）
