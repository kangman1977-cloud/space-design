隔一個選一個結案（八項全過）＋ 翻掉「不能開瀏覽器實測」那一條 —— 0 行程式

kang 回報「6 與 8 驗收 ok」→ 當場回寫結案。
動到 3 個檔：STATUS.md / PROJECT_LOG.md / HISTORY.md。
⛔ 建模器一行都沒動，測試仍是 2338。

────────────────────────────────────────────────
【① 隔一個選一個結案 —— 清單搬去 HISTORY.md】

  八項全過：AI 開線上版自己驗掉六項，kang 驗第 6、8 項。
  ⭐ 這是第一次由 AI 先驗掉大半的一批。

  照落點規則：STATUS 是替換式，結案就移出 →
  完整清單（含每一項的預期式子、AI 那六項的逐字提示與數字）
  逐字搬進 HISTORY.md「隔一個選一個：八項實測全過」那一節。
  STATUS 只留一行結案 ＋ 指標。

【🔴 ② 翻掉一條寫在協作規則裡的事實】

  舊版寫：「不能開瀏覽器實測 — 沙箱下載不到 Chrome，
           所以『畫面有沒有出來』永遠只能靠 kang 實際開啟確認」

  → 現在 Cowork 有內建瀏覽器，開得了【線上版】
    （3-上傳網路.bat 之後的 github.io），⛔ 但開不了本機 localhost。

  ⭐ 驗證的順序因此變了：
     舊：做完 → 等 kang 開
     新：做完 → 上傳 → AI 先驗一遍 → 剩下的才給 kang

  【AI 驗得了什麼 / 驗不了什麼】已列成表寫進權威版，重點：
     驗得了 = 按鈕狀態、title、提示訊息逐字、狀態列數字、
              版面（可模擬平板尺寸）、截圖看形狀
     驗不了 = 觸控手勢、好不好用、DXF/SVG/下載檔、本機 localhost

  ⚠ 舊版那句「AI 不能宣稱已驗證」⛔ 沒有被推翻，只是範圍縮小：
     【AI 驗過 ≠ kang 驗過】——
     AI 驗的是「數字與訊息對不對」，kang 驗的是「這是不是他要的東西」。

【🔴 ③ 同一條底下立一道新檢查：驗之前強制重抓模組】

  【實證 2026-08-29】第一次開線上版，index.html 是新的（新按鈕在 DOM 裡），
  而 main.js 是瀏覽器快取裡的舊版
  → 那顆按鈕永遠變灰、title 停在 HTML 寫死那句。
  ⚠ 症狀跟「disabled 條件寫錯」一模一樣，差點被回報成 bug。

  ⭐ ⛔ 沒有停在「應該是快取」那句推論（那正是 8/28 犯過的錯，檔頭第 1 條）。
     做法寫成三步，⛔ 走完第 3 步才敢下結論：
       ① 抓線上的 main.js，確認裡面有這次新加的識別字 ← 證明伺服器是新的
       ② 逐檔 fetch(路徑, {cache:'reload'}) 重抓整個模組圖
       ③ 重新載入 → 症狀當場消失

【④ 平板版面：刀具第 3 項量到數字（⛔ 但沒結案）】

  1920×1080 桌機 229px / 21% ・ 1024×768 平板橫 391px / 51% ←最糟
  768×1024 平板直 405px / 40%（3D 畫面只剩 399px）・ 三種都沒有被切掉

  ⭐「會不會被切掉」→ ⛔ 不會（flex-wrap 正常）。
  🔴「會不會吃掉太多畫面高度」→ 橫向平板吃掉一半。
  ⚠ ⛔ 不等於結案：量的是版面，好不好用要 kang 看。
  ⛔ 第 1、2 項（觸控手勢）仍然只能等真的平板。

【🔴 ⑤ 搬移的機械斷言第一版沒過 —— 我自己破了文件鐵律三】

  鐵律三：「搬移一律逐字搬 ＋ 機械斷言（行數／md5），⛔ 不做順手精簡改寫」

  第一版我搬 STATUS → HISTORY 時：
    · 在八項清單那張表【多加了一欄「誰驗的」】
    · 把「我驗到的」那張表【改寫成條列】
    · 把「你」順手改成「kang」

  → 機械斷言（把 STATUS 刪掉的每一行拿去 HISTORY 逐行找）
     【38 行裡 31 行不過】。

  ⚠ 內容其實沒少（改寫後的版本還多了逐字提示）——
     🔴 但「沒少」是我說的，而這條規則的用意，
        正是讓它【不必靠我說】。⛔ 這就是它存在的理由。

  ✅ 已退回重做：HISTORY 那一節現在分成兩塊 ——
     「逐字搬入的原文（⛔ 不要修）」＋「搬入之後補的（⛔ 這一段不是原文）」。
     重跑機械斷言：【38 行 0 行不過】。

  ⚠ 逐字搬進去的原文裡還留著「⏳ 沒驗」「我」「你」那些字眼 ——
     ⭐ 那是【搬入當下】的原文，⛔ 不是現況，
        所以在那一節開頭明講了這件事，免得下一個人讀到當成現況。

────────────────────────────────────────────────
【落點】
  · STATUS.md（201 行，上限 276）—— 結案移出、穩定參考加一行指標
  · PROJECT_LOG.md「協作規則 → 沙箱能做什麼」—— 規則的權威版（唯一一份）
  · HISTORY.md ＋ 重跑目次產生器 —— 清單逐字搬入（30 節 / 4443 行）

【⏳ 待實測】
  · 只剩「刀具三項輔助的平板【第 1、2 項】」—— 卡平板不在身邊

【下一件】
  · 增長／縮減選取 ← 選取那一格最後一項
    🔴 動工前要問 kang：「擴」是共用一條【邊】還是共用一個【點】
       （方塊上兩種一樣，球才分得出來：5 vs 9。Blender 用共用點）
