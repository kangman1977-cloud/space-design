回寫：加點／刪點十項全過結案 ＋ 兩條協作規則進日誌

kang 2026-08-30 回報「我也驗收OK」，並同意把兩條規則寫進日誌。
⛔ 程式一行都沒改。動到 3 個檔：
  PROJECT_LOG.md（兩條規則）／ HISTORY.md（搬入）／ STATUS.md（移出）

════════════════════════════════════════════════
【① 結案】

  ✅ 加點 ＝ de Casteljau 分割（曲線等價）
  ✅ 刪點 ＝ 最小平方擬合（誤差比「直接接」小將近 90 倍）
  🔴 ⏳ 待實測再次清空 → 第 3 階段剩下兩件都可以開了。
  ⏳ 平板仍然沒驗。

【② 逐字搬進 HISTORY.md，md5 對過】

  STATUS.md 第 53–81 行 → HISTORY.md 新的一節
  md5 = 657709dded9e15208b75aa1a38c1e2e5（兩邊相同）

  ⭐ 那一節多記了三件「測試／實測抓到我」的經過，⛔ 因為它們會再發生：
     一、「形狀一格都沒變」是【我寫錯的話】——
         病不在 penAddAnchor()，在 flattenPenPath() 的細分不對齊。
         ⚠ 而我的第一個診斷（怪 tol）也是錯的：四種 tol 全部精確不變，
            真正的變因是【形狀規不規則】。
     二、nearestOnCubic 的細化【追不上初始誤差】——
         ⛔ 不管加幾輪都追不上（步長衰減比誤差還快），要換三分搜尋。
         ⭐ 教訓：⛔ 不要只調參數就當作修好了，先確定病根是不是參數。
     三、判準要看【位置】⛔ 不看 t。

════════════════════════════════════════════════
【🔴 ③ 兩條協作規則進 PROJECT_LOG.md】

 一、「看完差異 ≠ 已經存檔」→ 協作規則 → 版本控制與檢查流程

    kang 回「1-看差異.bat..OK」是【看完了】，⛔ 不是【存好了】。
    【實證 2026-08-30】那一輪確認完差異就直接開了新功能，
    下一輪的差異裡混進上一輪的 PROJECT_LOG 改動，
    而 commit-msg.txt 已經被覆蓋 → 上一輪的四項實測結果
    差點被一段沒提到它的訊息 commit 掉。

    ⭐ 每次讀差異，第一件事是問【這幾個檔都是這一輪動的嗎】，
       ⛔ 不是只看「有沒有動到不該動的」。

 二、「強制重抓模組圖」那一條【被量掉了兩步】→ 沙箱能做什麼

    🔴 舊版寫的三步做法，2026-08-30 實測證明第 2、3 步不夠：

      fetch(cache:'reload') 逐檔重抓 ＋ location.reload()  ❌ 不夠
      換 HTML 的 query string（?v=…）                      ❌ 沒有用
      開一個全新分頁                                        ❌ 沒有用
      kang 按 Ctrl+Shift+R（硬重載）                        ✅ 唯一可靠

    ⭐ 決定性的數字是 performance 給的：
       載入的 encodedBodySize 60916 vs 伺服器 118549，
       而且 transferSize === 0 ←【那份是快取來的】。

    → 新做法：① no-store 抓檔案確認伺服器是新的（AI 驗得了的全部）
              ② 驗執行結果前先確認 transferSize !== 0，是 0 就⛔ 不下結論
              ③ 交付時一定要提醒 kang 硬重載
                 ⚠ 不提醒的話他會看到舊畫面、合理地回報「你沒改到」，
                    而那是快取在說謊（誤報比漏報更糟）

════════════════════════════════════════════════
【怎麼知道做對了】

  ⛔ 沒有可測的量 —— 這一輪只有文件回寫。
  ✅ 搬移的 md5 對得上（見 ②）
  ✅ 目次產生器 --all（PROJECT_LOG 與 HISTORY 都動了節，一定要重跑）
  ⛔ 另外兩道機械檢查不適用（沒改 js/、沒改建模器核心）

【⏳ 待實測】無

【下一件】鋼筆 第 3 階段剩下兩件（互不相依，要問 kang 先做哪一個）
  ② 做洞 —— 缺的不是幾何，是三個接點（假設已驗，見 PROJECT_LOG）
  ③ 開放與封閉 —— 🔴 結果不唯一，動工前一定要先問
