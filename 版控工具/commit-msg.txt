日誌整理成 V6.4：下一步是環切，並補上四個 commit 編號

只動文件，一行程式都沒改。程式在 6707622 已經存過而且 kang 驗過。

一、開場那段補三塊（都是「每次都要看」的東西）

1. 🔴 下一步是「環切」
   前提已備好（bake() 會把網格變成四邊形），ring walker 實測只有一行。
   ⚠ 但只驗過 ring 找得到，沒驗切下去之後的網格對不對。
   ⚠ 還有一個問題要先問 kang：切完那一圈新邊要不要自動選中。

2. 🔴 「把幾個面合併」這個題目的三條結論

   還原多邊形   ✅ 沒有按鈕，bake() 時自動做。畫面上看不出來是正常的
   壓平         ✅ 工具列「壓平」。⚠ 形狀會變
   溶解面       ✘ 明確不做 —— 實測偏離平面 0.9561 cm ＝ 可切容許值的 96 倍

   ⛔ 不要因為 Blender 有溶解面就補上。想把幾片變成一個面，正解是壓平。

3. ⛔ 不要再自己寫 for (let i = 2; i < vs.length; i++)（扇形三角化）
   那只對凸多邊形成立，而它曾經有 8 個出口，每一個都是同一個 bug
   （畫面、面積、STL、布林、展開面積都錯過）。
   全專案唯一的入口是 mesh.faceTriangles()。

二、待辦：環切那一條寫成「下次一打開就能動工」

寫進去的東西：
- 前提為什麼已經備好、ring 的實測數字（方塊 4 條閉環、圓柱 32 條閉環、
  水平邊 2 條）
- ⚠ 我們的頂點全是 3 價 —— Blender 的 edge-loop walker 走不了，
  環切用的是 edge ring 不是 edge loop，別再搞混
- ⏳ 動工前一定要先測的 5 項（χ、體積要精確不變、role/smooth 有沒有搬對、
  切在哪切幾刀、撞到非四邊形那半條 ring 合不合法）
- ❓ 要先問 kang 才寫得出規格的那一項

三、順序那一行同步

「環切 → 內縮 → 刪除面／溶解面 → 導角 → 合併頂點」
改成
「環切 → 內縮 → 刪除面 → 導角 → 合併頂點」
—— 溶解面已改成不做，那一行還留著舊的說法。

四、補上四個 commit 編號

PROJECT_LOG.md 三處、HISTORY.md 兩處（壓平那一節、三角化那一節）
全部補上 6707622。
