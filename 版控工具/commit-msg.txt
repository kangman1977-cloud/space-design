多選一起動 八項結案（kang 驗收）—— 清單搬 HISTORY

⛔ 程式一行都沒改。動到 3 個檔：HISTORY.md、STATUS.md、版控工具/commit-msg.txt

════════════════════════════════════════════════
【這次要做什麼】

  ✅ **kang：「我已都驗證OK」**（八項）→ 照日常流程第 6 步結案。

  · 清單**逐字搬、一字未改**：行數 **14**、md5 **a39a0ecd2a6fe0fa70a4274f711893b6**
  · `STATUS.md`「⏳ 待實測」清空 → **284 → 277 行**
    🔴🔴 **⚠ 仍然超過警報線 276 一行 —— 已停下來報告，⛔ 沒有自己動手砍。**
    ⚠ **⛔ 我先前預測「會回落到 272」是錯的**：那個預測**沒有算進
    這一輪自己加的 6 行**（「穩定參考」補的那條「模組圖檢查只驗解析階段」）。

════════════════════════════════════════════════
🔴🔴 【這一輪最該留下來的一條（權威版在 HISTORY 新那一節）】

  > **與其小心不要寫錯，不如換一個【寫錯也不會炸】的結構。**

  【實證】我在建構子裡寫 `v.scene.add(...)`，而 `const v = this.view`
  **在下面幾十行才宣告** ⇒ 暫時性死區 ⇒ ReferenceError
  ⇒ **整個網頁一行都不跑**，而**四道機械檢查全部放行**。

  ✅ **對策⛔ 不是再加一道檢查**，是把 `scene.add()` 改成
  「第一次要用才加」⇒ **建構子⛔ 沒有任何副作用** ⇒ 再寫錯也炸不掉模組。

【⚠ 順手修正的一條認知（已寫進 `STATUS.md`「穩定參考」）】

  🔴 **「模組圖真的載入」那道【只驗得到解析階段】。**
  【2026-09-01 量到】Node 載入 `main.js` 會在 **`OrbitControls` 那個
  import 就停下來** ⇒ **任何建構子根本不會被執行**
  ⇒ 執行期的錯（ReferenceError 那一類）它**一個都碰不到**。
  ⚠ 日誌檔頭 2026-08-28 那條說它能抓「重複宣告」—— **那是解析期**，
  ⛔ 不要因此以為它涵蓋執行期。

【怎麼知道做對了】

  ✅ 逐字斷言通過（14 行／md5 對得上，⛔ 不是靠眼睛比）
  ✅ 目次產生器 --all → HISTORY **56** 節
  🔴 **`STATUS.md` 284 → 277 行 —— ⚠ 還差 1 行才回到警報線以下。**
     **等 kang 決定，⛔ 不自己砍**（警報線的規則就是這樣定的）。
     可移出的兩塊（**兩塊在 `PROJECT_LOG.md` 都有權威版**）：
     · 「落點規則」**22 行** → `PROJECT_LOG.md` 第 17 節「文件分層與落點規則」
     · 「這個專案有哪些文件」**13 行** → `PROJECT_LOG.md` 檔頭的三層分工表
  ⛔ 測試／兩端／模組圖不用跑 —— ⛔ 沒動程式

【下一件】

  🔴 **原點指定：「原點搬到我點的地方」**（kang 認可的方向）。
  ⭐ **⛔ 不新增「基準點」狀態** —— 擴充**物件本來就有的原點**
  ⇒ 存檔、Undo、切換物件**全部免費**，箭頭也早就畫在原點上。
  ⚠ 要決定的只剩「在模型上點」那個模式怎麼進出。
