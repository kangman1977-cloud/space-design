⛔ 會過期的數字一律現算，不存 —— 總項數從文件裡全部拿掉

kang 2026-08-27 拍板。只整理文件，程式一行都沒動，測試重跑全過。
動到 3 個檔：PROJECT_LOG.md、規格/建模器-核心架構.md、
規格/建模器-測試與驗證.md。HISTORY.md 這次沒動。

【🔴 一、立了一條新規則：⛔ 會過期的數字一律現算，不存】

  > 測試的總項數，文件裡從此一處都不寫。

  病因：那個數字同時存在六個地方（日誌四處、核心架構兩處、
  測試與驗證一處），而它每加一輪功能就變一次。
  結果昨天查出來：規格檔停在 1231，實際 1973 —— 過期 742 項沒人發現。

  🔴 收回昨天那個做法：昨天我把它「收成一處」（其餘指過去）。
     那不夠 —— 剩下的那一處照樣會過期，這次過期的就是那一處。

  ⭐ 正解是一處都不留：
     node 建模器/tests/run.mjs 跑完本來就印「通過 N　失敗 0」。
     答案一直都在，我們只是另外抄了一份。

  ⭐ 這是這個專案自己示範過三次的同一招：
     尺寸「⛔ 現算，不存」／
     active 改成當場算出來的（沒有第二份資料就沒有東西會不同步）／
     圓弧擬合每次展開時現算（沒有東西要存、也沒有東西會過期）。
     〔坑第 31 條：與其讓兩條路對齊，不如換一個只有一條路的定義
      —— 這裡連一條都不留〕

  ⚠ ⛔ 不適用於「基準數字」：13800、108000、124/64、12.5664 那些不會變
     （會變就是改壞了），照樣留在 規格/建模器-測試與驗證.md。
     判準：這個數字改對了會變，還是改壞了才會變？
     前者不存，後者一定要存。

  全文寫在 PROJECT_LOG.md「文件分層 →
  ⛔ 會過期的數字一律現算，不存」。

【⚠ 二、「下游事實」原來存了三份，刪成一份 ＋ 升格】

  同一件事（kang 會用 AI 繪圖開 DXF／SVG）存在三個地方：
    1. 規格/建模器-展開與分片.md 第 370 節 ← 權威版，早就有
    2. PROJECT_LOG.md 檔頭摘要
    3. PROJECT_LOG.md 待辦區一整塊 ← 第三份

  → 待辦區那塊刪掉（16 行）。
  → 裡面兩句規格檔沒有、而且是跨功能的教訓，升格進
    「鐵律 → 一、動手之前 → 目的藏在下游」，收成一張表：

      | 拿到的下游事實 | 省掉了什麼 |
      | 會用 AI 繪圖開 DXF 或是 SVG（8/26） | ⛔ 沒問到就會去做「引線拉出去」那種大工程 |
      | DXF 會先進 Illustrator 才給 CNC（8/22） | 「板材尺寸上限檢查」整項取消 |

    🔴 兩條都是問出來的，不是推出來的，而且兩條指向同一件事：
       他的下游有 Illustrator。⭐ 這種事沒有任何規格文件會寫。
    ⚠ ⛔ 不要推得太遠：「列印」那顆按鈕還在，⛔ 不可以據此把字再往下調。

  ⭐ 又是坑第 33 條：教訓寫在別的功能底下就等於沒寫。

  連帶：待辦區從此只剩 20 項，全部是 [ ]，
  沒有核取方塊的條目一個都沒有了。

【機械斷言（全過）】

  ★★ 流水帳（A → B）一格都沒動：改前 4 對、改後 4 對，完全相同
     —— 這正是 2026-08-25 連續犯兩次的那個病（整批取代把歷史一起改掉）
  ★★ 差異檔 34 行刪除逐行看過，34 行全部是預期內的：
     版本號 1 行、六處現況數字 6 行、下游事實那塊 16 行、
     舊的「現況/歷史」規則段 3 行、昨天自己寫的說明改寫 8 行。
     ⛔ 沒有一行是意外刪掉的
  ★★ 全專案已無「現況總項數」；剩下的 1973 只有一處，
     是「測試 1954 → 1973 項」那個流水帳，該留
  ★★ 基準數字都還在：13800×3、108000×5、124/64×1、12.5664×3
  ★★ 待辦區 180 行 / 20 項全是 [ ]，無核取方塊的頂層條目 0 個
  ★★ HISTORY.md 這次完全沒動（不在 [1] 的清單裡，絕對行數仍 3300）
  ★★ 三個檔都過 iconv -f UTF-8 -t UTF-8
  ★★ node 建模器/tests/run.mjs 全部通過

【⏳ 報給 kang、還沒動的一件】

  檔頭那一大塊「這一輪做了什麼」（V11.6 那段）已經 200 行，
  而且每一輪都會再長一段。它本質是流水帳，HISTORY.md 才是它的家 ——
  這正是「開新對話時先讀這段」一直長回去的來源。
  要不要立個規矩（例如只留最近兩輪，其餘搬走），下次再談。

日誌版本 V11.5 → V11.6。
