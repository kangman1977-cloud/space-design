還原：刀具「吸到角」整輪撤掉 —— 它讓刀具【每一次都切不下去】
　　　＋把教訓升格成鐵律

動到 2 個檔（程式）：建模器/js/view/select.js／建模器/js/main.js
文件：規格/建模器-點線面編輯.md／PROJECT_LOG.md／STATUS.md

════════════════════════════════════════════════
【① 🔴🔴 為什麼撤回：下游明文擋掉，而我沒去讀】

  kang 實測時問「刀具的邏輯是什麼?有多點連接這功能嗎?」，
  接著自己判斷「**刀具邏輯不能選角點**」。

  🔴 **我在沙箱直接跑 `knifePath()` 求證（⛔ 不是推論），程式自己回答**：

      ok = false
      「有一個點太靠近邊的端點了 —— 那裡會長出長度 0 的線。
      　想從角落切就直接點那個角（用「多點連接」）」

  ⇒ `knifePath()` 的 `len*s < PLANAR_TOL_CM` **本來就擋掉切點落在端點**，
     ⭐ **而它的錯誤訊息自己就寫著要去用 `多點連接`。**

  | | 加之前 | 加之後 |
  |---|---|---|
  | 點在角附近 | 落在邊上靠近角的位置 → **切得下去**（不夠準）| 吸到角 → **每一次都切不下去** |

  🔴 **原本只是不夠準，加上去之後變成不能用** —— ⛔ 這比「沒有意義」嚴重。

【② 還原了什麼】

  · `pickEdgePoint()` 的端點吸附 ＋ 多回傳的 `snapped`
  · 預覽標記整套（`_queueKnifeHint`／`_setKnifeHint`／三處事件分支／
    `setKnifeMode` 的清除）
  · `main.js` 的 `onKnifeVertHint`
  ✅ **機械斷言**：`grep -rn "knifeHint|KnifeVertHint|snapped" js/` → **0 條**

【③ ⛔ 沒有一起刪掉的：教訓】

  · `規格\建模器-點線面編輯.md` 那一節**改寫成「✘ 明確不做」**，
    保留 kang 的原話、實測輸出、為什麼正主是 `多點連接`。
    ⚠ **⛔ 不留的話下一個人會再提一次一模一樣的想法** ——
    而它**看起來非常合理**（跟 `吸中點` 同形狀，只換一個數字，
    ⛔ 一行新數學都不用寫）。
  · `select.js` 的 `pickEdgePoint()` 裡留一段「⛔ 這裡不可以加」的註解
    —— ⭐ **擋在會被改的那一行旁邊**，⛔ 不是只寫在文件裡。

【④ 🔴🔴 升格成鐵律（`PROJECT_LOG.md`「一、動手之前」）】

  > **改一個東西之前，先問「誰在用它、他們要什麼」。**

  ⚠ **⛔ 這一條不是「多做一道檢查」擋得住的** —— 兩次的東西
  **本身都是對的**，斷掉的是**它跟別人之間的約定**。

  **同一天連著兩次**：
  · 寫 `orbit.enableRotate` → **沒問誰在寫它**（框選也在寫）
  · 改 `pickEdgePoint()` 的輸出 → **沒問誰在用它的結果**（`knifePath()`）

  ⭐ 可執行的兩句：
  · 動共用的開關／狀態 → `grep` 誰在寫它
  · 動一支函式的輸出 → 讀拿它結果去做事的那一支要什麼

【怎麼知道做對了】

  ✅ 殘骸 grep → 0 條
  ✅ 測試 2560 全過／兩端檢查全過／兩個檔 ESM 解析乾淨
  ✅ `STATUS.md` 的「⏳ 待實測」回到**空的**（那 11 步清單一起撤掉）

【⏳ 待實測】**⛔ 沒有。**

【⚠ kang 原本的需求還在，⛔ 沒有被解決】
  「分切時要準確點到物件的點」→ 現行解法是 `多點連接`
  （先 `拉點線面` 選角 → 按它）。
  🔴 **如果那條路他覺得太麻煩，那才是真正該解的題** ——
  而解法會是**讓 `多點連接` 更好用**，⛔ 不是再去改刀具。

【下一件】⏳ 等 kang 決定：① 讓 `多點連接` 更好用，
  ② 或回到對照表那 19 項重挑。
