PROJECT_LOG V3.3：剖面分切完成，三條生產路徑全通，以及三條新的坑

只動 PROJECT_LOG.md。+237 −35 行，2074 → 2276 行。
35 個刪除全部是本次刻意覆蓋的過時內容，沒有動到任何不相關的段落。

## 覆蓋

- 版本 V3.2 → V3.3；「開新對話時先讀這段」改成三條路徑全通、
  下一步只剩第 4／6 期、測試 694 → 796 項
- 「目標與範圍」的三條路表格：第三條從「還沒做」改成 0507c51。
  並記下 kang 說明的**疊合後會人工補土打磨** —— 這件事解釋了
  為什麼「取每片中央的截面」是對的（誤差對稱，打磨與補土各吃一半），
  以及為什麼「成品留孔」的代價比原估低
- 八期路線圖與「路線圖現況」：剖面分切完成；
  **第 7 期（自由曲面切分近似）從「大概取消」改成取消** ——
  剖面分切解同一個需求而且沒有近似
- 「剖面分切」整節從規劃改寫成完成：新增「用法／分段／定位孔／輸出」
  四小節，刪掉「還沒決定的」四項（全部有答案了），
  並刪掉一段重複的「一定要做的檢查」
- 檔案結構加三個新檔；回歸測試段改 796 項，
  並把 slice 加進「改了要跑」的清單
- 待辦：剖面分切打勾，註明**還沒實際下料疊過**

## 新增

**踩過的坑第 26～28 條**，全部是 kang 實測抓到的：

26. 判準要挑「有物理意義的量」去比 —— 「湊得剛剛好」用 1e-6 cm
    （十奈米）當門檻，而 Manifold 的座標帶 1e-4 殘留，
    於是跳出「還差 0 cm 沒疊到」。同一個容許值也要用在
    「剩下的填滿」，只修警告會變成「不跳警告但少一片」
27. 不要被最極端的樣本綁死 —— 球的頭尾小片把兩個定位孔綁死成
    2.66cm 間距（60cm 的球）。只有真正需要全域的約束才全域，
    而且放寬要講清楚代價
28. 使用者自己選的東西不能當成錯誤 —— 刻意選 1 孔卻跳兩個紅框。
    紅色只留給「程式做不到你要求的事」。
    gizmo 軸標併在這一條下方（含「縮放模式永遠用本地軸」那個坑）

**剖面分切的基準數字表**（16 項）。最值錢的三項：
三軸交叉驗證 Σ(片面積×片厚) 都等於 108000、外輪廓必須逆時針（防鏡射）、
球的切片體積隨片厚收斂到網格體積。

**進度紀錄**新增一節：commit、行數、三個實測問題、
以及「Illustrator 驗過但還沒下料疊過」。

## 沒動到

協作規則、部署設定、子專案 1～3 的所有內容、歷史進度紀錄
（43b0c03 那行的「694 項」是當時的事實，保留）。
