草圖那四題全部拍板 —— 旋轉成形要變成【參數物件】，輪廓吃鋼筆＋SVG

⛔ 程式一行都沒改。動到 3 個檔：
  M  草圖與造型-設計方向.md  新增「四題的答案」＋「接下來要做什麼」
  M  STATUS.md               下一件那一段跟著改
  M  版控工具/commit-msg.txt

════════════════════════════════════════════════
【四題的答案（kang 2026-09-02 拍板）】

  | 題 | 答案 |
  |---|---|
  | ① 那條線是什麼 | **丙-1.5** —— 線留在 `src` 裡，**改了東西跟著變** |
  | ② 輪廓來源 | **鋼筆 ＋ 匯入線稿（SVG）**。⛔ 模型上選的一串邊不做 |
  | ③ 躺著還是站著 | **旋轉成形多一個「立起來」**，⛔ 不動鋼筆、⛔ 不動 SVG |
  | ④ 中心線 | **參考線優先**（綁著、可拖、即時連動），沒有就打數字；**刪時自動脫鉤** |

════════════════════════════════════════════════
【🔴 這一輪 kang 問到的三件，每一件都改變了方向】

  ① 「**丙感覺比較治本**」
     ⇒ ⭐ 而丙的機制**這個專案早就有了**【已查證 · `io.js` 檔頭：`src` 是一棵樹，
     　 `bool`／`array`／`pen` 都記著自己從哪來】——**⛔ 不是從零開始**。

  ② 「**鋼筆不也是躺著?**」
     🔴 一句話把問題問到位：**這⛔ 不是 SVG 的問題，是所有輪廓都躺在地板上**。
     ⇒ 「立起來」因此放在**旋轉成形的出口**（改一個地方），
     　 ⛔ 不是放在輪廓那一端（要改兩套座標系）。

  ③ 「**參考線也可以直接放線…移動時數字也會連動**」
     ✅ **他是對的**【已查證 · `規格\建模器-參考線.md`：「點到那條線 → 變亮 →
     　 直接拖（位置欄跟著跑）」「拖與打數字接的是同一條線、同一個欄位」】
     ⇒ **軸交給參考線 ＝ 直接繼承「看得到 ＋ 可拖 ＋ 數字連動」**，
     　 ⛔ 不要在旋轉成形裡再造一次輪子。

════════════════════════════════════════════════
【⚠ 兩個查證出來、會擋路的事實】

  ① **SVG 現在把開放路徑挑掉了**【`js/sketch/profile.js:114`】：
     `const open = shapes.filter(s => !s.closed);`
     ⚠ 而旋轉成形要的**正是開放的線**（碗的側面）。
     ✅ 好消息：**解析那一半是現成的**（`svgPath.js` 認得 `closed:false`），
     　 要做的只是**讓它活下來**，⛔ 不能破壞現有的擠出。

  ② **參考線是一個【平面】，⛔ 不是一條線**【`scene.js` `_guideSegments()`：
     一個平面畫成 600cm 的方框 ＋ 中央十字】。
     ⇒ 一條定不出軸，**但它跟地板的交線就是軸** —— 正好對上「軸要兩個座標」。

════════════════════════════════════════════════
【⭐ 附帶解掉一個補丁】

  第 1 階段加的「**自動填最小值**」是為了讓端點落在軸上（手畫不可能剛好）。
  ⇒ 中心線變成**看得見又吸得上去的參考線**之後，
  **那個補丁從「必要」降級成「保險」。**

════════════════════════════════════════════════
【怎麼知道做對了】

  ✅ `草圖與造型-設計方向.md` 檔頭從「⛔ 這份還沒有結論」改成「四題已拍板」
     —— ⚠ 但仍寫明「**這還不是執行計畫**」
  ✅ 原本那四題的**選項全部留著**（規則：被否決的方向要留著）
  ✅ 目次產生器 --all → 兩份都最新
  ⛔ 測試／兩端／模組圖不用跑 —— ⛔ 沒動程式
  ⛔ 這一輪**沒有東西要 kang 實測**

════════════════════════════════════════════════
【🔴 `STATUS.md` 行數】

  上一輪 289 → 這一輪（下一件那段改寫）。⛔ 沒有自己動手砍。
