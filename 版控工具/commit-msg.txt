平行複製一圈邊 ＋ 抽出環切的共用後半段（測試 2288 → 2312 項）

kang 2026-08-28 同意的下一件。動到 5 個檔：
edit.js / main.js / index.html / tests/run.mjs ＋ 規格/建模器-點線面編輯.md
（日誌與對照表一併回寫）

【它是什麼】

  在選到的那一圈邊，【兩側各加一圈平行的線】。做加強筋、邊框、溝槽。
  工具列 `平行複製` ＋ 寬度（單位跟 `沿面滑動` 共用同一顆「公分／％」）。

  ⚠ 它跟沿面滑動吃同一種選取，但做的事相反：
     沿面滑動 ＝ 把那一圈【挪走】（形狀會變，只動座標）
     平行複製 ＝ 在那一圈【兩側各加一圈】（形狀不變，只加線）

────────────────────────────────────────────────
【🔴 這一輪動了一支已經驗過的函式：抽出 cutFacesAlongInserts()】

  環切與平行複製【輸入不同，但後半段一模一樣】：

    環切     → 要切 edgeRing 走出來的一圈（橫著跨過四邊形），切點均分
    平行複製 → 要切「選到那圈邊上每個點的兩條軌道」，切點在距離 w 處

  ⛔ 各寫一份就是兩條路算同一件事（坑第 31 條）——
     而漏掉的那一半會是「標記安靜消失」，畫面上完全看不出來。
  〔跟 rebuildWithInserts() 當初抽出來的理由一字不差。
    差別：那一支只插點不切面（邊上加點用），這一支會切開〕

  🔴 抽對了的證明 ＝ 環切自己的測試一項都沒掉（2288 → 2288）。
     動手前先存了基準，抽完立刻比對 —— 掉一項就退回。

  ⚠ 抽的時候改了三處，每一處都是「原本綁在環切身上」的東西：
     · n 從插點數【現算】，⛔ 不從外面傳 cuts 進來
       （環切一條邊插 cuts 個、平行複製只插 1 個）
     · 錯誤訊息的「環切」改成參數 label
     · 回傳裡 ringLen/closed/cuts 移出去，改由呼叫端用 extra 併入

【⭐ 一行新數學都沒有，但我猜錯了一個零件】

  計畫時我說加線那半可以借 rebuildWithInserts()。
  🔴 【那是錯的】—— 那支只插點、⛔ 不切開面（邊上加點用的就是它）。
     正解是抽 loopCut() 的後半段。
  ⚠ 那是「零件現成」這個估計裡，我沒讀到底的一半。

  ⭐ 而它剛好落進共用函式的守備範圍是【幾何必然，⛔ 不是巧合】：
     選到的邊 e=(v1,v2)，上側那個面是四邊形 (v1, v2, v2', v1')，
     而 v1／v2 的上軌道正好是它的【對面兩條邊】——
     那正是共用函式切得開的唯一情形。

【⚠ 上限刻意是 99.9%，⛔ 不是 100%】

  頂到底時夾住（照 導角／沿面滑動 那個 clamped 的先例），
  但 ⛔ 不可以剛好貼在端點 —— 那會長出【零長度的邊】，
  是退化幾何，會一路帶到布林與 STL 而且不報錯
  〔ON_PLANE_CM 那一段燒過同一件事〕。
  ％ 打 100 直接擋下來（那等於疊在原本那一圈上）。

────────────────────────────────────────────────
【對答案】

  方塊環切 → 平行複製 5 公分
    面 10 → 18、新線 8 條、χ=2、封閉、孤點 0
    🔴 體積 108000 → 108000、面積 13800 → 13800（精確不變）
    新的兩圈位置精確 ＝ 原本 ± 5.000
  圓柱 seg32 → 3 公分
    面 66 → 130、新線 64 條、體積精確不變
  ％ 25%（軌道 20）→ 跟打 5 公分同一個答案
  偏移 500 公分 → 夾住、clamped 8、⛔ 沒有零長度的邊
  判準跟 slideEdges 一字不差（端點價數）：
    只選一條【原本的】邊 → 做得出來（2 條新線）
    環切線上的一條邊     → 擋下來

────────────────────────────────────────────────
【順手做掉的兩件文件事】

  ① 對照表回寫（橋接 ＋ 沿面滑動 ＋ 平行複製）
     🔴 那張表 8/27 才整組刷新，8/28 又過期了 ——
        它自己的 10.7 節寫著病因：
        「做完一件功能這條路上，沒有任何一步會走到這張表。」

     ⛔ 我【不】為它做機械檢查：試過「表上標 ⭐ 但工具列已有按鈕」那道，
        9 項裡 7 項是誤報（「擠出頂點」撞到 `擠出`、「旋轉邊」撞到 `旋轉`）。
        誤報比漏報更糟 —— 裝上去只會讓人學會忽略它。
     → 改成在第 10 節開頭放一行【最後回寫日期】，
        並在日誌那句「決定下一件先看第 10 節」旁邊補一句：
        ⚠ 先確認那個日期 ⛔ 不比最後一個 commit 舊。
        它擋不住忘記回寫的人，但擋得住【照著過期的表做決定】。
     ⭐ 這一輪是【做完當下就回寫】，那道防線第一次派上用場。

  ② 修掉一句沒改到的註解
     slideEdges() 的註解裡還留著「選到的不是一整圈」——
     那個說法 8/28 被 kang 實測翻掉了，我當時改了訊息與 tooltip，
     🔴 【第三處漏了】。又一次「只改一處等於沒改」。

【機械檢查】

  · node --check：edit.js / main.js 都過
  · 🔴 模組圖真的載入一次：解析全過
  · 「被呼叫但不存在的函式」：只有 isMarkable（已知誤報）
  · id 對照：offsetLoop / offsetW 在 html 與 js 都對得上
  · 全域搜「不是一整圈／要選一整圈」：只剩註解裡在講「⛔ 不可以這樣寫」的那兩處
  · node 建模器/tests/run.mjs：通過 2312　失敗 0（新增 24 項）
  · ⭐ 現算：對照表 ⭐ 22 → 21、⭐⭐ 0

【⏳ 還沒做的】

  · kang 的桌機實測（6 項清單已寫進日誌，數字講成公式）
    ⚠ 順手按一下「環切」確認沒壞 —— 這一輪抽了它的後半段
  · 平板版面仍然沒驗（卡硬體）
  · 下一件（⭐ 從對照表挑）：隔一個選一個（Checker Deselect）→
    增長／縮減選取（⚠ 它有「擴 ＝ 共用邊還是共用點」沒解，要問 kang 一題）
