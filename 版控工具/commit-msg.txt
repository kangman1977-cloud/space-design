刀具三項輔助：桌機十項 kang 全部實測通過 —— 回寫日誌 ＋ 改掉會被誤讀的用詞

程式只動註解與文案用詞，行為一格都沒變。測試 1890 項全過。

【kang 2026-08-26 實測結果（第二輪，桌機全過）】
  ✅ 右鍵拖只轉視角，不會再多畫一條線        ← 上一輪 a15b5dd 修的
  ✅ 右鍵「點一下」也不會多一個切點          ← 同一個病的另一半
  ✅ 一筆畫的預覽線跟得上游標
  ✅ 快點兩下真的閉合並切下去
  ✅ 慢慢再點一次還是取消最後一點（逐點按的行為沒有變）
  ✅ 「吸中點」開關看得出來是開著的，切點有落到邊的正中間
  ✅ 先拖一段、再輕點補幾個位置（混用）接得起來
  ✅ 「切一刀」打數字時，青色的預覽線畫在對的位置上
  （連同第一輪的右鍵轉視角／左鍵畫線／離開後左鍵有還原，共十項）

⏳ 只剩平板三項，卡的是硬體不在身邊，不是還沒做完：
  1. 單指／觸控筆畫得出線、兩指轉得動視角
  2. 兩指轉視角時，不會憑空多出一條切線
     ⚠ 這一項沒有人實際踩過，是照「右鍵那個 bug」同一個病推出來順手擋的
       —— 所以它比其他項更需要真的驗一次（推論不是權威事實）
  3. 版面：工具列又多一顆「吸中點」，有沒有被擠掉

  → 日誌加了一句：要開下一件之前先問「平板拿到了嗎」，
    ⛔ 不要無限期停在這裡。

【改掉一個會被誤讀的詞：「輕點微調」→「再輕點補幾個精準的位置」】
kang 連續兩次把「混用」讀成「切好之後去移動切線位置」。

  病因是「微調」這個詞會讓人以為**已經放下的點可以拉**
  —— 實際上只能取消最後一點，點下去就定了。

  ⭐ 他讀到的意思才算數，不是我寫的時候想的意思。

  改了 4 處：core/stroke.js、main.js（2 處）、view/select.js，
  以及 規格/建模器-點線面編輯.md 那一份。⚠ 純註解與文件，沒有行為改變。

【日誌與規格的回寫】
  · 檔頭第七條改成「桌機十項全過」，並把 a15b5dd 那個 bug 與教訓寫進去
  · 「先讀這段」的待實測收成「只剩平板三項」
  · 待辦那條的兩張清單改成 ✅／⏳，清單本身留著當對照
  · 教訓升格：換掉一個既有手勢，就一定要開始分「哪一顆鍵」
    （完整內容在 規格/建模器-點線面編輯.md）

【機械檢查】
  · node --check 全過
  · 「被呼叫但不存在的函式」：只剩 isMarkable（那是 import，舊有的誤報）
  · 跨檔 import/export 逐項對照：全部對得上
  · node 建模器/tests/run.mjs → 1890 項全過，0 失敗

【檔案】
  修改  建模器/js/core/stroke.js、js/main.js、js/view/select.js（只有註解）
  修改  PROJECT_LOG.md、規格/建模器-點線面編輯.md
