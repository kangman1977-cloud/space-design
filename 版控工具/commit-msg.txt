旋轉成形的介面收起來（kang 決定）—— 幾何與 22 項測試原封不動留著

🔴🔴 收的理由是 kang 一句話問到根本：
    「你只是為了達成旋轉成形…並不是真正的計畫」

動到 6 個檔：
  ?? 草圖與造型-設計方向.md（**全新檔**，⛔ 沒有結論，只有事實與待決的問題）
  M  建模器/index.html          那 8 個控制項整段【註解掉】（原文留著）
  M  建模器/js/main.js          接線與四支函式拿掉（5831 → 5593，−238）
  M  建模器/js/build/revolve.js 檔頭補「介面收起來了」那一段（⛔ 幾何一行沒動）
  M  STATUS.md                  待實測十項【取消】；下一件改成「先讀設計方向」
  M  外部參考-Blender編輯.md    Spin ✅ → ⏭；順手補掉一個一直存在的假數字
  M  規格/建模器-旋轉成形.md    「介面收起來了」＋ 接回來的四個步驟
  M  版控工具/commit-msg.txt

════════════════════════════════════════════════
【這次要做什麼】

  kang 選了「把按鈕收起來」（⛔ 不是否決，⛔ 也不是整個撤掉）。
  ⭐ **先例是他自己定的**：`增長／縮減選取` 2026-08-29 就是這樣處理的。

  ⚠ **⛔ 留著能按的代價是真的**：工具列多 8 個控制項、用法將來還會變，
  而現在這版**其實按不太出東西**（要先畫鋼筆、再打兩個座標、還是盲畫）。

════════════════════════════════════════════════
【🔴🔴 這一輪真正該記的：他問到的兩件事，兩件都成立】

  | 他問的 | 查證結果 |
  |---|---|
  | 「鋼筆不可能只畫線」 | ✅ **對的**。按 `不封口` → 生一片板或一道牆；沒按 → 擠成一塊實心的。⇒ **鋼筆做不出「只有一條線」** |
  | 「照你這樣做下去，螺旋一定會出問題」 | ✅ **對的**。彈簧是**封閉的小斷面**繞著轉 ⇒ 螺旋的輪廓必須封閉；而鋼筆封起來直接擠成柱體 ⇒ 連封閉的線都拿不到 |

  ⇒ 真正缺的是**草圖**那一層，⛔ 不是「輪廓物件」那個補丁。
  → 新開 `草圖與造型-設計方向.md`（照 `編輯功能設計方向.md` 的先例），
  　 裡面**⛔ 沒有結論**，只有查證過的事實 ＋ **四個還沒決定的問題**。

════════════════════════════════════════════════
【⭐ 為什麼幾何一行都不用刪】

  `revolve.js` **只吃一串點** —— ⛔ 不認識鋼筆、⛔ 不認識選取、⛔ 不認識文件。
  **誰餵給它都行** ⇒ 換輸入⛔ 不用重寫幾何。
  🔴 這正是 kang 2026-08-25 批准的**四動作框架**兌現的地方。

════════════════════════════════════════════════
【⚠ 順手補掉一個一直存在的假數字】

  那張對照表的「記號」只定義了五種（✅⭐➖✘❓），
  **而 `增長／縮減選取` 2026-08-29 就標成 ⏭** ——
  🔴 **計數指令漏了它，所以它從那天起就沒被算進總數。**
  ⇒ 以前算出來的 **86／87／90 每一個都少 1**。
  ✅ 記號表補上 ⏭，計數指令也補上，並寫明「⛔ 不要拿舊數字對答案」。

════════════════════════════════════════════════
【怎麼知道做對了】⛔ 要能對答案的量

  ✅ 測試 **2667 項全過** —— ⛔ 一項都沒少（測試直接 import revolve.js，
     ⛔ 不經過介面）。⭐ 這就是「幾何真的沒被動到」的機械證明
  ✅ `main.js` 5831 → **5593**（−238），`grep revolve|revA|revSeg|…` ＝ **0** 個殘留
  ✅ 兩端檢查通過（那 8 個 id 兩端都不見了 ⇒ ⛔ 不會有孤兒開關）
  ✅ 模組圖真的載入一次 —— 解析階段過
  ✅ 用**更正後**的指令現算：能力列 **91**、⭐ 還剩 **20**
  ✅ `STATUS.md` 293 → **280 行**（⚠ 仍超過 276，報告在下面）
  ⛔ DXF／STL／畫面沙箱驗不了 —— 但**這一輪沒有東西要 kang 實測**

════════════════════════════════════════════════
【🔴 停下來報告：`STATUS.md` 280 行，還是超過 276】

  ⛔ **沒有自己動手砍。** 比上一輪的 293 少了 13（待實測那十項取消了）。
  可以移出的仍是上次列過的甲（平板那則 −8）與乙（非等比縮放那則 −5）。
  ⭐ **AI 的建議：這一次也⛔ 不動它** —— 底下「真待辦」那條 ⏭ 是這一輪的結論，
  　 而它會在草圖那條路定案時整條改寫。
