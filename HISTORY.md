# 網頁空間設計程式 — 歷史封存檔（HISTORY）

最後更新：2026-08-27

> **這是冷檔，不從頭讀。** 用法＝在目次找到節名，再 Grep 那個字串跳過去。
>
> 🔴 **本檔不是規則的家。** 它只回答一件事：
> 「**當初是怎麼做的、怎麼驗出來的、踩過什麼**」。
> 凡是「以後動手時要遵守的規則」，權威版一律在 `PROJECT_LOG.md` 的「鐵律」一節；
> 本檔留著原文只是為了查得到經過。**兩邊對不上時，以 `PROJECT_LOG.md` 為準。**
>
> 其餘去處：功能規格 → `規格\` 對應檔（先 `Glob 規格\*`）；
> 現況、待辦、協作規則、鐵律 → `PROJECT_LOG.md`。
>
> **只搬不改事實**：從 `PROJECT_LOG.md` 搬進來的內容一字未動。

---

## 📑 目次（用 Grep 跳過去，不要從頭讀）

### 進度紀錄

- 平板化與上線（2026-08-20，分支 `touch` → 已合併回 `main`）
- 建模器啟動（2026-08-21）
- 建模器第 2 期（上）：布林運算（2026-08-21）
- 建模器第 2 期（中）：陣列與鏡射（2026-08-21）
- 建模器第 2 期（下）：管、圓角方塊、折板（2026-08-21）
- 建模器第 3 期：展開 v1 ＋ DXF／SVG 出圖（2026-08-21）
- 建模器第 3.5 期：STL 匯出（2026-08-21）
- 第 5 期(A)：算不準的展開要講出來（2026-08-22）
- 第 5 期(B)：展開改走外皮弦長（2026-08-22）
- 第 5 期(C)：實體可直接展開 ＋ STL 修正（2026-08-22）
- 指定分片（2026-08-22）—— 原路線圖裡沒有的一項
- 接合編號（2026-08-22）—— 問出來的，不是規劃出來的
- 剖面分切與對齊三視圖的討論（2026-08-22）—— 只有討論，沒有程式
- 編輯與組裝（2026-08-22）
- 剖面分切（2026-08-22）—— 第三條生產路徑，一天做完
- 匯入 Illustrator 線稿（2026-08-22）—— 瓶頸換邊之後的第一步
- 曲線帶與一段一個接合編號（2026-08-22）—— 讀他的程式，不要猜
- 匯入件的原點與打散（2026-08-22）—— 一打開畫面就看得到的兩個問題
- 第 6 期第一刀：拉點／拉邊／拉面（2026-08-23）—— 測試擋下兩次
- 第 6 期第一刀的介面，以及三個只有實測抓得到的錯（2026-08-23）
- 第 6 期第二刀：擠出面（2026-08-23）—— 先調查，才動手
- 第 6 期第三刀：變換 ＝ 種類 × 方向 × 中心（2026-08-23）—— 一行寫死擋住整個第 6 期
- 第 6 期地基（下）：子元素多選 ＋ active（2026-08-24）—— 一個 getter 省掉五處改動
- 六個工具的調查、兩輪實測，以及面合併（2026-08-24）—— 推論被翻掉四次
- 壓平，以及為什麼「溶解面」不做（2026-08-24）—— 三個概念正交的第二次兌現
- 扇形三角化只對凸多邊形成立（2026-08-24）—— 一個潛伏的 bug 被自己的新功能叫出來

### 這個子專案踩過的坑

- 1. 半邊的 role 只設單邊
- 2. gizmo 初始化順序
- 3. 曲率計算是 O(頂點×面)
- 4. three.js 的事件名稱搜尋不到
- 5. 把只對特例成立的假設寫死
- 6. 展開長度不能用網格量
- 7. 加厚時折彎處會被削薄　⚠ 第 3 期做折彎補償時會再遇到同一件事
- 8. 板件不該做布林聯集
- 9. 規格上合法 ≠ 別人的軟體讀得動　⚠ 凡是要送出去的格式都適用
- 10. 圓弧至少要切兩段才認得出來
- 11. 「產生 `<a download>` 然後 click」有三個地雷
- 12. 讀不出尺寸的圖，比沒有圖更糟
- 13. 開新階段前要先確認「目的」，不是先確認「規格」　⚠ 這條最貴
- 14. 「目的」包含用什麼材料　⚠ 這條跟第 13 條一樣貴，而且是同一個病
- 15. 弦長還是弧長，是材料問題，不是幾何問題
- 16. 攤平的生成樹要先走最平的邊，否則共面的東西會被拆散
- 17. 「中途對、最後錯」的 bug，只測中途抓不到　⚠ 這條是送出去的檔案錯了
- 18. 誤報比漏報更糟
- 19. 「少了某個呼叫」不等於漏了
- 20. 正確的數字，錯誤的意思　⚠ 做介面時最容易犯
- 21. 數學上正確，但使用者以為壞了
- 22. 畫面更新的熱路徑上不能有 O(全部)　⚠ 坑第 3 條的重演
- 23. 問「下游拿去做什麼」，比問「你要什麼功能」有用
- 24. 自由度沒鎖滿，比算錯更糟　⚠ 這條的觸發點很特別，值得記住
- 25. 浮點數不能用精確比對
- 26. 判準要挑「有物理意義的量」去比　⚠ kang 實測抓到的
- 27. 不要被最極端的那個樣本綁死
- 28. 使用者自己選的東西，不能當成錯誤
- 29. 座標系換手時，「直覺的繞向」是錯的　⚠ 這種錯畫面上看不出來
- 30. 重複的點不能只靠索引排除
- 31. 要兩邊一致的東西，就別讓它有機會不一致　⚠ 這條的修法比錯誤本身值錢
- 32. 一條邊的兩側，可能都在同一片上
- 33. 教訓寫在別的功能底下，就等於沒寫　⚠ 這條的重點不是錯誤本身
- 34. 不要在日誌裡寫一個不存在的退路

### 第 6 期第一刀：拉點／拉邊／拉面（2026-08-23）—— 測試擋下兩次

- 動工前的唯讀調查（結論比預期好）
- ⭐ 測試擋下的第一件：「一個面」不是三角形
- ⭐ 測試擋下的第二件：拿方塊測不出「面平不平」
- 另外兩個寫測試時才發現的事實
- 三件連帶重算（`refreshAfterEdit()`）
- 最後一組測試才是重點
- 順手留給第二刀的地雷
- 還沒做

### 第 6 期第一刀的介面，以及三個只有實測抓得到的錯（2026-08-23）

- 兩個寫的時候才發現、但非做不可的東西
- ⭐ kang 的截圖抓到三個沙箱永遠驗不到的錯
- 順手用數學回答了「拉點正確嗎」

### 第 6 期第二刀：擠出面（2026-08-23）—— 先調查，才動手

- 調查換到三件推理推不出來的事
- 一個先被當成「前置」、其實是既有 bug 的東西
- 實作決定
- 介面：kang 拍板方案 C
- 驗過的數字（全部手算得出來）


### 第 6 期編輯功能收尾（2026-08-24～25）—— 2026-08-26 從日誌搬入

- 環切收尾、法向與選一圈（2026-08-24～25）
- 內縮、導角、導圓角、outer miter、全選邊（2026-08-25）—— 四動作框架連續兌現四次
- 刪除面 ＋ 補洞（2026-08-25）—— 「補洞當時根本沒有輸入」
- 任意切線（切一刀）（2026-08-25）—— 兩個介面 bug 比功能本身重要
- 連接兩點 → 多點連接、面上加線、凹面穿出邊界（2026-08-25）
- 邊上加點 ＋ 顯示點（2026-08-25）—— 加了卻看不見等於沒加
- 分離（2026-08-25）—— 「切開」跟「拆成兩個物件」是兩件事
- 刀具兩版（2026-08-25）—— 第一版被實測否決，第二版是 kang 想出來的
- 量測第 1、3 步（2026-08-25）—— 一個「時好時壞」的 bug

### 日誌檔頭的輪次紀錄（2026-08-27 從日誌搬入）

- 日誌檔頭「這一輪做了什麼」二十一則（2026-08-25～27）—— 逐字，一字未改

### 三批實測清單全過（2026-08-27 從日誌搬入）

- `選轉角`＋`選相似` 11 項、`變成正圓` 8 項、**破洞在哪裡** 7 項 —— 逐字，一字未改
- 🔴 其中「**板厚會把破洞自動加厚救回來**」那一條的權威版留在 `PROJECT_LOG.md`

### 已結案的待辦（2026-08-27 從日誌搬入）

- 已結案的待辦 40 項（`[x]` 完成 35、`[~]` 撤案 5）—— 照原順序，一字未改

### 畫面與顯示

- 表面上一條一條的斜紋：陰影的深度比對沒有容許值（2026-08-27）
  —— ⭐ kang 的四個對照（一直都在／放一個也會／方塊不會／球會）
  一句話翻掉三個推論。⚠ 那兩個數字**沙箱驗不了**，紅線在 `scene.js` 註解裡

### 從日誌檔頭被擠出去的「翻掉了什麼」

> 🔴 檔頭那一欄是**覆蓋型、上限 5 條**，被擠出去的逐字搬到這裡，⛔ 不是刪掉。

- 球 segW12 的「相鄰經向夾角正好 30 度」是錯的（2026-08-27 擠出）
  —— 30 度是**經度間隔**，二面角實測 1.507～29.965 度，隨緯度變
- 「講數量與形狀」那條規則破了第五次（2026-08-27 擠出）
  —— 清單寫「360 個」漏掉單位。⚠ 規則的權威版在 `PROJECT_LOG.md`

---

## 進度紀錄
- 2026-08-20　v1 完成：2D 平面編輯器（拖拉、吸附、物件庫、存讀檔、匯出 PNG）
- 2026-08-20　v2 完成：加入物件高度與類型、3D 環繞檢視與第一人稱漫遊、牆體碰撞
- 2026-08-20　v2.1 介面：左右側欄改為可拖曳調寬（150–620px，雙擊還原）、
  可一鍵收合、寬度記憶於 localStorage（key: `floorplan_panels`）；
  修正欄位文字截斷（物件庫名稱過長改省略號、屬性面板標籤縮窄）
- 2026-08-20　v2.2 分層造型：物件可堆疊多層方塊做出上寬下窄等形狀，
  含 4 組快速範本與逐層編輯器；2D 用外廓實線＋內層虛線呈現；
  物件庫的工作桌／機台／CNC／台車／料架已預掛範本
- 2026-08-20　v2.3 新增「人」物件（kind: person）作為空間比例參考，
  身高／肩寬可調，角度＝面向方向
- 2026-08-20　開啟子專案 2「鋁擠型框架設計器」v1：
  40 系列型材、4 範本、T 溝斷面 3D、桿件點選編輯、切割料表與配件清單、CSV 匯出
  （後來移出資料夾，未納入版控）
- 2026-08-20　開啟子專案 3「組裝系統結構說明表」：取代 Illustrator 手工表單，
  標準料排料 DP、多說明表、套數、備料與批次匯出，並與平面規劃器互通

### 平板化與上線（2026-08-20，分支 `touch` → 已合併回 `main`）
- `ae07255` 說明表加入觸控支援，並建立版控工具
- `3c5c0b3` 版控工具：平板測試伺服器 + stale lock 偵測
- `354da13` 空間平面規劃器加入觸控支援
- `c10b80a` 修正平板工具列文字擠成直排
- `e8080fe` 修正小物件難以點選與移動
- `3a2910a` 修正 iPad 全螢幕被狀態列蓋住
- `dcc84e4` three.js 本地化 + 加到主畫面
- `a4ec883` 入口頁與上傳工具
- 上線 GitHub Pages，iPad 實測通過

**設計原則貫穿全部觸控改造**：事件入口分開、動作邏輯共用。
桌機 `mouse*` 路徑行為不變，觸控是另外新增的入口，兩者呼叫同一組動作函式，
因此計算結果保證一致，未來改邏輯也只改一處。
已用「抽出計算邏輯比對 SHA-256」與「同尺寸實跑比對排料備料結果」驗證過。

### 建模器啟動（2026-08-21）

- **第 0 期技術驗證通過** — 桌機與 iPad 上 3D 直接編輯（點選、三軸 gizmo、
  布林挖孔、觸控）手感都可接受，架構**不必**改成 2D 主導。
  驗證檔留在 `XXX/建模器驗證/`（不進版控），日後正式版手感變差時可回頭比對
- `2b030f0` 建模器第 1 期：半邊網格核心與參數化基本體（15 檔、2,746 行自寫程式碼）
  - 純加法，未修改任何既有檔案
  - kang 已在瀏覽器實際開啟確認：畫面正常、60 FPS，
    面板數字與沙箱算出來的完全一致

**同一條原則延續到建模器**：`select.js` 的滑鼠與觸控走同一組動作函式；
第 0 期在 iPad 實測過的「移動 8px 內、450ms 內算點選」「拖 gizmo 時關掉視角旋轉」
「手指裝置 gizmo 放大 1.5 倍」三項設定原封不動搬進正式版。

### 建模器第 2 期（上）：布林運算（2026-08-21）

- `e7cff61` 布林運算：差集／聯集／交集，存運算樹而不是存三角形
  - 挖完孔還能在面板上回頭改孔徑與孔的位置，另有「凍結成網格」
  - 檔案格式 v1 → v2；v1 舊檔仍讀得動
  - **推翻原訂的 BVH 方案，改用 Manifold**（理由見「關鍵決定」第 4 條）
  - 建立回歸測試 `tests/run.mjs`，67 項
- `386cef3` 修正結構分析對「有洞」與「分成多塊」物體的誤判
  - 見「踩過的坑」第 5 條。測試 67 → 89 項
  - `mesh.js` 新增 `componentCount()`，`validate()` 多回傳 `components`

kang 已在本機與 GitHub Pages 兩邊實測確認：挖孔正常、改半徑孔跟著變、
復原可一路退回、線上版 WASM 正常載入。

### 建模器第 2 期（中）：陣列與鏡射（2026-08-21）

- `e47bcd8` 線性／環形／鏡射三種排列，做成**修飾器**而不是複製成 N 個物件
  - 理由是備料：12 支一樣的橫料，展開要出「一張圖 ×12」，不是 12 張一樣的圖。
    跟說明表的「套數」是同一個概念。`ModelObject.copies` 帶著這個數字
  - 線性可吃兩個方向 → 散熱孔的網格就是這樣排的
  - 資訊只能往下走，所以預設留在修飾器，另外提供「打散」
  - 檔案格式 v2 → v3；測試 89 → 153 項
  - 順帶把布林面板的「可展開子項」抽成共用的 `_itemBox` / `_srcBody`

### 建模器第 2 期（下）：管、圓角方塊、折板（2026-08-21）

- `8d2de61` 三個新參數體，加上板件在畫面上顯示厚度
  - 折板做成**折彎序列**，L／U／Z／多折用同一套；面板可加減道數
  - 板件顯示加厚（`mesh.shell()`），資料層仍是「中性面 ＋ 厚度屬性」
  - 測試 153 → 235 項
  - **這一期學到三件會直接影響第 3 期的事，寫在「踩過的坑」第 6、7、8 條**
  - kang 實測時抓到「板件不能做陣列」，修正後一併補上測試

**第 2 期到此完成。**

### 建模器第 3 期：展開 v1 ＋ DXF／SVG 出圖（2026-08-21）

- `77f386b` 板件可以攤平成下料圖了。新增 7 檔 2,466 行，測試 235 → 407 項
  - **折彎半徑改成「內側 R ＋ K 因子」**，見「關鍵決定」第 5 條。
    檔案格式 v3 → v4，舊檔轉換後形狀與展開長完全不變
  - 展開幾何核心 `unfold/flatten.js` 完全不知道材料是什麼；
    六種材料規則各自獨立在 `unfold/rules.js`
  - **圓弧修正**：從網格辨識圓弧折彎帶，把弦長換成真弧長。
    與參數公式交叉驗證，`arcSeg` 2～24 全部一致到 1e-14
  - 出圖拆成「決定畫什麼」（純資料）與「怎麼畫出來」兩層，
    Canvas 預覽、SVG、列印畫的內容保證一致，而且**測得到**
  - DXF R12（圖層分切割線／折線／折彎區／尺寸／文字）、SVG、
    A4 橫向列印、備料 CSV、指定存放位置

**kang 實測回報了四個沙箱驗不出來的問題，全部修正並補進測試：**
DXF 在 Illustrator 打不開（缺骨架）、SVG 被當成純文字開（缺 XML 宣告）、
下載檔名變成 Chrome 的暫存名、折彎多的件標註擠成一團。
四條都寫進「踩過的坑」第 9～12 條。

**這一輪最重要的教訓是第 9 條**：規格上合法不等於別人的軟體讀得動，
而這類問題**沙箱一律驗不了**，只能靠實際開啟。以後凡是要送出去的格式，
交付時就要明講「這部分我驗不了，請你用真正的軟體開一次」。

### 建模器第 3.5 期：STL 匯出（2026-08-21）

- `4530697` 3D 列印那條路的出口。新增 2 檔 543 行，測試 407 → 461 項
  - 這一期是**對焦之後補的**：kang 說明用意是「建模拿去 3D 列印、
    展開拿去 CNC」，而原本的八期路線圖完全沒有 3D 列印的出口。
    教訓寫在「踩過的坑」第 13 條 —— **這條最貴**
  - 匯出自動處理四件事：cm→mm、Y 軸向上→Z 軸向上、落到平台 Z=0、
    板件自動加厚。詳見上方「STL 匯出」一節
  - **列印前檢查**才是重點：封閉性、法向、獨立塊數、尺寸。
    第 1、2 期打的地基（半邊結構、Manifold、尤拉數）第一次直接
    變成使用者看得到的價值

**外部驗證（這次特別重要，因為坑第 9 條說過沙箱驗不了）**

1. 另寫一支獨立的 Python 解析器讀回檔案（不碰本專案程式碼）：
   結構、體積、外框、貼平台皆正確；**每條有向邊只出現一次且都找得到
   反向邊** → 封閉且流形。
2. kang 用第三方線上工具（`iamrapid.com/tools/stl-analysis`）實測自己
   匯出的模型，判定「可直接列印 － 防水且多歧管」。
   它報的 498 三角形／251 頂點代回尤拉公式：`251 − 747 + 498 = 2`，
   拓撲完美。那個數字跟本專案毫無關係，對得上就表示一條邊都沒接錯。

kang 目前沒有切片軟體與 3D 列印機。但 STL 的風險遠低於 DXF ——
它就是「一個檔頭 ＋ 一串三角形」，沒有 DXF 那種可以缺的結構區段。

> ⚠ **這次的外部驗證後來被證明有漏**：兩項驗的都是**錐體**，
> 而錐體全是三角形面，剛好避開了「四邊形面頂點共用」那個 bug。
> 板件（平板、折板）的 STL 從第 3.5 期起就一直是壞的，
> 直到 2026-08-22 才被發現。見「踩過的坑」第 17 條。

**兩條生產路徑到此都通了**：
建模 → STL → 3D 列印；建模 → 展開 → DXF → 雷切。

### 第 5 期(A)：算不準的展開要講出來（2026-08-22）

- `766a3fd` 止血。測試 461 → 492 項

  圓錐展開走**弦長相加**，而當時以為那是錯的（對捲得起來的材料確實是）。
  真正的修法是極座標展開，但那是大工程，所以這一輪只做
  「算不準的片一定要標出來」。

  - `nonDevelopable` 維持 `false` 但補上一大段「**為什麼做不出來**」：
    用角虧判定會給錯答案，而且錯在「哪些頂點算內部」這個定義上，
    兩種定義各錯一半（圓錐與球冠剛好相反）。
    寧可留一個誠實的 false，也不要放一個會在圓錐上說「可以下料」的判定
  - 一度加了「封閉實體一律拒絕展開」，**被測試打回來** ——
    那會打死壓克力箱體（六片分開下料）這條天天在用的路
  - 警告合併：31 道折彎的錐面原本吐 31 行一模一樣的字，改成講一次

### 第 5 期(B)：展開改走外皮弦長（2026-08-22）

- `7c4fa25` 這一輪推翻了 (A) 的部分前提。測試 492 → 548 項

  kang 說明材料之後才發現：**板材捲不起來，弦長本來就是對的**，
  而程式反過來把它拉成弧長，4 角柱錯了 11.07%。見坑第 14、15 條。

  - 材料規則多回答 `canRoll()`，新增「珍珠板／發泡板／薄木板」，
    預設材質從 `steel` 改成它
  - 生成樹改成**先走最平的邊**，修掉封閉件攤成星芒的問題（坑第 16 條）
  - 警告判準從「折線不平行」改成「**是不是錐面**」——
    前者在鋼板方塊上誤報（坑第 18 條）
  - 3D 列印不受影響（實測：三種情況匯出的 STL 位元組完全相同）

### 第 5 期(C)：實體可直接展開 ＋ STL 修正（2026-08-22）

- `91a7f03` 測試 548 → 593 項

  - 拿掉「不是板件就拒絕展開」那道門（關鍵決定 3 修訂）
  - 「要不要加厚」的判斷從 `kind` 標籤改成 `mesh.isClosed()` 幾何事實
  - **修好 `triangles()` 沒有 clone `a` 的 bug** —— 板件的 STL
    從第 3.5 期起就一直是壞的（坑第 17 條）

  **外部驗證（這次刻意挑有四邊形面的）**：kang 用同一個線上工具
  驗平板與折板，兩個都判定「可直接列印 － 防水且多歧管」，八項檢查全綠。
  它報的表面積 12,064.00 cm² 可手算對答案：
  `2×(100×60) + 2×(100×0.2) + 2×(60×0.2) = 12064`，一分不差；
  尤拉數回推也都是 2（平板 8−18+12、折板 28−78+52）。

### 指定分片（2026-08-22）—— 原路線圖裡沒有的一項

這一項是問 kang「下一步做什麼」時浮出來的。**又一次印證坑第 13 條**：
八期路線圖是規格，規格裡沒有的東西不代表不需要 —— 要問目的。

**第一輪：資料層** `08f5b90`　測試 593 → 617 項

- 新增 `js/unfold/seam.js`，**沒有修改任何既有檔案的邏輯**
- 先做資料層是因為它整輪都能在 Node 裡用數學對答案；
  介面那一輪沙箱一律驗不了
- 讀程式碼時發現地基比預期完整：`mesh.toJSON()` 早就在存 `roles`，
  而且是用**頂點索引配對**存的，所以**存檔格式根本不用從 v4 改到 v5**

**第二輪：介面** `efa0c3b`　測試 617 → 632 項

- 6 個檔案 +521 / −2。兩行刪除都是同一行改寫，不是刪除內容
- 展開幾何核心與 `core/` **零改動**
- 自己抓到並修掉兩個問題，都寫進「踩過的坑」第 21、22 條

**kang 實測七項全過**：轉成網格→分片、點稜線變橙、點面整圈切開、
再點收回、復原重做、存讀檔標記還在、離開模式 gizmo 回來。

### 接合編號（2026-08-22）—— 問出來的，不是規劃出來的

`2aab963`　測試 632 → 647 項　4 檔 +272 / −8

原本要做「板材尺寸上限檢查」，**動手前問了一句就取消了** ——
kang 的 DXF 會先進 Illustrator 排版才給 CNC，塞不塞得下那裡就看得到。
改問「進 Illustrator 之後什麼最麻煩」，才問出真正缺的是**分切之後
怎麼接回去**。經過寫在坑第 23 條 —— 這是第 13、14 條的第三次，
但**第一次在動手前攔下來**。

- 每一對接合邊同號，綠色數字往料內縮 1.8cm；DXF 獨立 `JOIN` 圖層
- 「分切過的就不合併」是 kang 的取捨 —— 接合編號與合併本質上互斥
- 回歸測試擋下我一次：加圖層卻忘了改 DXF 的圖層數宣告（群組碼 70），
  而 DXF 本身「看起來」完全正常

### 剖面分切與對齊三視圖的討論（2026-08-22）—— 只有討論，沒有程式

kang 在繼續開發前主動提出兩件事，兩件都不在路線圖裡：

**一、編輯方式** —— 「當一個造型由多個物件組合時，每個物件要如何準確
移動到正確位置」，希望操作像 Illustrator 一樣直觀，並問到三視圖。
→ 變成「對齊／均分 ＋ 正交三視圖」，**kang 指定先做這個**。

**二、另一種轉出模式** —— 把造型切成很多剖面，每片轉 DXF 去 CNC 裁切，
人工疊合黏起來變成立體。
→ 這是**第三條生產路徑**，而且**不受可展性限制**，原訂第 7 期因此取消。
   對位方式選定「定位孔 ＋ 串桿」。**當天就做完了**（`0507c51`），
   完成後的內容見「剖面分切」一節。

**一個順帶的釐清**：kang 原話提到「也就可以轉出成 STL 給 3D 列印」——
STL **現在就已經可以直接匯出**，不需要先切片。3D 列印的切片是切片軟體的
工作，我們給它 STL 就好。剖面分切純粹是給板材疊層用的，兩條路各走各的。

### 編輯與組裝（2026-08-22）

**對齊／均分 ＋ 正交三視圖 ＋ 框選** `71f2d21`　測試 647 → 675 項

- 做到一半 kang 指出「似乎沒有框選這一個機制」—— 確實沒有，
  平面規劃器有、建模器沒有。當場補上，做成模式（平板沒有 Shift）
- 自己抓到並修掉：接縫線每幀重算的效能問題不在這輪，
  但「全部入鏡」寫死等角方向會搶走剛選好的視角，這輪修掉了

**貼合** `43b0c03`　測試 675 → 694 項

- kang 實測回報「點對點、邊對邊沒辦法驗證準確，不知道是不是操作有誤」
  → **不是操作有誤，是功能沒給他可以驗證的東西。** 兩個修正：
  邊對邊多鎖一個自由度（坑第 24 條）、加上點選標示（黃色，
  面畫外框、邊畫線段、點畫球）
- 測試抓到 `moved` 用精確比對浮點數的 bug（坑第 25 條）
- 面對面刻意不做定位，kang 確認維持兩步：貼合貼平、對齊定位

### 剖面分切（2026-08-22）—— 第三條生產路徑，一天做完

`0507c51`　測試 694 → 796 項　新增 3 檔 1,320 行、改 6 檔

- 新增 `js/slice/section.js`（幾何）、`js/slice/pegs.js`（定位孔）、
  `js/ui/slicePanel.js`（介面）。**展開與 STL 兩條路的幾何核心一行都沒動**
  （flatten / rules / part / seam / stl / mesh / region 行數完全不變）
- `out/sheet.js` 把 `toSVG` 與 `printPieces` 的算繪端抽成
  `progSVG` / `printSVGs` 給兩條路共用 —— 各寫一份的話，
  遲早有一邊漏掉 Illustrator 的那兩個地雷（坑第 9 條）
- 動工前先問了四件事才寫（三軸、一個檔、孔位自動、板厚預設），
  路上 kang 又追加「每段可以指定孔數」，經過見「剖面分切」一節

**kang 實測抓到三個問題，全部修掉並補進測試** —— 三條都寫進坑第 26～28 條：

1. 「還差 **0** cm 沒疊到」：判準用 1e-6 cm 當門檻，那是浮點數的尺度
2. 球的頭尾小片把兩個定位孔綁死成 2.66cm 間距（60cm 的球）
3. 使用者自己選 1 孔，卻被兩個紅框當成錯誤唸兩次

**外部驗證**：DXF 在 Illustrator 開得起來，圖層依板厚分開（黑 `CUT_T50`、
紅 `CUT_T100`），圓孔是真的 `CIRCLE` 不是多邊形，層號跟著片走，9 片不重疊。
布林聯集的 L 形、球體、三軸都試過。
**但還沒實際下料疊過** —— 那一段沙箱與 Illustrator 都驗不了。

順手做了 gizmo 的 X／Y／Z 軸標（kang 回報常搞錯軸），見坑第 28 條下方。

### 匯入 Illustrator 線稿（2026-08-22）—— 瓶頸換邊之後的第一步

`0b86cb8`　測試 796 → 853 項　新增 4 檔 1,451 行、改 4 檔

問「下一步做什麼」時，kang 講出了方向：**這套工具的核心是編輯**，
並提到貝茲線、匯入圖片描圖、匯入 AI 線稿。那三件是同一個 2D 草圖環境的
三個入口，而**「別人給的」該排在「自己畫的」前面** ——
他天天在用 Illustrator，瀏覽器裡做的貝茲永遠比不上。理由見「匯入線稿」。

- 新增 `js/sketch/svgPath.js`、`js/sketch/profile.js`、
  `js/build/extrude.js`、`js/ui/importPanel.js`
- `prim.js` 加一個 `extrude` 建構器，**刻意不放進 `PRIM_DEFAULTS`**
- **既有的幾何邏輯一行都沒動**（core / unfold / slice / out / view 行數不變）
- 順手讓工具列的單一組按鈕也能換行 —— 每加一顆按鈕就更窄一分，
  單組塞不下時只會被切掉，而被切掉的按鈕看起來就像不存在

**動手前先要了 kang 的真實檔案來看**，而不是照 SVG 規格猜。
一個檔就照出四件事：檔案宣告的是 cm（第一層防護成立）、
X 與 Y 比例因為四捨五入不會完全相等、`fill="none"` 的路徑照樣是封閉的
（不能拿 fill 判斷）、同圖層的路徑沒有 id（錯誤訊息要能退回索引）。
這是坑第 9 條的反面：那次是我寫出去別人開不了，這次是別人寫出來我讀不了。

**寫的時候被自己的測試抓到兩個**，都寫進坑第 29、30 條：
法向整個朝內（體積 −500000）、開橋的重複點讓三角化靜靜切不出東西。

**kang 實測**：真實檔案讀到 2 個形狀、2 個內孔，大圓量到 40.05cm
（＝他畫的 40 公分），擠出後兩個形狀在 3D 裡維持 Illustrator 的相對位置。

### 曲線帶與一段一個接合編號（2026-08-22）—— 讀他的程式，不要猜

`b2e9d24`　測試 853 → 877 項　改 9 檔（沒有新增檔案）

kang 把匯入的 S 字擠出、展開，得到一張讀不出來的圖：
側邊條 196 道折彎、398 個接合編號，整張圖是一團綠色數字。

**經過本身比結果值錢**：

1. 他先傳截圖說明想法（「側邊的眾多小面能不能編輯成單一面」）
2. 我實際跑了一次，指出**幾何已經是對的**（側面展開就是 194×3 的長條），
   問題 100% 在出圖，不需要編輯幾何
3. 我提了一個「用角度門檻判斷哪些是真折線」的方案 —— **那是用猜的**
4. 他說「你也可以參考一支程式」，給了 `SideUnfold.jsx`
5. **讀那支程式的十分鐘，換掉了一個註定會在自由曲線上失敗的設計** ——
   它讀得到貝茲控制把手，所以同一個 S 只切出 10 段

→ 這是「動手前先看真實的東西」的第三次，而這次真實的東西是**他的程式**。
  日誌前兩次是他的示意圖、他的 SVG 檔。

技術上的核心見「真轉角」與「關鍵決定」第 6 條：
**上游知道的事不要在中途丟掉**。攤平時把「這個點是不是真轉角」一起帶出來，
展開就自動從 196 道變成 12 處。

**寫的時候被自己的測試擋下兩次**，都寫進坑第 31、32 條：
兩側走出來的分段不一致（改用無向邊聯集）、
一條邊的兩側都在同一片上（環狀側牆自己接自己）。
第二個是靠「每個編號恰好出現兩次」抓到的 —— 那條測試是做接合編號時
順手寫的，這次救了一次。

順手修了 `mesh.js` 的 `fromJSON`：`verts.indexOf()` 寫在迴圈裡，
是 O(頂點×邊)，匯入件動輒上千頂點會讓讀檔明顯變慢。改成 Map 查表。
跟坑第 3、22 條同一種錯，只是這次在讀檔不在畫面上。

**下一步**：點線面重塑（第 6 期，點選那一半已經做好了）、
內建貝茲、匯入圖片描圖。見「編輯這條路」。
輪到它們之前一樣先問「下游拿去做什麼」（坑第 13、14、23 條）。

而且現在多一條：**先問「你手上有沒有已經在用的東西」** ——
kang 的 `SideUnfold.jsx` 就在隔壁資料夾，早問早省事。

### 匯入件的原點與打散（2026-08-22）—— 一打開畫面就看得到的兩個問題

`a21a803`　測試 877 → 898 項　改 6 檔

kang 把 S 匯進來、打開 3D 畫面，問了兩件事：

1. **「XYZ 控制桿為什麼不在物件上？」**
   —— 是 bug，而且不只難看：旋轉與縮放都繞著遠處的原點在轉。
   我把置中做在物件上而不是幾何上，踩進 `align.js` 早就警告過的陷阱。
   坑第 33 條，重點在「**教訓寫在別的功能底下就等於沒寫**」。
2. **「匯入的 SVG 有兩個以上物件時，只能整體控制？」**
   —— 是。而且我在日誌裡承諾過的退路（「可以用現成的打散」）
   **根本不存在**。坑第 34 條。

修完之後：匯入預設**一個形狀一個物件**（他選的），用圖層名命名，
版面完全維持原樣；合成一個也留著，而且「打散」現在真的做出來了。

→ 這兩個問題都不是算錯，是**一打開畫面就看得到的**。
  沙箱看不到畫面，所以這一類只能靠 kang 實測 ——
  跟坑第 9 條（別的軟體讀不讀得動）同一種「沙箱驗不了」。

---

## 這個子專案踩過的坑

1. **半邊的 role 只設單邊** — 邊界切割線標記後顯示 0 條，
   因為 `edges()` 讀到的是另一條半邊。**一律用 `setRole()`**
2. **gizmo 初始化順序** — `setSnap()` 在 `this.tc` 指派前被呼叫，直接爆掉。
   建構子裡要先 `this.tc = tc` 再做其他設定
3. **曲率計算是 O(頂點×面)** — 上萬面會卡死。改成掃一遍 O(面)，
   16,128 面從卡住變成 7ms。凡是「對每個頂點都查一次全部的面」都要警覺
4. **three.js 的事件名稱搜尋不到** — `dragging-changed` 是執行時用
   `屬性名 + '-changed'` 組出來的，字面搜尋會誤判成不存在。
   要找 `defineProperty( '屬性名'` 才對

5. **把只對特例成立的假設寫死** — 第 1 期的結構分析面板寫死
   「封閉 → 尤拉數一定是 2、角虧總和一定是 4π」。那時只有基本體，
   做不出有洞的東西，所以從沒出事。第 2 期一挖貫穿孔就變成 χ=0、
   角虧總和 0，面板把**完全正確的結果打了兩個紅叉**。
   正確的式子是 `χ = 2×實體數 − 2×貫穿孔數`、`角虧總和 = 2πχ`。

   → 這類 bug 不會讓程式當掉，但會讓人**不敢相信工具給的任何數字**，
     而那個面板存在的意義就是讓人相信數字。
   → **規則**：寫「應該是多少」的判定時，先問「這條規則的成立條件是什麼」，
     把條件寫進註解；而且判定邏輯要抽成不碰 DOM 的純函式，才測得到。
     `topologyCheck()` 就是這樣抽出來的。

6. **展開長度不能用網格量 —— 這條會讓實際下料出錯**　⚠ 第 3 期必讀

   圓弧在網格上被切成 `arcSeg` 段直線，**弦長永遠比弧長短**，
   所以拿網格去量會**往少的方向錯**，而且每道折彎都少一點。

   | 同一片折板（40 ＋ R2 的 90° 彎 ＋ 30） | 長度 |
   |---|---|
   | 網格量到的（弦長） | 73.12 cm |
   | 真正的展開長（弧長 r×θ） | 73.14 cm |

   差 0.03%，折三道就差到零點幾公分。雷切下去才發現就來不及。

   → **規則**：展開長度一律用 `bendDevelopedLength()`（真弧長），
     **絕對不要用 `mesh.area()` 或量網格頂點距離去反推**。
     第 3 期的展開圖、DXF、備料長度都走這個函式。
     測試裡刻意把「網格面積用弦長對」與「展開長度用弧長對」分成兩項，
     就是為了讓這個差別一直看得見。

7. **加厚時折彎處會被削薄**　⚠ 第 3 期做折彎補償時會再遇到同一件事

   `shell()` 第一版沿頂點法向推半個板厚，體積少了 0.076%。
   頂點法向是**角平分線**，只推 t/2 的話折角處實際厚度變成
   `t × cos(半夾角)` —— 90 度折就只剩 0.71 倍。

   → 正確做法是除以那個餘弦（鈑金與繪圖裡講的「尖角接合 miter」）。
     修正後 1／3／8 段 × 45／90／135 度全部精確到浮點誤差。
   → **更一般的教訓**：凡是「沿法向偏移」的運算，在不共面的地方
     都要想一下有沒有這個係數。肉眼看不出來，只有算體積才抓得到。

8. **板件不該做布林聯集 —— 那是語意問題，不是函式庫限制**

   陣列原本一律走布林聯集，板件（開放的面）整個失敗。
   當下的直覺是「想辦法讓布林吃下去」，但那是錯的方向：

   | | 該怎麼做 | 為什麼 |
   |---|---|---|
   | 實體 | 布林聯集 | 兩份重疊處只能算一次，接縫要真的縫起來 |
   | **板件** | **直接拼接，不合併**（`Mesh.merge()`） | 12 片一樣的側板就是 12 片，展開要分開出圖。黏成一體反而讓第 3 期分不出來 |

   → 判斷依據用**網格封不封閉**，不是物件的 `kind` ——
     kind 是使用者隨手可改的標籤，封閉與否才是幾何事實。
   → **拼接時折線一定要跟著搬過去**，掉了折線第 3 期就展不開。
     測試有一項專門盯這個（5 條折線的折板做成 5 片必須是 25 條）。

9. **規格上合法 ≠ 別人的軟體讀得動**　⚠ 凡是要送出去的格式都適用

   第一版 DXF 只寫了 HEADER／TABLES(只有 LAYER)／ENTITIES 三段。
   完全符合 R12 規格，AutoCAD 讀得動，**Illustrator 直接跳錯誤視窗**。
   缺的是：BLOCKS 區段（空的也要有）、LTYPE 表、STYLE 表、圖層 0。
   另外 `$INSUNITS` 是 R14 才有的變數，寫在 AC1009 裡是時代錯亂。

   SVG 那邊沒寫 `<?xml ... ?>` 宣告，Illustrator 根本沒認出那是 SVG，
   **當成純文字檔開**，畫面一堆標籤原始碼，中文還變亂碼。
   另外它不支援 `dominant-baseline`，所有標註會往上跳半個字高；
   線寬寫成 0.35 個使用者單位（1 單位 ＝ 1cm）＝ 印出來 3.5mm 粗，
   製圖標準大概 0.5mm，粗了七倍。

   → **規則**：要送出去給別的軟體開的格式，只能用最保守的寫法。
     不用選用屬性、不用新版才有的欄位、骨架寧可多寫幾十行。
   → **更重要的規則**：這類東西**沙箱驗不了**。
     語法檢查過、瀏覽器看起來對，都不算驗證過；
     **一定要 kang 用真正的目標軟體開過**。這條推翻了原本
     「沙箱能做靜態檢查就算驗證」的假設。

10. **圓弧至少要切兩段才認得出來**

    切一段的「圓弧」在網格上就是一個平面四邊形，跟一個倒角
    **完全一樣**，沒有任何幾何資訊能分辨。
    這不是演算法保守，是資訊真的不存在。
    → `prim.js` 的 `arcSeg` 下限設成 2，並在面板提示裡寫明理由。
    → 同樣的道理：等寬等角的連續折面會被判定成圓弧，
      因為它幾何上就是。要表達「刻意做成折面板」得等第 6 期的面編輯。

11. **「產生 `<a download>` 然後 click」有三個地雷**

    實測存出來的檔案叫「尚未確認的 636844.DXF」—— 那是 Chrome
    自己的暫存名，副檔名還被改成大寫，所以 Illustrator 認不得。
    **看起來像匯出功能壞掉，其實檔案內容完全正確。**
    另外還留下一堆沒下載完的 `.crdownload`。三個原因：

    | 地雷 | 後果 | 修法 |
    |---|---|---|
    | `<a>` 沒掛進 DOM | Chrome 忽略 `download` 檔名 | appendChild 再 click，click 完移除 |
    | blob URL 1 秒就 revoke | 下載被中斷 | 改成 5 秒 |
    | 連續下載多個檔案 | 只放行第一個 | 每個錯開 250ms |

    → 修法直接沿用**組裝系統結構說明表**已經驗證過的那一套，
      抽成 `js/out/save.js` 共用，兩個工具的存檔行為因此完全一致。
      **踩過的坑要往回看自己的專案，不要重新踩一遍。**
    → 順帶補上「指定存放位置」（File System Access API）。
      它只在**安全環境**可用：https 或 **localhost**，
      `http://192.168.x.x` 不算。用 IP 開會自動退回一般下載，
      勾選框淡化並在提示裡講清楚原因 ——
      **沉默地退回是最糟的做法**，使用者會以為勾選壞掉了。

12. **讀不出尺寸的圖，比沒有圖更糟**

    折彎多的件（圓角方塊 21 道）分段尺寸全擠在同一排，
    變成一團「0.9787.05.24」。看的人會以為自己讀到了。

    → 但也**不能因為放不下就不標**：折彎區只有 3.33cm，
      那個數字正是師傅畫線要用的，丟掉等於這張圖不能下料。
      改成錯開兩排，兩排都塞不下才放棄（並到備料明細去查）。
    → 測試加了一組**自動偵測標註重疊**的檢查。
      這件事測得到，是因為出圖刻意拆成「決定畫什麼」（純資料）
      與「怎麼畫出來」兩層 —— 又是同一條分層原則的回報。

13. **開新階段前要先確認「目的」，不是先確認「規格」**　⚠ 這條最貴

    第 3 期整期做完之後，kang 才說明做這套程式的用意是
    「建模拿去 3D 列印、展開拿去 CNC」。

    而原本的八期路線圖**完全沒有 3D 列印的出口**。
    「目標與範圍」只寫展開，唯一相關的一句是第 8 期順帶提到的 glTF ——
    而 glTF 根本不是 3D 列印的格式。
    **照原規劃走完八期，東西還是印不出來。**

    我從第 1 期做到第 3 期，一次都沒問過「這套做出來要拿來幹嘛」，
    只照著既有文件推進。文件寫得再詳細，也只是「當時想到的規格」，
    不等於「真正要達成的目的」。

    → **規則**：開新階段（或接手一個專案）之前，先問一句
      「這個做出來要拿來做什麼、給誰用、下一步會接到哪裡」。
      規格是從目的推出來的，順序不能反。
    → 補救很便宜（STL 匯出半天就做完，因為地基早就在了），
      但如果拖到第 8 期才發現，中間好幾期的設計取捨可能都要重來。
    → 這條也解釋了為什麼「目標與範圍」那一節現在最上面放的是
      **kang 的原話**，而不是規格 —— 規格會過期，目的不會。

14. **「目的」包含用什麼材料**　⚠ 這條跟第 13 條一樣貴，而且是同一個病

    第 13 條之後，AI 仍然從第一句就假設這是**鈑金工具** ——
    因為日誌通篇是鈑金語言（K 因子、中性層、折彎補償、最小折邊），
    材料表六種有三種是金屬。**沒問過實際用什麼材料，就照著文件往下推。**

    整整推了一場：花大量工夫追「圓錐展開偏短 0.16～2.5%」，
    直到 kang 主動說明「我們用紙材、發泡板、珍珠板、木板、壓克力，
    一種金屬都不用」，才發現**那個弦長本來就是對的**，
    而程式真正的錯是反過來的 —— 把正確的弦長拉成錯的弧長，
    4 角柱因此長了 **11.07%**。

    → **規則**：第 13 條要補一句 ——「目的」包含**用什麼材料、
      給誰做、做出來怎麼組**。這三件事沒問清楚，規格一定是猜的。
    → 這也是為什麼「開新對話時先讀這段」現在第四條就是材料，
      而且刻意寫成警告。

15. **弦長還是弧長，是材料問題，不是幾何問題**

    網格上的一段「圓弧」是一圈平面小面。它到底代表
    「真的被捲圓的曲面」還是「一圈平板拼出來的多邊形」，
    **幾何本身分不出來**（這就是坑第 10 條的另一面）。

    第 3 期把「一律當成捲圓的」寫死在幾何核心裡，等於替所有材料
    做了同一個假設。對板材而言那個假設一直是錯的：

    | | 程式給的 | 外緣相加（真值） | 誤差 |
    |---|---|---|---|
    | 4 角柱 | 188.50 | 169.71 | **＋11.07%** |
    | 6 角柱 | 188.50 | 180.00 | ＋4.72% |
    | 8 段圓柱 | 188.50 | 183.69 | ＋2.62% |

    → 修法是讓**材料規則**回答（`canRoll`），幾何核心只多 6 行。
      這是「規則分開、幾何共用」原則第 N 次派上用場。
    → **判準**：一段邏輯需不需要知道材料是什麼？需要就別寫在幾何核心裡。
      第 3 期問過這個問題，但漏掉了「圓弧修正」這一段。

16. **攤平的生成樹要先走最平的邊，否則共面的東西會被拆散**

    圓錐加底蓋展開後是一張**從中心炸出去的星芒**，尖刺互相交疊，
    外框 220.56×182.05，完全不能用。

    成因不是切割規則，是**走訪順序**：底蓋由 32 個共面三角形組成，
    先進先出的佇列會讓每個底蓋三角形各自從**旁邊那片側面**跨輪圈接上去，
    而不是接到隔壁的底蓋三角形，底蓋因此被拆散、散落在扇形四周。

    → 改成生成樹**優先跨過轉折角最小的邊**（共面的鄰居其實是同一片板子）。
      外框變成 126.33×149.41，星芒消失。
    → **不改變任何「哪些邊可以折」的規則，只改變先攤哪一條。**
      攤平是剛體運動，順序不影響每一片自己的形狀與尺寸。
    → 副產品：圓角方塊的四個圓角**終於被辨識成圓弧帶**（先前是圓弧 0），
      球冠的重疊偵測也開始生效。

    ── 這條之前繞了很久，值得記下來 ──
    一開始判斷是「輪圈是封閉的圓，不該當折線」，想從幾何推論出規則。
    **試了五種判準，五種全部誤傷合法的件**（方塊被拆成六片、
    折板 L 被拆散、底蓋跟每片側面都只共用一條邊……）。
    五次撞牆之後才回頭看真正的成因，而它跟切割規則無關。
    → **教訓：連續幾種「聽起來很對」的規則都失敗時，
      多半是問錯問題了，不是還沒想到第六種規則。**

17. **「中途對、最後錯」的 bug，只測中途抓不到**　⚠ 這條是送出去的檔案錯了

    100×60 的平板加厚後放在 Y=44cm 處匯出 STL：

    | | 外框 | 體積 |
    |---|---|---|
    | `dropToBed` 前 | 1000×600×**2** mm | +1,200,000 ✓ |
    | `dropToBed` 後 | 1000×600×**439** mm | **−86,600,000** ✗ |

    原因：`triangles()` 做扇形三角化時，`p0` 每個面只算一次，
    而 `out.push({ a, b: b.clone(), c: c.clone() })` —— **`a` 沒有 clone**。
    一個四邊形切出來的兩個三角形因此共用同一個 `Vector3`，
    而 `dropToBed()` 是就地平移，共用的頂點被**減了兩次**。

    → 修法：三個頂點都 clone。
    → **為什麼一直沒被發現**：三角形面（方塊、圓錐、球）每個面只產生
      一個三角形，共用不到。這個 bug 只在**有四邊形面**的東西上發作
      —— 平板、折板、管、圓柱側面、圓角方塊，而那正是板件。
      而且物件**在原點時不發作**（位移是 0，直接 return）。
    → **第 3.5 期的外部驗證用的是錐體 —— 剛好是不受影響的那一類。**
      所以那次「外部驗證通過」其實完全沒有涵蓋板件。
    → 折板其實也一直是壞的（外框 1030.8mm 應為 321.8mm），
      只是體積剛好還是正的，沒被判「印不出來」，所以沒人注意。

    ── 三條規則 ──
    → **多段轉換的輸出路徑，驗證要放在最末端**，而且要用
      「跑完全程之後仍然守恆的量」去對（這裡是體積與外框）。
      原本的測試量的是 `mesh.volume()` 與加厚後的體積 —— 那兩個一直都對。
    → **外部驗證挑樣本要涵蓋不同的「網格結構」，不只是不同的形狀。**
      錐體與平板在使用者眼中是兩個形狀，在這個 bug 眼中是
      「全三角形」與「有四邊形」兩類 —— 只驗了前者等於沒驗。
    → **要測病因，不只測症狀。** 症狀（體積變負）會變，
      病因（任兩個三角形共用頂點物件）不會。
      現在兩者都測，日後有人為了省一次 clone 又改回去會立刻被擋下。

18. **誤報比漏報更糟**

    「算不準的展開要講出來」第一版的判準是「折線沒有全部互相平行」，
    結果**鋼板方塊**攤成十字型時 5 道折線分兩個方向，
    於是在**最常見的件**上跳出「請勿據以下料」——
    可是方塊根本沒有圓弧（圓弧 0、尖角 5），那張圖完全正確。

    → 判準改成直接問「**這是不是錐面**」：折線是否放射狀匯聚到同一點。
      分離度很寬：圓錐（seg 4～64）匯聚比例一律 100%，
      方塊 2/5、圓角方塊 2/21、角柱 2/7、球 127/511 —— 非錐面最高 25%。
    → **測試那一節寫了六個正面案例卻漏掉鋼板方塊**，因為只測了
      壓克力方塊，而壓克力的折線全被切開、等於沒有折線 ——
      **看起來測過了，其實測了個空的。**
    → 誤報會讓人學會忽略整個警告欄，那警告欄就等於不存在。
      這跟坑第 5 條（讓人不敢相信工具給的數字）是同一種傷害。

19. **「少了某個呼叫」不等於漏了 —— 要跟著呼叫鏈走一層**

    2026-08-22 審查時判定「展開視窗的材質選單 `onchange` 沒有呼叫
    `saveOpt()`，選了材質關掉再開會跳回預設」。**這個判定是錯的。**

    `mat.onchange` 確實沒有直接寫 `saveOpt()`，但它呼叫了 `_syncK()`，
    而 `_syncK()` 的最後一行就是 `saveOpt(this.opt)`。材質有存到，
    重開會讀回上次選的。

    → **規則**：說某個副作用「沒做」之前，要沿著呼叫鏈往下看一層，
      不能只在同一個函式體裡找那個函式名。grep 到「這裡沒有」
      只證明「這一行沒有」。
    → 真正沒存的是手改 K 值（`kIn.onchange` 設完就 `run()`），
      但那是**刻意的** —— K 是臨時覆寫，換材質時會被清成 `null`
      回到該材質的建議值，不留下來才對。

20. **正確的數字，錯誤的意思**　⚠ 做介面時最容易犯

    展開引擎回傳的 `pieces.length` 是「**幾張圖**」，不是「幾片」——
    它把一樣的片合併成一張圖加一個 `qty`（「12 支一樣的橫料出
    一張圖 ×12，不是 12 張一樣的圖」，第 2 期就這樣設計的）。

    | 壓克力方塊 60×45×40 | 值 |
    |---|---|
    | `pieces.length`（張數） | 3 |
    | `stats.total`（片數） | **6** |

    六個面是 3 種形狀各 2 片。介面上如果顯示 3，使用者會少切一半的料 ——
    而那個 3 本身完全正確，程式也沒有 bug，所以不會有任何東西報錯。

    → **規則**：把內部的數字放到介面上之前，先問一句
      「這個數字的**單位**是什麼」。`length` 特別危險，因為它總是「某種東西
      的個數」，而那個「某種東西」到底是什麼，只有寫的人知道。
    → 測試現在把兩個數字都固定住，另外加一項交叉對答案
      （各張圖面積×份數相加 ＝ `stats.area`），盯 `qty` 有沒有被漏乘。

21. **數學上正確，但使用者以為壞了**

    分片標記做完之後實測：60×45×40 的方塊**標一條邊，片數仍然是 1**。
    要切到足以把面的鄰接關係切斷才會多一片（切滿一個面周圍 4 條才變 2 片）。

    這完全正確 —— 但使用者標了一條邊、畫面毫無反應，第一個念頭一定是
    「壞了」。跟坑第 5 條（面板把正確結果打紅叉）、第 18 條（在最常見的件
    上誤報）是同一種傷害：**讓人不敢相信這個工具。**

    → 修法不是改演算法，是**補回饋**：面板即時顯示「展開後 N 片」，
      每次操作的 toast 也講一次，另外給一顆「點面的中央 ＝ 整圈切開」
      讓人一按就看得到結果。
    → **更一般的規則**：一個操作如果「有時候看起來沒作用」，
      那就必須有東西**持續顯示它到底有沒有作用**。
      不能靠使用者自己推理。

22. **畫面更新的熱路徑上不能有 O(全部)**　⚠ 坑第 3 條的重演

    接縫線第一版每次 `view.sync()` 都走訪一遍所有的邊來決定要不要重建。
    而 `sync()` 在**拖曳 gizmo 時是每一幀都跑的** ——
    16,128 面的球有兩萬多條邊，每幀掃一遍再組一個字串，手感直接毀掉。

    跟坑第 3 條（曲率計算 O(頂點×面) 讓上萬面卡死）是同一種錯，
    差別只在這次在畫面更新的熱路徑上，而且**平常測試的小模型完全看不出來**。

    → 改成只有兩種情況才重建：網格換人了，或**有人明講**
      `markSeamsDirty()`。標接縫本來就是使用者一次一下的動作，
      明講一聲的成本是零；讓畫面每幀去猜有沒有變，成本卻是所有人一起付。
    → **規則**：寫進 `sync()` / 每幀迴圈裡的東西，先問
      「這段會不會隨模型大小成長」。會的話就要有辦法跳過它。

23. **問「下游拿去做什麼」，比問「你要什麼功能」有用**
    　　⚠ 坑第 13、14 條的第三次 —— 但**這次是動手前抓到的**

    正要開始做「板材尺寸上限檢查」（一片放不進板材時標紅）。
    那是我自己從「分片」推出來的：分片是為了讓每片切得出來，
    所以應該檢查塞不塞得下 —— 聽起來完全合理。

    動手前隨口問了一句板材尺寸，kang 的回答是：
    「**其實這問題並不很重要……因為只要能轉出 DXF，
      還會經過 AI 軟體整體處理後，才會提供 CNC 處理。**」

    整個前提垮了。DXF 會先進 Illustrator 排版才給 CNC，
    **排版時就看得到塞不塞得下**，而且 Illustrator 看得到全部的片
    一起排，判斷比建模器好。我要做的是一件重複、而且比較差的事。

    改問「那份 DXF 進 Illustrator 之後什麼最麻煩」，答案是
    「簡單的展開圖很好區分，**遇到複雜的分切展開，標示就需要研究一下**」
    —— 那才是真正缺的東西，也正是他最早那句話的後半段
    「切割後**再組裝結合**」。於是做了接合編號。

    ── 跟前兩次的差別 ──
    第 13 條（沒有 3D 列印出口）與第 14 條（不是鈑金）都是**做完才發現**，
    補救花了額外的工。這次是**還沒動手就攔下來**，成本是一個問題。

    → **規則**：規劃一個功能之前，問的不該是「你要不要這個功能」
      （對方多半會說好），而是「**這個東西產出之後，你下一步拿它做什麼、
      交給誰、用什麼軟體開**」。下游決定了上游該做什麼，
      而下游是只有使用者知道的事。
    → 這條跟第 13 條的「先確認目的」是同一件事，但更具體：
      **目的藏在下游。** 問下游就會問出目的。

24. **自由度沒鎖滿，比算錯更糟**　⚠ 這條的觸發點很特別，值得記住

    貼合的第一版，邊對邊只做「邊方向平行 ＋ 中點重合」。
    數學上沒錯，但那樣物件還能**繞著那條邊自轉任意角度** ——
    兩條邊仍然完全重合，成品方位卻是隨機的
    （程式挑的是「轉最少」的那個角度，不是任何人想要的那個）。

    ── 觸發點 ──
    kang 的回報不是「這個是錯的」，而是
    **「點對點、邊對邊我沒辦法驗證準確，不知道是不是操作有誤」**。

    我一開始還往「操作有誤」的方向想。但那句話本身就是訊號：
    **使用者說不出對錯的時候，通常不是他不會用，
    是那個東西沒給他判斷的依據。**

    → **規則**：設計一個操作時要先問「**這個操作的結果唯一嗎**」。
      不唯一的話，要嘛補條件補到唯一，要嘛明講出來。
      放著不管的話，使用者根本無法判斷它有沒有做對 ——
      而算錯還會被發現，隨機只會讓人默默不敢用那個功能。
    → 修法：邊對邊多鎖一個自由度（相鄰面法線反向）。
      真的補不到唯一時（沒有相鄰面可參考），就誠實標成 ambiguous
      並跳提示，不假裝結果是確定的。
    → 這跟坑第 21 條（數學上正確但使用者以為壞了）是同一個家族：
      **正確不等於可用，可用的前提是使用者驗得出來。**

25. **浮點數不能用精確比對**

    貼合的 `moved` 判斷寫成「四元數 `equals()` 單位四元數」。
    那是精確比對，而「已經貼好了」算出來的是 `(0, 0, 0, 0.9999999999)`，
    永遠不等於單位四元數。

    於是每次都回報「移動了」—— 使用者按第二下想確認有沒有貼上去，
    看到的卻是「又動了一次」，然後開始懷疑前一次到底有沒有成功。

    → 改成看**轉了幾度**：`angle = 2·acos(|w|)`，單位四元數是 0 度。
    → **規則**：浮點數的相等判斷一律要有容許值。
      而且要挑一個**有物理意義的量**去比（這裡是角度，不是四元數分量）——
      那樣容許值才選得出合理的數字，也才講得清楚「差多少算一樣」。
    → 這種 bug 不會讓程式當掉，只會讓回報訊息騙人，
      跟坑第 5 條（面板把正確結果打紅叉）是同一種傷害。

26. **判準要挑「有物理意義的量」去比**　⚠ kang 實測抓到的

    「疊到的高度跟模型高度湊得剛剛好嗎」判準寫 `> 1e-6`（公分）。
    布林聯集出來的物件座標帶著 Manifold 的 1e-4 級精度殘留，
    模型高不是剛好 60 而是 60.0001，於是面板跳出
    **「還差 0 cm 沒疊到」** —— 一句自相矛盾的警告。

    1e-6 cm ＝ 十奈米。**那是浮點數的尺度，不是板材的尺度。**
    這個專案切的是珍珠板與壓克力，0.1mm 已經遠低於任何切得出來的東西。

    → 改成 `FIT_TOL = 0.01cm`，而且**同一個容許值要用在兩個地方**：
      顯示警告時，以及「剩下的填滿」算片數時。
      只修警告的話，殘留是負的（59.9999）時「剩下」會**少算一整片**，
      變成「不跳警告但少一片」，比原本更難查。
    → 這跟坑第 25 條是同一件事的兩面：那次是拿四元數分量比，
      這次是拿浮點誤差當公差。**容許值要挑一個講得出物理意義的量。**

27. **不要被最極端的那個樣本綁死**

    定位孔第一版要求「每一個孔都要在每一片上成立」。聽起來最安全，
    球體實測整個垮掉：球切 15 片，頭尾是直徑只有 10cm 的小圓片，
    兩個孔被擠進那個小圓裡，**只距離 2.66cm** ——
    在一個 60cm 的球上等於白放第二個孔。

    | | 中段兩孔的間距 |
    |---|---|
    | 舊：所有孔都要每片成立 | 2.66 cm |
    | 新：孔①全域、其餘各段自求 | **20.06 cm** |
    | 段2 改成 3 孔 | **26.83 cm** |

    → 拆開來看：**只有真正需要全域的那個約束才全域**（孔①是通到底的
      那根桿子，非全域不可），其餘的放寬到它實際作用的範圍。
    → 一併學到的：**放寬要講清楚代價**。孔②③ 只穿得過那一段的片，
      不是同一根桿子 —— 不寫在圖上的話，現場會拿一根長桿子想穿過全部，
      穿到一半才發現穿不過去。
    → 順帶修掉一個反方向的錯：第 2 孔的安全度門檻本來取「孔①的四成」，
      結果球的中段被綁在離邊緣 8.8cm，白白損失一半間距；
      而且那條線**會隨物件大小浮動**，同一個形狀放大兩倍判準就變，
      講不出道理。改成「使用者自己設的淨距的兩倍」。

28. **使用者自己選的東西，不能當成錯誤**

    分段表可以指定每段幾個孔。kang 刻意把球的頭尾設成 1 孔
    （那幾片是圓的，轉了也看不出來），畫面就跳出**兩個紅色警告**
    唸同一件事：「單孔可以繞著孔轉」。

    他明確選的東西被程式當成錯誤，而且同一句話拆成兩框。

    → **紅色只留給「程式做不到你要求的事」**（要 3 孔只放得下 2 孔、
      兩孔太近等於白放、有片是空的、孔快切到料緣）。
      自己選的後果用藍色說明，而且合併成一句講一次。
    → 這是坑第 18 條（誤報讓人學會忽略整個警告欄）的軟性版本：
      不是報錯，但一樣會讓紅色失去意義。
    → 測試現在有一項專門盯「使用者自己選 1 孔時警告數必須是 0」，
      免得日後有人把它加回去。

    ── 同一輪順手做的：gizmo 軸標 ──
    kang 回報「操作上時常搞錯 XYZ」。三根拉桿尖端外面各標一個字。
    **顯示與否只看一件事：gizmo 自己看不看得見** ——
    不另外判斷「有沒有選取」「是不是分片模式」，那些條件本來就決定了
    `helper.visible`，再抄一份到這裡，兩邊遲早不一致，
    結果就是「箭頭在但字不見」或反過來。
    寫的時候抓到一個：**縮放模式在 three.js 裡永遠用物件的本地軸**
    （`scale always oriented to local rotation`），只看 `space` 的話，
    物件轉過角度時字會跟箭頭錯開 —— 而那正是最需要看清楚的時候。

29. **座標系換手時，「直覺的繞向」是錯的**　⚠ 這種錯畫面上看不出來

    擠出把輪廓的 (x, y) 對應到世界的 (x, z)。頂面的一圈照「在 (x,z) 裡逆時針」
    寫下去，法向卻是朝**下**的 —— 因為 **x̂ × ẑ ＝ −ŷ**，
    (x,z) 對 +Y 而言是左手系。結果整個模型法向朝內，體積算出來 **−500000**。

    → **這種事不能靠推理，要靠對答案。** 測試盯「體積 ＝ 面積 × 高，
      而且是正的」，第一次跑就擋下來了。
    → 為什麼一定要擋：**法向朝內的模型在畫面上看起來完全正常**
      （three.js 預設雙面算光），只有匯出 STL 的列印前檢查才會說
      「體積是負的」。而那已經是要送出去的時候了。
    → 凡是把 2D 座標搬到 3D 平面上的地方都要問一次這個問題。
      剖面分切的 `AXES` 那張表也是為了同一件事（見坑第 27 條旁邊那條
      「外輪廓必須逆時針」的測試）。

30. **重複的點不能只靠索引排除**

    帶洞多邊形的三角化用「開橋」把孔接進外圈：橋墩那兩個點會**在同一圈裡
    出現兩次**，那正是橋的意思。耳切法檢查「有沒有別的點落在耳朵裡」時，
    我用索引排除相鄰的三個點 —— 但另一個副本的索引不同、座標相同，
    於是每一個角都被判定成「裡面有點」，**一個耳朵都找不到**。

    症狀：三角形數回傳 0。**不報錯**，只是蓋子整片消失。

    → 改成比**座標**。判斷「是不是同一個點」時，索引是實作細節，
      座標才是事實 —— 跟坑第 8 條（用網格封不封閉判斷，不用 kind 標籤）、
      坑第 21 條（用幾何事實不用標籤）同一條原則。
    → 測試現在同時盯「三角形面積總和 ＝ 多邊形面積」與
      「三角形數量 > 0」。只驗面積的話，0 個三角形的面積總和是 0，
      而 0 ≠ 9600 會被抓到 —— 但訊息會指向錯的地方。
      **兩個都測，壞掉時才知道壞在哪。**

31. **要兩邊一致的東西，就別讓它有機會不一致**　⚠ 這條的修法比錯誤本身值錢

    接合編號從「一條邊一個」改成「一段一個」。第一版的做法是
    **從一條切割邊往前後走，把同一段串起來**。寫得出來，跑得動，
    面板那邊也確實從 198 個號碼併成 7 段。

    但**兩側走出來的分段不一致**：面板併成 7 段，側邊條還是 198 段，
    於是「每個編號恰好出現兩次」直接破功 —— 連方塊都從 12 組變 18 組。

    → 第一個念頭是「把走訪寫得更小心一點，讓兩邊對齊」。**那是錯的方向。**
      改成定義在**無向邊上的聯集**：兩條切割邊共用一個頂點、
      而且那個頂點上其他的折線全是平滑的，就併成同一段。
      這個定義跟走訪順序無關，所以兩側算出來**根本不可能不一致**。
    → **規則**：需要「兩邊算出同一個答案」的時候，
      與其小心地讓兩條路對齊，不如換一個**只有一條路**的定義。
      前者要靠紀律維持，後者靠結構保證。

32. **一條邊的兩側，可能都在同一片上**

    接上一條。改成聯集之後，S 字的編號 15 **只出現一次**。

    那是側邊條自己的接縫：環狀的側牆攤開時引擎要找個地方剪開一刀，
    而那一刀的兩端都留在同一條長條上（長條要繞回去接自己）。
    我用「段」當唯一的 key，兩側被併成同一組，所以只畫了一個號碼。

    → key 要再帶一個「哪一側」（同一對半邊裡 id 小的算 0、大的算 1）。
    → **為什麼抓得到**：因為第一次做接合編號時就把
      「每個編號恰好出現兩次」寫成測試了。**出現一次代表有一端接不回去，
      而圖看起來完全正常** —— 沒有那條測試，這個錯會一路走到現場。
    → 順帶一個提醒：寫「這種情況不會發生」的假設之前，先想一遍
      **封閉的、繞回來接自己的**那種形狀。坑第 5 條（把只對特例成立的
      假設寫死）也是同一個家族。

33. **教訓寫在別的功能底下，就等於沒寫**　⚠ 這條的重點不是錯誤本身

    匯入線稿時，我把網格留在 SVG 的畫布座標上，然後把**物件**搬到
    `pos = -中心` 去抵銷。畫面上東西置中了，但物件的**原點**在很遠的地方
    —— gizmo 長在原點上，**旋轉與縮放也都繞著那個遠處的點在轉**。

    kang 一打開 3D 畫面就問「控制桿為什麼不在物件上」。

    → 修法很簡單：置中做在**幾何**上，`pos` 留在原點。
    → **真正該記的是這個**：`core/align.js` 的開頭早就寫了
      「**絕對不能拿 `obj.pos` 當物件位置**，`pos` 是原點，
      而網格不一定以原點為中心」，而且那一段還特地說折板、布林、陣列
      都會偏一邊。**我照樣踩進去了。**

      為什麼？因為那條警告寫在「對齊與均分」那一節，
      而我當時在寫「匯入」。**同一個陷阱在不同功能底下會長得不一樣，
      而人只會讀自己正在做的那一節。**
    → 所以這種跨功能的原則要放進「關鍵決定」或「踩過的坑」，
      不能只留在第一次遇到它的那個功能的說明裡。
      （這也是為什麼坑的編號一路排下來、不分功能。）

34. **不要在日誌裡寫一個不存在的退路**

    「一份 SVG 匯成一個物件」那段，我寫了
    「**要拆開的人可以用現成的『打散』**」。

    打散當時只對**陣列**有效，匯入件根本按不到那顆按鈕。

    → 危險的地方不是那句話錯了，是**我靠那個退路做了設計決定** ——
      「反正可以打散」所以預設合成一個。退路不存在的話，
      那個決定的前提就垮了。
    → 日誌是給未來的 AI 讀的。**讀的人會照著它做決定，
      而且不會回頭驗證每一句話。** 寫一個不存在的功能，
      比不寫更糟。
    → **規則**：日誌裡提到「可以用某某功能」時，
      要嘛當場確認它存在，要嘛寫成待辦。


---

## 第 6 期第一刀：拉點／拉邊／拉面（2026-08-23）—— 測試擋下兩次

新增 `js/core/edit.js`（1 檔）＋ 測試 898 → **949 項**。
**既有程式一行都沒改**（core / build / unfold / slice / out / view 行數不變），
只有 `tests/run.mjs` 加了一個 import 與一整段。

### 動工前的唯讀調查（結論比預期好）

`mesh.js` 32 個方法**全部是讀、標記、整體變換、建構**，
**沒有任何改變拓撲的 API**（加頂點、分裂邊、擠出面都不存在）。
擠出面非它不可，所以那是第二刀。

**但拉點根本不需要它** —— `Vertex.p` 就是一個 `THREE.Vector3`，改它就好。
所以第一刀的核心幾乎是免費的，工全在「改完之後的連帶重算」。

### ⭐ 測試擋下的第一件：「一個面」不是三角形

第一版 `pushFace()` 對命中的那**一個三角形**動手。方塊 60×45×40
推頂面 +10，體積只增加 **20000** 而不是 24000 —— 因為方塊在網格裡是
12 個三角形，頂面是其中兩個，只推了一半。

**而這條警告 `unfold/seam.js` 的 `cutAroundFace()` 早就逐字寫著：**

> 「一個面」指的是共面區域，不是三角形。方塊在網格裡是 12 個三角形，
> 但使用者看到的是 6 個正方形面。

**照樣踩進去了。** 原因就是坑第 33 條那句：教訓寫在別的功能底下就等於沒寫
（那條寫在「指定分片」，而我在寫「編輯」）。這條已升格進 `PROJECT_LOG.md`
「鐵律」二，不再只留在 `seam.js` 裡。

修法：`regionOf()` 先跑 `planarRegions()` 取整個共面區域再動手。

### ⭐ 測試擋下的第二件：拿方塊測不出「面平不平」

「自由拉一個頂點會把面拉歪」這條測試，用方塊寫，**永遠是 0 個不平的面** ——
因為三角形恆為平面。

實查各參數體的面：`box` / `plate` / `cylinder` / `cone` / `prism` / `sphere`
**全部是三角形**，只有 `tube`（64 個四邊形）與 `roundBox`（20 個四邊形
＋ 2 個 20 邊形）有真的多邊形面。測試改用管，才驗得到。

→ 這是坑第 17 條「外部驗證挑樣本要涵蓋不同的**網格結構**，不只是不同的形狀」
　的第二次，只是這次發生在自己寫的單元測試上。

### 另外兩個寫測試時才發現的事實

- **珍珠板方塊展成 1 片，不是 6 片。** 6 片 3 張那組基準數字是**壓克力**的
  （銑 45 度 V 溝可折 vs 折不起來）。我一開始照 6 寫，被打回來。
- `buildPrim` 的簽名是 `buildPrim(type, params)`，不是 `buildPrim({type, ...})`。

### 三件連帶重算（`refreshAfterEdit()`）

順序不能換：**法向 → 折線 → smooth**，後兩者都要問 `dihedral()`。

- `remarkFolds()` **不能直接用 `mesh.autoMarkFolds()`** —— 那支用 `setRole`
  直接覆蓋，會把使用者標的 CUT 洗回 FOLD。測試有一組**對照**專門證明
  「autoMarkFolds 真的會洗掉」，免得日後有人覺得多寫一支是多餘的。
  另外它比 `autoMarkFolds()` 多做一件：清掉已經被拉平的過期 FOLD（那支只加不減）。
- `demoteSmooth()` **單向，只關不開**。門檻沿用真轉角那個 3 度
  （`svgPath.js` 的 `DEFAULT_CORNER_DEG`），不另外發明數字。
  128 段圓柱那條測試盯的是**不能誤關** —— 512 條夾角在門檻內的 smooth 一條都沒少。
- `moveVerts()` **刻意不自己呼叫重算** —— 拖曳 gizmo 時它每幀都跑，
  而重算是 O(邊數)。放進熱路徑就是坑第 3、22 條的第三次。

### 最後一組測試才是重點

「改得動」不等於「改完還能出圖」。所以最後一組驗的是**整條下游**：
推高 10 之後展開總面積 ＝ **15800**（＝ 2×(60×55 + 60×40 + 55×40)，手算對得起來）、
體積 132000、片數不變；換成壓克力仍然是 6 片 3 張。

### 順手留給第二刀的地雷

`mesh.js` 的 `_copyRolesTo()` **只搬 `role`，不搬 `smooth`**。
擠出面最省的做法是走 `Mesh.fromFaceList()` 拆掉重建，
那條路上這個缺口會讓匯入件的展開圖從 12 處變回 196 道折彎，
**而且座標完全正確、看不出哪裡不對**。已寫進待辦與規格檔。

### 還沒做

**介面。畫面上按不到。** 第二輪要做選取過濾器三顆按鈕、gizmo 掛到子元素、
拖曳手感 —— 那一輪沙箱一律驗不了，只能靠 kang 實測。
（分片那一項也是這樣分兩輪的：`08f5b90` 資料層 → `efa0c3b` 介面。）

---

## 第 6 期第一刀的介面，以及三個只有實測抓得到的錯（2026-08-23）

`39051cd` ＋ `270cb24`　測試 949 → 963 項

資料層那輪畫面上按不到，這兩輪把它接上工具列，並修掉實測抓出來的問題。

### 兩個寫的時候才發現、但非做不可的東西

**一、`scene.js` 要補 `markGeomDirty()`。**
`_updateNode()` 用 `node.userData.geomKey !== mesh` 判斷要不要重建 geometry，
而編輯是**就地改同一個 mesh 物件的頂點座標** —— 參考值一模一樣，
那個判斷永遠是 false，畫面會停在舊的樣子。
使用者看到「我拉了但東西沒動」，而資料其實已經改了 ——
**畫面與資料不一致，比單純沒反應更難查。**

**二、三個模式（編輯／分片／貼合）必須互斥。**
`pointerup` 的判斷鏈是照順序寫的，排前面那個會把後面的整個吃掉 ——
開著編輯就再也點不到分片，**而按鈕看起來是亮的**。
畫面說「分片開著」實際上沒作用，比按鈕沒反應更難查。

### ⭐ kang 的截圖抓到三個沙箱永遠驗不到的錯

**一、「面」的黃色標示畫成蝴蝶結。**
成因＝拿 `regionOf().verts` 那份頂點去串成封閉迴圈，而那是從 `Set`
出來的、**順序任意**。幾何完全正確（4 個頂點沒錯、拉起來也對），
**只有畫出來的意思是錯的** —— 坑第 20 條「正確的數字，錯誤的意思」的圖形版。
修法＝新增 `regionBoundaryEdges()`，畫區域的邊界邊。

**二、面板寫著「還沒有選取物件」。**
`refresh()` 開頭那個 `!obj` 早退 —— 編輯選的是子元素，物件本身沒被選取
（`sel.active` 是 null），所以每次都走那條早退。**畫面在否認他剛做的事。**

**三、我在規格檔寫了一個不存在的功能。**
原文寫「拉面預設沿法向，自由三軸也給但要使用者自己切過去」。那是假的：
`pushFace()` 只有測試在呼叫，介面一次都沒用到，開關根本不存在。
是 kang 把一個面往 X 拉開、方塊變斜的截圖照出來的。

**這條坑（鐵律六「不要寫一個不存在的退路」）我當天早上才親手抄進鐵律，
下午就犯了。攔下來的是他的截圖，不是文件。**
所以鐵律六補了一句可執行的動作：**確認方式 ＝ grep 那個函式名，
看有沒有介面在呼叫它。**「函式寫好了、測試也過了」不等於「使用者按得到」。

順帶釐清一件原本判斷就錯的事：擔心「自由移動會把面拉歪」對**整個面**
不成立 —— 一整個共面區域一起平移是剛體運動，面一定還是平的。
真正會拉歪的是只移動一個頂點或一條邊。

### 順手用數學回答了「拉點正確嗎」

kang 問截圖裡的拉點對不對。不用眼睛看，用手算對答案：
頂點從 (30,22.5,20) 拉到 (30,38.5,20)，屬於頂面 1 個三角形，
體積增加 6400 ＝ (1/3)×1200×16，與理論值精確吻合；拉完仍封閉、尤拉數 2、
展開面積 ＝ 網格表面積。**答案是對的。**

但驗算順便照出一件事：**拉方塊頂面的四個角，體積差兩倍**（6400 vs 12800）——
對角線兩端的角屬於兩個三角形（屋脊），另外兩個只屬於一個（單斜面），
**而那條對角線在拉之前看不見**。
這跟「指定分片」第 3 個決定是同一件事的反面：那邊擋住了看不見的對角線，
這邊看不見的對角線決定了結果的形狀。不是 bug（Blender 也一樣），
不修，但用測試釘住 —— 日後有人改三角化會亮紅燈。

## 第 6 期第二刀：擠出面（2026-08-23）—— 先調查，才動手

`88784e3`（資料層）＋ 介面那輪　測試 963 → 1018 項

⭐ **這一輪是 kang 立下「先調查清楚再動手」那條規則之後的第一次實作**，
照他給的順序走：本專案有沒有解過同一個問題 → 實際讀要動的程式 →
kang 手上有沒有現成的 → 開源怎麼解。

### 調查換到三件推理推不出來的事

**一、`build/extrude.js` 第 91 行已經解過側牆繞向**，而且踩過
「體積 −500000」那個坑。它的答案跟我自己手算 Newell 法向的答案一致 ——
**兩條獨立的路得到同一個結論，比只有推理可靠。**

**二、Blender 手冊點出我沒想到的分岔**：邊界邊只屬於一個面時要「複製」
選取的面，否則「不複製」（免得留一個面在實體裡面）。也就是封閉實體與
開放邊緣是兩種行為。第一版只做前者，開放邊緣擋下來並說明原因 ——
「複製還是搬移」是使用者的取捨，不該由程式猜。

**三、實查九種參數體，一條 `smooth` 都沒標**，只有匯入的擠出件會標。
所以側牆垂直邊的 smooth 改成「**只繼承上游那條被延長的邊，不從幾何猜**」。
刻意不做「轉角小於 3 度就算平滑」：標成 smooth ＝ 這裡不算折線，
猜錯會**漏掉折彎**，而漏折彎 ＝ 東西做出來是錯的；標多了只是多一道線。
**這個方向不對稱，所以寧可不猜。**

### 一個先被當成「前置」、其實是既有 bug 的東西

`90d65d7`　測試 963 → 974 項

本來只打算補 `_copySmoothTo()` 當擠出面的前置，一驗才發現
**`clone()` 與 `transformed()` 一直都在把 `smooth` 掉光**，
而 `part.js`（展開前的縮放）、`array.js`（陣列鏡射）、`slicePanel.js`
都走那兩支。

實測（自由曲線的擠出件，形狀完全沒變、只是縮放 2 倍）：
折彎處從 **5 處變成 45 處**。那正是「196 道折彎、一團綠色數字」的同一個症狀。

⚠ **要用自由曲線才驗得到。** 等距取樣的半圓、參數體的圓柱，
幾何猜測（等寬等角）本來就抓得到，掉了 smooth 也看不出來 ——
我一開始拿圓柱測，兩次都顯示「沒事」。
**這是坑第 17 條「樣本要涵蓋不同的網格結構」的第三次。**

### 實作決定

- 走 `fromFaceList()` **拆掉重建**，不做半邊手術（半邊接線錯了不會報錯）
- 邊界頂點複製一份、區域內部頂點直接搬（複製會留孤點，而孤點不會報錯）
- 側牆繞向 `(a, b, b', a')`
- 標記**不能只叫 `_copyMarksTo()`** —— 蓋子搬到新頂點，索引配對整組不同，
  蓋子內部的邊要自己再搬一次，否則使用者標在蓋子上的分片會安靜消失
- 回傳新網格，不改原網格（原網格還在 Undo 的快照裡）

寫的時候自己抓到一個坑第 3 條：`byPair` 那段原本寫成 `out.verts.indexOf()`，
是 O(頂點×邊)。改成先建索引表。

### 介面：kang 拍板方案 C

三條路各有代價，他選了 C：**按一下先長「一個吸附格距」，新蓋子自動選中，
再用已經驗過的「拉面」調到想要的長度。**

好處是**完全不需要新的拖曳邏輯**。拖曳決定距離的話，
擠出是拆掉重建，每拖一格就要重建一次網格 —— 上千頂點的匯入件會卡，
Undo 也難處理。**一個動作一件事。**

預設距離用吸附格距：那是使用者自己已經設定的尺度，講得出物理意義（鐵律三）。

🔴 `selectFace()` 一定要在 `commit()` **之後**呼叫 —— `commit()` 走
`revalidate()`，而擠出換掉了整個網格物件，那裡會把子元素選取清掉
（本來就該清，舊的 `Face` 參考已經不在文件裡了）。先選後 commit 等於白做。

### 驗過的數字（全部手算得出來）

方塊擠出 20 → 體積 156000、V 8→12、F 12→16、χ 仍為 2；負值 −15 → 72000；
連續擠出三次每次 ＋24000；斜面體積是正的（繞向錯了會變負）；
管端面兩個邊界迴圈、體積增加 ＝ **16 邊形環面積** × 10
（不是理想圓 —— 一開始拿理想圓比，差 2.6%，那不是誤差是我公式用錯）；
蓋子上的 CUT 存活；原網格沒被改；`capFace` 回傳的是對的那一片。

**kang 實測通過。**

---

## 第 6 期第三刀：變換 ＝ 種類 × 方向 × 中心（2026-08-23，`94f6c3f`）—— 一行寫死擋住整個第 6 期

「三件事」的第二件。做完之後**梯形、收尖、斜面推拉都做得出來了**。

### 起點：一行註解，以及它擋住了什麼

`select.js` 的 `_attachEditProxy()` 有一行：

> 元素只能移動，不能旋轉縮放 ——「把一個頂點旋轉 30 度」沒有意義

**那句話對「點」成立，對「面」完全不成立。** 而那一行把
拉梯形、收尖、推斜面、擠出後轉方向**整組**擋在門外。

`外部參考調查.md` 第 1 節把它列為「這個專案的功能邏輯不從推論來」的頭號證據 ——
它不是寫錯，是**只驗證了一種情況就推廣成規則**。

### 照 Blender 的三層分解做（讀邏輯，沒有抄程式碼）

`外部參考-Blender編輯.md` 第 3 節：**變換 ＝ 種類 × 方向 × 中心**，三個正交。
分開之後，「沿法向擠出」就不再需要一個寫死的工具，
它是「移動 × 法向 × 任意中心」的一個組合。

刻意只取一小塊（過篩子：對板材切割有沒有用）：

| 概念 | 做了 | 沒做 |
|---|---|---|
| 種類 | 移動（已有）＋ **旋轉／縮放** | — |
| 方向 | 世界（已有）＋ **法向** | Gimbal／View／游標／父物件／自訂 |
| 中心 | 重心（已有） | 外框中心／3D 游標／各自原點／**選取元素本身** |

**中心一種都沒加**：單選時「重心」跟「選取元素本身」是同一個點，
分不出來 —— 等做了多選才有意義。**不為想像中的問題先寫程式。**

### 做法上兩個關鍵決定

**一、方向不去改 `TransformControls`，改替身。**
它的 `space` 只有 `world`／`local`，而 `local` 取的是掛著那個物件的世界四元數。
替身是我們自己的空 `Object3D` —— **把基底寫進替身的 `quaternion` ＋ `space` 設 `local`**，
箭頭就朝法向了。規格檔原本擔心「要改自訂軸向，那不是它原生擅長的事」，
**那件事根本不用做。**

⚠ 不能偷懶改成「一律 local ＋ 單位四元數」：物件本身轉過角度時，
那會變成沿**物件的軸**走，不是世界軸。所以方向是「世界」時 `space` 維持 `world`。

**二、記初始座標，每幀從初始值重算（取代增量累加）。**
舊寫法的註解說得很清楚：「拿絕對值算會每幀重複套用一次，一拖就飛出去」——
那個問題是真的，但它是**增量寫法自己造成的**。記了初始值就不存在了。

而**價值不是手感是正確性**：旋轉根本沒辦法用增量累加做對。
取消、打數字是它的副產品，不是另外寫的功能 ——
`applyElementTransform()` 那一段，拖曳跟打數字走的是同一行。

### 三個實作上踩到／預先擋掉的地方

1. **`_rebaseProxy()` 不能清掉快照。** `commit()` 會走 `revalidate()` →
   `_refresh()` → 它，也就是**每放一次手都會經過**。在那裡清掉的話，
   放手之後就再也打不了數字了 —— 而那正是最需要打數字的時候。
2. **打數字要用「拖曳開始時那一份四元數」算軸向。** 放手之後替身會重新對準
   （幾何變了，法向也跟著變），拿新的去換算，數字會沿**另一根軸**走，
   而數字看起來完全正常。
3. **離開編輯模式要把 `space` 設回 `world`。** 它是 gizmo 自己的狀態，
   不會自己還原 —— 不設回來，接著拖一般物件時箭頭沿物件的軸走，
   而使用者不知道自己換過方向。

另外連帶檢查了 X／Y／Z 三個字：`syncGizmoLabels()` 問的是 `tc.object`
（＝替身），**本來就是對的**，不必改。〔這一項是先假設它壞了才去查的，
查完發現沒壞 —— 鐵律一第 2 條「要動的那幾支程式，實際讀過」〕

### ✗ 測試抓到一次推論失誤

原本寫了一條測試：「頂面繞法向轉 180 度，60×40 的矩形對應到自己，
**體積必須一格都不變**」。**實測 108000 → 36000。**

錯在把「四個角的**位置集合**不變」讀成「每個點回到自己的位置」——
四個角**兩兩對調**了，而側牆連的是「底下第 i 個角 ↔ 上面第 i 個角」，
角一換，側牆就跟著**扭成麻花**。

不是 bug（Blender 轉一個面也是這樣，拓撲沒變，側牆只好跟著扭），
但它是使用者要知道的事：**旋轉面是拿來做斜面與梯形的（小角度），
不是拿來「把一片轉個方向」的。** 已寫進規格。

〔鐵律二：**這種事不能靠推理，要靠對答案。** 這是同一天第二次 ——
上一次是「拉面預設沿法向」那個不存在的開關。〕

### 驗過的數字

測試 1048 → **1102 項**。核心的幾條：

- ★ **沿法向拉 Z 10 ＝ `pushFace(10)`**：體積都是 132000，
  而且**每一個頂點都在同一個位置**（最大差 0）—— 兩條路對同一個答案，
  新路徑錯了會被舊路徑吵出來（鐵律三）
- ★ 頂面兩個切線各縮一半 → 棱台體積 **63000**（擬柱體公式手算）
- ★ 取消之後體積精確回到 108000，**每一個座標跟拖之前一模一樣**
- ★ 繞法向轉 37 度是剛體運動、面仍在原平面上
- 基底：Z 就是法向、三軸正交且右手系、切線 ＝ 最長的邊界邊、
  同一個面問兩次給同一個答案、四種退化情況一律 `ok:false` 不丟例外

### 連帶

- 待辦「把 `pushFace()` 接上介面」**消失了** —— 它變成「方向＝法向」的一個組合。
  函式留著但降級成**測試用的第二條路**。
- 工具列多一組「方向／數值」，**只在編輯模式顯示**。
  「移動／旋轉／縮放」不另外做，共用既有的「模式」那三顆 ——
  同一件事只給一個入口。
- Undo 標籤從一律「拉面」改成「拉／轉／縮放」＋ kind，
  否則三種變換在 Undo 清單裡長得一模一樣。

**⏳ 沙箱驗不了畫面，待 kang 實測**（九項，見 `PROJECT_LOG.md` 待辦那一條）。

---

## 第 6 期地基（下）：子元素多選 ＋ active（2026-08-24，`42a003f`）—— 一個 getter 省掉五處改動

「三件事」第三件的第一步（外部參考調查的 1-3）。

### 容器：陣列 ＋ getter

`editSels` 是有順序的陣列，`editSel` 改成**回傳最後一筆的 getter**。

這個決定是先數過呼叫點才下的：外面有五個地方在讀 `editSel`
（`main.js` 四處、`toolbar.js` 一處），而它們要的一直都是「active 那一個」——
改成 getter，**那五處一行都不用動**。

「順序即 active」抄的是物件層自己的寫法（`ids` 有順序、`active` 取最後一個），
而 Blender 的 select history 也是同一個做法。它換來一件免費的事：
**再點一次同一個 ＝ 取消選它，而 active 自然退回上一個**，不必寫任何特別處理。

### 四個約束，每一個都有理由

| 約束 | 為什麼 |
|---|---|
| 同一次多選**型別必須一致** | kang 拍板。混型別會讓法向、中心、面板、擠出把關全部多一種情況，而想不出真的會混選的場景 |
| **不能跨物件** | 變換寫回的是「某一個網格」的頂點座標 |
| 加選**重用工具列那顆「加選」** | 同一件事一個入口。平板沒 Shift，那顆按鈕本來就在 |
| 換過濾器**只清不合型別的** | 原本是全部清掉。多選之後不能接受 —— 選了六條邊再按一下「邊」就全沒了 |

前三條都會**講一句** —— 選取安靜地變少最讓人不敢相信工具（坑第 11、21 條）。

### 三個「畫面上完全看不出來」的重複

這一輪真正花時間的不是容器，是**同一個東西被選進去兩次**的三種情形。
三種的共同點：**畫面上一模一樣，只有數字或拖曳距離會露餡。**

1. **面比三角形。** 方塊頂面是 2 個三角形，點左半邊與點右半邊得到**不同的
   `Face` 物件**，而使用者眼中那是同一個面。→ `_findEditSel()` 改成比
   **共面區域**（`regionOf().faces` 的集合）。
2. **邊要連 `twin` 一起比。** 同一條邊有兩條半邊，`nearestMarkableEdge()`
   回哪一條取決於點在哪一側。兩條半邊畫出來是同一條線，
   只有拖的時候發現走了兩倍距離。
3. **頂點聯集要去重。** 相鄰的兩個面共用一條邊上的頂點，那真的是**同一個
   `Vertex` 物件** —— 不去重就會被平移兩次，而看起來只是「拉太多了」。
   → `elementVerts()` 用 `Set` 比**物件本身**（不比 id 也不比座標）。
   測試釘住：頂面 ＋ 右側面 ＝ **6 個頂點不是 8 個**，而且每個都剛好走 10。

### 中心終於有意義了

上一輪刻意沒做中心，理由是「單選時『重心』跟『選取元素本身』是同一個點」。
多選做出來，差別就很具體了：**選兩個面要縮放時，「重心」是兩面中間、
「最後選的」是剛點的那一面 —— 它決定東西往哪邊收。**

⚠ 而且中心會**回過頭影響方向**：`elementBasis()` 的切線本來就只看 active。
兩個概念名義上正交，實作上方向依賴中心 —— Blender 也是這樣
（算方向的函式吃 pivot 當參數）。

### 多選的方向：法向取和、切線照 active

- **法向取和**是 Blender 的規則。實測：+Y 與 +X 兩個面 →
  Z ＝ (0.7071, 0.7071, 0)，**剛好指在兩面中間**。
- ⚠ **每個元素的 Z 要先正規化再加。** 一個面的 Z 是「那個共面區域裡所有
  三角形法向的和」，不正規化的話 12 個三角形的區域會**壓過** 2 個三角形的，
  而使用者眼中那只是「兩個面」。
- **切線只看 active**，因為它必須唯一：把好幾條切線平均起來，
  兩個垂直的面會得到零向量然後退化。
  測試釘住：對調兩個面的順序，**Z 完全不變、整組基底會轉**。

### active 畫成橘色，而且不能借用綠色

`scene.js` 的 `setPickMarks()` 多一個角色：`src` 黃、`dst` 綠、**`active` 橘**。

⚠ **不能借綠色。** 綠色在貼合模式已經有意思了（目標），
同一個顏色在兩個模式代表不同的東西，畫面就開始騙人，
而使用者不會記得自己現在在哪個模式。**多一個顏色比省一個顏色便宜。**

⚠ 而且 active 一定要看得出來，這不是裝飾：中心與切線**都只看它** ——
分不出哪一個是 active，「箭頭為什麼朝那邊」就沒有答案（坑第 24 條）。

### ✘ 撤掉一支寫好的函式：`remapElements()`

原本照調查清單把「網格重建時搬移選取（targetmap）」也寫了 ——
**寫完發現它沒有任何呼叫者。**

唯一會拆掉重建的工具是擠出，而它一次只擠一個面、新蓋子由 `capFace`
直接回傳（那已經是一個夠用的一次性 targetmap）；
Undo／讀檔換上來的是**另一個模型狀態**，那裡就該老實清掉、不該搬。

留著它就會變成**第二個 `pushFace()`**：函式在、測試在、沒有介面在呼叫，
而下一個人讀文件會以為那條路是通的。所以整段換成一則墓碑註解，
把配對規則寫在裡面**當規格**，等第一個「會拆掉重建、而且使用者可能同時
選著好幾個元素」的工具（合併頂點／刪除面／環切）動工時再寫。

〔鐵律六：不要寫一個不存在的退路。這一次是**自己在完工前抓到的**，
不是等 kang 實測截圖照出來 —— 上一次（「拉面預設沿法向」）是後者。〕

### 驗過的數字

測試 1102 → **1128 項**。

聯集去重 6 不是 8、每個頂點剛好走 10、兩種中心相距 > 1cm、對調順序 active
中心從 (30,0,0) 換成 (0,22.5,0)、單選時兩種中心是同一個點、
兩面法向和 ＝ (0.7071,0.7071,0)、合併後仍正交右手系、
同一個面放兩次 Z 方向不變、對調順序 Z 不變但基底會轉、空陣列一律安全。

**⏳ 沙箱驗不了畫面，待 kang 實測**（九項，見 `PROJECT_LOG.md` 待辦那一條）。

---

## 六個工具的調查、兩輪實測，以及還原多邊形（2026-08-24，`6b89835`）—— 推論被翻掉五次

kang 定的規矩：「**推理可以先做，但推理之後要去調查它成不成立** ——
這樣真正執行時才會順暢。」這一輪就是照這條走的，而它翻掉了四個推論。

### 起點：kang 問了四個問題

多個網格能不能合併／點邊能不能增加／選了點邊面能不能刪除／
選了面能不能內縮再擠出。查完之後發現那四題其實是同一件事：
**「這個網格編輯器缺哪些基本動作」**，而缺口是內縮、刪除、加線、合併、導角。

### 第一輪推論 → 第一輪實測，翻掉兩個

我先排了一張順序表。kang 要求「先調查清楚再安排」，實測之後：

| 推論 | 實測 |
|---|---|
| 「合併頂點最便宜，把兩個索引換成同一個就好」 | ✘ **錯得最離譜。** `fromFaceList()` 照建出 `χ0 closed=false ok=false`，2 個面退化成線、1 個孤點，**沒有任何一步擋下來**。清乾淨還要重新編號，**而那會打破索引契約** |
| 「環切排第 1 批」 | ✘ **前提不成立。** 16 段圓柱是 **64 個三角形、0 個四邊形**，而環切穿的是四邊形 —— 沒有東西可以走 |
| 「刪除面便宜」 | ✅ 成立（但第二輪又翻了，見下） |
| 「內縮的難點在角落」 | ✅ 成立，**而且我們早就解過** —— `shell()` 的 miter 修正，連「夾角趨近 180 度時推距爆掉，設 5 倍上限」都寫好了 |
| 「導角要拆點、鄰接面變 n 邊形」 | ✅ 拓撲前提成立 |

🔴 **最大的發現是「限制性溶解」**：Blender 那個「量面與面夾角，低於門檻就合併」
**正是我們 `planarRegions()` 在做的事**，差別只在它是貼標籤、沒真的合併。
零件全現成，而它一口氣解掉三件事。

### 第二輪：把「還沒踩實的四項」也測掉

第一輪之後我主動列出建議裡還有四處是推論。kang 說照建議做，於是：

| 推論 | 實測 |
|---|---|
| 環切的 walker | ✘ **我把兩種 walker 搞混了。** 搜尋讀到的「四價走中間那條邊」是 **edge loop**；環切用的是 **edge ring**（穿過四邊形）。而**我們的頂點全是 3 價** —— edge-loop walker 一步都走不了，ring walker 走得非常好（圓柱 32 條閉環）。**而且 ring walker 只有一行** |
| 區域內縮 | ✅ `boundaryLoops()` 撐得住（頂面＋側面 → 1 圈 6 條）；折角上的點方向不唯一，正是 miter |
| 「刪除面最乾淨」 | ✘ **只測了一個案例就下結論。** 刪一個面孤點 0，但**刪一個角周圍的 3 個面 → 孤點 1** |
| 導角 | ✅ **真的建出來了**：繞向 `(A1,A2,B2,B1)` → 體積 **107250 精確吻合**手算的 `108000 − (5²/2)×60`；另一個繞向是非流形。⚠ 但它也產生孤點 |

### 🔴 貫穿全部的模式（第二輪最大的收穫）

**四個工具全部會產生孤點，而 `validate()` 一個都抓不到
（`ok=true`、`closed=true`、沒有 issues）—— 唯一露餡的是尤拉數 χ。**

這正是鐵律三那條「讓兩個數字互相對得起來，錯誤才會自己現形」在運作：
`ok` 是孤零零的一個布林值，沒有人驗得了；**χ 是由 V／E／F 推得出來的**，
所以它一對不上就自己叫出來。

→ `preflightRebuild()` 不是「值得加的檢查」，是**拆掉重建這條路的必要配件**。
→ 而清孤點就要重新編號 → **targetmap 也是必要配件**。兩者要一起做。

### 施工：三個配件 ＋ 面合併 ＋ 一道防護

**⚠ 三個配件如果單獨做會全部沒有呼叫者** —— 那就是第二個 `pushFace()`。
所以第 0 批併進第 1 批，讓**面合併當它們的第一個呼叫者**。

- `preflightRebuild()`：**不修，只回報**。分「一定壞掉」與「可以修掉」兩類，
  因為呼叫端的處理方式不同
- `cleanRebuild()`：清退化面／重複面／孤點，**並把 `remap` 交出來**
- `remapElements()`：靠 `remap` **精確配對**（不是猜），三種 kind 各有規則
- `mergeCoplanarFaces()`：`planarRegions` ＋ `boundaryLoops` ＋ `cleanRebuild` 接起來
- `Delete` 在編輯模式擋下來（查程式時發現的地雷：原本會刪掉整個物件）

⚠ 順手修掉一則**沒有結尾 `*/` 的註解**（上一輪那則墓碑），
它把後面的區塊標題吃進註解裡 —— 語法合法，所以測試抓不到。

### 驗過的數字

測試 1128 → **1179 項**。

面合併：方塊 12→6、圓柱 128→34、球 960→512，**體積與面積相對誤差 1e−7 內**
（實際小數點後 6 位全同）、χ 仍是 2、四邊形面 6／32／448。
管一個面都不合併（側面本來就是四邊形，端面是環形），**而且理由要講出「環形」**。
合併之後使用者標的 CUT 一條都沒少。
`remap` 證明索引真的位移（原頂點 7 → 6）。選取三種 kind 都搬得過去。

**⏳ 沙箱驗不了畫面，待 kang 實測**（六項，見 `PROJECT_LOG.md` 待辦）。

---

## 壓平，以及為什麼「溶解面」不做（2026-08-24，`6707622`）—— 三個概念正交的第二次兌現

### 起點：kang 選了 3 個 seg，按「面合併」沒反應

查下去發現兩件事疊在一起：那顆按鈕**不看選取**，而且他的圓柱已經整理過了。
但更根本的是 —— **它按下去畫面上沒有任何地方會變**（線框畫的是送給顯示卡的
三角形，而 GPU 只吃三角形）。那顆按鈕因此整個被移除，改在 `bake()` 時自動做。

而 kang 真正想要的「**把 3 個 seg 變成一個面**」，是另一件事。

### 兩條路，先量再選

| | 做法 | 實測 |
|---|---|---|
| **A 溶解面** | 不管夾角，直接併成一個 n 邊形 | 那個 8 邊形**偏離平面 0.9561 cm** |
| **B 壓平** | 把頂點壓到同一個平面 | 壓完偏離 **2.4e-15 cm** |

而可切得出來的容許值是 **0.01 cm**（0.1mm）。
A 超標 **96 倍** —— 那不是「精確變近似」，是**做不出來**。

→ **A 明確不做，寫進「不做」清單，理由是一個量出來的數字，不是主觀判斷。**

### 🔴 而 B 根本不用寫

實測之前先查了一件事：**壓平會不會已經做得到？**

會。**「縮放 × 法向 × Z 打 0」就是壓平** —— 選 3 片側面、方向切法向、
種類切縮放、Z 打 0，8 個頂點全部落在同一個平面上（偏離 2.4e-15）。

這是「變換 ＝ 種類 × 方向 × 中心」三個概念正交換來的**第二個免費功能**
（第一個是「沿法向推拉 ＝ 移動 × 法向」，它讓 `pushFace()` 那條待辦消失）。

所以 `flattenElements()` **一行新的數學都沒有**，完全走現成的
`elementBasis()` ＋ `applyElementTransform()`。它存在的理由只是**讓它按得到** ——
「方向法向、種類縮放、Z 打 0」沒有人猜得到。跟擠出的方案 C 同一個形狀。

### 🔴 而且壓平之後，「併成一個面」是免費的

壓完那三片**真的共面**了，`mergeCoplanarFaces()` 就會自己併掉：

```
壓平 3 片 → 變成同一個共面區域（34 → 32 區）
再併     → F 34 → 32，那三片變成一個 8 邊形
不平度   → 2.3e-15 cm（真正平的）
```

所以 kang 要的東西**兩條現成的路接起來就有了**，而且結果比 A 好：
**它是一個真正平的面，展開仍然精確。**

### 一道我自己加的防護，擋住了它

`applyEditNumber()` 裡有一行 `if (num === 0) return false;`，
理由寫著「縮到 0 ＝ 把面壓成零面積」。**那個判斷錯了兩次**：

1. 沿**法向**縮到 0 不是壓成零面積，是**壓平** —— 完全合理的操作
2. 它**沉默地拒絕**（坑第 11 條）—— 打 0 按 Enter 什麼都不會發生，也不講為什麼

而「壓成零面積」本來就有人管：`refreshAfterEdit()` 會回報 `degenerate` 並提醒。
**程式沒資格替人決定做不做得出來**（跟 `nonPlanarFaces()` 只提醒不擋同一條）。

→ 那三行拿掉了。

### 三個實作決定

1. **toast 要講「壓平前偏離多少」。** `flattenElements()` 回傳 `before` ——
   那是使用者即將付出的代價，而且**他自己驗得出來**（坑第 24 條）。
   只講「壓平了」等於沒講：形狀變了多少他看不出來。
2. **本來就平的面 → 什麼都不做而且講一句**，不記 Undo。
   悶著記一步「什麼都沒改」，使用者會以為壞掉了。
3. 🔴 **`remapElements()` 要去重。** 三個舊面搬過去之後**全部指向同一個新面** ——
   不去重的話「選了幾個」會說 3，而畫面上只有一個。**那個數字在說謊**（坑第 20 條）。

### 驗過的數字

測試 1199 → **1219 項**。

壓平 3 片：移動 8 個頂點、回報偏離 0.478、壓完全部共面（1e-9 內）、
34 → 32 區、再併成一個 8 邊形、不平度 < 1 微米、χ2 封閉、選取去重成 1 筆。
對照組：直接溶解的 8 邊形偏離 0.956 ＝ 容許值的 96 倍。
本來就平的面：`before = 0`、體積一格沒動。

**⏳ 沙箱驗不了畫面，待 kang 實測。**

---

## 扇形三角化只對凸多邊形成立（2026-08-24，`6707622`）—— 一個潛伏的 bug 被自己的新功能叫出來

### kang 的一張截圖

「為什麼我合併後..我選面..然後移動拉伸...會出現一個很奇怪的三角區域?」

原因：他把壓平後那一片往內拉，**上蓋變成非凸的 32 邊形**。
而 `toGeometry()` 用的是**扇形三角化**（從第一個頂點拉到每一組相鄰邊）——
那個做法**只對凸多邊形成立**。上蓋一凹，就有三角形跑到多邊形外面、繞向翻掉。

```
真實面積 1798.39　扇形算出來 1880.34　多畫 81.95 cm²（4.56%）
繞向翻掉的三角形 1 個
```

### 🔴 但它不只是畫面 —— 全專案有 8 個出口

| 位置 | 算什麼 | 非凸時 |
|---|---|---|
| `mesh.volume()` | **有號**體積 | ✅ 對 |
| `mesh.area()` | 絕對值相加 | 🔴 多算 4.56% |
| `mesh.toGeometry()` | 畫面 | 🔴 kang 看到的那塊 |
| `region.js` | 區域面積、重心 | 🔴 錯 |
| `stl.js` | **STL 匯出** | 🔴 有翻掉的三角形 → 切片軟體報非流形 |
| `flatten.js` | **展開片面積** | 🔴 多算 → **備料會算錯** |
| `bool.js` | 送給 Manifold | 🔴 **布林拿到壞掉的實體** |
| `edit.js` | 判零面積 | ⚠ 影響小 |

⚠ **STL 體積照樣對得上**（71935.55）—— 因為它是**有號**的，翻掉的三角形在裡面
也不影響。那正是坑第 17 條：**中途的量一直都是對的，末端才錯。**
要驗這件事就得**逐個三角形問它的法向對不對**，測試現在這樣做。

### 🔴 這個 bug 一直都在，是我讓它浮出來的

n 邊形本來只出現在 `roundBox`（20 邊形，**凸**）與 `tube`（四邊形），
而匯入的擠出件早就自己耳切成三角形了（`extrude.js`）。
**所以從來沒有非凸的 n 邊形。**

「還原多邊形」讓每個共面區域都變成 n 邊形，而編輯可以把它拉成非凸的 ——
一個潛伏的 bug 就這樣被叫出來。

⭐ **這件事本身是新功能的價值**：它把一個一直錯著沒人發現的東西逼出來了。

### 修法：一個入口，八個出口

**鐵律一第 1 條先問「本專案有沒有解過同一個問題」** —— 有。
`build/extrude.js` 的 `earClip()` 已經 export、已經測過
（「三角形面積總和 ＝ 多邊形面積」）。

⚠ 但 `extrude.js` **匯入 `mesh.js`**，所以耳切不能留在那裡（會循環依賴）。

1. 抽出 `core/triangulate.js` —— **逐字搬**（165 行 / 6215 bytes /
   md5 `54e206726bd481859460e8bd16b13ae1`，搬完對過），不匯入任何東西。
   `extrude.js` 改成 re-export，**所以原本 import 它的地方一行都不用改**。
2. `mesh.faceTriangles()` 當**全專案唯一的三角化入口**：
   三角形／四邊形／凸多邊形 → 扇形（O(n)，而且**跟以前逐字相同**）；
   非凸 → 投影到面的平面 → 耳切（O(n²)，但很少走到）。
   ⚠ 耳切失敗就**退回扇形，不要回傳空的** —— 回空的話那個面會整片消失，
   而使用者會以為模型壞了（坑第 11 條）。
3. 8 個出口全部改用它。⛔ 註解裡明寫「不要再自己寫 `for (let i = 2; ...)`」。

### 安全網就是既有的測試

改完之後 **1219 項一項都沒掉** —— 那是「凸的行為完全沒變」的機械斷言。
再加上一條逐字比對（330 個面，`faceTriangles` 與扇形完全相同）。

新增的非凸測試裡**刻意留著舊做法的算式當對照組** ——
證明這組測試真的測得到東西，不然改壞了也不會有人發現
（鐵律：要測病因，不只測症狀）。

測試 1219 → **1231 項**。

### ✅ 外部驗證通過（鐵律五）

kang 用線上 STL 分析工具開了兩個案例（往外拉、往內拉）：
**水密（封閉表面）：是**、**可直接列印 —— 防水且多歧管**。

⭐ 而報表上的 **124 三角形 / 64 頂點** 自己就是一份交叉驗證：
32 個側面四邊形 ×2 ＝ 64，加兩個 32 邊形端蓋各 30 個 ＝ 60，合計 124；
頂點上下各 32 ＝ 64。**若端蓋還是原本的扇形，會是 128 / 66**（多出兩個扇心）。

→ **還原多邊形與耳切兩件事都在正確運作**，而且這個結論是從
**外部工具的數字反推**出來的，不是我們自己說的。
那正是鐵律三那條「讓兩個數字互相對得起來」在專案外面的一次兌現。

---

## 環切收尾、法向與選一圈（2026-08-24～25）

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **環切已完成**（2026-08-24，`531dde6`）。工具列「編輯」那組的 `環切` ＋ 刀數輸入框。
  選一條邊 → 繞一整圈加線 → **新的那一圈自動選中** → 用「拉邊」拉。
  **只加線、不改形狀**：體積與面積**精確不變**（108000.000000 →
  108000.000000）。用法見 `規格\建模器-點線面編輯.md`。

- ✅ **法向「修法向／翻面」已完成，kang 實測通過**（2026-08-24，`8902d43`，
  對照表冒出來的第一急件。六項全過：匯入件與布林件修得動且體積對得上、
  **3D 列印面板原本那則「法向朝內」警告修完就消失**、板件回報「開放的殼沒有
  外側可言」、參數物件兩顆都是灰的、選一圈的圈數正確、平板版面沒被擠掉）。
  工具列多一組「法向」，**不需要進編輯模式**。
  🔴 它補的是一個**講了問題卻沒有出路**的洞：`out/stl.js` 早就在報
  「法向朝內（體積算出來是負的），印出來會內外相反」，**但沒有任何修法**。
  用法見 `規格\建模器-點線面編輯.md`。
- ✅ **「選一圈」已完成**（同上，`8902d43`）—— 走訪是環切那一輪寫好的，幾乎免費。


---

## 內縮、導角、導圓角、outer miter、全選邊（2026-08-25）—— 四動作框架連續兌現四次

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **內縮已完成，而且它證實了那個框架**（2026-08-25）。
  工具列「編輯」那組的 `內縮` ＋ 寬度輸入框。**一行新的數學都沒有** ——
  miter 是 `shell()` 現成的、外緣是 `boundaryLoops()`、新邊標 `hard` 是環切定的規則。
  🔴 **只加線，體積面積精確不變**；實際牆距量出來剛好是輸入值
  （不做 miter 修正的話是 3.5355 ＝ 5×cos45°）。
  內圈那個面自動選中 → 用「拉面 × 法向」推就是凹槽（方案 C，不做 Depth）。
  用法見 `規格\建模器-點線面編輯.md`。

- ✅ **導角（單段）已完成**（2026-08-25）—— 含**角落**。工具列的 `導角` ＋ 寬度輸入框，
  可以一次選好幾條邊。⚠ **刻意不自動選中新面**（形狀當下就完成了，
  而且 12 條邊全導會長出 20 片，gizmo 會停在模型正中央擋住視線）。
  用法見 `規格\建模器-點線面編輯.md`。

- ✅ **導圓角（多段）已完成**（2026-08-25）—— **同一顆按鈕，多一個段數框**。
  段數 1 ＝ 現在的 45 度斜切邊，2 以上 ＝ 圓角（預設 1、上限 32，kang 選的）。
  🔴 **不是兩個功能**（第 12.1 節），所以**刻意不做成第二顆按鈕**。
  ✅ **kang 實測通過**（2026-08-25，含他抓到的兩個 bug 修完之後的重驗）。

  ⭐ **「推到弧上」是免費的，不必事後推。** 第 12.4 節排的第三步根本不必做：
  > **弧上的點 ＝ 兩個面法向的球面插值 × 半徑 ＋ 圓心。**

  因為圓角面跟原本兩個面**相切**，所以 `圓心 = 代表點 − w × 面法向`，
  而代表點到圓心的方向**就是那個面的法向**。角落同理，三個法向張成一塊球面。

  🔴 **判準不是「是不是直角」，是「圓心對不對得起來」。**
  ⛔ 不要去判斷角度 —— 直接從每個面算圓心，**對不上就擋下來**。
  那個檢查本身就是「非直角時沒有一個球同時相切」的定義。

  🔴 **段數 1 逐個頂點完全相同**（誤差 1e-12）—— 多段走另一條路，
  單段那段程式一行都沒改，所以舊行為不變是**結構保證**。

- ⚠ **圓角的點數跟第 12 節那張表對不上，而且兩個都對** —— 這件事的處理方式
  比結果重要。表的角落是 `n²+n+1` 個點，我們是 `(n+1)(n+2)/2`，
  **而面數兩邊逐項吻合**（`6+12n+8n²`）。差別在角落球面的三角形怎麼排：
  表是「6 片板＋12 個 1/4 圓柱＋8 個 1/8 球」**直接拼**的，接縫點各算各的；
  我們是從導角長出來、**接縫共用點**。表因此稍準（段數 16：0.0158% vs 0.0166%）。

  ⛔ **沒有為了湊表上的數字去改結構**（那會變成為數字而改）。
  正確性改由四條**獨立於那張表**的斷言保證：
  每個頂點到內盒距離**精確等於 r**／體積收斂到**解析解**／
  段數加倍誤差縮**四分之一**／體積**永遠小於解析解且單調遞增**。
  🔴 最後一條是表給不了的：**頂點跑到弧外面時，體積看起來還是很接近**，
  只有「內接」這條抓得到。

- ✅ **全選邊已完成**（2026-08-25）。工具列「編輯」那組的 `全選邊`：
  把這個物件所有**看得見的**邊一次選起來（方塊 12 條）。
  **選到物件就能按，不必先選任何邊。** 用法見 `規格\建模器-點線面編輯.md`。

- 🔴 **它是實測逼出來的，而且教訓比功能重要：功能沒問題，難的是選取。**

  kang 2026-08-25 要做方塊六面 12 邊圓角，回報「**就是不行**」。
  查下去發現**圓角本來就做得到**（沙箱驗過段數 1～16 全部封閉），
  卡住的是介面：**要開加選點 12 下**，中間漏一條、或重複點到同一條邊的
  另一側，就會有角落只導到兩條 → 被擋下來，
  而**畫面上看不出哪一條沒選到**。

  ⚠ **我一開始還去查資料層**，因為沙箱測試說可以 —— 兩邊對不上時，
  差別就在**畫面那一半**（日誌早就寫著「沙箱驗得了幾何，驗不了畫面」）。
  🔴 **下次「沙箱說可以、kang 說不行」，先看介面，不要先懷疑演算法。**

  ⭐ 這也補上一個我漏掉的缺口：能力對照表第 10.7 節列的三大缺口是
  **加線／補面／選取**，而我一直只在做「加線」那一類。

  ⚠ **判準用 `isMarkable()`，⛔ 不要另外寫一套** ——
  共面的三角化對角線畫面上根本沒有，選進來方塊會變 18 條而不是 12 條，
  **介面上那個數字就說謊了**（坑第 20 條），而他正是因為數不出來才踩坑的。

- 🔴 **kang 第一輪實測就抓到兩件事（2026-08-25），兩件都已修。**

  **一、只導一條邊，網格會裂開** —— 側面上冒出一堆奇怪的線。
  原因是「沒被導到的那個面」只吸收了弧的**兩端**，中間的點它不知道。
  實測沒修之前：χ 2 → **0**、不封閉、邊界半邊 2n+2 條。
  🔴 **而 `validate()` 是 true，抓不到** —— 只有 χ 露餡。

  ⚠ **我的測試沒抓到，因為只驗了「12 條邊全導」** —— 那時每個面都有
  自己的代表點，**沒有任何面需要吸收**，那條路根本沒被走到。
  🔴 **這是坑第 17 條的又一次重演**：挑樣本要涵蓋不同的**網格結構**，
  不只是不同的形狀。⛔ 日後動導角，「只導一條」那組一定要一起跑。

  **二、選頂面四條邊跳「這個角落不是直角」—— 可是方塊明明是直角。**
  真正的原因是那個角落的**垂直邊沒被導**。**擋下來是對的，錯的是訊息**：
  講「不是直角」會讓人往「換個形狀試試」的方向走，
  而正解是「**把那個角落的邊全部一起選**」。
  現在兩種情形分開講，測試同時釘住「不可以再說不是直角」與
  「**它講的那條出路真的走得通**」（坑第 34 條）。

- ✅ **outer miter 已完成**（2026-08-25）—— **只導一部分的邊也能圓角了**。
  圓角桌面、圓角面板 ＝ 只導頂面四條邊，kang 第一輪實測就試到的那個。
  段數 2／4／8／16 全部 χ＝2 封閉，體積收斂到解析解 106921.2755
  （0.357% → 0.0058%），**面數 ＝ 6+4n+8n²**。
  ✅ **kang 實測通過**（2026-08-25）。用法見 `規格\建模器-點線面編輯.md`。

  ⭐ **關鍵是「兩條弧共用同一個圓心」**，而這一段卡了兩輪 ——
  一直用「每個面在這個頂點的代表點」去算圓心，
  而**共用面的代表點是「兩條邊都內縮」的角點**，不是任一條邊的偏移。
  > **圓心 ＝ 共用面的代表點 − 寬度 × 那個面的法向。兩條弧都用它。**

  ⚠ **只做直角**，非直角仍然擋下來（那正是 Blender 開三個選項的地方）。

- 🔴 **miter 這一輪犯了兩次同一個錯，值得記住：「吸收」永遠是整條，不是兩端。**

  角落那片的第三條邊界**整條**躺在側面上，只放兩端的話中間的點配不到邊
  → 破洞。實測 **χ 2 → −6，而體積照樣正確收斂** ——
  **坑第 17 條的教科書案例：中途的量一直都是對的，末端才錯。**
  ⚠ 而**同一輪稍早才因為一模一樣的機制壞過一次**（只導一條邊時，
  沒被導到的面要吸收整條弧）。⛔ 日後動導角，先問「這個面要吸收的是幾個點」。

- ⚠ **另一個要記的：訊息在功能長大之後會變成謊話。**
  miter 做完之後「只導一部分」**本身已經做得到了**，
  原本那句「把邊全部一起選就可以」對圓錐（非直角）變成**死路**。
  已重新分類並釘成測試。〔坑第 34 條：不要寫一個不存在的退路〕
  🔴 **加新功能時，順手檢查舊的錯誤訊息有沒有因此過期。**

  🔴 **這一輪帶出一條該記住的教訓（kang 2026-08-25 提醒的）：**
  > 「你是否也可以**搜尋是否有甚麼可以解決的方法來做參考**呢?」

  查完發現**我自己推導出來的東西正好就是 Blender 的預設做法**，
  但**先查可以省掉三次失敗的嘗試**。
  ⚠ 鐵律一「先調查清楚再動手，不要只依據推理」講的就是這件事，
  而這次是**使用者提醒我去遵守它的**。
  ⛔ 下次遇到「這個做不做得到」的問題，**先查外面怎麼解，再自己推**。

- ✅ **kang 實測通過（導圓角 ＋ outer miter，2026-08-25）**：
  只導一條邊段數 5 側面乾淨了／段數 1 跟以前完全一樣／12 邊全導圓角成功
  （截圖 426 個三角形）／**只導頂面四條邊段數 4 做得出來**／
  12 條全導照樣正常／圓錐還是擋下來而且訊息沒再叫他「全部一起選」。

  ✅ **任意切線（切一刀）也實測過了，八項全部通過**（2026-08-25，見下方那則）。
  第 5 項（旋轉後的座標對位）原本卡著沒辦法量，**後來被「量測第 1 步」
  的面板解開，一行程式都沒改** —— 判準與四個對得起來的數字見下方。

- ✘ **已作廢：原本列的實測清單（導圓角，已被上面那則取代，留著當對照）**：
  1. 🔴 **只導一條邊、段數 5** —— 側面**不可以再有奇怪的線**（就是他抓到的那個）
  2. 段數維持 1，導角跟以前**完全一樣**（回歸，最重要）
  3. 方塊 12 條邊全導、段數 4，看起來是不是圓的
  4. 🔴 **選頂面四條邊、段數 4** —— 訊息要說「只導了一部分的邊…全部一起選」，
     **不可以再說「不是直角」**
  5. 段數調到 16、32，會不會卡（平板尤其）
  6. 🔴 **圓角之後展開** —— 片數會不會暴增（沙箱只驗了「不會爆掉」，
     沒驗片數，因為那取決於材料規則）
  7. 圓角之後轉 STL，切片軟體吃不吃得下
  8. **平板版面**：導角那一組現在是「導角＋寬度＋段數」三個東西

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。

- 🔴 **導角那一輪抓到一個不只影響導角的 bug：假邊界會變成分片線。**
  `_buildBoundaryLoops()` 會把「沒有 twin 的半邊」自動標成 CUT（**那條規則是對的**），
  但**繞向不一致的網格，那些邊只是暫時配不到 twin，不是真的邊界**。
  `recalcNormalsOutside()` 修好繞向之後它們變回內部邊，**而 CUT 已經搬進來了**。
  → **「修法向」按鈕也會中**：修一個繞向壞掉的模型，畫面上憑空多出幾條分片線。
  已修（只清「原本是邊界、現在變成內部」的那些），兩個方向都釘成測試。

- ✅ **kang 實測全部通過（10 項，2026-08-25）** —— A／B／C 三組全過：

  | 組 | 項目 | 結果 |
  |---|---|---|
  | **A** | 刪除面破洞、補洞補回來、開放的提示、板件補洞體積 0 | ✅ 4/4 |
  | **B** | **先水平四條再垂直四條**、圓錐 4 價、曲面不平的提示、方塊不該有那句 | ✅ 4/4 |
  | **C** | 內縮寬度過大的提示、導角完選取有清掉 | ✅ 2/2 |

  ⭐ **B 那一組就是 kang 原本抓到會壞的那條路**（「先選水平的四個邊導角，
  再選垂直的四邊導角，就會出現奇怪的形狀」）—— 角落規則解掉之後正常了。


---

## 刪除面 ＋ 補洞（2026-08-25）—— 「補洞當時根本沒有輸入」

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **刪除面 ＋ 補洞已完成**（2026-08-25）。工具列「編輯」那組的
  `刪除面` 與 `補洞`。**刪掉再補回來，體積／面數／頂點數精確還原。**

  ⚠ **原本排的順序是「補面先做，因為它是刪除面的前提」—— 那個說法有問題**：
  **沒有刪除面就做不出洞，補洞當時根本沒有輸入。**
  〔是 kang 回一句「不懂意思」才發現的 —— 沙箱測得到，是因為 AI 自己用程式
  手工挖了洞，而**使用者在介面上做不到那件事**。
  🔴 **教訓：驗證的時候，要問「使用者做得出這個輸入嗎」**，
  跟那條「開實測清單前先問他做得出來嗎」是同一件事〕

  🔴 **順手擋掉一個 `mesh.js` 的既有限制**：`_buildBoundaryLoops()` 用
  「頂點 → 邊界半邊」的 Map 串迴圈，**一個頂點只放得下一條** ——
  刪掉的面只在一個頂點相接時邊界迴圈會斷掉（實測球 seg8 刪 4 個面就中）。
  先擋下來並講清楚；**正解是讓邊界迴圈支援「一個頂點多條」**，見待辦。

  ⚠ **兩個不是 bug 的限制**：補洞不知道原本那裡是什麼形狀（只把每個迴圈補成
  一個面）；**環形的面補不回環形**（管的端面補完內孔會被塞住，χ 0 → 2）——
  那是「一個面裝不下兩個迴圈」的結構限制。用法見 `規格\建模器-點線面編輯.md`。


---

## 任意切線（切一刀）（2026-08-25）—— 兩個介面 bug 比功能本身重要

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **任意切線已完成**（2026-08-25）。工具列「編輯」那組的
  `切一刀` ＋ 軸下拉（X／Y／Z）＋ 座標框 ＋ **範圍提示**。
  **只加線，體積面積精確不變。** 用法見 `規格\建模器-點線面編輯.md`。
  ✅ **kang 實測八項裡七項通過**（2026-08-25，清單見本段最後），
  過程中抓到**兩個介面 bug，兩個都已修**（見下面兩則）。

  **⚠ 它跟同組其他按鈕不同：不需要選任何元素。** 環切、內縮、導角都是
  「對選到的東西動手」，這一顆是**對整個物件動手**，位置由軸與座標決定。

  🔴 **kang 2026-08-25 定的：座標是「空間裡的實際位置」**，
  跟對齊、貼合、剖面分切同一套數字 —— 不是「離邊緣多遠」，也不是百分比。
  所以座標框旁邊那行**範圍提示**（`X：−30 ～ 30`）不是裝飾：
  沒有它使用者猜不出該打什麼，物件被搬過位置之後更猜不到。

- 🔴 **任意切線帶出兩件事，都比功能本身值得記。**

  **一、網格存的是「物件自己的座標」，物件另外帶著位置與旋轉。**
  使用者打的是**世界**座標，物件被轉過的話，那個平面在網格自己的座標裡
  是**斜的**。不轉換就會**切錯地方，而形狀完全正常**。
  → `worldAxisPlane()` 負責換算，已釘成測試（新頂點回到世界座標必須
  剛好落在 X＝5 上）。
  ⭐ 副產品：日後做「拿選到的那個面當平面」**只差介面** ——
  底層本來就吃斜平面。
  〔這一條**推不出來，是讀程式才發現的** —— 又一次印證鐵律一〕

  **二、`sectionAt()` 踩過的坑要沿用，但解法必須換。**
  它算的是 2D 線段，做法是「距離 0 一律推到正側」。
  **那條規則在出圖是對的，拿來改網格會出事**：頂點剛好落在平面上時
  `s` 會算出 0 或 1，於是**在既有頂點上插一個重複的點**，長出零長度的邊。
  2D 可以事後濾掉，網格不行。
  → 改成三態，判準用 `PLANAR_TOL_CM`（0.1mm）：
  離平面比 0.1mm 更近的頂點**直接拿來用、不插新點**。
  機械斷言：八角柱有兩個頂點剛好在 X＝0 上，切完**頂點數完全不變**。
  ⚠ 而且插了也**不會報錯**：體積、面積、χ 全部照樣正確（坑第 17 條）。

- ⚠ **任意切線那一輪差點寫出一條假通過的測試，寫下來免得重犯。**
  球的 `segH` 是偶數時**赤道上剛好有一整圈既有頂點**，用 Y＝0 去切會變成
  「每個面的兩個交點都相鄰」→ **一個面都不會被切開**，
  而 χ、體積、面積、跳過數**全部照樣通過**。
  → 改切 Y＝7.3，並且**明確斷言真的有面被切開**。
  〔前三次導角就是這樣被騙的：「每次都通過了一部分案例，看起來對」〕

- 🔴 **實測抓到的第一個：座標預設值寫死 `0`，跟範圍沒有關聯。**
  `index.html` 寫死 `value="0"`，而範圍提示是**照物件算出來的** ——
  物件只要不跨過 0，**第一次按必定跳「沒有切到東西」**。
  kang 第一次按就踩到，以為功能壞了。
  → 已修（`updateBisectRange()`）：**框裡的數字切不到就換成範圍中點**，
  切得到就完全不動，**正在打字時完全不碰**。✅ kang 實測通過。
  ⚠ 那正是鐵律三反過來的樣子：**範圍是算的、預設值是寫死的，對不起來。**

- 🔴 **實測抓到的第二個，而且它是這一輪最重要的一條：
  「選了哪個物件」與「選了哪個面」原本是兩份分開記的資料。**

  > **同一個畫面、只點了一次，兩群按鈕給出相反的答案。**

  編輯模式下點一個面，`pickEdit()` **只更新 `editSels`，從來不碰 `ids`**。於是
  擠出／壓平／環切／內縮／導角／刪除面（看元素）**亮著**，
  而切一刀／全選邊／法向／補洞（看 `sel.active`）**是灰的**。

  🔴 **不灰的時候更糟**：`active` 還留著上一個物件（Undo 叫回東西時很常見），
  元素卻選在另一個物件上 → 按「切一刀」**會去切一個使用者根本沒在看的物件**，
  而且**形狀改對了、沒有任何錯誤，只是改錯了對象**。

  → 已修：**`get active()` 改成「選了元素就以那個元素的物件為準」**。
  ⛔ **刻意沒有在九個寫入點各補一行同步** —— 那是靠紀律，下次加第十個就又破。
  改成**只有一條路的定義**，沒有第二份資料，也就沒有東西會不同步。
  〔坑第 31 條的又一次現行示範〕
  ✅ **kang 實測通過**（含「先選方塊、再點造型模型的面，切的是造型模型」）。

  ⚠ **沙箱驗不了這一支**：`select.js` 在 Node 載不進來（要 three.js 的 gizmo
  與 DOM），確認過了。**1574 項全過只證明沒弄壞別的。**

  💡 **病因是 kang 一句話指出來的**：「**我沒有去選擇其他物件…我就是選面**」。
  🔴 **下次「使用者說他沒做過某件事，但程式表現得像他做了」——
  先去找是不是有兩份資料在記同一件事。**

- ✅ **kang 實測（任意切線，2026-08-25）：八項裡七項通過。**

  | # | 項目 | 結果 |
  |---|---|---|
  | 1 | 不選任何元素，「切一刀」是不是亮的 | ✅ |
  | 2 | 範圍提示有出來，換軸會跟著換 | ✅ |
  | 3 | 方塊切一刀：多出一圈線、自動選中、拉得動 | ✅ |
  | 4 | 打範圍外的數字，擋下來並說原因 | ✅ |
  | **5** | **把物件轉一個角度再切，位置對不對得上打的數字** | ✅ **已驗**（見下則）|
  | 6 | 圓柱攔腰切，「切開 N 個面」跟畫面合得起來 | ✅ |
  | 7 | 存檔再開，切線還在 | ✅ |
  | 8 | 平板版面沒被擠掉 | ✅ |

- ✅ **第 5 項後來驗過了，而且是「量測第 1 步」解開的，一行程式都沒改**
  （2026-08-25。原本寫「等量測機制，kang 現在沒有辦法量」—— **那個前提已經消失**）。

  **怎麼驗的**：方塊 60×45×40、位置 Y 22.5、**繞 Y 轉 −30 度**，
  切一刀軸 X、打 5 → 新的一圈邊自動選中 → 看編輯造型面板那兩行。

  🔴 **判準要兩個一起看，只看重心驗不了：**

  > **重心 X ＝ 5.00（平面在你打的位置）
  > ＋ 外框 X ＝ 0.00（整圈確實躺在同一個平面上）**

  ⚠ **只看重心會被騙** —— 切歪了的話左右對稱抵消，重心 X 照樣是 5。
  **外框 X ＝ 0 才是「整圈共面」那句話的機械斷言。**

  ⭐ **而且畫面上四個數字互相對得起來，那比判準本身更值錢**
  （鐵律三「讓兩個數字互相對得起來，錯誤才會自己現形」）：

  | 畫面上的數字 | 手算 | |
  |---|---|---|
  | 範圍 X：−35.98 ～ 35.98 | ±(30cos30 ＋ 20sin30) ＝ **±35.98** | ✅ |
  | 外框 Y 45.00 | 方塊高 45，切平面平行 Y 軸 | ✅ |
  | 外框 Z **46.19** | 截面 z 從 −20.21 到 25.98 ＝ **46.19** | ✅ |
  | 中心 Z −20.21 | 「最後選的」那條垂直邊，z ＝ **−20.21** | ✅ |

  四個數字來自四條不同的路（範圍提示、量測外框、量測重心、手算），
  **沒有一個是從另一個推出來的**。`worldAxisPlane()` 的本地／世界換算是對的。

  🔴 **留著的教訓（這一則的前提沒了，但它仍然成立）：**
  當初開這一項時 kang 驗不出來，而它偏偏是**唯一「錯了也看不出來」的那一項** ——
  切錯位置形狀完全正常、體積也對（坑第 17 條）。
  ⛔ **下次開實測清單，每一項都要先問一次「使用者驗得出這個結果嗎」**，
  跟「使用者做得出這個輸入嗎」是同一條的兩半。

  💡 **解法的形狀值得記**：那一項不是靠「補一個量測功能」解開的，
  是**既有的面板本來就印得出來，只是沒有人想到拿它去驗**。
  ⛔ 遇到「驗不出來」先查現有的面板印了什麼，不要先排一個新功能。

  🔴 **上面那張八項實測清單一定要放在日誌裡，不可以只寫在 commit 訊息**
  （坑第 34 條）。〔⚠ 原本這句緊接在八項表底下，中間插進這一則之後
  「這張清單」會被讀成四個數字那張表 —— 所以改成指名道姓〕


---

## 連接兩點 → 多點連接、面上加線、凹面穿出邊界（2026-08-25）

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- 🔴 **Blender 一共有七種切法，我們現在有兩種**
  （`外部參考-Blender編輯.md` 第 10 節，第一手）：
  環切 ✅／平面切開 ✅／**刀具（手畫線）**／**連接兩點**／細分／
  刀具投影／用選取的面切。後兩種用得少。
  ⭐ **真正的缺口是「在一個面上加一條線」**（刀具與連接兩點那兩種）——
  對照表第 10.7 節早就寫著「手動畫一條線切開**完全做不到**」，
  今天做的只補掉「加線」那一類的一半。

- ✅ **連接兩點已完成**（2026-08-25）。工具列「編輯」那組的 `連接兩點`
  （在 `切一刀` 旁邊）。**在同一個面上選兩個角 → 中間長出一條線 →
  那個面被切成兩塊**，形狀完全不變，新的線自動選中。
  測試 1606 → **1640 項**。用法見 `規格\建模器-點線面編輯.md`。

  ⭐ **四動作框架的第四個案例，而且最便宜** —— 加線 × 一圈邊（環切）／
  面的內縮輪廓（內縮）／平面（切一刀）／**兩個既有頂點**（這個）。
  骨架跟 `bisect()` 逐項對應，**沒有開任何新的重建路徑**。
  🔴 **它比前三個多一條斷言：頂點數也不變**（那三個都會加點，這個不會）。

- 🔴 **這一輪最值得記的是：查外面就翻掉了一個假設。**

  `外部參考-Blender編輯.md` 第 10.1 節原本寫「連接兩點成邊（**J**）」＋
  「連接頂點路徑」，當成同一件事的單點版與多點版。**讀官方手冊內文才發現
  名字是反的，而且是兩個不同的指令**：

  | | 行為 |
  |---|---|
  | **Connect Vertex Pairs** | 只連**共用同一個面**的兩點，忽略選取順序。沒有快捷鍵 |
  | **Connect Vertex Path（J）** | **跨多個面**，依選取順序沿路徑切開 |

  ⚠ 這正是對照表 10.8 節自己標的「**只抓了操作索引，沒有逐支讀內文**」的代價。
  ⛔ **動到哪一項就去讀那一頁**，不要照索引上那一行推。已修正並記在該節。

- 🔴 **沙箱實測抓到一個 bug，而且它是坑第 34 條的第三次重演：
  檢查的順序會決定訊息說不說謊。**

  相鄰的兩個點**本來就同時屬於兩個面**（共用那條邊的左右兩片），
  所以「同時在好幾個面上」那個檢查**先觸發** —— 使用者被指去想面的問題，
  而真正的原因是「**這兩點之間本來就有線**」。

  → 相鄰改成**最先擋**，而且判準改成「有沒有邊」，跟面無關。
  兩條測試同時釘住「要說本來就有線」與「⛔ 不可以說成同時在好幾個面上」。
  〔導角那一輪「這個角落不是直角」才剛犯過同一個病〕

- ⚠ **第一版只收「正好兩個點」。** Blender 的 Pairs 可以一次連好幾對，
  但方塊頂面選四個角會有**兩條對角線交叉在中間而沒有交點** ——
  那裡會長出什麼**沒有驗證過**。⛔ 結果不唯一就不要猜（坑第 24 條）。

- ✅ **連接兩點／多點連接　kang 實測通過**（2026-08-26）。
  ⏳ **待實測：只有第 8 項（平板版面）** —— kang 手邊沒平板。清單留著當對照：
  1. 🔴 **過濾器切到「點」→ 選兩個對角的點 → 按「連接兩點」** ——
     方塊頂面應該多一條斜線
  2. **形狀有沒有變**（不可以變）
  3. 新的線**有沒有自動選中**，接著用箭頭拉得動（拉起來就是屋頂）
  4. 🔴 **選相鄰的兩個點** —— 訊息要說「本來就已經有一條線了」，
     **⛔ 不可以說成「同時在好幾個面上」**
  5. **選一個頂面的點 ＋ 一個底面的點** —— 要說「不在同一個面上…還沒做」
  6. 選三個點 → 按鈕要灰掉，而且滑過去會說「要正好兩個點，現在選了 3 個」
  7. 存檔再開，線還在
  8. **平板版面**：這一組現在多了一顆按鈕

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。

- ✅ **面上加線已完成**（2026-08-25）。工具列「編輯」那組的 `面上加線`
  ＋ 位置框（預設 **0.5 ＝ 正中間**）。**選兩條邊 → 各長一個點 → 連起來**，
  方塊頂面就切成兩個矩形。測試 1640 → **1681 項**。

  🔴 **它是 kang 驗收「連接兩點」之後才長出來的，而且他的話點出了真正的需求**：
  > 「我以為是我可以在面或邊上做切一刀..**讓面變成兩等分**」

  **「連接兩點」做不到那件事，因為邊的中間本來沒有點。**

  | | 連的是 | 方塊頂面切出來 |
  |---|---|---|
  | 連接兩點 | 兩個**既有的角** | 兩個三角形 |
  | **面上加線** | 兩條邊上**新長出來的點** | 兩個**矩形** |

  ⛔ **kang 拍板分成兩顆按鈕**（「畢竟效果呈現不同」），不要合成一顆。

- 🔴 **這一輪的技術重點只有一個：第二條邊要反過來算。**

  兩條邊在面的迴圈上是**繞著走**的，各自的 `0.3` 會落在**對角** →
  切出來是**斜線**，不是使用者要的平行線。→ **A 取 `t`、B 取 `1 − t`**。

  ⚠ **`t=0.5` 是對稱的，反不反過來結果一樣** —— 只驗 0.5 等於沒驗到。
  ⭐ **最直接的斷言是「新線的方向不隨 `t` 改變」**，比面積比還好用：
  沒反過來算的話，`t` 一離開 0.5 線就歪了。兩條都釘進測試。

- ⚠ **插點一定會波及鄰面，而且那是使用者看得見的** ——
  方塊頂面加一條線，**兩個側面會從四邊形變成五邊形**（多一條短線）。
  不做就破洞，所以 toast 會講「旁邊 N 個面也跟著多了一個點」。

- ✅ **面上加線　kang 實測通過**（2026-08-26）。
  ⏳ **待實測：只有第 8 項（平板版面）** —— kang 手邊沒平板。清單留著當對照：
  1. 🔴 過濾器切「**邊**」→ 選頂面**兩條對邊** → 按「面上加線」→
     **頂面應該被切成兩個一樣大的矩形**（那就是他要的兩等分）
  2. 🔴 **位置改成 0.3 再切一次** —— 線要**平行**地往一邊移，⛔ 不可以是斜的
  3. **側面有沒有多一條短線**（正常，而且 toast 有講）
  4. 新的線自動選中、拉得動
  5. 選**相鄰**的兩條邊 → 切出一個角（也該成功）
  6. 位置打 0 或 1 → 擋下來說範圍
  7. 選兩個**點**（不是邊）→ 按鈕灰掉，滑過去說「只吃邊」
  8. **平板版面**：這一組又多了一顆按鈕 ＋ 一個框

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。

- 🔴 **kang 實測抓到第三個 bug，而且是這三輪裡最嚴重的一個：
  凹的面上，新拉的線會跑出邊界。**

  他的原話：「面切一刀...如果**遇到兩個邊不一樣大時**...會很奇怪」
  （2026-08-25，附 L 形跟方塊的對照截圖）。

  查下去**不只是難看**：

  > **L 形頂面：面積 2200 → 2600，而 `validate()`、χ、體積全部照樣正確。**

  線穿出邊界之後，切出來的兩塊是**自交的多邊形**，三角化會把外面那塊
  也算進去。⚠ **體積是有號量所以照樣精確** —— 跟導角那次
  「只有 `volume()` 是對的」一模一樣（坑第 17 條）。

  ⚠ **兩顆按鈕都中**（連接兩點、面上加線），已修：共用一道
  `chordInsideFace()` 檢查（⛔ 不各寫一份，坑第 31 條）。
  測試 1681 → **1695 項**。

  🔴 **我的測試為什麼沒抓到**：樣本是方塊的四邊形與圓柱的 32 邊形 ——
  **兩個都是凸的**，而**凸多邊形的任兩點連線一定在裡面**，
  那條路根本沒被走到。
  ⛔ **日後任何「在面上拉線」的功能，L 形那組一定要一起跑。**
  〔坑第 17 條：挑樣本要涵蓋不同的**網格結構**。而「凸／非凸」是這個專案
  　反覆踩到的那一組 —— 扇形三角化那次有 8 個出口，這是第 9 個〕

  ⭐ **⛔ 不可以因為「這個面是凹的」就整個擋掉** —— L 形頂面大部分的
  連法都是合法的，只有穿過凹口那幾條要擋。整個擋掉是誤報（坑第 18 條）。
  測試同時釘住「該擋的擋了」與「不該擋的沒擋，而且面積精確不變」。

- ✅ **凹面穿出邊界的修正　kang 實測通過**（2026-08-26，**三項全過**）。
  🔴 **第 2 項是重點而且他真的驗了**：L 形上不會穿出去的位置**照樣做得到** ——
  ⛔ 不是「凹的面就整個擋掉」（那會是誤報，坑第 18 條）。清單留著當對照：
  1. 🔴 **L 形（或任何凹的面）上，選會穿出去的那兩個位置** ——
     要被擋下來並說「那個面是凹的…穿出了邊界」
  2. 🔴 **同一個 L 形，選不會穿出去的位置** —— **要照樣做得到**
     （⛔ 不可以整個功能都不能用了）
  3. 方塊（凸的）完全不受影響，跟之前一樣

- ✅ **「連接兩點」已擴充成「多點連接」**（2026-08-25，kang 定的名字）。
  選好幾個角，**照選的順序一段一段連**。兩個點就是連一條線（行為完全沒變）。
  測試 1695 → **1720 項**。

  ⚠ **原本刻意只收兩個點**，理由是「四個角會有兩條對角線交叉在中間」——
  **那個顧慮只對 Blender 的 `Connect Vertices`（不看順序、全部配對）成立**，
  依序連沒有那個問題。
  ⭐ **選取順序本來就有**（`editSels` 是有順序的陣列），一行新機制都沒加。

  🔴 **一定要依序、一段一段，不能一次算完**：切完第一段面就變了，
  第二段的兩個點可能分屬切開後的不同塊。


---

## 邊上加點 ＋ 顯示點（2026-08-25）—— 加了卻看不見等於沒加

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **邊上加點已完成**（2026-08-25）。工具列「編輯」那組的 `邊上加點`
  ＋ 個數框（預設 1 ＝ 中點）。**在選到的邊上放點，什麼都不連。**
  測試 1720 → **1752 項**。

  🔴 **它是 kang 問出來的**：「是不是還有功能是**可以增加點**..
  然後再使用多點連接功能?」—— 能加點的四顆按鈕**加完都順手把線連掉了**，
  沒有一顆是「只放一個點」。
  ⭐ **有了它，「多點連接」才真的自由** —— 在這之前只連得到既有的角。

  ⛔ **刻意不做位置參數**：只給「幾個點」（均分），要放在特定位置就
  **加完用「拉點」移過去** —— 跟「加線只加線、形狀交給拉點」同一條，
  而且**是 kang 自己示範的**（「本來會產生歪的切線..人工拉點後做調整」）。

- 🔴 **這一輪最該記住的一條斷言：共線的點不可以被吃掉。**

  方塊頂面加一個點之後是**五邊形**，其中一個角是 180 度。
  ⚠ 而這個專案別處都在**消滅**多餘的點（還原多邊形、清孤點），
  `bake()` 甚至會自動跑 `mergeCoplanarFaces()`。

  **被吃掉的話這顆按鈕就是按了沒反應**（坑第 21 條），
  而且體積、面積、χ **全部照樣正確**（坑第 17 條）。已釘成測試（★★★）。

  ⭐ 另一條 ★★★ 是「**加完的點可以直接拿去多點連接**」——
  ⛔ 少了它，兩顆各自通過也不代表流程走得通。

- ✅ **邊上加點　kang 實測通過**（2026-08-26）。
  ⏳ **待實測：只有第 8 項（平板版面）** —— kang 手邊沒平板。清單留著當對照：
  1. 🔴 過濾器切「**邊**」→ 選一條邊 → 按 `邊上加點` →
     **那條邊中間要多一個點，而且看得到**（點很小，有沒有被標出來）
  2. 🔴 **接著按 `多點連接`** —— 新的點選得到、連得起來
  3. 個數框改成 3 → 一條邊上三個點平均分佈
  4. 選好幾條邊一起加
  5. **加完用「拉點」把那個點移到想要的位置**（那是刻意的分工）
  6. 形狀有沒有變（**不可以變**）
  7. 存檔再開，點還在
  8. **平板版面**：這一組又多了一顆按鈕 ＋ 一個框

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。

- ✅ **顯示點已完成**（2026-08-25）。工具列「編輯」那組、過濾器旁邊的
  `顯示點`（**預設開**）：把選到那個物件**所有的點**標成小白圈。

  🔴 **kang 實測抓到的洞**：「角點還可以分辨..但是**新增的點**..
  除非開線框才可以找的到位置」——「邊上加點」加在邊中間的點
  **沒有任何視覺線索**，等於加了卻不知道加在哪（坑第 21 條）。

  ⚠ **這正是我自己在實測清單第 1 項寫的那句話**（「沙箱驗得了『點在
  資料裡』，**驗不了『你看得見』**」）—— 寫得出來，還是要他驗才知道。

- 🔴 **這一輪的技術重點：不能沿用既有的畫法，而原因是量出來的。**

  `setPickMarks()` **每個點做一顆球**（`SphereGeometry(1.4,12,8)` ≈ 200
  個三角形的獨立 Mesh）。選取標記只有幾個，那樣寫沒問題 ——
  **拿來畫「全部的點」就是 1000 顆球、20 萬個三角形。**

  → 改用 **`THREE.Points`**（整批一次交給顯示卡），
  1000 個點的成本**遠低於模型本身已經在畫的那些三角形**。

  ⭐ **kang 的直覺是對的（球 seg32 會不會卡），但原因不是「點太多」，
  是既有的畫法。** 他提「那只顯示新加的點呢」—— 那個做不到：
  **程式不記得哪些點是新加的**（編輯會拆掉重建，記號會消失，
  就是 `hard` 邊燒過的同一件事），要記就得再開一種標記，
  而那是量測那條紅線擋掉的東西。

  ⚠ `sizeAttenuation: false` 是刻意的：點的大小**固定螢幕像素**，
  拉遠拉近都看得到（球會跟著縮放，拉遠就消失）。

- ✅ **顯示點　kang 實測通過**（2026-08-26）。
  ⏳ **待實測：只有第 8 項（平板）** —— kang 手邊沒平板。清單留著當對照：
  1. 🔴 **邊上加點之後，那個點看得到嗎**（小白圈，不必開線框）
  2. 🔴 **拉遠拉近**，白圈大小要一樣（⛔ 不可以拉遠就不見）
  3. 過濾器切到「**邊**」或「**面**」→ 白圈要**收掉**（那時候點選不到）
  4. 按 `顯示點` 關掉 → 白圈消失，而且有講一句
  5. 離開編輯模式 → 白圈收掉
  6. 選到的點仍然是**橘／黃的球**，蓋在白圈上面
  7. 🔴 **球 seg32 開顯示點，會不會卡**（桌機應該完全沒感覺）
  8. **平板**：同上，而且這一項只有平板驗得了
  9. 白圈**擋不擋得到點選**（⛔ 不可以擋）

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。


---

## 分離（2026-08-25）—— 「切開」跟「拆成兩個物件」是兩件事

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **分離已完成**（2026-08-25）。工具列「編輯」那組的 `分離`
  （在 `切一刀` 旁邊）：**沿著選到的那一圈邊，把物件拆成兩個獨立的物件。**
  測試 1752 → **1779 項**。

  🔴 **kang 講的那件事**：「刀具在我的想法中是將模型切開...如果是一個球..
  我可以把球**切成兩半...變成兩個半圓模型**」。

  **「切開」跟「拆成兩個物件」是兩件事，而我們原本只有前者** ——
  切一刀／環切都只加線，網格從頭到尾是一整塊。

  ⭐ **銜接點剛剛好**：切一刀、環切做完那圈邊**本來就自動選中了**，
  切完直接按「分離」。〔kang：「都可以套用在切一刀或是環切」〕

- 🔴 **判準是「圍不圍得成封閉邊界」，不是「有沒有切成兩半」** ——
  那圈邊當**牆**，走訪走不過去的就是另一塊。
  ⚠ **判準是「走不走得過去」，不是幾何位置**（拿平面分左右會被凹形狀騙）。

  ⭐ **所以它也能把任意一塊面分出去**，不限於切兩半 ——
  那正是對照表說的「**這對分片是另一條路：與其標接縫，直接切成好幾個物件**」。

  💡 **這個能力是測試自己撞出來的**：原本寫「隨便抓三條邊應該分不開」，
  結果分開了 —— 那三條剛好圍住球極點的一個三角形。**程式是對的，測試錯了。**
  〔⚠ 挑樣本要先去量，不要隨便抓 —— 任意切線案例五、面上加線的 60×45
  　都栽在同一件事，這是第三次〕

- ⚠ **拆出來的兩塊是「開放的」，那是正確的。**
  球切兩半 ＝ 兩個**碗**，不是兩個實心半球。要補按現成的 `補洞`
  （已釘成 ★★★ 測試：拆出來的那塊補完是封閉的、χ 回到 2、體積是正的）。
  ⛔ **不在分離裡偷偷補** —— 使用者要的可能就是碗（薄殼件）。
  🔴 但 toast **一定要講**，並指出「補洞」那條**真的走得通**的路（坑第 34 條）。

- ⚠ **位置刻意不動**：兩塊都沿用原物件的變換，網格座標一格都不改 ——
  分離之後畫面上**什麼都不會跳**。
  ⚠ 它是同組按鈕裡**唯一會改變文件結構**的（走「打散」那條
  `doc.remove` ＋ `doc.add`，⛔ 不是 `setMesh()`）。

- ✅ **分離　kang 實測通過**（2026-08-26）。
  ⏳ **待實測：只有第 8 項（平板版面）** —— kang 手邊沒平板。清單留著當對照：
  1. 🔴 **球 → 切一刀 → 直接按 `分離`** → 變成**兩個物件**
     （右上角物件數應該 +1，兩塊可以分開選、分開移動）
  2. 🔴 **選一塊按 `補洞`** → 變成完整的半球（那才是他要的「半圓模型」）
  3. 分離之後**畫面上不可以跳**（位置維持原樣）
  4. 環切之後分離（圓柱斷成兩截）
  5. 只選一條邊按分離 → 要擋，而且說「用切一刀或環切切一圈」
  6. 分開的兩塊各自**展開／3D 列印**還走得通
  7. 存檔再開，兩個物件都在
  8. **平板版面**

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。


---

## 刀具兩版（2026-08-25）—— 第一版被實測否決，第二版是 kang 想出來的

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- ✅ **刀具（自由切）已完成**（2026-08-25）。工具列「編輯」那組的 `刀具`。
  **它是一個模式**：按一下進入 → 在畫面上點兩個位置定出切線 →
  整個物件照那條線切開。測試 1779 → **1806 項**。

  🔴 **kang 要的**：「切一刀是要用數值控制...**我不能真的自由切我想要的
  區塊**..但是刀具我是想要是**自由切**」。

  ⭐ **它唯一「新」的只有一段**：`planeFromTwoRays()` ——
  **你點的兩個位置 ＋ 現在的視角 ＝ 一個平面**，切開網格本來就會。

  🔴 **一個公式同時對透視與正交，那不是巧合**：平面過
  `o1`、`o1+d1`、`o2+d2` 三點 → 透視退化成 `d1×d2`（過相機那個平面）、
  正交退化成 `d1×(o2−o1)`（順著視線切）。⛔ 不為兩種相機各寫一段。

- 🔴 **順手把一個「散在五個地方的判斷」收成一條路。**

  加刀具時發現「gizmo 該不該收起來」（`!seamMode && !mateMode`）
  **散在五處** —— 第四種模式一來就要五處都記得改。
  → 抽成 `get inPickMode()`。⛔ 靠紀律維持的東西遲早漏，
  而漏掉的症狀（**箭頭擋在畫面中間**）很難聯想到原因。
  〔坑第 31 條。同一輪還把 `worldAxisPlane()` 收成
  　`worldPlaneToLocal()` 的特例，改完 **1779 項一項都沒掉**〕

- ⚠ **刀具那顆按鈕的鎖定規則有一個陷阱，值得記：**
  **沒選物件要鎖**（進了模式就選不了物件了），
  🔴 **但模式開著時一定要可以按** —— 那是唯一的取消方式，
  **鎖起來就出不去了**。

- 🔴 **kang 實測（2026-08-25）：功能六項都對，但這顆按鈕不好用。
  而他指出的問題比任何一個 bug 都重要。**

  > 「我以為是可以**環繞模型切開**...光是兩點...**測試後也不知道要如何
  > 　點兩點呈現我想要的切一刀位置**...我不太知道要怎麼說」

  ⭐ **他表達得夠清楚了** —— 「不知道怎麼點才切得到想要的位置」就是問題本身。

  **原因**：那條線**往螢幕深處延伸**成一片無限大的刀片，
  而「往深處延伸」那個方向**使用者看不見**，所以按下去才知道切到哪。

  🔴 **那不是「結果不唯一」**（程式沒有亂猜），
  是**結果唯一但使用者事先看不到** —— 一樣不能用。
  ⚠ **這是坑第 21 條的一個新面向**：原本那條講的是「看不出有沒有作用」，
  這裡是「**看不出會作用在哪**」。

  ⭐ **而且他一開始的直覺是對的**：他想的「環繞模型切開」正是
  **Blender 刀具的預設行為（貼著表面走）**，我做的是它按 C 之後的
  那一種（切穿）。

- ✅ **兩條預覽線都補上了**（2026-08-25，測試 1806 → **1815 項**）：

  | 畫什麼 | 說明什麼 |
  |---|---|
  | 螢幕上的**橘虛線** | 你**畫過哪裡**（第一版就有，但**壞了**，見下）|
  | 模型上的**橘實線** | 🔴 **會切到哪裡** ← kang 缺的是這個 |

  🔴 **橘虛線原本根本沒出現，是我的 bug**：`#knifeLine` 那個 SVG 疊層
  只寫了 `inset:0` **沒給明確寬高** —— SVG 是 replaced element，
  寬高 auto 時用自己的預設尺寸（300×150），而它預設 `overflow:hidden`，
  **線畫在那個框外面就直接被裁掉**。
  ⚠ **語法沒錯、邏輯沒錯，就是看不見** —— 沙箱驗不了畫面的典型代價。

  ⚠ `slice/section.js` 的 `sectionAt()` **重用不了**（綁死在 X／Y／Z 軸上，
  而刀具的平面是任意方向）。〔又一次「讀程式，不要照它的描述推」——
  我原本在計畫裡寫「那個算法現成」〕
  ⭐ 但要的比它簡單：**只要線段，不必串成封閉輪廓**。

  ⚠ 用 `LineSegments`（每兩點一段），⛔ 不是 `Line`；
  ⚠ **rAF 節流**（O(面數) × pointermove 很密，坑第 22 條）。

- 🔴 **同一輪 kang 又抓到一個，而且病因是用錯 API：
  `hidden` 是 HTML 元素的屬性，對 SVG 元素不保證生效。**

  症狀：切完之後**那條橘虛線還掛在畫面上**，停在最後移動到的位置。

  ⭐ **「只有虛線留著」正好把病因指出來** —— 模型上那條實線用的是
  另一套機制（`scene.remove()`），它正常消失了。**兩條線的行為分岔，
  就把「藏不起來」縮小到 SVG 那一半。**

  → 藏／顯示一律改 `style.display`，HTML 那邊的 `hidden` 屬性也拿掉，
  初始隱藏改用 CSS。

  ⚠ **踩到的原因是照抄了旁邊的 `#marqueeBox`** —— 那個是 **div**，
  `hidden` 對它有效。⛔ **同一個檔案裡的寫法不一定能照抄，
  要先看那是什麼元素。**

- ✅ **刀具第二版完成，kang 驗收「滿意」**（2026-08-25，測試 1815 → **1842 項**）。
  🔴 **操作方式是 kang 自己想出來的**（第一版被他實測否決之後）：

  > 「改成可以任意點選**不只兩個點**...同一面兩點指定完...我再按刀具
  > 　就切這一個面...正面兩點然後我反側面再選一點..就會切兩個面...
  > 　以此類推...**環繞一圈**」

  ⭐ **它剛好解掉第一版「預覽一直亂跳」的病**：點的是**模型上的實際位置**
  （吸到最近的邊），不是「從眼睛射出去的一條線」——
  **轉視角完全不影響**，轉到背面再點天經地義。

  ⭐ **而且一段新的幾何都沒寫**：
  1. 在每個點的位置**插一個頂點**（＝「邊上加點」的後半段）
  2. 那些點就變成**既有頂點**
  3. 直接餵給 **`connectVertsPath()`**（＝「多點連接」）

  🔴 **「跨面」也是免費的**：正面與側面的**共用邊同時屬於兩個面**，
  在共用邊上點一下，前後兩段就自然接起來。

  介面：按鈕文字會變（`刀具` → `刀具（點兩個以上）` → **`切下去（3 點）`**）、
  旁邊一顆 `取消`、**再點同一個地方 ＝ 取消最後那一點**。

- 🔴🔴 **這一輪我把 `main.js` 改壞過一次，教訓比功能重要。**

  用「找兩個錨點、整段替換」的方式改程式，**錨點抓錯** ——
  一次吃掉中間 **18 個函式**（擠出、壓平、環切、分離、多點連接、
  編輯模式切換…）。

  ⚠ **而 `node --check` 說「語法 OK」** —— **刪掉整個函式不會有語法錯誤**，
  那道檢查完全沒攔到。〔坑第 17 條的形狀：中途的檢查都過，末端才錯〕

  🔴 **日誌鐵律早就寫著**（「文件分層 → 兩條鐵律」）：
  > **搬移一律逐字搬 ＋ 機械斷言，⛔ 不做「順手精簡改寫」。**
  > 手寫重組會靜默掉東西 —— ERP 那次重組掉了一整節，沒有任何東西報錯。

  **那條規則本來只寫在「搬文件」底下，而我這次是在搬程式** ——
  ⛔ **它對兩者都成立**，已經在該處補上一句。

  → 還原方式：kang 執行 `git checkout -- 建模器/js/main.js`
  （只還原那一個檔，其他改動都保留）。
  🔴 **然後改用「逐個函式精準編輯」重做，⛔ 不再整段替換。**

  ⭐ **順手加了一道檢查，日後每次動 `main.js` 都該跑**：
  ```
  for f in $(grep -o "=> [a-zA-Z]*(" js/main.js | sed 's/=> //;s/(//' | sort -u); do
    grep -q "^function $f" js/main.js || echo "缺 $f"
  done
  ```
  它比 `node --check` 抓得到更多 —— **「被呼叫但不存在的函式」正是
  刪錯東西的症狀**。

- ⚠ **兩個函式現在沒有呼叫點了**（第一版刀具的殘骸），要處理：
  | 函式 | 現況 |
  |---|---|
  | `planeFromTwoRays()` | ❌ **沒人用** —— 那是被否決的「兩點定平面」的核心 |
  | `planeCrossSegments()` | ❌ 沒人用，但**明顯有用途**：接上「切一刀」的預覽 |

  ⛔ **不要當成「還有人在用」** —— 兩個都還留著測試（共 36 項）。
  見待辦那一條。

- ✘ **已作廢：第一版刀具的實測清單（留著當對照）**：
  1. 🔴 **點第一下之後，螢幕上有沒有橘色虛線跟著走**（上次完全沒出現）
  2. 🔴 **模型上有沒有一圈橘色實線，指出這一刀會切在哪** ← **這一輪的重點**
  3. 移動時那圈線跟著變，而且不會卡
  4. 🔴 **有了它之後，切得到你想要的位置了嗎** ←
     ⚠ **這一項才是真正的驗收**，前三項只是「東西有畫出來」
  5. 取消（再按一次刀具）之後兩條線都要消失
  6. 球那種面多的模型，移動時會不會頓

- ✘ **已作廢：第一版的實測清單（已由上面那則取代，留著當對照）**：
  1. 🔴 **觸控筆／滑鼠點兩下，會不會被當成轉視角**
     （kang 2026-08-25 自己提的觸控筆，這一項只有他驗得了）
  2. 🔴 **預覽線**：點第一下之後有沒有一條橘色虛線跟著走
  3. 斜著切一個方塊 → 切面是斜的（那正是切一刀做不到的）
  4. **切完自動選中那圈線**，而且**自動進編輯模式** → 直接按 `分離` 拆得開
  5. **再按一次「刀具」取消**（點了第一下之後也要取消得掉）
  6. 沒選物件時按鈕是**灰的**，滑過去說「先點一個物件」
  7. 兩點點在幾乎同一個位置 → 擋下來說「離太近」
  8. **平板 ＋ 觸控筆**（第 1 項的真正戰場）

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息**（坑第 34 條）。

- ⏳ **刀具「貼著表面走」那一種還沒做。** Blender **預設**是貼表面
  （按 C 才穿透），我們做的是**穿透**那一種。
  ⚠ kang：「我還不知道真正刀具效果是如何..所以先同意你的意見」——
  **那是他誠實說不知道，不是同意永遠不做。等他用過穿透版再問一次。**

- 〔原本這裡寫「再下一步：刀具」，已完成〕 kang 2026-08-25 澄清過分工：
  > 「切一刀是要用數值控制...我不能真的**自由切我想要的區塊**..
  > 　但是你說的分離..這很不錯...**都可以套用在切一刀或是環切**...
  > 　但是刀具我是想要是**自由切**」

  ⭐ **分離沒有取代刀具**，兩件事都要做。刀具做「穿透」那一種：
  **在畫面上畫一條線，照那條線把整個物件切開** ——
  底層還是 `bisect()`，只是平面**從畫的線算出來**而不是從軸選。

  ⏳ **「貼著表面走」那一種先不做**（kang：「我還不知道真正刀具效果是如何..
  所以先同意你的意見」）—— ⚠ 那是他**誠實說不知道**，不是同意永遠不做。
  **等他用過穿透版再問一次。**


---

## 量測第 1、3 步（2026-08-25）—— 一個「時好時壞」的 bug

> 〔2026-08-26 從 `PROJECT_LOG.md`「開新對話時先讀這段」逐字搬入，一字未改。
> 　規則的權威版仍在 `PROJECT_LOG.md`；本檔只回答「當初怎麼做的、怎麼驗出來的」。〕

- 🔴 **量測那一輪抓到一個「時好時壞」的 bug，值得記住它為什麼難查。**

  打完數字按 Enter，**有時候有效、有時候跳紅字**。
  原因是 `TransformControls.axis` 是「**滑鼠現在停在哪根把手上**」，
  不是「這次在拖哪根軸」—— 放手之後指標掃過空白處軸就沒了；
  但如果指標**直接從把手移出畫布**，畫布收不到事件，軸就還留著。

  🔴 **kang 的原話是「似乎沒作用」，而「似乎」兩個字就是線索** ——
  ⛔ 使用者說「似乎」「有時候」的時候，不要當成他沒操作好，
  **那多半是狀態依賴，而且依賴的是一個你沒想到的東西。**

  → 已修：**軸與種類在拖曳開始時就記進 `_drag.start`**，不再事後去問。
  ⭐ 跟「記初始座標、每幀從初始值重算」同一條：**軸也是初始狀態的一部分。**

  ⚠ 順帶修掉另一個：按 Enter 會讓 `commitEditNumber()` **跑兩次**
  （`keydown` 一次、`change` 再一次），第一次成功、第二次跳紅字，
  看起來像「做了又被打槍」。Enter 改成 `blur()`，
  **入口分開、動作只走一條**。



---

## 已結案的待辦（2026-08-27 從 `PROJECT_LOG.md`「待辦 / 可延伸」搬入）

> **只搬不改事實**：40 項一字未動，順序照原檔（35 項 `[x]` 已完成、5 項 `[~]` 已撤案）。
> 搬的理由：待辦區 820 行裡有 67% 是已經結案的，照落點規則「結案就移出，不堆積」。
>
> 🔴 **規則的權威版仍在 `PROJECT_LOG.md`**（鐵律那一節），本檔只回答「當初怎麼做、怎麼驗的」。
> ⛔ **`[~]` 那 5 項是「明確不做」，⛔ 不要因為搬到冷檔就當成可以重提。**

- [x] **第 2 期全部完成**（`8d2de61`）：布林運算、陣列鏡射、九種參數體
- [x] **第 3 期全部完成**（`77f386b`）：展開 v1、K 因子、DXF／SVG／列印／備料 CSV

- [x] **第 5 期(A)(B)(C) 完成**（`766a3fd` / `7c4fa25` / `91a7f03`）：
      止血、外皮弦長、生成樹先走最平的邊、實體可直接展開、STL 修正

- [x] **指定分片的介面**（`08f5b90` 資料層 ＋ `efa0c3b` 介面）
      ★ kang 實際最需要、但原路線圖裡沒有的一項。用法見 `規格\建模器-展開與分片.md`

- [x] **對齊／均分 ＋ 正交三視圖 ＋ 框選 ＋ 貼合**（`71f2d21` ＋ `43b0c03`）
      用法與設計決定見 `規格\建模器-編輯與組裝.md`

- [x] **剖面分切**（第三條生產路徑）`0507c51` —— 三條路到此全通。
      用法與設計決定見 `規格\建模器-剖面分切.md`。**還沒實際下料疊過**
      （順手做了 gizmo 的 X／Y／Z 軸標，見坑第 28 條下方）

- [~] **板材尺寸上限檢查　⚠ 已取消，不要再做**（2026-08-22）
      kang 的 DXF **會先進 Illustrator 做整體處理才給 CNC**，
      所以「一片塞不塞得進板材」是排版時就看得到的事。在建模器裡再做
      一次是重複勞動，而且判斷還比 Illustrator 差（它看得到全部的片
      一起排）。這一項是問了一句才發現前提不成立的，經過見坑第 23 條

- [~] **板件（折板）的分片　⚠ 已取消，不要再做**（2026-08-23）
      板件是鈑金概念（網格代表中性面），而**中性面存在的唯一理由是金屬折彎會拉伸**，
      我們一種金屬都不用。第 6 期的編輯也不支援板件。
      理由與推導見 `編輯功能設計方向.md` 第 9 節。
      既有板件留著不動 —— 動它要改 898 項測試裡一大票與 v4 舊檔相容，
      而它現在沒有在害人（「為了整齊去改能用的東西，不划算」）

- [x] **第 6 期第一刀：拉點／拉邊／拉面　完成，kang 實測通過**（2026-08-23）
      資料層 `js/core/edit.js`（測試 898 → 949 項）＋ 介面（工具列「編輯」那一組）。
      **沙箱驗不了畫面**，實測項目與設計決定見 `規格\建模器-點線面編輯.md`。
      ⚠ 實測前先看該檔「介面」那節的四個實作決定與 `markGeomDirty()` 那條
- [x] **第 6 期第二刀：擠出面　完成，kang 實測通過**（2026-08-23）
      `edit.extrudeFace()` 走拆掉重建 ＋ 工具列「擠出」按鈕。
      操作方式 kang 拍板**方案 C**：按一下先長「一個吸附格距」，
      新蓋子自動選中，再用已經驗過的「拉面」調到想要的長度 ——
      擠出不需要新的拖曳邏輯，一個動作一件事。
      驗過：方塊／負值／連續擠出三次／斜面（法向朝外）／帶洞的面（兩個迴圈）／
      蓋子上的 CUT 存活／原網格沒被改／`capFace` 回傳的是對的那一片。
      **沙箱驗不了畫面**，用法與實作決定見 `規格\建模器-點線面編輯.md`
- [~] **把 `pushFace()` 接上介面　⚠ 已消失，不要再做**（2026-08-23）
      「第二件」做完之後它變成「移動 × 法向 × Z 那根箭頭」的一個組合，
      不需要獨立的函式與開關 —— 這正是三個概念正交的好處。
      `pushFace()` **留著但降級**：測試拿它當已知正確的答案去對新路徑
      （同一個位移，兩條路每個頂點都要在同一個位置）。⛔ 不要再為它加介面。
- [x] ✅ **外部參考調查　三條線全部做完**（2026-08-23）——
      Blender 編輯／兩個開源展開器／Pepakura，四份文件見檔頭那張表。
      **不要重查。** 結論與優先序在 `外部參考調查.md` 第 4 節，
      收斂之後的行動計畫在本檔「三件事」那一節。

- [x] 🔴 **第一件：尺寸的依據定調為網格　完成**（2026-08-23，`4361556`）
      ⚠ **做的跟原本寫的完全相反。** 原本寫「尺寸會錯，要用圓弧擬合補成 2πR」，
      推導之後發現**尺寸從來沒有錯過** —— 156.83 就是那個模型的真值。
      實際做的是把材料從尺寸計算裡拔乾淨（`canRoll` 分支、`applyStretch()`
      整支刪除），並補掉三個「說謊」的地方。測試 1027 → 1048 項。
      **完整定義見「尺寸的依據」那一節，動展開之前必讀。**
      ✅ **kang 實測通過**（2026-08-24）：展開圖折彎標註字變長
      （「弧長 X」→「展開 X　N 段」）沒有把窄的地方擠掉；
      紙材既有圖 157.08 → 156.83 如預期。

- [x] 🔴 **量測 第 1 步：面板顯示尺寸　完成，kang 實測通過**（2026-08-25）
      新增 `js/core/measure.js`（純函式，測得到）＋ `toolbar.js` 的
      `_editBox()` 與 `_renderAnalysis()`。測試 1574 → **1606 項**。
      選到邊 → 長度；選到面 → 面積、周長；多選 → 總長／總面積／外框；
      結構分析多一行**外框 X × Y × Z**。用法見 `規格\建模器-量測.md`。
      🔴 **順帶改掉一個沒人發現的錯**：面板的座標本來是**網格自己的座標**，
      物件一被搬動或縮放就跟對齊／貼合／切一刀／剖面分切對不起來，
      **而畫面上完全看不出來**。現在五個地方同一套數字。

- [x] 🔴 **量測 第 3 步：拖曳中的即時讀數　完成，kang 實測通過**（2026-08-25）
      「數值」那個標籤會跟著動作變成**移動／旋轉／縮放** ——
      位移量一直都印在那裡，缺的是「這個數字是什麼」講不出來。
      🔴 同一輪修掉兩個 bug：**軸在拖曳開始時就記下來**（原本事後問
      `tc.axis`，所以時好時壞）、**Enter 改成 `blur()`**（原本會套用兩次，
      第二次跳紅字）。經過見「先讀這段」那一則。

- [x] 🔴 **第二件：編輯地基（變換 ＝ 種類 × 方向 × 中心）　完成**（2026-08-23，`94f6c3f`）
      `elementBasis()`（方向基底：面／邊／點 ＋ 退化鏈）、
      `applyElementTransform()`＋`snapshotVerts()`／`restoreVerts()`
      （**記初始座標、每幀從初始值重算**，取代增量累加）、
      拿掉 `_attachEditProxy()` 那行寫死的 translate（只剩「點」還鎖）、
      工具列多一組「方向（世界／法向）＋ 數值」（只在編輯模式顯示）、
      拖曳中 `Esc` 取消、拖完可打精確數字。測試 1048 → **1102 項**。
      用法與實作決定見 `規格\建模器-點線面編輯.md`。
      ✅ **kang 實測通過**（2026-08-24，九項全過：法向箭頭、軸標沒錯開、
      斜面推得動、梯形收尖、點的旋轉縮放有灰掉、Esc 取消、打數字、
      離開編輯模式箭頭回世界、平板版面沒被擠掉）

- [x] 🔴 **第三件之一：子元素多選 ＋ active ＋ 中心　完成**（2026-08-24，`42a003f`）
      `editSels` 改成有順序的陣列、`editSel` 變成回傳最後一筆的 getter
      （**外面五個呼叫點一行都沒改**）；加選重用工具列那顆「加選」與 Shift；
      再點同一個 ＝ 取消選它（**active 自然退回上一個**）；
      換過濾器只清掉不合型別的（原本是全部清掉）；
      中心加「最後選的」；active 畫成**橘色**（新的 `role:'active'`）。
      三支純函式都改成吃陣列。測試 1102 → **1128 項**。
      用法與實作決定見 `規格\建模器-點線面編輯.md`。

      ⚠ **1-3 的第三件「網格重建時搬移選取」刻意沒寫** —— 見「三件事」第三件。
      ✅ **kang 實測通過**（2026-08-24，九項全過：三個都亮著且最後那個是橘色、
      再點一次會取消且橘色退回上一個、兩種中心收的方向不同、
      兩個斜面的法向指在中間、換過濾器留著同型別的、少掉時有講、
      混型別有擋、多個面按擠出有擋、面板寫出數量與中心種類）

- [x] 🔴 **六個工具的調查 ＋ 兩輪沙箱實測　完成**（2026-08-24，`6b89835`）
      **完整內容在 `外部參考-Blender編輯.md` 第 9 節，動工前必讀。**
      ⚠ 那一節的 Blender 部分是**二手**（那次抓不到原始碼），
      但第 9.7～9.8 的沙箱實測是第一手，**兩邊對不上以實測為準**。
      第 9.10 節列了**到現在仍然沒驗證的四項**，不要當成已知。

- [x] 🔴 **拆掉重建的三個必要配件　完成**（2026-08-24，`6b89835`）
      `preflightRebuild()`（不修只回報）／`cleanRebuild()`（清乾淨並交出 remap）／
      `remapElements()`（把選取搬到新網格）。
      🔴 **實測證明它們不是可選項**：`fromFaceList()` 對壞資料照建不報錯，
      `validate()` 抓不到孤點（只有 χ 露餡），而清孤點會讓索引位移（7 → 6）。
      **四個工具全部會產生孤點。**

- [x] 🔴 **還原多邊形　完成，而且沒有按鈕**（2026-08-24，`6b89835`）
      把共面的三角形還原成真正的多邊形面：方塊 12→6、圓柱 128→34、
      球 960→512，**體積面積精確不變**、χ 仍是 2。
      🔴 **在 `bake()`（轉成可編輯網格）時自動做，不是使用者的功能。**
      本來做成一顆「面合併」按鈕，kang 實測當場推翻 ——
      **它按下去畫面上沒有任何地方會變**（線框畫的是送給顯示卡的三角形，
      GPU 只吃三角形；稜線檢視兩邊都濾掉共面的邊），那就是坑第 21 條。
      它解掉「`box` 四邊形化」「**環切的前提**」，而且讓
      「畫面上看得見的才是可以標的」從**過濾**變成**結構保證**
      （方塊從 18 條邊變成 12 條，那 6 條隱形對角線根本不存在了）。
      ⚠ 管的兩個端面是**環形**，一個面裝不下兩圈，維持原狀並講明理由。
      🔴 **平面性防護**：判準是新常數 `MERGE_FLAT_TOL_CM`（1 微米），
      **不可以借用 `PLANAR_TOL_CM`（0.1mm）** —— 借了會鬆三個數量級，
      seg 719／720 照樣溜過去而且**展開面積會變**。1 微米是量出來挑的
      （真共面最糟 1.0e-6，要擋的 1.9e-3）。
      測試 1128 → **1199 項**。

- [x] ⚠ **`Delete` 鍵在編輯模式的防護　完成**（2026-08-24，`6b89835`）
      原本編輯模式按 Delete 會**刪掉整個物件**（查程式時發現的地雷）。
      現在擋下來並講清楚「刪除面」還沒做。

      ✅ **kang 實測通過**（2026-08-24，五項全過）：bake 後形狀完全不變、
      點一下就選到整個面、圓柱側面是 32 個獨立的四邊形、分片標記還在、
      編輯模式按 Delete 有被擋下來並說明。
      ⚠ **線框檢視看不出差別是正常的** —— 它畫的是送給顯示卡的三角形，
      而顯示卡只吃三角形。要看差別請看「結構分析」面板的「頂點/邊/面」。

- [x] 🔴 **壓平　完成**（2026-08-24，`6707622`）
      工具列「編輯」那組多一顆 `壓平`：選面（可多選）→ 壓到同一個平面 →
      **自動併成一個面**。測試 1199 → **1219 項**。
      ⭐ **它不是新功能**，底下是「縮放 × 法向 × Z 打 0」，一行新數學都沒有 ——
      三個概念正交換來的第二個免費功能（第一個是「沿法向推拉 ＝ 移動 × 法向」）。
      🔴 **這才是「把幾片 seg 變成一個面」的正解**：
      壓平之後它們**真的共面**，還原多邊形就會自己併掉，而且是**真正平的**面。
      ⚠ 形狀會變，所以 toast 講出「壓平前偏離多少」（那是使用者付的代價）。
      ⚠ 本來就平的面 → 什麼都不做並講一句，不記 Undo。
      ⚠ 順手拿掉 `applyEditNumber()` 那道**沉默拒絕 0** 的防護（坑第 11 條）——
      沿法向縮到 0 就是壓平，完全合法。
      ⚠ `remapElements()` 加了去重：三個舊面全指向同一個新面。

- [x] 🔴 **扇形三角化只對凸多邊形成立 —— 8 個出口一次修乾淨**（2026-08-24，`6707622`）
      **kang 實測抓到的**：圓柱壓平一段再往內拉，畫面上出現一塊奇怪的三角形。
      原因是 `toGeometry()` 用扇形三角化，而**那只對凸多邊形成立** ——
      上蓋一凹就有三角形跑到外面、繞向翻掉（多畫 81.95 cm²＝4.56%）。

      🔴 **這個 bug 一直都在，是「還原多邊形」讓它浮出來的**：
      n 邊形本來只出現在 `roundBox`（凸）與 `tube`（四邊形），
      而匯入的擠出件早就自己耳切了 —— 從來沒有非凸的 n 邊形。

      全專案有 **8 個地方**在做同一件事，影響遠不只畫面：
      `mesh.area()` 多算、`region.js` 面積與重心錯、
      **STL 有繞向翻掉的三角形（切片軟體會報非流形）**、
      **`bool.js` 送給 Manifold 的是壞掉的實體**、**展開片面積多算（備料會錯）**。
      ⚠ 只有 `volume()` 是對的 —— 它是**有號**量。而那正是坑第 17 條：
      **中途的量一直都是對的，末端才錯。**

      **修法**：抽出 `core/triangulate.js`（耳切，逐字搬自 `extrude.js`，
      md5 對過）＋ `mesh.faceTriangles()` 當**全專案唯一的三角化入口**，
      凸的走扇形（快，而且跟以前**逐字相同**）、非凸走耳切。8 個出口全部改用它。
      ⛔ **不要再自己寫 `for (let i = 2; i < vs.length; i++)`。**
      測試 1219 → **1231 項**，而**既有 1219 項一項都沒掉** —— 那就是
      「凸的行為完全沒變」的機械斷言。
      ✅ **kang 外部驗證通過**（2026-08-24，線上 STL 分析工具）：
      **水密、可直接列印、manifold**。而且 **124 三角形／64 頂點**
      自己就對得起來（32 側面 ×2 ＋ 兩個 32 邊蓋 ×30；扇形的話會是 128／66）——
      **還原多邊形與耳切都在正確運作**，是從外部工具的數字反推出來的。

- [~] ✘ **溶解面（不共面的那種）　明確不做**（2026-08-24，`6707622`）
      「不管夾角、把選取的面併成一個 n 邊形」是 Blender 的溶解面。
      **實測：3 片 seg 併出來的 8 邊形偏離平面 0.9561 cm，是可切容許值
      （0.1mm）的 96 倍** —— 那不是「精確變近似」，是做不出來。
      ⛔ 不要因為 Blender 有就補上。真的想把幾片變成一個面，正解是**壓平**。
      〔溶解**共面**的面那就是還原多邊形，已經有了〕

- [x] 🔴 **環切　完成**（2026-08-24，`531dde6`）
      工具列「編輯」那組的 `環切` ＋ 刀數輸入框。選一條邊 → 繞一整圈加線 →
      **新的那一圈自動選中** → 用已經驗過的「拉邊」拉（跟擠出方案 C 同一個分工）。
      測試 1231 → **1281 項**。用法與實作決定見 `規格\建模器-點線面編輯.md`。

      🔴 **它逼出了一條新規則 `hard` 邊，那比環切本身重要** —— 見「先讀這段」。
      日誌原本寫「前提已經備好」，**那句話是錯的**：`bake()` 給了四邊形
      （ring walker 走得動），但切出來的線在原本的地基上
      **畫面上看不見、點不到、半塊面拉不動、按壓平就被併回去** ——
      四個出口全中，等於一顆按了什麼都不會發生的按鈕。

      **五項未驗事項全部驗過（實測，不是推論）**：
      1. χ 仍是 2、封閉、`ok=true`（方塊／圓柱／半條 ring 都是）
      2. 🔴 **體積與面積精確不變**：108000.000000 → 108000.000000、
         117054.196468 → 117054.196468、13800 → 13800
      3. 標記：⚠ **一條被切成 n+1 段，每一段都要繼承** ——
         `_copyMarksTo()` 那種索引配對**一定落空**（實測不補的話 CUT 0 條）。
         `loopCut()` 自己記一本帳
      4. 切點 `i/(cuts+1)`，刀數可調（1／2／3 刀都驗過）
      5. 半條 ring：撞到的 n 邊形**只插點不切開**（32 邊形→33 邊形），合法

      ⭐ **環切是四個「拆掉重建」的工具裡唯一不產生孤點的**（實測 0 個）——
      它只加不減。〔日誌原本寫「一定會產生孤點」，**那句也是錯的**〕
      ✅ **展開完全不受影響**：`flatten.js` 從頭到尾沒有用 `planarRegions()`，
      實測方塊環切前後片數與警告數一樣。已釘成測試。

      ✅ **kang 實測通過**（2026-08-24），而且**抓到一個 bug**：
      環切 2 刀以上 → 選兩個面壓平 → **其餘的切線全部消失**。
      原因是 `edit.js` 有兩處手寫的標記搬移沒跟上 `hard`
      （併面走的正是其中一條）。已修，並收成 `marksOf()`／`applyMarks()`
      一個入口 ＋ 一組自動展開的回歸測試（見上方「先讀這段」）。
      測試 1281 → **1291 項**。

      ✅ **修完之後 kang 再驗一輪，十項全過**（2026-08-24，`531dde6` 之後）：
      壓平不再吃掉切線、畫面上真的多出一圈線、自動選中、拉得出斜面收腰、
      半塊面點得到、圈數 toast 正確、半條 ring 的說明正確、刀數 3 切出三圈、
      存檔再開切線還在。

      💡 **kang 問過而且值得記下來的一件事**：「選水平邊會切垂直面？」——
      **對，那就是它的定義。** ring 找的是「**會被攔腰切斷的那些邊**」，
      新的線是把它們的中點串起來，所以**必然垂直穿過你選的那條邊**。
      → 要在圓柱腰上切一圈就**選垂直邊**；要把某一片側面直向切開就**選水平邊**。
      （選水平邊只繞到 2 條就停，是因為往上下會撞到端面的多邊形 ——
      　所以縱向繞一圈**幾何上做不到**，端面不是四邊形。）

- [x] 🔴 **任意切線（Bisect）　完成**（2026-08-25）
      `edit.js` 的 `bisect()` ＋ `worldAxisPlane()`、`main.js` 的
      `bisectSelected()` ＋ `updateBisectRange()`、工具列三個元件。
      測試 1415 → **1471 項**。用法見 `規格\建模器-點線面編輯.md`。
      ✅ **kang 實測八項全部通過**（2026-08-25，表在「先讀這段」）。
      第 5 項（旋轉後的座標對位）當時卡在「kang 沒有辦法量」，
      **後來被「量測第 1 步」的面板解開**（重心 X ＝ 5.00 ＋ 外框 X ＝ 0.00，
      四個數字互相對得起來），**一行程式都沒改**。
      🔴 **實測抓到兩個介面 bug，兩個都已修並實測通過**（都在「先讀這段」）：
      預設值寫死 `0`、以及**選取有兩份資料會切錯物件**。後者是核心。
      🔴 兩件比功能本身重要的事（本地／世界座標、三態容許值）也在那裡。
      ⭐ **照四動作框架的第三個案例，一樣成立**：
      加線 × 一圈邊（環切）／加線 × 面的內縮輪廓（內縮）／**加線 × 平面**（這個）。
      骨架跟 `loopCut()` 逐項對應，沒有新的重建路徑。

- [x] 🔴 **導圓角（多段導角）　完成**（2026-08-25）
      `bevelEdges()` 加 `opt.segments` ＋ 工具列的段數框（預設 1、上限 32）。
      測試 1471 → **1537 項**。用法見 `規格\建模器-點線面編輯.md`。
      ✅ **kang 實測通過**（2026-08-25，含他抓到的兩個 bug 修完之後的重驗）。
      ⭐ **第 12.4 節排的「把新點推到弧上」根本不必做** ——
      弧上的點就是兩個面法向的球面插值，精確落在弧上。
      🔴 **段數 1 逐個頂點完全相同**（單段那段程式一行都沒改）。
      ⚠ **非直角的角落擋下來說原因**，還沒解。
      ⚠ **點數跟第 12 節那張表不同、而且兩個都對** —— 見「先讀這段」那則，
      ⛔ 不要為了湊表上的數字去改結構。

- [x] 🔴 **outer miter：只導一部分的邊也能圓角　完成**（2026-08-25）
      方案見 `外部參考-Blender編輯.md` 第 13 節（Blender 三種接法裡選 **Sharp**）。
      段數 2／4／8／16 全部 χ＝2 封閉，體積收斂到 106921.2755、
      面數 ＝ 6+4n+8n²，沒有頂點跑到理想圓角外面。
      ⭐ **關鍵是「兩條弧共用同一個圓心」**（＝ 共用面的代表點 − w × 該面法向）。
      ⚠ **只做直角**，非直角仍然擋下來。
      ✅ **kang 實測通過**（2026-08-25，三項：只導頂面四條邊做得出來、
      12 條全導照樣正常、圓錐仍然擋下來且訊息沒再叫他「全部一起選」）。

- [x] 🔴 **連接兩點（Connect Vertex Pairs）　完成**（2026-08-25）
      `edit.js` 的 `connectVerts()` ＋ `main.js` 的 `connectVertsSelected()`
      ＋ 工具列一顆按鈕。測試 1606 → **1640 項**。
      用法見 `規格\建模器-點線面編輯.md`。
      ⭐ **四動作框架第四例，最便宜的一個** —— 骨架跟 `bisect()` 逐項對應，
      而且**連插新點那一半都不用**（兩個端點是使用者選的既有頂點）。
      🔴 **多一條斷言：頂點數也不變**（前三個都會加點）。
      🔴 **查外面翻掉一個假設**：對照表把 Pairs 與 Path 的名字寫反了，已修正。
      🔴 **沙箱抓到一個訊息說謊的 bug**：相鄰要最先擋，判準是「有沒有邊」。
      ✅ **kang 實測通過**（2026-08-26，清單在「先讀這段」；⏳ **待實測：只剩平板版面**）。

- [x] 🔴 **面上加線（＝ Blender Subdivide 選兩條邊）　完成**（2026-08-25）
      `edit.js` 的 `splitFaceByEdges()` ＋ `main.js` 的 `splitFaceSelected()`
      ＋ 工具列一顆按鈕 ＋ 位置框。測試 1640 → **1681 項**。
      用法見 `規格\建模器-點線面編輯.md`。
      🔴 **它是 kang 驗收「連接兩點」之後指出來的真正需求**
      （「讓面變成兩等分」），**兩顆按鈕分開是他拍板的**。
      🔴 **技術重點：第二條邊要反過來算（`t` 與 `1 − t`）**，
      否則 0.3 會切出斜線。斷言用「新線方向不隨 t 改變」。
      ⚠ **插點會波及鄰面**（側面四邊形 → 五邊形），必須，且 toast 有講。
      ✅ **kang 實測通過**（2026-08-26，清單在「先讀這段」；⏳ **待實測：只剩平板版面**）。
      ✅ **凹面穿出邊界的修正也一起驗過**（三項全過，含「不會穿出去的位置照樣做得到」）。

- [x] 🔴 **「加線」那一類已經補齊**（2026-08-25）
      環切／內縮／切一刀／連接兩點→多點連接／面上加線／邊上加點／
      **刀具（點一串位置）**。
      ✘ **「連接頂點路徑」自動跨面明確不做** —— 結果不唯一（見上方那則）；
      ⭐ 而**刀具已經涵蓋那個需求**：使用者在共用邊上點一下就跨過去了，
      而且**是他自己決定從哪裡跨**，不是程式猜。

- [x] 🔴 **選取那一組（第一批）　完成，kang 實測十項全過**（2026-08-26）
      　　測試 1890 → **1932 項**。新檔 `js/core/selectops.js`（純函式，測得到）。
      　　三項：**hover** ／ **選一條線（邊迴圈）** ／ **編輯模式框選子元素**。

      🔴 **這一輪最重要的不是三顆按鈕，是翻掉了對照表上的一格。**

      > 對照表寫「邊迴圈 ❌ 走不動 —— 它要四價頂點，
      > **而方塊與圓柱的頂點是 3 價**」。
      > **那句話的後半被拿去推到全部，而它只量過方塊與圓柱。**

      | 模型 | 價數分布 | 邊迴圈實測 |
      |---|---|---|
      | 方塊 | `{3: 8}` | 1 條 ✅ 對照表是對的 |
      | 圓柱 seg32 | `{3: 64}` | 1 條 ✅ 對照表是對的 |
      | **球 segW12 segH32** | **`{4: 372, 12: 2}`** | **32 條 ＝ 一整條經線** |
      | **球 segW32 segH16** | **`{4: 480, 32: 2}`** | **16 條 ＝ 一整條經線** |

      ⭐ **球有 372 個四價點，只有兩極不是** —— 而**球正好是瓣片展開的對象**。
      迴圈**剛好停在極點**（經線本來就從極走到極），那不是限制是正確行為。
      → **「瓣片展開 A 要點 384 條邊」解掉了：一條經線 1 下，12 瓣 12 下。**

      ⚠ **連帶翻掉另一則**：日誌原本寫「動工第一件事：實測『選一圈』
      抓不抓得到經線」—— **答案是抓不到**（同一條經線只抓到 1 條，
      邊環是**繞著球跑**的）。⭐ 兩者已釘成對照測試。
      〔又一個「推論不是權威事實」，而這次**被推論的是那張表自己**。
      　**表上寫 ❌ 的，要看它是拿什麼量出來的。**〕

      🔴 **kang 拍板：選一圈與選一條線是兩顆按鈕**（判準沿用
      「切一刀 vs 環切」那條：**功能之間的定位不可以互相模糊**）。

      | 按鈕 | 走法 | 球上選一條經線邊會拿到 |
      |---|---|---|
      | `選一圈`（邊環） | **橫著跨過**四邊形 | 繞球一圈，每條經線各 1 條 |
      | `選一條線`（邊迴圈） | **順著**同一條線走 | **整條經線**，從極走到極 |

      🔴 **hover 動工才查到的一件事，沒查會白做一輪**：
      【實證·grep 過 `lib/three/three.core.min.js`】
      **這一版的渲染器一次都沒有呼叫 `gl.lineWidth()`（0 次）** ——
      `LineBasicMaterial.linewidth` 存得進去但**永遠不會被套用**。
      → 邊要「變粗」只能畫成**細圓柱**。⛔ 不要再試 `linewidth`。

      🔴 **`HOVER_FACE_MAX ＝ 3500` 是量出來的，⛔ 不是猜的**：
      `nearestFace()` 約 **0.52 µs／面**（512 面 0.32ms → 8706 面 4.50ms），
      給 hover 的預算訂 2ms（一幀的 12%）＝ 3846 面，取 3500 留餘裕。
      ⚠ **⛔ 不要再犯 `VERT_DOTS_MAX ＝ 5000` 那個病**（那個是猜的）。

      🔴 **框選子元素連背面一起選**（kang 拍板，跟刀具「點到哪切到哪」同作風）
      —— ⚠ **所以一定要講出數量**，否則使用者以為選了 6 條實際選了 12 條，
      而下一步會照 12 條做（坑第 11、21 條）。

      ✅ **kang 實測十項全過**（2026-08-26。清單留著當對照）：
      1. ✅ 指到**點**會變大；指到**邊**會變粗
      2. ✅ 指到**面**，它的邊界會變粗
      3. ✅ **指到一個已經選到的東西**：黃色**又變大**（兩個狀態疊加成立）
      4. ✅ 過濾器切「點」時指到邊沒有反應
      5. ✅ 游標移出畫布，標記會消失
      6. ✅ 移動游標時不會卡
      7. ✅ **球上一條經線 → 16 條**（預設球，緯線 16）
      8. ✅ **球上一條緯線 → 32 條，繞一圈回來**
      9. ✅ 編輯模式拉框 → 邊被選起來，數量與「含背面」有講
      10. ✅ 過濾器切「點」框到點、切「面」框到面

      🔴 **第 7 項他一開始覺得不對：「我選經線…不是繞一圈…是繞半圈」。**
      **半圈才是對的** —— 而且是**面板上的數字自己證明的**：

      | 面板數字 | 意思 |
      |---|---|
      | 16 邊 / **17 頂點** | 開放的鏈（頭尾沒接起來）|
      | 外框 Z ＝ **0** | 這 16 條全在**同一個平面**上 ＝ 一個經線平面 |
      | 外框 Y ＝ **60** | ＝ 直徑，**從北極跨到南極** |
      | 外框 X ＝ **30** | ＝ **半徑**，所以只有**半個大圓** |
      | 總長 **94.10** | 16 × 60 × sin(5.625°) ＝ 94.10 **精確吻合** |

      ⭐ **經線本來就是半圈**（北極到南極），繞整圈的是緯線。
      **而半圈正是瓣片展開要的**：12 瓣的球是被 **12 條**經線分開的；
      若每條都繞整圈，6 條就分完了，數量會對不上。
      ⭐ 這也是「**讓兩個數字互相對得起來**」那條的現行示範 ——
      **不是我查出來的，是面板上那幾個數字自己吵出來的。**

- [x] 🔴 **狀態列的「三角形」改成模型的屬性　完成**（2026-08-26）
      　　✅ **kang 實測通過**（2026-08-26，四項全過；第 5 項作廢，見下）
      　　測試 1932 → **1954 項**。kang 的話：「**三角形數字…亂跳**」。

      **病因有兩個，都跟幾何無關：**

      1. 顯示的是 `renderer.info.render.triangles` ＝ **上一幀顯示卡實際畫了幾個**
         —— 轉視角會變（畫面外的被剔除）、切線框會掉（線框走的是線不是三角形）、
         gizmo 在不在也算、連陰影那一趟也算進去。
         **而標籤寫「三角形」，人讀到的是「這個模型有多複雜」。**
         🔴 那是坑第 20 條：**把內部的數字放上介面之前，先問這個數字的單位是什麼。**
      2. ⚠ **更新掛在 `loop()` 裡，而且只有 FPS 剛好變了才順便更新** ——
         所以顯示的是**一串不相干時刻的快照**，中間跳過的看不到。
         **那才是「亂跳」的直接原因。**

      → **kang 選甲**：改成 `mesh.triangleCount()`，**模型的屬性，穩定**，
      只有模型真的變了才會變。`scene.js` 的 `get triangles()` 已刪除並留墓碑。

      ⭐ **不必真的去三角化**：任何簡單多邊形三角化後**剛好 `n − 2` 個三角形**，
      所以把每個面的「頂點數 − 2」加起來就好 —— O(面數)，**一個暫存陣列都不配置**。
      ⚠ **那是量出來的不是推的**：八種參數體 ＋ **一個凹的 L 形面**（走耳切那條路），
      `faceTriangles()` 的總數與 `Σ(n−2)` **全部相同**。已釘成 ★★★ 測試。

      ⚠ `規格\建模器-STL匯出.md` 那個檢查表裡的「三角形」**是另一回事**
      （`out/stl.js` 算的真實 STL 三角形數），⛔ 沒有動到。

      ✅ **kang 實測四項全過**（2026-08-26）：
      1. ✅ 轉視角、縮放時那個數字不再變
      2. ✅ 切線框／稜線時也不再變
      3. ✅ 預設球顯示 **960**
      4. ✅ 環切／導角之後數字有跟著變

      🔴 **第 5 項「滑鼠移上去看說明」我開了一個驗不了的項目 ——
      作廢，不是通過。**

      【實證·讀過 `index.html`】`#status` 是 `pointer-events: none` ——
      **滑鼠穿過去不會停在它身上，所以 `title` 永遠不會跳出來**。
      kang 的回報就是一句「**我要怎麼看？**」

      ⚠ 那正是這條漏掉的：**開實測清單之前，每一項都要問一次
      「使用者驗得出這個結果嗎」**（鐵律五）。⛔ 我沒問。

      ⛔ **不要拿掉 `pointer-events: none` 去救那個 title** ——
      那樣狀態列會擋住左下角那塊模型（連從那裡開始拖曳轉視角都會失效）。
      **為了一個說明賠掉一塊可以操作的畫面，不划算。** 已在 `index.html`
      原地留註解擋住下一個人。

      🔴 **kang 的回應比那個 bug 更值得記**：他說「**我不懂這功能的用意是什麼…
      所以我無法給你回覆**」。

      ⭐ 那不是他不會答，是**我從來沒講過那個數字有什麼用就要他決定標籤**。
      → 講清楚之後（列印檔會不會太肥／畫面會不會卡，⚠ 而且它跟
      **結構分析**的「頂點/邊/面」與**匯出面板**的檢查表部分重疊），
      他選**維持現狀，只把死掉的說明拿掉**。
      ⭐ 判準是他自己定的那條：**為了整齊去改能用的東西，不划算。**

- [x] 🔴 **刀具的三項輔助　完成，桌機十項 kang 實測通過**（2026-08-26）
      　　⏳ **待實測：只剩平板三項**
      　　測試 1842 → **1890 項**（⚠ 中間刪掉 14 項，見「文件分層」那節的說明）。
      　　新檔 `js/core/stroke.js`（純函式，測得到）＋ `select.js`／`scene.js`／
      　　`main.js`／`index.html`。

      | 加什麼 | 做出來是什麼 |
      |---|---|
      | **一筆畫** | 按住拖一條線過表面 → 放開，**線穿過邊的地方變成切點** |
      | **雙擊閉合迴圈** | 在最後一點**快點兩下** → 接回起點並切下去 |
      | **吸中點** | 切點落在**邊的正中間**。工具列一顆開關 ＋ 桌機的 `Shift` |

      🔴 **三個決定，都是 kang 2026-08-26 拍板的**：

      1. **吸中點給兩條路**（工具列開關 ＋ `Shift`）—— ⚠ **平板沒有 `Shift`**，
         開關那條路不是方便，是平板唯一的路。〔跟「框選做成模式」同一個理由〕
      2. **一筆畫的起終點自動吸到最近的邊** —— ⚠ 吸的是**所在那個面**的邊，
         ⛔ 不是全網格最近的邊：後者可能在隔壁面上，那樣第一段的兩端就不同面，
         `connectVertsPath()` 會擋下來，而使用者只看得到「切不下去」。
      3. **快點兩下 ＝ 閉合；慢慢再點一次 ＝ 維持原本的取消最後一點。**
         🔴 **逐點按的行為一格都沒變**，快慢是唯一的差別。

      ⭐ **主斷言（結構保證，不是碰運氣）：產出的切點串，每相鄰兩個必定同屬一個面。**
      起點吸 `f0` 的邊、第 i 個交點是 `f_{i-1}|f_i` 的共用邊、終點吸 `f_n` 的邊 ——
      逐項推得出來，而那正是 `connectVertsPath()` 的規則。**已釘成測試**（每個案例都驗）。

      ⚠ **劃太快會跨過整個面**（兩端的面不相鄰）→ 把那一段**對半切、取中點
      重新問它在哪個面**，遞迴到相鄰為止（上限 128 份）。
      ⛔ 不猜一條邊（坑第 24 條），接不起來就明講「劃太快」。

      ⚠ **交點落在角落時往內挪 0.015 cm 並講出來** —— 不挪的話 `knifePath()`
      會因為一個不巧的位置把**整筆**退回。挪的幅度低於可切容許值（0.1mm），
      不會改變做出來的東西，但 ⛔ **不可以安靜地挪**（坑第 11 條）。

      ✅ **沙箱驗過（實測，不是推論）**：頂面橫劃／跨面／環繞一圈／只有兩個
      取樣點／圓柱腰繞半圈／打在角落上 —— **體積、表面積、χ 全部精確不變**。

      ✅ **kang 實測第一輪（2026-08-26）**：
      1. ✅ **桌機右鍵拖轉得動視角**
      2. ✅ **桌機左鍵拖畫得出線**
      3. ✅ **離開刀具之後左鍵轉視角有回來**

  🔴 **而且他抓到一個 bug：「右鍵也可以按住畫一條線..跟轉視角功能撞一起」。**

      **病因**：四個 `pointer*` 監聽**完全沒有分是哪一顆鍵** ——
      右鍵拖的時候 OrbitControls 在轉視角、一筆畫的取樣器**同時也在記線**。
      ⚠ **沙箱驗不了這種事**：資料層算出來的切點完全正確，
      錯的是「**誰該收這個手勢**」（介面那一半，1890 項數學一項都碰不到）。

      **修法照這個專案自己的前例**：**把「哪一顆鍵」在按下去的當下就記起來**
      （`this._down.button`）—— ⛔ 不是等到 `pointerup` 再問 `e.button`。
      ⭐ 那正是 `TransformControls.axis` 那次的教訓（2026-08-25）：
      **鍵跟軸一樣，是初始狀態的一部分。**

      🔴 **順手擋掉一個平板上一定會中、但還沒被測到的同類問題**：
      **兩指轉視角時，第一根手指早就開始畫線了。**
      → 規則是「**畫到一半又有人按下來 ＝ 這是轉視角，砍掉這一筆**」。
      ⚠ ⛔ **沒有用「現在有幾根手指」的集合去擋** —— 那種集合只要漏收一次
      `pointerup` 就永遠卡住（症狀是「突然就畫不了了」）。
      現在這個判準**自己會清乾淨**。

      ⚠ **限制只加在刀具模式裡**：物件選取那條路本來就不分鍵，
      而那是 kang 早就驗過的行為，⛔ 沒有順手一起改。

  ✅ **kang 實測第二輪：桌機七項全過**（2026-08-26。清單留著當對照）
      1. ✅ **右鍵拖只轉視角，⛔ 不會再多畫一條線**（上一輪修的）
      2. ✅ **右鍵「點一下」也不會多一個切點**（同一個病的另一半）
      3. ✅ 一筆畫的預覽線跟得上游標
      4. ✅ 快點兩下真的閉合並切下去
      5. ✅ 慢慢再點一次還是取消最後一點（**逐點按的行為沒有變**）
      6. ✅ 「吸中點」那顆開關看得出來是開著的，切點有落到邊的正中間
      7. ✅ 先拖一段、再輕點補幾個位置（混用）接得起來
      8. ✅ 「切一刀」打數字時，青色的預覽線畫在對的位置上

  ⏳ **待實測：只剩平板那三項**（kang 手邊沒平板）
      1. 🔴 **單指／觸控筆畫得出線、兩指轉得動視角**（`DOLLY_ROTATE`）
      2. 🔴 **兩指轉視角時，⛔ 不會憑空多出一條切線**
         〔⚠ 這一項**沒有人實際踩過**，是修右鍵那個 bug 時**照同一個病推出來**
         　順手擋的 —— 所以它比其他項更需要真的驗一次〕
      3. **平板版面**：工具列又多一顆「吸中點」，有沒有被擠掉

  🔴 **這一輪學到的，比功能本身值得記（已寫進規格檔）**：

  > **換掉一個既有手勢，就一定要開始分「哪一顆鍵」。**

  以前加功能都是**多一顆按鈕**，每個手勢只有一個收件人，所以四個
  `pointer*` 監聽**不分鍵也沒事**。一換手勢就變成**兩個收件人在搶同一次拖曳**。
  ⚠ **沙箱驗不了**：算出來的切點完全正確，錯的是「誰該收這個手勢」。

  ⚠ **kang 連續兩次把「混用」讀成「切好之後去移動切線」**，
  病因是我在註解與對話裡寫了「輕點**微調**」——
  **那個詞會讓人以為已經放下的點可以拉**（其實只能取消最後一點）。
  已全部改成「**再輕點補幾個精準的位置**」。
  ⭐ 這是「跟他講話不要用會被誤讀的詞」的一個實例：**他讀到的意思才算數，
  不是我寫的時候想的意思。**

      ⚠ **舊版的方案敘述已經整段換掉**（`OrbitControls` 行號那張表、
      「兩種輸入並存」的推導）—— 那是**動工前的調查**，做完之後的權威在程式裡：
      `scene.js` 的 `setKnifeInput()`、`select.js` 的 `_surfaceHit()`／
      `_wantSnapMid()`、`core/stroke.js` 檔頭。

      ✘ **角度限制／軸限制不做**（kang 2026-08-26 否決，⛔ 不要重提）——
      他的理由：**「不符合我們刀具功能的『自由』需求。」**
      ✘ **`C` 切穿背面不做** —— 那就是被否決的第一版，⛔ 不要當成「輔助」再列一次。
      → **顯示長度／角度**那一項歸「量測第 2 步」，不專屬刀具。

- [x] ✅ **殘骸清乾淨了，而且是四個不是三個**（2026-08-26）

      | 殘骸 | 處置 |
      |---|---|
      | `index.html` 的 `#knifeLine`（橘虛線） | **刪掉**（元素 ＋ CSS），原地留墓碑註解 |
      | `planeFromTwoRays()` | **刪掉，連 14 項測試一起** |
      | `select.js` 的 `screenRay()` | **刪掉** ← ⚠ **第四個，原本沒人發現** |
      | `planeCrossSegments()` | ✅ **接上「切一刀」的即時預覽**（青色線） |

      🔴 **`screenRay()` 是清的時候 grep 才發現的** ——
      它跟 `planeFromTwoRays()` 是同一版的一對（螢幕座標 → 射線 → 平面），
      而**待辦只記了三個**。⭐ **殘骸要用 grep 找，⛔ 不要照清單推。**

      ⚠ 順手還修掉一則**說謊的註解**：`select.js` 的 `pointermove` 上面寫著
      「刀具的預覽線要跟著游標走」，而**底下的程式只有框選** ——
      那是坑第 34 條的第二種長相（當初是真的，後來才變假的）。

      ⭐ `planeCrossSegments()` 接上去補的是跟刀具**同一個病**：
      打一個座標，那個平面落在模型的哪裡使用者看不見（坑第 21 條）。
      ⚠ 用 `oninput` 不是 `onchange` —— 要**打字的當下**就看得到。

- [x] **K 因子退出展開圖　完成**（2026-08-23，`cedbab3`）
      kang：「K 因子…這都是造成混亂的條件…**不應該在真實尺寸中出現**」。
      拿掉：`flatten.js` 的 `ri` 欄位（圖上改標**網格量出來的半徑**）、
      `sheet.js` 標題欄的 `K 0.5`、`dxf.js` 標題的 ` K0.5`、
      **展開面板的 K 因子輸入框與「用建議值」按鈕整組**。
      ⚠ **實測的關鍵事實**：K 從頭到尾**沒有參與圓柱的建模**
      （`buildPrim` 直接用 r=25，`neutralRadius()` 根本沒被叫到），
      但圖上卻印 R24.9 而網格是 25.0 —— 那個數字描述的是不存在的東西（坑 20）。
      ⭐ 連帶把 `arcCorrection()` 與 `bandsInGroup()` 的 `rule` 參數也拿掉了，
      所以「幾何核心不知道材料是什麼」現在是**結構保證**，不是靠註解提醒。
      ⚠ **折板的 K 留著，那是另一回事** —— 它在 `buildPrim()` 裡真的參與建模
      （換 K 連網格都變），跟 seg 決定圓柱有幾邊同類。**建模參數 ≠ 展開參數。**

- [x] 🔴 **接合編號：改成印在「有料的地方」　完成，kang 實測通過**（2026-08-26）
      　　測試 1954 → **1973 項**。只動 `js/out/sheet.js` 一支。

      🔴 **真正的病比 kang 回報的嚴重，而且是重現時才查出來的。**

      他回報的是「疊在一起」。用他的參數重現（管 r25/20、高 70、段數 32、
      紙模打樣 0.2）之後量出來：
      > **環形端面 66 個接合編號裡，有 32 個印在洞裡** —— 那塊料會被挖掉。

      **病因是一行規則**：號碼「往片的**重心**那一側縮 1.8」。
      而**環形片的重心在洞的正中央** —— 所以內圈那一半被往洞裡推。
      ⭐ 他看到的「疊在一起」只是連帶症狀：那一堆被擠進更小的圈裡。

      **修法**：判準改成「**落在料上**」（`pointInPoly(outline)`）。
      ⭐ **環形片沒有 `holes` 資料**（實測 `holes.length === 0`）——
      它的 `outline` 是一條**穿過缺口繞到內圈再繞回來**的封閉線，
      **那條線本身就已經把洞排除掉了**，所以問它就對了。

      🔴 **第二件：塞不下才縮字（`1.8 → 1.4 → 1.1 → 0.9`，第一個不擠的就用）。**
      ⚠ 環形端面的料只有 **5 公分寬**要塞 **66 個號碼** —— 物理上塞不下。
      ⛔ **不是全面改小**：本來就不擠的片一格都沒動（外側面仍是 1.8）。

      | 片 | 修之前 | 修之後 |
      |---|---|---|
      | 環形端面 ×2 | 洞裡 32 個 ＋ 壓 13 對 | **洞裡 0 個**、壓 1 對、字縮小 |
      | 內側面 125×70 | 壓 30 對 | **0 對**（字 1.8 → 1.4）|
      | 外側面 156×70 | 0 對 | **0 對，字高沒動** |

      🔴 **這個 bug 活這麼久，是因為既有的檢查碰不到它** ——
      「標註不可以疊在一起」那一組**只比同一排的 y**（它是為尺寸那一排寫的），
      而接合編號是**散在片內各處**的。已另外加一個**真正的 2D 外框相交**檢查。

      ⚠ **我兩次講得不精確，都是被實測或測試抓到的**：
      ① 說他 8/23 報的「圓柱蓋 67 疊住」重現不了 —— 那是我的標接縫方式跟他不同；
      ② 說「側面都跟以前一樣」—— **內側面其實縮到了 1.4**（測試當場抓到）。
      ⭐ 真正該釘的規則是「**本來就不擠的不要動它**」，⛔ 不是「側面都別動」。

      ⏭ **缺口處還會剩 1 對**（`131↔131`、`132↔132`）——
      同一個號碼的兩端在缺口碰頭，跟字大小無關。**kang 說沒關係**（見下）。

- [x] **第 4 期的一半：擠出**（`0b86cb8`）—— 輪廓從 Illustrator 匯入，
      用法與設計決定見 `規格\建模器-匯入線稿.md`

- [~] **第 5 期：圓錐的極座標展開　⚠ 已取消，不要再做**（2026-08-23）
      錐面的攤平**本來就精確**：扇形半徑 ＝ 母線，實測 seg 6～64 一律
      76.1579 對 76.1577。底緣是弦長，那就是那根 seg 邊錐的真實底緣。
      ⚠ 這一條的內容被推翻過兩次（「只剩紙材需要」→「扇形弧長 ＝ 底圓周長」），
      **兩個舊說法都作廢**，經過見「路線圖現況」第一段。
- [x] **主入口頁加上建模器的按鈕**（`0428b14`）
      連帶做了圖示（`icons/modeler-*.png`）與 `manifest-modeler.webmanifest`，
      所以建模器也能「加到主畫面」了。
      manifest 的 `scope` 是 `./建模器/`（另外兩個是 `./`）——
      從主畫面開建模器時視窗只圈住建模器，點回首頁會用一般瀏覽器開，
      不會把 PWA 視窗帶走。

---

## 日誌檔頭的輪次紀錄（2026-08-27 從 `PROJECT_LOG.md` 檔頭搬入）

> **只搬不改事實**：221 行一字未動。
> 搬的理由：那是「這一輪做了什麼」的**累加欄位**，每輪加一則，已經長到 221 行；
> 而裡面每一則的權威版都在別處（鐵律／文件分層／待辦／規格檔）—— **它是第四份**
> （前三份：`版控工具\commit-msg.txt`、本檔、規則各自的家）。
>
> 🔴 **日誌檔頭已改成「覆蓋型固定欄位」**（只留「最近一輪翻掉了什麼」，上限 5 條），
> 所以**這一節不會再增加新的則** —— 之後每一輪的經過寫進 `commit-msg.txt`，
> 要查就 Grep 本檔或看 commit 訊息。

最後更新：2026-08-27　V12.0（🔴 **這一輪只整理文件，一行程式都沒動。九件事**：

**二十一、🔴 抓到官方手冊原文了 —— 方法是「抓 `.rst` 原始檔，⛔ 不要抓網頁」。**
昨天說「驗不了」是**假的困難**：⛔ 沒有被擋，是 `docs.blender.org` 的網頁版
**把整站導覽放在正文前面**，91,867 字全被導覽吃光，**正文一個字都沒進來**，
而且**看起來像抓成功了**。把 `.html` 換成 `.rst`、網域換成
`projects.blender.org/blender/blender-manual/raw/branch/main/...` 就是純文字。
**網址寫進對照表第 10 節開頭。**
🔴 **原文佐證了 kang 的重講**：Dissolve Faces 官方只寫
「Merges each patch of connected faces into a single face」，**整段沒有平面性條件**。
⭐ **順手翻出四件我們寫得不準的**（全部升級成第一手）：
限制性溶解**只做到 Max Angle 固定 0 的特例**（Blender 可調，還有 Delimit 五個開關）／
刪除**是五種不是三種**（差別只在「孤點要不要留」，⭐ Blender 給使用者選、我們一律清掉）／
🔴 **`F` 是一顆鍵兩種行為**（建面 ＋ 溶解面，我們當成兩項在排）／
🔴 **整張表漏了一支**：`Collapse Edges & Faces`（唯一「簡化但不產生 n 邊形」的路），
**已補列** —— ⛔ **不要因為表上寫「一支都沒跳過」就相信它完整**。
✘ **手冊全文不存進專案**（kang 問過）：能重抓、會過期、repo 是 Public 而它是 CC-BY-SA。
⚠ **GIT 那條 kang 當天自己收回了**（「那只是我猜測」）—— 已從兩處改掉，
⛔ 不要當成他定的方向；**但「這張表靠什麼不過期」仍然無解，那一格留著。**

**二十、🔴🔴 kang 重講了一句話，而那句話升格成鐵律：
「原來不是不能做…而是需求擋住」。**
起因是他問「Blender 也做不到不共面又不想改形狀？」——查下去發現
**我們的網格本來就建得起不平的 n 邊形**（【實證】沙箱，`外部參考-Blender編輯.md` 738 行），
⛔ **溶解面從來不是「做不到」，是被「那張圖要從平板子上切下來」擋住的。**
🔴 **兩種寫法會導向不同的決定**：寫「做不到」，下一個人連想都不會再想；
寫「**被什麼擋住**」，他就知道**擋住的條件一變、這條要重新看**。
⚠ **同一天三個實例，全部是 kang 問出來的**：溶解面／
對照表「邊迴圈走不動」（只量過方塊圓柱卻推到全部，**球走得動**）／
對照表「沒有任何補洞的能力」（**早就有了**）。
⭐ 新鐵律掛在「**每次寫 ✘ 的當下**」，⛔ 不是「以後記得回來檢查」
（後者這個專案已經證明沒用）。溶解面那條**收成一份權威版**（規格檔），
日誌與對照表只寫「這條在那邊」；⛔ **`HISTORY.md` 不動**（流水帳，檔頭已明文降級）。

**十九、🔴 對照表刷新了 —— 它自己過期，而它是「排順序的唯一判準」。**
kang 問「為什麼我沒看到可以合併面」才查出來的。8/24 寫的，8/25～8/26 做完的
**一項都沒回寫**：5 格標著「⭐ 沒有、值得做」而東西早就做完了
（**Bisect ＝ 切一刀**、**刀具**、**分離**、細分 edge ring、
連接頂點路徑**已被 kang 否決卻還標 ⭐⭐**），
另外 4 格說明過期（導角「多段仍未做」、框選「編輯模式沒有」、
「由點建面」寫著補洞非它不可、細分沒提已涵蓋兩塊），
**10.7 那四個結論整組過期**。
🔴 **三大缺口現在只剩一個**：加線 ✅ 補齊、補面 ✅ 大半、**⏳ 選取才是缺口**。
⭐ 順手把「約 25 項」那個會過期的數字也拿掉了（改成數 ⭐ 現算）。
🔴 **病因寫進 10.8：「做完一件功能」這條路上，沒有任何一步會走到那張表** ——
跟「kang 說 OK 卻沒回寫」同一個結構問題，那個解掉了、**這個還沒有**。
⏳ **這一格無解** —— ⚠ kang 當天提過「我認為是 GIT 的問題」，**又自己收回了**
（「那只是我猜測」），⛔ **不要當成他定的方向去追**。詳見 10.8。

**十八、🔴🔴 kang 一句話問掉一輪白工：「選一條線是編輯區，分片不是編輯區」。**
我前一刻才寫「瓣片展開 A **只剩一步**」——**那是推論，我沒讀程式。**
去讀之後：【實證】`setEditMode(false)` 會 `clearEditSel()`（`select.js:1413-1415`）、
進分片模式**一定會**呼叫它（`main.js:597-599`）、`seamPick()` **不看 `editSels`**。
→ **在編輯模式選好 32 條，切到分片模式的當下全部消失。**
🔴 **卡點只解掉一半**：走訪解了（`edgeLoop()` 是純函式、不依賴模式），**搬運沒解**。
⭐ 推薦解法**甲**：分片模式加一顆「選一條線」——
`cutAroundFace()`（點一個面 → 標一整圈邊）**就是同一個骨架**。⛔ 還沒拍板。
⚠ **那句錯話原本寫在兩個地方**（待辦區 A ＋「再下一步」），**只改一處等於沒改**。
🔴 **這是「推論不是權威事實」的第三個實例，而且這次是 kang 直接指出來的。**
⏭ **kang 拍板：瓣片展開 A／B 排到後面，而且要一起做**
（⛔ 不是否決；A 不再是可插隊的小項；第 8 節那個 ❓ 因此結案 ＝ 要橫向也圓 ＝ 要省道）。

**十七、🔴🔴 立了一條新規則：⛔ 會過期的數字一律現算，不存。**
**測試的總項數，文件裡從此一處都不寫。**
病因是那個數字**同時存在六個地方**，而它每加一輪功能就變一次 ——
結果規格檔停在 **1231**，實際 **1973**，**過期 742 項沒人發現**。
🔴 **收成一處不夠，那一處照樣會過期** —— ⭐ 正解是**一處都不留**：
`run.mjs` 跑完本來就印「通過 N　失敗 0」，**答案一直都在，我們只是另外抄了一份**。
⚠ **判準：這個數字改對了會變，還是改壞了才會變？**
前者不存（總項數）；後者一定要存（13800、108000、124/64 那些基準數字）。
全文見「文件分層 → ⛔ 會過期的數字一律現算，不存」。

**十六、⚠ 「下游事實」原來存了三份，刪成一份 ＋ 升格。**
`規格\建模器-展開與分片.md` 早就有權威版，檔頭有摘要，**待辦區還有第三份**。
→ 待辦區那塊刪掉；裡面**兩句跨功能的教訓升格進「鐵律 → 目的藏在下游」**
（收成一張表：兩條下游事實各省掉了什麼工，**兩條都指向 Illustrator**）。
⭐ 又是坑第 33 條：**教訓寫在別的功能底下就等於沒寫。**

**十五、🔴 待辦區減重：40 項已結案的搬進 `HISTORY.md`，820 → 196 行。**
照日誌自己那條「⛔ 不看檔案大小，看六個徵兆，中三個以上才動手」量過 ——
**中 3 個**：①瘦身完又長回去（「先讀這段」8/26 剩 329 行，8/27 已 381 行；
全檔行數比改造前少、**位元組反而多 15KB**）、⑤**待辦區 60 項裡 40 項已結案（67%）**、
⑥**`規格\建模器-測試與驗證.md` 寫「1231 項」，實跑是 1973，過期 742 項沒人發現**。
✗ 沒中的：④地圖 0 斷鏈；❓ ③沒量，不列入計分。
**分型是乙型**（一份檔案身兼「待辦」與「流水帳」兩職）→ 判準規定
**⛔ 不重新分類，只把不同角色的內容抽出去** —— 所以只搬，⛔ 沒有拆檔、沒有改寫。
⚠ **`規格\建模器-點線面編輯.md`（2746 行）刻意不動** —— 8/26 已量過只中 0～1 個。
⭐ 機械斷言：40 塊逐字吻合、日誌裡 0 塊殘留、**`HISTORY.md` 舊內容 md5 完全相同**
（`a66b570d3598`，只在目次插 4 行、尾端 append 641 行）、測試項數鏈的缺口
仍是原本那四個（1309→1338／1367→1385／1537→1548／1548→1574），**沒有新增缺口**。

**十四、⚠ 三處 `⏳ 待實測` 是過期的，已回寫。**
狀態列「三角形」kang 早就驗過四項全過（第 5 項作廢），但第 127、379、1845 行
還掛著「待實測」——**正是「通過也要當場回寫」那條沒守住**。
現在 `Grep 待實測` 撈到的只剩**真正沒驗的平板那一批**。

**十三、🔴 接合編號原來有一半印在洞裡 —— 比 kang 回報的「疊在一起」嚴重。**
他報的是疊住；用他的參數重現（管 r25/20、段數 32）之後量出來：
**環形端面 66 個號碼裡有 32 個落在洞裡**，那塊料會被挖掉。
病因是一行規則：號碼「往**重心**那一側縮」，而**環形片的重心在洞的正中央**。
→ 判準改成「**落在料上**」；並且**塞不下才縮字**（⛔ 不是全面改小，
本來就不擠的片一格沒動）。測試 **1954 → 1973 項**。
🔴 **這個 bug 活這麼久，是因為既有的檢查只比同一排的 y** ——
而接合編號散在片內各處。已補一個真正的 2D 檢查。
⭐ **拿到一條下游事實**：kang「**會用 AI 繪圖開 DXF 或 SVG**」，
所以字小、缺口剩 1 對都可接受 —— ⛔ 沒問到這條我會去做「引線拉出去」那種大工程。



**十二、🔴🔴 立了第二條紀律：權威不是任何一方的說法 —— 對 kang 的話也成立。**
⚠ 我在他的截圖上讀到「三角形 232」與「2,176」，他回「兩張都是 232」，
**我當場就說「是我讀錯了」—— 而我根本沒回去重看**。後來確認：**我讀對了。**
🔴 撤回一個正確的觀察，成本是**那個病差點沒被查下去**。
⭐ 判準：**那個東西還在不在？** 截圖／程式／輸出檔可以重讀 → 就去重讀，
⛔ 不要用「誰說的」定案；記憶不能重讀 → 該撤就撤。
⚠ 這跟「kang 的實測是唯一的現實檢查」不衝突：**畫面上發生了什麼一律以他為準**，
這一條講的是**一份兩個人都看得到的證據**。全文見「鐵律 → 一、動手之前」。

**十一、🔴 立了一條「講話怎麼講」的規則，因為同一個病犯了四次。**
kang：「**因為我聽不懂你的專業術語。**」
`toast` ／「輕點**微調**」（他連兩次讀成「移動切線」）／`segW`（畫面上叫**經線**，
他因此卡住一項）／「整條經線亮起來」（實際是**半圈**，他以為是 bug）。
→ **開實測清單時，欄位名與按鈕名一律去 `index.html` 複製那個字**，
並且**講數量與形狀，⛔ 不要只講名字**（「16 條，從最上面連到最下面，半圈」）。
⭐ 掛在「開清單」那一步上，因為那是每次交付必定會走到的地方 ——
**前三次都只是提醒，而提醒擋不住第四次。**
⚠ 同一輪還修掉狀態列的「三角形」：它顯示的是**上一幀畫了幾個**
（隨視角、線框、gizmo、陰影而變），標籤卻寫「三角形」——**坑第 20 條**；
而且更新只掛在「FPS 剛好變了」那一刻，所以看起來亂跳。
**已改成模型的三角形總數**（測試 1932 → **1954 項**）。



**十、🔴 選取那一組第一批完成，而重點是它翻掉了對照表上的一格。**
三顆：**指到哪就亮哪（hover）／選一條線（邊迴圈）／編輯模式框選子元素**。
測試 **1890 → 1932 項**。新檔 `js/core/selectops.js`。
> 對照表寫「邊迴圈 ❌ 走不動 —— 它要四價頂點，**而方塊與圓柱的頂點是 3 價**」。
> **那句話只量過方塊與圓柱，卻被推到全部。**
**球有 372 個四價點，只有兩極不是** —— 實測一條經線邊走出**整條經線**（32 條），
而**球正好是瓣片展開的對象**。→ **「瓣片展開 A 要點 384 條邊」解掉了。**
⚠ 連帶：「選一圈抓不抓得到經線」的答案是**抓不到**（邊環繞著球跑）。
⭐ **表上寫 ❌ 的，要看它是拿什麼量出來的。**
🔴 動工才查到、沒查會白做一輪：**這一版的 three.js 渲染器一次都沒呼叫
`gl.lineWidth()`（grep 0 次）** —— `linewidth` 永遠不生效，邊要變粗只能畫細圓柱。
🔴 `HOVER_FACE_MAX ＝ 3500` **是量出來的**（0.52 µs／面，預算 2ms），
⛔ 不是又一個 `VERT_DOTS_MAX` 那種猜的數字。



**七、✅ 刀具三項輔助完成，桌機十項 kang 全部實測通過**（一筆畫／雙擊閉合／吸中點）。
測試 **1842 → 1890 項**。⏳ **只剩平板三項待驗**（他手邊沒平板）。
🔴 **實測抓到一個 bug 並修掉：右鍵拖會同時轉視角又畫線**（`a15b5dd`）——
病因是四個 `pointer*` 監聽**不分是哪一顆鍵**。
⭐ **教訓升格：換掉一個既有手勢，就一定要開始分「哪一顆鍵」**，
而且**鍵要在按下去的當下記起來**（＝ `TransformControls.axis` 那條，2026-08-25）。
新檔 `js/core/stroke.js`（純函式，測得到）——「一串表面點 → 一串切點」。
⭐ **主斷言是結構保證**：產出的切點串，**每相鄰兩個必定同屬一個面**
（＝ `connectVertsPath()` 的規則），所以算得出來就一定切得下去。
沙箱驗過六種劃法，**體積、表面積、χ 全部精確不變**。
🔴 kang 拍板三件：**吸中點給兩條路**（工具列開關 ＋ `Shift`，⚠ 平板沒有 `Shift`）、
**起終點吸到「所在那個面」的邊**（⛔ 不是全網格最近的邊）、
**快點兩下＝閉合，慢慢再點一次＝維持原本的取消**（逐點按的行為一格都沒變）。
⚠ **「輕點微調」那個詞改掉了** —— kang 連續兩次把「混用」讀成
「切好之後去移動切線位置」。**已放下的點只能取消，不能拉。**
⭐ **他讀到的意思才算數，不是我寫的時候想的意思。**

**八、🔴 殘骸清乾淨了，而且是四個不是三個。**
`#knifeLine`、`planeFromTwoRays()`（連 14 項測試）、
**`screenRay()` ← 第四個，待辦裡沒記，是 grep 才發現的**，三個刪掉；
`planeCrossSegments()` **接上「切一刀」的即時預覽**（打數字當下就看得到會切在哪）。
⭐ **殘骸要用 grep 找，⛔ 不要照清單推。**
⚠ 順手修掉一則說謊的註解（`select.js` 寫著「刀具預覽要跟著游標走」，
而底下只有框選）。

**九、⚠ 測試項數第一次「減少」過**：實際走的是 **1842 → 1828 → 1890**
（中間刪掉 14 項）。日誌只記端點，判準已補進「文件分層」那一節 ——
**看到新增數對不上，先想是不是有東西被刪了，⛔ 不要以為是漏記。**



**★、🔴🔴 立了一條最高紀律：推論不是權威事實，程式才是唯一權威。**
kang 2026-08-26 明確要求，**ERP 與系統整合兩個專案都把這條列為最高紀律**，
措辭沿用他們的。**理由是他自己講的：「才不會做白工。」**
含**權威層級**（官方文件／一手實查／讀原始碼 ＞ 常識）、**「這是我自己開檔看到的，
還是讀來的？」那一問**、**事實要標【實證·出處】或【推論】**、
🔴 **以及 kang 的權利：只要我講到行號或函式名，他可以直接問「這幾行你打開過嗎？」**
⚠ **立條當天我就違反兩次**（刀具那兩則），兩次都是他連問三次才逼我回頭讀程式與官方手冊。
全文見「鐵律 → 一、動手之前」。

**〇、⛔ 收回一個提案，而且收回的理由比提案重要。**
我本來要把 `規格\建模器-點線面編輯.md`（2352 行 / 128KB）拆成六個檔，
**理由純粹是「太大」**。去讀了 `D:\彩藝通系統整合\整理方法.md` 之後**收回** ——
那份方法明文寫著「⛔ 不要看大小，看六個徵兆，中三個以上才動手」，
而實際量下來**只中 0～1 個**（地圖對得上、0 斷鏈、58 個識別碼全搜得到）。
→ **改成給它補目次**（32 節，Grep 節名跳過去），檔案一行都沒拆。
⭐ 判準已抄進「文件分層 → 什麼時候該整理」，**權威版在那個專案，對不上以那份為準**。

**一、補掉一個結構缺口：實測「通過」沒有回寫的觸發點。**
六張清單 kang 早就一項一項說過 OK，日誌卻全掛著「還沒實測」——
**不是他沒講，是「OK」不產生任何動作**（沒有改檔、沒有差異、沒有 commit），
所以整條回寫鏈一次都沒被觸發。已補：**工作流程第 6 步**、
**協作規則「實測結果的回寫」**、**開場先 `Grep 待實測`**。
⭐ 修法跟 `sel.active` 那次同一招：**把觸發點搬到「每次必定發生」的那件事上。**

**二、日誌三天就長回 2806 行的老樣子，已經瘦回來。**
「開新對話時先讀這段」原本 1113 行（41%），現在 **329 行** ——
8/25 那十三輪的**經過**逐字搬進 `HISTORY.md`（機械斷言：903 行新增、
舊內容 1 行未動），日誌只留「以後動它要知道的」。
🔴 **病因是 `HISTORY.md` 最後一筆停在 8/24，十三輪一筆都沒搬過去。**

**三、四條埋在功能底下的教訓升格進「鐵律」**（坑第 33 條：
教訓寫在別的功能底下就等於沒寫）：沙箱說可以但 kang 說不行先看介面／
「似乎」「有時候」是狀態依賴／舊訊息會過期／同檔案的寫法不能照抄。
⭐ 動 `main.js` 那道「被呼叫但不存在的函式」檢查也進了協作規則。

✅ 六張實測清單（多點連接／面上加線／凹面穿邊界／邊上加點／顯示點／分離）
**全部通過**；✅ 刀具三項輔助的**桌機十項也全過**；
✅ 選取那一組第一批**十項也全過**（hover ／ 選一條線 ／ 編輯模式框選）；
✅ 狀態列「三角形」**四項全過**（第 5 項作廢，kang 拍板維持現狀）；
⏳ **待實測一批**：刀具的**平板三項**（kang 手邊沒平板）；
⏳ 量測第 2、4 步還沒做）

---

## 三批實測清單全過（2026-08-27 從 `PROJECT_LOG.md`「開新對話時先讀這段」搬入）

> **這一節是流水帳，⛔ 不是規則。** 三批清單都已由 kang 實測全過，
> 逐字搬進來當對照，**一字未改**。
>
> 🔴 **其中唯一「以後動手還會用到」的那一條，權威版留在 `PROJECT_LOG.md`**：
> 「**只要物件有板厚，破洞就會被自動加厚救回來 → 那顆「指出來」不會出現**」。
> 兩邊對不上時以 `PROJECT_LOG.md` 為準。

- ✅ **破洞在哪裡（3D 列印面板的「指出來」）：7 項 kang 全部實測通過（2026-08-27）**
  〔資料層測試 2034 → **2048 項**〕

  ⭐ **他的實測自己吵出一組對得起來的數字**（鐵律三的現行示範）：
  開口圓柱 seg32 按「指出來」→ **上下兩圈紅線**，接著按 `補洞` →

  | | 補洞前 | 補洞後 |
  |---|---|---|
  | 三角形 | **64** | **124** |
  | 尤拉數 χ | **0** | **2** |
  | 封閉 | 否 | 是 |

  **64 ＋ 60 ＝ 124** —— 兩個洞各是 32 邊形，補起來每個 30 個三角形
  （32−2），兩個剛好 60。**χ 從 0 變 2** 正是「管子 → 封閉實體」。
  ⭐ 而**紅線在補洞之後自己消失了** ＝ `markGeomDirty()` 那條清除路徑成立。

  🔴🔴 **⛔ 先看這一條，否則會以為按鈕壞掉了（kang 2026-08-27 實際卡住）：**

  > **只要物件有板厚，破洞就會被 `shell()` 自動加厚救回來
  > → 那則紅字不出現 → 「指出來」也不出現。**

  ⚠ **而板厚欄位只有「種類 ＝ 板件 sheet」時才看得到**，
  但那個值（`io.js` 預設 **0.2**）**在實體 solid 上照樣生效** ——
  **看不到、改不了，卻在影響結果**。
  → **要測「指出來」，得先把種類切成 `板件 sheet`、板厚打 `0`。**

  ⭐ **kang 2026-08-27 拍板：「不算問題，就這樣」** ——
  自動加厚正是想要的行為。⛔ 不要為了讓按鈕更常出現去動它。
  ⚠ 但他實測到的**代價要記著**：同一個挖了洞的方塊，
  **板厚 0.2 → 體積 2,280,008 mm³（空心盒）；板厚 0 → 72,000,000 mm³**，
  **差 30 倍**，而卡片上只有一行小字「已依板厚自動加厚」。

  | # | 做什麼 | 應該看到 | |
  |---|---|---|---|
  | 1 | 種類切 `板件 sheet`、板厚 `0` 的**開放物件** → 按工具列「**3D 列印**」 | 卡片紅字「**不是封閉的（有 N 條邊界邊）**」，**那行紅字後面**多一顆「**指出來**」 | ✅ |
  | 2 | 🔴 按「**指出來**」 | **視窗自己關掉**，破洞四周變成**紅線** | ✅ |
  | 3 | 方塊 → **刪除面**（挖一個洞）→ 板厚 0 → 「3D 列印」→「指出來」 | 那個洞的**四周一圈紅線** | ✅ |
  | 4 | 🔴 接著按「**補洞**」 | **紅線要自己消失**（⛔ 補好了紅線還在就是說謊） | ✅ **通過** |
  | 5 | 🔴 圓柱**取消勾選兩端封口**、板厚 0 →「指出來」 | **上下兩圈紅線**，講「**2 個**破洞（共 64 條邊，最大那個洞 32 條）」 | ✅ **通過** |
  | 6 | 同一個物件**板厚給 0.2** → 按「3D 列印」 | 卡片說「已依板厚自動加厚」「可以列印」，**⛔ 沒有紅字也沒有「指出來」** | ✅ |
  | 7 | 標了紅線之後，**再按一次「3D 列印」** | 上一次的紅線**清掉了** | ✅ |

  🔴 **清單第一版寫錯了，⛔ 留著當教訓**：原本第 1 項只寫「新增一個平板 →
  按 3D 列印」，**沒說要先把板厚設 0** —— 而平板預設就有板厚 0.2，
  **照著做永遠看不到那顆按鈕**。
  ⚠ 那是「**使用者做得出這個輸入嗎**」那條又破一次（鐵律五）：
  我只問了「他驗得出這個結果嗎」。

- ✅ **`變成正圓`：8 項 kang 全部實測通過（2026-08-27）**
  〔資料層測試 2012 → **2034 項**，含兩條交叉驗證；**介面這 8 項是 kang 驗的**〕

  **清單留著當對照。先按「拉點線面」進編輯模式。**

  | # | 做什麼 | 應該看到 | |
  |---|---|---|---|
  | 1 | 新增**圓柱**（分段 32）→ 選**頂面那一片** | 旁邊「變成正圓」的**半徑欄自動出現 25.000** | ✅ |
  | 2 | 直接按 `變成正圓` | 提示說「**這一圈本來就已經是圓了**」，⛔ 形狀不變、⛔ 不記 Undo | ✅ |
  | 3 | 🔴 半徑欄**改成 40** → 按 `變成正圓` | 頂面**變大成半徑 40 的圓**，底面不動 → 變成一個喇叭形 | ✅ |
  | 4 | 🔴 選頂面 → 用箭頭**把一個點拉歪** → 選頂面 → 按 `變成正圓` | 歪掉的點被**推回圓上**，提示講出「原本半徑最多差 ◯◯ cm」 | ✅ |
  | 5 | 🔴 半徑欄**自己打一個數字之後不要動選取** | 那個數字**⛔ 不可以被自動蓋掉** | ✅ |
  | 6 | 選**方塊的一個面**（4 個角）→ `變成正圓` | 長方形的面**變成正方形**（四個角離中心一樣遠） | ✅ |
  | 7 | 選**一個點**或**一條邊** | 按鈕是**灰的**（至少要 3 個點） | ✅ |
  | 8 | 🔴 **版面**：工具列多了 `變成正圓` ＋半徑欄 | 有沒有被擠掉 | ✅ **桌機通過** |

  🔴 **第 8 項只涵蓋桌機** —— 平板版面仍然沒驗（跟刀具那三項卡在同一件事）。

- ✅ **選轉角 ＋ 選相似：11 項 kang 全部實測通過（2026-08-27）**
  〔資料層測試 1973 → **2012 項**；**介面這 11 項是 kang 驗的** ——
  　⚠ 沙箱一項都驗不了，見「鐵律 → 一、動手之前」那則〕

  **清單留著當對照。先按「拉點線面」進編輯模式，選到物件再按。**

  | # | 做什麼 | 應該看到 | |
  |---|---|---|---|
  | 1 | 方塊 → 按 `選轉角`（度數 30） | **12 條邊亮起來**，提示說「已選起 12 條轉角」 | ✅ |
  | 2 | 🔴 同一個方塊，度數改成 **90** → 再按一次 | **還是 12 條**。⛔ 不是 0 條 —— 方塊的角正好 90.0000 度，程式刻意讓「等於門檻」算數 | ✅ **通過** |
  | 3 | 度數改成 **91** → 再按 | 紅字提示「沒有一條邊折得到 91 度」，⛔ 不可以安靜沒反應 | ✅ |
  | 4 | 🔴 新增一個**平板** → 按 `選轉角` | 紅字要講出**「全部是開口邊緣（4 條）」**。⛔ 只說「沒有選到」就是壞的 —— 那會讓人一直調度數，而調到 1 度也永遠不會有反應 | ✅ **通過** |
  | 5 | 圓柱（分段 32）→ 按 `選轉角`（30） | **64 條**：上下兩個蓋子邊各 32 條。**側面那 32 條不會亮**（夾角只有 11.25 度） | ✅ |
  | 6 | 方塊 → 選**一個面** → 判準挑「同邊數」→ 按 `選相似` | **6 個面全亮** | ✅ |
  | 7 | 同上，判準改「同面積」 | **2 個面**（它自己 ＋ 對面那一片） | ✅ 〔被第 10 項那個更嚴格的樣本涵蓋〕 |
  | 8 | 方塊 → 選**一條長邊** → 判準「同邊長」 | **4 條** | ✅ |
  | 9 | 🔴 方塊 → 選**一條邊** → 判準挑「**同面積**」→ 按 | 紅字提示「要先選一個【面】當範本」。⛔ 安靜地沒反應是壞的 | ✅ **通過** |
  | 10 | 球（**經線 12、緯線 32**）→ 選一個**四邊形**的面 → 「同邊數」 | **360 個面**（＝ 12 × 30）。⭐ 畫面上是「**整顆球除了頭頂那一圈與底部那一圈以外全部亮起來**」—— 那兩圈是三角形（12＋12 ＝ 24 個），360 ＋ 24 ＝ 384 ＝ 總面數 | ✅ **通過（用更強的變體，見下）** |
  | 11 | 🔴 **版面**：工具列這一輪多了 `選轉角` ＋度數欄、`選相似` ＋判準下拉 | 有沒有被擠掉或切掉、整體高度有沒有吃掉太多畫面 | ✅ **桌機通過** |

  ⭐ **第 10 項 kang 換了一個更強的驗法（2026-08-27，截圖）**，
  而且**沙箱重跑完全吻合**：

  > 球**經線 32、緯線 16** → 選赤道帶一個面 → **「同面積」** →
  > **64 個面、共 96 個頂點**

  | 數字 | 怎麼來的 |
  |---|---|
  | **64 個面** | 一圈 32 個 × **南北對稱兩圈** |
  | **96 個頂點** | 32 × **3 層**（兩圈四邊形要三層頂點）|

  🔴 **那兩個數字自己就把答案驗掉了**：極點那兩圈三角形**也剛好是 64 個面**，
  但頂點會是 **66** 不是 96 —— **所以 96 證明了亮起來的是赤道帶，不是極點。**
  〔鐵律三「**讓兩個數字互相對得起來，錯誤才會自己現形**」的現行示範，
  　而這次那兩個數字是**結構分析面板自己印出來的**，不是測試裡的〕

  ⭐ **而且他驗的比清單上那一項值錢**：清單第 10 項是「同**邊數**」——
  整數比較，**用不到容許值**；他驗的「同**面積**」才真的在用
  **0.01 cm²** 那個容許值（坑第 25、26 條那條規則）。

  🔴 **第 11 項只涵蓋桌機** —— 平板版面仍然沒驗（跟刀具那三項卡在同一件事）。
  ⚠ **他的截圖照出工具列已經換到第三行**（`.grp` 的 `flex-wrap` 在正常運作，
  按鈕全都還在、按得到），跟「工具列的優化延後」那一則記的現況一致。

  ⭐ **值得記：這一輪的「錯了也看不出來」四項是先標出來、他照著挑的。**
  〔規則「🔴 標記的項目要追問一次」原本是**我要去問**；
  　這一次他直接回報編號 —— **標記真的在做事，⛔ 不是裝飾**〕


---

## 從日誌檔頭被擠出去的「翻掉了什麼」

> 🔴 **檔頭那一欄是覆蓋型、上限 5 條**（見 `PROJECT_LOG.md`
> 「文件分層 → 檔頭那一欄是覆蓋型」）。被擠出去的逐字搬到這裡，
> ⛔ **不是刪掉**。要查就 Grep 這一節。

### 球 segW12 的「相鄰經向夾角正好 30 度」是錯的（2026-08-27 擠出）

> 🔴 **「球 segW12 的相鄰經向夾角正好 30 度」是錯的** —— 實測 **1.507 ～ 29.965 度**，隨緯度變。30 度是**經度間隔**，不是**二面角**。〔⚠ 那句話是 AI 寫在自己剛寫的註解裡當理由用的〕

⭐ **兩件事值得記**：

1. **30 度那個數字本身沒錯，錯在它回答的是另一個問題** ——
   `360 ÷ 12 = 30` 是**經線之間的經度間隔**；而 `選轉角` 問的是
   **二面角**（兩個面折起來的角度），那個隨緯度變，赤道附近最大、極點附近趨近 0。
   〔鐵律三「把內部的數字放上介面之前，先問這個數字的單位是什麼」的同一類：
   　**這次不是單位錯，是「這個數字在回答哪個問題」錯了**〕

2. 🔴 **那句話是 AI 寫在自己剛寫的註解裡，當理由用的** ——
   ⛔ 沒有任何外部來源可以對照，**自己寫的東西後來被自己當成依據**。
   〔「推論不是權威事實」的一種特別難抓的長相：
   　推論被寫進註解之後，**看起來就跟實證一樣**〕

### 日誌檔頭的輪次紀錄（2026-08-27 改成覆蓋型時搬入）

> 原文見本檔前面「日誌檔頭的輪次紀錄」那一節（2026-08-27 較早搬入的 221 行）。

### 「講數量與形狀」那條規則破了第五次（2026-08-27 擠出）

> **「講數量與形狀」那條規則破了第五次** —— 實測清單寫「360 個」**漏掉單位**，kang 當場反問「360 個甚麼?邊?」。⛔ 數量沒有單位就不是數量。

⭐ **正解不只是補一個「面」字，是補上形狀**：
「**整顆球除了頭頂那一圈與底部那一圈以外全部亮起來**」——
**360 這個數字他根本沒辦法數**，而那句話一眼就驗得出來。

🔴 **規則的權威版在 `PROJECT_LOG.md`「協作規則 → 開清單時，名字一律照介面上的字」**
（那裡有五次實例的完整對照表）。本節只留這一次被擠出檔頭的原文。

---

## 表面上一條一條的斜紋：陰影的深度比對沒有容許值（2026-08-27）

**kang 回報**：「模型的面的顏色..有時候會出現一條一條的紋路...這是正常的嗎?
是不影響操作啦..」

### 🔴 他的三個事實把病因直接指出來了

第一輪我給了三個推論：**兩個物件疊在一起／壓在地板網格上／相機 near-far
精度不夠**，並請他做一個「把物件移開」的驗法。

他回：

> **「一直都在..放一個物件也會...但方塊沒發生過..剛剛那是平板..球體也發生過」**

⭐ **這三句一次翻掉全部三個推論**：

| 他說的 | 砍掉了什麼 |
|---|---|
| **「放一個物件也會」** | ⛔ 兩個物件重疊 —— **那本來是我排第一的猜測** |
| **「方塊沒發生過」** | ⛔ 相機精度（那會是全域的，方塊也該中） |
| **「平板、球體會」** | → 指向「**跟形狀有關**」，而且是**面的角度** |

### 病因（算式擺出來就很清楚）

【實證】`scene.js`：影子貼圖 `2048×2048`，涵蓋範圍 `s*2 = 1000 cm`
→ **一個影子像素約 0.49 cm**。而 `sun.shadow.bias` **完全沒設**（預設 0）。

> **比對深度時只要誤差超過 0.49 cm，表面就會一格一格地把自己判成
> 「被自己擋住」** —— 規律的細斜紋（shadow acne）。

🔴 **病因是「⛔ 沒設容許值」，⛔ 不是陰影開錯了。**

⚠ **這是坑第 25／26 條那條規則的第三個現場**：
「浮點數的判斷一律要有容許值，而且要挑一個**有物理意義的量**去比」——
這裡的物理量就是「**一個影子像素在世界裡有多大**」＝ 0.49 cm。

### 為什麼方塊不會、球與平板會

**面跟陽光越斜，同一個影子像素跨越的深度差就越大。**

| 形狀 | 為什麼 |
|---|---|
| **方塊** | 六個面都是軸對齊的，對光的角度單純 → 誤差還在 0.49 cm 內 |
| **球體** | 曲面上**一定有一整圈「幾乎與陽光平行」的區域** → 那裡最嚴重 |
| **平板** | 面積大，同一片面跨過很多個影子像素 |

### 改了什麼（兩個參數，⛔ 不要只設一個）

```
sun.shadow.bias       = -0.0004   （貼圖空間，無單位）
sun.shadow.normalBias = 0.6       （世界單位 ＝ cm）
```

| | 做什麼 | 太大的副作用 |
|---|---|---|
| `bias` | 比對前把深度往內縮 —— **全域固定量，治不了斜面** | 影子脫離物體，像浮在空中（peter-panning） |
| `normalBias` | 取樣點沿**面的法向**推開 —— **跟著法向走**，正好對上「越斜越嚴重」 | 薄板的影子會漏光 |

⭐ **曲面靠 `normalBias` 才治得好。**

### ⚠ 這兩個數字沙箱驗不了

沙箱連畫面都跑不起來，所以紅線寫在**程式註解**裡（程式是活庫）：
「要調就照 kang 實際看到的結果調，⛔ 不要為想像中的數字辯護」，
並附調整順序（先只動 `normalBias` 0.3～0.8，還有紋路再碰 `bias`）。
⭐ 判準沿用 `VERT_DOTS_MAX` 那一則的寫法 —— **那個數字也是猜的**。

✅ **kang 2026-08-27 實測通過**：紋路消失，且⛔ 沒有出現影子脫離物體
或薄板漏光（兩個副作用都問過了）。

### ⭐ 這一輪的方法論

🔴 **「一直都在／放一個也會／方塊不會／球會」—— 使用者的四個對照，
比我三輪推論值錢。**
⚠ 我第一輪把三個猜測並排丟給他，⛔ 那不是壞事（他一句話就篩掉了兩個），
**但真正有效的是我附上的「一秒鐘分得出來的驗法」** ——
⭐ 他沒照那個驗法做，而是**直接給了更好的對照組**（換形狀，不是換位置）。
