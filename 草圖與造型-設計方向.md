# 草圖與造型 — 設計方向

> **這份回答**：造型的入口現在長什麼樣、缺的是什麼、**四題的答案（kang 2026-09-02 拍板）**。
> **這份不回答**：怎麼實作（那是規格檔的事）、做到哪了（`STATUS.md`）。
> **識別碼（給 Grep 用）**：草圖／輪廓／造型入口／旋轉成形／螺旋／掃掠／放樣／丙-1.5
>
> ✅ **四題 2026-09-02 全部討論完並拍板 → 見「四題的答案」那一節。**
> ⚠ **但這仍然⛔ 不是執行計畫** —— 計畫要照那四個答案另外列，動工前還要畫互動圖。

---

## 這份文件是怎麼來的（2026-09-02）

做完 `旋轉成形` 第 1 階段之後，kang 一句話問到根本：

> **「你只是為了達成旋轉成形…並不是真正的計畫」**

⭐ **他是對的。** 那一整條路是這樣走的：

```
旋轉成形要一條線 → 去鋼筆挖 → 挖出來有面（多一個沒用的物件）
→ 再補一個「輪廓物件」 → 中心線看不到 → 再拿參考線補
```

🔴 **每一步都是為了讓那顆按鈕能動，⛔ 沒有一步是問「這個建模器缺什麼」。**

而在那之前他還問了兩句，兩句都比答案重要：

| 他問的 | 事實（查證過） |
|---|---|
| 「這一個有形狀的線…到底只有線還是有包括面? **鋼筆不可能只畫線**」 | ✅ **對的**。鋼筆按 `不封口` → 生出**一片板**或**一道牆**；沒按 → 頭尾接起來**擠成一塊實心的**。⇒ **鋼筆做不出「只有一條線」** |
| 「照你這樣做下去…**螺旋一定會出問題**」 | ✅ **對的**。彈簧是**一個封閉的小斷面**繞著轉，⇒ 螺旋的輪廓**必須封閉**；而鋼筆封起來就直接擠成柱體，⇒ 連「一條封閉的線」都拿不到 |

---

## 造型的入口現在有三個（查證過的現況）

| 入口 | 它產出什麼 | 定位 |
|---|---|---|
| **新增**（下拉 9 種）| 方塊、圓柱、錐體、球、角柱、管、圓角方塊、平板、折板 | 參數化的**基本形狀** |
| **鋼筆**（左側直條）| 板／牆／實心塊 | **畫完直接生出一個東西** |
| **匯入線稿**（SVG）| 擠出件 | 外面畫好的輪廓 → 直接擠出 |

🔴 **三個都是「一按就得到一個模型」。⛔ 沒有一個產出「純粹的線」。**

⚠ 而 `參考線` 是純粹的線，**但它只有直線**（`GUIDE_AXES` 是 x／y／z），
⛔ 畫不出碗的側面。

---

## 缺的是什麼

> **一組線，本身⛔ 不是模型，只是造型的來源。**

四個功能都要吃它，而且要求各不相同：

| 功能 | 需要草圖具備什麼 |
|---|---|
| **旋轉成形**（幾何已完成）| 一條**開放**的線 ＋ 一根中心線 |
| **螺旋** | 一條**封閉**的線（斷面）＋ 中心線 ＋ 每圈上升多少 |
| **掃掠** | **兩條**：一條斷面 ＋ 一條路徑 |
| **放樣** | **好幾條**斷面，照順序過渡 |

⇒ **⛔ 這四個沒辦法各自去別的物件身上挖線。** 需要一個共用的東西。

---

## ✅ 四題的答案（kang 2026-09-02 拍板）

> 🔴 **底下那一節「還沒決定的問題」是討論用的原始選項，⛔ 已經全部答完。**
> 　 留著是為了**看得到被否決的那些**〔規則：被否決的方向要留著〕。

| 題 | 答案 |
|---|---|
| ① 那條線是什麼 | **丙-1.5** —— 線**留在 `src` 裡**，**改了東西跟著變** |
| ② 輪廓來源 | **鋼筆 ＋ 匯入線稿（SVG）**。⛔ **模型上選的一串邊不做** |
| ③ 躺著還是站著 | **旋轉成形多一個「立起來」的選項**，⛔ 不動鋼筆、⛔ 不動 SVG |
| ④ 中心線 | **參考線優先**（**綁著**、可拖、即時連動），沒有就打數字；**刪參考線時自動脫鉤** |

### ① 為什麼是丙-1.5

kang 的話：「**丙感覺比較治本**」「同意丙-1.5…因為能吃匯入 AI 繪圖軟體的 SVG 線稿蠻重要的」。

⭐ **丙的機制這個專案早就有了，⛔ 不是從零開始** ——【已查證 · `io.js` 檔頭】
`src` 是一棵樹，而且已經有三種會「記住自己從哪來」的東西：
`{type:'bool', op, items}`／`{type:'array', mode, child}`／`{type:'pen', paths, …}`。
⇒ 改參數 → 重新生成，**就是你現在改方塊長寬高走的那條路**。

🔴 **⛔ 這跟被否決的那個補丁不一樣**：被否決的是「**去鋼筆背後挖一條線出來，另外生一個物件**」
（挖完那片沒用的板還留著 ＝ 兩個東西）。
丙-1.5 是「**它本來就能生出碗**」—— ⛔ 沒有挖、⛔ 沒有多餘的板、參數留著。

### ② 為什麼「模型上選的一串邊」⛔ 不做

🔴 **它本質上就做不到丙** —— 網格上的邊**⛔ 沒有參數**，那是烘好的東西，改了不會連動。

⚠ **而 SVG 有一件事要先解**【已查證 · `js/sketch/profile.js:114`】：
```
const open = shapes.filter(s => !s.closed);   ← 開放路徑被挑掉了
```
✅ **解析那一半是現成的**（`svgPath.js` 認得 `closed:false`，還會把端點標成轉角），
⇒ 要做的只是**讓開放路徑活下來**給旋轉成形用，
⚠ **⛔ 不能破壞現有的擠出**（開放路徑對擠出仍然無效，那是對的）。

### ③ 為什麼「立起來」放在旋轉成形那一端

🔴 **kang 一句話問到位**：「**鋼筆不也是躺著?**」——
⇒ 這**⛔ 不是 SVG 的問題，是這個建模器所有的輪廓都躺在地板上**。

⭐ 而「立起來」放在**旋轉成形的出口**，就**只改一個地方**；
放在輪廓那一端要改**兩套座標系**（鋼筆一套、SVG 一套）。
⭐ 形狀跟鋼筆的 `往上長` 一樣：**同一條線，換一個生成方向**。

⚠ **真正「治本」的是讓輪廓能畫在垂直的面上**（從前面／側面畫），
⛔ 但那會動到鋼筆的地基（它三個階段的行為都建立在「畫在地板上」）。
→ ⏭ **排到後面，⛔ 不是否決。**

### ④ 中心線：綁著參考線，刪的時候脫鉤

🔴 **kang 指出參考線早就解掉這件事了**：「參考線也可以直接放線…也可以移動…移動時數字也會連動」
【已查證 · `規格\建模器-參考線.md`：「**點到那條線 → 變亮 → 直接拖（位置欄跟著跑）**；
或在位置欄打精確數字（線跟著跑）」「**拖與打數字接的是同一條線、同一個欄位**」】

⇒ **軸交給參考線 ＝ 軸直接繼承「看得到 ＋ 可拖 ＋ 數字連動」三件事。**
⭐ 而且掉出一個效果：**拖那條參考線 → 碗當場變粗變細**。

⚠ **參考線是一個【平面】，⛔ 不是一條線**【已查證 · `scene.js` `_guideSegments()`：
一個平面畫成 600cm 的方框 ＋ 中央十字】。
⇒ 一條參考線定不出軸，**但它跟地板的交線就是軸** —— 而那正好對上「軸要兩個座標」。

**刪參考線時：自動脫鉤**（把「跟著那條線」換成「記住它當時的位置」）。
⭐ **形狀跟 `轉成可編輯網格` 一樣**：從「跟著參數走」變成「烘成固定的」，
**⛔ 不會壞、⛔ 不會卡，只是回不去**。訊息要講出「有 N 個物件受影響」。

🔴 **對參考線現有功能的影響：只有一處** ——
`刪這條` 按下去時要先問「有沒有人在用」。
加線、拖、打數字、`吸到參考線`、顯示／隱藏 **全部不動**。

### ⭐ 附帶解掉的一個補丁

要轉出**封閉的實體**，輪廓端點得**剛好**落在中心線上（容許值 1 微米），
而手畫不可能剛好 —— 所以第 1 階段加了「**自動填最小值**」去補。
⇒ **中心線變成一條看得見、又吸得上去的參考線之後**，
畫的時候端點就真的落在上面了，**那個補丁從「必要」降級成「保險」**。

---

## 🔴 還沒決定的問題（⛔ 原始選項，✅ 上面已全部答完 —— 留著是為了看得到被否決的那些）

### ① 草圖畫在哪個平面？

現在鋼筆**只畫得在地板上**【已查證 · `io.js` `penPathToWorld()`：形狀的 (x,y) 對到世界的 (x,z)】。
⚠ 而碗的輪廓是**側面剖面** —— 要在腦子裡轉 90 度才畫得對。

- 甲：**維持只能在地板畫**，轉出來的東西是躺著的，再自己轉正
- 乙：草圖可以選一個平面（前／側／上）
- 丙：草圖可以畫在**模型的任何一個面**上

### ② 鋼筆要不要重新定位？

現在它是**建模工具**（畫完直接生出東西）。若草圖成立，它比較像**畫草圖的工具**，
而「生成板／牆／實心塊」變成草圖的**一種用法**。

- 甲：鋼筆重新定位成畫草圖的工具（⚠ 動到現有行為）
- 乙：鋼筆維持原樣，**另外**有一個畫草圖的入口（⚠ 兩個畫線工具，定位會模糊）
- 丙：鋼筆多一個「只留線」的收工方式（⚠ 補丁，但最便宜）

### ③ 草圖是不是一種「物件」？

- 甲：**照 `參考線` 的做法**（另一個清單，⛔ 不進 `objects`）
  ⭐ 好處：**83 個 `.mesh()` 呼叫點一個都不用碰**，
  　 而 `STATUS.md` 記著的那個隱患（3D 列印會誤報不封閉）**自動消失**
  ⚠ 代價：⛔ 不能用物件那一套（移動／旋轉／複製／鏡射）
- 乙：新增一種物件種類（⚠ 83 個呼叫點要一一確認）

### ④ 中心線怎麼指定？

kang 2026-09-02 的原話：現在是**盲畫** —— 畫的時候看不到中心線在哪。

- 甲：用 `參考線` 當中心線（⭐ 畫的時候看得到，鋼筆還有 `吸到參考線`）
- 乙：草圖自己帶一根中心線
- 丙：打兩個座標（⚠ 現在這版，已經證明不好用）

---

## ⛔ 明確排除的（⛔ 不要重提）

- **自由曲面**（車殼、船殼、人臉那種兩個方向同時彎的）——
  那要細分曲面或雕塑，**而那正是對照表上標 ➖ 的那一堆**
  〔kang 2026-08-24 定調：「不做跟建模無關的」〕

---

## 目前的處置（2026-09-02）

- **旋轉成形的幾何**（`js/build/revolve.js` 249 行 ＋ 22 項測試）**留著跑**
- **它的介面收起來了**（kang 決定，⛔ 不是否決）——
  接回來的方式寫在 `規格\建模器-旋轉成形.md`「介面收起來了」那一節
- ⏳ **那十項實測取消**，kang 不用測

---

## 🔴 接下來要做什麼（⚠ 這是【順序】，⛔ 還不是執行計畫）

> 四題答完了，但**⛔ 還沒有計畫** —— 計畫要照那四個答案另外列，
> 而且照協作規則，**動工前要先畫互動圖確認理解、再問拍板**。

**要列計畫時，這幾件是已經定死的（⛔ 不必再問）：**

1. 新增一種 `src`：**旋轉成形是一種【參數物件】**，⛔ 不是烘好的網格
   （現在第 1 階段做的是 `{type:'mesh'}`，**那一半要換掉**）
2. 輪廓來源**兩個**：鋼筆的 `paths`、SVG 的輪廓
3. **SVG 要讓開放路徑活下來**（`profile.js:114`），⛔ 不能破壞擠出
4. 旋轉成形多一個 **「立起來」** 的選項
5. 中心線**綁參考線**（沒有就打數字），**刪參考線時脫鉤 ＋ 講出受影響幾個**
6. ⭐ **`revolve.js` 那 249 行幾何一行都不用改** —— 它只吃一串點

**⚠ 還沒答、要動工前才問的：**

- **螺旋**（`rise`）要不要同一輪一起做？（⭐ 它只是多一個欄位）
- 「立起來」要不要記在 `src` 裡（那樣改了會連動）還是只在建立時轉一次
- 刪參考線的訊息要長什麼樣

### ⏭ 排到後面的（⛔ 不是否決）

- **輪廓能畫在垂直的面上**（③ 的「治本」版）—— 會動到鋼筆的地基
- **線變成獨立的物件**（丙-2）—— 🔴 **要做掃掠／放樣時它才變成非有不可**
  （掃掠要**兩條**線、放樣要**好幾條**）
