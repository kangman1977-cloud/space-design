# ── 平常從網頁存出來的檔案，不需要進版控 ──
存檔/
輸出/
/*.json
/*.csv
/*.png
/*.jpg
/*.jpeg

# ── 版控工具產生的結果檔（給 AI 讀的，不需要進版控）──
版控工具/git-log-*.txt

# ── 暫時不用的東西 ──
XXX/

# ── 沙箱跑回歸測試時自動產生的墊片，不是專案的一部分 ──
node_modules/

# ── Python 自動產生的位元碼（版控工具/平板伺服器.py 一跑就會出現）──
#    2026-08-31 加：AI 跑 py_compile 驗語法時它跑出來，
#    是 1-看差異.bat 的「?? 全新檔」那一欄抓到的。
__pycache__/
*.pyc

# ── 系統與編輯器產生的雜檔 ──
Thumbs.db
desktop.ini
.DS_Store
~$*
*.tmp
*.bak
