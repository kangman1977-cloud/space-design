# 編輯功能設計方向

最後更新：2026-08-23　V1.0

> **這份文件的主要讀者是 AI，不是人。**
> 跟 `PROJECT_LOG.md` 一樣寫「目前確定的狀態」，但多寫一樣東西：**理由**。
> 日誌只放結論，推導過程放這裡。

---

## 這份文件是什麼

`PROJECT_LOG.md` 在 2026-08-23 已經 2789 行。那個長度會讓 AI 讀到一半被截斷、
或是讀了前半就開始動手。日誌坑第 33 條說過同一件事的另一面：
**教訓寫在別的功能底下，就等於沒寫**——寫了但沒被讀到，效果一樣。

所以從這一份開始改成：

```
PROJECT_LOG.md   目前確定的狀態（結論、規格、踩過的坑）
專屬文件          某個大題目的推導過程、調查、被否決的方向
                 日誌在對應章節放指標與三句摘要
```

**動任何「編輯」相關的東西之前，先讀這一份。**

kang 的原話（2026-08-23）：

> 「不應該直接放進日誌...你應該特別寫一份專屬文件來記錄...
> 這樣才不會讓日誌肥大...造成你調查閱讀時的遺漏」

---

## 1. 程式定位：補上 Blender 那一半

**這是整份文件最重要的一句話：**

> **Pepakura 只做展開。它處理的那些模型（低多邊形鹿頭、盔甲道具），
> 全部是在 Blender、3ds Max 裡建好才丟進去的。
> 所以真正的門檻不是 Pepakura，是「得先去學 Blender」。**

這個程式要補的就是那一半：**建模 → 展開／分切／列印 → 半成品 → 人工加工**，
一條龍在同一個工具裡完成。

kang 的原話（2026-08-23）：

> 「Pepakura 能夠處理的模型...這些造型模型要如何做出來....
> 這就是我們程式編輯要做的事...當能做出這些造型...才能搭配我們的展開圖功能..
> 剖面分切功能..3D列印功能...產生半成品...才能人為加工」

### 由此推出的三條界線

| | |
|---|---|
| **建模是主體** | 匯入是輔助，不能取代它 |
| 🔴 **不做跟建模無關的** | **建模編輯的功能要做齊** —— 見底下那則澄清 |
| **不做雕塑** | 見第 5 節。⚠ 這條的邊界因為下面那則澄清變模糊了，見該處 |

> 🔴 **「不做 Blender 那種全能軟體」的正確讀法（kang 2026-08-24 澄清）**
>
> 這一條**被誤讀過，而且是被 AI 誤讀的**。原本寫成「只做 Blender 做得到但我們
> 天天要用的**那一小塊**」，於是 AI 拿它去質疑 kang「編輯能力對齊 Blender」
> 這個目標，說兩者衝突。**那不是衝突，是讀錯了。**
>
> kang 的原話：
>
> > 「我指的是不做 Blender 那種全能軟體...是因為 **Blender 很多功能都跟建模無關**...
> > 　**我們不需要無關的功能**..**我們需要的是 Blender 的建模編輯功能**」
>
> 所以判準是**「相不相關」，不是「多不多」**：
>
> | | |
> |---|---|
> | ✘ **不做** | 跟建模無關的：動畫、算圖、UV、材質、骨架、權重、細分曲面、幾何節點 |
> | ⭐ **要做齊** | **建模編輯的功能** |
>
> → 完整的逐項對照在 **`外部參考-Blender編輯.md` 第 10 節**（第一手，
> 　Blender 全部 85 支編輯操作 ＋ 13 支選取方式）。那張表上標 ➖ 的
> 　**正好就是 kang 說的「無關」那一堆**，標 ⭐ 的約 25 項**全部在範圍內**。
>
> ⚠ **這則澄清翻掉了兩項原本標「不做」的**，因為它們其實是建模功能：
> - **細分（Subdivide）**：原本寫「先不做」，理由是四種補法要問 kang ——
>   那是**順序問題不是範圍問題**，收回，重新排進待辦。
> - **平滑頂點／比例編輯**：確實是建模編輯，但當初以「雕塑類」否決（第 5 節）。
>   ❓ **邊界未定** —— 排到它們的時候要重新問 kang，不要照第 5 節直接擋掉。

> ⚠ **為什麼「匯入 STL／OBJ」不能排在前面**（2026-08-23 實際發生的誤判）
>
> AI 曾建議「先做匯入 STL，因為工程量最小、立刻解掉做不出來的限制」。
> kang 當場否決：「如果我可以匯入 STL／OBJ...我還要這一個程式做甚麼」。
>
> 那個建議的錯誤在於：它把「匯入 SVG 線稿」的成功經驗錯誤類比。
> **線稿是 kang 自己在 Illustrator 畫的產出，STL 是別人做好的模型。**
> 前者是把他的工作接進來，後者是承認模型在別處建——那我們就退化成
> 另一個 Pepakura 了。
>
> **匯入 STL／OBJ 仍然要做，但定位是「客戶給了檔案要拿來做」的附加功能，
> 不是主力，排序在編輯之後。**

---

## 2. 造型的形式：幾何化多面體

**幾何化不是妥協，是最適合板材的形式**——板材本來就是平的，
用平板做出來的東西自然就是平面拼的。

而且它有一個結構性的好處：

> **全部是平面 → 全部可展 → 展開圖那條路永遠走得通，而且是精確解。**

（攤平是剛體運動，平面攤平尺寸完全不變。）

### 跟剖面分切的分工

| | 幾何化 ＋ 展開 | 剖面分切疊層 |
|---|---|---|
| 成品 | **中空、輕** | 實心、重 |
| 材料 | 省 | 費 |
| 後製 | 接縫處理 | **要補土打磨** |
| 精確性 | 精確 | 精確（但有階梯感） |
| 適用 | 幾何化得出來的東西 | 幾何化不划算、或真的要實心 |

Pepakura 二十年來的主要用途就是低多邊形紙模／板模（鹿頭牆飾、盔甲道具），
而它的 `Separate All Faces` 功能官方註明是**給 styrene board（珍珠板）用的**。
這條路是驗證過的。

### 導圓角：讓幾何化不再硬邦邦

導圓角在多面體裡就是**把一條邊變成一圈小面**。

kang 的原話（2026-08-23）：

> 「有些造型會需要導圓角...因為可以讓模型在幾何化中更細緻」

**代價會直接反映在分片上，而且不同材料差很多：**

| 材料 | 那一圈小面 | 結果 |
|---|---|---|
| 珍珠板、發泡板、薄木板 | 銑 45 度 V 溝折起來 | **保持一片** |
| 壓克力、木板 | 折不起來，全部切開 | **片數暴增** |

同一個導了圓角的模型，珍珠板做是一片，壓克力做可能是十幾片。
這正是 `unfold/rules.js` 的材料規則在管的事，而**指定分片讓使用者可以覆蓋它的判斷**。

> **現況**：`roundBox`（圓角方塊）只能倒**方塊的四條垂直邊**，而且是參數體。
> 真正需要的是「**選任意一條邊 → 導成 N 段**」，不管那條邊在什麼形狀上。

---

## 3. 為什麼出口先做（這個順序是刻意的）

到 2026-08-22 為止，三條出口全部完成：**展開＋指定分片、剖面分切、STL**。

日誌那句「剖面分切做完之後**瓶頸換邊了**」不是意外，是把出口做完的結果：

```
出口沒做完時：做出複雜造型 → 沒辦法變成實體 → 那些造型是廢的
出口做完之後：任何封閉形狀都出得了圖 → 限制變成「建不建得出來」
```

**現在瓶頸就在入口。** 所以第 6 期（編輯）是第一優先，沒有繞路。

而 kang 早就在做這件事——2026-08-22 他先要求做「指定分片」與「剖面分切」，
兩者都不在原訂八期路線圖裡。理由到 2026-08-23 才講清楚：

> 造型一複雜，展開的片數就上去，**哪裡切開變成製造決定，不能交給程式亂猜**（→ 指定分片）
> 複雜到展開已經不划算 → 換一條路（→ 剖面分切）

**先確保「做出來的東西一定有辦法變成實體」，再擴大「能做出什麼」。**

---

## 4. 編輯的範圍：box modeling 那一套

要建出 Pepakura 那類模型（低多邊形鹿頭、盔甲），核心動作是這六個：

| 動作 | 用途 | 現況 |
|---|---|---|
| **選點／邊／面** | 一切的前提 | 一半已有（`seam.js`），但沒有點/邊/面過濾器 |
| **移動／旋轉／縮放選取的部分** | gizmo 現在只作用在整個物件 | ❌ |
| **擠出面** | **從一個面長出新的一段** | ❌ |
| **導任意一條邊的圓角**（N 段可調） | 讓幾何化細緻 | ❌（只有 `roundBox` 這個特例） |
| **細分** | 把面切細，才有點可調 | ❌ |
| **合併頂點** | 收尖（鹿角尖端、鼻尖） | ❌ |
| **刪除面** | 開口、做成殼 | ❌ |

### ⚠ 「擠出面」跟「推拉面」不是同一件事

| | 做什麼 |
|---|---|
| **推拉面** | 把既有的面往外移。方塊還是方塊，只是變高了 |
| **擠出面** | 選一個面，**長出全新的一段**。原本的面變成新一段的頂端 |

**擠出面才是主力。** 做鹿角就是：從頭部選一個小面 → 擠出一段 → 轉方向 →
再擠出 → 縮小 → 再擠出 → 合併收尖。全程都是平面。

這就是 box modeling——從一個方塊開始，靠擠出、細分、拉點逼近形狀。

### 共同的障礙只有一個

擠出面、拉點、導圓角看起來是三件事，卡住的是同一點：

> **選到一個元素之後，能不能改變它。**

**選那一半已經做好而且測得到**——`seam.js` 的 `nearestVertex` /
`nearestMarkableEdge` / `nearestFace`，現在被指定分片與貼合共用。
**缺的自始至終是「改變」那一半。**

### 現在做得出什麼、做不出什麼

分界線比想像中清楚：**這東西能不能用「一個輪廓 ＋ 擠出」表達？**

| | 現在 | 為什麼 |
|---|---|---|
| **汽車** | ✅ 做得出來 | 車身從側面是一個輪廓（Illustrator 畫好 → 匯入 → 擠出），車輪是圓柱，斜面用布林切 |
| **鹿頭、盔甲** | ❌ 做不出來 | **三個方向都在變**，沒有任何一個方向能用單一輪廓擠出 |

鹿頭是幾十個角度全不同的不規則面拼的。用布林做等於每個面都要建一個切割體、
擺位置、轉角度，做幾十次——而且做完不能微調。

> **所以編輯不是「精緻化工具」，是「可行性工具」**：
> 把「三個方向都在變」的東西，從做不出來變成做得出來。

---

## 5. 「雕塑類」這一節 —— 🔴🔴 **2026-09-02 kang 翻掉了一半**

> **⛔ 這一節原本三條全部標「不做」。現在只剩一條。**

### 🔴 kang 2026-09-02 的原話（這是判準，優先於底下一切推論）

> 「我其實**只是需要能夠增加模型的精細度**…我**不是要做出人體模型這些複雜的曲面**…
> 　我只希望能夠**多一些可以控制點邊面細分的工具**」
>
> 「舉例：我想做一個**安全帽**…但不是要求要非常圓滑…
> 　而是能夠有多一些工具能幫助我能夠**控制點邊面**，達到目的」

⚠ **而他也講了為什麼**：這個專案有**兩大輸出核心**，
**展開圖**（`瓣片展開` 是它的核心之一）與 **3D 列印**（**列印得出比較多曲面的模型**）。
⇒ **「更精細的曲面」服務的是輸出，⛔ 不是畫面好看。**

### 現在的定性

| 原本 | 現在 | 理由 |
|---|---|---|
| **影響範圍拉點**（proportional editing）| 🔴 **收回「不做」→ 要做**，而且是**三顆裡的第一顆** | ⚠ 舊理由寫「那是雕人臉用的，一次動一個元素就夠」—— **而安全帽那一類形狀證明⛔ 不夠**：一次動一個點只拉得出**尖角**，一次動一整圈又是**整圈一起動**，⛔ 做不出「中間多、旁邊漸漸少」|
| **平滑頂點** | 🔴 **收回「不做」→ 要做**（第三顆）| 同上。它是「把拉歪的地方順一順」，**按幾次由使用者決定** |
| **對稱雕塑** | ⏳ **沒討論到** —— ⛔ 不要當成已否決 | ⚠ 對照表上 `對稱化（Symmetrize）` 標 ⭐，跟這一條可能是同一件事，**沒查證過** |
| **平滑細分**（subdivision surface）| ✘ **維持不做**，⚠ **但理由換掉了** | 🔴 **舊理由「跟『全部是平面』的前提相反」已經⛔ 不成立** —— 球、圓柱、圓錐、旋轉成形早就都是曲面的多邊形近似。**真正的理由是下面那三個** |

### 🔴 `平滑細分` 維持不做的【真正】理由（2026-09-02 換掉舊的）

⚠ **kang 問過一個好問題**：「如果是它，不是也可以用『瓣片展開』處理?」
**✅ 他的理解是對的** —— 它產出的一樣是網格，瓣片展開處理得了。
⇒ **⛔ 不能再用「它跟我們的路線無關」當理由。** 真正的代價是這三個：

1. **形狀由程式決定** —— 按下去整個形狀當場被改掉（方塊變球），使用者只能改參數。
   　⚠ **而 kang 要的正好相反**：「不是要求要非常圓滑，而是能夠**控制**」
2. **面數 ×4** —— 按三次就是 64 倍。對 3D 列印沒差，
   　⚠ **但在展開圖上那是「瓣片數 ×4」＝ 要黏的接縫暴增**
3. 🔴 **尺寸會縮** —— 它會往內收，體積少一大截，
   　⚠ 而這個專案有一條硬規則：**「尺寸的依據是網格真值」**
   　⇒ 按下去之後**原本量好的尺寸就不是那個尺寸了**
   　⚠ **第 3 點 AI ⛔ 沒有實測過，是待驗的假設，⛔ 不是事實**

---

## 5.1 🔴 安全帽那一類形狀：盤點（2026-09-02，kang 認可）

> ⚠ **安全帽只是【這一類】的代表** —— kang：「安全帽是比較明顯的曲面形狀做舉例而已」。
> 共通特徵：**主體是曲面殼、⛔ 不是完美的旋轉體、要能局部調整而不破壞整體平順**。

| # | 步驟 | 現在做得到嗎 |
|---|---|---|
| 1 | **做出粗胚**（半球殼）| ✅ `球` ＋ `切一刀` ＋ `刪除面`（⭐ `旋轉成形` 更直接，但介面收起來了）|
| 2 | **加密** | 🔴 **卡住** —— 只能在**建立時**調高球的經線／緯線【`PRIM_DEFAULTS.sphere` ＝ 32×16】，而**轉成網格後參數就回不去**【`io.js` `isParametric`】。⛔ **不能只加密某一區** |
| 3 | **局部調形狀** | 🔴 **卡住** —— 拉一個點 ＝ **一個尖角**；拉一整圈 ＝ **整圈一起動**。⛔ 做不出「中間多、旁邊漸漸少」|
| 4 | **順一順** | 🔴 **卡住** —— ⛔ 目前沒有任何工具 |
| 5 | **帽簷** | ✅ `選一圈面` ＋ `擠出`，或 `擠出邊` |
| 6 | **通風孔** | ✅ `刪除面`，或布林差集 |

🔴 **六步卡三步，而三步卡的是同一件事：⛔ 沒有「局部加密 ＋ 局部平順地調整」的能力。**

### ⇒ 三顆的順序（⚠ 這是排序，⛔ 還不是規格）

| 順序 | 哪一顆 | 為什麼 |
|---|---|---|
| **1** | **比例編輯**（影響範圍拉點）| 🔴 **最關鍵** —— 沒有它，第 3 步永遠只拉得出尖角。⭐ 球預設就有 512 個點，**⛔ 先不加密也玩得起來** |
| **2** | **細分（一次細分整個選取）** | 點不夠時才需要，⭐ 而且它讓「**局部**加密」變可能（第 2 步）。表上標 ⭐ 半 |
| **3** | **平滑頂點** | 收尾用，⛔ 前兩顆沒有它也能動 |

⚠ 「拉一個點就是一個尖角」是**幾何必然**，⛔ 不是實測的 ——
要的話可以在沙箱量那個點周圍的夾角。

⚠ **⛔ 三顆都還沒有規格**：影響半徑怎麼給、衰減曲線用哪一種、
平滑要不要保體積 —— **動工前要各自畫互動圖問 kang**。

---

## 6. 外部軟體調查（2026-08-23）

調查方式是讀官方說明與教學文章，**沒有實際安裝操作過**，
所以「操作手感」那部分是二手的。kang 若實際看過畫面，他的判斷優先。

### 6.1 值得抄、而且地基現成的

| 來源 | 做法 | 為什麼對我們有用 |
|---|---|---|
| Pepakura | **雙擊選一整圈連續邊**、「兩點之間的邊」 | 我們的分片只能一條一條點。S 字 198 條邊等於不能用。地基是現成的 `he.smooth`（連續平滑折線＝一條曲線帶） |
| Shapr3D / Blender | **選取過濾器**（點／邊／面三個開關） | 平板沒鍵盤，做成三顆按鈕。第 6 期非它不可 |
| SketchUp | **懸停一秒跳出「你點到什麼」**（端點／中點／面中心，附顏色與文字） | 直接解掉坑第 24 條那句「我沒辦法驗證準確，不知道是不是操作有誤」 |
| Pepakura ＋ Shapr3D | **框選左→右＝完全框住才選；右→左＝碰到就選** | 業界慣例。我們現在只有「碰到就算」 |
| SketchUp | **鎖軸時虛線變實線** | 純視覺回饋。我們有 XYZ 軸標了，但拖曳中沒有「現在鎖在哪軸」 |
| Pepakura | **Separate All Faces** 一鍵全部面分開 | 官方註明是**給珍珠板用的**。我們只有「點面整圈切開」 |

### 6.2 我們沒想到的功能

| 來源 | 東西 | 判斷 |
|---|---|---|
| Slicer for Fusion 360 | **Interlocked slices（縱橫互卡）** | kang 認為**可以考慮納入**。不留孔、不用串桿、省材料。`section.js` 已會沿任意軸切，缺的是開槽 |
| Slicer | Radial slices（放射切） | 對旋轉對稱造型比水平切好 |
| boxes.py | **living hinge**（雷切一排細縫讓硬板彎起來） | 「壓克力／木板不能折」那一格的例外，雷切獨有 |
| Pepakura | **Reload 3D Model**（原模型改了只更新變動部分，保留已編輯的排版） | 對應我們 `bake()` 不可逆的痛點 |
| Slicer | 算「這個尺寸的板材要幾張」 | 跟已取消的「塞不塞得下」不同——那是排版時看得到的，這是估料 |
| potrace-js / imagetracerjs | 瀏覽器可用的自動描圖函式庫（npm 可取，能放進 `lib/` 不連外） | 日誌寫「自動描圖是另一個等級的工程」——**那句的前提該更新**。而且輸出是 SVG 路徑，現成的 `svgPath.js` 直接吃得下，連真轉角判定都繼承 |

### 6.3 出圖與組裝標示

| 來源 | 做法 |
|---|---|
| Pepakura | **2D↔3D 對應檢查**：點 2D 的一片，3D 對應的面亮起來、其餘半透明，反向也行 |
| Pepakura | 綠色箭頭指出「這片要黏到哪」，空白鍵把整支箭頭框進畫面 |
| Pepakura／Slicer | 組裝動畫 |

⚠ 但 kang 明確定調（2026-08-23）：

> 「要如何組裝都是由人為做分配...組裝資訊也只是輔助而已」

跟他更早劃的界線一致：「**只要做到準確地展開，邊的處理方式是 CNC 與實際操作的範圍**」。

**唯一的例外**：接合編號那條「**每個編號恰好出現兩次**」的檢查**不是輔助，是驗證**——
出現一次代表有一端接不回去，三次代表撞號會接錯，而**兩種情況圖看起來都完全正常**。
坑第 32 條就是靠它抓到的。

### 6.4 他們刷掉的坑

- **Slicer for Fusion 360 已於 2020 停止維護**（前身 123D Make）。這塊留下空缺
- Slicer 的已知限制：複雜有機造型會產生**重疊或孤島的片**
  → 提醒我們檢查 `section.js`：一刀切出多個輪廓時，有沒有片是浮在空中、串桿穿不到的
- Pepakura 二十年來幾何編輯**只提供一項**（細分邊並使其平滑），力氣全放在分片、排版、標示
  → 它的克制值得參考，但我們的定位不同：**它可以假設模型是別處建好的，我們不能**

---

## 7. 被否決的方向與理由 ★

**這一節是這份文件最值錢的部分。** 不寫下來的話，下一輪對話的 AI 會再提一次，
而 kang 得再解釋一次。

| 被否決的 | 誰決定 | 理由 |
|---|---|---|
| **切口補償（kerf／burn）** | kang 2026-08-23 | 「補償會是人工方式做處理」，程式不納入 |
| **匯入 STL／OBJ 當主力** | kang 2026-08-23 | 「我還要這一個程式做甚麼」——會讓程式退化成另一個 Pepakura。**保留為附加功能，排在編輯之後** |
| **有機造型雕塑工具**（影響範圍、對稱、平滑細分） | 2026-08-23 討論收斂 | 一切用幾何化多面體表達，人臉也做成低多邊形。這個分岔不存在 |
| **板材尺寸上限檢查** | kang 2026-08-22 | DXF 會先進 Illustrator 排版才給 CNC，塞不塞得下那裡就看得到（日誌坑第 23 條） |
| **程式替人決定怎麼組裝** | kang 2026-08-23 | 組裝是人為分配，程式只負責準確 |
| **第 7 期自由曲面切分近似** | 2026-08-22 | 剖面分切解同一個需求而且沒有近似 |
| **板件（sheet）繼續擴充** | 2026-08-23 | 中性面是鈑金概念，我們不用金屬。詳見第 9 節 |

---

## 8. 尚未定案

- **導圓角的段數**怎麼給（固定 N？依邊長自動？）
- **細分要做到哪**（只做「一分四」，還是要有環切／刀切）
- **擠出面的操作方式**（拖拉？輸入數值？兩者？平板上怎麼做）
- **第 6 期的編輯要不要支援板件**（傾向不支援，見第 9 節）
- **6.3「配方重播」（保留參數、改了尺寸重播編輯）值不值得做**
  → 取決於 kang 回頭改尺寸的頻率。目前有 Undo（快照式）與存讀檔兩層防護，可能夠用

---

## 9. 板件退場（2026-08-23 決定）

kang 的判斷：

> 「板件這似乎都偏向金屬加工的專業領域...但實際上我們完全用不到金屬這種材料」

**推論鏈是乾淨的：**

```
中性面存在的唯一理由 = 金屬折彎時外側被拉長、內側被壓縮
我們的材料是銑 45 度 V 溝再折 → 外皮長度不變 → 沒有拉伸
沒有拉伸 → 沒有中性層 → 「零厚度中性面」沒有東西可以代表
而實體的網格本來就是外表面，正好是我們要的
```

（關鍵決定 3 修訂時已經證明過：同一個方塊標成實體或板件，展開結果**完全一樣**。）

### 板件現在的帳

| 好處 | 代價 |
|---|---|
| 資料輕一點（一個面 vs 六個面） | 坑 7：加厚時折彎處被削薄 |
| | 坑 8：不能做布林聯集 |
| | 坑 17：**STL 壞了將近兩週沒人發現** |
| | 待辦：板件的分片**沒驗過** |

### 決定：分兩步走

**現在就做（省工，零風險）：**

- 第 6 期的點線面編輯**不支援板件**——省掉「命中點落在加厚的殼上而不是中性面」整組麻煩
- 日誌待辦那條「板件（折板）的分片沒有驗過」→ **直接劃掉，不用做了**

**暫時不動：**

既有的板件留著。要拆它得動 898 項測試裡一大票（板件加厚體積、板件陣列、
折板展開長度）跟 v4 舊檔相容，而**它現在沒有在害人**。
日誌 `seam.js` 那條註記講過同一件事：**為了整齊去改能用的東西，不划算。**

### 連帶：K 因子　✅ **已於 2026-08-23（`cedbab3`）從展開圖拿掉**

> ⚠ **本節原本的寫法已作廢，不要照它做。**
> 原本寫：「**選到捲不起來的材料時**，K 因子那些欄位直接收起來。」
> → **判準錯了**：跟捲不捲得起來無關。
> 現在的規則是**尺寸一律以網格為準、與材料無關**
> （見 `PROJECT_LOG.md`「尺寸的依據」），所以 K 對**任何**材料
> 都不該出現在展開圖上，不是只有板材。

kang 2026-08-23：「K 因子…這都是造成混亂的條件…**不應該在真實尺寸中出現**」。

**已經做掉的**：展開面板的 K 因子輸入框與「用建議值」按鈕整組移除；
圖上的「內側 R」改成標**網格量出來的半徑**；`sheet.js`／`dxf.js`
標題欄的 `K 0.5` 拿掉。

**刻意留著的**：折板參數面板的 `k`、`neutralRadius()`、`bendAllowance()`。
它們在 `buildPrim()` 裡**真的參與建模**（換 K 連網格都變），
跟 seg 決定圓柱有幾邊同類。**建模參數 ≠ 展開參數。**

---

## 10. 編輯與既有標記的關係（結論：問題比想像中小）

一度規劃過一整套「標記健康度」的安全網，後來發現大部分是多餘的。

### 三類屬性，只有一類需要新規則

| 屬性 | 誰決定的 | 編輯之後 |
|---|---|---|
| `role: fold` | 幾何自動標的（`autoMarkFolds()`，依轉折角） | ✅ 重算就好，函式現成 |
| `role: cut`（使用者標的分片） | **使用者的決定** | ✅ 拉點不改拓撲，那條邊還在，標記依然成立 |
| `smooth` | 上游（貝茲錨點） | ⚠️ 只有這個需要新規則 |

### ⚠ 兩個讀程式碼時發現的實作要點

**1. 不能直接重跑 `autoMarkFolds()`——它會洗掉使用者標的分片**

```js
autoMarkFolds(flatTolDeg = 0.5) {
  for (const he of this.edges()) {
    if (!this.isFlat(he, flatTolDeg)) { this.setRole(he, EDGE_ROLE.FOLD); }
  }
}
```

`setRole` 直接覆蓋——標成 CUT 的邊只要不平就會被改回 FOLD。
**這正是日誌說「開檔時 `autoMarkFolds()` 重跑，標記就沒了」的機制。**
要寫一個保留 CUT 的版本。

**2. `autoMarkFolds()` 只加不減**——原本是折線的邊被拉平之後不會清回「未指定」。

### `smooth` 的新規則：以幾何為準（單向）

`flatten.js` 這一行證實問題存在：

```js
if (Math.abs(角度) > FLAT_TOL_DEG && !he.smooth) {   // 是 smooth 就永遠不算折線
```

只要 `smooth` 還寫著 true，**夾角拉到 30 度它照樣不算折線**，展開長度就錯了。

> **規則：上游說平滑，但實際夾角超過 3°（跟真轉角同一個門檻）→ 以幾何為準，
> `smooth` 自動關掉。**
>
> 理由：**使用者拉那個點就是故意要它有角度，他的意圖比檔案裡的舊資訊新。**
>
> 反向不成立——原本是真轉角的邊拉到剛好共線，不該自動變平滑（可能只是巧合）。

### 為什麼不需要更多

**線性流程（建模 → 標分片 → 展開）下，編輯永遠在分片之前，分片還不存在。**
問題只出現在「回頭」，而回頭已經有兩層現成的防護：

| 距離 | 用什麼 |
|---|---|
| 剛剛那幾步 | Undo（`history.js`，快照式） |
| 很久以前 | 存檔／讀檔 |

**但存檔救不了一種情況**：它解決「我知道我做錯了，想重來」，
不解決「**我不知道我做錯了**」。上面那條 `smooth` 規則補的就是後者——
日誌上最貴的坑（第 6、17、20、32 條）全部是「程式沒報錯、圖沒異狀、東西做出來是錯的」。

### ⚠ 一個便宜的補強

現在唯一不可逆的門檻是「轉成可編輯網格」（`bake()` 只有一行，把參數整組覆蓋掉）。

→ **按下那顆按鈕時自動存一份**，檔名帶時間。極小的工，補的是唯一回不去的地方。

---

## 附：這場討論的關鍵轉折（給未來的 AI）

2026-08-23 這場討論，AI 偏了四次，四次都是同一個病：
**拿別人的成功經驗套上來，而沒有先確認目的。**

| # | AI 的主張 | kang 的修正 | 病因 |
|---|---|---|---|
| 1 | 把 kerf 補償列為「最實際的採集」 | 「補償會是人工方式做處理」 | 從 boxes.py 抄，沒問誰在做 |
| 2 | 蓋一整套「標記健康度」安全網 | 「我們順序是先編輯造型才做分片，為何會影響分片」 | 為線性流程不會走到的方向設防 |
| 3 | **「先做匯入 STL／OBJ」排第一名** | 「我還要這一個程式做甚麼」 | 錯誤類比「匯入 SVG 線稿」的成功——**線稿是他的產出，STL 是別人的** |
| 4 | 把編輯分成「有機造型」與「機構造型」兩套 | 一切都幾何化，人臉也做成低多邊形 | 拿通用建模軟體的分類硬套 |

**共通點**：日誌坑第 13、14、23 條講了三次「先確認目的，不是先確認規格」，
而這次是**第四、五、六、七次**。目的包含：用什麼材料、給誰做、
下游拿去做什麼、**以及這個程式存在的理由是什麼**。

最後那一項是這次新增的——第 3 次偏差（匯入 STL）不是規格問題，是**定位問題**：
一個功能可能技術上完全合理、工程量最小、立刻解決限制，
**但它會掏空這個程式存在的理由。**

---

## 相關文件

- `PROJECT_LOG.md` — 目前確定的狀態
- `剖面分切參考/` — 剖面分切的示意圖（2 張，動那一項之前先開圖）
