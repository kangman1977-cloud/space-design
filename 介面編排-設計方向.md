# 介面編排 — 設計方向

最後更新：2026-08-31　V1.0

> 🔴 **這一份是「動工前的推導」，⛔ 不是規格也不是待辦。**
> 動介面之前讀完；讀完之後**⛔ 不要再從舊路線圖或印象推**。
>
> **家**：推導與被否決的方向在這裡；**現況與待辦在 `STATUS.md`**；
> **做完之後的規格**去 `規格\`。〔落點規則見 `STATUS.md`〕

---

## 為什麼現在做（kang 2026-08-31 決定）

> **他的原話：「接下來我們要優先處理界面的問題..因為功能已經有一定的數量..
> 　可以設計界面的安排了」**

⚠ **⛔ 不是因為「不好看」** —— 是**數量到了**。量出來的：

| | 數字 |
|---|---|
| 工具列 | **13 組、86 顆按鈕** |
| 其中「編輯」那一組 | **47 顆 ＋ 16 個數字欄 ＋ 2 個下拉** |

**86 顆裡有 47 顆擠在同一組。**

---

## 🔴🔴 最重要的發現：上下文資料**早就有了**，只是用錯方式表達

**⛔ 這一節是這份文件的核心，⛔ 不要跳過。**

`updateBar()` 管著 **62 個元件**，而它們分成兩種表達方式：

| 表達方式 | 幾個 | 是誰 |
|---|---|---|
| **隱藏／出現**（＝ 換頁） | **16** | ⭐ **只有鋼筆（14）與刀具（2）** —— 最近做的那兩個 |
| **變灰**（永遠佔著版面） | **46** | 其餘全部 |

而那 46 個的變灰條件，一共 **17 個不同的判斷式**：

```
!face  !edge1  !edgeAny  !edgeSome  !edgeAnySel  !slideOk  !canAll
!canSimilar  !canChecker  !canBisect  !canKnife  !canFill  !twoEdges
!manyVerts  !threeVerts  !fixable  sel.count === 0
```

🔴 **它們幾乎全部在回答同一個問題：「使用者現在選到什麼？」**

⚠ **底下這個歸類是我做的（⛔ 不是量出來的），但每一列的成員是量出來的**：

| 選到什麼 | 那時候該出現的東西 |
|---|---|
| **面** | 擠出　內縮＋寬度　刪除面　壓平 |
| **一條邊**（剛好一條） | 環切＋刀數　選一圈　選一條線　選一圈面 |
| **一圈邊** | 沿面滑動＋量＋單位　平行複製＋寬度 |
| **任意邊** | 導角＋寬度＋段數　全選邊　選轉角＋度　分離　邊上加點＋個數 |
| **點** | 多點連接　點連成面　面上加線＋位置 |
| **物件**（沒進編輯） | 補洞　橋接＋轉幾格　切一刀＋軸＋座標　刀具　修法向　翻面　複製　刪除 |

⇒ **⛔ 我不必發明一套分類。程式裡已經有了** —— 它只是選擇「變灰」而不是「換掉」。

---

## 從 Illustrator 的截圖讀到什麼（kang 2026-08-31 給的參考範本）

它把東西放在**四個地方**，而分法⛔ 不是「按功能主題」，是**按「這東西是什麼性質」**：

| 位置 | 放什麼 | 性質 |
|---|---|---|
| **左側直條**（圖示、無文字、2 欄） | 選取／鋼筆／形狀／文字／縮放… | **模式工具** —— 按了狀態**會留著** |
| **頂端選單列** | 全部命令，含深層不常用的 | **命令** —— 按一下做完就結束 |
| **頂端控制列**（第二列） | 截圖是「**無選取範圍**」→ 填色／筆畫／寬度／樣式 | **上下文參數** —— 選到文字時整條換掉 |
| **右側面板群 ＋ 左下浮動面板** | 字元｜段落｜OpenType、色票｜顏色｜內容、變形｜對齊｜路徑管理員… | **屬性面板 —— 分頁疊在一起，一次只看到一個** |

⭐ **最關鍵的一條是右側那個「分頁疊在一起」**：一次只顯示一個，所以塞得下十幾個面板。
**⛔ 它能容納這麼多功能，⛔ 不是因為排得比較整齊。**

### ⚠ 但有一條⛔ 不可以照抄

**Illustrator 的選單命令是「隨時都能按」的，所以要有選單。**
**⛔ 而我們的命令絕大多數是「選到特定東西才能按」** ——
那是**控制列**的行為，⛔ 不是選單的行為。

> 🔴 **所以「那 58 個命令要搬去哪個選單」這個問題本身問錯了。**
> 它們**大部分根本不該有固定的家**，該跟著「選到什麼」出現與消失。

---

## kang 拍板的三條（2026-08-31）

1. **照分類原則，⛔ 不照長相** —— 拿「工具／命令／上下文參數／面板各有各的家」
   這個原則，但具體怎麼擺依我們自己的情況（特別是平板）。
2. **一套版面，桌機平板都能用** —— ⛔ 不做兩套。
   ⚠ 這跟他一直以來的做法一致（`尖角`／`鎖角度`／`確定曲線` 都是鍵盤的替代鈕）。
   🔴 **代價：這一次他必須拿平板實測** —— 平板版面**從頭到尾沒驗過**。
3. **那 46 個「變灰」的改成「沒輪到它就不出現」，但【工具永遠在】。**

---

## 🔴 已知的代價（kang 選之前就攤開講過的）

> **變灰 → 換頁，代價是「按鈕會不見」。**
> 使用者找不到一顆他知道存在的按鈕時，**那跟「壞掉了」分不出來** ——
> 而**變灰至少還看得到它在那裡**，滑過去還能從提示知道為什麼不能按。

⭐ **Illustrator 是靠「左側工具箱永遠固定在那裡」化解的** ——
工具⛔ 不會消失，消失的只有參數。
🔴 **所以「工具永遠在」⛔ 不是順便加的一句，它是這個代價的解藥。**
⛔ 拿掉它，整個方案就不成立。

⚠ **被否決的另一條路**：「沒選東西時那一區塊寫『選一個面，這裡會出現擠出／內縮／壓平』」。
kang 2026-08-31 選了⛔ 不寫。〔記在這裡，免得日後有人以為沒想過〕

---

## 🔴🔴 kang 2026-08-31 補的第 4 條：主工具 vs 其餘

> **他的原話：「鋼筆..刀具是主工具...其餘可以在選擇工具後..控制列在顯示」**

⭐ **這一句把「工具鈕上到底該寫什麼」講死了**：

| | 放哪 | 寫什麼 |
|---|---|---|
| **主工具** | 左側直條 | **身分** —— 永遠是「鋼筆」「刀具」，⛔ 不會變 |
| **其餘**（狀態字、次級按鈕、收工鈕） | **控制列** | **狀態與動作** —— 「12 個點．共 3 條」「切下去（3 點）」 |

🔴 **這一條是被【量】逼出來的，⛔ 不是美學**：
左側直條的字只放得下 **50px**，而原本工具鈕會顯示
「切下去（3 點）」**71.7px**、「刀具（點兩個以上）」**94.5px**、
「鋼筆 12·3條」**54.8px** —— **⛔ 三個都塞不進去**。
〔量法：canvas `measureText`，字體字級照 `index.html` 實際的〕

⚠ **⛔ 而這不是「因為塞不下所以將就」** —— 塞不下正好照出一件事：
**那些字本來就不是工具的名字，是工具的狀態。**

---

## 圖示：⛔ 能沿用已經認得的就不要另外發明

kang 2026-08-31 看過 `樣板-工具圖示候選.html` 之後拍板照建議採用。

- **鋼筆／刀具** → 沿用 **Illustrator 的筆尖與美工刀** —— 他每天在用，⛔ 不必重新學
- **移動／旋轉／縮放** → 跨軟體通用符號（十字箭頭／迴轉箭頭／方框加角落箭頭）
- **拉點線面／貼合／分片** → 這三個 AI 裡沒有對應，是自己畫的

**⛔ 被否決的（理由都在那份樣板裡，⛔ 不要重提）**：
「方框＋四角控制點」當拉點線面（**那在 AI 裡是變形框**）、
磁鐵當貼合（**磁鐵在別的軟體是吸附 snap**）、
攤開的盒子當分片（**那是展開圖的結果，⛔ 不是「標切割線」這個動作**）、
放大鏡當縮放（**那是「看得比較大」，⛔ 不是「東西變大」**）。

🔴 **兩份樣板留著⛔ 不要刪**：`樣板-左側工具直條.html`（三案比較）、
`樣板-工具圖示候選.html`（每個圖示的候選與否決理由）。

---

## 進度

- ✅ **第 1 段完成（2026-08-31）**：`#rail` ＋ 8 顆工具搬過去；
  控制列多 `#toolDone`（收工）與 `#toolState`（做到哪）。
  ⏳ **外觀與平板都還沒實測。**
- ⏳ 第 2 段：46 個「變灰」改成「隨選到什麼出現」
- ⏳ 第 3 段：次級開關與顯示開關歸位
- ⏳ 第 4 段：剩下 39 顆常駐的怎麼擺

---

## ⏳ 還沒決定的（動工前要問 kang）

- **那 16 個模式工具真的要放左側直條嗎？** 平板橫放時左側直條會吃掉寬度。
- **一次全改，還是分階段？**（見下）
- **圖示還是文字？** Illustrator 左側是純圖示 —— ⚠ 而這個專案有一條規則：
  **⛔ 不用程式圈的行話，欄位名去 `index.html` 抄** ——
  圖示是另一種「要先學會」的語言，跟那條規則的精神⛔ 不一致。
- **檔案／視角／布林／陣列那 39 顆**（⛔ 不隨選取變的）要放哪。

---

## ⚠ 動工前必須先接受的一件事

🔴 **這是一次會動到 `index.html` ＋ `main.js` `updateBar()` ＋ CSS 的大手術，
而這個專案在版面上踩過一個安靜的坑**：

> 2026-08-20 一次編輯**意外刪掉 `@media print {` 那一行開頭** ——
> 大括號平衡、JS 合法、瀏覽器一聲不吭，
> **但每一條列印規則變成全域的，整個介面消失了。**

⭐ **語法檢查抓不到這一類，差異看得到** —— 所以**每一步都要小到看得完差異**。
〔`版控工具\1-看差異.bat` 檔頭寫的就是這件事〕
