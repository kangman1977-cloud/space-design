# 外部參考：展開與分片的演算法

最後更新：2026-08-23　V1.0（第 1～5 節完成；第 4 節論文**只做到知道有哪些**，沒讀原文）

> **來源全部是第一手原始碼。** 讀過：
> `Export-Paper-Model-from-Blender/unfolder.py`＋`nesting.py`（addam，GPL）／
> `PolyZamboni/polyzamboni/` 的 `autozamboni.py`＋`unfolding.py`＋`utils.py`＋`zambonipolice.py`＋`readme.md`（AntonFlorey，GPL）

> **這份文件的主要讀者是 AI，不是人。**
>
> 🔴 **界線與篩子在 `外部參考調查.md` 第 2 節，這裡不重抄。**
> 🔴 **不出現任何一行外部原始碼**（`io_export_paper_model` 與 PolyZamboni 都是 GPL）。
> 只記邏輯：資料結構、處理順序、判斷條件、為什麼那樣設計。
>
> **過篩子的問題**：這個對**板材切割／展開／剖面分切**這條路有沒有用？
> 紙模的**黏合片**沒用（我們是銑 45° V 溝或切開再接）；
> **自動補刀、重疊處理、排版**直接相關。

---

## 1. 現況基準：我們的展開現在到底怎麼做

> 🔴 實際讀 `unfold/flatten.js`、`unfold/seam.js`、`out/sheet.js` 讀出來的。
>
> ⚠ **本節寫於 2026-08-23 上午，當天下午展開就被改過一輪**（`4361556`）。
> 已就地更新的段落標了〔已修〕。**基準規則的家是
> `PROJECT_LOG.md`「尺寸的依據」**，本檔不重抄。

### 1.1 整條流程

```
指定分片(seam.js 標 CUT) → 分片(splitPatches) → 攤平(生成樹) → 認出圓弧帶 → 重疊偵測 → 出圖(sheet.js)
```

`flatten.js` 檔頭自己寫的：「這裡只做純幾何」。材料規則在 `unfold/rules.js`，分開的。

### 1.2 接縫怎麼定義（＝我們的「指定分片」）

- `seam.js` **一行幾何運算都沒有**。它只做一件事：**把某些邊的 `role` 改成 `CUT`**。
  之後 `flatten.js` 的 `edgeIsCut()` 第二件事就是尊重 `EDGE_ROLE.CUT`，分片自然跟著變。
- **哪些邊可以標**：`isMarkable()` ＝ 有隔壁 **且** 不共面。
  判準是「**畫面上看得見的稜線，才是可以標的邊**」——
  因為 `scene.js` 畫稜線用 `EdgesGeometry(geometry, 1)`，只畫轉折超過 1° 的邊。
  兩者必須是同一組，否則畫面在騙人。
- 🔴 **只做「強制切開」，不做「強制折起來」。** 這個不對稱是刻意的：
  標成 CUT ＝ 一定做得出來（多切一刀）；取消 ＝ 交還給材料規則，**不是**強迫折。
  若允許強制折，使用者就能在壓克力上標一條折線 —— 而壓克力折不起來。
  **現在這個設計不可能產生做不出來的圖。**
- **一鍵切出一個面**（`cutAroundFace`）：因為標一條邊通常什麼都不會發生
  （60×45×40 方塊標 1 條邊，片數仍是 1），使用者會以為壞了。
- **參數物件不能標**（`canMarkSeams`）：存檔只存參數，開檔重新生成，標記會消失。
  正確順序是 **尺寸先確定 → 轉成可編輯網格 → 標分片 → 出圖**。

### 1.3 攤平：生成樹「先走最平的邊」

不是廣度優先。用 `MinHeap` **優先跨過轉折角絕對值最小的邊**。

理由：共面的鄰居其實是同一片板子，要讓它們黏在一起。
實測（圓錐加底蓋）：先進先出會讓 32 個底蓋三角形各自從旁邊的側面跨輪圈接上去，
底蓋被拆散、散落在扇形四周 —— 一張星芒狀的廢圖。
改成先走最平的邊之後，底蓋轉折角是 0 一定先走完，輪圈（113°）最後才走而且只走一條，
其餘 31 條自動變成**隱含切割線**。

> ⚠ 附帶的教訓（寫在原始碼註解裡）：**計時一定要先暖機再取多次最小值。**
> 曾以為線性版慢五倍，那是 Node JIT 冷啟動的假象；實測兩者一樣快。
> 留堆積是因為 O(n log n) 對大曲面比較安全，**不是因為它現在比較快**。

### 1.4 🔴 重疊偵測：只偵測，不解，而且是**二元**的

`detectOverlap(polys)`：

| 面向 | 現況 |
|---|---|
| 演算法 | 外接框先篩掉九成配對 → **分離軸測試**（展開後的面幾乎都是凸的三角形／四邊形） |
| 相鄰判定 | `sharesEdge()` ＝ **共用兩個點**才算相鄰。只共用一個角不算 —— 否則方塊展開圖會漏判 |
| 回傳 | 〔已修〕**`{hit, checked}`** —— 原本是一個布林值 |
| 上限 | 〔已修〕超過 3000 片回 `checked:false` 並**明講「沒有檢查」**。原本**直接回 false**，等於大模型顯示「沒問題」而且不會說 |
| 後續動作 | 〔已修〕三態訊息（乾淨／有重疊／沒檢查）。原本只推一條「需要切分（第 7 期會做自動分片）」 |

✅ **這兩項就是本檔第 5 節第 2、3 項的結論，已於 `4361556` 做掉。**
「第 7 期」那句假退路也一併刪了（第 7 期已取消，見 `PROJECT_LOG.md` 路線圖）。

⚠ 但**重疊之後仍然沒有人會自動去解** —— 那是刻意的：
自動分刀已明確不做（`PROJECT_LOG.md`「✘ 明確不做」），
訊息改成指路「請用『分片』把它切開」。

### 1.5 自動補刀：**沒有**

使用者標到哪裡就切到哪裡。程式不會為了讓某一片攤得平／不重疊而自己多切一刀。

### 1.6 排版：**沒有**

`out/sheet.js` 是**一片一頁 A4 橫向**，交給瀏覽器自己的列印功能。
沒有 nesting、沒有旋轉擺放、沒有材料利用率。

⚠ 相關的一項待辦**已被取消**：「板材尺寸上限檢查」——
kang 的 DXF **會先進 Illustrator 做整體處理才給 CNC**，排版是在那裡做的。

### 1.7 其他已有的

- **接合編號**（`collectJoints`／`seamPartition`）：切開的兩側標同一個號碼。
- **認出圓弧帶**（`arcCorrection`）：〔已修〕只為了**標註與報告**，
  不改任何尺寸。原本它會把弦長拉成弧長（`applyStretch`，已整支刪除）。
- **相同片合併**（`groupIdentical`）：一樣的片只出一張圖，標數量。
- **材料規則**在 `unfold/rules.js`，七種材料。
  〔已修〕**材料已完全退出尺寸計算，K 因子也退出展開圖了**（`cedbab3`）——
  幾何核心現在只問 `canFold`（改變片數）與 `validate`（只講話）。
  ⭐ `arcCorrection()` 與 `bandsInGroup()` **連 `rule` 都收不到了**，
  所以「幾何核心不知道材料是什麼」是**結構保證**，不是靠註解提醒。
  〔本行原本寫「還有 `k·thickness` 算標註用的內側 R」—— 那個已經拿掉，
  　圖上改標**網格量出來的半徑**〕
  原本這裡寫「捲得起來的材料展開錐面會跳『請勿據以下料』」——
  **那則警告已整則刪除**，因為錐面的攤平本來就精確（扇形半徑 ＝ 母線，實測）。

---

## 2. Export Paper Model（Blender 內建的 `io_export_paper_model`）

**來源**：`unfolder.py`（完整讀過，58KB）、`nesting.py`（完整讀過）。GPL，只記邏輯。

### 2.1 🔴 它的演算法方向跟我們**相反**

| | 起點 | 動作 |
|---|---|---|
| **我們** | 一整片 | 從一個種子面出發長生成樹，**跨過去就是連著**；跨不過去（CUT）就分片 |
| **它** | **每一個面各自是一片**（等於全部的邊都先切開） | 依優先序**試著把兩片合併**，合不起來就維持切開 |

「合不起來」的判準只有一個：**合併之後會不會重疊或超出紙張**。

🔴 **所以重疊不是「事後偵測」，是「從頭到尾不可能發生」。**

### 2.2 合併的優先序（決定接縫落在哪裡）

每一條邊算一個 priority，**由小到大**依序嘗試合併：

```
凸邊：  0.5  × (夾角 / π)
凹邊：  1.0  × (−夾角 / π)
再加：  −0.05 × (這條邊長 / 平均邊長)
```

三個係數是使用者可調的。讀出來的意思是：

- **越平的邊越早合併**（夾角小 → 值小）。⭐ **跟我們「生成樹先走最平的邊」是同一個直覺。**
- **凹邊的懲罰是凸邊的兩倍** → 同樣角度，**優先切在凹處**。
  （紙模的凹折比凸折難做，而且凹處的接縫看不見。）
- **長度係數是負的** → **長邊優先合併**，也就是**優先切在短邊上**，
  這樣接縫總長度短、對位誤差少。⭐ 我們完全沒有這一項。

### 2.3 `join()`：合併前的五道關卡（全部通過才動資料）

🔴 **關鍵設計：先算出一份「假設合併之後」的預覽座標（phantom），所有檢查都對預覽做；
任何一關失敗就直接放棄，原始資料一個位元都沒動。**

| 關 | 檢查什麼 |
|---|---|
| 0 | 兩邊是不是同一片（同一片就是形成迴圈，拒絕） |
| 1 | **永遠把小的併進大的**（先 swap），純粹是效能 |
| 2 | **紙張尺寸**：合併後的外框放不放得進一頁。而且不是只比外框，還會做一次**最小外接矩形旋轉試算**再比 |
| 3 | **合併相距很近的 2D 頂點**，用 **union-find（含路徑壓縮）**。⚠ 容許值是 **邊長 × epsilon，是相對值不是絕對值** |
| 4 | **同一點上有 3 條以上邊界線段時**，依斜率排序、檢查繞向 —— 抓「只在一個角碰到」的情形 |
| 5 | **自相交檢測：掃描線（sweepline）** |

**第 5 關的做法值得單獨記：它有兩套掃描線。**

- `QuickSweepline`：二分搜尋插入，**只檢查上下鄰居**，O(n log n)。
- `BruteSweepline`：**兩兩全比**，安全但慢。
- 平常用 Quick；一旦比較函式碰到**退化情形**（四個叉積全為 0，分不出上下）就丟例外，
  **改用 Brute 重跑一次**，並且把這一片**永久標記為 `has_safe_geometry = False`**，
  之後合併只要牽涉到它就一律用 Brute。

> ⭐ **這是「快路徑 ＋ 安全路徑 ＋ 自動降級」的完整範例。**
> 不是「挑一個演算法」，是「**用快的，測得出來不可靠時自己退回慢的，而且記住**」。

### 2.4 展開前的體檢（`check_correct`）

在做任何事之前先擋四種網格：

| 擋什麼 | 判準 |
|---|---|
| 零長度的邊 | 長度 < epsilon **且**有面連著 |
| 零面積的面 | 面積 < epsilon |
| **扭曲的多邊形**（不平的 n 邊形） | 頂點離平面的距離 > **該面直徑的 1%**（⭐ **相對值**） |
| **整個物件被鏡射成內外相反** | 矩陣行列式 ≤ 0 |

🔴 **而且錯誤訊息會做三件事**：講出有幾個、**講出該用哪個工具修**（Remove Doubles／Triangulate）、
**並且直接在畫面上把那些元素選起來**（連選取模式都一起切過去）。

### 2.5 排版（`finalize_islands` ＋ `nesting.py`）

分兩層，**兩層都不是「隨便擺」**：

1. **每一片先自己轉角度**（`cage_fit`）：取凸包，列舉候選旋轉角，
   找出「在指定長寬比之下外接矩形最矮」的那個角度。
2. **再分頁**（`nesting.py`）：**branch and bound 窮舉**，目標是**頁數最少**，
   可行性測試用 `box_pack_2d`（外框裝箱）。
   🔴 **有 15 秒 timeout**，超時就用目前最好的解。

⚠ 所以它是**外框排版，不是真正的輪廓嵌套**。凹形的片之間塞不進去。

**放不下時它怎麼講**：「有一片太大放不進這個尺寸的頁面。請縮小模型，
或者**自己找到那一片、手動把它切開**。」
→ **它不會在出圖階段自動切**；控制尺寸的手段是第 2.3 節第 2 關，在合併階段就擋住。

### 2.6 兩個順手撿到的小東西

- **`is_upsidedown_wrong()`**：檢查一個編號字串**上下顛倒看會不會變成別的意思**
  （由 `69NZMWpbqd` 這些字元組成、且整串都在 `80oOxXIl` ∪ 前者之內）。
  是的話就**在後面加一個句點**。⭐ **這直接適用於我們的接合編號。**
- **非流形邊（3 個以上的面共用）**：挑「法向最接近的那一對面」當主面，其餘一律當成切開。
  ⭐ 一個便宜的容錯策略。

### → 為什麼那樣設計

- **「先切光再合併」而不是「先連著再切」**，是因為**合併有一個乾淨的失敗條件**（會不會重疊），
  而切開沒有 —— 「該不該切這一刀」問不出一個局部就能回答的問題。
  換方向之後，整個演算法變成一個帶約束的貪婪合併，**每一步都可驗證**。
- 檢查全部對 phantom 做：因為**合併是不可逆的**，做了才發現不對就回不去了。

### → 我們現在怎麼做 ／ 差在哪 ／ 該不該學

（現況見第 1 節。逐項結論見第 5 節。）

---

## 3. PolyZamboni

**來源**：`readme.md`、`polyzamboni/autozamboni.py`（自動分刀，完整讀過）、
`polyzamboni/unfolding.py`、`polyzamboni/utils.py`、`polyzamboni/zambonipolice.py`。GPL，只記邏輯。

### 3.1 🔴 它的世界觀：**對偶圖 ＋ 一片必須是一棵樹**

- 面 ＝ 節點，相鄰 ＝ 邊 → **對偶圖**（用 networkx）。
- 切開一條邊 ＝ 從對偶圖移掉一條邊。**一片 ＝ 一個連通元件。**
- 🔴 **一個元件攤得平的充要條件是「它的對偶子圖沒有迴圈」，也就是必須是一棵樹。**
  有迴圈的元件被標成紅色，而且**整個模型不給出圖**。

> ⭐ **這是我們跟它最大的哲學差異，值得單獨標出來。**
>
> 我們的攤平是「長一棵生成樹，**走不到的邊自動變成隱含切割線**」——
> 所以永遠攤得平，代價是**程式替使用者決定了額外的接縫在哪裡**。
>
> 它的立場是：有迴圈就**不是一片合法的片**，紅色，你自己去切。
> 理由是迴圈要能黏合，繞一圈回來必須剛好接上，而**一般情況下接不上**。

它也明講自己**處理不了**哪些網格（readme）：
非流形頂點或邊／**兩個面碰在超過一條邊上**（＝對偶圖出現重邊，它會直接放棄）／
它的三角化切不開的面／**高度非平面的面**。而且提供「把需要修的面選起來」。

### 3.2 自動分刀：跟 Export Paper Model 同一個方向，但約束不同

```
先把所有邊都切開 → 依序嘗試「把這一刀拿掉」
```

拿掉一刀之前要過五關：

| 關 | 檢查 |
|---|---|
| 0 | 邊界邊 → 本來就不必切，直接清掉 |
| 1 | 🔴 **兩側屬於同一個元件 → 跳過**（否則會產生迴圈） |
| 2 | 🔴 **合併後的面數 > 使用者設的上限 → 跳過**（預設 10） |
| 3 | 合併後會不會重疊（**一樣是小的併進大的**） |
| 4 | 會不會產生**重疊的黏合片**；會的話依品質等級決定要不要**把這一刀加回去** |

**處理順序**：把所有邊**依「跟指定軸（X／Y／Z）的對齊程度」由大到小排序**。
最對齊那個軸的邊最早被拿掉 → 合併出來的片自然變成**繞著那個軸的一圈一圈**。
⭐ 這就是 readme 說的「loops wrap around X/Y/Z」，實作上只是一個排序鍵。

**品質等級是三態**（不是布林）：
`不允許任何重疊` ／ `允許黏合片重疊` ／ `全部允許`。

**整支演算法是一個 generator，逐步 yield 進度** → 進度條、可取消。

### 3.3 四級顏色回饋（對照我們的布林值）

| 顏色 | 意思 |
|---|---|
| 🔴 紅 | 這一區**攤不平**（對偶圖有迴圈）→ 根本做不出來 |
| 🟠 橘 | 攤得平但**片會重疊** → 做得出來但圖沒法用 |
| 🟡 黃 | 只有**黏合片**重疊 → 大致可用 |
| 🟢 綠 | 乾淨 |

邊也分三色：**紅＝手動切、綠＝手動標「不准切」、藍＝自動切**。
🔴 **綠色那一種我們完全沒有** —— 它是「**鎖住這條邊，自動演算法不准動它**」。

### 3.4 它另外有、而我們沒有的

- **Build Sections（組裝分段）＋ Build Step Numbers**：把片分組，每組內編 1..n 的組裝順序，
  印在圖上。並且有一支獨立的檢查函式驗證「編號是不是剛好構成 1..n」。
- **Define Region**：選一組面，一次把「選取與非選取之間」的邊全部切開。
  ⭐ 我們的 `cutAroundFace()` 是它的單面版本。
- **Separate Materials**：材質不同就自動切開 —— 「用既有的分類當切割依據」。
- **頁面版面可以手動編輯**：選片、`g` 移動、`r` 旋轉、`f` 改編號 ——
  ⭐ **快捷鍵刻意跟 Blender 的變換一致**（呼應研究線一第 5 節）。
- ⚠ **改動切割會清掉所有版面資訊**，而且事先警告。

### → 為什麼那樣設計

- **對偶圖 ＋ 樹**這個模型把「一片合不合法」變成一個**純圖論問題**，
  不必碰幾何就答得出來，而且答案唯一。幾何（重疊）只在「合併」那一步才需要。
- **面數上限**而不是尺寸上限：因為它的使用者是低多邊形紙模，一片太多面就難組。
  （對我們不成立 —— 我們的限制是板材尺寸，而那件事已經確認由 Illustrator 處理。）

---

## 4. 公開論文

> ⚠ **這一節只做到「知道有哪些、核心想法是什麼」，沒有讀原文。**
> 不假裝查過，需要時再回來。

- **Mitani & Suzuki 2004, "Making papercraft toys from meshes using strip-based approximate unfolding"**（SIGGRAPH）——
  把網格依特徵切成部件、把三角形歸成帶狀區域、簡化成**沒有內部頂點的三角形帶**，
  帶就一定攤得平。**這是「近似展開」那一路的源頭。**
  ⚠ **過篩子：對我們用處不大。** 它的前提是「可以改變形狀去換取可展性」，
  而我們的三條生產路徑裡，需要精確的走剖面分切、不精確的本來就不做。
- 兩篇 2024 年的、值得日後看一眼的方向：
  - **Kerfing（在硬板材上開槽讓它彎）** —— ⭐ 這個跟珍珠板／木板**直接相關**，
    是「捲不起來的材料」的第三條路（既不是折、也不是切開再接）。
  - **用曲面流做網格近似再展開**。

（本節不足以支撐任何決定，**不要據以動工**。）

## 5. 逐項對照與過篩子結論

> 篩子：**「這個對板材切割／展開／剖面分切這條路，有沒有用？」**
> ⚠ 這裡只做「該不該學」的判斷，**排優先序在 `外部參考調查.md` 的結論表**，全部查完才寫。

| # | 項目 | 他們 | 我們 | 結論 |
|---|---|---|---|---|
| 1 | **演算法方向** | 先全部切開，再依序**試著合併**；合不起來就維持切開 | 從種子面長生成樹 | ⚠ **記著，先不翻。** 換方向的好處是「重疊從頭到尾不可能發生」，但我們的展開**已經在生產用了**，翻掉風險極高。**先做第 2、3 項，那兩項不必換方向也拿得到** |
| 2 | 🔴 **重疊回饋要分級，不能是布林** | 四級顏色；三態品質等級 | ~~`true`/`false` 一個值~~ → **三態** | ✅ **已做**（`4361556`）：乾淨／有重疊／太大沒檢查。⚠ 我們仍然沒有「攤不平（對偶圖有迴圈）」那一級 —— 我們的生成樹永遠攤得平，代價見第 6 項 |
| 3 | 🔴 **重疊偵測的沉默上限** | 沒有這種東西 | ~~`polys > 3000` 直接回 false 而且不說~~ | ✅ **已做**（`4361556`）：改成明講「面數超過 3000，重疊偵測跳過了 —— 這一片是『沒有檢查』，不是『沒有問題』」 |
| 4 | 🔴 **展開前的體檢** | 零長邊／零面積面／**扭曲的 n 邊形（門檻是面直徑的 1%）**／鏡射成內外相反；**而且會把問題元素選起來** | 沒有這道關 | ⭐⭐ **要做，而且很便宜。** `edit.js` 已經有 `facePlanarity()`、`degenerateFaces()`、`nonPlanarFaces()` —— **零件都在，只是沒有接到展開的入口** |
| 5 | ⭐ **相對容許值** | 「面直徑的 1%」「邊長 × epsilon」 | `PLANAR_TOL_CM = 0.01`（絕對值，cm） | ⚠ **不改。** 我們的絕對值是**刻意的**且理由更強：我們切的是實體板材，0.1mm 是**物理尺度**（鐵律三，坑 25／26）。相對值適合無單位的 CG，不適合下料 |
| ★ | 🔴 **它們展開時完全不看材料** | 沒有板厚、沒有 K 因子、沒有捲不捲得起來；補償（黏合片）是**攤平完成後才疊上去的一層** | ~~`canRoll` 決定走弧長還是弦長~~ | ✅ **已改成一致**（`4361556`，kang 2026-08-23 定調）：**展開尺寸 ＝ 網格攤平後的總和，與材料無關。** ⭐ 這一項當初查的時候沒被列進表裡 —— 因為那時我們還以為「材料決定尺寸」是對的，所以看到它們沒有材料概念，只覺得是「紙模不需要」。**它其實是這份調查最重要的一個交集。** 完整定義見 `PROJECT_LOG.md`「尺寸的依據」 |
| 6 | 🔴 **一片必須是一棵樹（對偶圖無迴圈）** | 有迴圈＝紅色，不給出圖 | 生成樹走不到的邊**自動變成隱含切割線** | ⭐ **要講出來，不一定要改行為。** 我們現在**替使用者決定了額外的接縫在哪裡**，而且圖上看不出哪幾條是他標的、哪幾條是程式加的。最低限度：**隱含切割線要在圖上跟指定分片區分開** |
| 7 | 🔴 **「鎖住這條邊，自動演算法不准動」** | 綠色邊 | 沒有 | ⭐ **要做，但要等第 8 項先做。** 沒有自動分刀就沒有東西需要鎖 |
| 8 | **自動分刀** | 兩邊都有，且都是「先切光再合併／再拆刀」 | **完全沒有** | ⚠ **想做，但先別動。** 兩邊的約束都跟紙模綁著（面數上限、黏合片、繞軸成圈），**對板材不成立**。要做得先問一句：kang 的分片決定是照什麼？ → 這是**下一輪要問的問題，不是現在能寫的規格** |
| 9 | **接縫優先序帶「邊長」項** | 長邊優先合併 ＝ **優先切在短邊**，接縫總長短、對位誤差少 | 只看夾角 | ⭐ **便宜且有用。** 我們的 MinHeap 已經在排序，加一個長度項就好。⚠ 但這會改變現有圖的接縫位置 → **要有開關，而且要 kang 先看過對照** |
| 10 | **凹邊優先切** | 凹的懲罰是凸的兩倍 | 沒有分凹凸 | ⚠ **紙模邏輯，我們不一定成立。** 銑 45° V 溝是從外面銑，凹凸的難易跟紙不同。**先問 kang** |
| 11 | ⭐ **合併前先算 phantom，全部檢查通過才動資料** | 一律如此 | `extrudeFace()` 是「湊好再一次建」，同一個精神；但展開流程沒有 | ⭐ **觀念要保留。** 跟研究線一的 `preflightRebuild` 是同一件事 |
| 12 | ⭐⭐ **快路徑 ＋ 安全路徑 ＋ 自動降級** | 掃描線碰到退化情形 → 丟例外 → **改用暴力法重跑，並永久記住這一片不安全** | 沒有。`detectOverlap` 只有一條路，撐不住就放棄 | ⭐⭐ **這是整份文件裡最值得學的「做法」**，而且正好解掉第 3 項：超過 3000 片時不是回 false，是**降級**（或至少誠實回報） |
| 13 | **排版** | 每片先轉到外框最矮 → branch and bound 求最少頁數 → **15 秒 timeout** | 沒有，一片一頁 A4 | ✘ **不做。** kang 的 DXF **先進 Illustrator 做整體處理**，排版在那裡（`PROJECT_LOG.md` 已因同一理由取消「板材尺寸上限檢查」）。⭐ 但 **timeout ＋ 拿目前最好的解**這個模式值得記 |
| 14 | ⭐ **編號上下顛倒會誤讀就加句點** | `69NZMWpbqd`／`80oOxXIl` 判定 | 沒有 | ⭐ **要做，十行的事。** 我們有接合編號，而**下料後的板子會被翻來翻去** |
| 15 | **非流形邊挑「法向最接近的一對面」當主面** | 有 | `fromFaceList` 記 issue，`twin` 只配對一次 | ⭐ 便宜的容錯。但要先確認我們真的會遇到（布林走 Manifold、匯入 SVG 是擠出）→ **先不做，記著** |
| 16 | **黏合片（glue flap）** | 兩邊都是核心功能 | 沒有 | ✘ **不做。** 我們是銑 45° V 溝或切開再接。**過篩子第一個被刷掉的** |
| 17 | **Build Sections ＋ 組裝順序編號** | PolyZamboni 有，且有獨立驗證 | 有接合編號，沒有組裝順序 | ⚠ **可能有用，要問 kang。** 「依序讓 CNC 切割後再組裝結合」那句原話裡有「**依序**」兩個字 |
| 18 | **Define Region（選一組面，一次切開外圍）** | 有 | `cutAroundFace()` 是它的**單面版本** | ⭐ **多選做完就順手能做**（見研究線一第 4 節）|
| 19 | **「用既有分類自動切」**（它用材質） | 有 | 沒有 | ⚠ 我們沒有材質，但**有 `smooth` 旗標與共面區域** —— 同一個想法可以是「沿著真轉角自動切」。**記著** |
| 20 | **改動切割會清掉版面，而且事先警告** | 有 | 我們沒有版面 | ✘ 不適用 |

---

## 接下來去讀程式的哪一支

- 分片標記 → `建模器/js/unfold/seam.js`
- 攤平、生成樹、重疊偵測、接合編號 → `建模器/js/unfold/flatten.js`
- 七種材料規則 → `建模器/js/unfold/rules.js`
- 出圖（螢幕／列印／SVG 三者必須一致）→ `建模器/js/out/sheet.js`
- DXF → `建模器/js/out/dxf.js`

## 相關文件

- `外部參考調查.md` —— 界線、篩子、五段格式（**先讀**）
- `外部參考-Blender編輯.md` —— 研究線一
- `外部參考-Pepakura.md` —— 範圍與介面
- `規格\建模器-展開與分片.md` —— 已經做出來的展開與指定分片
