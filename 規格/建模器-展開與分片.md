# 建模器：展開、材料規則、指定分片

> **這份回答**：展開引擎怎麼分層（規則分開、幾何共用）、七種材料各回答哪些問題、
> **板材與金屬到底差在哪**、曲線帶與圓弧帶怎麼認出來、為什麼有些形狀數學上展不開、
> 以及**指定分片**（接縫位置由使用者決定）與接合編號。
>
> **這份不回答**：**展開尺寸的依據是什麼** → `PROJECT_LOG.md`「尺寸的依據」
> （那條規則的家在那裡，本檔只引用不重抄）
>
> **這份不回答**：
> - 剖面分切（切成很多片疊起來）→ `規格\建模器-剖面分切.md`
> - STL／3D 列印 → `規格\建模器-STL匯出.md`
> - 半邊結構、參數體、K 因子存法 → `規格\建模器-核心架構.md`
> - 貼合與對齊用的那套點選 → `規格\建模器-編輯與組裝.md`
>
> 🔴 **這份最重要的一條（2026-08-23 定調，`4361556`）**：
>
> > **展開尺寸 ＝ 網格攤平後的總和。與材料無關。**
>
> 模型就是網格，網格裡沒有曲面。seg 段的「圓柱」就是一根 seg 邊柱，
> 展開寬 ＝ seg 片平板相加。要更接近真正的圓就**把 seg 開高**。
> **完整定義在 `PROJECT_LOG.md`「尺寸的依據」那一節 —— 那裡是這條規則的家，
> 本檔不重抄，對不上時以那裡為準。**
>
> ⚠ **本檔曾經寫著另一句話，已作廢，不要照它推**：
> 「板材捲不起來，**弦長就是正確答案**，不需要修正成弧長。」
> 結論碰巧一樣（都是弦長），但**理由完全不同** ——
> 舊的拿「材料捲不捲得起來」當判準，新的是「網格就是真值，跟材料無關」。
> **理由錯了會傳染**：照舊理由推下去，紙材就該改用弧長，
> 於是同一個模型換材料就換尺寸。
>
> 🔴 **這不是鈑金工具**（這條仍然成立）。材料是紙材、發泡板、珍珠板、
> 木板、壓克力，**一種金屬都不用**。
> 〔照鈑金推論下去，角柱會長 11.07%。經過見 `HISTORY.md` 坑 14、15〕
> 但要記得界線：**材料決定「做不做得出來」，不決定「數字是多少」。**
>
> **識別碼（給 Grep 用）**：`js/unfold/rules.js` ／ `js/unfold/flatten.js` ／
> `js/unfold/part.js` ／ `js/unfold/seam.js` ／ `canFold` ／ `canRoll` ／ `allowance` ／
> `margin()` ／ `validate` ／ `MATERIALS` ／ `arcCorrection()` ／ `applyStretch()` ／
> `EDGE_ROLE.CUT` ／ `edgeIsCut()` ／ `autoMarkFolds()` ／ `bake()` ／ `isCurve` ／
> `he.smooth` ／ `nearestMarkableEdge` ／ `nearestFace` ／ `JOIN` ／ `nonDevelopable` ／
> 角虧 ／ 高斯絕妙定理 ／ 接合編號 ／ 中性層 ／ K 因子

---

## 展開架構：規則分開，幾何共用

**第 3 期已照這個分層實作完成**（`unfold/rules.js` ＋ `unfold/flatten.js`），
沿用專案既有原則「入口分開、動作邏輯共用」。

```
┌─ 材料規則（各自獨立，互不相干）────────────┐
│  鈑金規則   帆布規則   板材規則   紙模規則  │
│  哪裡能折？補償多少？餘量？檢查什麼？        │
└────────────────┬───────────────────────────┘
                 │ 只把「規則」傳下去
┌────────────────▼───────────────────────────┐
│  幾何核心（完全不知道材料是什麼）            │
│  區域合併 → 曲面分類 → 攤平 → 重疊偵測 → 分片│
└────────────────────────────────────────────┘
```

**判準**：這段邏輯需不需要知道材料是什麼？需要就獨立，不需要就共用。

| 需要知道材料 → 獨立 | 不需要 → 共用 |
|---|---|
| 哪條邊能折、哪條必須切 | 把相鄰的面攤平 |
| 折彎補償、縫份、黏合片 | 偵測攤平後有沒有重疊 |
| 檢查規則（最小折邊等） | 重疊了要怎麼切分 |
| 輸出圖面樣式 | |

每個規則模組只需回答**五個**問題（2026-08-22 從四個增為五個）：
`canFold(角度)` / `canRoll` / `allowance(角度, 內側R)` / `margin()` / `validate(片)`
**新增材料 ＝ 在 `MATERIALS` 多加一筆，幾何核心一行都不用動。**

已實作的七種。**預設是珍珠板**（2026-08-22 從 `steel` 改過來）：

| 材料 | 折？ | 捲？ | K | 特性 |
|---|---|---|---|---|
| **珍珠板／發泡板／薄木板** | ✔ | ✘ | — | **預設**。銑 45 度 V 溝可折，展開走外皮弦長 |
| 紙模打樣 | ✔ | ✔ | 0.50 | 黏合片 1.0cm |
| 壓克力／木板 | ✘ | ✘ | — | 所有折線強制轉切割線，各片分開下料 |
| 帆布／軟膜 | ✔ | ✔ | 0.50 | 縫份 1.5cm |
| 鐵板／鍍鋅鋼板 | ✔ | ✔ | 0.40 | 目前用不到，規則保留 |
| 不鏽鋼 | ✔ | ✔ | 0.45 | 同上 |
| 鋁板 | ✔ | ✔ | 0.35 | 同上。內R 小於板厚會裂（會警告） |

**珍珠板那一筆填的是前六種都表達不出來的格子：折得起來，但捲不起來。**
金屬與帆布兩者皆可，壓克力／木板兩者皆否。
少了這一筆，就沒有東西能描述「銑 45 度 V 溝折起來的珍珠板」。

⚠ **但「捲不捲得起來」已經不影響尺寸了**（2026-08-23，見下方 `canRoll` 那一節）。
原本這裡寫著「少了這一筆，做角柱會被當成滾圓，展開長往多錯 4～11%」——
**那個後果現在不會發生**，因為尺寸一律是網格真值，
不管你填什麼材料，角柱都給弦長。
這一欄現在純粹是**材料事實的描述**，留著是為了日後做補償層時用得到。

`canFold` 只問尖角折。**圓弧折彎不問** —— 它是滾出來的，
總角度 360 度的圓筒照樣做得出來，拿「折不折得起來」去問它會誤報。

**尚未實作、但規劃時就想清楚的兩件差異**（第 5 期以後再補）：
帆布可以「少切幾刀」（軟料能硬拗，不必每個轉折都切開）；
紙模的黏合片目前只在標題欄提醒「圖上未含，下料時另加」，
還沒有真的畫出黏合片的形狀。

**最難的是「重疊偵測與自動分片」**，佔整個引擎約 70% 的工作量，而且是純幾何。
現在只做到**偵測與告知**。

⚠ **原本這裡寫「自動分片排在第 7 期」—— 第 7 期已取消，而且自動分刀已明確不做**
（`PROJECT_LOG.md`「✘ 明確不做」）：兩個開源展開器的自動分刀判準全部是幾何，
而 kang 的判準一個都不在幾何裡。**分片是製造決定，手動分切是正確答案。**

**重疊偵測回三態**（2026-08-23）：乾淨／有重疊／**太大沒檢查**。
最後那一態很重要 —— 片數超過 3000 會跳過偵測，
而「沒有檢查」跟「沒有問題」是兩件事，不能用同一個 false 表達。

### 曲線帶：先問上游，再猜（2026-08-22 新增，`b2e9d24`）

底下那套「圓弧修正」是為**圓柱、圓角方塊**寫的 —— 它靠
**等寬 ＋ 等角**的連續段認出圓弧，而那些形狀的每一格本來就一模一樣。

Illustrator 畫的自由曲線每一段長度和角度都不同（實測一個 S 字：
段長 0.001～7.47cm、角度中位數 3.75°），所以**一格都認不出來** ——
196 道折彎全部被當成尖角折，展開圖變成一團數字。

現在多一條路：**連續的平滑折線（`he.smooth`）＝ 一條曲線帶**，
不管每一格寬不寬、角度等不等。兩條路並存：

| 形狀來源 | 走哪一條 |
|---|---|
| 參數體（圓柱、圓角方塊、折板） | 幾何猜測（等寬等角）—— 它的格子本來就整齊 |
| 匯入的線稿 | 上游帶下來的 `smooth` 標記 |

**曲線帶跟真正的圓弧要分開**（`isCurve`）。圓弧有唯一的半徑，
標「R3」師傅看得懂；自由曲線每一段曲率都不同，**沒有一個 R 可言**。
標一個算出來的假半徑比不標更糟（坑第 20 條：正確的數字，錯誤的意思）。
圖上標成「曲線 37.18 cm、50 段、共轉 204.39°」。

曲線帶**不做拉伸** —— 沒有單一半徑就算不出真弧長，硬給一個是騙人。

### 認出圓弧帶：**只為了標註，不改任何尺寸**

> ⚠ **這一節以前叫「圓弧修正：這一期真正的重點」，內容是把弦長拉長到
> `r × Θ`。那件事在 2026-08-23 已經整個拿掉**（`applyStretch()` 整支刪除）。
> 理由見 `PROJECT_LOG.md`「尺寸的依據」。

攤平是剛體運動，**長度一格都不變** —— 所以攤出來的就是答案，
沒有東西需要被拉長。圓弧在網格上是一串直線段，那串直線段
**就是這個模型本身**，不是「圓的近似」。

那為什麼還要認出「哪幾條折線同屬一段圓弧」？**三件事，沒有一件會動到尺寸**：

1. 圖上把整條帶畫成**一個折彎區**，而不是 32 條各自標註的折線
2. 標「R25、32 段、共轉 360°」，而不是 32 個 11.25°
3. `arcW`（視為理想圓的周長）留給**報告**用 ——
   「網格 156.83／視為理想圓 157.08／差 0.25」。
   ⛔ **那是資訊，不是修正。** 看到差額不要以為是誤差要補回去。

**怎麼認**：抓「**等寬 ＋ 等角**」的連續段。
⚠ 這一招只對參數體有效（圓柱、圓角方塊的格子本來就整齊）；
匯入的自由曲線走 `he.smooth` 標記那一條路（見「曲線帶」）。

**驗證方式仍然是交叉對答案**，只是右邊換了：
`flatten.js` 從網格辨識 vs **網格真值手算 ＝ `bendDevelopedLength()` − Σ弧弦差**。
`bendDevelopedLength()` 沒有被廢掉，只是**降級成「理想圓弧那一側的參考值」**，
不再是圖面尺寸的權威。測試裡另外釘死一條：**公式一定大於網格，兩者本來就不該相等。**


## 板材與金屬的差別（2026-08-22 新增，這一節最容易被忽略）

kang 的原話：「跟鋁與鐵**只是接合方式不同**而已」、
「像**摺紙**一樣把展開圖組合起來」。

兩者都能讓材料保持一整片，但**展開長度的算法完全不同**：

| | 金屬折彎 | 板材（銑 45 度 V 溝） |
|---|---|---|
| 長度基準 | **中性層**（板厚中間某一層） | **外皮**（外側表面） |
| 公式 | `BA = θ × (ri + K×t)` | 各面外緣尺寸直接相加 |
| 板厚的作用 | 決定中性層位置 | 決定 V 溝寬度（90 度時 ＝ 2t） |
| 進不進展開圖 | **進** | **不進** |

外皮在折的時候長度不變，所以 100mm 的立方體、10mm 板，
四面側板攤開就是**剛好 400mm**，在 100／200／300 的位置各銑一道
20mm 寬的 V 溝（尖端落在外皮上）。

**板厚不進展開圖**，因為 45 度斜接的本質就是每一片都切到外緣尺寸，
斜面自動把板厚吃掉。板厚在這個專案只剩三個用途，全部在展開圖之後：
畫面上看起來像真的、STL 匯出加厚、V 溝要銑多寬（而最後那個是現場的事）。

### 邊的處理方式（kang 說明，程式**不管**這一層）

| 處理 | 結果 |
|---|---|
| 45 度**切到底** | 兩片完全分開，斜面對接 |
| V 溝**不切到底**（留薄皮） | 保持一片，折得起來 |
| 壓痕／壓線（紙材） | 保持一片 |
| 黏貼邊 | 某片邊上額外加料 |

kang 明確劃了界線：「**只要做到準確地展開，邊的處理方式是 CNC
與實際操作的範圍**」。所以程式不做 V 溝深度、留幾 mm 薄皮、
壓痕工具、黏貼邊寬度這些 —— 那些是現場決定的。

### ⛔ `canRoll` 已經不影響任何尺寸（2026-08-23）

> ⚠ **這一節以前寫的是「捲得起來 vs 捲不起來」，說 `canRoll` 決定
> 展開長度走弧長還是弦長。那已經作廢。**

**舊的說法**：網格上的一段「圓弧」是一圈平面小面，這些面到底代表
「真的被捲圓的曲面」還是「一圈平板拼出來的多邊形」，幾何分不出來
（坑第 10 條），只有材料分得出來 —— 所以問 `canRoll()`。

**為什麼作廢**：那個問題**問錯了**。模型就是網格 ——
那些小面不「代表」什麼，它們**就是**這個模型。不需要分辨。

而錯的理由會傳染：它讓同一個模型換材料就換尺寸，
於是圖上沒有一個數字有單一意義。

**現在**：`arcCorrection()` 一行都不問材料，`applyStretch()` 整支已刪除。
`canRoll` 欄位還留在 `rules.js`（材料表本來就該描述這個事實，
而且拿掉要動一票測試與舊檔相容），但**幾何核心不准再問它** ——
`rules.js` 那一段有 ⛔ 標記。

**軟性材料真的會伸長怎麼辦**：那是**疊在真值之上的獨立一層**，
照 `margin()` 的現成樣板做（「圖上未含 X cm，下料時另加」）。
**那一層目前不存在，而且刻意不預先開鉤子。**
kang 的順序：「先把真實的依據做正確，再去處理材料上的誤差數值」。

### 幾何核心現在只問材料三件事

**沒有一件會改變任何長度**（權威清單在 `js/unfold/flatten.js` 檔頭）：

| 問什麼 | 影響到什麼 |
|---|---|
| `canFold()` 這條邊折不折得起來 | 改變**片數**（壓克力被拆成一片片） |
| `validate()` 檢查（最小折邊等） | 只講話 |
| `k` / `thickness` 算內側 R | 只是**標註**（⚠ 金屬中性層概念，待辦排著要收起來） |


## 為什麼一定要切分（數學根據）

高斯絕妙定理：曲面展開成平面時高斯曲率不變。所以：

| 類型 | 例子 | 能否無失真展開 |
|---|---|---|
| 可展曲面 | 平面、圓柱、圓錐、稜柱、稜錐 | 能，有精確解 |
| 不可展曲面 | 球面、環面、自由曲面 | **數學上不可能** |

不可展的只有三條路：切分成近似可展的小片、允許材料變形、加剪口。

程式用**角虧**（angle defect ＝ 360° 減去頂點周圍所有夾角）判斷：
≈0 攤得平、>0 像球面外鼓、<0 像馬鞍。這是離散版高斯曲率。
**封閉曲面的角虧總和恆等於 4π ≈ 12.5664**（高斯–博內定理），
接錯一條邊就湊不出這個數字，所以它同時是最強的拓撲驗證工具。


## 指定分片（2026-08-22，`js/unfold/seam.js`）

**接縫位置終於是使用者的決定，不再是攤平走訪順序的副產品。**

一個立方體可以六面展成一片（銑 45 度 V 溝折起來）、拆成三片、
或六片全分開。**選哪一種是製造決定，不是計算結果** ——
少一片就少一道對位誤差，但要多一道銑溝工序。

### 用法

工具列「展開」那一組的「分片」按鈕。進入之後：

| 動作 | 結果 |
|---|---|
| 點稜線 | 從那裡切開／取消（橙色線 ＝ 已標記）|
| 點面的中央 | 那個面整圈切開；再點一次收回 |
| 右側面板 | 「展開後 N 片、已標記 M / 全部 條邊」|

**物件必須先「轉成可編輯網格」**，否則擋下來並說明原因。理由見下。

### 四個決定

**1. 只做「強制切開」，不做「強制折起來」**

標成 CUT ＝ 我要它切開，多切一刀一定做得出來。
取消標記 ＝ **交還給材料規則決定**，不是強迫它折起來。

這個不對稱是刻意的。若允許強制折，使用者就能在壓克力上標一條折線 ——
而壓克力物理上折不起來，那張圖做不出來。
**現在這個設計不可能產生做不出來的圖**，測試有一項專門盯它
（清除標記後換壓克力，照樣拆成 6 片）。

**2. 只有已烘成網格的物件能標**

參數物件存檔只存 `{type:'box', w:60...}`，開檔時網格重新生成、
`autoMarkFolds()` 重跑，標記就沒了。而它在**同一次開著的時候又是好的**
（`_mesh` 有快取）—— 這種「用起來正常、存檔重開才發現不見」是最糟的失敗。

所以正確順序是：**尺寸先確定 → 轉成可編輯網格 → 標分片 → 出圖。**
代價是尺寸也回不去了（`bake()` 不可逆），所以順序不能顛倒。

**3. 只開放「畫面上看得見的稜線」**

方塊 18 條邊裡有 6 條是三角化產生的共面對角線，畫面上看不到
（`scene.js` 畫稜線用 `EdgesGeometry(geometry, 1)`，只畫轉折 > 1 度的）。
讓人標到看不見的邊，結果是「一個面被斜切成兩半」——
正確但絕對不是他要的，而且他不知道自己點了什麼。

判準：**畫面上看得見的，才是可以標的。** 兩者必須是同一組，
否則畫面就在騙人（跟平面規劃器「命中判斷與繪製必須用同一個函式」同一條教訓）。

**4. 點選用「命中點的 3D 座標」，不用 raycast 的 `faceIndex`**

`faceIndex` 是三角化之後的三角形編號，要對回半邊網格得另外維護一份對照表，
而那份表一旦跟 `toGeometry()` 的順序脫節就會**靜靜地指錯面**。
座標是幾何事實，不依賴任何編號。

而且命中點必定落在**朝向鏡頭的那一面**上，所以背面的邊不會被誤選 ——
這是免費送的。

「是否算點中」一律用**螢幕 px 距離**判斷（桌機 14、觸控 26），
這樣拉遠拉近手感一樣。3D 距離只用來挑候選。

### 零幾何改動

展開幾何核心（`flatten.js` / `rules.js` / `part.js`）與 `core/` 全部沒動。
這一項只做兩件事：把某些邊的 role 改成 CUT、把結果畫出來。
分片之所以會變，是因為 `edgeIsCut()` 本來第二件事就是尊重 `EDGE_ROLE.CUT`。

### 接合編號（2026-08-22 補上）

**「切割後再組裝結合」的後半段。** 每一對接合邊給同一個號碼，
A 片的 ③ 對上 B 片的 ③ —— 就是紙模型的慣例。

- 綠色數字畫在邊的中點，**往料裡面縮**
  （外面已經被尺寸與折彎標註佔滿了，再擠進去就是坑第 12 條那團字）

#### 🔴 「往哪一側縮」的判準是**落在料上**，⛔ 不是「往重心那一側」

〔2026-08-26 修。**舊版是「往重心那一側」，那對環形片是錯的。**〕

⚠ **環形端面（管的兩個端面）的重心在洞的正中央** ——
所以內圈那一半的號碼被往洞裡推，**印在會被挖掉的料上**。

🔴 **實測（kang 重現：管 r25/20、高 70、段數 32、紙模打樣 0.2）**：
**66 個號碼裡有 32 個落在內半徑 20 以內，也就是洞裡。**
⭐ 他回報的「疊在一起」只是連帶症狀 —— 那一堆被擠進更小的圈裡。

⭐ **環形片沒有 `holes` 資料**（實測 `holes.length === 0`）：
它的 `outline` 是一條**從外圈穿過缺口繞到內圈再繞回來**的封閉線
（66 點 ＝ 外圈 32 ＋ 內圈 32 ＋ 缺口 2）。
**所以問那條線就對了** —— 它本身已經把洞排除掉。

⚠ 兩側都在料上（很窄的片）或兩側都不在時，**退回舊的重心規則**。
⛔ 不要在那種情況硬選一邊（坑第 24 條）。

#### 🔴 塞不下才縮字（`1.8 → 1.4 → 1.1 → 0.9`）

⚠ 環形端面的料只有 **5 公分寬**要塞 **66 個號碼** —— 物理上塞不下。
逐級試，**第一個不擠的就用它**。

⛔ **不是全面改小** —— 本來就不擠的片一格都不動：

| 片 | 修之前 | 修之後 |
|---|---|---|
| 環形端面 ×2 | 洞裡 32 個 ＋ 壓 13 對 | **洞裡 0 個**、壓 1 對、字縮小 |
| 內側面 125×70 | 壓 30 對 | **0 對**（字 1.8 → 1.4）|
| 外側面 156×70 | 0 對 | **0 對，字高沒動** |

🔴 **規則是「本來就不擠的不要動它」，⛔ 不是「側面都別動」** ——
內側面**本來就在壓 30 對**，所以它該縮。
〔⚠ 我第一次把測試的期望值寫成「兩片側面都維持 1.8」，**測試當場抓到**〕

#### ⭐ 下游放寬了判準：kang 用 AI 開 DXF／SVG

> kang 2026-08-26：「**會用 AI 繪圖開 DXF 或是 SVG**」

所以字**不必在紙上直接讀得出來** —— 他會在 Illustrator 裡放大看。
→ **字縮小可以接受**、**缺口那剩下的 1 對也可以接受**。

⚠ **⛔ 不要據此把字再往下調** —— 「列印」那顆按鈕還在，
真的直接印的時候字太小還是讀不出來。

⚠ **缺口處會剩 1 對**（`131↔131`、`132↔132`）：同一個號碼的兩端在缺口碰頭，
跟字大小無關，**kang 說沒關係**，這一輪沒解。

#### 🔴 為什麼這個 bug 活了三天沒被測試抓到

「標註不可以疊在一起」那一組**只比同一排的 y**（它是為尺寸那一排寫的），
而**接合編號是散在片內各處的** —— 那組檢查根本碰不到它。
→ 已另外加一個**真正的 2D 外框相交**檢查，以及「一個號碼都不可以落在料外」。
- 標題欄加一行「接合編號 N 處　同號碼的邊接在一起」
- DXF 多一個 **`JOIN` 圖層** —— 它是**給組裝的人看的，不是給機器的**，
  進 Illustrator 重新排版時可以先關掉，排完再開回來

**⚠ 接合編號與「一樣的片合併成一張圖」本質上互斥。**

合併只看形狀。方塊的頂面與底面形狀一樣會被併成一張圖，但它們接的是
不同的邊 —— 一張圖只能標一組號碼。**任何「說明這片裝在哪裡」的標示
都會讓合併失效**，這不是實作問題。

kang 的決定：**分切過的就不合併，全部給編號。**
判準是「**使用者有沒有手動標過分片**」，不是「展開結果有沒有超過一片」——
壓克力箱體是材料規則拆開的，用後者當判準會靜靜改掉那條天天在用的路。

**最重要的性質：每個編號恰好出現兩次。**
出現一次 ＝ 有邊找不到對象、接不回去；三次以上 ＝ 撞號、會接錯。
兩種都是「圖看起來完全正常，但東西組不起來」，而那要切完料才會發現。
編號用 `min(he.id, twin.id)` 當 key，所以兩側各自跑到那裡時一定同號。

### 尚未支援

- **板件（折板）的分片沒有驗過。** 板件畫在畫面上的是加厚後的殼
  （`scene.js` 的 `mesh.shell()`），命中點落在殼上而不是中性面上。
  板厚 0.2cm 的偏移很小，理論上仍然可用，但**沒有驗過，不要當成支援了**。
- ✅ ~~編號擠在一起~~ **2026-08-26 已修**（見上方「往哪一側縮」與「塞不下才縮字」）。
  ⚠ **查下去才發現真正的病是「印在洞裡」**，疊在一起只是連帶症狀。
  kang 的實測截圖：圓柱蓋接縫兩側的 `67` 疊住、圓環的 `112` 與 `137` 疊住。
  修法現成（坑第 12 條的「錯開兩排」）。
  〔本條原本寫「真的遇到再做 —— 不為想像中的問題先寫程式」。
  　那個判準沒有錯，只是**條件成立了**：它已經不是想像中的問題。〕

  ⚠ 順帶釐清：**同一個號碼出現在同一片上是正常的** ——
  圓盤被切開一條縫，縫的兩側要接回自己（測試裡「側邊條自己接自己」那一項）。
  問題只是畫在一起看不清，不是編號配對錯了。


---

## 接下來去讀哪裡（程式才是最終權威）

`建模器/js/unfold/rules.js`（材料規則·**新增材料就改這裡，幾何核心一行不動**）、
`js/unfold/flatten.js`（攤平幾何核心·**完全不知道材料是什麼**）、
`js/unfold/part.js`（轉接層與備料 CSV）、`js/unfold/seam.js`（分片標記與點選幾何）、
`js/out/sheet.js`（展開圖·決定畫什麼）、`js/out/dxf.js`（DXF R12）、
`js/ui/unfoldPanel.js`（介面）。
改過就跑 `node 建模器/tests/run.mjs`。**行號會漂，一律以現況程式為準。**

⚠ **DXF／SVG 沙箱驗不了** —— 一定要 kang 用 Illustrator 開過。
見 `PROJECT_LOG.md`「鐵律」五。

## 原文出處

`PROJECT_LOG.md` 四節合併，2026-08-23 逐字搬移、語意未改，順序改成閱讀順序：
「展開架構：規則分開，幾何共用」→「板材與金屬的差別」→「為什麼一定要切分（數學根據）」
→「指定分片」。施工流水帳見 `HISTORY.md` 各同名節。
