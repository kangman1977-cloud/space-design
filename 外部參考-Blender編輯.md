# 外部參考：Blender 的編輯邏輯

最後更新：2026-08-24　V2.0（新增第 9 節：第 6 期剩下六個工具 ＋ 沙箱實測）

> **來源全部是第一手**：Blender 原始碼與它自己的設計說明文件。
> 讀過的檔：`bmesh.hh`／`bmesh_class.hh`／`bmesh_core.hh`／`bmesh_mods.hh`／
> `bmesh_marking.hh`／`bmesh_log.hh`／`editors/transform/transform.hh`／
> `editors/transform/transform_orientations.cc`／`editors/mesh/editmesh_undo.cc`
>
> ⚠ **官方線上手冊（docs.blender.org）抓下來幾乎全是側欄目錄，正文抓不到。**
> 所以**第 1～7 節**完全走原始碼，沒有依賴手冊。
> 這反而更好 —— 原始碼的註解本身就在講「為什麼」。
>
> 🔴 **第 9 節（2026-08-24 新增）來源不同，可信度也不同。**
> 那一次 `raw.githubusercontent.com` 與 `docs.blender.org` **全部逾時抓不到**，
> Blender 那一半只好走搜尋與手冊敘述，是**二手資料**。
> 但第 9.7 小節是**我們自己的沙箱實測**，第一手 ——
> 而且它翻掉了兩個原本排好的計畫。**細節見第 9 節開頭那段警告。**

> **這份文件的主要讀者是 AI，不是人。**
>
> 🔴 **界線與篩子在 `外部參考調查.md` 第 2 節，這裡不重抄**
> （同一條規則只寫一次）。動手前先讀那一份。
>
> 🔴 **這份文件裡不出現任何一行 Blender 原始碼。**
> 只寫三種東西：①它的資料結構長什麼樣（用我們的詞描述）
> ②它的處理順序與判斷條件（散文，或**我們自己寫的**偽碼）③為什麼那樣設計。
> 理由有兩個，而且兩個都成立：
> **GPL 有傳染性**（貼了片段，日後有人照抄進 `js/`，整個建模器就得以 GPL 釋出）；
> 以及 **語法對我們沒用** —— 我們是 JS ＋ 半邊網格，Blender 是 C++ ＋ BMesh，
> 抄了也接不上。kang 2026-08-23 原話：「我們要調查的是**邏輯**..不是程式語法...
> 因為我們建模器的語法跟 Blender 是不同的..但**邏輯架構行為是相通的**」。

---

## 1. 現況基準：我們現在到底怎麼做

> 🔴 **這一節全部是「實際讀程式讀出來的」，不是照日誌抄的。**
> 五段格式裡「我們現在怎麼做」如果來自日誌描述，「差在哪」就是假的 ——
> 2026-08-23 那五次推論失誤全是這個病（`pushFace()` 那個不存在的開關就是這樣寫出來的）。
>
> 讀過的檔：`core/mesh.js`(813) `core/edit.js`(624) `core/region.js`(319)
> `core/history.js` `unfold/seam.js`(322) `unfold/flatten.js`(1469 部分)
> `view/select.js`(894) `js/main.js`(935 部分) `ui/toolbar.js`(1218 部分) `index.html`

### 1.1 網格結構

| 項目 | 現況 |
|---|---|
| 結構 | 半邊網格。`Vertex{id,p,he}` / `HalfEdge{id,v,face,next,prev,twin,role,smooth}` / `Face{id,he,normal,region}` |
| 「一條邊」 | 兩條半邊。`edges()` 只回傳 id 較小的那條 |
| 邊界 | 補一條 `face=null` 的半邊並串成迴圈（`_buildBoundaryLoops`），所以走訪碼不必到處判 `twin===null` |
| n 邊形 | **撐得住**。`fromFaceList` 吃任意長度，`roundBox` 實際有 20 邊形的面 |
| `box` | 走 `THREE.BoxGeometry` → **12 個三角形、18 條邊**。`tube`／`roundBox`／`plate`／`extrude` 是手工 `fromFaceList` 建的，**有四邊形** |
| 元素上的旗標 | 只有兩個：`role`(FREE/FOLD/CUT) 與 `smooth`。**沒有 select、沒有 hidden、沒有通用 tag** |
| 兩個旗標的分工 | `role` 回答**製造問題**（切/折/不管）；`smooth` 回答**形狀問題**（造型的一部分，還是離散化的產物）。刻意不合併 |
| 索引 | 沒有常駐表。要用時現建 `Map(id→索引)`（`_vertIndex()`） |
| 標記搬移 | 一律用**頂點索引配對**，不用 id（id 每次重新編號）。入口是 `_copyMarksTo()` |
| 自我檢查 | `validate()` 回 V/E/F/χ/封閉/連通元件數。χ 判定用 `2c−2g`，不寫死 2 |

### 1.2 🔴 沒有任何「改變拓撲」的 API

`mesh.js` 的方法全部是四類：**讀取、標記、整體變換、建構**。
沒有 split edge、沒有 collapse、沒有 dissolve、沒有 add/remove face。

因此**唯一改變拓撲的路徑是「拆掉重建」**：湊出 `points[]` 與 `faces[][]`，
再叫 `Mesh.fromFaceList()`。`edit.extrudeFace()` 就是這樣做的，
而且必須自己把標記搬回去（蓋子內部的邊索引配對整組換掉，`_copyMarksTo()` 接不到）。

### 1.3 使用者眼中的「一個面」

不是三角形，是 **`planarRegions()` 算出來的共面區域**（泛洪，預設容許 0.5°）。
它是「貼標籤」（寫 `face.region`）不是重建網格，所以**可逆、跑幾次都不會弄壞資料**。

`region.js` 另外做曲面分類，用**角虧**（離散高斯曲率）分成
`PLANAR` / `DEVELOPABLE` / `NON_DEVELOPABLE`。

### 1.4 選取

| 層級 | 現況 |
|---|---|
| **物件層** | `sel.ids` 是**有順序的陣列**；`get active()` 取**最後一個**，gizmo 掛它、屬性面板顯示它 |
| **子元素層** | `sel.editSel` 是**單一一個**。**不能多選，也沒有 active 的概念**（只有一個，無從分主從） |
| 過濾器 | `editFilter`：`auto` / `vertex` / `edge` / `face`。做成**按鈕**不是快捷鍵 —— 平板沒有鍵盤（跟分片、框選同一個決定） |
| auto 的順序 | **點 → 邊 → 面**（小的優先）。指定 only 時**點不到就回 null，不往下掉** |
| 命中判定 | 3D 距離只用來挑候選，**是否算命中一律用螢幕 px**（桌機 14px／觸控 26px），所以拉遠拉近手感一樣 |
| 命中來源 | **命中點的 3D 座標**，不用 raycast 的 `faceIndex`（那是三角形編號，會跟 `toGeometry()` 脫節） |
| 模式 | `editMode`／`seamMode`／`mateMode`／`marqueeMode` **互斥**，進一個就 `exitOtherModes()` |
| 選取標示 | 面畫的是**共面區域的邊界邊**（`regionBoundaryEdges`），不可拿 `elementVerts()` 那份頂點串迴圈（Set 出來的順序是任意的，會畫成蝴蝶結） |

### 1.5 變換（🔴 三個正交概念我們只有一個半）

底層是 three.js 的 `TransformControls`。

| Blender 的三個概念 | 我們 |
|---|---|
| **種類** | 物件層有 translate／rotate／scale（W／E／R）；**編輯模式強制 translate**（`_attachEditProxy()` 寫死切回去） |
| **方向** | `tc.space` 有 `local`／`world` 兩種，**但編輯模式沒有任何介面切它**，實際就是世界 XYZ。**沒有 Normal 方向** |
| **中心** | **沒有這個概念**。編輯模式一律是 `elementCenter()` ＝ 涉及頂點的**重心**，寫死 |

編輯模式的做法：放一個空的 `Object3D`（`_proxy`）在元素重心上，掛 gizmo，
**用增量位移**（這一幀減上一幀）寫回頂點座標 —— 不能用絕對值，因為重心會跟著跑。
`_proxy` 掛在 node 底下，所以座標系直接就是網格自己的，不必手動 `worldToLocal`。

沿面法向推拉的 `pushFace()` **存在、測試涵蓋得到、但沒有任何介面呼叫它**。

吸附：位移 1／5／10cm 可選（或關），旋轉 15°，縮放 0.05。

### 1.6 互動模型（🔴 最大的落差）

**兩步式，不是 modal：**

```
按按鈕 → 立刻以固定值執行 → 結果選中 → 再去拖 gizmo 調整
```

擠出就是這個形狀（kang 拍板的方案 C）：按一下先長「一個吸附格距」，
新蓋子自動選中，再用已經驗過的「拉面」調長度。
好處寫在 `main.js`：**擠出不需要新的拖曳邏輯，一個動作一件事**。

沒有的東西：即時預覽、確認/取消（右鍵取消）、拖曳中鎖軸、拖曳中直接打數字。

**快捷鍵**（`main.js` 第 755 行起，全部在這裡了）：

| 鍵 | 作用 |
|---|---|
| `W`／`E`／`R` | 移動／旋轉／縮放（物件層 gizmo 模式） |
| `F` | 全部入鏡 |
| `Delete`／`Backspace` | 刪除選取 |
| `Esc` | 清除選取 |
| `Ctrl+Z` / `Ctrl+Shift+Z` | 復原／重做 |
| `Ctrl+D` / `Ctrl+A` / `Ctrl+S` | 複製／全選／存檔 |

🔴 **編輯工具一個快捷鍵都沒有**（擠出、過濾器、拉點線面全靠按鈕）。

點／拖分辨：移動 8px 內、450ms 內算「點一下」。拖 gizmo 時關掉視角旋轉。

### 1.7 Undo

`history.js`：**整份文件的 JSON 字串快照**（`JSON.stringify(_get())`），
每次 `commit(label)` 推一份完整快照，走過 undo 又動作就砍掉分岔。
粒度 ＝ **一次操作一份完整文件**，沒有分層、沒有增量。

`silent()` 用來執行「不該被記錄」的變動（套用快照時自己觸發的那些）。

### 1.8 改完幾何之後的連帶重算

`refreshAfterEdit()`，順序**不能換**：

```
computeNormals() → remarkFolds() → demoteSmooth() → nonPlanarFaces()/degenerateFaces()
```

後兩者都要問 `dihedral()`，而它讀 `face.normal`。法向沒先算，後面全是拿舊資料判斷。

- `remarkFolds()` 跟 `mesh.autoMarkFolds()` 有兩個刻意的差別：**CUT 一律不動**（那是使用者的決定）、**會清掉過期的 FOLD**（autoMarkFolds 只加不減）。
- `demoteSmooth()` **單向，只關不開**。關掉是安全的（頂多多一道折線），打開是危險的（會漏掉一道折線）。
- ⚠ **這一整支不可以放進拖曳的每一幀**，它走訪所有的邊，是 O(邊數)。放手時跑一次。

---

## 2. BMesh 的結構與設計取捨

**來源**：`bmesh.hh`（BMesh 的設計說明文件，寫在檔頭）、`bmesh_class.hh`（結構定義與註解）。

### 它怎麼做

BMesh 自我定位是「**non-manifold boundary representation，為了支援進階編輯而設計**」。

**四種元素：頂點、邊、Loop、面。**
🔴 **面不直接指向頂點或邊，全部透過 Loop。**

`Loop` ＝「**面角**」（face-corner）。一個 Loop 綁三件事：一個頂點、一條邊（從自己的頂點連到下一個 Loop 的頂點）、一個面。

三條環狀串列撐起整個拓撲：

| 環 | 掛在哪 | 回答什麼 |
|---|---|---|
| **face cycle**（loop 的 next／prev） | 面 | 這個面有哪些頂點與邊。**串列的方向就是面的繞向，反轉串列＝翻面** |
| **radial cycle**（loop 的 radial_next／prev） | 邊 | 這條邊被哪些面用。**環狀，所以沒有第一個／最後一個**，邊只記其中任一個 loop |
| **disk cycle**（邊上兩個 disk link） | 頂點 | 繞著這個頂點有哪些邊 |

**旗標分成兩層，而且有一條鐵律：**

| 層 | 內容 | 誰能碰 |
|---|---|---|
| **header flags**（持久） | 選取、隱藏、**接縫（SEAM）**、平滑、暫用 tag | 一般 API |
| **tool flags**（每次 operator 各自配置一組） | operator 自己的暫時標記 | 只有該次 operator |

> 🔴 **「operator 絕對不准讀寫 header flags」**（原文用 `never` 加粗）。
> operator 只透過具名的 input/output slot 拿到要處理的元素清單。
> 少數例外（例如查詢面是不是 smooth）被特別記下來當成例外。

**走訪一律走抽象層**：一般查詢走 iterator API；需要堆疊的（遞迴、擴散）走 **walker API**，
而 **walker 支援 delimiter flag** —— 呼叫端可以標記「走到這種元素就停」。

**刻意允許的「不合法」狀態**：一條邊可以被三個以上的面共用；兩個頂點之間可以有多條邊
（但「使用者永遠不該看到」，只能是編輯中的暫態）；甚至支援 2 邊形的面
（原文：「只能當中間步驟，不該留在最終網格裡」）。

### → 為什麼那樣設計

- **Loop 存在的理由有兩個**：讓「面→頂點/邊」與「邊→面」兩個方向都是 O(1)；
  以及給 **per-face-per-vertex 的資料**（UV、頂點色）一個家 ——
  同一個頂點在不同面上可以有不同的 UV，那份資料不屬於頂點，也不屬於面。
- **non-manifold 是刻意的**：編輯過程中一定會產生中間狀態。若資料結構禁止，
  每個工具就得「先在外面組好，再一次性換掉」。BMesh 選擇**允許暫時無效、由呼叫端負責收尾**。
- **兩層旗標**：因為「使用者的狀態」和「演算法的暫時記號」是兩種壽命不同的東西。
  混用會讓一個 operator 執行完之後把使用者的選取洗掉。

### → 我們現在怎麼做

半邊網格。`he.v／face／next／prev／twin`。等價於 BMesh 的 **Loop ＋ 一個只能有兩個成員的 radial cycle** —— 因為 `twin` 是單一指標，所以**結構上強制二流形**。

- **沒有 disk cycle**。`vertOutgoing()` 用 `prev.twin` 繞，而它自己的註解寫著「碰到邊界，走不下去」→ **邊界頂點只走得到半邊的扇形**。
- 旗標只有 `role` 與 `smooth`，**沒有分持久／暫時兩層**。
- 沒有 iterator／walker 抽象，各檔自己寫泛洪迴圈（`region.js` 的 `planarRegions`、`mesh.js` 的 `componentCount`、`flatten.js` 的 `splitPatches`、`edit.js` 的 `boundaryLoops` —— **同一個模式寫了四次**）。

### → 差在哪 ／ 該不該學

| # | 項目 | 結論 |
|---|---|---|
| 1 | **non-manifold 容忍度** | ✘ **不學。** 布林走 Manifold 函式庫本來就保證二流形；我們的編輯是「拆掉重建」，沒有中間態。但要記住這是我們**只能拆掉重建**的根本原因之一 |
| 2 | **walker ＋ delimiter** | ⭐ **值得做。** 「從這個面出發，一直擴散到遇到接縫為止」＝ 我們的分片、展開分片、共面區域、連通元件**全都是這個**，已經各寫一次。抽一支帶停止條件的走訪函式，四處都能收斂 |
| 3 | **兩層旗標** | ⚠ **記著，先不動。** 我們的 `role` 同時裝「使用者標的 CUT」和「算出來的 FOLD」，已經逼出 `remarkFolds()` 那段「CUT 一律不動」的特例 —— 那正是 Blender 用兩層旗標避開的問題。但改它要動存檔格式，代價高 |
| 4 | **資料層不三角化** | ⭐ **值得修，而且很便宜。** BMesh 只在**算繪時**三角化。我們只有 `box` 被迫三角化（借 `THREE.BoxGeometry`），其他參數體都是手工 `fromFaceList` 建的、本來就有四邊形。`mesh.js` 撐得住 n 邊形（`roundBox` 有 20 邊形面）。→ **`box` 應該手工建成 6 個四邊形**，`planarRegions()` 每次重跑泛洪去還原「使用者看到的面」，修的是一個在建構時就可以不要發生的問題 |

---

## 3. 變換的三層分解（種類 × 方向 × 中心）

**來源**：`transform_orientations.cc`（完整讀過）、`transform.hh`。

### 它怎麼做

**方向（orientation）＝ 一個 3×3 矩陣**，來源有九種：

| 種類 | 矩陣怎麼來 |
|---|---|
| GLOBAL | 單位矩陣 |
| LOCAL | 物件矩陣正規化 |
| **NORMAL** | 從選取推導（見下） |
| GIMBAL | 目前旋轉模式的尤拉軸 |
| VIEW | 相機矩陣反矩陣 |
| CURSOR | 3D 游標的旋轉 |
| PARENT | 父物件的矩陣 |
| CUSTOM／CUSTOM_MATRIX | 使用者存的 |

九種之間有**明確的退化鏈**：Gimbal 算不出來 → 落到 Parent → 落到 Normal → 落到 Local → 落到 Global。
**永遠有答案，永遠不會沉默地什麼都不做。**

🔴 **NORMAL 的推導規則（這一段是本節重點，而且我們可以直接照做）**

先從選取算出兩個向量：`normal`（將成為 Z 軸）與 `plane`／切線（將成為 Y 軸），
再用「Z ＝ normal、Y ＝ tangent、X ＝ 兩者叉積」建一組正交基底。

| 選到什麼 | normal（Z） | plane（Y） |
|---|---|---|
| **有 active 元素且中心＝Active** | 就用那**一個**元素的法向與切線，不平均 | 同左 |
| **≥1 個面** | 所有選取面的法向**相加** | 每個面算一組候選切線（兩條），選「與法向叉積比較長」的那一條 |
| **剛好 3 個頂點** | 三點的三角形法向；若與三點的頂點法向反向就翻過來 | 三點之間**最長的那條選取邊** |
| **1 條邊 或 2 個頂點** | 兩端頂點法向相加，再**投影到與邊垂直的平面上** | 沿邊方向，**指向 active 頂點**；邊界邊用固定繞向 |
| **1 個頂點** | 頂點法向 | 若剛好連兩條邊，取兩條邊方向的和；否則沒有 → 退回只用法向 |
| **>3 個頂點** | 所有選取頂點法向相加 | 沒有 → 退回只用法向 |

退化保護：normal 與 tangent 共線 → 換一個正交的 fallback；normal 全零 → 單位矩陣。

**中心（pivot）有五種**：外框中心／**重心（預設）**／3D 游標／各自的原點／**active 元素**。

🔴 **而且中心會回過頭影響方向**：算方向的函式**吃 pivot 當參數** ——
中心設成 Active 時，方向只取 active 那一個元素的法向，不做平均。
**兩個概念名義上正交，實作上方向依賴中心。**

### → 為什麼那樣設計

- 三者分開之後，「沿法向擠出」**不是一個獨立功能**，是「移動 × NORMAL × 任意中心」的一個組合。
  否則每加一種需求就要多一個寫死的工具。
- **切線挑「最不共線」的那一條**是實務修出來的（原始碼註記了 bug 編號），
  理由是共線會讓矩陣建不起來。這正是我們鐵律二「凡是把 2D 搬到 3D 平面的地方都要問一次繞向對不對」的同一家族 —— **退化情況要當第一等公民處理，不是例外**。

### → 我們現在怎麼做

只有 **移動 × 世界 XYZ × 重心（三者全部寫死）**。

`pushFace()` 實際上就是「NORMAL 方向」的一個特例，但它被寫成一支**獨立函式**而不是一個方向選項 —— 所以它接不上 gizmo，也就永遠不會有介面。

### → 差在哪 ／ 該不該學

🔴 **這是整個研究線一裡最直接可用的一條。**

「擠出好像沒用」「斜面推不動」「拉面拉不出梯形」根源都是**只有世界 XYZ 一種方向**。
而 three.js 的 `TransformControls` 本來就有 `space` 屬性 ——
**我們只要算得出那個 3×3，箭頭就能朝法向。**

過篩子之後建議的範圍（**刻意只取一小塊**）：

| 概念 | 建議做 | 建議不做（對板材切割沒用） |
|---|---|---|
| 種類 | 移動（已有）＋ **旋轉／縮放**（做梯形、收尖要用） | — |
| 方向 | 世界（已有）＋ **法向** | Gimbal／View／Cursor／Parent／Custom |
| 中心 | 重心（已有）＋ **選取元素本身** | 外框中心／3D 游標／各自原點 |

法向的推導只需要「面」「邊」「單一頂點」三種 —— 因為我們的子元素選取本來就只有一個。

⚠ 連帶要處理的：`select.js` 的 `_attachEditProxy()` 現在**寫死把 gizmo 切回 translate**
（註解理由是「把一個頂點旋轉 30 度沒有意義」）。那句對**點**成立，對**面**完全不成立。

---

## 4. 選取模式與 active element

**來源**：`bmesh_marking.hh`（選取 API 全文）、`bmesh_class.hh`、`transform_orientations.cc` 裡實際取用 active 的那一段。

### 它怎麼做

- **選取狀態是元素上的一個 header flag，不是一份清單。**
- 另外維護一份 **select history**：一個鏈結串列，每筆記「元素指標 ＋ 型別」。
  🔴 **最後一筆就是 active。**
- 取 active 的函式**有後備**：select history 空的時候用 `act_face`。
  而 `act_face` **即使沒被選中也保留**，原始碼寫明理由：UV 編輯器要一直顯示 active face 的圖，
  否則使用者在 3D 視窗改選取時，UV 那邊的圖會**閃爍**。
- 三種選取模式（點／邊／面）是**位元組合，可以同時開**。
- 🔴 **flush（往上灌）**：一條邊的兩個頂點都被選 → 那條邊自動算被選；
  一個面的邊全被選 → 那個面自動算被選。往下灌是選用的。
- 🔴 **select mode clean**：換模式時清掉「這個模式撐不住的」選取 ——
  邊模式下一個孤立的選取頂點必須連到一條選取邊，否則清掉。
- 要拿「前 n 個選取元素」時，**優先從 select history 反向取**，
  而且要求**連續同型別**，否則整個放棄、退回掃全網格。
  → **「使用者最後點的順序」是有意義的資料，不是副產品。**
- 網格被重建時，用一份對照表（targetmap）把 select history 搬過去；
  另有 validate（元素可能已被刪掉）與 backup／restore 巨集。

### → 為什麼那樣設計

- active 做成「歷史的最後一筆」而不是一個獨立欄位 →
  **取消選取時自然退回上一個**，不必寫任何特別處理。
- flush 讓三種模式共用同一份底層狀態，不必為每個模式各維護一份清單。

### → 我們現在怎麼做

- **物件層**：`ids` 是有順序的陣列，`active` 取最後一個。
  ⭐ **這在概念上就是 select history**，我們已經有了。
- **子元素層**：`editSel` 只能存**一個**。沒有多選、沒有歷史、沒有 flush。
- 過濾器是**互斥的四選一**（auto／點／邊／面），不是可組合的位元。
- 網格被換掉時（Undo／讀檔／擠出）**整個清掉**，理由寫在 `revalidate()`：
  舊的 Vertex／HalfEdge／Face 參考還活著，拖曳照樣「成功」，只是改的是一份已經不在文件裡的網格。

### → 差在哪 ／ 該不該學

| # | 項目 | 結論 |
|---|---|---|
| 1 | **子元素多選 ＋ 順序即 active** | ⭐ **值得做，而且我們物件層有現成寫法可抄。** 分片標記、對齊、貼合都想「一次選好幾條邊」。缺的只是一個容器 —— 有了容器，active 幾乎是免費的 |
| 2 | **flush（往上灌）** | ✘ **先不做。** 我們的過濾器是互斥的（平板只能按按鈕），沒有「三種模式同時開」的需求，flush 的價值大幅下降 |
| 3 | **active face 沒選中也保留** | ✘ **不做。** 那是為 UV 編輯器閃爍問題設計的，我們沒有 |
| 4 | **模式切換時清掉撐不住的選取** | ⭐ 我們現在是**全部清掉**（`setEditFilter` 直接 `clearEditSel()`）。多選之後這條就必須改成「清掉不合這個型別的，留下合的」 |
| 5 | 🔴 **網格重建時搬移選取（targetmap）** | ⭐ **多選一旦做了，這件事就必須一起做。** 我們現在的 `capFace` 就是一個一次性、只支援一個元素的 targetmap —— 擠出完手動補選那一片。多選之後手動補選不可行 |

---

## 5. Modal operator 的互動模型

**來源**：`transform.hh`（TransInfo、狀態列舉、modal keymap 全表）。

### 它怎麼做

一次變換是一個**狀態機**：`STARTING → RUNNING → CONFIRM ／ CANCEL`。

**開始（STARTING）時記下一份初始狀態**：起始滑鼠位置、中心（全域與螢幕座標各一份）、
方向矩陣與其反矩陣、以及**每一個受影響元素的初始座標**（`iloc`，「initial location」）。

🔴 **之後每一幀都是「從初始值重算」，不是在上一幀的結果上累加。**

RUNNING 期間可以接收的事件（全部有編號、可重新綁定的 modal keymap）：

```
取消 / 確認 / 中途換種類(移動·旋轉·縮放) /
鎖單軸 X·Y·Z / 鎖平面 X·Y·Z / 解除約束 / 自動約束 /
吸附開關 · 吸附反轉 · 加減吸附點 / 精細模式 /
數字輸入 / 比例編輯半徑增減 / 略過導覽(讓視角操作穿過去)
```

其他刻意的設計：

- **約束物件（TransCon）自己帶一段要顯示在狀態列的文字**，以及三個 apply 函式（向量／縮放／旋轉各一）。
- **滑鼠輸入被抽象成十幾種模式**（向量／彈簧／角度／軌跡球／水平比例／垂直絕對…）。
  精細模式有 **virtual cursor**：切換精細時以虛擬座標累加，**避免數值跳動**。
- 值存**三份**：`values`（modal 輸入）、`values_modal_offset`（額外偏移）、
  **`values_final`（最終值，給 redo 面板顯示）**。
- `T_RELEASE_CONFIRM`：從拖曳啟動的操作，**放開就是確認**。
- 重繪分 SOFT／HARD 兩級。

### → 為什麼那樣設計

- **「每幀從初始值重算」** 讓取消、鎖軸、換方向、換種類都可以隨時發生而**不留殘渣**，
  而且不累積浮點誤差。取消變成「什麼都不做」，是免費的。
- **最終值另存一份** → 操作完可以在 redo 面板改數字重跑，不必再拖一次。

### → 我們現在怎麼做

兩步式：**按鈕 → 立刻以固定值執行 → 結果自動選中 → 再拖 gizmo 調整**。

gizmo 拖曳走 three.js 的 `TransformControls`，位移**用增量累加**——
`_writeBackEdit()` 的註解寫明：拿絕對值算會每幀重複套用一次，一拖就飛出去，
因為頂點移動之後元素重心也跟著跑到 proxy 的新位置。

沒有：取消、鎖軸（軸靠「拉哪一根箭頭」決定）、拖曳中打數字、redo 面板。

### → 差在哪 ／ 該不該學

| # | 項目 | 結論 |
|---|---|---|
| 1 | 🔴 **記下初始座標，每幀從初始值重算** | ⭐⭐ **強烈建議學，而且理由不是手感、是正確性。** 我們用增量累加，正是因為「重心會跟著跑」。改成記 `iloc` 之後那個問題**自動消失**，而且取消、鎖軸、換方向全部變成免費的。這一條是後面所有互動改進的地基 |
| 2 | **拖曳中直接打數字** | ⭐ **便宜又直接有用。** 下料尺寸本來就是打出來的，不是拖出來的 |
| 3 | **redo 面板（做完可改參數重跑）** | ⭐ 對「擠出 3.7cm」這種事極有用，而且我們已經有 `commit(label)`，接得上 |
| 4 | **擠出改成 modal（拖著看）** | ⚠ **先不動。** 方案 C（按一下＋再用拉面）kang 已經實測通過。但要記一筆：日誌裡「拖曳時每幀重建網格會卡」的顧慮**從來沒實測過，只是推論**。要動之前先量（而且**要暖機再取多次最小值**） |
| 5 | 十幾種滑鼠輸入模式／軌跡球／virtual cursor | ✘ **不做。** 過篩子：我們只有移動與少數旋轉縮放 |
| 6 | **鎖軸 ＋ 鎖平面** | ⚠ 我們用「拉哪根箭頭」達成同一件事，而且平板上更好用。**平手，不急** |

---

## 6. 各編輯工具的定義與邊界情況

**來源**：`bmesh_core.hh`（kernel／Euler operators 全文）、`bmesh_mods.hh`（上層工具）。

### 它怎麼做

**所有改變拓撲的事，最終都落到七八個可逆的原子操作**（Euler operators）：

| 原子操作 | 做什麼 | 反向 |
|---|---|---|
| **SEMV** Split Edge Make Vert | 一條邊切成兩條，中間長一個新頂點 | JEKV |
| **JEKV** Join Edge Kill Vert | 兩條邊併成一條、殺掉中間頂點。**要求該頂點恰好連兩條邊** | SEMV |
| **SFME** Split Face Make Edge | 面內兩個角之間拉一條邊，把面切成兩個 | JFKE |
| **JFKE** Join Face Kill Edge | 共用**一條**邊的兩個面併成一個。共用多條邊會報錯 | SFME |
| **JVKE** Join Vert Kill Edge | 塌陷一條邊，**任意價數都行**（比 JEKV 通用） | — |
| **URMV** Un-glue Region Make Vert | 把一個面從頂點的扇形上撕下來，給它一個新頂點 | splice |
| vert／edge **splice** | 兩個頂點（兩條邊）焊成一個 | separate |
| vert／edge **separate** | 拆開成多個 | splice |

每一個都**明講前置條件，以及會產生什麼不合法狀態**。例如：

- JEKV「**有可能造出只有兩條邊的面。要拿它怎麼辦由呼叫端決定**」。
- SFME「輸入的兩個頂點可以在同一條邊上，這會做出一個兩邊形的面 —— **這是刻意的，導圓角（edge bevel）非它不可**」。
- JFKE 附了兩張圖，一張標 Ok、一張標 Wrong，並說明 Wrong 那種要先用 JEKV 處理掉多餘的邊。

🔴 **而且有一整組預檢函式**：塌陷／焊接／切割**會不會產生重複的面或重複的邊** ——
`*_check_double_edge`、`*_check_double_face`。**先問，再做。**

**刪除（Delete）與溶解（Dissolve）是兩個不同的指令：**

| | 做什麼 |
|---|---|
| **Delete** | 元素連同依附它的東西一起殺掉。刪一個面 → 留一個洞 |
| **Dissolve** | 元素移除，但**把周圍併起來**。溶解一個頂點 → 周圍的面併成一個多邊形；溶解一條邊 → 兩個面併成一個 |

溶解頂點併不成面時，會**退化成「塌陷那條邊」**，而不是失敗。

### → 為什麼那樣設計

- 幾何工具的正確性問題幾乎都是**拓撲問題**，而拓撲問題可以被歸約到七八個可逆的原子操作。
  上層工具（擠出、內插、導圓角、環切）全部是這些原子的組合，所以**只有原子需要被證明是對的**。
- **「先檢查會不會產生重複」比「做完再修」便宜得多**，而且做完常常修不回來。

### → 我們現在怎麼做

🔴 **一個 kernel 都沒有。** `mesh.js` 的方法全是讀取／標記／整體變換／建構。

唯一改變拓撲的路徑是 **`Mesh.fromFaceList()` 拆掉重建**。
`extrudeFace()` 就是手工湊出 `points[]` 與 `faces[][]` 再重建，並且要**自己處理標記搬移**
（蓋子內部的邊兩個端點索引全換，`_copyMarksTo()` 接不到，必須另外再搬一次）。

**沒有任何預檢。**

### → 差在哪 ／ 該不該學

| # | 項目 | 結論 |
|---|---|---|
| 1 | **整套 Euler operators** | ✘ **不建議照搬。** `edit.js` 檔頭自己寫了理由：「半邊手術要從零長出一整層，接線錯了不會報錯，只會產生一個結構壞掉的網格」。而第 6 期剩下的四個工具**用拆掉重建都做得出來**，只是慢 |
| 2 | ⭐ **「刪除 vs 溶解」的區分** | ⭐⭐ **一定要學，而且是設計層面的。** 使用者按「刪除面」想要哪一個？**答案不唯一** —— 那正是鐵律三「設計一個操作時先問這個操作的結果唯一嗎」。Blender 的答案是**做成兩個不同的指令**，不是猜 |
| 3 | ⭐ **預檢的觀念** | ⭐ **值得做。** 拆掉重建之後「有沒有孤點／重複面／零面積面」我們已經吃過虧（`extrudeFace` 註解：「孤點不會報錯，只會讓頂點數對不上、尤拉數算錯」）。應該有一支 `preflightRebuild(points, faces)`，在 `fromFaceList` 之前跑 |
| 4 | **合併頂點**（對應 JVKE／splice） | ⭐ **最便宜的一個。** 拆掉重建時把兩個索引換成同一個就好 |
| 5 | **刪除面** | ⭐ 便宜。拆掉重建時少放那幾片 |
| 6 | **環切 Loop Cut**（SEMV ＋ SFME 繞一圈） | ⚠ 四個裡**次貴**。難的不是切，是「找出一圈邊」—— 那正是第 2 節說的 **walker**，我們沒有 |
| 7 | 🔴 **導任意一條邊的圓角 Bevel** | ⚠⚠ **最大的未知，而且這次調查照出一個硬阻礙：** Blender 的 edge bevel 底層**需要「兩邊形的面」當中間步驟**（SFME 的註解特別點名）。而我們的 `fromFaceList` **直接跳過少於 3 個點的面**（會記 issue）。→ **Blender 那條路我們走不了，必須另外想一條。** 動工前一定要再查一輪 |

---

## 7. Undo 的粒度

**來源**：`editmesh_undo.cc`（編輯模式 undo 的完整實作，含大量設計註解與 bug 編號）、`bmesh_log.hh`（雕塑模式用的增量 log，**編輯模式不用它**）。

### 它怎麼做

🔴 **每一步 undo 確實是「整份網格的完整快照」**（BMesh 轉回 Mesh 的陣列形式）。
**跟我們一樣。**

差別在存進去之前多做三件事：

1. **BArrayStore 分塊去重。** 每個資料陣列切成塊，跟**上一個 undo step 的同一個陣列**比對，
   相同的塊共用、不重存。塊大小以位元組為目標算（64KB／至少 256 個元素）。
   > 原始碼的歷史註記：塊數原本固定 256，**選取狀態改成布林陣列之後負擔太大**，
   > 高面數模型會出現「管理大量小塊很慢、卡住使用者互動」，才改成以位元組為目標（附了兩個 bug 編號）。
2. **布林陣列先做 RLE 編碼再去重。** 因為布林值太不獨特，分塊去重效果差（附 bug 編號）。
   代價寫得很誠實：**即使什麼都沒變，也得先編碼才能比對**。
   實測數字也附了：3200 萬個布林值，隨機資料約 0.11 秒、均勻資料約 0.0025 秒。
3. 🔴 **壓縮丟到背景執行緒**（低優先的 task pool），**不擋使用者**。要讀回來時才等它做完。

另外：每一步 undo **還存了 selectmode** ——
原始碼寫明理由：「不能讓使用者 undo 之後，場景設定顯示的選取狀態跟他剛才在編輯的不一樣」。

### → 為什麼那樣設計

- **完整快照在正確性上最簡單** —— 不必為每一種操作寫一個反向操作。
  代價是記憶體；分塊去重把代價壓回接近增量的水準，**而且完全不動上層任何邏輯**。
- 壓縮是純資料處理、跟畫面無關，所以可以丟背景。

### → 我們現在怎麼做

`history.js`：`JSON.stringify(整份文件)`，每次 `commit()` 推一份。
沒有去重、沒有壓縮、**同步執行**。有一個 `bytes` 估算欄位（除錯用）。

### → 差在哪 ／ 該不該學

| # | 項目 | 結論 |
|---|---|---|
| 1 | ⭐ **「完整快照」這個方向是對的** | ⭐⭐ **最重要的一條收穫是「不用改」。** Blender 也是完整快照。**不要**為了省記憶體改成增量式，那會讓每一個工具都要寫反向操作 —— 而那正是 bug 的溫床 |
| 2 | **我們的快照是 JSON 字串** | ⚠ 比二進位陣列大一個數量級，而且 `stringify` 是**同步**的。剖面分切／描圖匯入那種上千頂點的模型，每 commit 一次就整份 stringify 一次。**這件事從來沒有量過** |
| 3 | 分塊去重 ／ RLE ／ 背景壓縮 | ⚠ **先量再說，而且這是最後手段。** 若真的卡，更便宜的兩步是：**只在快照真的變了才推**、以及**限制堆疊長度**。⚠ 量的時候要暖機、取多次最小值（`flatten.js` 的 MinHeap 註解那個教訓） |
| 4 | **undo 要一起還原「當時的模式」** | ⭐ 小事但值得做。我們的 `editFilter` 與模式（編輯／分片／貼合）**不在快照裡** —— undo 回去之後模式還是現在的，使用者會困惑 |

## 8. 過篩子的結論與建議優先序

> 🔴 **每一節末尾的「該不該學」就是本份的結論。**
> **跨三條研究線的總表與優先序在 `外部參考調查.md` 第 4 節** ——
> 同一條規則只寫一次，這裡不抄第二份（否則下一個人會很有信心地讀到舊的那一份）。
>
> 本份貢獻到那張總表的項目：
> 第 1 組地基 1-1（變換三層）／1-2（記初始值）／1-3（多選與 active）／
> 1-4（走訪函式）／1-5（預檢）；第 2 組 2-5（`box` 改四邊形）／2-7（Undo 存模式）；
> 第 5 組 5-1（**導圓角走不了 Blender 那條路**）／5-2（環切）。

---

## 接下來去讀程式的哪一支

- 網格結構與拓撲 → `建模器/js/core/mesh.js`
- 改變幾何、擠出、連帶重算 → `建模器/js/core/edit.js`
- 共面區域、角虧 → `建模器/js/core/region.js`
- 選取、gizmo、模式 → `建模器/js/view/select.js`
- 元素命中（不碰 DOM，測得到）→ `建模器/js/unfold/seam.js` 後半
- 按鈕接線、快捷鍵、擠出流程 → `建模器/js/main.js`
- Undo → `建模器/js/core/history.js`

## 相關文件

- `外部參考調查.md` —— 界線、篩子、五段格式、三個來源分工（**先讀**）
- `外部參考-展開與分片.md` —— 研究線二
- `外部參考-Pepakura.md` —— 範圍與介面
- `編輯功能設計方向.md` —— 第 6 期的定位、範圍、**被否決的方向**

---

## 9. 第 6 期剩下六個工具的邏輯（2026-08-24）

> 🔴 **來源可信度跟第 1～7 節不一樣，一定要先讀這一段。**
>
> 第 1～7 節走的是**原始碼第一手**。這一節不同：`raw.githubusercontent.com` 與
> `docs.blender.org` 這次**全部逾時抓不到**，所以 Blender 那一半改走
> **搜尋 ＋ 官方手冊敘述 ＋ 二手教學**，是**二手資料**。
>
> 但本節第 9.7 小節（我們自己的地基在這六個動作下會怎樣）**全部是沙箱實測**，
> 是第一手，而且**它翻掉了兩個原本排好的計畫**。
> 兩邊對不上時以 9.7 為準 —— 那是量出來的，不是讀來的。
>
> ⛔ 本節一樣不出現任何一行外部原始碼。

### 9.1 環切（Loop Cut）

**它怎麼做**：從一條邊出發，在每一個「**四價頂點**」（剛好連四條邊）上
走「對面那條邊」，一路走到底或繞回起點。碰到不是四價的頂點就**停**。
所以它**只穿得過四邊形** —— 三角形與 n 邊形上「對面那條邊」沒有定義。
預設切在中點，切完可以滑動；刀數可調。

**→ 為什麼那樣設計**：四價是「這條線可以無歧義地延伸下去」的充分條件。
不是四價時路徑不唯一，而**不唯一就不猜**（跟我們鐵律三同一條）。

**→ 我們現在怎麼做**：沒有環切，也沒有 walker。
🔴 **而且實測發現前提根本不成立**（見 9.7）：
16 段圓柱是 **64 個三角形、0 個四邊形**。**沒有東西可以走。**

**→ 差在哪／該不該學**：⭐ 要做，但**它不是下一步**。
必須先讓網格有四邊形 —— 而那件事有現成的解（見 9.4 的限制性溶解）。

### 9.2 細分（Subdivide）

**它怎麼做**：把選取的邊切成 N 段。難的是「**只選了一部分邊**」時那個面怎麼補：
Blender 給了四種 `Quad Corner Type`（Fan／Innervert／Path／Straight Cut），
讓使用者決定，**不自己猜**。
n 邊形則是：**邊被切開，但面本身不細分**。

**→ 為什麼那樣設計**：同一個「切兩條相鄰邊」的動作，補法有好幾種而且形狀差很多。
**結果不唯一 → 做成選項，不是猜一個。**

**→ 我們現在怎麼做**：沒有。

**→ 差在哪／該不該學**：⚠ **優先度比想像中低。**
我們的造型是幾何化多面體，要「把面切細好有點可調」的需求，
大部分可以用**擠出＋拉**達成。而細分那四種補法要先問 kang 才寫得出規格。
**先不做。**

### 9.3 內縮（Inset）

**它怎麼做**：沿著選取區域的邊界往**面內**偏移一圈，長出一圈新的面。
四個關鍵選項：

| 選項 | 做什麼 |
|---|---|
| **Individual** | 每個面各自內縮 vs 整片區域一起內縮 |
| **Boundary** | 開放邊要不要也內縮 |
| 🔴 **Offset Even**（**預設開**） | 讓內縮後的邊「四周一樣寬」 |
| Relative Offset | 寬度跟著周圍幾何的尺度走 |
| Depth | 內縮的同時往法向推（＝內縮＋擠出一次做完） |

**→ 為什麼那樣設計**：`Offset Even` 存在的理由就是**角落**。
只沿角平分線推 d，實際牆距會變成 `d × cos(半夾角)`。

**→ 我們現在怎麼做**：沒有內縮 —— **但我們解過同一個問題。**
🔴 `mesh.js` 的 `shell()` 加厚時踩過一模一樣的坑（坑第 7 條：折彎處被削薄），
它的註解寫著解法：**除以「頂點法向 · 相鄰面法向」**，並且
「夾角接近 180 度時餘弦趨近 0，推距會爆掉，所以設上限 5 倍」。
註解甚至已經點名這叫 **miter（尖角接合）**。

**沙箱實測**（方塊頂面，內縮 5）：
只沿角平分線推 → 實際牆距 **3.5355**（＝5×cos45°）；
除以 cos → **5.0000**，精確。

**→ 差在哪／該不該學**：⭐⭐ **值得做，而且解法是現成的。**
`Offset Even` ＝ `shell()` 那一段 miter 修正，抄過來就好。
`boundaryLoops()` 已經給出**有序**的邊界迴圈（實測頂面 1 圈 4 條，順序正確）。
**Depth 那一項尤其划算** —— 「內縮 ＋ 往內推」一次做完就是凹槽、面板開口、邊框。

### 9.4 刪除 vs 溶解（Delete / Dissolve）

**它怎麼做**：

| | 做什麼 |
|---|---|
| **刪除面** | 留一個洞，而且會**留下孤立的頂點與邊** |
| **溶解面** | 不留洞，把選取合併成**一個 n 邊形** |
| **溶解頂點** | 刪掉那個點，周圍的面併成一個 |
| **溶解邊** | 兩側的面併成一個 |
| 🔴 **限制性溶解**（Limited Dissolve） | **量面與面的夾角，低於門檻就合併** —— 尊重銳利邊 |

**→ 為什麼那樣設計**：使用者按「刪除面」想要哪一個？**答案不唯一**，
所以做成兩個不同的指令，不是猜。

**→ 我們現在怎麼做**：兩個都沒有。
🔴 **但「限制性溶解」我們等於已經有了半支** ——
`region.js` 的 `planarRegions()` 做的就是「量夾角、把共面的合成一區」
（預設容許 0.5°）。差別只在它是**貼標籤**，不是真的合併。

**→ 差在哪／該不該學**：🔴🔴 **這是整節最大的發現。**

「把每一個共面區域合併成一個 n 邊形」＝ 限制性溶解，
而三個零件**全部現成**：`planarRegions()`（哪些面共面）
＋ `boundaryLoops()`（有序的外緣）＋ `fromFaceList()`（重建）。

**沙箱實測（對答案通過）**：

| | 面數 | 體積 | 面積 |
|---|---|---|---|
| 方塊 | 12 → **6**（全是四邊形） | 108000 → **108000** | 13800 → **13800** |
| 32 段圓柱 | 128 → **34**（32 個四邊形 ＋ 2 個 32 邊形） | 78036.130979 → **78036.130979** | 10174.903617 → **10174.903617** |

**體積與面積精確不變**（小數點後 6 位全同）—— 合併只是把三角形併回它本來就
屬於的那個平面，不動幾何。

而它一口氣解掉三件事：

1. 待辦「`box` 改成手工建的 6 個四邊形」——**而且是通用解，不只 box**
2. **環切的前提**（9.1）：圓柱從 0 個四邊形變成 32 個
3. 「溶解」本身就是第 6 期要做的工具之一

⚠ **多迴圈的區域不能合併**：管的兩個端面是**環形**（外圈＋內圈兩個迴圈），
一個面裝不下兩個迴圈。實測 66 個區域裡剛好 2 個是這種，**要跳過**。

### 9.5 合併頂點（Merge）

**它怎麼做**：六種 —— 合到**中心**／**第一個選的**／**最後一個選的**／
3D 游標／**塌陷**（各自的島各自合到自己的中心）／**依距離**（自動焊接靠太近的）。

🔴 而「第一個／最後一個」**只有逐個點選時才出現**，框選時沒有 ——
因為那個順序來自 **select history**。

**→ 為什麼那樣設計**：合併之後那個點放哪裡，**結果不唯一**，所以列成選項。
而「使用者點選的順序」是有意義的資料，不是副產品。

**→ 我們現在怎麼做**：沒有合併頂點。
⭐ **但「最後一個選的」我們已經免費有了** —— 2026-08-24 做的多選就是
「順序即 active」，跟 Blender 的 select history 是同一個東西。

**→ 差在哪／該不該學**：⭐ 要做，但 🔴 **它不是最便宜的那一個 —— 我原本排錯了。**
理由見 9.7。

### 9.6 導角（Bevel）

**它怎麼做**：把一條邊換成一圈面。四組參數：

| 參數 | 說明 |
|---|---|
| **寬度的定義** | Offset／Width／Depth／Percent **四種**，同一個「寬」有四種量法 |
| **段數** | 1 段是斜切，多段逼近圓角 |
| 🔴 **斜接（Miter）** | **外側**（夾角 > 180°）與**內側**（< 180°）**分開設**，各有 Sharp／Patch／Arc |
| **Clamp Overlap** | 限制每條邊的導角不要跟鄰居自交 |

「Patch 斜接」的做法是：把兩條被導角的邊之間那個邊界點**拆成三個**，再拉兩段弧。

**→ 為什麼那樣設計**：**單獨導一條邊很單純，難的全在「相鄰的邊也被導」的角落。**
Blender 為此開了外側／內側兩組獨立選項 —— 那不是為了花俏，是因為那裡真的不唯一。

**→ 我們現在怎麼做**：只有 `roundBox` 這個參數體特例（只能倒方塊的四條垂直邊）。

**→ 差在哪／該不該學**：

🔴 **第 5 組 5-1 那個「Blender 那條路我們走不了」的阻礙，已經繞過了。**

原本的阻礙是「Blender 的 edge bevel 底層需要兩邊形的面當中間步驟，
而 `fromFaceList()` 直接跳過少於 3 點的面」。
**但那是它的實作路徑，不是導角的定義。** 用拆掉重建可以直接做出結果：

邊 `A—B`（兩側是面 F1、F2）→ `A` 拆成 `A1`(往 F1 偏)、`A2`(往 F2 偏)，
`B` 同理 → F1 改用 `A1,B1`、F2 改用 `A2,B2`、中間補一片 `(A1,B1,B2,A2)`。
**全程沒有兩邊形。**

**沙箱實測支持**：方塊 18 條邊裡有 **12 條**兩側屬於不同共面區域
（＝畫面上看得見的稜線，剛好等於可標記的 12 條）；
每個端點被 **3 個**區域共用 → 第三個區域吸收多出來的點，變成 n 邊形。
而 n 邊形實測撐得住（平面五邊形 `ok=true`；非平面五邊形也建得起來，
`facePlanarity()` 量得到偏離）。

⚠ **但角落斜接仍然是未知。** 第一刀的合理範圍：**一次導一條邊，
相鄰的邊擋下來並說原因**（跟擠出「只做封閉的那一種」同一個判斷）。

### 9.7 🔴 沙箱實測：我們的地基在這六個動作下會怎樣（第一手）

> **這一小節是本節最值錢的部分**，因為它**翻掉了兩個原本排好的計畫**。
> 全部是 2026-08-24 在沙箱實跑出來的，不是讀來的。

#### ① `fromFaceList()` 對壞資料**一律照建，不報錯**

把方塊的頂點 1 併到頂點 0（＝最天真的「合併頂點」寫法）：

```
V8 E20 F12 χ0 closed=false ok=false
issues:「邊 2→0 出現兩次（非流形）」…
```

**它建出來了。** 12 個面裡有 2 個退化成一條線、頂點 1 變成孤點，
而程式沒有任何一步擋下來。

→ 🔴 **`preflightRebuild()` 從「值得做」升級成「非做不可」** ——
接下來的工具**全部**走拆掉重建，而那條路現在**完全沒有檢查**。

#### ② `validate()` **不抓孤點**，只有尤拉數會露餡

把圓柱的共面區域合併成 n 邊形之後：

```
V66 E96 F34 χ4 closed=true ok=true　issues:（無）
```

`ok=true`、`closed=true`、沒有 issues —— **但 χ 是 4，封閉實體應該是 2。**
原因是端面扇形的 2 個中心點合併後沒人用了，變成孤點。

清掉孤點＋重新編號之後：`V64 E96 F34 χ2 ok=true`，體積面積精確不變。

→ 這正是鐵律三那條「**讓兩個數字互相對得起來，錯誤才會自己現形**」在運作：
`ok=true` 是孤零零的一個布林值，沒有人驗得了；**χ 是由 V、E、F 推得出來的**，
所以它一對不上就自己叫出來。

#### ③ 清孤點就要**重新編號**，而那會打破索引契約

「既有頂點保持原索引」是拆掉重建那條路的契約 ——
`role`、`smooth`、選取搬移全靠它。

實測：合併頂點之後**原頂點 7 變成索引 6**（位移了）。
圓柱那個案例剛好孤點在最後兩個（索引 64、65），**位移 0 個** —— 但那是運氣，
孤點在中間時一定會位移。

→ 🔴 **合併頂點是第一個會打破那個契約的工具**，
所以它**不是最便宜的**，而是那個逼我們把 targetmap 認真做出來的。
〔`edit.js` 那則 `remapElements()` 的墓碑註解，就是為它留的。〕

#### ④ 刪除面反而最乾淨

刪掉方塊頂面（少放 2 個三角形）：`V8 E17 F10 χ1 closed=false ok=true`，
**孤點 0 個、無 issues** —— 因為那 4 個點還被側面用著。

⚠ 代價要講清楚：網格變**開放** → 不能再做布林（`canBool` 擋開放件），
展開與 STL 的行為也會變。

#### ⑤ 內縮的角落：`shell()` 已經解過（見 9.3）

### 9.8 🔴 第二輪實測：把建議裡沒踩實的四項補掉（2026-08-24 同日）

> 第一輪之後，那張順序表裡**還有四處是推論**。kang 的原則：
> 「推理可以先做，但推理之後要去調查它成不成立，這樣真正執行時才會順暢。」
> 所以四項全部實測。**結果翻掉兩項、驗證一項、修正一項。**

#### ✘ ①「環切的 walker」—— 我把兩種 walker 搞混了

搜尋讀到的「在四價頂點走中間那條邊」是 **edge loop**（選一整圈邊）。
**環切用的不是它，是 edge ring**（穿過四邊形走「對面那條邊」）。

實測（四邊形化之後）：

| | ring 長度分佈 | 頂點價數 |
|---|---|---|
| 方塊 | **4(閉) × 12** | 全部 **3 價** |
| 32 段圓柱 | **32(閉) × 32**、2(閉) × 64 | 全部 **3 價** |

🔴 **方塊與圓柱的頂點全是 3 價** —— Blender 的 edge-loop walker 在上面**一步都走不了**。
⚠ **2026-08-25 導正**：原本這裡寫「我們的網格頂點全是 3 價」，**那是錯的** ——
圓錐 seg12 實測是 {3:10, 4:2, 5:2, 6:2}。環切不受影響（它只走四邊形），
但**導角的角落規則建在那句話上，被它害到了**（見第 11.7 節）。
但 **ring walker 走得非常好**：圓柱的 32 條垂直邊剛好繞成一個閉環，
切下去就是一圈水平的新邊。水平邊的 ring 只有 2 條（撞到端面的 32 邊形就停）——
**那是對的行為**，不是缺陷。

⭐ 而 ring walker 的實作只有一行：**四邊形的 `faceLoop` 取 `index + 2`**。
→ **環切比原本排的位置便宜得多，應該往前移。**

#### ✅ ②「區域內縮」—— 撐得住

頂面 ＋ 右側面（兩個相鄰但**不共面**的四邊形）→ `boundaryLoops()` 回
**1 圈 6 條**，共用的那條邊正確地不算邊界。

迴圈上有 **2 個點「屬於 2 個選取面」** —— 那是折角上的點，
內縮方向不唯一，要取兩個面的合成，**正是 `shell()` 那個 miter 情形**。

#### ✘ ③「刪除面最乾淨」—— 只測了一個案例就下結論

| 刪什麼 | 結果 | 孤點 |
|---|---|---|
| 1 個面（頂面） | χ1 ok=true | 0 |
| **一個角周圍的 3 個面** | χ2 ok=true | **1** |

→ 刪除面跟合併頂點是**同一類問題**，不是風險最低的那一個。

#### ✅ ④「導角」—— 真的建出來了，而且體積精確吻合

方塊 60×45×40，把長 60 的那條頂邊導 45 度斜切、寬 5：

```
繞向 (A1, A2, B2, B1) → 體積 107250.0000　手算 108000 − (5²/2)×60 = 107250　✅ 精確
繞向 (A1, B1, B2, A2) → 體積  99750　　　 非流形，issues 報「邊出現兩次」　✘
```

🔴 **繞向確定是 `(A1, A2, B2, B1)`** —— 錯的那個**畫面上完全看不出來**，
只有體積會露餡（坑第 29 條）。**這一項以後不要再推，直接用這個順序。**

⚠ 但它 `χ=4` 而 `closed=true ok=true`：原本的 A、B 兩個頂點沒人用了，變成孤點。

#### 🔴 一個貫穿全部的模式（這是第二輪最大的收穫）

**四個工具 —— 合併頂點、面合併、刪除面、導角 —— 全部會產生孤點，
而 `validate()` 一個都抓不到（`ok=true`、`closed=true`、沒有 issues），
唯一露餡的是尤拉數 χ。**

→ 所以 `preflightRebuild()` 的核心確定了：**清孤點 ＋ 對尤拉數**。
它不是「值得加的檢查」，是**拆掉重建這條路的必要配件**。

→ 而清孤點就要重新編號 → 打破「既有頂點保持原索引」的契約 →
**targetmap 也是必要配件，不是選項。** 兩者要一起做。

### 9.9 過篩子的結論與順序（兩輪實測之後）

| 順序 | 做什麼 | 為什麼是這個位置 |
|---|---|---|
| **0** | 🔴 **`preflightRebuild()` ＋ 清孤點 ＋ targetmap** | 四個工具全部會產生孤點，而 `validate()` 抓不到。三件是同一件事的三面 |
| **0** | ⚠ **`Delete` 鍵在編輯模式的防護** | 現在選一個面按 Delete，**整個物件會被刪掉** |
| **1** | 🔴 **面合併（限制性溶解）** | 零件全現成、對答案通過（體積面積小數點後 6 位全同），一口氣解掉 `box` 四邊形化、環切前提、以及「溶解」本身 |
| **2** | ✅ **環切　已完成（2026-08-24）** | ring walker 確實只有一行；⚠ 但**真正的工是「hard 邊」那條新規則**，不是 walker。實作見 `規格\建模器-點線面編輯.md` |
| **3** | ✅ **內縮　已完成** | 2026-08-25。四動作框架的試金石 —— **一行新數學都沒有**。⚠ 不做 Depth（改用擠出方案 C 的形狀：內圈那個面自動選中，深度交給「拉面 × 法向」）|
| **4** | **刪除面／溶解面** | 會產生孤點，要等第 0 批 |
| **5** | ✅ **導角（單段，可多條邊）　已完成** | 2026-08-25。重用內縮那一支，如預期幾乎是免費的。⚠ 多段（圓角）另計，**角落真的不唯一** |
| **6** | **合併頂點** | 最貴 —— 索引位移最嚴重 |
| ✘ | **細分** | 四種補法要先問 kang；擠出＋拉多半可以取代。先不做 |

### 9.10 ⚠ 到現在**仍然沒有驗證**的（不要當成已知）

> 照本專案的規矩：**查不到就明寫查不到，不要留白讓下一個人以為驗過了。**

| 項目 | 狀態 |
|---|---|
| ~~**導角的角落斜接（miter）**~~ | ✅ **已解（2026-08-25），見第 11 節。** 「不唯一」只對**多段導角**成立；**單段在 3 價頂點上是唯一的**。四個案例實測 χ=2 封閉，體積用兩條獨立的路對過答案。⚠ **4 價以上只導一部分會留洞，已擋下來**（見 11.7）；多段（圓角）的方案見第 12 節 |
| **內縮的 Individual 模式** | ⚠ 只驗過整片區域一起內縮，**每個面各自內縮沒驗** |
| ~~**環切切在哪、切幾刀、滑動**~~ | ✅ **已驗完（2026-08-24），環切已完成。** χ 仍是 2、體積面積精確不變、孤點 0 個、刀數 1／2／3 都驗過、半條 ring 合法（n 邊形只插點）。⚠ **滑動切點仍然沒做**（現在固定平均分佈，要挪就直接拉那一圈邊）。🔴 而且它翻掉了本節兩句話：「前提已經備好」（**不成立** —— 共面的線在原本的地基上看不見也點不到，逼出新的 `hard` 邊規則）、「一定會產生孤點」（**相反** —— 環切是四個工具裡唯一不產生孤點的）|
| **面合併對「幾乎共面但不完全共面」的行為** | ⚠ 容許值 0.5° 是 `planarRegions()` 的預設。合併之後那個 n 邊形會**不是平的**，展開會受影響 —— 沒測 |
| 第 9.1～9.6 的 Blender 敘述本身 | ⚠ **二手**（手冊與搜尋，抓不到原始碼）。動工前該再確認 |

---

## 10. 🔴 完整能力對照表（2026-08-24，**第一手**）

> **這一節回答 kang 的那個問題**：「我不清楚 Blender 到底跟我們的建模器還有哪些差異」。
>
> 🔴 **來源是官方手冊的操作索引本身，第一手**：
> `docs.blender.org/manual/en/latest/modeling/meshes/editing/` 與 `.../selecting/`。
> 〔第 9.1～9.6 節是二手的（那次抓不到），**這一節不是**。兩邊對不上以本節為準。〕
>
> **涵蓋範圍 ＝ Blender 編輯模式的全部操作**（85 支）**＋ 全部選取方式**（13 支），
> 一支都沒有跳過。用不到的也列出來並寫明理由 —— 照本專案的規矩：
> **查不到、不做的都要明寫，不要留白讓下一個人以為漏了。**
>
> ⛔ 一樣不出現任何一行外部原始碼，只記邏輯。

### 記號

| 記號 | 意思 |
|---|---|
| ✅ | **已經有**，括號裡是我們用什麼做到的 |
| ⭐ | **沒有，而且值得做** —— 對這個專案（板材切割 ＋ 3D 列印）真的有用 |
| ➖ | 沒有，而且**跟建模無關 → 不做** —— 它服務的是 Blender 的其他目的（動畫、算圖、細分曲面、UV、骨架）。🔴 **這一欄就是 kang 說的「Blender 很多功能都跟建模無關，我們不需要」那一堆**（2026-08-24 澄清，見 `編輯功能設計方向.md` 第 1 節）|
| ✘ | **明確不做**，括號裡是當初的理由 |
| ❓ | **沒調查過**，不要當成已知 |

---

### 10.1 頂點（Vertex）

> 🔴 **這一節修正過一次（2026-08-25），而修正的過程比內容重要。**
>
> 原本寫「連接兩點成邊（**J**）」＋「連接頂點路徑」，把兩者當成同一件事的
> 單點版與多點版。**讀官方手冊內文之後發現名字是反的，而且它們是兩個
> 不同的指令**：
>
> | | 行為 |
> |---|---|
> | **Connect Vertex Pairs** | 只連**共用同一個面**的兩點，**忽略選取順序**。沒有預設快捷鍵 |
> | **Connect Vertex Path（J）** | **跨多個面**，**依選取順序**沿路徑切開 |
>
> ⚠ **這正是 10.8 節自己標的「只抓了操作索引，沒有逐支讀內文」的代價。**
> 動到哪一項就去讀那一頁 —— 這一次就翻掉了一個設計假設。
>
> ⭐ 而且**是 kang 提醒才去查的**（他 8/25 說「你是否也可以搜尋是否有
> 甚麼可以解決的方法來做參考呢」）。鐵律一講的就是這件事。

| Blender | 我們 | 說明 |
|---|---|---|
| 移動／旋轉／縮放頂點 | ✅ | 「拉點線面」。⚠ 點的旋轉縮放刻意灰掉（一個點轉了不會有變化）|
| **擠出頂點** | ⭐ | 從一個點長出一條新的邊。畫骨架、加支撐點時要 |
| 擠出到游標 | ➖ | 我們沒有 3D 游標這個概念 |
| **頂點導角** | ⭐ | 把一個尖角削成一個面。⚠ 排在邊導角之後 |
| **連接兩點（Connect Vertex Pairs）** | ✅ | **2026-08-25 完成**。只連**共用同一個面**的兩個點，把那個面切成兩半。工具列「編輯」那組的 `連接兩點`。⚠ **沒有預設快捷鍵**（J 是下面那個）|
| **連接頂點路徑（Connect Vertex Path，J）** | ✘ | **跨多個面**，依選取順序沿路徑切開中間的幾何。🔴 **kang 2026-08-25 明確不做**（⛔ 不要重提）—— **結果不唯一**：方塊選頂面一角、底面另一頭一角，那條線要從側面哪裡過去？繞左、繞右、斜著過都說得通（坑第 24 條）。⭐ **真要跨面切，「刀具」才是誠實的答案** —— 畫到哪切到哪，而且是使用者自己決定從哪裡跨 |
| **撕開（Rip）／撕開並填補** | ⭐ | 把黏在一起的頂點拆開。⚠ 我們現在沒有任何「拆開」的動作 |
| **沿邊滑動頂點（GG）** | ⭐ | 把一個點沿著它所在的邊滑動，不離開表面 |
| 平滑頂點／Laplacian 平滑 | ❓ | 當初以「雕塑類」否決（第 5 節），但它確實是**建模編輯**功能。⚠ **邊界未定**，排到再問 kang，不要直接擋掉 |
| **由選取的點建面／邊（F）** | ⭐⭐ | 選 3～4 個點直接補一個面。⚠ **2026-08-27 更正：原本寫「補洞、封口非它不可」—— 那句已經不成立**，`補洞` 2026-08-25 就完成了（把每個**邊界迴圈**補成一個 n 邊形），⛔ 沒有靠這一支。→ **它現在補的是另一格**：補洞只認得「完整的一圈邊界」，**選任意幾個點自己拉一個面**還是做不到 |
| 頂點折線權重（crease） | ➖ | 服務細分曲面（Subdivision Surface），我們不做 |
| 混合形狀／Hook／頂點群組／頂點父子 | ➖ | 全部服務動畫與骨架 |

### 10.2 邊（Edge）

| Blender | 我們 | 說明 |
|---|---|---|
| 移動／旋轉／縮放邊 | ✅ | 「拉點線面」，含法向方向 |
| **導角（Bevel）** | ✅ | **單段（斜切）與多段（圓角）2026-08-25 都完成，含角落**（🔴 **2026-08-27 更正：原本寫「多段仍未做」，那是 8/24 的狀態**）。工具列的 `導角` ＋ 寬度 ＋ **段數**（預設 1、上限 32），可一次選好幾條邊，**只導一部分的邊也可以**（outer miter，Blender 三種接法裡選 Sharp）。⭐ 沒有專用的數學 ＝ 內縮 ＋ 加面。⚠ **非直角的角落仍然擋下來**（圓錐那類），那才是「角落不唯一」還沒解的那一半 |
| **橋接邊迴圈（Bridge Edge Loops）** | ⭐⭐ | 選兩圈邊，中間長出一段管狀連接。**做過渡段、接頭、變徑管** |
| **標記接縫（Mark Seam）** | ✅ | **就是我們的「指定分片」**（`EDGE_ROLE.CUT`）。⭐ 名字不同，做的事一樣 |
| 標記銳邊（Mark Sharp）／折線權重 | ➖ | 服務算圖的著色與細分曲面 |
| **沿面滑動邊（Edge Slide, GG）** | ⭐⭐ | **環切之後要挪切線的位置就是它**。現在只能用「拉邊」硬拉，會離開表面 |
| **擠出邊** | ⭐ | 從一條邊長出一個面。⚠ 我們的擠出只吃面 |
| **環切（Loop Cut）** | ✅ | 2026-08-24 完成。⚠ **Slide（切完滑動）沒做** |
| **平行複製一圈邊（Offset Edge Slide）** | ⭐ | 在一圈邊的兩側各加一圈。做加強筋、邊框 |
| 旋轉邊（Rotate Edge） | ⭐ | 把四邊形的對角線翻到另一邊。用來修「三角化切錯邊」 |
| **螺旋（Screw）** | ⭐ | 一條輪廓繞軸旋轉並上升 → 螺紋、彈簧、螺旋樓梯 |
| **細分（Subdivide）** | ⭐ 半 | ⚠ **2026-08-27 補：已經涵蓋掉兩塊** —— `面上加線`（＝ Subdivide 選兩條邊，把面切成兩等分，2026-08-25）與 `邊上加點`（＝ 只放點什麼都不連，2026-08-25）。**還缺的是「一次細分整個選取」那一種。** 🔴 **2026-08-24 從「不做」收回。** 原因是「四種補法（Fan／Innervert／Path／Straight Cut）要先問 kang」—— 那是**順序問題不是範圍問題**，而它是不折不扣的建模編輯功能 |
| 細分 edge ring | ✅ | ＝ 環切多刀。**我們的環切刀數已經涵蓋**（🔴 **2026-08-27 更正：它自己的說明就寫著「已經涵蓋」，記號卻標 ⭐ ——** 自相矛盾）|
| 反細分（Unsubdivide） | ➖ | 減面用，我們的模型本來就低面數 |

### 10.3 面（Face）

| Blender | 我們 | 說明 |
|---|---|---|
| **擠出面** | ✅ | `extrudeFace()`。⚠ 一次一個共面區域、只做封閉的那一種 |
| 沿法向擠出／各自擠出 | ⭐ | 多個面一起擠時「各自擠」與「一起擠」結果不同 |
| **內縮（Inset）** | ✅ | **2026-08-25 完成**，四動作框架的試金石。⚠ 不做 Depth —— 內縮完內圈那個面自動選中，深度交給「拉面 × 法向」|
| **填充（F）／格狀填充（Grid Fill）** | ✅ 半 | **「補洞」2026-08-25 完成** —— 把每個邊界迴圈補成一個 n 邊形。⚠ **Grid Fill（補成整齊的四邊形）還沒做**；⚠ 環形的面補不回環形（一個面裝不下兩個迴圈）|
| **面層級布林（Intersect Boolean）** | ✅ 半 | 我們有**物件層級**布林（Manifold）。面層級的沒有 —— ❓ 需求未確認 |
| 用選取的面切（Intersect Knife） | ⭐ | 拿選取的面當刀切其餘的面 |
| Poke（面中心打點成扇形） | ➖ | 產生一堆三角形，跟我們「盡量四邊形」的方向相反 |
| **加厚（Solidify）** | ✅ | `mesh.shell()`（板件加厚、STL 匯出用）|
| **三角形↔四邊形（Tris to Quads）** | ✅ 近似 | 我們的**還原多邊形**（`bake()` 時自動）更進一步 —— 直接還原成 n 邊形 |
| 三角化面 | ✅ 內部 | `mesh.faceTriangles()`（全專案唯一入口），但**沒有給使用者的按鈕**，也不需要 |
| Beautify Faces | ➖ | 美化三角形分佈，服務算圖 |
| 銲接邊／面（Weld） | ⭐ | ＝ 合併頂點的面版本 |
| 線框化（Wireframe） | ➖ | 把網格變成一堆管子，造型玩具 |
| 每個面的著色（Shade Smooth／Flat） | ➖ | 只影響畫面打光，不影響幾何與出圖 |

### 10.4 整體網格（Mesh）

| Blender | 我們 | 說明 |
|---|---|---|
| **平面切開（Bisect）** | ✅ | **2026-08-25 完成**（🔴 **2026-08-27 更正：原本標 ⭐⭐⭐**）。工具列的 **`切一刀`** ＋ 軸 ＋ 座標 ＋ 範圍提示，整個物件橫切、位置精確（**世界座標**），打數字當下就有青色預覽線。⭐ 四動作框架第三例 ＝ **加線 × 平面**，骨架跟 `loopCut()` 逐項對應。⚠ ✘ **「只切選到的面」kang 否決**（會跟刀具模糊）；⏳ 非凸的面被穿超過兩次還沒做。🔴 **這一格當初的預測沒兌現，值得記**：舊版寫「跟剖面分切是同一個數學（`slice/section.js` 已經在算平面與網格的交線），只是沒拿來改網格」——【實證·2026-08-27 讀過 `edit.js`】**`sectionAt()` 重用不了**（`edit.js` 第 1394 行自己就寫著），因為它算的是**2D 線段給 DXF 用**。實際走的是另一條路（`bisect()` ＋ `worldAxisPlane()`）。⭐ **「同一個數學」不等於「同一支程式重用得了」** |
| **刀具（Knife, K）** | ✅ | **2026-08-26 完成**（🔴 **2026-08-27 更正：原本標 ⭐⭐**）。工具列的 **`刀具`** ＋ **`吸中點`**：在模型上**點一串位置**，或**按住拖劃一條線**（兩種可混用），快點兩下閉合。⭐ 跟 Blender 一樣**貼著表面走、不准在面中間收尾**（兩邊都讀過權威）；⭐ 而我們多一項自由：**點到哪切到哪**，轉過去點背面天經地義。⚠ ✘ 角度／軸限制 kang 否決（不符合「自由」）。⏳ **平板三項待實測** |
| 刀具投影（Knife Project） | ⭐ | 用另一個物件的輪廓投影下去切。⚠ 我們的「匯入線稿擠出」是另一條路 |
| **刪除（面／邊／點）** | ✅ 面 | **刪除面 2026-08-25 完成**（跟補洞一對）。⚠ 會讓網格變開放 → 不能再布林，toast 會講並指出「補洞」補得回來。邊／點的刪除還沒做 |
| 溶解（Dissolve 面／邊／點） | ✘ | **不共面的溶解：在「要切出來」的用途下不做。** 🔴 **⛔ 不是做不到** —— 網格建得起不平的 n 邊形（第 738 行實證），擋住它的是**那張圖要從平板子上切下來**（實測偏離 0.9561 cm ＝ 可切容許值的 96 倍）。**權威版在 `規格\建模器-點線面編輯.md`**，⛔ 不在這裡抄第二份。共面的溶解 ＝ 我們的還原多邊形，✅ 已經有 |
| **限制性溶解（Limited Dissolve）** | ✅ | ＝ 還原多邊形（`mergeCoplanarFaces()`）|
| **合併頂點（Merge）** | ⭐ | 六種（中心／第一個／最後一個／依距離／塌陷…）。⚠ **最貴的一個** —— 索引位移最嚴重 |
| **清理（Cleanup）** | ✅ 半 | `preflightRebuild()` / `cleanRebuild()` 做了「清孤點、退化面、重複面」。⚠ Blender 還有**填補破洞**、**壓平非平面的面**，我們沒有 |
| 複製（Duplicate） | ✅ 物件層級 | 工具列「複製」。⚠ 編輯模式內複製元素沒有 |
| **分離（Separate）** | ✅ | **2026-08-25 完成**（🔴 **2026-08-27 更正：原本標 ⭐⭐**）。工具列的 **`分離`**：沿選到的那圈邊，把物件拆成兩個獨立物件。⚠ 它跟「切開」是**兩件事** —— 切開只是加線，分離是拆成兩個物件。🔴 **這對分片仍然是另一條路**（與其標接縫，直接切成好幾個物件）—— ⏳ 那條路還沒被走過，⛔ 不要以為做完 `分離` 就等於走過了 |
| 分裂（Split） | ⭐ | 同上但不建新物件，只是斷開 |
| **鏡射（Mirror）** | ✅ 物件層級 | 工具列「鏡射」。⚠ 編輯模式內的鏡射沒有 |
| 對稱化（Symmetrize）／吸附對稱 | ⭐ | 把一半複製到另一半。做左右對稱件時省一半工 |
| **法向：重算外側／翻轉** | ✅ | **2026-08-24 完成**（工具列「法向」那一組）。連帶治好兩種現有檢查抓不到的病：一致但整個顛倒（`ok=true closed=true`，只有體積是負的）、兩個物件一正一反（總體積剛好 0）。用法見 `規格\建模器-點線面編輯.md` |
| 自訂法向 | ➖ | 服務算圖 |
| 排序元素（Sort Elements） | ➖ | 索引整理，我們用不到 |
| 凸包（Convex Hull） | ⭐ | 把一堆點包成最小凸多面體。做保護罩、外框時偶爾有用 |
| 設定屬性／權重／UV | ➖ | 服務算圖與貼圖 |

### 10.5 變換（Transform）

| Blender | 我們 | 說明 |
|---|---|---|
| **移動／旋轉／縮放 × 軸向 × 中心** | ✅ | 種類（3）× 方向（世界／法向）× 中心（重心／最後選的）。⚠ Blender 的方向還有 View／Cursor／Custom |
| **壓平到平面（Flatten / Align to Plane）** | ✅ | 「壓平」按鈕（＝ 縮放 × 法向 × Z 打 0）|
| **沿法向縮放（Shrink/Fatten）** | ✅ 近似 | ＝ 我們的「移動 × 法向」。⚠ 多個面各自沿自己的法向不一樣，❓ 沒驗 |
| **剪切（Shear）** | ⭐ | 做傾斜、斜體。⚠ 我們現在做斜面要靠「旋轉 × 法向」 |
| **變成正圓（To Circle）** | ⭐⭐ | 🔴 **把歪掉的一圈點推回正圓**。跟我們已經驗過的「圓弧擬合」是同一件事的另一半 —— 擬合是**量**，這個是**改** |
| 變成球（To Sphere） | ➖ | 造型用 |
| **邊迴圈平均分佈（Space Evenly）** | ⭐ | 讓一圈點等距。做等分格柵、百葉時要 |
| 彎曲（Bend）／扭曲（Warp） | ➖ | 自由造型，而且會讓面不再是平的 → 板材做不出來 |
| 推拉（Push/Pull） | ➖ | 往中心聚攏／散開，造型用 |
| 隨機化（Randomize） | ➖ | 造型用 |
| Skin Resize | ➖ | 服務 Skin 修改器 |
| **比例編輯（Proportional Editing）** | ❓ | 當初以「雕塑類」否決（第 5 節），但它確實是**建模編輯**功能。⚠ **邊界未定**，排到再問 kang |

### 10.6 選取（Select）

| Blender | 我們 | 說明 |
|---|---|---|
| 點選／加選（Shift）／再點取消 | ✅ | 含「順序即 active」，跟 Blender 的 select history 是同一個東西 |
| 框選 | ✅ | **物件層級與編輯模式兩個層級都有**（🔴 **2026-08-27 更正：原本寫「編輯模式下框選子元素沒有」，那是 8/24 的狀態 —— 編輯模式框選 2026-08-26 完成**）。工具列「框選」。🔴 **它連背面一起選**（kang 拍板，跟刀具「點到哪切到哪」同作風）—— ⚠ **所以一定要講出數量**，否則使用者以為選了 6 條實際選了 12 條 |
| 點／邊／面模式 | ✅ | 過濾器「自動／點／邊／面」|
| **邊迴圈（Edge Loop）** | ✅ | `edgeLoop()` ＋ 工具列「**選一條線**」（2026-08-26 完成）。🔴 **這一格 8/26 被實測翻掉，改法見下方那則** |
| **邊環（Edge Ring）** | ✅ | `edgeRing()` ＋ 工具列「選一圈」按鈕（2026-08-24 完成）|
| 面迴圈 | ⭐ | 還沒做。走訪跟邊迴圈同一支，換一個對象 |
| **選相連的（L / Ctrl+L）** | ⭐ | 選一整塊相連的。分離、刪除時很常用 |
| **增長／縮減選取（Ctrl + / −）** | ⭐ | 從一個面往外擴一圈。**多選好幾片時比一個一個點快得多** |
| **依銳邊選（Sharp Edges）** | ⭐⭐ | 依夾角選出所有銳邊。🔴 **這對分片是一條捷徑** —— 一次選出所有轉角，再決定哪些要切 |
| **選相似的（Shift+G）** | ⭐ | 同面積、同法向、同邊長、同邊數…。選「全部 32 片 seg」一次搞定 |
| 依特徵全選（非流形、鬆散元素、內側面…） | ⭐⭐ | 🔴 **這其實是檢查工具**，不是選取工具。「非流形」正是 STL 列印失敗的原因，我們現在只能靠 `validate()` 與 χ 事後報數字 |
| 隔一個選一個（Checker Deselect） | ⭐ | 做格柵、鏤空時 |
| 鏡射選取／active 的某一側 | ➖ | 對稱建模的輔助 |
| 隨機選取 | ➖ | 造型用 |
| 依屬性選 | ➖ | 服務幾何節點 |

#### 🔴 2026-08-26：「邊迴圈走不動」那一格被實測翻掉了

> **舊版寫的是**：「邊迴圈 ❌ 走不動 —— 它要四價頂點，
> **而方塊與圓柱的頂點是 3 價**（已釘成測試）」。

**那句話的前半對，後半被拿去推到全部** —— 而它只量過方塊與圓柱：

| 模型 | 價數分布 | 邊迴圈實測 |
|---|---|---|
| 方塊 | `{3: 8}` | 1 條 ✅ 舊版是對的 |
| 圓柱 seg32 | `{3: 64}` | 1 條 ✅ 舊版是對的 |
| **球 segW12 segH32** | **`{4: 372, 12: 2}`** | **32 條 ＝ 一整條經線** |
| **球 segW32 segH16** | **`{4: 480, 32: 2}`** | **16 條 ＝ 一整條經線** |

🔴 **球有 372 個四價點，只有兩極不是。** 而且迴圈**剛好停在極點** ——
一條經線本來就該從極走到極，所以那不是限制，是正確行為。

⭐ **而球正好是瓣片展開的對象。** 這一項直接解掉「瓣片展開 A 要點 384 條邊」。

⚠ **連帶翻掉另一則**：日誌原本寫「動工第一件事：實測『選一圈』（`edgeRing()`）
抓不抓得到經線」—— **實測答案是抓不到**（同一條經線只抓到 1 條，
它是繞著球跑的）。要的是邊迴圈，不是邊環。**兩者已釘成對照測試。**

〔又一個「推論不是權威事實」的實例，而且這次**被推論的是這張表自己**。
　⭐ 教訓：**表上寫 ❌ 的，要看它是拿什麼量出來的。**〕

---

### 10.7 🔴 這張表看下來的四個結論

> 🔴 **這四個結論在 2026-08-27 整組刷新過。**
> 原本是 2026-08-24 寫的，而 8/25～8/26 做完的東西（Bisect／刀具／分離／
> 導圓角／補洞／編輯模式框選…）**一項都沒有回寫**，所以四個結論**全部過期**。
> 病因不是有人偷懶：**「做完一件功能」這條路上，沒有任何一步會走到這張表。**
> 〔跟「實測通過沒有回寫觸發點」是同一個結構問題〕

**一、「編輯能力對齊 Blender」比想像中近。**
85 支操作裡，**服務動畫／算圖／UV／細分曲面／骨架的佔了將近一半**（表上的 ➖）——
🔴 **那一欄正好就是 kang 說的「跟建模無關、我們不需要」那一堆**，直接出局。
真正屬於建模編輯、而我們還沒有的，**全部在範圍內**，剩下的只是順序問題。

🔴 **⛔ 數量不寫在這裡（會過期）—— 要知道還剩幾項就數表上的 ⭐。**
〔2026-08-27：原本寫「大約 25 項」，而 8/25～8/26 做掉的沒回寫，那個數字**當時就不對了**。
　照日誌那條「**⛔ 會過期的數字一律現算，不存**」改成現算。〕

```
awk '/^### 10\.1/,/^### 10\.7/' 外部參考-Blender編輯.md | grep -c '^| .*| ⭐'
```

⚠ **⛔ 不可以簡化成 `grep -c "| ⭐"`** —— 那會算出 **47**，正解是 **30**。
兩個原因：**`⭐` 在整份文件裡也被拿來當強調符號**（「⭐ 沒有專用的數學」那種），
而且 **10.7 那張結論小表也有記號欄**。
🔴 所以一定要**同時**限定「**只看 10.1～10.6**」與「**只看記號欄（第二欄）**」。
〔2026-08-27 實際踩到：第一版就是寫 `grep -c "| ⭐"`，
　**跑出來 47，當場被自己的斷言抓到** —— ⭐ 這正是「**現算**」比「**存一個數字**」好的地方：
　**指令錯了跑一次就現形，數字錯了可以錯 742 項沒人發現。**〕

**二、缺口不是平均分佈的，集中在三類：**

| 類 | 缺什麼 | 為什麼重要 |
|---|---|---|
| **加線** | ✅ **已經補齊** | ~~連接兩點~~✅ ~~Bisect~~✅ ~~刀具~~✅ ~~面上加線~~✅ ~~邊上加點~~✅ ~~環切~~✅ ~~內縮~~✅。**連接頂點路徑（跨面）✘ kang 否決**（結果不唯一；⭐ 刀具已涵蓋那個需求）。⏳ 只剩**細分整個選取**與 **Edge Slide（切完滑動）** |
| **補面** | ✅ **大半補掉** | 🔴 **2026-08-27 更正：原本寫「我們現在沒有任何『把洞補起來』的能力」—— 那句已經不成立**，`補洞` 2026-08-25 完成（跟 `刪除面` 一對，體積面數頂點數精確還原）。⏳ 剩 **由點建面（F）**（補洞只認得完整的一圈邊界）、**橋接邊迴圈**、Grid Fill |
| **選取** | ⏳ **這格才是現在真正的缺口** | 🔴 **2026-08-27 更正：原本寫「多選只能一個一個點、32 片 seg 要選 32 下」—— 已經不成立**。`全選邊`（8/25）、`選一圈`（8/24）、`選一條線`（8/26）、**編輯模式框選**（8/26）、hover（8/26）都做了。⏳ 剩**相連／增長縮減／依銳邊／選相似**（＝待辦的「選取第二批」）＋ 面迴圈、依特徵全選 |

**三、有三項是「我們已經有一半，另一半幾乎免費」—— 🔴 三項裡兩項已經兌現：**

- ✅ **選一整圈邊** —— **2026-08-24 完成**（`edgeRing()` ＋ 工具列 `選一圈`）。
  ⭐ 當初估「差一顆按鈕」，**估對了**
- ✅ **Bisect（平面切開網格）** —— **2026-08-25 完成**（工具列 `切一刀`）
- ⏳ **變成正圓（To Circle）** —— **還沒做，這一項的估計仍然成立**：
  圓弧擬合已經驗過可行（實測半徑 25.000、偏差 0.00000），那是**量**；
  To Circle 是拿同一個結果去**改**。
  ⚠ 動工前先重測「方塊沒被誤判成圓」那一格（日誌待辦記著那一格沒驗證過為什麼成立）

⭐ **這一組的預測命中率值得記**：三項估「另一半幾乎免費」，
**兌現的兩項都真的很便宜**，而且第三項的理由到今天仍然成立。
〔對照組：**同一張表估「邊迴圈走不動」，那個是錯的**（見 10.6 那一格）——
　⭐ **差別在於：估「便宜」是看我們自己有什麼（查得到）；
　估「走不動」是看模型長什麼樣（要實測）。**〕

**四、有一項是這張表裡最該補、但路線圖上完全沒有的：✅ 已經補掉了（2026-08-24）**

🔴 **法向「重算外側」與「翻轉」。**
繞向錯了**畫面上完全看不出來**（three.js 雙面打光），只有體積會變負 ——
這個專案已經為它踩過坑第 29 條、導角實測也再中一次
（`(A1,B1,B2,A2)` 那個順序畫面完全正常、體積卻是 99750）。
而 STL 送去列印時，繞向錯 ＝ 切片軟體報非流形。

🔴 **而真正的理由比「Blender 有」更強**：`out/stl.js` **早就在報這件事**
（「法向朝內（體積算出來是負的），印出來會內外相反」）——**卻沒有給任何修法**。
講了問題卻沒有出路，那是坑第 11 條的近親。
→ **已完成**，見 `規格\建模器-點線面編輯.md`「法向」那一節。
⭐ 同一輪順手把「選一圈」也做了（走訪是環切寫好的，幾乎免費）。

### 10.8 ⚠ 這一節沒有回答的

| | |
|---|---|
| **每一支的參數細節** | 只抓了操作索引，**沒有逐支讀內文**。要做哪一項時再去讀那一頁 |
| **多個面一起沿法向縮放的行為** | ❓ 沒驗 |
| **面層級布林我們需不需要** | ❓ 需求未確認 —— 物件層級的已經在用 |
| **修改器（Modifier）整組** | 本節只涵蓋**編輯模式**。Blender 的 Array／Mirror／Solidify／Bevel 修改器是另一套（非破壞式），我們是直接改網格。⚠ **要不要走非破壞式是一個沒討論過的大方向** |
| 🔴 **這張表要靠什麼保持不過期** | ❓ **還沒有答案 —— 這是目前最要緊的一格。** 見下 |

#### 🔴 這張表為什麼會過期，以及還沒解掉的那一半（2026-08-27）

2026-08-27 刷新時發現：**8/24 寫的四個結論，到 8/27 全部過期**，
表上還有 5 格標著「⭐ 沒有、值得做」而**東西早就做完了**
（Bisect 8/25、刀具 8/26、分離 8/25、細分 edge ring、連接頂點路徑已被否決）。

🔴 **病因不是有人偷懶**：
> **「做完一件功能」這條路上，沒有任何一步會走到這張表。**

⚠ 這跟「kang 說 OK 卻沒有回寫日誌」是**同一個結構問題** ——
那一個已經解掉了（觸發點搬到「開新功能前先 Grep `待實測`」），**這一個還沒有**。

⛔ **不要用「加一條規則提醒自己回來改」來解** ——
這個專案已經證明過提醒沒用（「講話不要用術語」那條**前三次都只是提醒，
而提醒擋不住第四次**）。

🔴 **而這張表的難處是：它沒辦法「現算」。**
測試項數可以（跑 `run.mjs` 就印出來），但這張表是拿我們跟 Blender 比，
**那本來就是一份快照**。

⏳ **kang 2026-08-27 提出「我認為是 GIT 的問題」，他還在想。**
目前浮出來的方向是把機械檢查掛進 `版控工具\1-看差異.bat`
（＝流程第 2 步，**每次必定會走到**，而且唯讀、零風險）——
例如「表上標 ⭐ 但 `index.html` 已經有那顆按鈕」就能自動抓出來。
⛔ **還沒拍板，⛔ 不要當成已經決定。**

---

## 11. 🔴 導角的角落：已經有方案了（2026-08-25，沙箱實測）

> **第 9.6 節與 9.10 節都寫著「角落斜接完全沒驗」。這一節把它解掉。**
> 兩邊對不上以本節為準 —— 本節是量出來的。
>
> 起因是 kang 問「導角會有問題，那我們有方案可以解決嗎？」
> 照協作規則那條（**提問 ≠ 插隊，但要查到有方案為止**）做的調查。

### 11.1 「角落不唯一」是有條件的，而那個條件我們不滿足

Blender 為角落開了 Miter 外側／內側兩組選項（Sharp／Patch／Arc），
**那是因為多段導角（圓角）** —— 角落要用弧面接，接法真的有好幾種。

🔴 **但單段導角（斜切）在 3 價頂點上，角落是唯一的**：
就是那幾個偏移點圍成的多邊形，**沒得選**。

方塊與 32 段圓柱的頂點確實全是 3 價（實測，已釘成回歸測試）。
🔴 ⚠ **但「我們的頂點全是 3 價」是錯的** —— 圓錐 seg12 是 {3:10, 4:2, 5:2, 6:2}。
**這句話害到了角落規則，經過見第 11.7 節。**

### 11.2 演算法：導角 ＝ 加線 ＋ 加面（框架成立）

⭐ **沒有任何「導角專用」的數學。** 它是四動作框架的一個組合：

| 步驟 | 是哪個動作 | 用什麼 |
|---|---|---|
| 每個面沿「被導的邊」往面內縮，兩邊都被導就取交點 | **加線**（＝內縮） | `shell()` 的 miter：除以 cos、上限 5 倍 |
| 每條被導的邊 → 一片側面 | **加面** | 四邊形 |
| 角落 → 一個多邊形 | **加面** | 那幾個偏移點 |

「往面內」的方向 ＝ `面法向 × 邊方向`（左手邊就是內側）。

### 11.3 🔴 三個實測翻掉推論的地方（每一個都會讓網格壞掉）

**① 內縮方向反了** —— 第一版體積**不減反增**（108000 → 117000）。
`n × d` 才是往內，我多乘了一個 −1。

**② 「每個面在頂點只出一個代表點」不成立。**
只導一部分的邊時，**沒被導到的那個面要「吸收」鄰居的代表點**
（從 n 邊形變 n+1 邊形）—— 因為那條斜切線會橫過它的角。
不吸收的話網格不封閉（實測 χ −2）。

> 這件事第 9.6 節其實提過（「第三個區域吸收多出來的點」），
> **但寫成了一句觀察，沒有寫成規則，所以實作時照樣踩進去。**
> 〔坑第 33 條的同一家族〕

**③ 共用「沒被導的邊」的兩個面，代表點必須是同一個。**
它們在幾何上本來就是同一點（那條邊沒被動，只是變短），
但逐面算會給出兩個不同的索引 → **網格從那裡裂開**。
實測 16 段圓柱上下兩圈導角：χ **−14**、不封閉。
🔴 **解法用併查集，不要用容許值** —— 它是結構問題，不是數值問題。

### 11.4 驗過的數字（四個案例全部 χ=2、封閉、`ok=true`）

| 案例 | 結果 |
|---|---|
| 一條長 60 的邊，w=5 | V8→10 E12→15 F6→7、**體積 107250.0000** |
| ↑ 手算 | 108000 − (5²/2)×60 ＝ **107250** ✅ 精確吻合 |
| **一個角落三條邊一起導** | V13 E21 F10、角落自動長出 **1 個三角形**、體積 106270.8333 |
| **12 條邊全部導** | V24 E48 F26、**8 個角落三角形**、體積 101416.6667 |
| 16 段圓柱上下兩圈 | V64 E112 F50、角落面 0（併完只剩 2 點，兩片側面自己接起來）|

**②③ 的手算太繁，改用蒙地卡羅獨立重算**（把結果當成一堆半空間的交集：
每對垂直的面距和 ≥ w、每個角的三個面距和 ≥ 2w）：

```
② 網格報 106270.8333　蒙地卡羅 106271.03
③ 網格報 101416.6667　蒙地卡羅 101414.95
```

🔴 **兩條獨立的路對得起來**（坑第 31 條那招）。

### 11.5 ⚠ 仍然沒解的（不要當成已知）

| 項目 | 狀態 |
|---|---|
| **多段導角（圓角）** | 🔴 **方案已經有了（2026-08-25，見第 12 節）**：單段導角就是它的「段數 1」，角落是球面的一塊、水密、收斂到解析解。⚠ 仍未驗：**非直角的角落**（那才是 Blender 開 Miter 的真正理由）|
| **凹邊**（夾角 > 180°） | ⚠ 沒測 |
| **非 3 價頂點** | 🔴 **2026-08-25 實測：我們現在就有**（圓錐 seg12 是 {3:10, 4:2, 5:2, 6:2}）—— 原本這裡寫「我們現在沒有」，那是錯的。4 價以上只導一部分會留洞，**已擋下來**，見 11.7 |
| 寬度的四種定義（Offset／Width／Depth／Percent） | ⚠ 只做了 Offset |
| 自交（導角寬度大於邊長的一半） | ⚠ 沒測。Blender 有 Clamp Overlap |

### 11.6 ✅ 實作完成之後補的（2026-08-25）

演算法照第 11.2～11.4 搬進 `edit.js` 的 `bevelEdges()`，四個案例的數字**一格都沒變**。
但搬的過程又抓到**兩個本節原本沒寫到的問題**：

**① 端點被「吸收」掉的邊，標記會落空。**
沒被導到的那個面吸收了鄰居的代表點之後，原本的頂點變成孤點被清掉 ——
而標記搬移還在拿舊索引配對。實測**方塊導一條邊，4 條沒被導的邊標記直接消失**。
→ 要問的不是「這個面在這個頂點的代表點」，而是
「**這條邊在這個面上，現在從哪一點出發**」（`repForEdge()`）。

**② 🔴 假邊界會變成分片線 —— 而且這一個不只影響導角。**
`mesh.js` 的 `_buildBoundaryLoops()` 把沒有 twin 的半邊自動標成 `CUT`
（**那條規則本身是對的**），但繞向不一致的網格，那些邊只是**暫時**配不到 twin。
`recalcNormalsOutside()` 把繞向修好之後它們變回內部邊，**CUT 卻已經搬進來了**。
→ **「修法向」按鈕也會中**：修一個繞向壞掉的模型，
**畫面上憑空多出幾條分片線，而使用者從來沒標過**（實測方塊多出 4 條）。
已在 `recalcNormalsOutside()` 修掉 —— 只清「原本是邊界、現在變成內部」的那些，
真正的邊界與使用者標的都不動。兩個方向都釘成測試。

⚠ **兩個都是推理找不出來、加追蹤才看到的。**
第二個連續推錯三輪，最後是印出「標記搬移之前 out 上的 role」才現形。
〔鐵律一：**這個專案的功能邏輯不從推論來**〕

### 11.7 🔴 kang 實測抓到的：偏移方向算錯 ＋ 一句被當成事實的錯話（2026-08-25）

> kang 的原話：「**一個邊執行一次導角..就會出現奇怪的形狀**」。
> 查下去牽出兩件事，第二件比第一件嚴重得多。

#### ① 病因不是「一次導幾條」，是偏移的方向算錯

只有**一條**邊被導時，旁邊那個面的角要**沿著「沒被導的那條邊」滑**，
才會留在鄰居的平面上（那條邊正是兩個面的交線）。
原本寫成「**垂直於被導的邊偏移 w**」。

🔴 **方塊把這個錯誤藏起來了**：方塊的兩條邊互相垂直，
「垂直於 A 偏移 w」跟「沿 B 滑 w」**剛好是同一點**。
所以一次全導、或第一刀，看起來都完全正常。

症狀：**吸收它的那個面變成非平面**，三角化之後畫面上出現奇怪的形狀。

| | 非平面的面 |
|---|---|
| 修之前，一條一條導三次 | 0 → **1 → 2** |
| 修之後 | 0 → 0 → 0 |

**距離換算**：沿 B 滑 t，離 A 的垂直距離是 `t·sin(A,B 夾角)`，所以 `t = w / sin`。
夾角趨近 0／180° 時沿用 `shell()` 那條上限 5 倍的老規矩。

⭐ **修完之後掉出一個更強的性質**：
**一條一條導三次 ＝ 一次導三條**（體積 106270.833333 完全相同、面數相同、χ 都是 2）。
順序無關正是這個 bug 壞掉時第一個會破的性質，已釘成測試。
⚠ 頂點數可以不同（實測 14 vs 12）—— 一條一條會留下**多餘但無害的點**，
幾何一模一樣。**所以測試驗幾何，不驗頂點數。**

#### ② 🔴 「我們的頂點全是 3 價」——**那句話是錯的**

它寫在第 9.8 節、第 11.1 節、日誌、規格檔、程式註解裡，被當成全域事實，
**而導角的角落規則就建在它上面**。

實測價數分布：

| | 分布 |
|---|---|
| 方塊、32 段圓柱 | 全部 **3 價** ✅ |
| **圓錐 seg12** | **{3 價 10 個、4 價 2 個、5 價 2 個、6 價 2 個}** ❌ |

角落規則是「**每個面都被導到才補**」，而它**只對 3 價成立** ——
3 價時那個沒被導到的面會**同時吸收兩邊的代表點**，自己把縫補起來；
**4 價以上兩個吸收面各自只拿到一邊，中間留一個三角形的洞**
（實測圓錐導一條邊：χ 1、邊界半邊 3 條）。

#### ✅ 通用的角落規則：**第四次解出來了**（2026-08-25）

> 前三次見下方。第四次成功的關鍵**不是新想法，是把第二次的順序修對**。

**規則**（繞著頂點 V 走一圈，收集「每個面在這一角現在用的是哪一點」）：

| 面的情形 | 它貢獻幾個點 |
|---|---|
| 在 V 有被導的邊 | **1 個** —— 它自己的代表點 |
| 沒有 | **2 個** —— 兩側鄰居的代表點（鄰居也沒有就用 V 本身）|

連續去重（含頭尾相接）之後，**剩幾個相異點**就決定要不要補：

| 情形 | 相異點 | 補嗎 |
|---|---|---|
| 圓柱邊緣：2 條被導、代表點併掉了 | 2 | 不補（兩片斜切面自己接起來）|
| **3 價、只導一條** | **2** | **不補** —— 那個沒被導到的面同時吸收了兩邊，自己補起來了 |
| **4 價、只導一條** | **3** | **要補** ← 就是這一格讓 kang 的流程壞掉 |
| 3 價、三條全導 | 3 | 要補 |

🔴 **真正的難處只有一個，而且它是反的：**

> `vertOutgoing()` 走的是 `he.prev.twin` —— **從這個面的 `prv` 邊跨到下一個面**。
> 所以繞一圈時每個面要**先放 `cur` 側、後放 `prv` 側**。
> 而**面迴圈裡剛好相反**（`prv` 先、`cur` 後，因為沿 prv 進來、沿 cur 出去）。

**第二次就是死在這裡** —— 邏輯完全正確，兩個順序寫反，串出來的環就亂掉。

#### ✅ 驗證：先建驗證集，再改程式

四次失敗的共同點是「**每次都通過了一部分案例，看起來對騙了我**」。
所以第四次**先把驗證集寫好、判準釘死，才動程式**：

| 案例 | 結果 |
|---|---|
| 方塊一條邊（體積可手算 107250）| ✅ |
| 方塊一個角落三條邊（MC 106271.03）| ✅ |
| 方塊 12 條全導（MC 101414.95）| ✅ |
| 16 段圓柱上下兩圈 | ✅ |
| 圓角方塊（非直角）一條一條導三次 | ✅ |
| **圓錐（4～6 價頂點）導一條底圓邊** | ✅ |
| **kang 的流程：先頂面四條、再垂直四條** | ✅ |
| 順序無關：一條一條 ＝ 一次全導 | ✅ |

再加一輪**壓力測試**：8 種形狀 × 40 組隨機選法，每組再連導第二刀 ——
**320 組，拓撲壞掉 0 次、被擋 0 次**。防護已移除。

⚠ 壓力測試自己也踩到一個坑：**χ 不可以寫死 2** ——
管有貫穿孔，它的 χ 本來就是 0。要跟「導角之前那個網格的 χ」比。

#### ⚠ 曲面上的斜切面不會是平的 —— 那是取捨，不是 bug

斜切面的四個角是「兩個相鄰面各自的偏移點」，而偏移是**沿著沒被導的那條邊滑**。
相鄰邊不垂直於被導的邊時，兩端滑的距離不一樣 → **那四個點不共面**。

🔴 **不可能兩邊都平**：改成「垂直偏移」的話斜切面會變平，
**但旁邊那個吸收它的既有面會不平** —— 而弄壞一個使用者沒碰過的面更糟。

實測佐證（320 組）：**不平的面全部是 4 邊形（斜切面），
被吸收的 n 邊形一個都沒有不平**。最大偏離 0.11 cm ＝ 可切容許值的 11 倍。

⛔ **不擋，但一定要講**（toast 報「幾片不平、最大偏離多少」）——
方塊、圓柱那些常見情形本來就是平的，只有真曲面才會遇到，
而那時使用者需要的是**知道**，不是被拒絕。〔坑第 18 條、鐵律三〕

#### 前三次分別錯在哪（留著，免得有人重走）

〔當時照 kang 的規則「**連續失敗超過 3 次就停下回報**」停下來了，
是他說「既然有機會解決，我們先解決」才回頭做第四次。〕三次分別是：

| 試法 | 結果 |
|---|---|
| 1. 每個面在頂點出一個代表點 | 只導一部分時**不封閉**（χ −2）|
| 2. 依「繞一圈剩幾個相異點」決定補不補 | 方塊反而多出假的角落面；圓錐 **χ −35** |
| 3. 把面表裡「只用到一次」的邊串成迴圈補 | 方塊的角落案例反而破了（χ 1）|

⚠ **那三次曾經導致一道防護**（4 價以上只導一部分就擋掉）。
**第四次解出來之後防護已移除** —— 因為那個限制擋掉的是日常操作：
**導角自己就會製造 4 價頂點**（方塊導完頂面四條，價數 {3:8} → {3:8, 4:4}）。

⛔ **要再動角落那一段之前，先把上面那四次看完，並且先跑驗證集** ——
每一次都通過了一部分案例，所以「看起來對」不是證據。

---

## 12. 🔴 導圓角（多段）：方案已經有了（2026-08-25，沙箱實測）

> 第 11.5 節寫著「多段導角的角落真的不唯一，還沒解」。**這一節把它往前推一大步。**
> 起因是 kang 問「導角有了，導圓角是不是後續也會處理？」——
> 照協作規則那條（**提問 ≠ 插隊，但要查到有方案為止**）做的調查。

### 12.1 ⭐ 最重要的發現：單段導角就是圓角的「段數 1」

**不是兩個功能，是同一個功能的段數 1 跟段數 N。**

實測（方塊 60×40×45、r=5，把所有頂點放到對應的圓柱／球面上）：

| 段數 | 拓撲 | 體積 | 離解析解 |
|---|---|---|---|
| **1** | V24 E48 F26 χ2 封閉 | **101416.6667** | 3.4639% |
| 2 | V56 E116 F62 χ2 封閉 | 104034.0969 | 0.9724% |
| 4 | V168 E348 F182 χ2 封閉 | 104792.5435 | 0.2505% |
| 8 | V584 E1196 F614 χ2 封閉 | 104989.3975 | 0.0631% |
| 16 | V2184 E4428 F2246 χ2 封閉 | 105039.0764 | 0.0158% |
| — | 解析解 | **105055.6777** | — |

🔴 **段數 1 那一列的體積 101416.6667，跟我們單段導角實測出來的數字一模一樣。**
所以現有的 `bevelEdges()` 已經落在這一族上，不是另一條路。

〔解析解 ＝ 內盒 50×30×35 ＋ 6 片板 ＋ 12 個 1/4 圓柱 ＋ 8 個 1/8 球〕

### 12.2 角落沒有歧義 —— 它是球面的一塊

**每一個段數都是 χ=2、封閉、`ok=true`。**
三片圓柱面跟角落的球面patch接得**水密**，沒有任何「要怎麼接」的選擇。

Blender 開 Miter（Sharp／Patch／Arc）是為了**一般情況**；
在「均勻半徑 ＋ 直角角落」這個條件下（＝箱體、造型件最常見的情形），**答案唯一**。

### 12.3 ⭐ 它跟「尺寸的依據」是同一條規則

誤差 3.46% → 0.97% → 0.25% → 0.063% → 0.016%，**段數加倍就縮到四分之一**。

這跟日誌「尺寸的依據」那張 seg 表**是同一件事**：
**網格裡沒有真的圓，段數開高就更接近**，而**網格量出來的就是真值，不是誤差**。

⛔ 所以**不要**拿解析解去「修正」圓角的尺寸 —— 那正是那一節推翻過兩次的錯誤方向。
段數是**建模階段的決定**。

### 12.4 實作路徑（四動作框架內）

> **導圓角 ＝ 導角（已有）＋ 環切那片斜切面（已有）＋ 把新的點推到弧上**

第三項是新的，但**它就是第 10 節對照表上「變成正圓（To Circle）」的同一個運算** ——
本來就在清單上。所以圓角不需要獨立的一大塊工程。

### 12.5 ⚠ 這一節驗了什麼、沒驗什麼

| | |
|---|---|
| ✅ **驗了** | 目標幾何成立：水密、χ=2、收斂到解析解、角落唯一 |
| ✅ **驗了** | 現有的單段導角落在同一族上（段數 1 的體積完全吻合）|
| ✅ **2026-08-25 補驗** | **我們的管線真的做得出它** —— 已實作，見下方 12.6 |
| ✅ **2026-08-25 解掉** | **非直角的角落**：判準是「三個面算出來的球心對不對得起來」，對不上就擋下來。⚠ **擋掉了，不是解掉了** —— 怎麼接仍然沒有答案，那正是 Blender 開 Miter 的地方 |
| ⚠ **仍然沒驗** | 圓角之後展開會變成什麼樣（材料規則：珍珠板一片、壓克力片數暴增）。測試只斷言「不會爆掉」，**片數要 kang 實測** |

### 12.6 ⚠ 實作出來的點數跟 12.1 那張表不一樣，而且**兩個都對**

**面數逐項吻合**（`6 + 12n + 8n²` ＝ 26／62／182／614／2246），
**點數不同**：這張表的角落是 **n²+n+1** 個點，實作出來是 **(n+1)(n+2)/2**。

差別在**角落那塊球面的三角形怎麼排**：
上表的模型是括號裡寫的「6 片板 ＋ 12 個 1/4 圓柱 ＋ 8 個 1/8 球」**直接拼**出來的，
接縫上的點各算各的；實作是從導角長出來、**接縫共用點**，所以點少。
上表因此逼近得稍好（段數 16：0.0158% vs 0.0166%，**差 0.0008 個百分點**）。

| 段數 | 表 V | 實作 V | 表離解析解 | 實作離解析解 |
|---|---|---|---|---|
| 1 | 24 | 24 | 3.4639% | 3.4639% |
| 2 | 56 | 48 | 0.9724% | 1.0053% |
| 4 | 168 | 120 | 0.2505% | 0.2622% |
| 8 | 584 | 360 | 0.0631% | 0.0663% |
| 16 | 2184 | 1224 | 0.0158% | 0.0166% |

⛔ **不要為了湊上表的點數去改實作的結構。** 正確性改由四條**獨立於這張表**的
斷言保證（見 `規格\建模器-點線面編輯.md`）：每個頂點到內盒距離**精確等於 r**／
體積收斂到**解析解**／段數加倍誤差縮**四分之一**／體積**永遠小於解析解且單調遞增**。

🔴 最後一條是這張表給不了的：**頂點跑到弧外面時，體積看起來還是很接近**，
只有「內接」這條抓得到。

⚠ **12.1 那張表的沙箱腳本沒進版控**（跟 Bisect 原型一樣），所以
**無法重現它當初怎麼分割角落** —— 上面的推測是從點數公式反推的，沒有第一手證據。

---

## 13. 🔴 Miter：只導一部分的邊，角落怎麼接（2026-08-25，方案已經有了）

> 起因是 kang 2026-08-25 實測：方塊**只導頂面四條邊**（＝圓角桌面、圓角面板）
> 想做圓角，被擋下來。他問「**沒有辦法解決嗎？**」
> 照協作規則那條（**提問 ≠ 插隊，但要查到有方案為止**）做的調查。
>
> ⚠ **前兩輪我只靠自己推導**，是 kang 提醒「可以搜尋看看有沒有參考」才去查的 ——
> 又一次印證鐵律一「**先調查清楚再動手，不要只依據推理**」。
> 查完發現：**我推導出來的東西正好就是 Blender 的預設做法**，
> 但如果先查，可以省掉三次失敗的嘗試。

### 13.1 Blender 的定義：這就是 outer miter

> **兩條被導的邊在一個角相遇時，角度大於 180 度的那一側，叫 outer miter。**

**我們撞到的正是這個** —— 頂面兩條邊被導、垂直那條沒被導，兩片圓角面在角落相遇。

### 13.2 三種接法，以及**為什麼我們選 Sharp**

| 種類 | 做什麼 | 對我們 |
|---|---|---|
| **Sharp**（預設） | 兩片圓角面**延伸到相交於一個尖點**，**不加任何額外頂點** | ⭐ **選這個** |
| Patch | 在尖點附近**加兩個頂點**，讓那裡不那麼擠。`Spread` 控制離尖點多遠 | ➖ |
| Arc | 加兩個頂點，中間**用一段弧**連起來。`Spread` 控距離、`Profile` 控弧形（0.5 ＝ 圓弧、0.25 ＝ 平的、更小 ＝ 凹的） | ➖ |

🔴 **Patch 與 Arc 解的是「算圖時的著色問題」，不是幾何正確性。**
教學文的原話是選 Arc 因為「**less shading issues**」——
那是 Blender 的其他目的，正是對照表上標 ➖ 的那一類（動畫／算圖／UV）。
**我們不算圖，Sharp 幾何最簡單、頂點最少，而且是 Blender 自己的預設。**

⛔ **所以不要因為 Blender 有三種就做三種。**〔判準見第 10 節記號表〕

### 13.3 Sharp 的參數化（沙箱推導 ＋ 對過答案）

以方塊 60×40×45、只導頂面四條邊、r=5 的一個角落為例，
局部座標 `u = x−25, v = y−15, w = z−17.5`，範圍都是 `[0,5]`：

```
片1（繞沿 x 那條邊的圓柱）： v = 5cosθ, w = 5sinθ, u ∈ [0, 5sinθ]
片2（繞沿 z 那條邊的圓柱）： u = 5sinφ, v = 5cosφ, w ∈ [0, 5sinφ]
交線（θ = φ）：              (5sinθ, 5cosθ, 5sinθ)   ← 兩片給同一點
```

⭐ **交線有封閉形式**，不必解聯立 —— 兩片各自延伸到 `u = w` 那個 45 度平面就相遇。

⚠ **交線是橢圓弧，不是圓弧**（兩個等半徑圓柱正交相交的標準結果）。
在 45 度平面內令 `s = u√2`，交線滿足 `v² + s²/2 = 25` —— 橢圓，半軸 5 與 5√2。
⛔ **不要以為它是圓弧去套圓弧公式。**

### 13.6 ✅ 已完成（2026-08-25），以及實作時才發現的兩件事

**做出來了**：方塊只導頂面四條邊，段數 2／4／8／16 全部 χ＝2、封閉，
體積收斂到解析解 106921.2755（0.357% → 0.0058%），面數 ＝ `6 + 4n + 8n²`。

**⭐ 13.3 的參數化少講了最關鍵的一句：兩條弧共用同一個圓心。**

實作卡了兩輪，因為一直用「每個面在這個頂點的代表點」去算圓心，
而**共用面 T 的代表點是「兩條邊都內縮」的角點**，不是任一條邊的偏移：

> **圓心 c ＝ `T 的代表點 − w × n_T`。弧1、弧2、交線全部以它為基準。**

⚠ 13.3 寫的是局部座標的公式，看起來很清楚，但**沒說 c 從哪來** ——
而那正是實作時唯一真正的難處。

**⚠ 那兩片會吃掉旁邊側面的一整條邊界，不是兩個端點。**

角落那片的第三條邊界（`rows[segs]`）**整條**躺在側面上。
13.5 已經預告「側面的邊界要多出頂點」，但**實作時仍然只放了兩端** →
實測 **χ 2 → −6，而體積照樣正確收斂**。
🔴 **這就是為什麼 13.4 特別交代要同時驗 χ** —— 只看體積會以為成功了。

### 13.4 驗到什麼、沒驗到什麼

**目標值先用兩條獨立的路對過**（鐵律三）：

| 方法 | 整體體積 |
|---|---|
| 蒙地卡羅（只取樣 y∈[15,20] 的殼層，2000 萬點） | 106921.47 |
| 手算解析解 | **106921.2755** |

〔解析 ＝ 108000 − 四條邊的非角落段 912.0578 − 四個角落 166.6667；
每個角落留下 `∫₀⁵(25−v²)dv = 83.3333`〕

**角落那一塊的體積，照 13.3 生成網格實測**：

| 段數 | 體積 | 離解析解 83.3333 |
|---|---|---|
| 1 | 41.6667 | 50.00% |
| 2 | 71.1294 | 14.64% |
| 4 | 80.1616 | 3.806% |
| 8 | 82.5327 | 0.961% |
| 16 | 83.1327 | 0.241% |
| 32 | 83.2831 | 0.060% |

✅ **段數加倍，誤差縮到四分之一** —— 跟球面角落同一族的收斂行為。

❌ **沒驗完：拓撲。** 一次性腳本算出 χ = −2n（**封面的多邊形拼錯**），
改了三次沒對就停了〔照「連續失敗超過 3 次停下來回報，不盲目重試」〕。

🔴 **但體積這樣收斂，證明外表面（兩片 ＋ 交線）是對的** ——
錯的是我另外補的那三個封面，不是 13.3 的參數化。
⚠ 真正動工時**先建驗證集，而且要同時驗體積與 χ** ——
只驗體積會漏掉拓撲（坑第 17 條：中途的量一直都是對的）。

### 13.5 動工前還要想清楚的一件事

角落的兩片會**吃掉旁邊那兩個側面的一角**，所以那兩個面的邊界要多出頂點
（在 `u=0` 與 `w=0` 那兩條線上）。
⚠ 那正是圓角這一輪 kang 抓到的第一個 bug 的同一個機制 ——
「**沒被導到的面要吸收整條弧**」。⛔ 動工前先讀 `規格\建模器-點線面編輯.md`
那一則，不要再犯一次。
