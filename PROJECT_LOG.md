# 專案日誌：網頁空間設計程式

本資料夾目前有三個子專案：
1. **空間平面規劃器**（`空間平面規劃器.html`）— 廠房／辦公室平面配置，單位 cm
2. **鋁擠型框架設計器**（`鋁擠型框架設計器.html`）— 40 系列型材框架與料表，單位 mm
3. **組裝系統結構說明表**（`組裝系統結構說明表.html`）— 活動系統排料與說明表，單位 cm　←　目前主力

---

# 子專案 1：空間平面規劃器

## 專案目的
網頁式的空間配置工具，讓非設計軟體使用者能在瀏覽器上自由擺放物件、
用真實尺寸預覽廠房／辦公室配置，並可切換 3D 檢視感受空間感。

## 確定狀態
- **主檔案**：`空間平面規劃器.html`（單一檔案，雙擊即可用瀏覽器開啟）
- **技術**：原生 JS + Canvas 2D；3D 使用 three.js r128（CDN 載入，需網路）
- **單位**：全部以 **cm** 為內部單位
- **座標**：2D 的 (x, y) → 3D 的 (x, z)；物件 `z` 欄位＝高度

## 已完成功能

### 2D 平面
- 物件庫點擊新增、自訂尺寸新增
- 拖曳移動、四角把手縮放、旋轉把手（Shift 鎖 15°）
- 網格吸附（5 / 10 / 25 / 50 / 100 cm 可切換）
- 框選多選、靠左／靠上對齊、水平／垂直均分
- 屬性面板可精確輸入 X / Y / 寬 / 深 / 高 / 角度 / 顏色 / 類型
- 量測工具（量走道寬度）、底圖比例校正工具
- Undo / Redo、JSON 存讀檔、PNG 匯出、localStorage 自動暫存

### 3D 預覽
- 環繞檢視（左鍵轉、右鍵平移、滾輪遠近）
- 第一人稱漫遊（點畫面鎖定滑鼠，WASD 移動、Shift 快走、Q/E 升降）
- 牆體碰撞（房間只擋牆、可走進室內；名稱以「門」開頭的物件不擋路；可勾「穿牆」關閉）
- 底圖可貼在地板上、外牆、名稱標籤、陰影，皆可開關

## 分層造型（tiers）
物件可由多層方塊堆疊，做出「上寬下窄」等造型（如工作桌桌面外伸、機台底座／機身／機頭）。

```
o.tiers = [ {name, w, h, z0, z1, dx, dy, color}, ... ]   // 沒有此欄位＝單一方塊
```
- `z0/z1`＝該層離地的下緣／上緣；`dx/dy`＝相對物件中心的水平偏移
- `o.w / o.h / o.z` 永遠是所有層的**外框**（由 `fitTiers()` 自動重算並重新置中）
- 改主面板的寬／深／高 ＝ 等比縮放所有層（`setObjSize()`），只在 change 時觸發避免逐字累積誤差
- 內建範本：`desk`(工作桌) / `machine`(機台) / `cabinet`(櫃體) / `platform`(平台)
- 只有 `kind==='solid'` 支援分層；改成房間或區域會清掉 tiers

## 從「組裝系統結構說明表」匯入（kind: system）
說明表按「⇪ 匯出給平面圖」產生交換 JSON，平面規劃器按「⇩ 匯入組裝系統」讀入。
**是加入現有平面圖，不會清空既有物件**；匯入後自動選取新物件，可直接拖曳定位。

交換格式：`{type:'assembly-system', v:1, unit:'cm', head, systems:[...]}`
每個 system 帶 `w/h/d`、`ps`、`postXs`（立柱中心線 X）、`bays`、
`levels:{front,back,depth}`（橫料各層中心高度）、`seats`、`expr`、`bom`。

物件對應：`w`＝系統寬、`h`＝系統深、`z`＝系統高，結構存在 `o.sys`。
`qty` 為 N 時會產生 N 個獨立物件（名稱加 `#1 #2`），依序往後錯開 60cm。

- **2D**：矩形＋各支立柱的俯視腳位（白色小方塊）
- **3D**：依 `postXs` 與 `levels` 建出真實骨架（立柱／前後橫料／深度料）。
  已驗算 272×200×34 的系統會產生 22 支桿件，與說明表備料明細完全一致
- **尺寸鎖定**：`hitHandle()` 對 system 不回傳縮放把手，避免拉伸後圖面與實際用料不符。
  要改尺寸得回說明表重新排料再匯出

## 物件類型（kind）
| 類型 | 值 | 3D 呈現 |
|---|---|---|
| 實體 | `solid` | 一整塊量體（桌、機台、柱、牆） |
| 房間 | `room` | 四面牆＋地板，中空可走進去 |
| 區域 | `zone` | 貼地薄片，用於標示走道／成品區 |
| 人形 | `person` | 比例參考人偶。2D＝肩橢圓＋頭＋面向三角形；3D＝腿/身/手臂/頭組成的人形。寬＝肩寬、深＝身體厚度、高＝身高（預設 45×35×170）。不參與漫遊碰撞、不支援分層 |

## 資料格式（存檔 JSON）
```
{ v:2, nextId, objects:[{id,name,x,y,w,h,z,rot,kind,color}],
  roomW, roomH, roomZ, bg:{src,x,y,w,h}, bgAlpha, view }
```
- `x,y` 為物件**中心**座標；`rot` 為度數
- `bg.src` 是 base64 DataURL，所以存檔會把底圖一起帶著

---

# 子專案 2：鋁擠型框架設計器

**主檔案**：`鋁擠型框架設計器.html`（獨立網頁，單位 **mm**）

## 用途
用 40 系列鋁擠型搭工作桌架／料架／防護框，3D 即時預覽，自動產生切割料表與配件清單。

## 座標與資料模型
X＝長、Z＝深、Y＝高，原點在框架**底部中心**。

```
member = { id, prof:'4040', p1:[x,y,z], p2:[x,y,z], joint:'corner'|'butt', name }
```
桿件用起點／終點表示，長度自動算，因此斜撐也能做。

## 兩個獨立選項（重要，別混淆）
| 選項 | 影響 | 說明 |
|---|---|---|
| **框架接法** `post` / `beam` | **切料長度** | `post` 立柱通長＝H，橫梁扣兩端立柱寬；`beam` 橫梁通長＝L，立柱扣上下梁高 |
| **接合方式** `corner` / `butt` | **只影響配件** | 角件外掛 vs 端面內接。兩者桿件切料長度相同 |

驗證過的數字（1800×750×800、4040）：
- 柱通長 → 立柱 800×4、長梁 1720×4、短梁 670×4
- 梁通長 → 立柱 720×4、長梁 1800×4、短梁 670×4
- 換成 4080（斷面寬 80）→ 長梁 1640、短梁 590

## 型材資料（`PROF`）
4040 / 4040輕型 / 4080 / 40120 / 8080，各含斷面尺寸與 kg/m。
**kg/m 為市售概略值**，要精確請依供應商規格改 `PROF` 這個物件。

## 配件推算規則
桿件端點若落在另一支桿件的斷面範圍內（半寬＋3mm）即算一個**接點**，其餘為**外露端**。
- 角件外掛：每接點 N 個角件（預設 2，可調），每角件配 2 支 M8 螺栓 + 2 個 T 型螺帽
- 端面內接：每接點依斷面模組數配內接組（4040＝1、4080＝2、8080＝4）
- 外露端各 1 個封蓋；位於最低處的外露端另計調整腳杯

## 功能
- 4 個範本：箱型框架 / 工作桌架 / 多層料架（層數可設）/ 防護圍籬
- 3D：真 T 溝斷面（ExtrudeGeometry，可關成方管加速）、等角/前/側/俯視、陰影、外框尺寸標註
- 點選 3D 桿件即選取，右側可改型材、接合、起訖點、長度；可複製／刪除／加單支桿件
- 切割料表（含可調鋸切裕度）＋ 配件清單，可匯出 CSV
- JSON 存讀檔、PNG 截圖、localStorage 自動暫存（key: `alu_frame`、`alu_panels`）

## 待辦 / 可延伸
- [ ] 桌面板／壓克力面板（含開孔）
- [ ] 斜撐快速加入
- [ ] 單價設定與報價單輸出
- [ ] 標準長度套裁最佳化（6m 原料如何切最省）
- [x] 匯出到「空間平面規劃器」當成物件（已完成，見子專案 1 的「從組裝系統結構說明表匯入」）

---

# 子專案 3：組裝系統結構說明表

**主檔案**：`組裝系統結構說明表.html`（獨立網頁，單位 **cm**）

## 用途
取代目前用 Illustrator 手工畫的「組裝系統結構說明表」。輸入需求尺寸 → 自動排料 →
產生展開計算式、正面／側面圖、備料明細，可列印或存 PNG。

## 標準料（公司實際規格，cm）
`26 / 34 / 40 / 46 / 60 / 94 / 150 / 200 / 292 / 300`
- 預設 **292 與 300 只當立柱、不當橫料**（由實際案例回推的慣例），可用勾選調整
- 立柱斷面預設 4cm

## 資料結構（v2：一個案件多張說明表）
```
DOC = {
  head: { date, no, name },        // 全案共用表頭
  sheets: [ sheet, ... ],          // 多張說明表
  cur: 0                           // 目前編輯第幾張
}
sheet = { name, qty, req:{w,h,d}, ps, lv:{f,b,d},
          bays:[{len,pieces}], postH, postHPieces, depth:{len,pieces}, pickSig }
```
- `qty` ＝這張說明表要做**幾套**（同一組配置重複製作）。圖形不變，備料自動乘上去。
  `computeBOM()` 每個品項同時回傳 `per`（單套）與 `qty`（＝per×套數），
  `cut`／`seats`／`bars` 皆已含套數。已驗算：一張 3 套 ＝ 三張各 1 套。
- `S` 是模組層變數，永遠指向 `DOC.sheets[DOC.cur]`
- 左側欄位是「編輯介面」：切換說明表時 `syncToDOM()`，欄位改動時 `syncFromDOM()`
- `withSheet(i, fn)` 可暫時把 `S` 切到第 i 張執行 fn 再切回（批次匯出／合併備料用）
- 標準料勾選與排料條件（容許差、每組、最多座數、單跨接料）是**全案共用**
- 讀檔相容舊版單張格式（`v` 不等於 2 時自動包成一張）

## 核心邏輯
```
系統寬 = 4 + 跨距1 + 4 + 跨距2 + 4 + … （n 支立柱、n-1 個跨距）
系統深 = 4 + 深度跨距 + 4
系統高 = 立柱長
```
- 括號外＝客戶需求尺寸，括號內＝標準料湊出的實際組裝尺寸
- 跨距可由 1～3 支標準料接起來，寫成 `(150+26)`
- 「前3後2」＝正面 3 層橫料、背面 2 層 → **每個跨距 5 支橫料**
- 深度料：每座前後連接，層數可設（預設 2）

## 演算法
- **跨距排列**：分層 DP，`dp[k][w]` ＝ 恰好 k 個跨距、佔用 ≤ w 的最大佔用量；
  另有可達性表 `reach[k][w]` 用來列出每個座數的前 N 名方案
- **自動選用偏好**：差距 ≤ 容許差（預設 30cm）之中，取「**接料最少 → 座數最少 → 差距最小**」
- **立柱／深度選料**：單支優先；單支差距超過容許差才考慮接料

## 驗算結果（對照公司實際兩張表）
| 案例 | 需求寬 | 公司原表 | 程式自動選用 |
|---|---|---|---|
| ABA-0107 | 290 | `4+60+4+200+4=272` | 相同 ✓ |
| ABA-0125 | 400 | `4+200+4+(150+26)+4=388` | `4+34+4+150+4+200+4=400`（差 0、單支料） |

高 220 → 立柱 200 ✓　深 36 → 34 ✓（皆與原表一致）

ABA-0125 程式給的方案客觀上更好（差 0 且不需接料），公司原排法可能有現場或庫存因素。
把「每組顯示」調大即可在 3 座那組看到 388 的排法。**若之後確認有其他排料偏好規則，改
`autoPick()` 的排序條件即可。**

## 功能
- **一個案件多張說明表**：上方分頁列切換，可新增／複製／改名／前後移動／刪除；
  表上右上角標示「名稱（第幾張/總數）」
- **每張可設套數**：分頁標籤顯示 `×3`，表上右上角標「×3 套」，
  備料明細框加註單套用量，右側面板以 `(單套×套數)` 呈現，CSV 有「單套／套數／總數量」三欄
- **備料可切換「這張」與「全案合計」**
- **批次輸出**：列印全部（A4 橫向、一張一頁）、PNG 一次匯出全部、CSV 含分張明細＋全案合計
- **可指定存放位置**：工具列「指定存放位置」勾選後，單檔匯出會跳「另存新檔」；
  「全部 PNG」則先選一次資料夾再整批寫入。用 File System Access API
  （`showSaveFilePicker` / `showDirectoryPicker`），偵測不到或被擋時自動退回一般下載並提示一次。
  相關函式：`saveBlob()` / `downloadBlob()` / `dataURLtoBlob()`（同步轉檔，避免 await 吃掉使用者手勢）
- 表頭欄位（進場日期／客戶單號／案件名稱）為全案共用
- 排料方案依座數分組，每組列出前 N 名，點選即套用
- 逐跨距手動改（下拉選標準料或接料組合）、可增刪跨距
- 說明表 Canvas 輸出（虛線格、正面圖、側面圖、展開式、備料明細框），PNG／列印／PDF
- 備料明細 + 實際切料統計，可匯出 CSV
- 3D 立體預覽（等角／前／側／俯視）
- JSON 存讀檔、localStorage 自動暫存（key: `sys_sheet`）

## 已知細節
- **立柱斷面寬**（`pS`，預設 4）＝展開式裡的「4」，每支立柱佔掉的寬度
- 說明表的紅線用**固定線寬 9px** 繪製，但側視圖比例被放大約 3 倍，
  若橫線畫到外緣（0～總深）會超出立柱線。因此橫線一律畫到**頭尾立柱的中心線**為止，
  端點由接點圓標蓋住。正面圖同樣處理。

## 平板支援（進行中，分支 `touch`）
**設計原則：事件入口分開，動作邏輯共用。** 桌機的 `mouse*` 路徑保持原行為，
觸控是另外新增的入口，兩者最後呼叫同一組動作函式，所以計算結果保證一致，
未來要改邏輯也只改一處。

- 3D 視角：共用 `orbitBegin / orbitMove / orbitEnd / orbitZoom / orbitPanBy`
  - 滑鼠：左鍵轉、右鍵平移、滾輪縮放（與原本完全相同）
  - 觸控：單指轉、雙指捏合縮放＋雙指拖曳平移
- 側欄分隔線：共用 `grabStart / grabMove / grabEnd`，滑鼠與觸控各接一組入口
- 說明表預覽：新增 `SV{z,x,y}` 縮放狀態，雙指捏合以手指中心為基準縮放
  （已驗算按住的點不會位移），單指拖曳平移，右下角有 −／⤢／＋ 按鈕
- CSS 隔離：
  - `@media (pointer:coarse)` 加大點擊目標 → 只對手指生效，滑鼠桌機不受影響
  - `@media (max-width:1100px)` 側欄改抽屜（含遮罩、點擊關閉）→ **1100px 以上完全維持原版面**
  - `touch-action` 只設在 3D 畫布、分隔線、說明表預覽，其餘保留瀏覽器預設
  - `overscroll-behavior:none` 擋掉下拉重新整理搶走拖曳

**尚未完成**：three.js 內嵌（離線）、PWA、套用到空間平面規劃器。

## 待辦 / 可延伸
- [ ] 確認並加入其他排料偏好規則（例如背板分割、單跨最大容許跨距）
- [ ] 表單版面微調到與現行 AI 檔完全一致（字級、位置）
- [ ] 多系統（一個案子多組）合併備料
- [ ] 庫存扣帳／可用料查詢

---

## 協作規則

### 版本控制與檢查流程
- 本資料夾已納入 Git。**Claude 一律不執行任何 git 指令，連唯讀的 `git status` 也不行。**
  掛載點不支援刪除 `.git` 內的檔案，唯讀指令建立的 `index.lock` 會留下來，
  導致 kang 下次存檔 fatal 失敗（2026-08-20 實際發生過）。
  Claude 只用 Read／grep 讀檔案內容；所有 git 動作由 kang 在 Windows 端執行。
- **操作流程與工具見 `版控工具\README.md`**：
  `1-看差異.bat`（唯讀）→ Claude 讀結果檔確認 → Claude 寫 `commit-msg.txt` → `2-存檔.bat`（純加法）
- 切分支、退回**不做成一鍵**，由 Claude 當場給指令
  （判準參考 ERP 專案：一鍵只給唯讀與純加法，會改變現況的動作不給捷徑）
- `.bat` 內容必須**純 ASCII + CRLF**（中文會在 Big5 環境亂碼；LF 會讓 `goto` 標籤失效），
  已用 `.gitattributes` 鎖住 `*.bat text eol=crlf`
- **改完檔案後一定要跑 `git diff` 自我檢查**，特別注意有沒有 `-` 掉不該刪的行。
  教訓：2026-08-20 插入 CSS 時誤刪 `@media print{` 開頭，導致列印規則全域生效、
  側欄工具列全被隱藏。這種錯誤**語法檢查抓不到**（大括號仍然平衡、JS 也沒問題），
  只有 diff 看得出來
- CSS 大量改動後，額外檢查：大括號配對、每個 `@media` 是否正確閉合、
  `display:none!important` 這類破壞性規則是否確實被包在該有的 media query 內

- 回答用繁體中文、簡潔
- 多檔案修改前先列計畫並提關鍵疑問
- 修錯連續失敗超過 3 次就停下回報

## 使用流程建議
1. Illustrator 平面圖 → 輸出 PNG
2. 右側「上傳底圖」
3. 工具列「⚖ 校正比例」沿一段已知長度（如 618cm 柱距）拉線，輸入實際 cm
4. 從左側物件庫擺放物件、調高度
5. 切到「◈ 3D 預覽」用第一人稱走一遍確認走道與空間感
6. 「💾 存檔」存成 JSON，下次「📂 讀檔」續做

## 待辦 / 可延伸
- [ ] 圖層與群組
- [ ] 門開口（讓房間牆真的開洞）
- [ ] 列印排版（A3 含比例尺與圖例）
- [ ] 材質貼圖（地板、牆面）
- [ ] 家具 3D 模型匯入（glTF）
- [ ] 走道淨寬自動檢查（低於設定值時警示）

## 進度紀錄
- 2026-08-20　v1 完成：2D 平面編輯器（拖拉、吸附、物件庫、存讀檔、匯出 PNG）
- 2026-08-20　v2 完成：加入物件高度與類型、3D 環繞檢視與第一人稱漫遊、牆體碰撞
- 2026-08-20　v2.1 介面：左右側欄改為可拖曳調寬（150–620px，雙擊還原）、
  可一鍵收合、寬度記憶於 localStorage（key: `floorplan_panels`）；
  修正欄位文字截斷（物件庫名稱過長改省略號、屬性面板標籤縮窄）
- 2026-08-20　v2.2 分層造型：物件可堆疊多層方塊做出上寬下窄等形狀，
  含 4 組快速範本與逐層編輯器；2D 用外廓實線＋內層虛線呈現；
  物件庫的工作桌／機台／CNC／台車／料架已預掛範本
- 2026-08-20　v2.3 新增「人」物件（kind: person）作為空間比例參考，
  身高／肩寬可調，角度＝面向方向
- 2026-08-20　開啟子專案 2「鋁擠型框架設計器」v1：
  40 系列型材、4 範本、T 溝斷面 3D、桿件點選編輯、切割料表與配件清單、CSV 匯出
