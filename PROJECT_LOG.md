# 專案日誌：網頁空間設計程式

最後更新：2026-08-28　V14.0

> ⚠ **最近一輪翻掉了什麼**（🔴 **⛔ 這一欄覆蓋，不累積。上限 5 條，超過就砍最舊的**）：
> 1. 🔴🔴 **`node --check` 對 ES 模組是瞎的 —— ⛔ 它抓不到「重複宣告」，而那會讓整個網頁一行都不執行。** 【實證 2026-08-28，kang 當場踩到】我在 `updateBar()` 裡宣告 `const edgeAny`，而**導角那段早就用了同一個名字**（同一個函式作用域）。重複宣告是 **SyntaxError**，而 ES 模組**解析階段就失敗 → 整份檔案一行都不跑**。⚠ **症狀完全看不出病因**：工具列在、CSS 正常、新加的按鈕也在，但「新增」下拉是空的、`– FPS`、物件 0。⭐ **kang 的伺服器 log 是決定性的證據**：那幾次重新載入，伺服器**只被要了 `/建模器/` 與 `js/main.js` 兩個檔案** —— `edit.js`／`scene.js` 一個都沒被請求，因為瀏覽器連解析都沒過，還沒走到「去抓 import 的東西」。🔴 **四道機械檢查（語法／id 對照／函式存在／2282 項測試）全部放它過關。** ✅ **防線已補**：改過 `js/` 就跑「**真的載入一次模組圖**」那道（指令在「協作規則 → 沙箱能做什麼」，實測故意重犯會當場抓到）。⚠ **⛔ 我一開始說「應該是快取」——那是推論，而且是錯的**，是 kang 一句「我不要止血，我要知道原因」擋下來的。
> 2. 🔴🔴 **「橋接 ＝ 選兩圈邊」是錯的 —— ⛔ 那條路根本走不通**（日誌自己是這樣寫的）。【實證 2026-08-28】兩道牆：**① 洞的邊緣一條都選不到**（`isMarkable()` 第一行擋掉邊界邊 —— 方塊刪頂面：邊界邊 **4 條**、`markableEdges()` 給 **0 條**），⚠ **而洞的邊緣正是要接的東西**；**② 跨物件不能一起選**（`select.js`「跨物件也不行」），⚠ 而「兩根管子接起來」是最直覺的用途。⭐ **正解是⛔ 不進選取，直接問網格「你有幾個洞」**（`boundaryEdges()` 早就把洞分好組了）—— 跟 `指出來` 同一個骨架；跨物件那道牆用 `∪ 聯集` 繞掉。🔴 **這一條的普遍版**（承接被擠掉的「量距離的介面已經有了」）：**日誌／規格檔寫「介面已經有了」「解法現成」時，⛔ 那是待驗的假設，不是事實** —— 這是它**第二次**應驗。
> 3. 🔴🔴 **「碰巧通過」比失敗危險 —— 沿面滑動的方向算錯了，⛔ 而方塊照樣過關。** 【實證 2026-08-28】第一版拿「**所有軌道向量的和**」當共同方向，而每個點**一條往上、一條往下**，Y 分量**互相抵消** → 那個和永遠 ≈ 0 → 退化鏈一路掉到 Z。🔴 **然後方塊通過了全部的位移斷言**（軌道在 Z 上的投影都是 0，`>=` 於是永遠挑 `rails[0]`，**而它剛好是往上那條**）—— **露出馬腳的只有回報文字寫著「＋Z」**。⭐ 正解是用**兩條軌道的差**（`d0 − d1`），⛔ 不是和 —— **差不會抵消**。⚠ **⛔ 教訓不是「和 vs 差」**，是：**一個斷言通過，要先確定它 ⛔ 不是因為那個案例剛好對稱** —— 現在測試裡釘著「橫躺的方塊」（Y 分不出來）與「驗回報文字」兩項專門守這條路。
> 4. 🔴🔴 **側牆的繞向 ⛔ 不可以照抄 `extrudeFace()` 的 `(a, b, b', a')`** —— 【實證 2026-08-28】第一版照抄，**χ ＝ −62、不封閉**。病因：`boundaryEdges()` 回的是**有面的那一條**半邊，新面同方向 ＝ **兩個面在同一條邊上走同方向** ＝ 面貼反了，`fromFaceList()` 配不出 twin。⚠ **擠出不會遇到，因為它的蓋子被移到新位置了** —— 「同一個檔案裡的寫法不一定能照抄」的又一例。⭐ **而抓到它的是邊數，⛔ 不是 χ**：接 32 片牆，每片只該長出 **1** 條新的垂直邊 → 正好 **+32**；排錯是 **+96**（三條邊全沒接上）。〔鐵律三：讓兩個數字互相對得起來，錯誤才會自己現形〕
> 5. 🔴🔴 **「開放的網格也算得出體積」是錯的 —— ⛔ 而那個假數字被印在畫面上很久了。** `volume()` 走散度定理，**前提是封閉**；而 `橋接`／`補洞` **動手之前必定是開的**，所以提示訊息裡的 `before` **每次都是垃圾**。【實證 2026-08-28】同一個「缺一個蓋的圓柱」，y+0 → **113802.69**、y+124 → **33165.36**、y+500 → **−211347.85** —— **形狀一模一樣、數字一直變，還會變負的**（對照組：封閉的搬到哪都一樣）。🔴 **⛔ 它不會報錯也沒有徵兆**：六位數、有小數點、單位 cm³，而 kang 畫面上那句甚至讓體積看起來「**少了 3329**」，**實際是多了一段錐台 —— 方向是反的**。✅ 兩處都改成只印做完的那個。⚠ **⛔ 這個假數字在 `補洞` 裡從一開始就存在，只是從來沒有人去對過它** —— 是 kang 實測第 3 項照出來的。〔規則的家：**鐵律 → 三、做介面與回饋時**「這個數字在這個狀態下有沒有意義」〕

> 〔⛔ 覆蓋滿了，2026-08-28 一共擠出五條：「那道機械檢查會誤報 `isMarkable`」
> 　（⭐ **權威版就寫在那道檢查的指令旁邊**，⛔ 離開檔頭不會漏掉）、
> 　「把量測的數字標在元素身上是錯的」
> 　（🔴 **⛔ 它還沒結案** —— 量測第 2 步的「每一個」仍在待辦等 kang 想呈現方式，
> 　而**待辦那一條明文記著「他回來提想法時第一個要面對的是這條」**，
> 　所以 ⛔ 不會因為離開檔頭就漏掉）、
> 　「⛔ 判準不是『數量對不對』」
> 　（⚠ **⛔ 它是通則，不是修好的 bug** —— 擠出來只因為檔頭滿了，
> 　而**規則的權威版本來就在「鐵律 → 二、寫幾何與演算法時」**）、
> 　「量距離的介面已經有了是錯的」
> 　（⚠ **它的普遍版沒有消失，併進了上面第 3 條** —— 那一條正是它第二次應驗）、
> 　「球的預設是經線 32 緯線 16」（權威版在「實測清單：`選一圈面`」那一節，
> 　而 `選一圈面` 已經 kang 實測通過結案 —— **兩件都滿足才擠**）。
> 　**五條都在 `HISTORY.md`「從日誌檔頭被擠出去的『翻掉了什麼』」那一節。**〕
>
> 〔⛔ 覆蓋滿了，2026-08-27 被擠出去六條：「球 segW12 相鄰經向夾角 30 度是錯的」、
> 　「講數量與形狀破了第五次」、「方塊沒有被誤判成圓那一格是錯的」、
> 　「kang 已經在用 3D 列印是錯的」、「`mesh.issues` 說非流形 ≠ 真的是非流形」、
> 　「非流形邊比破洞難只對一半」、「`翻面` 翻的是整個物件」、
> 　**「`loopFaces` 的 `closed` 不可以照抄」**（⚠ 它**已升格成通則**留在第 5 條）。
> 　**八條都在 `HISTORY.md`「從日誌檔頭被擠出去的『翻掉了什麼』」那一節。**
> 　⚠ **擠掉誰的判準**：那一條的**權威版已經完整寫在本檔本文裡**，
> 　而且**那個功能已經 kang 實測通過結案** —— 兩件都滿足才擠。
> 　⭐ **留下來的優先是「還會再發生」那一類**（例如第 3 條講的
> 　「規格檔寫『解法現成』是待驗的假設」），⛔ 不是「已經修好的那個 bug」。〕
>
> 〔⛔ 覆蓋滿了，2026-08-27 被擠出去兩條：「能力對照表自己過期過」
> 　（仍在「kang 定的方向」那一節）、「瓣片展開 A 只剩一步是錯的」
> 　（仍在待辦「瓣片展開 A」那一條）。**兩條都沒有消失，只是不在檔頭。**〕
>
> 🔴 **上一輪做了什麼，⛔ 不寫在這裡** —— 家是 `版控工具\commit-msg.txt` 與 `HISTORY.md`。
> 〔2026-08-27：這裡原本是「這一輪做了什麼」的**累加欄位，已經長到 221 行**，
> 　而裡面每一則的權威版都在別處（鐵律／文件分層／待辦／規格檔），**它是第四份**。
> 　⭐ 換成「覆蓋型固定欄位」是為了讓它**長不大**，⛔ 不是靠紀律記得搬走。
> 　原文逐字搬進 `HISTORY.md`「日誌檔頭的輪次紀錄」那一節〕

> **這份日誌的主要讀者是 AI，不是人。**
> 因此一律寫「目前確定的狀態」，過時的內容直接覆蓋或明確標記為已失效，
> 不留需要猜測的空間。kang 只做抽查，不需要靠它回想細節。
>
> 🔴 **本檔已不是功能規格的家（2026-08-23 起）。**
> **功能規格一律在 `規格\` 一功能一檔** → 先 `Glob 規格\*`。
> 本檔只放**每次開場都要看的東西**。
> **⛔ 新的功能規格不要寫進本檔**，寫進 `規格\` 對應檔（落點規則見本檔最後一節）。
>
> **三層分工**（判準是「**誰、在什麼時候會來查這件事**」）：
>
> | 檔 | 放什麼 | 怎麼被讀 |
> |---|---|---|
> | `PROJECT_LOG.md`（本檔） | 先讀這段／**尺寸的依據**／三個工具／工作流程／部署／建模器的目的與路線圖／**鐵律**／待辦／協作規則／落點規則 | **每次從頭讀完** |
> | `規格\*.md` | 一個功能一檔的完整規格 | 需要時**精準取一份**，讀完整 |
> | `HISTORY.md` | 已結案流水帳、34 條坑的完整經過、**已結案的待辦 40 項**（2026-08-27 搬入） | **只被 Grep**，不從頭讀 |
> | `尺寸依據的推導.md` | **為什麼展開尺寸是網格不是公式**：推導、被推翻過的三個版本、成熟工具怎麼做、kang 原話 | **想推翻「尺寸的依據」、或又冒出「差額是不是誤差」的念頭時** |
> | `編輯功能設計方向.md` | 第 6 期動工前必讀（含被否決的方向） | 動編輯之前 |
> | `瓣片展開與省道-設計方向.md` | **曲面／球型怎麼用可彎材料做出來**：守恆律（切割線＋省道＝4π）、取捨曲線實測、⛔ 為什麼不做拉伸與充氣物理 | 動瓣片展開或省道之前，讀完整 |
> | `外部參考調查.md` | **索引**：兩條界線、篩子、記錄格式、三線總結 | 動編輯或展開之前 |
> | `外部參考-Blender編輯.md` | 研究線一：BMesh／變換三層／選取與 active／modal／工具／Undo　🔴 **第 10 節是完整能力對照表**（Blender 全部 85 支編輯 ＋ 13 支選取，逐項標我們有沒有）；**第 11 節導角（含角落）、第 12 節導圓角（多段）的完整演算法與實測** | 動編輯之前，讀完整。**要決定「下一個做什麼」時直接看第 10 節** |
> | `外部參考-展開與分片.md` | 研究線二：兩個開源展開器的演算法；20 項逐條對照 | 動展開或分片之前，讀完整 |
> | `外部參考-Pepakura.md` | 範圍與介面：一個只做展開的商業軟體，決定做什麼／不做什麼 | 討論「該做到哪」時 |
>
> 〔改造前本檔 2806 行 / 167KB，AI 讀到第 814 行就被截斷 ——
> 　規格明明寫在裡面，卻讀不到。做法參考 `D:\彩藝通ERP系統`。〕

---

## 開新對話時先讀這段

- 🔴🔴 **要開始下一個新功能之前，先 `Grep 待實測`，把未結案的一次問完 kang。**

  > **日誌裡還掛著 `⏳ 待實測`，就不要開新功能。**

  ⚠ **這不是提醒，是補一個結構缺口**：kang 說「OK」**不產生任何動作**
  （沒有改檔、沒有差異、沒有 commit），所以整條回寫鏈一次都不會被觸發 ——
  **2026-08-26 一次補記了六張他早就驗過的清單。**
  規則與病因見「協作規則 → 🔴 實測結果的回寫」。

  ⭐ **符號只有一個意思**：`⏳ 待實測` ＝ 做完了但還沒驗；
  `⏳` 單獨用 ＝ 還沒做；`✘ 已作廢` ＝ 被取代的舊清單。

- **現在有三個工具**：空間平面規劃器、組裝系統結構說明表、建模器
  （前兩個的規格在 `規格\`，建模器的目的與路線圖在本檔下方）
- **這套程式的用意**：建好的模型要能**做出東西**。目前有**三條路，並列，沒有主次**：
  3D 列印（STL）／展開後 2D 切割（DXF）／剖面分切疊層（DXF）。
  **三條都通了**，細節見「目標與範圍」

- ⚠ **這不是鈑金工具。使用的材料是紙材、發泡板、珍珠板、木板、壓克力，
  一種金屬都不用。**（kang 2026-08-22 說明）

  這條放這麼前面，是因為忽略它的代價很大：日誌通篇是鈑金語言
  （K 因子、中性層、折彎補償、最小折邊），材料表六種有三種是金屬，
  很容易照著文件推論下去，然後把整套工具當成鈑金工具做。
  **實際發生過，而且因此把正確的弦長改成錯的弧長，角柱一度長了 11%。**

  kang 的原話：「**像摺紙一樣把展開圖組合起來**」。摺紙沒有板厚。
  細節見 `規格\建模器-展開與分片.md`。

  ⚠ **但不要因此走到另一個極端**（2026-08-23 補）：
  **材料決定「這張圖做不做得出來」，不決定「這張圖上的數字是多少」。**
  尺寸一律是網格真值，跟材料無關 —— 見「尺寸的依據」那一節。

- **目前主力**：建模器。**三條生產路徑全部通了**：
  建模 → STL → 3D 列印；建模 → 展開 → DXF → CNC；
  建模 → **剖面分切** → DXF → CNC → 疊層黏合（`0507c51`，用法見 `規格\建模器-剖面分切.md`）
- **形狀也可以不在這裡建**：Illustrator 畫好的線稿存成 SVG 丟進來，
  直接擠出成 3D（`0b86cb8`，用法見 `規格\建模器-匯入線稿.md`）。
  剖面分切做完之後瓶頸換邊了 —— 任何封閉形狀都切得開，
  所以限制變成「**建不建得出來**」，而這一項是繞過建模
- **指定分片已完成**（2026-08-22，`08f5b90` ＋ `efa0c3b`）——
  接縫位置終於是使用者的決定，不再是攤平走訪順序的副產品。用法見 `規格\建模器-展開與分片.md`
- **編輯（第 6 期）已完成**：拉點線面／擠出面／**變換地基**／多選／
  **還原多邊形**／**壓平**／**環切**，全在工具列「編輯」那一組。
  用法見 `規格\建模器-點線面編輯.md`

- 🔴 **建模器現在有哪些編輯能力（工具列「編輯」那一組）** ——
  **⛔ 用法一律去 `規格\建模器-點線面編輯.md`，本檔只列有什麼。**

  | 按鈕 | 一句話 |
  |---|---|
  | `拉點線面` ＋方向／中心／數值 | 變換 ＝ 種類 × 方向 × 中心（地基，見下方那則） |
  | `擠出` | 從一個面長出新的一段，先長一格再用拉面調 |
  | `壓平` | 把幾個面壓到同一平面並自動併成一個面。⚠ 形狀會變 |
  | **`變成正圓`** ＋半徑 | 把選到的那一圈點推回一個正圓（**①壓到同一平面 ②推到同一半徑**）。半徑欄自動填擬合值，可打數字覆蓋。⛔ **不做平均分佈**（那是另一項） |
  | `環切` ＋刀數 | 順著形狀繞一圈加線，切在中間 |
  | **`沿面滑動`** ＋數量＋`公分`／`％` | **把選到的那一圈邊沿著表面挪位置**（環切的搭檔）。⚠ 跟 `拉點線面` 的差別：拉邊**會離開表面**，滑動**永遠貼著**。⛔ 只動座標、不改拓撲 |
  | **`平行複製`** ＋寬度 | **在選到的那一圈邊，兩側各加一圈平行的線**。做加強筋、邊框、溝槽。⚠ 跟 `沿面滑動` 吃同一種選取但做的事相反：滑動是**挪走**（形狀變），這個是**加線**（⛔ 體積面積精確不變）。⭐ 單位跟滑動**共用**同一顆 `公分`／`％` |
  | `內縮` ＋寬度 | 沿面的輪廓往內加一圈線（miter 修正過） |
  | `導角` ＋寬度＋段數 | 段數 1 ＝ 斜切邊，2 以上 ＝ 圓角。**只導一部分的邊也可以** |
  | `刪除面` ／ `補洞` | 一對。刪掉再補回來，體積面數頂點數精確還原 |
  | **`橋接`** ＋`轉幾格` | 物件上**剛好兩個洞** → 中間長出一段管（過渡段、接頭、變徑管）。⚠ **不必進編輯模式，也不選任何東西** —— 跟 `補洞` 同一類。兩根管子要先按 `∪ 聯集` 合成一個物件 |
  | `切一刀` ＋軸＋座標＋範圍提示 | 整個物件橫切，位置精確（世界座標） |
  | `多點連接` | 選好幾個既有的角，照順序一段一段連 |
  | `面上加線` ＋位置 | 兩條邊上各長一個點再連 ＝ 把面切成兩等分 |
  | `邊上加點` ＋個數 | **只放點、什麼都不連**，位置用「拉點」再調 |
  | `顯示點`（預設開） | 把所有的點標成小白圈，不必開線框 |
  | `分離` | 沿選到的那圈邊，把物件拆成兩個獨立物件 |
  | `刀具` ＋`吸中點` | **一個模式**：在模型上**點一串位置**，或**按住拖劃一條線**（兩種可混用），照那條路徑切開 |
  | `全選邊` ／ `選一圈` ／ **`選一條線`** ／ **框選（編輯模式）** | 選取。⚠ 難的一直是選取，不是功能本身。**`選一圈` 繞著物件跑、`選一條線` 順著同一條線走到底**（球的經線） |
  | **`選一圈面`** | 跟 `選一圈` **走同一條路，但選起來的是面**（方塊 4 片側牆、32 段圓柱 32 片、球一條緯度帶 12 片）。⚠ 一樣**先選一條邊** —— 從面出發有兩個方向，結果不唯一。按下去自動切到「面」 |
  | **`選轉角`** ＋幾度以上 | **依夾角一次選出所有折起來的邊**（方塊 12 條、32 段圓柱 64 條）。標分片切割線的捷徑。⚠ 平板是 0 條（整圈都是開口邊緣） |
  | **`選相似`** ＋判準 | 跟**最後選的**那一個同一類的全部選起來。四種判準：同法向／同面積／同邊長／同邊數 |
  | **指到哪就亮哪**（沒有按鈕） | 游標底下的點／邊／面會**變大變粗**。⛔ 不換顏色，所以可以跟「已選到」疊加 |
  | `法向`（修法向／翻面） | **不需要進編輯模式**。3D 列印那則「法向朝內」警告的出路 |

  ⛔ **加線那一類已經補齊**（環切／內縮／切一刀／多點連接／面上加線／
  邊上加點／刀具），**⛔ 不要重做**。⏭ 缺口剩**選取**（相連／增長縮減／
  依銳邊／選相似）與**合併頂點** —— 判準見「再下一步」那則。

- 🔴 **這幾輪「怎麼查出來的」全部在 `HISTORY.md`，⛔ 不要從頭讀，用 Grep。**

  2026-08-26 把 8/25 那十三輪的經過從本節搬過去了（**逐字，一字未改**），
  目次在 `HISTORY.md`「第 6 期編輯功能收尾」那一段。
  **本節只留「以後動它要知道的」**：規則、方向、否決紀錄、還沒做的事。

  ⚠ **搬走的不是垃圾** —— 例如「圓角的點數跟對照表對不上而兩個都對」、
  「miter 兩次犯同一個錯」、「把 `main.js` 改壞吃掉 18 個函式」，
  **要查那些事的經過就去 Grep**。規則的權威版仍在本檔「鐵律」那一節。

- 🔴 **環切帶出一條新規則，比環切本身重要：`hard` 邊。**

  > **`hard` ＝「這條邊是使用者刻意加的，即使兩側共面也要當它存在」。**

  這個專案原本有一條貫穿全域的規則：**「共面的邊 ＝ 畫面上看不見的邊 ＝
  不該存在的邊」**（它擋掉三角化的對角線，本來是對的）。
  **環切是第一個產生「共面但刻意存在」的邊的功能，它是這條規則的第一個例外。**

  那條規則有**四個出口，不開例外就四個全中**（實測）：
  畫不出來（`EdgesGeometry(geometry,1)`）、點不到（`isMarkable()`）、
  半塊面拉不動（`planarRegions()`）、按壓平會被併掉（`mergeCoplanarFaces()`，
  實測 F10 → F6）。**四個都改了。**

  ⛔ **日後任何「加共面的線」的功能都要記得標 `hard`**，否則就是坑第 21 條
  （按下去畫面上什麼都不會變）——「面合併」那顆按鈕已經中過一次。

- 🔴 **邊上的標記只有一個搬移入口：`mesh.marksOf()` / `mesh.applyMarks()`。**
  ⛔ **加第四個標記只改這一對，不要再去找那四條「拆掉重建」的路。**

  理由是這條規則已經**被違反兩次，兩次都出事**：
  2026-08-23 加 `smooth` 漏了一條路 → 展開圖從 5 處折彎變成 45 處；
  2026-08-24 加 `hard` 漏了兩條路 → **按一次「壓平」，環切的線 48 條全部歸零**。
  兩次的症狀都一樣：**東西安靜地不見了，而形狀完全正常。**

  ⚠ **第二次是 kang 實測抓到的，不是測試攔下來的** —— 當時的測試只驗了
  「併不動」那條路，沒有驗「真的併下去」那條。現在有一組**照 `marksOf()`
  自動展開、逐條邊比對**的回歸測試守著，加新標記會自動被涵蓋。
  〔坑第 31 條的現行示範：與其讓好幾條路對齊，不如換一個只有一條路的定義〕

- 🔴 **kang 定的方向（2026-08-24）：編輯能力要對齊 Blender。**
  他的原話：「**希望在編輯功能上能夠可以跟 Blender 的編輯功能有相同的能力**…
  因為我不清楚 Blender 到底跟我們的建模器還有哪些差異…所以我沒辦法詳細告知細節」。

  → 所以做了一張**完整能力對照表**：`外部參考-Blender編輯.md` **第 10 節**
  （`8902d43`；Blender 編輯模式全部 85 支操作 ＋ 全部 13 支選取方式，**第一手**，
  逐項標「有／值得做／用不到／明確不做／沒調查」）。
  🔴 **要決定做什麼之前先讀那一節**，不要再照舊路線圖推。

  🔴🔴 **⚠ 但先看那一節開頭的「最後回寫」日期**（2026-08-28 加）——
  **⛔ 日期比最後一個 commit 舊，表上的 ⭐ 就是假的。**
  ⚠ **那張表已經過期兩次**（8/27、8/28）—— 病因寫在它的 10.7 節開頭：
  **「做完一件功能」這條路上，沒有任何一步會走到這張表。**
  ⭐ **⛔ 刻意不做機械檢查**：8/28 試過「表上標 ⭐ 但工具列已有按鈕」那道，
  **9 項裡 7 項誤報**（「擠出頂點」撞到 `擠出`、「旋轉邊」撞到 `旋轉`），
  **誤報比漏報更糟**。→ 改用一個日期：它擋不住忘記回寫的人，
  但**擋得住「照著過期的表做決定」** —— 而那才是會付出代價的地方。

  三句話摘要（🔴 **2026-08-27 刷新過，舊版整段作廢**）：
  **服務動畫／算圖／UV／雕塑的佔了將近一半**（表上的 ➖，直接出局）；
  **⛔ 還缺幾項不寫在這裡（會過期）** —— 現算的指令在
  `外部參考-Blender編輯.md` 第 10.7 節「結論一」底下（⚠ **⛔ 不要自己簡化成
  `grep -c "| ⭐"`，那會多算 17 項** —— 理由寫在那裡）。

  **三大缺口現在只剩一個**：
  ✅ **加線已經補齊**（連接兩點／Bisect／刀具／面上加線／邊上加點／環切／內縮）、
  ✅ **補面已經補齊**（`補洞` 8/25、`點連成面` 8/27、**`橋接` 8/28**）、
  ⏳ **選取才是現在真正的缺口**（相連／增長縮減／依銳邊／選相似）。

  而「三項已經有一半、另一半幾乎免費」的預測**兌現了兩項**：
  ✅ 選一整圈邊（8/24）、✅ Bisect（8/25）、⏳ **變成正圓還沒做**。

  ⚠ **舊版寫「補面：沒有任何補洞的能力」「選取：32 片 seg 要點 32 下」**
  —— **兩句都已經不成立**，是 8/24 的狀態。

  🔴 **判準（kang 2026-08-24 澄清，這一條覆蓋舊的讀法）**：
  > 「不做全能軟體」的意思是「**不做跟建模無關的**」（動畫、算圖、UV、材質、
  > 骨架、細分曲面），**不是**「Blender 的建模功能我們只做一小塊」。
  > **建模編輯的功能要做齊。**

  🔴 **「做齊」是有時程意義的（kang 2026-08-25 補充）**：
  > 「現在建模器都還在建置開發中...**並沒有進入實際工作流程**..
  > 　我們要**整個建模器都完工後**...我才會真實的導入實際工作」

  → **現階段排順序的唯一判準就是這張對照表**，⛔ 不是「這個實際上常不常用」。
  沒有人在實際用它，那種話一律是推論冒充現實 ——
  完整推導見「鐵律 → 一、動手之前 → 目的藏在下游」底下那則但書。

  ⚠ **AI 誤讀過這一條**，還拿它去質疑 kang 的目標，說兩者衝突 —— 那不是衝突。
  對照表上標 ➖ 的正好就是「無關」那一堆，標 ⭐ 的**全部在範圍內**。
  推導與 kang 原話見 `編輯功能設計方向.md` 第 1 節那則澄清。

  ⚠ 這則澄清**翻掉兩項原本標「不做」的**：**細分**（原因是「四種補法要問 kang」，
  那是順序問題不是範圍問題 → 收回）、**平滑頂點／比例編輯**
  （確實是建模編輯，但當初以「雕塑類」否決 → ❓ 邊界未定，排到再問）。

- 🔴 **方向已定（kang 2026-08-25 批准）：把對照表上標 ⭐ 的那些重新拆成
  四個正交的動作，不要一項一項移植。**

  > **加線 ／ 加面 ／ 移除 ／ 移動**（移動已經做完了 ＝ 種類×方向×中心）

  理由是這個專案自己示範過：`變換 ＝ 種類×方向×中心` 拆好之後，
  **「壓平」與「沿法向推拉」是免費掉出來的**，一行新數學都沒有。
  拓撲那一半用同一招 → 內縮 ＝「加線 × 面的內縮輪廓」、
  導角 ＝「加線 ＋ 加面」、環切／細分／刀具／Bisect 全是「加線」換個對象。
  **按鈕從 25 顆變成 4 顆**，而且平板按得到（Blender 那套鍵盤流在平板上不成立）。

  ✅ **框架已經驗過兩次，兩次都成立**：
  導角那一輪（第 11 節，＝ 加線 ＋ 加面）與 **kang 選的試金石「內縮」**
  （2026-08-25 實作完成，**一行新的數學都沒有**）。
  → **照這個框走。**
  ⚠ **完整推導只在這一段，沒有另開文件** —— 要改方向就改這裡。

- ✘ **刀具「貼著表面走／環繞模型切開」明確不做**（kang 2026-08-26 結案，⛔ 不要重提）。

  > **他的原話：「因為『環繞模型切開』跟我們目前刀具功能邏輯行為是相同的，
  > 　因此不用做這功能。」**

  🔴 **這一條原本掛著一個前提已經消失的等待**：舊版寫「等他用過**穿透版**再問一次」——
  而穿透版**被他實測否決、已經拿掉了**，那個「等他用過」永遠等不到。
  ⚠ **日誌就這樣掛著一條等不到的待辦，沒有人發現。**

  **兩邊的事實（照鐵律，兩邊都驗過權威，⛔ 不是從文件推的）：**

  | | 出處 | 結果 |
  |---|---|---|
  | **我們的刀具** | 【實證】讀 `edit.js` `knifePath()`、`select.js` `pickEdgePoint()` | 線只長在表面、每段在單一個面內、切點吸在邊上、**面中間不准收尾** |
  | **Blender 的刀具** | 【實證】官方手冊 Knife 那頁 | **預設也是貼著表面走**（`Occlude Geometry`），切點落在線與邊的交點，**同樣不准在面中間開始／結束**（Known Limitations · Unconnected Cuts） |

  → **能切出來的東西是同一類。差別只在「那一串邊上的點怎麼指定」。**
  ⭐ 而我們還多一項自由：Blender 預設只切**看得見**的面（按 `C` 才穿背面），
  我們是**點到哪切到哪**，轉過去點背面天經地義 —— 那正是 kang 定的操作方式。

  ⚠ **AI 在這一條上連錯兩次（2026-08-26）**，兩次都是**停在文件就下結論**：
  先從規格檔的機制**推論**「第二版就是貼著表面走」要求結案，
  再說「Blender 那半我驗不了」—— **而官方手冊白紙黑字寫著**。
  🔴 **這就是「鐵律 → 推論不是權威事實」那條的立條實例。**

- ✘ **「切一刀」不改成「只切選到的面」**（kang 2026-08-25 否決，⛔ 不要重提）。
  ⚠ Blender 的 Bisect 確實是選取式的，所以這個直覺很合理 ——
  **kang 自己先提出來，問完七種切法之後自己否決**：
  > 「既然這是七種之一就不要改…因為**一改就又跟刀具功能差不多了**」

  🔴 **判準是「功能之間的定位不可以互相模糊」，不是工多工少。**
  → 分工定死：**切一刀** ＝ 整個物件橫切、位置精確；
  **環切** ＝ 順著形狀繞一圈、切在中間；
  **刀具／連接兩點**（還沒做）＝ 在一個面上加一條線。

  💡 **kang 的第一個反應值得記**：「切一刀為什麼跟環切一樣？」——
  **在方塊上兩者結果確實一模一樣**，他看到的沒錯。差別要別的形狀才看得出來：
  環切切不出「離底部 12.5 公分」，也走不動球與圓錐。

- ✘ **「跨過好幾個面」那一半明確不做**（2026-08-25，⛔ 不要重提）。

  Blender 的 Connect Vertex Path 只選兩個點時「會**切過沒被選到的面**…
  限制在相連的面上做直線切」（官方原文，這次讀到內文了）。

  🔴 **不做的理由是結果不唯一**：方塊選頂面一個角、底面另一頭的角 ——
  那條線要從側面的哪裡通過？**繞左、繞右、斜著過去都說得通。**
  ⛔ 補不到唯一就明講，不要猜（坑第 24 條）。

  ⭐ **真要跨面切，「刀具」才是誠實的答案** —— 畫到哪切到哪。

  ⚠ **日誌原本估「連接頂點路徑比刀具便宜」，那個估計是錯的** ——
  它裡面藏了兩件事，一件便宜（依序連）、一件比刀具還難（跨面找路）。
  〔又一次「查清楚之後原本的排序不成立」〕

- ✅ **今天三批介面實測，kang 全部通過（2026-08-27，共 26 項）**
  —— `選轉角`＋`選相似` 11 項、`變成正圓` 8 項、**破洞在哪裡** 7 項。
  🔴 **三張完整清單（含每一項的預期數字與對答案的經過）已逐字搬進
  `HISTORY.md`「三批實測清單全過」那一節** —— ⛔ 不要從頭讀，Grep 節名跳過去。

  🔴🔴 **只有這一條要留在開場，因為 ⛔ 不講的話下一個人會以為按鈕壞了**
  〔kang 2026-08-27 實際卡住過〕：

  > **只要物件有板厚，破洞就會被 `shell()` 自動加厚救回來
  > → 3D 列印面板那則紅字不出現 → 「指出來」那顆按鈕也不出現。**

  ⚠ **而板厚欄位只有「種類 ＝ 板件 sheet」時才看得到**，
  但那個值（`io.js` 預設 **0.2**）**在實體 solid 上照樣生效** ——
  **看不到、改不了，卻在影響結果**。
  → **要測「指出來」，先把種類切成 `板件 sheet`、板厚打 `0`。**
  ⭐ **kang 拍板：「不算問題，就這樣」**（自動加厚正是想要的行為），
  ⛔ 不要為了讓按鈕更常出現去動它。

  ⏳ **三批都只涵蓋桌機** —— 平板版面仍然沒驗，跟刀具那三項卡在同一件事。

- ⏳ **待實測（兩批）**：

  1. 🔴 **`平行複製` 六項（桌機）** —— 清單在「待辦」那一條，Grep `待實測：\`平行複製\``。
     ⚠ **順手按一下 `環切` 確認沒壞** —— 這一輪把它的後半段抽出來共用了。
  2. **刀具三項輔助的平板三項**（見下）。⚠ 卡的是**平板不在身邊**，不是還沒做完。

  ✅ **`沿面滑動` 八項**（2026-08-28 桌機全過。🔴 **第 8 項是我的預期寫錯**，
  　　⛔ 不是程式的問題 —— 經過在 `HISTORY.md`「2026-08-28 搬入的 7 條」那一節）。

  ✅ **`橋接` 七項**（2026-08-28 桌機全過。🔴 **第 3 項照出一個 bug 並當天修掉** ——
  　　「開放的網格也算得出體積」，同上那一節）。
  　　⭐ **規則的權威版在「鐵律 → 三、做介面與回饋時」**
  　　（「這個數字在這個狀態下，有沒有意義？」），⛔ 那條沒有跟著搬走。

  ✅ **3D 列印面板那四則**（2026-08-27 桌機全過，清單在下一則）。
  ✅ **`選一圈面` 五項**（2026-08-27 桌機全過，清單見下）。
  ✅ **`標尺寸`（量測第 2 步）七項**（2026-08-27 桌機全過。⚠ 第一版被他當場退回過
  　　—— 經過與完整清單在 `HISTORY.md`「量測第 2 步：標尺寸」那一節）。
  ✅ **`量距離` ＋ `量圓`（量測第 4 步）九項**（2026-08-27 桌機全過。
  　　⚠ **圓那一半的第一版被他用四張截圖打掉過** —— 經過與完整清單在
  　　`HISTORY.md`「量測第 4 步：量距離 ＋ 量圓」那一節）。
  ✅ **縮放（量圓的半徑 ＋ 結構分析的面積體積）四項**（2026-08-27 桌機全過。
  　　⭐ 他的數字自己吵出答案：半徑／弦長／周長／差額**全部同一個倍數** ——
  　　經過在 `HISTORY.md`「縮放：量圓的半徑與結構分析的面積體積」那一節）。

- ✅ **實測清單：`選一圈面` —— kang 2026-08-27 五項全過（桌機）**

  **前置**：`轉成可編輯網格` → 按 `拉點線面` 進編輯模式 → 上面那排切到「**邊**」。

  | # | 做什麼 | 結果 |
  |---|---|---|
  | 1 | **方塊**，點側面一條**直立**邊 | ✅ **4 個面**，總面積 **9000 cm²** |
  | 2 | 看上面那排「點／邊／面」 | ✅ **自動變成「面」** |
  | 3 | **圓柱** seg32，點一條**直立**的側邊 | ✅ **32 個面**，總面積 **10977.92 cm²** |
  | 4 | 同一個圓柱，改點一條**橫的**邊 | ✅ 另一個方向 —— **2 個面**（一片側面 ＋ 一個圓蓋），見下 |
  | 5 | **球**（預設 經線 32 緯線 16），點一條**直立**的邊 | ✅ **32 個面** ＝ 同一個高度繞一圈的帶子 |

  ⭐ **⛔ 不必相信我，那兩個面積自己把答案驗出來了**（鐵律三）：
  方塊 2×(60×45) ＋ 2×(40×45) ＝ **9000** ＝ 正好四片側牆；
  圓柱 32 × 弦長 4.9009 × 高 70 ＝ **10978.0** ≈ 面板的 10977.92。

  🔴🔴 **⛔ 這張清單原本寫錯三個地方，⛔ 不要照舊版再開一次**
  （kang 四個提問全部命中）：

  | 我原本寫 | 實際 |
  |---|---|
  | 「⛔ 頂底不亮」「⛔ 上下圓蓋不亮」 | ⚠ **講的是「面」，⛔ 不是邊** —— kang 兩次問「是指邊嗎?」。**⛔ 講數量與形狀時，單位要講清楚是點、邊還是面** |
  | 「球（**經線 12、緯線 32**）→ **12** 個面」 | 🔴 **兩處都錯**：預設值是 **經線 32、緯線 16**（`prim.js`），而且要點**直立**的邊。**kang 回報的 32 才是對的** |
  | 第 5 項叫他點「**橫的**」邊 | ⛔ 橫的邊走的是**經線方向**（實測 8 個面）；**繞一圈的帶子要點直立的邊** |

  🔴 **第 4 項挖出一個真 bug，已修**（見下方那則）。

- ✅ **實測清單：3D 列印面板的四則報告 —— kang 2026-08-27 全部通過（桌機）**

  🔴 **清單一定要放在日誌裡，⛔ 不可以只寫在 commit 訊息**（坑第 34 條）。

  | # | 做什麼 | 結果 |
  |---|---|---|
  | 1 | 方塊 → 按 `3D 列印` | ✅ 綠色「可以列印」，⛔ 一則紅字都沒有 |
  | 3～6 | `翻面` → `3D 列印` → `修法向` → `3D 列印` | ✅ 紅字一則 → 修完回到綠色 |
  | **新** | 同上，看那一則的**文字** | ✅ **「整個物件的面都朝內…按工具列的「修法向」就會自動修好」**（截圖逐字對過，⚠ 舊版沒有後面那半句） |
  | 7 | 平板板厚 0 → `指出來` | ✅ 四周變紅線、講「1 個破洞」（⛔ 不是 4 個） |

  ⚠ **第 2 項寫錯了（見下），⛔ 已作廢，不是通過。**

  ⚠ **⛔ 清單原本寫「只有一則」不精確，⛔ 不要照抄** ——
  應該是「**紅色的只有一則**」。kang 的方塊是 60×40×45 cm，
  所以底下還有一則**黃色**的「最長邊 600mm 超過常見成型範圍 250mm」——
  **那是對的，不是雜訊**（坑第 28 條：紅色只留給「程式做不到你要求的事」）。

  🔴🔴 **⛔ 第 2 項那句「點選一個面按 `翻面`」是錯的，⛔ 不要照它再開一次清單。**

  > 【實證】**`翻面` 翻的是整個物件**，⛔ 不是選到的那一個面。
  > `edit.js` `flipNormals()` 的註解寫著理由：
  > **「`recalcNormalsOutside()` 要修的病，我們沒有理由提供一個製造它的按鈕。」**

  ⚠ **連帶把「他驗過什麼」也翻掉了**：3～6 項他確實通過，但走的是
  **「法向朝內」**那條既有的路，⛔ 不是清單上寫的「正反面貼反了」那則。
  ⭐ **抓到它的是 kang 一句「第 2 項跟你說的不一樣」** ——
  🔴 而病因是**我沒讀 `flipNormals()` 就寫了預期畫面**（推論不是權威事實）。

  🔴 **三則裡有兩則 kang 做不出來，⛔ 這件事要寫在檯面上**：

  | 那一則 | 使用者做得出來嗎 |
  |---|---|
  | **整個朝內**（`inward`） | ✅ **按 `翻面` 就是** ← **唯一測得到的** |
  | **貼反了**（`flipped`，一部分面反） | ❌ **沒有任何按鈕做得出來**，⛔ 而且是刻意的 |
  | **黏在一起**（`nonmanifold`，紫線） | ❌ 參數基本體／布林／編輯功能全試過，一條路都走不到 |

  ⭐ **後兩則仍然要留**：它們是**程式自己弄壞網格**、或**日後匯入 STL／OBJ**
  時唯一的線索 —— ⛔ 不做的話那種情形只會印出「邊 0→1 出現兩次」。
  ⚠ **但 ⛔ 不要再把它們寫進實測清單**，那是開一項驗不了的。

  ✅ **選取那一組第一批的十項已經全過**（hover ／ 選一條線 ／ 編輯模式框選）。
  ✅ **狀態列「三角形」四項也全過**（2026-08-26。第 5 項「滑鼠移上去看說明」
  　　**作廢不是通過** —— `#status` 是 `pointer-events: none`，`title` 永遠跳不出來；
  　　kang 拍板維持現狀、只把死掉的說明拿掉。經過見「待辦 → 狀態列的『三角形』」）。

- ⏳ **刀具：只剩平板，三項**（2026-08-26 更新。
  **桌機那十項 kang 已經全部驗過**，清單在「待辦 → 刀具的三項輔助」）：

  1. 🔴 **單指／觸控筆畫得出線、兩指轉得動視角**
  2. 🔴 **兩指轉視角時，⛔ 不會憑空多出一條切線**
     ⚠ **這一項沒有人實際踩過**，是照「右鍵那個 bug」同一個病**推出來**順手擋的
     —— 所以它比其他項更需要真的驗一次（推論不是權威事實）。
  3. **版面**：工具列這幾輪多了「內縮＋寬度」「導角＋寬度」「刪除面」「補洞」
     「選一圈」「法向」，8/26 又多一顆「**吸中點**」，**有沒有被擠掉或切掉**。
     ⚠ `.grp` 已經有 `flex-wrap`（單一組塞不下會換行，不會被切掉），
     但整體高度會不會吃掉太多畫面沒驗過。

  🔴 **刀具那批第一次出現「換掉既有手勢」這種改動**，而且**當場就出事了** ——
  以前加功能都是多一顆按鈕，這次是**把左鍵拖曳的意思換掉**，
  結果右鍵拖變成「OrbitControls 轉視角」與「一筆畫記線」**兩個收件人在搶**。
  ⭐ **教訓已升格**：換手勢就一定要開始分「哪一顆鍵」，而且
  **鍵要在按下去的當下記起來**（＝ `TransformControls.axis` 那條）。
  完整內容在 `規格\建模器-點線面編輯.md`「換了手勢，就一定要分哪一顆鍵」。

  🔴 **這張清單一定要放在日誌裡，不可以只寫在 commit 訊息** ——
  commit 訊息是流水帳，下次覆蓋就沒了（坑第 34 條）。
  ⚠ **這條規則已經被違反兩次**。**寫完 commit-msg 就順手同步這裡。**

  ⚠ **用語**：說「提示訊息」不要說 **toast** ——
  那是 3D 畫面正上方中央那條圓角橫條，做完動作跳出來、2.6 秒後自己淡掉。
  〔kang 2026-08-25 問「toast 是什麼意思」。**跟他講話不要用程式圈的行話** ——
  　他驗的是畫面，不是原始碼〕

- ⚠ **`VERT_DOTS_MAX` ＝ 5000，那個數字是猜的，沒有量過。**
  超過就不畫並**講出實際數量**。桌機幾乎確定沒問題，
  但**平板的顯示晶片弱很多，而沙箱連畫面都驗不了**。
  🔴 **等 kang 平板實測再照真實結果調**，⛔ 不要為想像中的數字辯護。
  ⭐ **開關本身就是最好的保險** —— 覺得卡就關掉。

- ✘ **工具列的優化延後，⛔ 不要主動提**（kang 2026-08-25 決定）。
  > 「等建模器**完成一定的成果**後..我們再討論優化工具列的事情」

  ⚠ **現況要記下來，免得下一個 AI 以為是壞掉了**：這幾輪加了
  `顯示點`／`邊上加點`＋框／`多點連接`／`面上加線`＋框之後，
  **桌機的寬螢幕都已經換行**（kang 2026-08-25 截圖：「面上加線 0.5」
  掉到第二行）。

  ⭐ **那是 `.grp` 的 `flex-wrap` 在正常運作** —— 換行不是被切掉，
  按鈕全都還在、按得到。所以它現在**沒有在害人**，
  照「為了整齊去改能用的東西，不划算」那條先不動。

  🔴 但「平板版面沒測」那一項的份量因此變重了：
  從「應該沒事」變成「**確定會換行，只是不知道會不會吃掉太多畫面高度**」。

- 🔴🔴 **再下一步（2026-08-27 更新。⚠ 上一版寫「下一件 ＝ 鬆散元素」，
  那一項當天結案為「不做」）**：

  > 🔴 **「選取」那一格已經全部結束了，⛔ 下一件要從對照表重挑，
  > 　而且要問 kang。** 三個現成的候選（增長縮減／量測第 2 步／合併頂點）
  > 　與各自的卡點，寫在本節下方「**選取那一格全部結束**」那一則。

  > ⏳ **刀具三項輔助的桌機十項已經全過，只剩平板三項**（要等 kang 手邊有平板）。
  > 🔴 **日誌裡還掛著 `⏳ 待實測`，就不要開新功能** ——
  > ⚠ 但這三項卡的是**硬體不在身邊**，不是還沒做完。
  > **要開下一件之前先問一句「平板拿到了嗎」**，⛔ 不要無限期停在這裡。

  ✅ **選取第二批的「甲」已完成並實測通過**（2026-08-27）：
  **`選轉角`（依銳邊）＋ `選相似`** —— 介面 11 項 kang 全過（清單在上面那一批）。

  ⭐ **這一輪把四項拆成兩個正交的動作，⛔ 不是一項一項移植**
  （照 kang 2026-08-25 批准的框架）：

  | 動作 | 涵蓋 | 狀態 |
  |---|---|---|
  | **甲、照判準掃全物件** | 依銳邊、選相似 | ✅ **完成並實測通過** |
  | **乙、從選到的往外長** | 相連 ✘／增長縮減 ⏭ | ⚠ **⛔ 兩個的理由不一樣**：相連是**功能重複**（✘），增長縮減是**排到後面**（⏭，⛔ 不是不做）。見待辦那條 |

  ✅ **`變成正圓` 已完成並實測通過**（2026-08-27，介面 8 項 kang 全過）——
  對照表第 10.5 節標 ⭐⭐ 的那一項，測試 2012 → **2034 項**。
  ⭐ **「三項裡另一半幾乎免費」那個預測第三項也兌現了**
  （前兩項：選一整圈邊、Bisect）。

  ✅ **依特徵全選的第一項「破洞在哪裡」已完成**（2026-08-27，✅ 介面 7 項 kang 實測通過）——
  3D 列印面板那則「不是封閉的」旁邊多一顆 **`指出來`**，
  按下去破洞在畫面上變成**紅線**。測試 2034 → **2048 項**。

  🔴 **它做成「只畫不選」，⛔ 不進選取，理由是實測過的代價**：
  `isMarkable()` 刻意把邊界邊擋在選取之外（「它本來就是外輪廓」），
  要選就得開**第二個例外**，而那條規則開一次例外**有四個出口全要改**
  （環切那一輪實測）。⭐ 而使用者要的只是**看得到**，`補洞` 本來就存在。
  〔kang 2026-08-27 把這個決定交給 AI，理由是他還沒有 3D 列印經驗〕

  ⭐ **它補的是一個「講了問題卻沒有出路」的洞**（坑第 11 條）——
  ⚠ 而 `exportPanel.js` 的**檔頭一直寫著**「講得出是哪裡的問題」，
  **那句話在這一輪之前是假的**。

- ✅ **「3D 列印」面板現在把四種病分開講，每一種都給得出出路**
  （2026-08-27 完成，**桌機 kang 實測全部通過**）。測試 2048 → **2097 項**。

  | 面板會說 | 出路 | 顏色 | kang 做得出來嗎 |
  |---|---|---|---|
  | 「**整個物件的面都朝內**」 | 訊息寫「按 **`修法向`**」 | — | ✅ **按 `翻面` 就是** |
  | 「有 N 個面的朝向**跟鄰居相反**」 | 同上 | — | ❌ 沒有按鈕做得出來 |
  | 「有 N 條邊被 **3 個以上的面共用**」 | `指出來` | **紫線** | ❌ 一條路都走不到 |
  | 「不是封閉的（N 條邊界邊，**M 個破洞**）」 | `指出來` ＋ `補洞` | **紅線** | ✅ 平板板厚 0 |

  🔴🔴 **⛔ 三種病 ⛔ 不是同一件事，而日誌與 `mesh.js` 都把它們混成一句話。**

  > **`mesh.js` 那句「邊 a→b 出現兩次（非流形…）」同時涵蓋兩種病，
  > 　而其中一種根本不是非流形。**

  | | 是什麼 | 修法 | kang 做得出來嗎 |
  |---|---|---|---|
  | **貼反了** | 兩個面在同一條邊上**走同方向** | ✅ **`修法向`** | ✅ **`翻面` 只選一個面就是** |
  | **真非流形** | 一條邊被 **3 個以上**的面共用 | ⛔ **`修法向` 修不了**，要自己刪面 | ❌ **現有功能一條路都走不到** |

  【實證 2026-08-27】方塊翻一個面：`mesh.issues` 說「非流形」**6 次**，
  而真正的非流形邊是 **0 條**。⛔ **訊息叫使用者去找一個不存在的東西。**

  🔴 **⚠ 上一版日誌在這裡寫「非流形邊比破洞難，`issues` 存的是字串」——
  那句只對了一半，而漏掉的那一半更重要**：
  ⭐ **`edit.js` 的 `recalcNormalsOutside()` 早就用 `byEdge` 算過了**
  （無向邊 → 用到它的面），搬過來就好，**一行新的數學都沒有**。
  🔴 **而且它刻意不靠半邊結構**，理由寫在那支的註解裡：
  `fromFaceList()` 只配得出一組 twin，**第三個面那條半邊落單** →
  被補成邊界半邊 → **「哪幾條邊是非流形」這個問題，半邊結構自己答不出來。**

  ⭐ **順手修掉四個誤報**（坑第 18 條，全部沙箱實測抓到）——
  ⛔ **完整清單不抄在這裡，在 `版控工具\commit-msg.txt` 與 `HISTORY.md`**。
  只留一句非留不可的：

  > 🔴 **面貼反了會讓另外三則報告一起說謊**（破洞 6 條／半邊 113 沒有 next／
  > 　「由 2 個互不相連的塊組成」）—— **那是一個方塊，一個洞都沒有，也只有一塊。**
  > **三則都是症狀，現在全壓掉了**，只講「貼反了」那一則。

  🔴🔴 **選取那一格全部結束（2026-08-27）—— ⛔ 下一件要從對照表重挑。**

  > ⚠ **這裡原本寫「下一件 ＝ 鬆散元素」，⛔ 那句已經過期**（同日結案為
  > 「被 `cleanRebuild()` 擋住，不做」）。**⛔ 不要照它動工。**

  | 選取那一格 | 狀態 |
  |---|---|
  | 依銳邊（`選轉角`）／選相似 | ✅ 實測通過 |
  | 破洞／非流形（＋整個朝內、面貼反了） | ✅ 實測通過 |
  | 鬆散元素 | ✘ **被 `cleanRebuild()` 擋住** |
  | 面迴圈（`選一圈面`） | ✅ 實測通過 |
  | 相連 | ✘ **功能重複**（物件只有一塊時 ＝ `全選邊`） |
  | **增長／縮減** | ⏭ **排到後面，⛔ 不是不做** ← **選取那一格只剩它** |

  🔴 **下一件要問 kang 挑，⛔ 不要自己接著推。** 三個現成的候選：

  | 候選 | 現況 |
  |---|---|
  | **增長／縮減選取**（⭐） | 走訪現成（`separateAlongEdges()` 裡那段擴散走一層）。⚠ **有一個沒解的問題**：「擴」的定義是共用一條**邊**還是共用一個**點**（方塊上兩種一樣，球才分得出來：5 個 vs 9 個。Blender 用共用點）—— 結果不唯一，坑第 24 條 |
  | ~~**量測第 2 步**~~ | ✅ **kang 2026-08-27 挑了這一件，已完成並實測通過**（七項全過）。⛔ 這一格不要再當成候選 |
  | **合併頂點** | ⚠ **最貴** —— 會打破「既有頂點保持原索引」的契約 |

  ✅ **量測第 4 步已完成並實測通過**（九項全過），
  ✅ **縮放那兩件也完成並實測通過**（四項全過）——
  🔴 **量測整條線只剩一個決定要等**（第 2 步的「每一個」）。
  ✅ **`橋接` 2026-08-28 完成並實測通過**（七項全過）——
  **`⏳ 待實測` 現在只剩「刀具的平板三項」，而那一批卡的是硬體。**
  ⭐ **所以下一件（`沿面滑動邊`）可以開**，⚠ 但開之前先問一句「平板拿到了嗎」。

  🔴🔴 **下一件已經定了，⛔ 不必再問 kang 挑**（他 2026-08-27 拍板的順序）：

  | 順序 | 項目 | 開工前要知道的 |
  |---|---|---|
  | ✅ | ~~點連成面~~ | 完成並實測通過（六項全過）|
  | ✅ | ~~橋接邊迴圈~~ | **完成並實測通過**（七項全過）。🔴 **它 ⛔ 不是「選兩圈邊」** —— 見檔頭第 3 條 |
  | ✅ | ~~沿面滑動邊~~ | **完成，⏳ 待 kang 實測**。⚠ **日誌原本寫「難在拖曳介面、⛔ 不是數學」—— 只對一半**：數學確實現成，但它藏著「公分 vs ％」這個**結果不唯一**要拍板（kang 2026-08-28 選**兩個都給**）|
  | **1** | **內建鋼筆（貝茲繪製）** | ✅ **畫在地板上**已拍板；⭐ `flattenCubic()` 與擠出都是現成的 |

  ⛔ **細節一律去「待辦」那三條**（Grep `橋接邊迴圈` / `內建鋼筆`），
  ⛔ 不要在這裡抄第二份。
  ⚠ **要開之前先問一句「平板拿到了嗎」**（2026-08-27 問過：還沒有）。

  ⏭ **量測剩一條沒結案**：**第 2 步的「每一個」**（等 kang 的呈現方式）——
  ⚠ ⛔ 它不算未完成的功能，它在等一個決定。

  ⏭ **瓣片展開 A／B 由 kang 2026-08-27 排到後面**，⛔ 不是否決，
  也 ⛔ 不要再拿 A 當「隨時可插隊的小項」—— **他要 AB 一起做**。

  **之後**：合併頂點（⚠ 最貴，會打破「既有頂點保持原索引」的契約）。
  ⚠ **上一版在這裡寫「選取那一類 ⭐ 同時解掉『瓣片展開 A 要點 384 條邊』
  那個卡點」—— 那句是錯的**（2026-08-27 更正）：選一條線**只解掉走訪那一半**，
  **搬運那一半沒解**（`選一條線` 在編輯模式、標切割線在分片模式，兩個互斥，
  `setEditMode(false)` 會把選取清光）。⭐ 同一句錯話原本寫在**兩個地方**，
  兩處都已更正 —— **只改一處等於沒改**。
  其餘從對照表挑（`外部參考-Blender編輯.md` 第 10 節）。
  任意切線自己還缺兩項：**非凸的面被穿超過兩次**（要切成 3 片以上，
  實測球跳過 0 個所以先不做）、以及**「拿選到的面當平面」只差介面**。
  對照表點出來還缺的三類是**加線**（手動畫線切開做不到 ——
  任意切線補掉了一半）、**補面**（✅ 已完成）、
  **選取**（相連／增長縮減／依銳邊／選相似）。

- 🔴 **量測已經開工**（kang 2026-08-25 提出）。用法與紅線見
  **`規格\建模器-量測.md`**。三句話摘要：

  > **bake 之後尺寸就沒了，只剩座標 —— 所以尺寸一律從網格現算回來。**

  1. ✅ **第 1、3 步完成並實測通過**：選到邊看得到長度、選到面看得到
     面積與周長、多選看得到總長／總面積／外框、結構分析多了**外框 X×Y×Z**、
     拖曳讀數的標籤會跟著講「移動／旋轉／縮放」。測試 1574 → **1606 項**。
  2. 🔴 **面板的座標從網格本地座標改成世界座標** —— 物件一被搬動或縮放，
     原本的數字就跟對齊／貼合／切一刀／剖面分切**對不起來，而畫面上看不出來**。
     現在五個地方是同一套數字（kang 為切一刀拍板的那條，這裡照套）。
  3. ⛔ **現算，不存**；⛔ **多選時沒有意義的量回 `null`，不硬湊**
     （多選面的周長會把共用的邊算兩次）；⛔ **不做會跟著模型走的持久標註**
     —— 標註會引用頂點，而編輯功能每一個都在拆掉重建網格。

  ⏳ **第 2 步（把數字畫到 3D 畫面上，只有選到的才顯示）與第 4 步
  （量兩個元素的距離 ＋ 圓的半徑／弧長／弦長）還沒做。**

- 🔴 **展開那一側多了一條路：瓣片展開＋省道**（kang 2026-08-25 提出，
  **方案已查完，⏭ 但 2026-08-27 由 kang 排到後面**）。

  > **kang 2026-08-27：「既然這新的展開方式…整體需要仔細調查討論與解決…
  > 　這先不要先做」／「不是不做..是排到後面」／「如果要做 AB 全要一起做」**

  ⛔ **不是否決，是順序。** 三件事一起記著：**A 與 B 綁在一起做**、
  **A 不再是可插隊的小項**、**A 的選取卡點只解掉一半**（走訪解了、
  搬運沒解 —— 模式互斥，見待辦區那條）。

  完整內容在 **`瓣片展開與省道-設計方向.md`**，動工前讀完整。三句話摘要：

  > **切割線吸收的角虧 ＋ 省道吃掉的角虧 ＝ 4π。永遠。**

  1. ✅ **零省道的球我們今天就做得出來**（沙箱實測）：把每一條經線標成分片
     切割線 → `segW=12、segH=32` 出來就是 **12 片、省道需求 0.00°、零警告**，
     也就是地球儀紙模那張圖。**瓣片數 ＝ segW，縱向圓潤度 ＝ segH，一行新數學都沒有。**
     🔴 **缺的只有選取介面** —— 現在要點 384 條邊（＝「全選邊」那一輪的教訓重演）。
  2. 🔴 **跨不過去的線**：零失真的球，橫向**一定**是 `segW` 邊形。
     平的紙只能朝一個方向彎，要橫向也圓就是雙向曲率，非拉伸不可。
     ⭐ 這跟「尺寸的依據」是同一句話：**想更圓就把 seg 開高**。
     ⛔ 不要為了「讓它更圓」去動展開的數字。
  3. ⛔ **拉伸／鬆份與充氣物理明確不做**（kang 自己也否決了）——
     前者撞紅線（圖上的數字不再是網格真值），後者是獨立的大工程。

  ⚠ **它不取代剖面分切也不取代現有展開，三條並列** —— 它補的是
  「**大型造型件**」那一格（實心疊層太重、現有展開會爆片）。
  分工表在那份文件第 2 節，判準沿用「切一刀 vs 環切」那條。
- 🔴 **編輯的地基已經完成** —— **變換 ＝ 種類 × 方向 × 中心，三個都開了**：
  種類（移動／旋轉／縮放）、方向（**世界／法向**）、中心（**重心／最後選的**）。
  按下「拉點線面」之後右邊會多出「方向／中心／數值」那一組。
  梯形、收尖、斜面推拉做得出來了；`Esc` 取消、拖完可打精確數字。
  **子元素也可以多選了**（開工具列那顆「加選」或按 Shift），
  橘色的那個是 active —— 法向的切線與「最後選的」中心都看它。
  〔第二件 `94f6c3f`；多選 `42a003f`。**兩輪 kang 都實測通過**〕
- 🔴 **「把幾個面合併」這個題目已經完整（2026-08-24），三條結論**：

  | 動作 | 狀態 |
  |---|---|
  | **還原多邊形**（把共面三角形併回一個面） | ✅ **沒有按鈕**，`bake()` 時自動做。畫面上看不出來是正常的 |
  | **壓平**（把選的面壓到同一平面，然後自動併成一個面） | ✅ 工具列 `壓平`。⚠ 形狀會變 |
  | **溶解面**（不管夾角直接併成 n 邊形） | ✘ **在「要切出來」的用途下不做** —— ⛔ **不是做不到**（網格建得起不平的 n 邊形），是**那張圖切不出來**。🔴 **權威版在 `規格\建模器-點線面編輯.md`「壓平之後併成一個面是免費的」那一節**，⛔ 不在這裡抄第二份 |

  ⛔ **不要因為 Blender 有溶解面就補上**（那是「為了對齊而補」）。
  想把幾片變成一個面，**在要切出來的用途下，正解是 `壓平`**。
  ⚠ **但 ⛔ 不要把它讀成「這件事不能做」** —— 它是被用途擋住的，
  🔴 **哪天出現不用切的用途（例如只是想少點 32 下），這一條要重新看。**

- ⚠ **不要再自己寫 `for (let i = 2; i < vs.length; i++)`（扇形三角化）。**
  那只對**凸**多邊形成立，而它曾經有 8 個出口，每一個都是同一個 bug
  （畫面、面積、STL、布林、展開面積都錯過）。
  全專案唯一的入口是 **`mesh.faceTriangles()`**。

- ✅ **外部參考調查已經做完了（2026-08-23）。不要重查。**
  三條線（Blender 編輯／兩個開源展開器／Pepakura）全部走完，四份文件見上表。
  界線與篩子在 `外部參考調查.md` 第 2、3 節，**動編輯或展開之前先讀那一份**。
- 🔴 **尺寸的依據已經定調（2026-08-23，`4361556`）**：

  > **展開尺寸 ＝ 網格攤平後的總和。與材料無關。**

  模型就是網格，**網格裡沒有曲面，一個都沒有**。所以 seg 段的「圓柱」
  就是一根 seg 邊柱，展開寬 ＝ seg 片平板相加。
  **要更接近真正的圓就把 seg 開高** —— 那是建模階段的決定。
  詳見「尺寸的依據」那一節（本檔下方），**動展開之前一定要讀**。
  → **下一步照「三件事」那一節做**（本檔下方），不要照原八期路線圖推。
- **第 6 期的範圍在 2026-08-23 討論後大幅釐清。**
  ⚠ **動編輯相關的任何東西之前，先讀 `編輯功能設計方向.md`** ——
  那裡有定位、範圍、被否決的方向。不讀會重提已經否決過的東西
- **絕對不要執行任何 git 指令**，連 `git status` 也不行（理由見「協作規則」）
- **建模器不能雙擊開啟**，它用 ES 模組與 WebAssembly，必須走伺服器
- **改過建模器核心就跑 `node 建模器/tests/run.mjs`** —— 全部是數學驗證，對不上就是改壞了。
  🔴 **⛔ 總項數不寫在任何文件裡**（跑完自己會印「通過 N　失敗 0」）——
  理由見「文件分層 → ⛔ 會過期的數字一律現算，不存」
- **要送出去給別的軟體開的東西（DXF、SVG、下載檔），沙箱驗不了** ——
  一定要 kang 用真正的軟體開過才算數。見「鐵律」五
- 子專案編號 2（鋁擠型框架設計器）**已移出、不在版控內**，文件只是留存參考

---

## 🔴 尺寸的依據（2026-08-23 定調，`4361556`。動展開之前必讀）

> **這一節凌駕本檔與 `規格\` 裡其他關於展開尺寸的一切說法。**
> 對不上時以這一節為準。
>
> 🔴 **推導過程、被推翻過的三個版本、成熟工具怎麼做、kang 的完整原話
> 　　在 `尺寸依據的推導.md`。** 本節只放「目前確定的狀態」。
> 　　**想推翻這條、或又冒出「差額是不是誤差該補回去」的念頭時，去讀那一份。**

### 那句話

> **展開尺寸 ＝ 網格攤平後的總和。與材料無關。**

攤平是**剛體運動** —— 每個面原地轉到同一個平面，長度一格都不變。
所以攤出來的數字**就是**這個模型真正的尺寸，不需要任何修正。

**模型就是網格，而網格裡沒有曲面，一個都沒有。**
r=25、seg=32 的「圓柱」在網格裡是一根 32 邊柱，展開寬 ＝ 32 片相加 ＝ **156.83**。
2πr ＝ 157.08 算的是一個**從來沒被做出來過的理想圓**。
**起點不同，不是誰比較準。** 誤差方向永遠固定：弦短於弧。

### 想要更接近真正的圓 → **把 seg 開高**

| seg | 弦長總和 | 差 2πr | 捲起來的半徑誤差 |
|---|---|---|---|
| 4 | 141.42 | 15.66 | 24.9 mm |
| 8 | 153.07 | 4.01 | 6.4 mm |
| 32 | 156.83 | 0.25 | 0.40 mm |
| **64** | 157.02 | 0.06 | **0.10 mm**（＝本專案的物理尺度，坑 25／26） |
| 128 | 157.06 | 0.02 | 0.03 mm |

**精緻度是建模階段的決定，不是展開階段要補救的事。**
沒有人會問「立方體的 6 個面該不該修正成別的數字」—— 圓柱有 32 個面，道理一樣。

### 材料還管什麼、不管什麼

| 材料**還是要**回答 | 材料**不再**回答 |
|---|---|
| `canFold` 這條邊折不折得起來 → 改變**片數** | ~~`canRoll` 尺寸要不要拉成弧長~~ |
| `allowance` 折彎補償、`margin` 縫份 | ~~換材料就換尺寸~~ |
| `validate` 檢查（最小折邊等） | ~~誰看得到哪則警告~~ |

**材料決定「這張圖做不做得出來」，不決定「這張圖上的數字是多少」。**

⛔ **不要把任何材料判斷寫回長度計算裡。**
基準一旦有條件，圖上就再也沒有一個數字有單一意義了。

### 補償是疊在真值之上的獨立一層（**目前不存在，刻意不開鉤子**）

kang 的順序：「**必須先把真實的依據先做正確，再去處理材料上的誤差數值**」。

軟性材料真的需要補償時，照 `sheet.js` 的縫份那個現成樣板 ——
「圖上未含 X cm，下料時另加」：**真值歸真值、補償歸補償，兩個數字都看得見。**

⚠ **這條被推翻過兩次，兩次的結論都碰巧是弦長，但理由完全不同。**
不讀 `尺寸依據的推導.md` 第 2 節就動它，很可能把材料邏輯搬回來。

### 去哪裡看細節

| 要查什麼 | 去哪 |
|---|---|
| **推導、三個版本、成熟工具、kang 原話** | **`尺寸依據的推導.md`** |
| 規則的程式版 ＋ rule 只被問哪三件事 | `js/unfold/flatten.js` 檔頭 |
| `canRoll` 為什麼不准再影響尺寸 | `js/unfold/rules.js` 的 ⛔ 標記 |
| 為什麼不要重新想出 `applyStretch()` | `js/unfold/flatten.js` 的墓碑註解 |
| 守門員測試（★★ 六種材料同一個數字） | `tests/run.mjs`「🔴 尺寸的依據是網格」那節 |


## 🔴 三件事（2026-08-23 定案，取代原八期路線圖的推進方式）

> 外部參考調查做完之後，kang 與 AI 一起收斂出來的。
> **照這個做，不要照原八期路線圖的順序推。**
> 完整推導在 `外部參考調查.md` 第 4 節與另外三份外部參考文件。

### 一、~~展開圖：尺寸會錯~~　✅ **已完成，但做的跟原本想的完全不同**（`4361556`）

> ⚠ **這一件原本寫的是「圓弧擬合 → 曲面用真周長 → 圓錐排成正確扇形」。
> 　那整個方向被推翻了**，完整推導見「尺寸的依據」那一節。
> 　一句話：**尺寸從來沒有錯過，156.83 就是那個模型的真值。**

**實際做了什麼**：把材料從尺寸計算裡拔乾淨（`arcCorrection()` 的
`canRoll` 分支、`applyStretch()` 整支刪除），並補掉三個「說謊」的地方 ——
重疊偵測超過 3000 片的沉默上限、已取消的「第 7 期」字樣、
只對捲得起來的材料才發的錐面警告。測試 1027 → 1048 項。

**做完**：同一個模型，六種材料給同一個展開尺寸。DXF 可以直接下料。

### 二、~~拉點線面 ＋ 擠出被鎖死在「只能沿世界 XYZ 平移」~~　✅ **地基已解開**（2026-08-23，`94f6c3f`）

| kang 想做 | 現在 | 靠什麼 |
|---|---|---|
| 拉成梯形、收尖 | ✅ | 放開旋轉／縮放（只剩「點」還鎖著，那是對的） |
| 推一個斜面 | ✅ | **方向切「法向」→ 拉藍色那根（Z）** |
| 擠出斜面後調長度 | ✅ | 同上，擠出的蓋子選中後直接用 |
| 拖到一半反悔 | ✅ | 拖曳中按 `Esc` |
| 直接打數字 | ✅ | 拖過一次之後，「數值」欄打數字按 Enter |
| **一次選好幾條邊** | ❌ | **還沒做** —— 那是 1-3，見第三件下方 |

**做了什麼**：`elementBasis()`（方向基底，面／邊／點三種 ＋ 退化鏈）
＋ `applyElementTransform()`（**記下初始座標，每幀從初始值重算**，
取代原本的增量累加）＋ 拿掉 `_attachEditProxy()` 那行寫死的 translate。
測試 1048 → **1102 項**。

🔴 **那一行寫死的 translate，理由是「把一個頂點旋轉 30 度沒有意義」——
對點成立，對面完全不成立，而它把整個第 6 期擋在門外。**
這是「功能邏輯不從推論來」那條鐵律的頭號證據（`外部參考調查.md` 第 1 節）。

⚠ **記初始值的價值不是手感是正確性**：旋轉根本沒辦法用增量累加做對，
而取消、打數字是它的副產品，不是另外寫的功能。

⚠ **旋轉一個面會把側牆扭成麻花**（拓撲沒變，側牆只好跟著扭；Blender 也一樣）。
所以旋轉面是拿來做**斜面與梯形（小角度）**的，不是拿來「把一片轉個方向」。
〔測試抓到的：原本那條測試斷言「轉 180 度體積不變」，實測是 108000 → 36000〕

### 三、還沒有的四個工具（**現在輪到這裡了**）

✅ **1-3 的多選那一半已完成**（2026-08-24，`42a003f`）：子元素多選 ＋ 順序即 active
＋ 中心（重心／最後選的）。同一次只能同型別、不能跨物件。

⚠ **1-3 的「網格重建時搬移選取」刻意還沒寫。** 實際做完之後發現
**現在沒有任何一條路需要它**：唯一會拆掉重建的是擠出，它一次只擠一個面，
新蓋子由 `capFace` 直接回傳；Undo／讀檔換上來的是另一個模型狀態，
那裡就該老實清掉。⛔ **不要先寫好等著用**，那會變成第二個 `pushFace()`。
配對規則已經想好，寫在 `edit.js` 那段墓碑註解裡當規格 ——
**下面四個工具第一個動工時順手做掉。**

🔴 **2026-08-24：六個工具全部調查完，而且沙箱實測翻掉了兩個原本排好的計畫。**
**完整推導與重排後的順序在 `外部參考-Blender編輯.md` 第 9 節**（動工前必讀）。
三句話摘要：

- **環切的前提原本不成立** —— 16 段圓柱是 64 個三角形、**0 個四邊形**，
  而環切穿的是四邊形。→ 所以先做**面合併**（已完成），它把圓柱變成 32 個四邊形
- **合併頂點不是最便宜的，是最貴的** —— 它會打破「既有頂點保持原索引」的契約
- **導角的阻礙繞過了** —— 兩邊形是 Blender 的實作路徑，不是導角的定義。
  拆掉重建可以直接做，實測體積 **107250 精確吻合**。
  ⚠ 但**角落斜接（相鄰的邊也被導）仍然是未知**

✅ **還原多邊形已完成**（2026-08-24）：`bake()` 時自動把共面的三角形
還原成多邊形面。方塊 12→6、圓柱 128→34、球 960→512，**體積面積精確不變**。
它解掉「`box` 四邊形化」與「環切的前提」。
🔴 **沒有按鈕** —— 它在畫面上看不出作用，是程式內部的整理不是使用者功能。
⚠ 而「**溶解面**」（看選取、不管夾角、併成一個 n 邊形）是**另一個指令**，
還沒做 —— kang 已經伸手找它兩次了。

✅ **環切已完成**（2026-08-24，`531dde6`）—— 而且它逼出了一條新規則 `hard` 邊
（見「先讀這段」）。體積面積精確不變、孤點 0 個、展開完全不受影響。

**剩下的順序**：**內縮** → 刪除面 → 導角 → 合併頂點。
✘ **溶解面在「要切出來」的用途下不做**（⛔ 不是做不到，權威版在
`規格\建模器-點線面編輯.md`）、✘ **細分不做**（四種補法要先問 kang；
擠出＋拉多半可以取代）。

### 順序與相依

> ⚠ 這一段被改過兩次。
> 原本寫「一 → 二 → 三」；第一件完成而且結論翻轉之後改成「二 → 三」；
> **第二件也完成了（2026-08-23），所以現在只剩第三件。**

**三。而第三件自己的順序是：1-3（多選那一組）→ 其餘四個工具。**

### 隨時可插隊的小項（各自半天內，不影響上面三件）

- 接合編號**上下顛倒會誤讀就加句點**（`69NZMWpbqd`／`80oOxXIl`）—— 板子會被翻來翻去
- 接合編號旁邊**加箭頭**指出配對在哪一片
- **`box` 改成手工建的 6 個四邊形**（現在借 `THREE.BoxGeometry` 被迫變 12 個三角形）
- **Undo 一起還原「當時的模式與過濾器」**
- **隱含切割線要在圖上跟「指定分片」區分開**（現在使用者分不出哪幾條是程式加的）

### ✅ 已經證明的事（2026-08-23 沙箱實測，不要重推）

**「圓弧擬合」可行。** 拿一圈邊的頂點做最小平方擬合圓，實測結果：

| 測試 | 判定 | 半徑 | 最大偏差 | 網格真值 ／ 視為理想圓 |
|---|---|---|---|---|
| 圓柱 r=25 的兩個蓋子 | ✅ 是圓 | 25.000 | 0.00000 cm | 156.827 ／ 157.080 |
| 圓錐 r=30 的底圓 | ✅ 是圓 | 30.000 | 0.00000 cm | 188.193 ／ 188.496 |
| **半圓體（球 r=30 切一半）的赤道** | ✅ **是圓** | **30.000** | **0.000001 cm** | 188.193 ／ 188.496 |
| ~~方塊 60×45×40~~ | 🔴 **這一格是錯的，2026-08-27 重測翻掉** —— **方塊每一個面都被判成圓**（4 點共圓，偏差 3.55e-15）。詳見下方那則 | 30.1040／36.0555 | 3.55e-15 | — |
| 圓柱，故意把一個點拉歪 1.5cm | ❌ 不是圓（**那一圈**） | 25.001 | 0.04074 cm | 拒絕當成圓來報告 |
| ↑ 同一個圓柱，沒被動到的另一圈 | ✅ 還是圓 | 25.000 | 0.00000 cm | 156.827 ／ 157.080 |

🔴 **這解掉了 kang 問的那個問題**：「轉成網格編輯後，能不能再轉換成可以被計算的屬性？」
**可以。** 半圓體已經不是參數物件了，程式**還是**認出赤道是半徑 30 的圓。
因為它問的不是「你當初按了什麼」，而是「**這一圈現在還是不是圓**」。

**而且是逐圈判斷** —— 被拉歪的那一圈拒絕給公式尺寸，沒動到的那一圈照樣精確。

⚠ 也因此，前面討論過的 **smooth 旗標、bake 時帶資訊過去、逐條邊記錄、什麼時候失效
—— 全部不需要了。** 擬合是每次展開時現算的，沒有東西要存、也沒有東西會過期。

> 🔴 **這一項的「用途」在 2026-08-23 當天就被改掉了，改動比數字重要。**
>
> 原本寫的是「⚠ 還缺的一步：要讓展開圖真的用上這個數字，
> 圓柱身體的寬要改用 2πR」。**那一步不做了，而且不准做。**
> 尺寸一律是網格真值（見「尺寸的依據」）。
>
> **擬合現在只有一個用途：報告。** 例如在面板上多一行
> 「這一圈是圓（R25、32 段）　網格 156.83／視為理想圓 157.08／差 0.25」——
> **那是資訊，不是修正。** 差多少由使用者自己判斷要不要回頭調 seg。
> ⛔ 看到差額不要以為那是誤差要補回去。
>
> ⚠ 這一項**尚未接上介面**，目前只是「已證明可行」。要做的話它是
> 一個獨立的小項，不影響任何既有尺寸。

### 🔴 2026-08-27 重測：「方塊沒有被誤判成圓」那一格是**錯的**

> ⚠ **舊版寫的是**：「方塊 60×45×40 —— 沒有任何一圈被誤判成圓」，
> 　並猜「實測腳本裡一定還有一個沒被寫進來的門檻（大概是最少段數）」。
>
> 🔴 **重測結果：方塊每一個面都被判成圓。那個猜測擋不住這件事。**

**實測**（照舊表的案例重寫一份最小平方擬合，Kasa 法）：

| 樣本 | 點數 | 半徑 | 最大偏差 |
|---|---|---|---|
| **方塊的面** | 4 | 30.1040／36.0555 | **3.55e-15** ＝ 0 |
| **六角柱的蓋子** | 6 | 30.0000 | 2.13e-7 |
| 圓柱 r25 seg32 的蓋子 | 32 | 25.0000 | 3.54e-7 |
| 圓錐 r30 的底圓 | 32 | 30.0000 | 7.87e-7 |

🔴 **病因不是門檻沒寫進去，是問題問錯了**（鐵律二：
「連續幾種聽起來很對的規則都失敗時，多半是問錯問題了」）：

> **正多邊形的頂點永遠共圓，矩形的四個角也永遠共圓。**

**六角柱的蓋子在數學上真的是一個圓**（外接圓半徑 30），方塊的面也是。
所以「**這一圈共不共圓**」這個問法**選不出使用者心裡想的那種圓** ——
而「最少段數」定在哪也沒有答案（定 8 就排除掉 8 段圓柱）。

⭐ **⛔ 但這不擋「變成正圓」，而且它讓順序反過來**：

| | 需不需要「這一圈是不是圓」的判斷 |
|---|---|
| **變成正圓**（把歪掉的點推回圓） | ❌ **不需要** —— **使用者選了那一圈，就表示他要它變圓** |
| **自動報告「這一圈是圓、半徑多少」** | ✅ 需要 —— 而那個判斷**目前沒有答案** ❓ |

→ **`變成正圓` 可以直接做；⏳ 卡住的是「圓弧擬合接上介面」那一項。**
⚠ 日誌原本寫「動工前先重測」，**重測完的結論是：報告那一項要先解掉判準，
⛔ 不要因為擬合數學可行就以為它可以動工。**

⚠ **圓錐的頂點是一個退化案例**（rTop=0 時那 8 個點擠在同一處，
擬合出半徑 0.0001）—— 真的要做報告時記得擋掉。

〔實測腳本仍然是沙箱暫存檔，沒有進版控。要重跑就照上表的案例重寫一份。〕

### ✘ 明確不做（寫下來，免得重提）

**自動分刀**（2026-08-23 定案）。原本排在「要問 kang」，問完之後改成不做 ——
兩個開源展開器的自動分刀，判準全部是幾何（夾角、邊長、繞哪個軸、每片幾個面），
而 **kang 的判準一個都不在幾何裡**：材料要能分開選、省料、組裝好不好處理。
**分片是製造決定，手動分切是正確答案，不是暫時的替代方案。**

其餘不做的項目（黏合片、排料 nesting、匯入 STL／OBJ 當主力入口、增量式 Undo…）
見 `外部參考調查.md` 第 4 組。

---

## 🌐 線上網址

```
https://kangman1977-cloud.github.io/space-design/
```

任何裝置的瀏覽器都能開。平板建議按分享 →「加入主畫面」，會變全螢幕。

---

## 目前有三個工具

| # | 工具 | 檔案 | 單位 | 開啟方式 | 狀態 |
|---|---|---|---|---|---|
| 1 | 空間平面規劃器 | `空間平面規劃器.html` | cm | 雙擊可開 | 完成，上線中 |
| 2 | 組裝系統結構說明表 | `組裝系統結構說明表.html` | cm | 雙擊可開 | 完成，上線中 |
| 3 | 建模器 | `建模器/index.html` | cm | **必須走伺服器** | 第 3.5 期完成，STL 與 DXF 兩條路都通 |

**互通關係**

- 說明表 →（`assembly-system` JSON）→ 平面規劃器　**已完成**
- 建模器 →（`model-mesh` JSON）→ 平面規劃器　**規劃中，排在建模器第 8 期**

> **已移出的子專案**：`鋁擠型框架設計器.html`（40 系列型材框架，單位 mm）
> 已於 2026-08-20 移出資料夾，**不在版控內、資料夾裡也沒有這個檔案**。
> `規格\已移出-鋁擠型框架設計器.md` 僅供日後參考，不要當成現存的工具。

---

## 日常工作流程

| 步驟 | 誰做 |
|---|---|
| 1. 改檔案 | Claude |
| 2. 雙擊 `版控工具\1-看差異.bat` | kang |
| 3. 讀結果檔確認沒動到不該動的地方，並寫 **`版控工具\commit-msg.txt`** | Claude |
| 4. 雙擊 `版控工具\2-存檔.bat` | kang |
| 5. 雙擊 `版控工具\3-上傳網路.bat` | kang |
| **6. 🔴 實測 → 回報結果 → Claude 當場回寫日誌並結案** | **kang → Claude** |

推送後 GitHub Pages 約 1～2 分鐘重建，之後所有裝置重新整理即為最新版。

**先看再存。** 步驟 2 是唯讀的，誤按最多覆蓋一個文字檔。

🔴 **第 6 步是 2026-08-26 補的，而它缺席的代價比看起來大。**

**實測發生在上傳之後**（要有網址 kang 才測得到），而原本的流程**結束在第 5 步** ——
於是「測完了」這件事**沒有任何一格可以落腳**。
規則與完整病因見「協作規則 → 🔴 實測結果的回寫」。

---

## 部署設定（一次性，已完成）

- GitHub 帳號：`kangman1977-cloud`
- Repository：`space-design`（**Public**，免費帳號的 Pages 需要公開）
- Pages 設定：Settings → Pages → Deploy from a branch → `main` / `/(root)`
- 遠端：`https://github.com/kangman1977-cloud/space-design.git`
- `.nojekyll`：停用 Jekyll 處理，避免中文檔名被吃掉
- `index.html`：入口頁，**三個工具都在上面**（建模器於 `0428b14` 補上）。
  因為檔名是中文，網址會被編碼成一長串百分號，平板上打不出來，
  所以用入口頁讓使用者用點的。建模器更是只能從這裡點進去 ——
  它由多個 ES 模組組成，雙擊檔案開不起來
- 三個工具各有一份 `manifest-*.webmanifest` 與 `icons/*`，都能「加到主畫面」

**所有路徑都是相對路徑**，沒有任何以 `/` 開頭的絕對路徑，
因此放在 `/space-design/` 子目錄底下也能正常運作。改動時務必維持這一點。

**建模器不能雙擊開啟（重要）**
建模器拆成多個 ES 模組，瀏覽器的 CORS 政策會擋掉 `file://` 底下的模組載入。
因此只有兩種開法：

| 場合 | 網址 |
|---|---|
| 本機／平板測試（先跑 `版控工具\平板測試-啟動伺服器.bat`） | `http://<內網IP>:8080/建模器/` |
| 線上 | `https://kangman1977-cloud.github.io/space-design/建模器/` |

前兩個工具是單一 HTML 檔，不受影響，照樣可以雙擊開。

**離線與 PWA**：只做了 manifest（加到主畫面、全螢幕），**沒有做 Service Worker**。
原因是實際情境「平板就算沒 SIM 也會接熱點」，離線快取價值不高，
卻會帶來版本陳舊的風險。日後若需要離線，採「網路優先、失敗才用快取」策略。

---

# 三個工具的個別規格 → 已搬到 `規格\`

| 工具 | 規格檔 |
|---|---|
| 空間平面規劃器 | `規格\空間平面規劃器.md` |
| 組裝系統結構說明表 | `規格\組裝系統結構說明表.md` |
| 鋁擠型框架設計器（⚠ 已移出，檔案不存在） | `規格\已移出-鋁擠型框架設計器.md` |

> 前兩個已完成上線、規格穩定，**平常不必讀**，要動它們的時候才取一份。
> 建模器是目前的主力，它的**目的與路線圖留在本檔下方**，各功能的規格見再下一節。

---

# 子專案 4：建模器

**主檔案**：`建模器/index.html`（多檔案 ES 模組 ＋ WebAssembly，單位 **cm**）
**開啟方式**：**不能雙擊**，必須走伺服器
（本機 `http://localhost:8080/建模器/`、平板 `http://<內網IP>:8080/建模器/`）
**目前進度**：原訂八期已完成六期半，另外補了路線圖上沒有的四項
（指定分片、接合編號、對齊與貼合、剖面分切）。見「路線圖現況」。
**已經可以投入生產**：治具樣品印 3D、箱體護罩料槽出圖送雷切、
造型件剖面分切疊層、Illustrator 線稿直接匯入擠出。
**下一步是「編輯」那一條** —— 點線面重塑、內建貝茲、匯入圖片描圖。

> **本機請用 `localhost` 開，不要用內網 IP。**
> 「指定存放位置」用的 File System Access API 只在**安全環境**可用，
> https 與 localhost 算，`http://192.168.x.x` 不算。
> 用 IP 開會自動退回一般下載（勾選框會淡化並說明原因）。
> 內網 IP 那條路是給 iPad 用的。

## 目標與範圍（重要，避免走偏）

### 做這套的用意（kang 2026-08-21 說明，優先於底下的一切規格）

**建好的模型要能實際做出東西，兩條路並列，沒有主次：**

| 路徑 | 用途 | 需要的出口 |
|---|---|---|
| **建模 → 3D 列印** | 治具、樣品、小零件 | **STL**（切片軟體吃這個） |
| **建模 → 展開 → 2D 切割** | 紙材、發泡板、珍珠板、木板、壓克力 | **DXF**（雷切／水刀／2D CNC） |
| **建模 → 剖面分切 → 疊層黏合** | **任何形狀**，尤其是曲面造型 | **DXF**（每片一個輪廓）|

第三條是 2026-08-22 kang 提出、當天完成的（`0507c51`），見 `規格\建模器-剖面分切.md`。
它的重要性在於**不受可展性限制** —— 球面、自由曲面數學上無法無失真展開，
但它們全都切得開，而且是精確的。

**疊合之後還會經過人工補土打磨**（kang 2026-08-22 說明）。
這件事解釋了兩件事：為什麼「取每片中央的截面」是對的選擇
（誤差對稱，凸的地方少一點、凹的地方多一點，打磨與補土各吃一半），
以及為什麼「成品會留孔」的代價比原本估的低 —— 孔一起補掉。

「CNC」在這個專案裡指的是**雷切／水刀／2D 切割**，不是 3 軸銑削。
所以**不需要 STEP** —— 那是 B-rep 曲面格式，不是網格，
自己寫等於做半個 CAD 核心，不在範圍內。

### 形狀從哪來：兩個入口（2026-08-22 起）

出口有三條（上表），**入口現在有兩個**：

| 入口 | 適合什麼 |
|---|---|
| 九種參數基本體 ＋ 布林 ＋ 陣列 | 方正的、旋轉對稱的、可以用加減湊出來的 |
| **匯入 Illustrator 線稿（SVG）→ 擠出** | 任意斷面：L 型料、招牌造型、logo、字 |

剖面分切做完之後瓶頸換邊了：以前是「這個形狀做不做得出來」，
現在任何封閉形狀都切得開 —— **限制變成「建不建得出來」**。
所以下一步該由「你想做什麼東西」決定，不是由路線圖決定。

kang 已經講清楚方向了：**這套工具的核心是「編輯」**。見「編輯這條路」。

### 範圍

專攻**建模**與**展開**，**不做 Blender 那種全能軟體**。
判準：Blender 做不好、但公司天天要用的事才做；純多邊形雕塑不是重點。

要能涵蓋三類對象：機構件／治具（精確件）、設備量體、自由造型。
輸出要有：**STL（3D 列印／CAM）**、可列印展開圖（含尺寸標註）、DXF（2D 切割）。

**展開要對應的材料**（2026-08-22 由 kang 說明後改寫）：

| 材料 | 折得起來？ | 捲得起來？ |
|---|---|---|
| 紙材 | 壓線／壓痕 | **可以** |
| 發泡板、珍珠板、薄木板 | 銑 45 度 V 溝（留薄皮） | 不行 |
| 木板、壓克力 | 不行，切開再接 | 不行 |
| （帆布軟膜） | 可以 | 可以 |
| （鐵板、不鏽鋼、鋁板） | 可以 | 可以 |

括號裡的是**目前用不到的**，規則保留著以防日後外發或混用。

> **這一段被改寫過兩次，兩次都是同一個教訓。**
>
> 2026-08-21：原本只寫了展開那一條路，八期路線圖裡完全沒有 3D 列印的
> 出口 —— 照原規劃走完八期，東西還是印不出來。（坑第 13 條）
>
> 2026-08-22：原本寫「四種材料：**鈑金**、帆布軟膜、壓克力木板、紙模打樣」，
> 把鈑金排第一。實際上一種金屬都不用。**AI 從第一句就假設這是鈑金工具，
> 照著文件推了整整一場，直到 kang 主動說明材料才發現。**（坑第 14 條）
>
> 兩次的共同點：**規格寫得再詳細，也不等於目的。**
> 而「目的」包含用什麼材料、給誰做、做出來怎麼組。

## 八期路線圖

| 期 | 內容 | 狀態 |
|---|---|---|
| 0 | 技術驗證：3D 點選、三軸 gizmo、布林、觸控手感 | **完成**（放 `XXX/`，不進版控） |
| 1 | 核心：半邊網格、邊角色、平面區域合併、Undo、存讀檔、基本體 | **完成** `2b030f0` |
| 2 | 參數體、布林運算、陣列鏡射 | **完成** `8d2de61` |
| 3 | 展開 v1：平面與折彎件 → 列印圖 ＋ DXF | **完成** `77f386b` |
| 3.5 | **STL 匯出 ＋ 列印前檢查**（3D 列印那條路的出口） | **完成** `4530697` |
| 5(A) | 算不準的展開要講出來（止血） | **完成** `766a3fd` |
| 5(B) | 展開改走外皮弦長 ＋ 生成樹先走最平的邊 | **完成** `7c4fa25` |
| 5(C) | 實體可直接展開 ＋ STL 頂點共用修正 | **完成** `91a7f03` |
| ★ | **指定分片**（原路線圖裡沒有，kang 實際最需要的） | **完成** `08f5b90`＋`efa0c3b` |
| 4 | **草圖擠出** | **一半完成** `0b86cb8` —— 擠出做好了（輪廓從 SVG 匯入）；還缺「在建模器裡自己畫草圖」 |
| 5 | 展開 v2：圓錐的極座標展開 | **取消**（2026-08-23）—— 錐面攤平本來就精確，扇形半徑 ＝ 母線。見「路線圖現況」一 |
| ★ | **對齊／均分、正交三視圖、框選、貼合** | **完成** `71f2d21`＋`43b0c03` |
| ★ | **剖面分切**（第三條生產路徑） | **完成** `0507c51` |
| 6 | 自由造型：點／邊／面編輯 | 未開始（kang 確認用得到） |
| 7 | 展開 v3：自由曲面切分近似 ＋ 失真標示 | **取消** —— 剖面分切把同一個需求解得更好，而且沒有近似 |
| 8 | 互通：匯出 `model-mesh`，平面規劃器加 `kind:'mesh'` | 未開始 |

第 3.5 期是對焦之後插進來的；5(A)(B)(C) 三輪則是**原路線圖上完全沒有的**，
起因是實測與 kang 說明材料之後才發現的問題。

**第 8 期是唯一會動到 `空間平面規劃器.html` 的一期**，其餘都不碰現有檔案。

## 路線圖現況（2026-08-22）—— ★ 那項已補做，其餘仍需重排

八期路線圖是在「這是鈑金工具」的假設下訂的。材料一釐清，有兩項變了：

**一、原訂第 5 期（圓錐的極座標展開）—— 🔴 2026-08-23 定案：取消。**

> ⚠ **這一段被推翻過兩次。兩個舊說法都作廢，都不要照著推：**
>
> **舊說法甲**（2026-08-22）：「板材捲不起來，圓錐一定切成平板再接，
> **弦長就是正確答案**，只有紙材才需要真弧長。」
> → 結論碰巧對，但**理由錯**：它拿材料當判準。
>
> **舊說法乙**（2026-08-23 上午）：「kang 要的是**扇形弧長 ＝ 底圓周長**，
> 跟材料無關，解法是圓弧擬合 ＋ 用 2πR。」
> → **方向整個錯了**：它預設「網格量出來的是誤差、公式才是真值」。

**現在的定案**（見「尺寸的依據」那一節）：

**展開尺寸 ＝ 網格攤平後的總和。圓錐的攤平本來就精確，沒有東西要修。**

實測（r=30、h=70，seg 6／8／16／32／64）：展開圖上離頂點最遠的距離
一律 **76.1579**，母線 76.1577 —— **扇形半徑精確等於母線，一格誤差都沒有。**
底緣是 seg 段弦長，那就是那根 seg 邊錐的真實底緣。

→ **原第 5 期取消。** 那則「請勿據以下料」的錐面警告也整則刪了 ——
它叫人不要用一個正確的數字（坑第 18 條：誤報比漏報更糟）。
→ 實作見 `4361556`，測試改成驗「扇形半徑 ＝ 母線」。

**二、路線圖裡沒有 kang 真正最需要的那件事：指定分片。已經補做了。**

kang 的原話：「立體造型我們必須要做規劃，**如何分配展開的區域**，
能夠依序讓 CNC 切割後再組裝結合。」

這一項**已於 2026-08-22 完成**（`08f5b90` 資料層 ＋ `efa0c3b` 介面），
用法見 `規格\建模器-展開與分片.md`。它原本不在八期裡，是問過 kang「下一步做什麼」
之後才浮出來的 —— 又一次印證坑第 13 條：**先確認目的，不是先確認規格。**

補做得很便宜（一天之內），因為底層第 1 期就打好了：`EDGE_ROLE.CUT` 早就存在，
`edgeIsCut()` 第二件事就是尊重它，`mesh.toJSON()` 也早就在存 roles。
**缺的自始至終只有介面。**

**第 6 期（點／邊／面編輯）的地基因此也蓋掉一半了** ——
「點選邊」那套已經做出來而且測得到（`seam.js` 的 `nearestMarkableEdge`
與 `nearestFace`）。第 6 期真正還缺的是「改變幾何」那一半。

**三、2026-08-22 又多了兩項，而且第 7 期大概要取消。**

kang 提出「編輯方式」與「另一種轉出模式」之後，路線圖實際上變成：

| 順序 | 內容 | 狀態 |
|---|---|---|
| 1 | 對齊／均分、正交三視圖、框選、貼合 | **完成** `71f2d21`＋`43b0c03` |
| 2 | 剖面分切 | **完成** `0507c51`，見 `規格\建模器-剖面分切.md` |
| 3 | 匯入 Illustrator 線稿 → 擠出 | **完成** `0b86cb8`（＝第 4 期的一半），見 `規格\建模器-匯入線稿.md` |
| ? | 第 6 期點／邊／面編輯、內建貝茲、匯入圖片描圖 | 見「編輯這條路」 |
| ✗ | 第 7 期自由曲面切分近似 | **取消** —— 剖面分切解同一個需求而且沒有近似 |

🔴 原第 5 期（極座標展開）**已取消**（2026-08-23）—— 見上方「路線圖現況」一。

**第 4 期與第 6 期哪個先做仍然沒有定論。** 輪到它們之前先問 kang
「下游拿去做什麼」，不要照舊順序推（坑第 13、23 條）。

## 建模器各功能的規格 → 已搬到 `規格\`

| 要查什麼 | 去哪 |
|---|---|
| 半邊網格、**六個關鍵決定**、九種參數體、檔案結構、第 8 期交換格式 | `規格\建模器-核心架構.md` |
| 展開、七種材料規則、**板材與金屬的差別**、曲線帶、**指定分片**、接合編號 | `規格\建模器-展開與分片.md` |
| **剖面分切**、定位孔與串桿、疊層 DXF | `規格\建模器-剖面分切.md` |
| **匯入 Illustrator 線稿**（SVG）擠出、真轉角 | `規格\建模器-匯入線稿.md` |
| **STL 匯出**與列印前檢查 | `規格\建模器-STL匯出.md` |
| 對齊、均分、框選、**貼合**、正交三視圖（把**多個物件**擺到位置） | `規格\建模器-編輯與組裝.md` |
| **拉點／拉邊／拉面**、改完之後的連帶重算（改**一個物件的形狀**） | `規格\建模器-點線面編輯.md` |
| **量測**：選到什麼就顯示尺寸、為什麼一律世界座標、拖曳讀數、四個步驟 | `規格\建模器-量測.md` |
| 回歸測試怎麼跑、驗過哪些基準數字 | `規格\建模器-測試與驗證.md` |
| 第 6 期「編輯」的定位、範圍、**被否決的方向** | `編輯功能設計方向.md` |

> 每個規格檔開頭有「**這份回答什麼／這份不回答什麼**」與「**識別碼**」（給 Grep 用），
> 結尾有「**接下來去讀程式的哪一支**」。**文件負責帶到門口，門後面一律以程式為準。**

---

## 編輯這條路（2026-08-23 釐清，尚未動工）

**定位**：Pepakura 只做展開，它處理的那些模型（低多邊形鹿頭、盔甲）
全部是在 Blender 建好才丟進去的 —— **所以真正的門檻是得先去學 Blender**。
這個程式要補的就是那一半：建模到出圖一條龍。

**範圍**：box modeling —— 擠出面、拉點／邊／面、**導任意一條邊的圓角**、
細分、合併頂點、刪除面。造型一律是幾何化多面體。
**不做雕塑類**（影響範圍拉點、對稱雕塑、平滑細分）。

**共同障礙**：這些動作卡住的是同一點 ——「選到一個元素之後能不能改變它」。
選那一半**已經做好而且測得到**（`seam.js` 的 `nearestVertex` /
`nearestMarkableEdge` / `nearestFace`，被分片與貼合共用），缺的是「改變」那一半。

→ **完整內容見 `編輯功能設計方向.md`**：定位的推導、外部軟體調查採集、
  **被否決的方向與理由**、板件退場、編輯與既有標記的關係。
  動編輯之前一定要讀，否則會重提已經否決過的東西。

> 原本這一節（2026-08-22）把編輯分成四項，其中「內建貝茲」與
> 「匯入 PNG／JPEG 描圖」**沒有被否決，只是降級**，移到新文件的「尚未定案」。
> 降級的理由不變：它們補的是「懶得開 Illustrator 的那些小東西」，不是主力。

## 鐵律（從 34 條坑萃取，每次動手都適用）

> **這一節是規則的家。** 34 條坑的完整經過（現象、數字、怎麼查出來的）
> 在 `HISTORY.md`「這個子專案踩過的坑」一節，一字未改 ——
> 但**規則以這裡為準**，要改就改這裡。
>
> **為什麼要拆開**：34 條的「現象」約 520 行，「規則」只有底下這些。
> 規則埋在 520 行敘述裡，就是**坑第 33 條**講的那件事 ——
> 「教訓寫在別的功能底下，就等於沒寫」，只是這次埋它的是時間順序。
>
> 〔坑 N〕＝ 去 `HISTORY.md` Grep `N. **` 看完整經過。
> 本檔其他地方寫「坑第 N 條」時，指的也是同一個地方。

### 一、動手之前（這一組最貴，同一個病犯了三次）

- 🔴 **先確認「目的」，不是先確認「規格」。** 開新階段、或接手一個專案之前，
  先問一句「**這個做出來要拿來做什麼、給誰用、下一步會接到哪裡**」。
  規格是從目的推出來的，順序不能反。〔坑 13〕
- 🔴 **「目的」包含用什麼材料、給誰做、做出來怎麼組。**
  這三件事沒問清楚，規格一定是猜的。〔坑 14〕
- 🔴 **目的藏在下游。** 要問的不是「你要不要這個功能」（對方多半會說好），
  而是「**這個東西產出之後，你下一步拿它做什麼、交給誰、用什麼軟體開**」。〔坑 23〕

  ⚠ **但這一條有一個前提：下游要已經存在。**〔kang 2026-08-25 澄清〕
  > 「**現在建模器都還在建置開發中**...並沒有進入實際工作流程..
  > 　我們要**整個建模器都完工後**...我才會真實的導入實際工作」

  剖面分切、指定分片、匯入線稿那幾輪問得出東西，是因為下游**當時就存在**
  （Illustrator、CNC、疊層黏合）。而**編輯功能的下游是「用這個建模器建模」，
  那件事還沒開始發生** —— 不是 kang 不肯講，是還不存在。

  ⛔ **所以不要問 kang「這個功能你實際上多常用」** —— 他答不出來，
  而**逼他猜會得到一個推論冒充的現實**，然後那個答案會被拿去排順序。
  〔2026-08-25 實際犯過：問「你實際建模時多常需要量兩點距離」〕

  🔴 **現階段排序的唯一判準，是 kang 8/24 定的那條：對照表上標 ⭐ 的做齊。**
  ⛔ 不可以拿「這個實際上大概不常用」去砍範圍或改順序 ——
  沒有使用資料的時候，那種話就是「功能邏輯從推論來」的另一張臉。

  ⭐ **連帶：kang 的實測是現階段唯一的現實檢查，份量比看起來重。**
  他抓到的東西（12 條邊選不到／預設值寫死 0／選取有兩份資料／
  訊息叫人走死路）**全部是介面類的，沙箱一個都驗不了**。
  那不是巧合 —— **資料層有一整套數學守著，介面沒有。**

  ⭐ **問對了會省掉一整輪工，這裡有兩個實例，而且指向同一件事**：

  | 拿到的下游事實 | 省掉了什麼 |
  |---|---|
  | 「**會用 AI 繪圖開 DXF 或是 SVG**」〔2026-08-26〕 | 展開圖的字**不必在紙上讀得出來** → ⛔ 沒問到這條，我會去做「**引線拉出去**」那種大工程，只為了解缺口那剩下的 1 對 |
  | 「**DXF 會先進 Illustrator 做整體處理才給 CNC**」〔2026-08-22〕 | 「板材尺寸上限檢查」**整項取消** —— 排版時就看得到，而且 Illustrator 判斷得比我們好 |

  🔴 **兩條都是問出來的，不是推出來的**，而且**兩條指向同一件事**：
  他的下游有 Illustrator。⭐ **這種事沒有任何規格文件會寫。**
  ⚠ 但 ⛔ **不要把它推得太遠**：「列印」那顆按鈕還在，真的直接印的時候
  字太小還是讀不出來 —— **⛔ 不可以據此把字再往下調。**
  〔展開圖那一側的完整規則在 `規格\建模器-展開與分片.md`
  　「⭐ 下游放寬了判準：kang 用 AI 開 DXF／SVG」，⛔ 不在這裡抄第二份〕

- 🔴 **先調查清楚再動手，不要只依據推理。**〔kang 2026-08-23 明確指示〕
  > 「當你要進行任何編寫時..保持習慣..**先調查清楚再動手**...
  > 　才不會只依據推理..造成**反覆判斷失誤**」

  調查的順序，由近而遠：

  | 順序 | 查什麼 | 為什麼排這個位置 |
  |---|---|---|
  | 1 | **本專案有沒有解過同一個問題的程式** | 坑已經踩過、測試已經守著。鐵律五「往回看自己的專案」 |
  | 2 | **要動的那幾支程式，實際讀過** | 不是讀日誌對它的描述 —— 日誌會過期，程式不會 |
  | 3 | **kang 手上有沒有現成的東西** | `SideUnfold.jsx` 那十分鐘換掉一個註定失敗的設計 |
  | 4 | 🔴 **成熟工具怎麼解同一個問題** | 見下方那條，這已經升格成常設規則 |

- 🔴 **這個專案的功能邏輯不從推論來。**〔kang 2026-08-23 定調〕
  > 「我們的建模器..在很多功能的邏輯運用上..有很多缺失...
  > 　**因為我們很多都是推論出來的結果**...雖然使用的程式語言不同...
  > 　**但是功能的邏輯應該都是相同的**...我們應該吸取優點來優化我們的建模器」

  有成熟工具解過同一個問題時，**先去理解它的邏輯與結構**，再決定要不要用。
  **推論只用在「沒有人解過」的地方。**

  → **完整內容見 `外部參考調查.md`**：兩條界線（GPL 讀邏輯不抄程式碼／
    Pepakura 是商業軟體只讀公開文件）、過篩子的判準、兩條研究線與清單、
    以及**已經查到的部分**。**動編輯或展開之前一定要讀。**

  ⚠ **實測紀錄**：2026-08-23 一天之內光靠推理就錯了五次 ——
  猜錯函式簽名、拿壓克力的基準數字推珍珠板、以為圓柱側面有四邊形面、
  **寫了一個自己沒接上的功能**、兩次拿圓柱驗 smooth 都顯示「沒事」。
  同一天讀程式的四次（`mesh.js` 沒有拓撲 API、`geomKey !== mesh`、
  `_copyRolesTo` 不搬 smooth、`fromFaceList` 的索引順序）**全部是決定性的，
  而且沒有一件推得出來**。
- 🔴🔴 **推論不是權威事實。文件是前端調查，程式才是唯一權威。**
  〔kang 2026-08-26 明確要求。**ERP 與系統整合兩個專案都把這條列為最高紀律**，
  　措辭沿用他們的，⛔ 不自己改寫〕

  > kang 的原話：「**我尊重你的推論，但是推論不是權威事實，不能拿推論來做答案。
  > 　文件資料很重要，但是程式才是唯一權威。文件資料是幫助我們先做前端調查推論，
  > 　但是最終都要去驗證權威。**」
  >
  > 🔴 **理由（他自己講的）：「才不會做白工。」**
  > 推論錯了，照著它做的東西整批要重來 —— **成本不是「講錯一句話」，是整輪工。**

  **一、權威層級**（`D:\彩藝通系統整合\交接稿\給ERP對話.md`）：

  > **官方文件／一手實機實查／讀原始碼／實測 POC ＞ 訓練知識與常識。**

  ⚠ **文件是快照，只能當線索** —— ERP 那邊寫得更狠：「**權威來源 ＝ 活庫**」。
  本專案的對應版本是：**程式是活庫，`規格\` 與本檔都是快照。**

  **二、講具體事實前先自問一句**（`D:\彩藝通系統整合\PROJECT_LOG.md`）：

  > 🔴 **「這是我自己開檔看到的，還是讀來的？」**
  > **讀來的就標【推論】，或當場開檔驗。**

  **三、事實陳述要標【實證·出處】或【推論】** —— ⛔ 不准把推論當答案交出去。

  **四、🔴 kang 的權利（這一條要主動告訴他，不是等他發現）**：

  > 只要 AI 講到**行號、函式名、或「它現在就是這樣」**，
  > **kang 可以直接反問「這幾行你打開過嗎？」**
  > AI 必須**給出處，或當場承認是推論**。
  > **這是他的權利，不是冒犯。**

  **五、兩檔位**：討論／推論階段可以快，**但動工前要把「方案依賴的關鍵事實假設」
  逐項標【已權威驗證／僅推論待驗】，待驗的先驗過再動**。
  （純介面文案那種安全區照舊直接做，不必開清單。）

  ⚠ **立條當天就有兩個實例，而且都是 kang 連問三次才逼出來的**（2026-08-26）：

  | 我說的 | 我的依據 | 權威在哪 |
  |---|---|---|
  | 「第二版刀具就是貼著表面走」 | **規格檔的機制敘述** | `edit.js`／`select.js` —— **沒讀就下結論** |
  | 「Blender 可能允許在面中間結束，**我驗不了**」 | 手上的調查筆記 | **官方手冊白紙黑字寫著**（Known Limitations · Unconnected Cuts） |

  🔴 **第二個更難看：不是驗不了，是沒去驗就宣告驗不了。**
  ⭐ 而讀完程式與手冊之後，答案跟推論**方向相反也相同過** ——
  重點不是猜得準不準，是**猜出來的東西沒有資格當答案**。

  ⚠ **本專案原本就有這條，但它埋在「規格檔那張表」底下的一行小字**
  （「文件負責帶到門口，門後面一律以程式為準」）——
  **正是坑第 33 條：教訓寫在別的地方底下，就等於沒寫。** 所以搬到這裡。

- 🔴🔴 **⛔ 不要給止血方案。推論的方向要朝向「找出權威原因」。**
  〔kang 2026-08-28 明確要求。**措辭沿用他的，⛔ 不自己改寫**〕

  > kang 的原話：「**你的推論..我不反對..但是重點是不要給出止血方案..
  > 　因為止血方案不是治本..不知道原因..未來任何狀況都會無法清楚知道『為什麼』..
  > 　而且止血只會讓除錯時更為混淆真實原因...這非常重要..我不反對推論...
  > 　但推論的方向都是要找出權威原因治本..我們才能討論制定適合的解決方案**」

  **三層，第三層最狠而且最容易漏掉：**

  | | |
  |---|---|
  | 一 | **推論本身沒問題** —— 它是找答案的工具，⛔ 不是禁區 |
  | 二 | **推論的方向要朝向「找出權威原因」**，⛔ 不是朝向「讓症狀消失」|
  | 三 | 🔴 **止血會污染現場，讓下一次更難查** |

  ⚠ **第三層具體會怎麼發生**（2026-08-28 差一步就發生了）：
  網頁掛掉，我推論「應該是快取」並要 kang 按 `Ctrl+Shift+R`。
  **如果他按了、而畫面碰巧好了**（重整時順便抓到新檔），
  我會當場認定「果然是快取」—— 而真正的病（`edgeAny` 重複宣告）
  **還留在程式裡**，下次再爆，**而且下次我會更相信是快取**。
  🔴 **止血不只是沒治本，它會製造一個假的因果證據。**

  🔴 **可操作的判準（⛔ 不是「不准推論」）**：

  > **查證手段用盡之前，⛔ 不准提出任何「讓症狀消失」的動作。**
  > 真的用盡了，就老實說「**我查不出來**」——
  > ⛔ 而不是拿一個止血動作填補空白。

  ⚠ **那一次我具體錯在哪**：⛔ 不是「我推論了」，是
  **我手上還有沒用完的查證手段就轉向止血** ——
  那個手段（**用 node 實際把模組圖載入一次**）後來**一分鐘內就找到答案**，
  它一直都在，我只是先跳去給方案了。
  ⭐ 而且我的措辭把猜測講得像唯一解：我寫的是「那症狀就指向**一件事**」。

  〔這條跟上面「推論不是權威事實」是一對：那一條管**講出來的答案**，
  　這一條管**接下來要做什麼**。兩條都是 kang 當場擋下來的〕

- 🔴🔴 **「權威不是任何一方的說法」——這條對 kang 的話也成立。**
  〔2026-08-26 加。**它是上一條的另一半，而我只守住了對自己那一半。**〕

  > ⛔ **kang 說「我沒看到那個」時，不要立刻撤回一個自己看到的東西。**
  > **回去再看一次。**

  ⚠ **實例，而且代價差一點就付出去了**：我在他的截圖上讀到「三角形 232」
  與「2,176」，他回「兩張都是 232」，**我當場就說「是我讀錯了截圖」——
  而我根本沒回去重看**。後來確認：**截圖真的是 232 與 2176，我讀對了。**

  🔴 **撤回一個正確的觀察，成本是那個病差點沒被查下去**
  （幸好他接著說「數字亂跳」，才追出狀態列那個數字根本在講另一件事）。

  ⭐ **判準很簡單：那個東西還在不在？**
  截圖、程式、輸出檔**都可以重讀** —— 那就去重讀，⛔ 不要用「誰說的」定案。
  而**記憶不能重讀**，所以「我記得我看到…」該撤就撤。

  ⚠ **這跟「kang 的實測是現階段唯一的現實檢查」不衝突**：
  他驗的是**畫面上發生了什麼**（那我看不到，一律以他為準）；
  這一條講的是**一份兩個人都看得到的證據**（那就回去看，不要投票）。

- **動手前先要真實的檔案或示意圖，不要照規格猜。**
  一個真實的 SVG 就照出四件事，全部是規格文件上看不到的。
- 🔴 **同一個檔案裡的寫法不一定能照抄，要先看那是什麼元素。**

  〔2026-08-25。刀具的預覽線切完之後藏不起來，病因是 `hidden` 是
  **HTML 元素**的屬性，對 **SVG 元素**不保證生效 —— 而我照抄了旁邊的
  `#marqueeBox`，那個是 `div`。藏／顯示一律改用 `style.display`。
  ⭐ **「只有虛線留著、實線正常消失」正好把病因指出來** ——
  兩條線的行為分岔，就把問題縮小到 SVG 那一半。經過見 `HISTORY.md`〕

### 二、寫幾何與演算法時

- **寫「應該是多少」的判定之前，先問「這條規則的成立條件是什麼」**，
  把條件寫進註解；判定邏輯抽成不碰 DOM 的純函式，才測得到。〔坑 5〕
- 🔴 **展開長度一律以網格攤平後的總和為準**（見「尺寸的依據」那一節）。
  〔2026-08-23 覆蓋。**舊版寫的是「一律用 `bendDevelopedLength()`（真弧長）」，
  那條已作廢**，不要再照它推〕
  - `bendDevelopedLength()` **沒有被廢掉，只是降級**：它算的是**理想圓弧**，
    是「另一側的參考值」，不是圖面尺寸的權威。
    兩者的關係已經釘在測試裡：**網格 ＝ 公式 − Σ弧弦差**。
  - 坑 6 那條仍然成立而且要留：**不要用 `mesh.area()` 或量頂點距離去反推**
    展開長度。攤平的結果直接量就好，反推是繞遠路而且會錯。
- **凡是「沿法向偏移」的運算，在不共面的地方都要想一下有沒有餘弦係數。**
  肉眼看不出來，只有算體積才抓得到。〔坑 7〕
- **判斷依據用幾何事實，不用使用者可改的標籤。**
  「要不要加厚」看 `mesh.isClosed()` 不看 `kind`；
  「是不是同一個點」比座標不比索引。〔坑 8、30〕
- 🔴 **「一個面」指的是共面區域，不是三角形。** 方塊在網格裡是 12 個三角形，
  使用者看到的是 6 個正方形面。凡是「對一個面做事」都要先 `planarRegions()`。
  〔`seam.js` 的 `cutAroundFace()` 早就寫著，寫 `edit.js` 時照樣踩進去 ——
  鐵律六的現行示範。2026-08-23〕
- **一段邏輯需不需要知道材料是什麼？需要就別寫在幾何核心裡。**〔坑 15〕
- **這種事不能靠推理，要靠對答案。** 凡是把 2D 座標搬到 3D 平面上的地方，
  都要問一次繞向對不對 —— 法向朝內的模型在畫面上看起來完全正常。〔坑 29〕
- **需要「兩邊算出同一個答案」時，與其小心地讓兩條路對齊，
  不如換一個只有一條路的定義。** 前者靠紀律維持，後者靠結構保證。〔坑 31〕
- **寫「這種情況不會發生」的假設之前，先想一遍「封閉的、繞回來接自己的」
  那種形狀。**〔坑 32、5〕
- **連續幾種「聽起來很對」的規則都失敗時，多半是問錯問題了**，
  不是還沒想到第六種規則。〔坑 16〕
- **設定邊的角色一律用 `mesh.setRole()`**（它會同時設兩條半邊）。〔坑 1〕
- **`arcSeg` 下限是 2** —— 切一段的圓弧跟一個倒角在幾何上完全一樣，
  資訊真的不存在。〔坑 10〕

### 三、做介面與回饋時（這一組全部是「讓人不敢相信這個工具」）

- **把內部的數字放上介面之前，先問「這個數字的單位是什麼」。**
  `length` 特別危險 —— 它總是「某種東西的個數」，
  而那個「某種東西」只有寫的人知道。〔坑 20〕
- 🔴🔴 **⛔ 還要再問一句：「這個數字在這個狀態下，有沒有意義？」**
  〔2026-08-28 加。**坑第 20 條的另一半，而這一半更難發現**〕

  > **一個函式回得出數字，⛔ 不等於那個數字是答案。**

  ⚠ **實例**：`mesh.volume()` 走散度定理，**前提是網格封閉**。
  而 `橋接`／`補洞` **在動手之前必定是開的** —— 那時它照樣回一個數字，
  **看起來完全正常**（六位數、有小數點、單位 cm³），
  【實證】但同一個形狀**搬個高度那個數字就變，搬夠遠還會變負的**。

  🔴 **⛔ 危險在於它不會報錯，也沒有任何徵兆**：
  型別對、範圍看起來合理、旁邊那個數字（做完的）還是真的 ——
  ⚠ **kang 的畫面上那句甚至讓體積看起來「少了 3329」，而實際是多了一段。**

  ⭐ **判準**：那支函式的**成立條件**是什麼（封閉？共面？至少幾個點？），
  現在這個狀態滿不滿足。**不滿足就不要印，⛔ 不要印一個「僅供參考」的版本。**
  〔跟鐵律二「寫判定之前先問這條規則的成立條件是什麼」是同一條，
  　只是那一條管**算**，這一條管**印**〕
- **誤報比漏報更糟。** 誤報會讓人學會忽略整個警告欄，那警告欄就等於不存在。〔坑 18〕
- 🔴 **讓兩個數字互相對得起來，錯誤才會自己現形。**
  一個孤零零的數字沒有人能驗；旁邊放一個**由它推得出來**的數字，
  對不上就會被看見 —— 而且是被**使用者**看見，不必等有人想到要去查。

  〔2026-08-23 實測。展開圖上原本只標「弧長 147.03」，那個數字**是錯的**
  （少算了頭尾兩格），而它一直錯著沒人發現。加上「32 段」之後，
  147.03 ÷ 32 對不上每段 4.9009，矛盾當場浮出來 ——
  **bug 不是被查出來的，是被那兩個數字自己吵出來的。**〕

  同一招在這個專案已經用過三次，只是以前沒把它寫成規則：
  尤拉公式驗拓撲、角虧總和驗 4π、「管 vs 布林」兩條路算同一個體積。
  差別只在**那三次是寫在測試裡，這一次是印在使用者的圖上**。
  → **標註要成對**：長度旁邊放段數、角度旁邊放半徑、總長旁邊放分段和。
- **紅色只留給「程式做不到你要求的事」。** 使用者自己選的後果用藍色說明，
  而且合併成一句講一次。〔坑 28〕
- **一個操作如果「有時候看起來沒作用」，就必須有東西持續顯示它到底有沒有作用。**
  不能靠使用者自己推理。〔坑 21〕
- 🔴 **同一件事只准有一份資料。兩份就一定會不同步，而且畫面上看不出來。**
  判準很好認：**幾顆按鈕該不該亮，如果是問不同的來源，那就是兩份。**

  〔2026-08-25 實測。「選了哪個物件」與「選了哪個面」分開記，
  於是同一個畫面上擠出是亮的、切一刀是灰的；更糟的是不灰的時候
  **會去切一個使用者根本沒在看的物件**，而且形狀改對了、沒有任何錯誤。
  修法**不是**在九個寫入點各補一行同步（那是紀律），
  而是把 `active` 改成當場算出來的 —— **沒有第二份資料就沒有東西會不同步**。
  這是坑第 31 條從測試搬到介面上的版本〕
- 🔴 **「使用者說他沒做過某件事，但程式表現得像他做了」——
  先去找是不是有兩份資料在記同一件事，不要先懷疑他記錯。**
  〔病因是 kang 一句「我沒有去選擇其他物件…我就是選面」指出來的〕
- **設計一個操作時先問「這個操作的結果唯一嗎」。** 不唯一就補條件補到唯一，
  補不到就明講。算錯會被發現，隨機只會讓人默默不敢用那個功能。〔坑 24〕
- **浮點數的相等判斷一律要有容許值，而且要挑一個有物理意義的量去比。**
  這個專案切的是珍珠板與壓克力，1e-6 cm 是浮點數的尺度、不是板材的尺度。
  同一個容許值要用在**判斷**和**計算**兩個地方。〔坑 25、26〕
- **不能因為放不下就不標。** 讀不出尺寸的圖比沒有圖更糟 ——
  看的人會以為自己讀到了。〔坑 12〕
- **不要被最極端的那個樣本綁死。** 只有真正需要全域的約束才全域，
  其餘放寬到它實際作用的範圍；**放寬要講清楚代價**。〔坑 27〕
- **沉默地退回是最糟的做法** —— 使用者會以為功能壞掉了。〔坑 11〕
- 🔴 **「沙箱說可以、kang 說不行」——先看介面，⛔ 不要先懷疑演算法。**
  兩邊對不上時，差別就在**畫面那一半**（沙箱驗得了幾何，驗不了畫面）。

  〔2026-08-25。他要做方塊 12 邊圓角，回報「就是不行」。**圓角本來就做得到**
  （沙箱驗過段數 1～16 全部封閉），卡住的是**要開加選點 12 下**，
  漏一條或重複點到同一條邊的另一側就會被擋，而**畫面上看不出哪條沒選到**。
  ⚠ 我一開始還去查資料層。解法是加一顆「全選邊」——
  ⭐ **能力對照表列的三大缺口是加線／補面／選取，而我一直只在做加線那一類。**
  經過見 `HISTORY.md`〕
- 🔴 **使用者說「似乎」「有時候」「偶爾」時，⛔ 不要當成他沒操作好** ——
  **那多半是狀態依賴，而且依賴的是一個你沒想到的東西。**

  〔2026-08-25。打完數字按 Enter「似乎沒作用」。病因是 `TransformControls.axis`
  是「滑鼠現在停在哪根把手上」，不是「這次在拖哪根軸」——
  指標直接從把手移出畫布的話軸就還留著。
  修法是**軸在拖曳開始時就記進 `_drag.start`**，跟「記初始座標、
  每幀從初始值重算」同一條：**軸也是初始狀態的一部分。**經過見 `HISTORY.md`〕
- 🔴 **加新功能時，順手檢查舊的錯誤訊息有沒有因此過期。**
  **訊息會在功能長大之後變成謊話** —— 它指的那條退路可能已經不存在了。

  〔2026-08-25。outer miter 做完之後「只導一部分的邊」**本身已經做得到了**，
  而原本那句「把邊全部一起選就可以」對圓錐（非直角）變成**死路**。
  已重新分類並釘成測試。這是坑第 34 條「不要寫一個不存在的退路」的
  第二種長相：**當初是真的，後來才變假的。**〕

### 四、效能

- **凡是「對每個頂點都查一次全部的面」都要警覺。**〔坑 3〕
- **寫進 `sync()` 或每幀迴圈裡的東西，先問「這段會不會隨模型大小成長」。**
  會的話要有辦法跳過它。平常測試的小模型完全看不出來。〔坑 22〕

### 五、查證與交付

- 🔴 **規格上合法 ≠ 別人的軟體讀得動。** 要送出去給別的軟體開的格式，
  只能用最保守的寫法：不用選用屬性、不用新版才有的欄位、骨架寧可多寫幾十行。〔坑 9〕
- 🔴 **這類東西沙箱驗不了。** 語法檢查過、瀏覽器看起來對，都不算驗證過 ——
  **一定要 kang 用真正的目標軟體開過。**〔坑 9〕
- **多段轉換的輸出路徑，驗證要放在最末端**，而且要用「跑完全程之後仍然守恆的量」
  去對（體積、外框）。中途的量可能一直都是對的。〔坑 17〕
- **外部驗證挑樣本要涵蓋不同的「網格結構」，不只是不同的形狀。**
  錐體與平板在使用者眼中是兩個形狀，在 bug 眼中是「全三角形」與「有四邊形」兩類 ——
  只驗了前者等於沒驗。分類至少要涵蓋：全三角形／有四邊形、封閉／開放、在原點／不在原點。〔坑 17〕
- **要測病因，不只測症狀。** 症狀會變，病因不會。〔坑 17〕
- 🔴 **開實測清單之前，每一項都要問一次「使用者驗得出這個結果嗎」。**
  跟「使用者做得出這個輸入嗎」是同一條的兩半，兩半都問過才算數。

  〔2026-08-25。任意切線那八項裡，第 5 項「轉個角度再切，位置對不對得上」
  **kang 沒有辦法量** —— 他的回覆是「這牽涉量測尺寸的機制」。
  而那一項偏偏是唯一「**錯了也看不出來**」的：切錯位置形狀完全正常、
  體積也對。**等於我把最重要的那一項開成了驗不了的一項**〕
- **說某個副作用「沒做」之前，要沿著呼叫鏈往下看一層。**
  grep 到「這裡沒有」只證明「這一行沒有」。〔坑 19〕
- **踩過的坑要往回看自己的專案，不要重新踩一遍。**
  下載那三個地雷，組裝系統結構說明表早就解過了。〔坑 11〕

### 六、關於日誌本身

- 🔴 **跨功能的原則不能只留在第一次遇到它的那個功能底下。**
  同一個陷阱在不同功能底下會長得不一樣，而**人只會讀自己正在做的那一節**。
  〔坑 33 —— 這條也是「鐵律」這一節存在的理由〕
- 🔴 **不要在日誌裡寫一個不存在的退路。** 日誌是給未來的 AI 讀的，
  **讀的人會照著它做決定，而且不會回頭驗證每一句話。**
  提到「可以用某某功能」時，要嘛當場確認它存在，要嘛寫成待辦。〔坑 34〕
  - **怎麼確認＝ `grep` 那個函式名，看有沒有介面在呼叫它。**
    「函式寫好了、測試也過了」不等於「使用者按得到」——
    寫完文件掃一次，成本三十秒。
    〔2026-08-23 又犯一次：規格檔寫「拉面預設沿法向、自由三軸要自己切過去」，
    而 `pushFace()` **只有測試在呼叫**、那個開關根本不存在。
    是 kang 的實測截圖照出來的，不是文件攔下來的。**寫的人跟抄鐵律的人是同一個。**〕

- 🔴🔴 **寫「不行」的時候，一定要寫清楚它是被什麼擋住的。**
  〔kang 2026-08-27 的重講：**「原來不是不能做…而是需求擋住」**。措辭沿用他的〕

  > ⛔ **「✘ 不做」＋一個數字**，讀起來就是「**本來就不行**」。
  > ✅ **要寫成「被 X 擋住，所以在 Y 的用途下不做」。**

  ⚠ **差別是實際的**：前者讓下一個人**連想都不會再想**；
  後者讓他知道 **X 或 Y 一變，這條就要重新看**。

  🔴 **2026-08-27 一天之內三個實例，全部是 kang 問出來的**：

  | 原本寫 | 實際上 |
  |---|---|
  | 「溶解面 ✘ 明確不做（偏離 96 倍）」 | **做得到** —— 網格建得起不平的 n 邊形；擋住它的是「**那張圖要從平板子上切下來**」 |
  | 對照表「邊迴圈 ❌ 走不動 —— 頂點是 3 價」 | **只有方塊與圓柱走不動**，**球走得動**（372 個四價點）—— 那句話**只量過兩個模型卻被推到全部** |
  | 對照表「沒有任何補洞的能力」 | **早就有了**（`補洞` 2026-08-25） |

  ⭐ **共同點：一句「不行」，沒有寫清楚它是被什麼擋住的。**
  ⭐ 而 **⭐ 這一條掛在「每次寫 ✘ 的當下」**，不是掛在「以後記得回來檢查」——
  ⚠ **後者這個專案已經證明沒用**（提醒擋不住第四次）。

- 🔴 **⛔ 不要因為文件整理好了，就減少質問。**（2026-08-26 加，出處見下）

  > **這套結構的價值，是讓被問到時三跳查得到答案；不是讓人可以不問。**

  ⚠ **這一條有實證，而且很難看**：`D:\彩藝通系統整合\整理方法.md` 最後一節記著 ——
  建立那套結構的**那一整輪**，AI 自己犯了六次錯，**六次沒有一次是文件攔住的**，
  全是業主當場質問、或被逼著跑機械檢查才抓到。

  ⭐ **這正好對上今天查出來的事**：六張實測清單漏記，
  **不是文件寫得不夠清楚，是流程裡沒有那一步**（見「協作規則 → 實測結果的回寫」）。
  🔴 **文件擋不住的東西，要靠流程或機械檢查，⛔ 不是再寫一條規則。**

> **只有坑第 2 條（gizmo 初始化順序）萃取不出一般規則** ——
> 它是一支檔案的建構子順序問題，修掉就沒了。原文仍在 `HISTORY.md`。

## 這個子專案踩過的坑 → 已搬到 `HISTORY.md`

> 34 條的**完整經過**（現象、數字、怎麼查出來的、繞了多久）在 **`HISTORY.md`**，
> 一字未改。**規則看上面「鐵律」那一節就好**，經過只有在「這條到底怎麼回事」
> 講不清楚時才去翻。

---

## 待辦 / 可延伸

> 🔴 **已結案的 40 項（35 項 `[x]` 完成、5 項 `[~]` 撤案）已於 2026-08-27
> 　逐字搬進 `HISTORY.md`「已結案的待辦」一節** —— 照落點規則「結案就移出，不堆積」。
> **⛔ `[~]` 那 5 項是「明確不做」的**（板材尺寸上限檢查／板件分片／`pushFace()` 接介面／
> 溶解面／第 5 期極座標展開），搬走不等於解禁，要查就去 Grep 那一節。
> **本節從此只放還沒做完的事。**

- [ ] ⏭ **量測第 2 步的「每一個」：等 kang 的呈現方式**

      🔴 **⛔ 不是不做，是他要再想** —— kang 2026-08-27：
      > 「**每一個先暫時收起來...我思考一下還有甚麼方式可以呈現..會在跟你說想法**」

      它是「32 條邊各標自己的長度、**標在元素身上**」那一種。
      ✅ **左下角那一塊已經完成並實測通過**（七項全過，經過在 `HISTORY.md`
      「量測第 2 步：標尺寸」那一節）—— 這一條只剩「每一個」。

      **程式全都留著，⛔ 不是殘骸**：`measureLabels()` 的 `'each'`、
      `scene.js` 那一整套 Sprite、以及它們的測試，`setMeasureMode('each')`
      也還收得下 —— **只是目前沒有任何介面在傳它**。
      🔴 ⚠ 這代表「**寫好了而使用者按不到**」（那正是 `pushFace()` 那個坑），
      所以三個地方都寫著它在等什麼：這一條、`index.html` 那顆按鈕的註解、
      `setMeasureMode()` 的註解。

      🔴 **他回來提想法時，第一個要面對的是這條**（檔頭那一欄記著）：
      > **判準是「這個東西指不指著模型上的某個地方」** —— 指著就得畫進
      > 3D 場景，而**站在元素重心上就一定會跟 gizmo 搶位置**（第一版死在這裡）。

      ⛔ **用法與規格一律去 `規格\建模器-量測.md`**「⏭ `'each'`」那一節。


- [ ] 🔴🔴 **下一批：三項 ＋ 內建鋼筆（kang 2026-08-27 定的順序）**

      > **「沿面滑動邊...由點建面...橋接邊迴圈..我都想要有功能..
      > 　這對編輯建模很有幫助」**
      > **「我同意 1」**（＝ 先把那兩項做完，再做鋼筆）

      | 順序 | 項目 | 狀態 |
      |---|---|---|
      | ✅ | **點連成面** | **完成並實測通過**（六項全過，經過在 `HISTORY.md`「點連成面」）|
      | ✅ | **橋接邊迴圈** | **2026-08-28 完成並實測通過**（七項全過）|
      | ✅ | **沿面滑動邊** | **2026-08-28 完成並實測通過**（八項全過）。⚠ 這裡原本寫「難在拖曳介面、⛔ 不是數學」—— **只對一半**，它藏著「公分 vs ％」這個結果不唯一 |
      | **1** | **內建鋼筆（貝茲繪製）** | ⏳ 🔴 **kang 2026-08-27 決定要做**，見下方獨立那一條。⚠ **卡平板**（兩個要問的問題都跟平板綁著）|

      ✅ **第 10 節標 ⭐⭐ 的已經清空了**（2026-08-28）——
      ⭐ 現算：`awk '/^### 10\.1/,/^### 10\.7/' 外部參考-Blender編輯.md | grep -c '^| .*| ⭐⭐'`
      ⚠ **⛔ 跑之前先看那一節開頭的「最後回寫」日期**（見上方「kang 定的方向」那則）。

      ✅ **`平行複製一圈邊` 2026-08-28 完成，⏳ 待 kang 實測**
      （工具列 `平行複製` ＋ 寬度）。測試 2288 → **2312 項**。

      ⭐ **那個「零件現成」的估計驗證了，但 ⛔ 猜錯了一個零件**：
      加線那半**⛔ 不是 `rebuildWithInserts()`**（那支只插點、不切開面，
      `邊上加點` 用的就是它）—— 正解是**把 `loopCut()` 的後半段抽出來**
      成 `cutFacesAlongInserts()`。
      🔴 **抽對了的證明 ＝ 環切自己的測試一項都沒掉**（2288 → 2288）。

      ⛔ **用法與完整規格一律去 `規格\建模器-點線面編輯.md`「平行複製」那一節。**

      ⏭ **⛔ 不選「沿邊滑動頂點」，儘管它看起來是滑動的近親** ——
      **選一個點，它有 N 條鄰邊，沿哪一條滑不唯一**（坑第 24 條），
      要指定就得**同時選一個點和一條邊**，而 `editSels` 明文擋掉跨型別多選。
      🔴 **它不便宜，反而是卡住的那一個** —— 排到後面，⛔ 不是不做。

- [ ] ⏳ **待實測：`平行複製` 六項（桌機）**

      🔴 **這張清單一定要放在日誌裡，⛔ 不可以只寫在 commit 訊息**（坑第 34 條）。
      ⭐ **數字一律講成公式** 〔這一輪的教訓：同一個毛病犯了三次 ——
      實測清單寫死 223963.70、寫死「往上」、測試名稱寫死「34→98」〕。

      **前置**：方塊（`寬` 60、`深` 45、`高` 40）→ `轉成可編輯網格`
      → `拉點線面` → 切到「**邊**」→ 點一條**直立**的邊 → `環切`
      （切完那一圈自動選中）。

      | # | 做什麼 | 應該看到 |
      |---|---|---|
      | 1 | 按 `平行複製`（寬度留 2） | 原本那一圈的**兩側各多一圈**。訊息講「已在兩側各加一圈，共 **選到的條數×2** 條新的線」（方塊是 **8**）|
      | 2 | 🔴 看訊息最後那個體積 | **⛔ 跟按之前一模一樣**（方塊 60×45×40 ＝ **108000.00**）—— 它只加線 |
      | 3 | 🔴 按 `結構分析` | 面數 ＝ **環切後的面數 ＋ 選到的條數×2**（方塊 10 → **18**）；**尤拉數仍然是 2、封閉、結構無誤** |
      | 4 | 把寬度打 **500** 再按一次 | ✅ **⛔ 不擋**，新的兩圈停在端點，並講「⚠ 有 **N** 個地方頂到底了」|
      | 5 | 按一下 `公分` 變 `％` → 寬度打 **25** → 按 | ⭐ 跟打 **5 公分**是同一個結果（方塊的軌道長 20，25% ＝ 5）|
      | 6 | `％` 打 **100** → 按 | ✅ **擋下來**：「偏移 100% 會疊在原本那一圈上（那等於沒加）——請打小一點」|

      ⭐ **⛔ 不必相信我，第 2、3 項自己把答案驗出來了**（鐵律三）：
      **只加線的東西，體積一定不變、尤拉數一定還是 2**。變了就是改壞了。

      ⚠ **這一輪動了一支既有的函式**（把 `loopCut()` 的後半段抽出來共用），
      所以 **`環切` 也要順手按一下確認沒壞** —— 沙箱那 2312 項全過，
      但**畫面那一半沙箱驗不了**。

      ⏳ **平板版面沒驗**（跟刀具那三項卡在同一件事）。

- [ ] ⚠ **圓錐（尖頭那種）環切不出東西，所以也滑不了**（2026-08-28 記，⛔ 這一輪沒碰）

      【實證】圓錐側面是**三角形**，而 `edgeRing()` **只穿得過四邊形**
      → 環切一圈 **0 條**。🔴 **那 ⛔ 不是滑動的問題，是環切本來就走不動。**
      要解得動 `edgeRing()`／`loopCut()` 的核心 —— ⚠ **那是核心，動它要跑全部的測試。**

- [ ] 🔴🔴 **內建鋼筆（貝茲繪製）—— kang 2026-08-27 決定要做，畫在地板上**

      > **他的原話：「我想在建模器裡直接畫」／「因為確實『Illustrator 畫好
      > 　再匯入』這是一個方式..但是建模器還是要有這功能...雖然說是小東西..
      > 　但是同樣重要...這樣用途選擇才會更彈性自由」**

      ✅ **拍板了的**：**畫在地板上**（跟 `匯入線稿` 一樣，形狀平躺在地板，
      畫完往上拉厚度）——⭐ 跟他現在已經在用的那條路完全一致。

      ⭐ **調查結果：核心的數學全部是現成的，⛔ 缺的只有「畫」那個介面**

      | 需要什麼 | 現成的在哪 |
      |---|---|
      | **曲線變折線**（網格裡沒有曲線）| `svgPath.js` 的 **`flattenCubic()`** —— 匯入線稿天天在用 |
      | **哪些角是「真轉角」** | `svgPath.js` 的 `turnDeg()` —— ⭐ 連判定都繼承 |
      | **輪廓 → 3D（拉厚度）** | ✅ **整條都在**，擠出件還是參數體，**出口不必重做** |

      ⚠ **但規格檔當初那句話仍然成立，⛔ 不要假裝它很小**
      （`規格\建模器-匯入線稿.md`「為什麼是這一項，不是內建繪圖工具」）：
      > **「匯入一天做完，內建繪圖是從零長一個工具。」**

      🔴 **⚠ 那一段的另一半已經過期了，⛔ 不要照它推**：它說「**先做別人給的**」——
      而那個先後**已經走完**（匯入線稿 2026-08-22 完成）。
      ⭐ **kang 給的是新的一格理由：「用途選擇才會更彈性自由」**，
      ⛔ 不是「Illustrator 不好用」。

      ⏳ **還沒問的（動工前要問）**：平板上鋼筆怎麼操作
      （點放錨點 vs 拖拉把手 —— ⚠ **刀具那一輪解過同一類問題**，
      `setKnifeInput()` 把手勢整組換掉，⭐ 可以照抄）；
      曲線的把手怎麼折斷（Illustrator 按 Alt，⚠ **平板上沒有 Alt**）。

      ── 🔴 2026-08-28 併入：原本另有一條「內建貝茲繪製」（8/22 提出）──

      ⚠ **那是同一件事，卻各自掛著一條待辦** ——
      而**新的這條有調查結果與拍板內容，舊的那條不知道**：
      下一個人 Grep「貝茲」會撈到兩條，讀到舊的就會以為還沒定案。
      〔文件分層兩條鐵律之一：**同一條規則只寫一次**〕

      **舊那條裡有兩句是這裡沒有的，逐字留著**：

      > kang 2026-08-22 的原話：「**造型單純或補造型時就不一定需要
      > 　Illustrator 先製作**」。補的是「懶得開 Illustrator 的那些小東西」，
      > 　不是主力 —— **瀏覽器裡做的貝茲工具永遠不會比 Illustrator 好用**。

      🔴 **⚠ 那句「不是主力」跟他 8/27 說的「雖然說是小東西..但是同樣重要」
      ⛔ 不衝突，但要一起讀**：**做，而且要做好；但⛔ 不必贏過 Illustrator。**
      ⭐ 那正好也是「畫在地板上」這個決定的依據 —— 跟他現在已經在用的
      `匯入線稿` 走同一條路，⛔ 不另外發明一套。


- [ ] 🔴 **瓣片展開 A：選一整條經線 → 標成分片切割線**

      ⏭ **排到後面**（kang 2026-08-27）：
      > **「既然這新的展開方式…整體需要仔細調查討論與解決…這先不要先做」**
      > ——他隨即澄清：**「不是不做..是排到後面」**。

      ⛔ **不是否決** —— ⛔ 不要標成 `✘ 明確不做`，也不要當成可以刪。
      🔴 **kang 要 A 跟 B 一起做**（見下一條），所以 **A 不再是「小項、隨時可插隊」**，
      ⛔ 不要拿它當填空的小工。

      **幾何完全不用動**（沙箱已驗：12 片、省道需求 0.00°、零警告）。

      🔴 **選取介面的卡點只解掉一半，⛔ 上一版寫「只剩一步」是錯的**（2026-08-27 更正）：

      | | 狀態 |
      |---|---|
      | **怎麼走訪出整條經線** | ✅ **解了**。`選一條線`（邊迴圈）一下就選起整條經線（實測 32 條 ＝ segH）。⭐ `edgeLoop()` 是**純函式**，只吃 `(mesh, he0, opt)`，**不依賴任何模式** |
      | **怎麼把它交給分片** | ❌ **沒解**。⛔ **這一格上一版根本沒寫** |

      🔴 **病因（kang 2026-08-27 當場問出來的：「選一條線是編輯區，分片不是編輯區」）**：

      | 事實 | 出處 |
      |---|---|
      | `setEditMode(false)` 會 `clearEditSel()` —— **選取歸零** | 【實證】`js/view/select.js:1413-1415` |
      | 進分片模式時 `exitOtherModes()` **一定會**呼叫 `setEditMode(false)` | 【實證】`js/main.js:597-599` |
      | `seamPick()` **一次只處理一個 hit**，它**不看 `editSels`** | 【實證】`js/main.js` `seamPick()` |

      → **在編輯模式選好 32 條，切到分片模式的當下全部消失。**

      **三個候選解（⛔ 還沒拍板）**：

      | | 做法 | 評估 |
      |---|---|---|
      | **甲** | **分片模式加一顆「選一條線」**：點一條邊 → `edgeLoop()` → 整條一次 `setSeam()` | ⭐ **推薦**。⛔ 不動模式邊界、不動 `editSels`。⭐ **分片模式本來就有這種東西** —— `cutAroundFace()` 就是「點一個面 → 一次標一整圈邊」，**同一個骨架** |
      | **乙** | 讓選取跨模式帶過去 | ⛔ **不要做**。`select.js:1221-1241` 那段註解就是 kang 實測抓到的核心 bug（**選取有兩份資料會切錯物件**），修法正是「沒有第二份資料就沒有東西會不同步」。乙案等於把那個病種回去 |
      | **丙** | 編輯模式加一顆「標成切割線」 | ⛔ 違反 kang 定的「**功能之間的定位不可以互相模糊**」（切一刀 vs 環切、選一圈 vs 選一條線都照這條分） |

      ⚠ **另一則也更正過**：原本寫「動工第一件事：實測『選一圈』抓不抓得到經線」——
      **答案是抓不到**（邊環是繞著球跑的，同一條經線只抓到 1 條）。

      連帶：橫向折線要畫成壓線，而且要跟切割線在圖上分得出來
      〔跟既有待辦「隱含切割線要在圖上跟指定分片區分開」是同一件事〕。
      完整內容見 `瓣片展開與省道-設計方向.md` 第 6.A 節。

- [ ] 🔴 **瓣片展開 B：省道**　⏭ **跟 A 一起，排到後面**（kang 2026-08-27）

      🔴 **kang 拍板：「如果要做 AB 全要一起做」** ——
      ⛔ **所以「A 做完之前不要開始 B」那條分批的排法已經被他推翻**，
      ⛔ 不要再照舊的「A 先 B 後」推。

      ✅ **第 8 節那個 ❓ 因此結案了**：他要的是**（乙）12 瓣、橫向也要圓**
      ＝ **需要省道**。〔原問題：「12 瓣橫向 12 邊形」還是「橫向也要圓」，
      　工作量差一個數量級〕

      ⚠ **但一定要先跟他講清楚（已講，2026-08-27）**：
      **開了省道也不會變成數學上完美的圓。** 一張平的紙只能朝一個方向彎，
      橫向也要彎就是雙向曲率，**一定有失真**。想更圓只有兩條合法的路：
      **把 `segW` 開高**（片數變多、零失真）或 **開省道**（片數不變、材料吃曲率）。

      把「沒有出路的角虧」變成使用者看得見、控制得了的省道。
      兩個未解問題（**都要先解掉才動工**）：
      **① 省道位置不唯一**（坑第 24 條：結果不唯一就不要猜）——
      傾向讓使用者畫，跟「分片是製造決定」那條定案一致；
      **② 歸併的代價要量出來並印在圖上**，不可以藏起來。
      🔴 **還有第三件，動工前一定要查**：`瓣片展開與省道-設計方向.md` 第 4.4 節
      那個「**球完全不標接縫，卻一則警告都沒有**」——若屬實就是**沉默的漏報**
      （坑第 11、18 條），而 **B 整個建在「角虧沒有出路 ＝ 省道總量」那個數字上**。
      ⚠ 那一項**只跑過一次**，⛔ 不要據此下結論。
      完整內容見 `瓣片展開與省道-設計方向.md` 第 6.B 節。

- [ ] ⚠ **查清楚：球完全不標接縫時，為什麼一則警告都沒有**
      沙箱實測跑出「1 片、720° 全部靠隱含切割線吸收、零警告」，
      而 `flatten.js` 自己的註解說那張圖「根本不能下料」。
      若屬實就是**沉默的漏報**（坑第 11、18 條）。
      ⚠ **只跑過一次**，`overlap` 那條「太大就不檢查」的路有沒有被走到還沒確認 ——
      ⛔ 不要據此直接下結論。

- [ ] ⚠ **非直角的角落還是做不出圓角**（圓錐那類）。
      那正是 Blender 開 Miter（Sharp／Patch／Arc）三個選項的地方 ——
      `s = w × sin(t × 夾角)` 那條封閉式是從正交推出來的，非正交不成立。
      ⛔ 不唯一就不猜（坑第 24 條）。真的要做之前先查清楚那三種各解什麼。

- ⏭ **選取第二批的「乙」：相連 ✘ 先不做／增長縮減 ⏭ 排到後面**
      🔴 **⛔ 兩個都不是「做不到」，而且⛔ 兩個的理由不一樣，只有一個站得住。**
      〔**甲（依銳邊＋選相似）2026-08-27 完成並實測通過**〕

      | | 被什麼擋住 | 🔴 什麼時候要重新看 |
      |---|---|---|
      | **相連**（選一整塊相連的） | **我們的物件只有一塊** —— 【實證 2026-08-27 沙箱】方塊／圓柱／**管**／球**全部 1 個連通塊**，所以它按下去 ＝ 全選，跟 `全選邊` **定位模糊** | **出現真的有好幾塊的物件時**：匯入線稿有好幾個封閉輪廓、布林減出中空的殼、匯入 STL。⚠ 而真的分成兩塊時 `分離` 是更好的答案 |
      | **增長／縮減** | ⚠ **⛔ 這一格的理由已經收回，見下** | ⏭ **排到後面，⛔ 不是不做** |

      🔴 **只有「相連」那一格站得住，而它站得住的理由是「功能重複」** ——
      物件只有一塊時它按下去 ＝ `全選邊`，撞到 kang 定的
      「**功能之間的定位不可以互相模糊**」。**那是查出來的，不是猜的。**
      ⭐ 跟「溶解面被『圖要能從平板切下來』擋住」同一類。

      🔴🔴 **⛔ 而「增長／縮減」那一格的理由是錯的，2026-08-27 當天收回。**

      > 原本寫的是：**kang 說「目前經驗是沒有」**（沒遇過想選起一整片面）。

      ⚠ **問題不在他的答案，在我問了那個問題** ——
      「鐵律 → 目的藏在下游」的但書明文禁止：
      **⛔ 不要問 kang「這個功能你實際上多常用」**，
      因為**編輯功能的下游還不存在**，逼他猜會得到「推論冒充的現實」。

      🔴 **他當天把前提直接講明了**：
      > **「目前我並沒有 3D 列印的經驗…我做這個建模器..主要運用就是
      > 　未來會運用在 3D 列印與展開圖」**

      → **「沒有經驗」是必然的，不是判準。**
      **現階段排順序的唯一判準仍然是對照表**（kang 8/24 定的）。
      ⏭ 增長縮減標 ⭐ → **排在 ⭐⭐ 後面，⛔ 不是被砍掉。**

      ⚠ **這是同一條規則第二次被違反**
      〔第一次：2026-08-25 問「你實際建模時多常需要量兩點距離」〕。
      ⭐ **而且這次我還自己編了一個理由說服自己**：
      我說「3D 列印那條路他是真的在用的，所以這個問題他答得出來」——
      那句是從日誌「**已經可以投入生產**」推出來的，
      **而「可以投入」不等於「已經在用」。** 🔴 推論不是權威事實。

      ⭐ **走訪不必重寫** —— `edit.js` `separateAlongEdges()` 裡那段
      「從一個面出發沿 `he.twin.face` 擴散」**就是相連**，
      增長只是**同一段走訪走一層**。要做的時候搬過去就好。

      ⚠ **真的要做增長時，這個問題還沒解**（結果不唯一，坑第 24 條）：
      **擴的定義是「共用一條邊」還是「共用一個點」。**
      【實證】**方塊上兩種一樣（都是 5 個面）**，要到球才分得出來：
      共用邊 5 個（十字形）、共用點 9 個（九宮格）。Blender 用的是共用點。

- [ ] ⏭ **選取那一格還剩兩項**（2026-08-27 補列，以前待辦漏了）

      | | 狀態 |
      |---|---|
      | **面迴圈**（⭐） | ✅ **完成並實測通過**（2026-08-27，工具列 `選一圈面`） |
      | **依特徵全選**（⭐⭐） | ✅ **結束**（兩項完成、一項被擋住，見下） |

      🔴 **`選一圈面`（2026-08-27）—— ⭐ 走訪一行新的數學都沒有。**

      > **面迴圈 ＝ `edgeRing()` 走訪時「穿進去的那些面」。**
      > 邊環與面迴圈本來就是**同一條路徑的兩種讀法**（一個讀邊、一個讀面）。

      | 模型 | 選起來幾個面 |
      |---|---|
      | 方塊 | **4**（繞一圈的側面，⛔ 不含頂底） |
      | 圓柱 seg32 | **32** |
      | 球 segW12 segH32 | **12**（同一條緯度帶繞一圈） |

      🔴 **種子一定是「一條邊」，⛔ 不能是「一個面」** ——
      一個四邊形有**兩個方向**，從面出發**結果不唯一**（坑第 24 條）。
      ⚠ 這也是它跟 Blender 的差別：Blender 用「游標離哪條邊近」暗示方向，
      而我們是**平板優先** —— 明確點一條邊比猜游標位置可靠。

      🔴 **⚠ 順序不能反：先換過濾器，再選面**（kang 2026-08-27 拍板自動切）。
      `setEditFilter()` **會清掉不合型別的選取** —— 反過來寫的話
      「選好 32 個面 → 切到面 → **32 個全被清光**」，而畫面上什麼都不會發生。
      ⭐ 另外給 `setEditFilter()` 加了 `quiet` 參數（⛔ 不抄第二份切換流程），
      免得它的提示蓋掉按鈕自己那句話。

      ⛔ **它 ⛔ 不叫 `faceLoop`** —— `mesh.faceLoop(f)` 早就存在而且
      意思完全不同（一個面自己的半邊迴圈）。程式裡叫 **`loopFaces()`**。

      🔴🔴 **`closed` ⛔ 不可以照抄 `edgeRing()` 的**
      〔kang 2026-08-27 實測第 4 項問出來的〕：

      > 圓柱點「圓蓋與側面之間」那一圈的邊 → 只選到 **2 個面**
      > （一片側面 ＋ 一個 32 邊形的圓蓋，**穿不過去就停了**），
      > 而 `edgeRing()` 回 `closed=true` → toast 說
      > 「**已選起一整圈 2 個面（繞回來了）**」。⛔ **它根本沒繞回來。**

      ⭐ **判準：選到的面 ⛔ 全部都是四邊形**（面迴圈只穿得過四邊形，
      收進來一個非四邊形就是撞牆的證據）＋ `edgeRing` 自己也說繞回來了。

      ⚠ **⛔ 不要改用「面數 ＝＝ 穿過的邊數」** —— 試過，
      **那個案例裡兩個數字剛好都是 2，分不出來**。
      〔鐵律二的又一例：**該問的不是「數量對不對」，
      　而是「有沒有撞到走不過去的東西」**〕

      🔴 **依特徵全選其實是檢查工具，不是選取工具** ——
      三項全部是「STL 列印失敗的原因」，而 `validate()` 只報得出數字、
      **指不出來在哪**：

      | 項 | 狀態 |
      |---|---|
      | **破洞** | ✅ 完成並實測通過（2026-08-27，紅線） |
      | **非流形**（＋順手做掉的「**整個朝內**」與「**面貼反了**」） | ✅ **完成並實測通過**（2026-08-27） |
      | **鬆散元素**（孤點） | ✘ **被 `cleanRebuild()` 擋住**，見下 |

      🔴 **✘ 鬆散元素：被 `cleanRebuild()` 擋住，在目前的用途下不做**
      （2026-08-27 沙箱實測結案，⛔ 不要重提）。

      > **⛔ 不是做不到，是做了永遠報 0。**

      **【實證】把 kang 按得到的每條路跑一遍，孤點全部是 0**：
      方塊本身／刪除面／導角（1 條與全部 12 條 seg3）／內縮／環切／補洞／翻面。

      **病因是設計，不是巧合**：每一條「拆掉重建」的路**都先過
      `cleanRebuild()`**，而它三件事之一就是清掉孤點。
      `edit.js` 那段註解白紙黑字寫著這是刻意的：
      > **「這三支（預檢／清乾淨／搬選取）是同一件事的三面，不能只做一個。」**

      ⚠ **日誌原本寫「四個工具全部會產生孤點」那句是對的，⛔ 但它講的是
      「過程中」** —— `cleanRebuild()` 在把結果交出去之前就清乾淨了。
      🔴 **⛔ 不要照那句話推論「所以最終網格裡有孤點」。**

      🔴 **什麼時候要重新看**：做了**匯入 STL／OBJ** 之後 ——
      **外面來的檔案沒有人幫它過 `cleanRebuild()`**。
      〔那一項在待辦裡，標「附加功能，不是主力」〕

      ⭐ **鬆散元素的兩半都是現成的，⛔ 不必新寫任何東西**：

      | 要什麼 | 現成的在哪 |
      |---|---|
      | **偵測** | `edit.js` `preflight()` 回 `orphans:number[]`（沒被任何面用到的頂點索引） |
      | **畫法** | 【實證】`scene.js` `_buildLineOverlay()` 的**第三個參數 `dots`** —— 走 `THREE.Points`（`size:11`、`sizeAttenuation:false`、`depthTest:false`），⭐ 刀具的「點過的位置」就是用它 |

      ⚠ **只差一件事**：`_setIssuePreview()` 目前寫死傳 `null` 給 `dots`，
      要做的時候讓它收得下就好。⛔ **不要另開第三個 overlay 槽**
      （理由見 `scene.js` 那段註解：清除路徑有兩條，多一個欄位就會漂）。

- [ ] 🔴 **讓邊界迴圈支援「一個頂點多條」**（`mesh.js` `_buildBoundaryLoops()`）
      現在用 `Map<頂點id, 邊界半邊>` 串迴圈，所以一個頂點只放得下一條。
      **刪除面已經因此被擋掉一種情形**（洞在一個頂點上捏成一點）。
      ⚠ 這是**核心**，動它要跑全部的測試。

- [ ] **第三件之二：第 6 期其餘三個工具**
      🔴 **順序照 `外部參考-Blender編輯.md` 第 9.9 節**（兩輪實測後重排過）：
      **內縮 → 刪除面 → 導角 → 合併頂點**
      〔原本第三位是「刪除面／溶解面」，**溶解面那一半已改成不做**；
      　**環切已完成**〕
      - 🔴 **內縮 ← 下一個要做的就是這個**：解法現成 ——
        抄 `mesh.js` `shell()` 那段 miter 修正
        （只推角平分線 5 → 實際牆距 3.5355；除以 cos → 5.0000）
      - **刪除面**：留一個洞。⚠ 會產生孤點（實測過），要走預檢那三個配件
      - 🔴 **導角（單段）：演算法已經完整驗證，含角落**（2026-08-25，
        `外部參考-Blender編輯.md` **第 11 節**，動工前讀那一節就夠）。
        ⭐ **它沒有專用的數學** —— ＝ 加線（內縮）＋ 加面，所以**排在內縮之後
        幾乎是免費的**（內縮那一支直接重用）。
        四個案例全部 χ=2 封閉 ok：一條邊（體積 107250 ＝ 手算精確吻合）、
        一個角落三條邊、12 條邊全導、16 段圓柱上下兩圈。
        ②③ 另用**蒙地卡羅獨立重算**對過答案。
        ⚠ **多段（圓角）的角落真的不唯一，要選，那一項還沒解** ——
        但單段本身就有用（板材的斜切邊、45 度 V 溝），可以延後。
        🔴 **kang 2026-08-25 實測抓到兩件事**（見 `外部參考-Blender編輯.md` 11.7）：
      ① **偏移方向算錯**（只導一條邊時，角要沿「沒被導的那條邊」滑，
      不是垂直於被導的邊偏移）—— **方塊因為兩邊垂直，剛好把錯誤藏起來**。
      症狀是被吸收的面變成非平面，畫面上出現奇怪的形狀。已修，
      修完掉出「**一條一條導 ＝ 一次全導**」這個更強的性質。
      ② 🔴 **「我們的頂點全是 3 價」那句話是錯的** —— 圓錐 seg12 是
      {3:10, 4:2, 5:2, 6:2}，而角落規則建在那句話上。
      ✅ **4 價以上的角落，第四次解出來了**（2026-08-25，kang 說「既然有機會
      解決，我們先解決」之後回頭做的）。防護已移除。

      🔴 **那個限制本來比估的嚴重得多**：**導角自己就會製造 4 價頂點**
      （方塊導完頂面四條，價數 `{3:8}` → `{3:8, 4:4}`），所以擋掉的不是
      「圓錐那種特殊形狀」，而是**「先導一組、再導旁邊那一組」這種日常操作**
      （kang 原話：「先選水平的四個邊導角，再選垂直的四邊導角，就會出現奇怪的形狀」）。

      **解法的關鍵只有一件事，而且它是反的**：繞著頂點走一圈時
      （`vertOutgoing()`）的順序，跟**面迴圈裡的順序相反** ——
      前者要「`cur` 側先、`prv` 側後」，後者是「`prv` 先、`cur` 後」。
      **第二次嘗試邏輯完全正確，就死在這裡。**

      ⭐ **第四次成功的真正原因不是新想法，是先建驗證集再改程式** ——
      前三次的共同點是「每次都通過了一部分案例，看起來對騙了我」。
      驗證：8 個案例全過 ＋ **320 組隨機壓力測試（8 種形狀，每組再連導第二刀），
      拓撲壞掉 0 次**。

      ⚠ **仍然存在、但不是 bug 的**：曲面上的斜切面不會是平的
      （不平的**全部是 4 邊形斜切面**，被吸收的 n 邊形一個都沒有不平；
      最大偏離 0.11 cm）。**不可能兩邊都平**，改成垂直偏移的話旁邊那個
      既有的面反而會不平。→ **不擋，但 toast 會講出幾片不平、最大偏離多少。**
      ⚠ 凹邊、自交還沒測。
      - **合併頂點**：最貴，索引位移最嚴重
      ✘ **細分不做** —— 四種補法要先問 kang，而擠出＋拉多半可以取代
      ⚠ **「刪除」與「溶解」是兩個不同的指令**，不要合成一個
      （刪一個面留一個洞／溶解一個面把周圍併起來。**結果不唯一就不要猜**）。
      ⭐ **走訪函式已經有了**：`edit.js` 的 `edgeRing()`（環切那一輪做的），
      刪除面／導角要走一圈邊時直接用，不要再寫第二支。
      🔴 **導圓角動工前一定要再查一輪**：Blender 的 edge bevel 底層**需要
      「兩邊形的面」當中間步驟**，而 `Mesh.fromFaceList()` **直接跳過少於 3 點的面**
      —— **Blender 那條路我們走不了，必須另外想一條。**
- [ ] **匯入 STL／OBJ（附加功能，不是主力）**
      客戶給了檔案要拿來做時用。⚠ 2026-08-23 曾被誤排為第一優先，
      kang 當場否決：「如果我可以匯入 STL／OBJ...我還要這一個程式做甚麼」——
      那會讓程式退化成另一個 Pepakura。經過見 `編輯功能設計方向.md` 第 1 節

- [ ] **分段尺寸只有一段而且等於總長時，不要重複畫那一排**
      圓柱身體修好之後整片就是一個折彎區，底下兩排都印 `156.83`
      （上排分段、下排總長）。不算錯，但佔一行版面又讓人多看一眼。
      〔2026-08-23 kang 實測截圖。以前分段是 `4.9 / 147.03 / 4.9` 才有意義〕

- [ ] **匯入 PNG／JPEG 描圖**（kang 2026-08-22 提出）
      底圖 ＋ 比例校正那套**空間平面規劃器已經有現成做法**，可以搬。
      🔴 **要配合「內建鋼筆（貝茲繪製）」那條才有意義**（描圖 ＝ 有底圖的畫）
      〔⚠ 這裡原本寫「上一項」，而那一項 2026-08-28 併進「內建鋼筆」了 ——
      　**⛔ 位置指標會斷，一律用名字**〕。
      ⚠ **原本這裡寫「自動描圖（image trace）是另一個等級的工程，先不碰」——
      那句的前提已經不成立**（2026-08-23 調查）：`potrace-js`／`imagetracerjs`
      瀏覽器可用、npm 取得得到、放進 `lib/` 不必連外，而且**輸出就是 SVG 路徑**，
      現成的 `svgPath.js` 直接吃得下，連真轉角判定都繼承。
      詳見 `編輯功能設計方向.md` 第 6.2 節

- [ ] **匯入 DXF 線稿**
      第一輪刻意只做 SVG（曲線不會被 Illustrator 拆成折線）。
      DXF 進來是一堆散線段，串成封閉輪廓那段**剖面分切已經寫好了**
      （`section.js` 的 `chain()`），接得上

- [ ] 實體「抽殼」轉板件　**優先度已大幅下降**
      原本是「做了才能展開實體」的前提，但實體現在直接展開外表面就對了
      （關鍵決定 3 修訂）。抽殼只在「真的要做一個有內壁的空心件」時才需要，
      而且真正麻煩的是「哪幾個面要開口」，那需要點選面的介面（第 6 期）。
- [ ] **按「轉成可編輯網格」時自動存一份**（檔名帶時間）
      `bake()` 是整個程式**唯一不可逆**的動作（一行，把參數整組覆蓋掉），
      而正確流程「尺寸先確定 → 轉成網格 → 標分片／編輯」把它排在很前面。
      極小的工，補的是唯一回不去的地方。
      〔出處：`編輯功能設計方向.md` 第 10 節。**那份文件寫了但這裡沒有，
      等於會被忘記** —— 鐵律六「不要寫一個不存在的退路」的近親〕
- [ ] 展開片的排料與材料利用率（可接上說明表既有的備料邏輯）
- [ ] 展開圖加「摺線方向」的視覺區分（目前只有文字標 ↑上折／↓下折）

---

## 協作規則

### 版本控制與檢查流程
- 本資料夾已納入 Git。**Claude 一律不執行任何 git 指令，連唯讀的 `git status` 也不行。**
  掛載點不支援刪除 `.git` 內的檔案，唯讀指令建立的 `index.lock` 會留下來，
  導致 kang 下次存檔 fatal 失敗（2026-08-20 實際發生過）。
  Claude 只用 Read／grep 讀檔案內容；所有 git 動作由 kang 在 Windows 端執行。
- **操作流程與工具見 `版控工具\README.md`**：
  `1-看差異.bat`（唯讀）→ Claude 讀結果檔確認 → Claude 寫 `commit-msg.txt` → `2-存檔.bat`（純加法）
- 🔴 **commit 訊息一定要寫 `版控工具\commit-msg.txt`，不是根目錄那一份。**
  `2-存檔.bat` 讀的是 `%~dp0commit-msg.txt`（跟 `.bat` 同一個資料夾）。
  **根目錄也有一個同名檔案，而且也在版控裡，但沒有任何程式會讀它。**
  ⚠ 2026-08-24 實際寫錯過一次 —— 差異檔看起來完全正常，
  存下去才會發現 commit 訊息是上一次的。〔版控坑第 9 條〕
- 切分支、退回**不做成一鍵**，由 Claude 當場給指令
  （判準參考 ERP 專案：一鍵只給唯讀與純加法，會改變現況的動作不給捷徑）
- `.bat` 內容必須**純 ASCII + CRLF**（中文會在 Big5 環境亂碼；LF 會讓 `goto` 標籤失效），
  已用 `.gitattributes` 鎖住 `*.bat text eol=crlf`
- **改完檔案後一定要跑 `git diff` 自我檢查**，特別注意有沒有 `-` 掉不該刪的行。
  教訓：2026-08-20 插入 CSS 時誤刪 `@media print{` 開頭，導致列印規則全域生效、
  側欄工具列全被隱藏。這種錯誤**語法檢查抓不到**（大括號仍然平衡、JS 也沒問題），
  只有 diff 看得出來
- CSS 大量改動後，額外檢查：大括號配對、每個 `@media` 是否正確閉合、
  `display:none!important` 這類破壞性規則是否確實被包在該有的 media query 內
- ⚠ **`1-看差異.bat` 的輸出檔偶爾會把個別中文字寫成亂碼**（`�`）。
  那是 Windows console（Big5）寫檔造成的，**原始檔本身沒事**。
  2026-08-22 實際發生：差異檔裡「知」與「弧」變成亂碼，差點被當成寫壞的。
  → 驗法：`iconv -f UTF-8 -t UTF-8 檔名`，過了就是好的。
    看到亂碼先驗原始檔，不要急著「修」一個沒壞的東西。

### 版控與網頁本身踩過的坑（照時間，都是實際發生過的）

> ⚠ **這幾條的編號跟「鐵律」那 34 條是兩套，不要搞混。**
> 這裡是版控、`.bat`、CSS、Service Worker 這一類；
> 本檔別處寫「坑第 N 條」時，指的一律是**建模器那 34 條**（在 `HISTORY.md`）。

1. **沙箱跑 `git status` 也會鎖住 repo** — 唯讀指令一樣建立 `index.lock`，
   而沙箱刪不掉，導致 kang 存檔 fatal 失敗。→ 沙箱一律不執行任何 git 指令
2. **CSS 插入時誤刪 `@media print{` 開頭** — 大括號仍然平衡、JS 也沒問題、
   瀏覽器不報錯，但列印規則變成全域生效、整個介面消失。→ 只有 diff 抓得到
3. **`.bat` 內容含中文會亂碼、LF 換行會讓 `goto` 失效** — 檔名可中文，內容不行；
   已用 `.gitattributes` 鎖 CRLF
4. **轉檔腳本把檔案寫成 0 bytes** — 寫成 `open(p,'wb').write(txt.encode(...))`，
   `wb` 先清空檔案，`encode` 才拋例外。→ 必須「先編碼驗證成功，才開檔寫入」。
   這也是 `1-看差異.bat` 第 [5] 段「各檔絕對行數」存在的理由
5. **flex 項目預設會被壓縮** — 工具列空間不足時按鈕被壓到比文字還窄，
   文字直向折行。→ `flex:none` + `white-space:nowrap`
6. **小物件的縮放把手會蓋住物件本身** — 45×35cm 的「人」在常用比例下只有 14×11px，
   四角把手加起來遠大於它，一拖就變成改尺寸而不是移動。
   → 物件太小時不提供把手，且**命中判斷與繪製必須用同一個函式**，畫面才不會騙人
7. **iPad 全螢幕時內容跑到狀態列底下** — `black-translucent` 造成。
   → 用 `env(safe-area-inset-*)`，桌機這些值是 0 不受影響
8. **`localhost:8080` 被別的專案的 Service Worker 佔住** — 2026-08-21 發生。
   本機開入口頁跑出完全不相干的舊專案畫面，iPad 卻正常。
   原因是 Service Worker 綁在「**網址來源**」（`http://localhost:8080`）上，
   不是綁在資料夾，它會攔截該來源的所有請求並回自己的快取，
   **不管實際上是哪個伺服器在聽這個埠**。iPad 走內網 IP 是不同來源，所以沒事。
   → 清法：網址列左邊 ⓘ →「網站設定」→「刪除資料」→ `Ctrl+Shift+R`。
   → 本專案**刻意沒有 Service Worker**（見「部署設定」），所以不會反過來害到別人。
   → 若日後又常撞到，根本解法是把測試伺服器的埠改掉
     （`版控工具\平板測試-啟動伺服器.bat` 的 `set PORT=`），
     不同的埠就是不同的來源，永遠不會打架。
9. **`commit-msg.txt` 有兩份同名檔案，只有一份會被讀** — 2026-08-24 發生。
   `2-存檔.bat` 讀的是 `%~dp0commit-msg.txt`（＝ `版控工具\` 裡那一份），
   而**根目錄也有一個，也在版控裡，但沒有任何程式會讀它**。
   AI 寫錯了那一份，`1-看差異.bat` 照樣顯示「commit-msg.txt 已修改」，
   **完全看不出寫錯地方** —— 要存下去才會發現訊息是上一次的。
   → 兩份現在內容相同。**要寫就寫 `版控工具\` 那一份**，
     根目錄那份留著不動（刪檔要動 git，而它沒有在害人）。
   → 這是「同一份東西存在兩個地方」的典型代價，跟文件落點規則
     那條「同一條規則只寫一次」是同一個病。

10. **檔尾沒有換行 → 每次 append 都會多出一個假的「刪 1 行」** — 2026-08-27 發生。
    `HISTORY.md` 的最後一個位元組不是 `0a`（是中文字的尾巴），
    於是每次在後面接新內容，**差異檔都會顯示「− 1 行」**，
    而那一行的內容跟新加回去的一模一樣。
    🔴 **危險在於它是假訊號**：這個專案的整個檢查流程建立在
    「逐行看差異、確認沒動到不該動的地方」上，
    **而假的刪除會讓人習慣性忽略刪除欄** —— 那等於把檢查關掉。
    〔當天實際發生：機械斷言算出「舊內容 md5 完全相同」，
    　差異檔卻寫 `-1`，兩邊對不上，查了才知道是換行〕
    → **已補上換行；五個常改的文字檔檔尾現在都是 `0a`**
      （`PROJECT_LOG.md`／`HISTORY.md`／`外部參考-Blender編輯.md`／
      `規格\建模器-點線面編輯.md`／`版控工具\commit-msg.txt`）。
    → **驗法**：`tail -c1 檔名 | xxd -p`，是 `0a` 就對了。
      ⚠ 用整段覆寫的方式產生檔案時特別容易掉，**寫完順手驗一次**。

### 沙箱能做什麼、不能做什麼（影響每次交付的驗證方式）

- **不能開瀏覽器實測** — 沙箱下載不到 Chrome，無法跑 headless 測試。
  所以「畫面有沒有出來」永遠只能靠 kang 實際開啟確認，AI 不能宣稱已驗證。
- **不能驗「別的軟體讀不讀得動」**　⚠ 2026-08-21 實際踩到（坑第 9、11 條）
  匯出的 DXF／SVG 能不能在 Illustrator 開、下載下來的檔名對不對，
  沙箱一律驗不了。**格式合法、語法檢查過、內容正確，都不代表對方打得開。**
  → 凡是交付「要送出去給別的軟體開的東西」（DXF、SVG、PDF、下載檔），
    **交付時就要明講「這部分我驗不了，請你用真正的軟體開一次」**，
    不要讓 kang 以為已經驗過了。
  → **外部驗證挑樣本時，要涵蓋不同的「網格結構」，不只是不同的形狀。**
    2026-08-22 實際踩到（坑第 17 條）：第 3.5 期用錐體做外部驗證並通過，
    但錐體全是三角形面，剛好避開了「四邊形面頂點共用」那個 bug ——
    而板件（平板、折板、管、圓柱側面）全都中招，STL 一直是壞的。
    **只驗了一類等於沒驗。** 現在的分類至少要涵蓋：
    全三角形面／有四邊形面、封閉／開放、在原點／不在原點。
- **能做的驗證**：JS 語法檢查、跨檔匯入匯出對照、HTML 元素 id 對照、
  CSS 大括號與 media query 檢查、外部網路依賴掃描、函式庫 API 逐項確認、
  匯出檔案的**結構**（DXF 區段與表格是否齊全成對、SVG 是否良構）
- **能在 Node 裡跑真正的邏輯測試** — 只要模組不碰 DOM。
  建模器的核心刻意跟 three.js 場景切開，就是為了這件事。
  **凡是能用數學對答案的就一定要對**（尤拉公式、角虧總和、體積面積、展開長度），
  比「看起來沒錯」可靠得多。
  → 🔴 **動過 `main.js` 一定要加跑這一道**（`node --check` 抓不到，
    因為**刪掉整個函式不會有語法錯誤**）：

    ```
    for f in $(grep -o "=> [a-zA-Z]*(" js/main.js | sed 's/=> //;s/(//' | sort -u); do
      grep -q "^function $f" js/main.js || echo "缺 $f"
    done
    ```

    「**被呼叫但不存在的函式**」正是刪錯東西的症狀。
    〔2026-08-25 用「整段替換」改刀具，錨點抓錯**一次吃掉 18 個函式**，
    　而語法檢查說 OK。⛔ 改 `main.js` 一律逐個函式精準編輯。經過見 `HISTORY.md`〕

    🔴🔴 **⚠ 這道檢查會誤報 `isMarkable`，⛔ 那不是改壞了。**
    它只找 `^function f`，而 `isMarkable` 是**從 `seam.js` import 進來的**。
    【實證】跑那道檢查，**唯一的輸出就是「缺 isMarkable」**，
    而它好端端地在 import 清單裡。
    ⚠ **誤報比漏報更糟**（坑第 18 條）—— ⛔ 下一個人動 `main.js` 之後
    看到它，會以為自己吃掉了函式。
    〔⛔ 要改那道檢查得先讓 kang 知道。**這一句是它的權威版**〕

  → 🔴🔴 **動過任何 `js/` 底下的檔案，還要加跑這一道（2026-08-28 補）**：

    ```
    cd 建模器 && node --input-type=module -e "
    import('./js/main.js').then(
      () => console.log('OK'),
      e => { if (e instanceof SyntaxError) { console.log('🔴 解析失敗：' + e.message.split('\n')[0]); process.exit(1); }
             else console.log('OK（解析全過；跑到 ' + e.code + ' 是沙箱沒有 three 的 addons，不是問題）'); }
    );"
    ```

    🔴 **⛔ `node --check` 對 ES 模組是瞎的，這是實測出來的**：

    | 檔案內容 | `node --check` |
    |---|---|
    | 純 script，`const a` 宣告兩次 | ✅ **抓到** |
    | **同樣的東西，但檔案裡有 `import`**（＝ ES 模組）| 🔴 **說沒問題** |

    ⚠ **我們所有的 `js/` 都是 ES 模組**，所以那道檢查對「**重複宣告**」
    這一整類錯誤**完全攔不住**。

    ⭐ **這一道抓得到，因為它是真的去載入**（實測：故意重犯一次，
    當場印出「Identifier 'edgeAny' has already been declared」並回離開碼 1）。
    ⚠ **在沙箱裡它一定會停在 `ERR_PACKAGE_PATH_NOT_EXPORTED`**
    （`node_modules/three` 那個墊片沒有 `addons/`）——
    🔴 **那 ⛔ 不是失敗，那表示解析階段全部過了**。判準只看是不是 `SyntaxError`。
  → 建模器已有現成的測試腳本：`node 建模器/tests/run.mjs`。
    ⛔ **總項數不抄在這裡** —— 跑完自己會印。
    **改過 mesh / region / prim / bool / io / toolbar / unfold / slice / sketch / out
    就跑一次。**
    新功能請順手把「能用數學驗的部分」加進去，不要另外開檔案。
  → 連「圖上標了什麼、有沒有疊在一起」都測得到 ——
    因為出圖拆成「決定畫什麼」（純資料）與「怎麼畫出來」兩層。
    **要讓東西測得到，就把不碰 DOM 的那一半抽出來。**
- **npm 可用**（能抓函式庫原始碼），但一般 CDN 被擋。
  要用外部函式庫時：npm 取得 → 複製進 `lib/` → 連授權與版本說明一起放，
  跟 three.js 與 Manifold 同一套做法。**執行時不可以連外。**

### 一般規則
- 回答用繁體中文、簡潔
- 多檔案修改前先列計畫並提關鍵疑問
- 修錯連續失敗超過 3 次就停下回報

### 🔴 實測結果的回寫（2026-08-26 補。**這一節補的是一個結構缺口，不是提醒**）

> kang：「明明有實測...**為什麼都沒記錄到**」

**病因不是漏寫，是「通過」在原本的流程裡不產生任何動作。**

| kang 的回覆 | 接下來發生什麼 |
|---|---|
| **「這裡怪怪的」** | 要修 → 有改檔 → 有差異 → Claude 寫 commit-msg → **順手就回寫日誌了** |
| **「OK」** | **沒有東西要改** → 沒有改檔、沒有差異、沒有 commit → **整條回寫鏈一次都沒被觸發** |

🔴 **所以「通過」是這套流程裡唯一不產生動作的事件，而它偏偏是最該記的** ——
通過等於一個功能可以結案。

⚠ **實例（2026-08-26 發現）**：那一輪 8 個 commit 裡提到 kang 的只有兩個
（`59c23d3` 凹面、`4f23c4e` 顯示點），**兩個都是他抓到 bug 的**。
他說 OK 的六張清單**一個都沒進日誌**，全部掛著「還沒 kang 實測」——
而他早就一項一項說過 OK。

#### 三條規則

- 🔴 **一、通過也要當場回寫。** 收到「OK」就立刻把該處的 `⏳ 待實測`
  改成 `✅ kang 實測通過（日期）`，**清單本身留著當對照**。
  ⛔ 不要等到下一次有東西要改才順手做 —— 那正是這次失效的原因。

- 🔴 **二、🔴 標記的項目要追問一次**（kang 2026-08-26 選的顆粒度）。
  他說 OK 通常是整批的意思，其餘照記；**但清單裡標 🔴 的那幾項要問一句**。
  ⚠ 理由是那些正是「**錯了也看不出來**」的項目 ——
  〔2026-08-26 實例：凹面清單第 2 項「L 形上不會穿出去的位置**照樣做得到**」。
  　只驗第 1 項的話，一個把整個功能擋掉的誤報（坑第 18 條）會被記成通過〕
  ⛔ 但**不要逐項確認**，八項來回八次他不會想這樣回。

- 🔴 **三、沒有實測結果就不算完成。**
  待辦不可以打 `[x]`、⛔ 也不可以搬進 `HISTORY.md` 結案。
  ⚠ 資料層有一整套數學守著，**介面一項都沒有** ——
  kang 的實測是現階段唯一的現實檢查（見「鐵律 → 一、動手之前」那則）。

#### 🔴 開清單時，名字一律照介面上的字複製（2026-08-26 加）

> **⛔ 不要用程式裡的變數名，也不要用只有寫的人懂的詞。**
> **他驗的是畫面，畫面上沒有那個字，那一項他就做不下去。**

⚠ **這條有五次實例，而且是同一個病**：

| 我寫的 | 他在畫面上看到的 | 結果 |
|---|---|---|
| `toast` | 那條橫條 | 他問「toast 是什麼意思」 |
| 「輕點**微調**」 | —— | **連續兩次**讀成「切好之後移動切線」 |
| `segW` / `segH` | **經線** / **緯線** | **找不到欄位，那一項卡住** |
| 「整條經線亮起來」 | 半圈 | 以為是 bug（**經線本來就是半圈**）|
| 🔴 **「360 個」**（2026-08-27） | 面？邊？ | 他直接反問「**球 360 個甚麼?邊?**」，那一項驗不下去 |

🔴 **第五次翻掉的是「數量」那一半，而 ⛔ 那正是這條規則自己開的處方**：
同一張清單裡我寫了「12 條**邊**」「6 個**面**全亮」，
偏偏第 10 項只寫「360 個」——**漏掉單位，數量就不是數量了。**
⭐ 而正解不只是補一個「面」字，是**補上形狀**：
「**整顆球除了頭頂那一圈與底部那一圈以外全部亮起來**」——
**360 這個數字他根本沒辦法數**，而那句話一眼就驗得出來。

🔴 **kang 的原話（2026-08-26）：「因為我聽不懂你的專業術語。」**

**兩條具體做法**：

1. **欄位名、按鈕名一律去 `index.html` 複製那個字**，⛔ 不用程式裡的名字。
2. 🔴 **講「應該會看到什麼」時要講**數量與形狀**，⛔ 不要只講名字** ——
   「整條經線亮起來」聽起來像一個圈；「**16 條，從最上面連到最下面，半圈**」
   就沒有誤會的空間。
   ⭐ 那也順便讓清單變成**可以對答案的**（他回報數字，我就驗得了）。

⚠ **為什麼放在這一節**：它跟「實測結果的回寫」是同一個結構問題 ——
**清單是每次交付都必定會寫的東西**，所以規則掛在那一步上才擋得住。
〔前三次都只是「提醒」，而**提醒擋不住第四次**〕

#### 🔴 要他做決定之前，先講那個東西有什麼用（2026-08-26 加）

> **⛔ 不要拿一個他不知道用途的東西問他「要 A 還是 B」。**

⚠ **實例**：我拿狀態列的「三角形」問他標籤要怎麼改，
他的回答是「**我不懂這功能的用意是什麼…所以我無法給你回覆**」。

⭐ **那不是他不會答，是我從來沒講過那個數字有什麼用。**
講清楚之後（列印檔會不會太肥／畫面會不會卡，
⚠ 而且它跟**結構分析**與**匯出面板**部分重疊）他當場就決定了。

**要問的形狀是這三件，缺一不可：**

| | |
|---|---|
| **1. 它拿來回答什麼問題** | 用他的語言，⛔ 不是程式的語言 |
| **2. 這件事別的地方看不看得到** | 重疊的話，那個選項的價值就不同了 |
| **3. 每個選項的代價** | ⛔ 沒有代價的選項多半是我沒想清楚 |

⚠ **這條跟「目的藏在下游」不一樣**：那條是**我要去問他**目的；
這條是**我要先告訴他**這東西的用途，他才有辦法回答。
⭐ 兩條的共同點：**問題問得不對，答案一定不對。**

#### 🔴 觸發點在哪（**這一半才是結構保證**）

⛔ **上面三條靠的是紀律，而紀律一定會再破一次**（坑第 31 條）。
所以真正的防線是**把觸發點搬到一件每次必定發生的事上**：

> **開始下一個新功能之前，先 Grep `待實測`，把未結案的一次問完。**
> 〔kang 2026-08-26 選的時機：不是每次開對話都問，也不是交付當下問一次就算〕

⭐ 判準跟 `sel.active` 那次修法一模一樣：**與其在九個寫入點各補一行同步，
不如換一個「讀的時候才算」的定義。** 「你說 OK」不產生動作，
「開新功能前清點」則是每次都會走到的路。

#### ⏳ 這個符號只代表一件事

| 寫法 | 意思 |
|---|---|
| **`⏳ 待實測`** | 🔴 **做完了、但 kang 還沒驗** ← **清點時 Grep 的就是這四個字** |
| `⏳`（單獨用） | 這件事**還沒做** |
| `✘ 已作廢` | 被取代的舊清單，留著當對照 |

⚠ **2026-08-26 之前這三種全部混用 `⏳`**，所以「清點未實測」根本 Grep 不出來 ——
撈到的一大半是「功能還沒做」跟「作廢的舊清單」。⛔ 新增時不要再混。

### 🔴 kang 提問 ≠ 要求插隊（2026-08-25 定調，改變 AI 的回應方式）

> kang 的原話：
> 「我提問的都是希望能夠讓我們的建模器更好運用..其實都很重要...
> 　**而不是提到甚麼就處理甚麼**...**而是我們有方案可以處理**..
> 　這樣我們還是可以依照計畫進行..**這樣計劃的每一步都有方案可以處理..才會順暢**」

**收到一個「這個怎麼辦」的問題時，正確的回應是三步，不是兩步：**

| | 做什麼 |
|---|---|
| 1 | **查到有把握的方案為止**（能對答案就對答案，不要停在「應該可以」）|
| 2 | **寫進文件**，讓它變成計畫上一個「已經有方案」的格子 |
| 3 | **回到原本的順序繼續做** —— ⛔ 不要因為剛查完就改去做它 |

⛔ **不要把提問當成插隊指令。** 也 ⛔ **不要因為「還沒排到」就只給一個推測的答案** ——
那等於在計畫上留一個地雷，走到那一步才發現不成立。

⭐ **這條是「先調查清楚再動手」的放大版**：那一條管的是**單一次編寫**，
這一條管的是**整份計畫**。判準一樣 —— **推理可以先做，但推理之後要去調查它
成不成立**（kang 2026-08-24 原話）。

〔實例：2026-08-25 問「導角的角落有沒有方案」。照這條做 ——
沙箱驗到四個案例全部 χ=2 封閉、體積用兩條獨立的路對過答案、
過程中翻掉三次推論，然後**寫進文件、回頭做原訂的內縮**。
如果只回答「Blender 有 Miter 選項，應該可以照做」，那就是留了一個地雷。〕

### 參考圖資料夾（2026-08-22 起的做法）

有些事情用圖講比用文字快，而且**每次重講都可能講出不一樣的版本**。
所以討論過程中畫出來、kang 確認過的示意圖，存成一個 `XX參考/` 資料夾
放在專案根目錄，並在日誌對應的章節裡指過去。

目前有：`剖面分切參考/`（2 張）

kang 的原話：「這樣就不用一直說明討論」。
→ **要動某一項之前，先看有沒有對應的參考資料夾，有就先開圖。**
  文字寫得再清楚，跟看一眼還是不一樣。
- 這些圖**要進版控**（`.gitignore` 的 `/*.jpg` 只擋根目錄，子資料夾不受影響）

### 專屬文件（2026-08-23 起的做法）

日誌到 2789 行時已經會讓 AI 讀到一半被截斷、或是讀了前半就開始動手。
kang 的判斷：「**不應該直接放進日誌...這樣才不會讓日誌肥大...
造成你調查閱讀時的遺漏**」。

→ **日誌只放「目前確定的狀態」；推導過程、外部調查、被否決的方向
  寫成專屬文件，日誌在對應章節放三句摘要 ＋ 指標。**

這跟上面的參考圖資料夾是同一條原則，只是換成文字。
坑第 33 條講過反面：**教訓寫在別的功能底下就等於沒寫** ——
寫了但沒被讀到，效果一樣。

目前有：

- `尺寸依據的推導.md`（想推翻「尺寸的依據」那條規則之前必讀）
- `編輯功能設計方向.md`（第 6 期動工前必讀）
- `瓣片展開與省道-設計方向.md`（動瓣片展開或省道之前必讀）
- `外部參考調查.md`（**索引**：兩條界線、篩子、記錄格式、三線總結）
- `外部參考-Blender編輯.md`（研究線一，動編輯前讀完整）
- `外部參考-展開與分片.md`（研究線二，動展開或分片前讀完整）
- `外部參考-Pepakura.md`（範圍與介面）

🔴 **那四份裡有一條硬規則：不出現任何一行外部原始碼。**
只寫「它怎麼想、資料怎麼組織、判斷順序是什麼、為什麼那樣選」。
兩個理由都成立：GPL 有傳染性（貼了片段，日後有人照抄進 `js/`，
整個建模器就得以 GPL 釋出）；以及**語法本來就對我們沒用**
（我們是 JS ＋ 半邊網格，Blender 是 C++ ＋ BMesh，抄了也接不上）。
kang 2026-08-23 原話：「我們要調查的是**邏輯**..不是程式語法」。

## 文件分層與落點規則（2026-08-23 起　🔴 這一節決定文件會不會長回去）

> **重點不是「整理」，是「管理」** —— 整理是把現在這批排好，
> 管理是**讓它不可能亂**。這一節就是後者。

### 每次做完一件事，先問「這是現況、規格、還是流水帳？」

| 這種東西 | 家 |
|---|---|
| 一個功能的規格、紅線、決定與理由、否決紀錄 | **`規格\` 一功能一檔**（替換對應段落，不堆疊舊版）|
| 每次開場都要看的：先讀這段、協作規則、**鐵律**、部署、工作流程 | **`PROJECT_LOG.md`**（本檔）|
| 還沒做完的事、進度、狀態 | **本檔「待辦 / 可延伸」**（結案就移出，不堆積）|
| 已結案的實作流水帳、當初怎麼驗出來的、坑的完整經過 | **`HISTORY.md`**（append，只搬不改事實）|
| 🔴 **「這一輪做了什麼」** | **`版控工具\commit-msg.txt`**（每輪必寫）＋ **`HISTORY.md`**。⛔ **不放日誌檔頭。** 見下方那則 |
| 動工前的推導、外部調查、**被否決的方向** | **專屬文件**（如 `編輯功能設計方向.md`）|
| 行號、某個函式現在第幾行 | **不記** —— 會過期，要用時當場查 |
| **測試的總項數** | **不記** —— 跑 `run.mjs` 自己會印。見下方那則 |

### 🔴 檔頭那一欄是「覆蓋型」，⛔ 不是「累加型」（2026-08-27 定調）

> **⛔ 不要在日誌檔頭加「這一輪做了什麼」。**
> 檔頭只有一欄：**「最近一輪翻掉了什麼」，上限 5 條，⛔ 覆蓋不累積。**

**病因是容器的形狀，不是有人偷懶。**
那一欄原本是「每輪加一則」，而**寫它是每輪都會發生的事** ——
所以它必然會長：**2026-08-27 量到 221 行**，而裡面每一則的權威版**都在別處**
（鐵律／文件分層／待辦／規格檔），**它是第四份**
〔前三份：`commit-msg.txt`、`HISTORY.md`、規則各自的家〕。

⛔ **加一條「記得搬走」的規則擋不住** —— 這個專案已經證明四次
（「講話不要用術語」那條前三次都只是提醒，而提醒擋不住第四次）。
⭐ **所以換掉容器**：從「append 一則」變成「覆蓋一個固定欄位」。

🔴 **為什麼留下來的偏偏是「翻掉了什麼」**：

| 這一類 | 下一個人從哪知道 |
|---|---|
| 立了什麼規則 | 讀「鐵律」 |
| 做完什麼功能 | 讀「待辦」與 `規格\` |
| **「你以為的 X 其實是錯的」** | 🔴 **⛔ 開場不講，他不會知道要去查** ← **只有這一類非留不可** |

⚠ **這個做法不是純結構保證，要老實講**：真正結構性的只有一件事 ——
**那個欄位的形狀寫著「覆蓋」**，所以讓它長大得刻意違反格式，
比「順手再加一則」難得多。⛔ 但它擋不住存心要加的人。

### 🔴 ⛔ 會過期的數字一律現算，不存（2026-08-27 定調）

> **測試的總項數，文件裡一處都不寫。**

**病因**：那個數字曾經**同時存在六個地方**（日誌四處、`規格\建模器-核心架構.md`
兩處、`規格\建模器-測試與驗證.md` 一處），而它**每加一輪功能就變一次**。
結果是 2026-08-27 查出來：規格檔停在 **1231**，實際 **1973**，**過期 742 項沒人發現**。

⛔ **收成一處不夠** —— 那一處照樣會過期，這次就是。
⭐ **正解是一處都不留**：`node 建模器/tests/run.mjs` 跑完本來就印
「通過 N　失敗 0」，**答案一直都在，我們只是另外抄了一份**。

**這是這個專案自己示範過三次的同一招**：
尺寸「⛔ 現算，不存」／`active` 改成當場算出來的（**沒有第二份資料就沒有東西
會不同步**）／圓弧擬合每次展開時現算（**沒有東西要存、也沒有東西會過期**）。
〔坑第 31 條：與其讓兩條路對齊，不如換一個只有一條路的定義 ——
　**這裡連一條都不留。**〕

⚠ **⛔ 不適用於「基準數字」**：13800、108000、124/64、12.5664 那些**不會變**
（會變就是改壞了），它們照樣留在 `規格\建模器-測試與驗證.md`。
**判準：這個數字改對了會變，還是改壞了才會變？** 前者不存，後者一定要存。

> ⚠ **「現況的數字」與「歷史的數字」長得一樣，但不可以一起改。**
>
> 🔴 **總項數已經不再有「現況」身分了**（見上一則），
> 所以日誌裡剩下的項數**全部是流水帳**：
> 「環切那一輪測試 1231 → **1281 項**」，**永遠不動**。
>
> 🔴 **所以不要用整批搜尋取代去更新項數** —— 那會把歷史一起改掉，
> 而且**改完看起來完全正常**（數字仍然是個數字），只有差異檔看得出來。
> 〔2026-08-25 連續犯兩次：第一次改壞 1231→1231，第二次改壞
> 　1231→1338 與 1281→1338，兩次都是 `1-看差異.bat` 的逐行差異照出來的〕
>
> 🔴 **2026-08-26 起這條鏈跨三個檔**（日誌／`HISTORY.md`／`規格\`）——
> 搬進 `HISTORY.md` 時**項數是跟著原文逐字搬走的**，而各功能「驗過的數字」
> 本來就寫在規格檔裡。所以要驗就得**三個一起 grep**：
>
> ```
> grep -roh "1[0-9]\{3\} → \*\*1[0-9]\{3\}" PROJECT_LOG.md HISTORY.md 規格/ | sort -u
> ```
>
> ⚠ **單看日誌會以為 1537 之後斷掉了 —— 那不是斷，是搬走了。**
>
> ⚠ **而且這條鏈本來就有四個缺口，不是 8/26 搬壞的**（搬之前就查過）：
> **1309→1338、1367→1385、1537→1548、1548→1574** 四段從來沒有被寫成「A → B」。
> ⛔ **看到缺口不要直接當成改壞了** —— 先跟 `8c344f1`（搬移前最後一版）對一次。
>
> **驗法**：那些「A → B」應該串成一條不斷的鏈
> （1219→1231→1281→1291→1309→1338→1367→1385→1415→1471→1537→1548→1574→1606→1640→1681→1695→1720→1752→1779→1806→1815→1842→1890→1932→1954→1973）。斷掉就是被改壞了。
>
> 🔴 **2026-08-26 起這條鏈多了一種事件：項數會「減少」。**
> 那一輪刪掉 `planeFromTwoRays()`（沒有已知用途的殘骸），
> **連它的 14 項測試一起刪** —— 所以實際走的是 **1842 → 1828 → 1890**，
> 而日誌上只記端點 `1842 → 1890`。
> ⚠ **看到「新增 48 項」跟實際改動對不起來，先想想是不是有東西被刪了**，
> ⛔ 不要以為是漏記。刪的理由一律寫在待辦那一條裡。
>
> ⚠ **2026-08-25 又犯一次，而且是新花樣**：那一輪的 1548 只有「現況」身分，
> 所以我判斷可以整批取代 —— 但**我先手動把標題改成「…→1548→1574」，
> 那一步自己製造出一個「歷史」身分**，接著整批取代就把它一起吃掉了
> （標題變成「1537→1574→1574」、鏈少一節）。
> 🔴 **順序本身會製造衝突：先寫新鏈、再整批取代，等於自己打自己。**
> ⛔ 所以規則收嚴：**項數一律逐處改，不要整批取代，即使當下看起來安全。**

**該功能還沒有規格檔就開一個新的**，照現有那十檔的骨架寫。
**⛔ 不要因為「反正日誌裡也寫得下」就寫回本檔。**

> 🔴 **這條沒守住，一年後就會長回 2806 行。** ERP 專案實際發生過：
> 他們瘦身完又長回去，**不是沒整理，是落點規則沒跟著結構一起改**，
> 下一個人照舊把新規格寫回主檔。

### 🔴 什麼時候該整理（2026-08-26 加。**判準的權威版在別的專案**）

> **權威版**：`D:\彩藝通系統整合\整理方法.md`
> 　　第一部分是**通用的道理**（十條），第二部分是**他們的做法**，⚠ **明文寫著不要照抄**。
> ⚠ 那個資料夾**不一定掛得到**，所以底下抄了「要不要動手」那一關 ——
> **這是刻意容許的一份重複**（理由同下一節），**對不上以那份為準**。

🔴 **⛔ 不要看檔案大小就決定整理。看這六個徵兆，中三個以上才動手。**
**只中一兩個 → 順手修那一處就好，⛔ 不要整份重來。**

1. **瘦身完很快又長回去**（＝ 沒有落點規則，只是搬垃圾）
2. **同一件事在兩個地方都有內容**
3. **查一件事要翻三個檔以上才湊得出答案**
4. **地圖跟實際資料夾對不上**
5. **待辦裡混著已經撤案的東西**（所以永遠看起來做不完）
6. **文件裡的數字明顯過期，卻沒人發現**

**動手前先分型，治法完全不同：**

| | 病根 | 治法 |
|---|---|---|
| **甲型** | **儲存的鍵 ≠ 查詢的鍵**（按日期排、卻按對象查） | **重新分類** |
| **乙型** | **一份檔案身兼數職**（又是流水帳又是規格又是現況） | ⛔ **不重新分類**，只把不同角色的內容抽出去 |

⭐ **對大檔的標準處方是「給它目次」，不是「拆掉」** ——
「冷檔變大不是缺點（本來就不從頭讀），**但一定要順手給它目次**」。

〔2026-08-26 實例：我提議把 `規格\建模器-點線面編輯.md`（2352 行 / 128KB）
拆成六個檔，**理由純粹是大小** —— 照這一節量過之後**收回了**：
地圖對得上、0 斷鏈、58 個識別碼全搜得到、撤案項都標清楚，**六個徵兆只中 0～1 個**；
而且它按功能排（不是甲型）、流水帳只佔 10%（不太是乙型）。
→ **改成補目次**（32 節，Grep 節名跳過去），檔案不動。
🔴 **真要拆的訊號**：下次**因為讀不完而查錯、或發現該知道的事不在任何一節裡** ——
那時候照**現成的節界線一功能一檔**（`D:\彩藝通ERP系統\規格\` 那 28 檔的做法），
⛔ 不要另發明一套分類。「不排時程，看訊號。」〕

### 開新規格檔的骨架

```
# 這是什麼            ← 標題只寫「這是什麼」，⛔ 不寫狀態（✅完成／待施工）
> 這份回答：…         ← 狀態會變、規格不會；狀態的家永遠是「待辦」那一節
> 這份不回答：… → 去哪
> 識別碼（給 Grep 用）：檔名／函式名／欄位名／關鍵詞
---
（內容）
---
## 接下來去讀程式的哪一支    ← 檔名是路線，行號是座標、會漂
## 原文出處
```

### 兩條鐵律

- 🔴 **同一條規則只寫一次，別的檔只寫「這條在那邊」，不抄第二份。**
  重複的那份不只會漂 —— 它會讓下一個人**很有信心地答錯**：
  他看到了、也讀了，只是讀到的是舊的那一份，而且**不會有任何徵兆提示他該再查一次**。
  文件**無法**決定「會不會去查」，但**可以**決定「**查的時候看到的是不是對的那一份**」。
- 🔴 **搬移一律逐字搬 ＋ 機械斷言**（行數／位元組守恆、md5 比對），
  **不做「順手精簡改寫」**。手寫重組會靜默掉東西 ——
  ERP 那次重組掉了一整節，沒有任何東西報錯。

  🔴 **這一條對「搬程式」一樣成立**（2026-08-25 補）。
  ⚠ 原本它只寫在「搬文件」底下，而那天我是在改 `main.js`：
  用「找兩個錨點、整段替換」的方式重寫刀具，**錨點抓錯，
  一次吃掉中間 18 個函式**。
  🔴 **而 `node --check` 說語法 OK** —— 刪掉整個函式沒有語法錯誤。
  ⭐ **正解是逐個函式精準編輯**，加上一道
  「被呼叫但不存在的函式」檢查（見「先讀這段」那則）。
  ⚠ **全新檔案不會出現在 `1-看差異.bat` 的逐行差異裡**（git 沒有舊版可比，只標 `??`），
  那部分只能自己逐檔讀回來對。

### 已知的一份重複，以及為什麼容許它

**第二份（2026-08-26 加）**：「什麼時候該整理」那六個徵兆與甲／乙分型，
權威版在 `D:\彩藝通系統整合\整理方法.md`，本檔上一節抄了一份。
**容許的條件**：那個資料夾不一定掛得到，而**判斷要當場做得到才有用**；
已在該處明文寫「對不上以那份為準」。⛔ 要改判準就去改那一份，不要改這裡。

**第一份**：
34 條坑的「規則」在本檔「鐵律」一節，「經過」在 `HISTORY.md`，兩邊的規則句子重疊。
**容許的條件是 `HISTORY.md` 檔頭已明文降級**：「本檔不是規則的家，
兩邊對不上時以 `PROJECT_LOG.md` 為準」。**要改規則就改鐵律那一節，不要改 HISTORY。**

---

## 進度紀錄 → 已搬到 `HISTORY.md`

> 逐次的實作流水帳（哪一個 commit 做了什麼、測試從幾項變成幾項、
> kang 實測抓到什麼）全部在 **`HISTORY.md`**，一字未改。
> 那是冷檔，用 Grep 找節名跳過去，**不要從頭讀**。
