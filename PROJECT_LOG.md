# 專案日誌：網頁空間設計程式

最後更新：2026-08-22　V2.6

> **這份日誌的主要讀者是 AI，不是人。**
> 因此一律寫「目前確定的狀態」，過時的內容直接覆蓋或明確標記為已失效，
> 不留需要猜測的空間。kang 只做抽查，不需要靠它回想細節。

---

## 開新對話時先讀這段

- **現在有三個工具**：空間平面規劃器、組裝系統結構說明表、建模器（見下方各子專案）
- **這套程式的用意**：建好的模型要能**做出東西** —— 一條路走 3D 列印（STL），
  一條路走展開後 2D 切割（DXF）。**兩條並列，沒有主次**。細節見「目標與範圍」

- ⚠ **這不是鈑金工具。使用的材料是紙材、發泡板、珍珠板、木板、壓克力，
  一種金屬都不用。**（kang 2026-08-22 說明）

  這條放這麼前面，是因為忽略它的代價很大：日誌通篇是鈑金語言
  （K 因子、中性層、折彎補償、最小折邊），材料表六種有三種是金屬，
  很容易照著文件推論下去，然後把整套工具當成鈑金工具做。
  **實際發生過，而且因此把正確的弦長改成錯的弧長，角柱一度長了 11%。**

  kang 的原話：「**像摺紙一樣把展開圖組合起來**」。摺紙沒有板厚。
  細節見「板材與金屬的差別」。

- **目前主力**：建模器。第 3.5 期完成後又補了三輪（見「第 5 期(A)(B)(C)」），
  **兩條生產路徑都通了**：建模 → STL → 3D 列印；建模 → 展開 → DXF → CNC
- **下一步還沒定** —— 八期路線圖需要重排，理由見「路線圖現況」
- **絕對不要執行任何 git 指令**，連 `git status` 也不行（理由見「協作規則」）
- **建模器不能雙擊開啟**，它用 ES 模組與 WebAssembly，必須走伺服器
- **改過建模器核心就跑 `node 建模器/tests/run.mjs`**，593 項數學驗證，對不上就是改壞了
- **要送出去給別的軟體開的東西（DXF、SVG、下載檔），沙箱驗不了** ——
  一定要 kang 用真正的軟體開過才算數。見「踩過的坑」第 9、11 條
- 子專案編號 2（鋁擠型框架設計器）**已移出、不在版控內**，文件只是留存參考

---

## 🌐 線上網址

```
https://kangman1977-cloud.github.io/space-design/
```

任何裝置的瀏覽器都能開。平板建議按分享 →「加入主畫面」，會變全螢幕。

---

## 目前有三個工具

| # | 工具 | 檔案 | 單位 | 開啟方式 | 狀態 |
|---|---|---|---|---|---|
| 1 | 空間平面規劃器 | `空間平面規劃器.html` | cm | 雙擊可開 | 完成，上線中 |
| 2 | 組裝系統結構說明表 | `組裝系統結構說明表.html` | cm | 雙擊可開 | 完成，上線中 |
| 3 | 建模器 | `建模器/index.html` | cm | **必須走伺服器** | 第 3.5 期完成，STL 與 DXF 兩條路都通 |

**互通關係**

- 說明表 →（`assembly-system` JSON）→ 平面規劃器　**已完成**
- 建模器 →（`model-mesh` JSON）→ 平面規劃器　**規劃中，排在建模器第 8 期**

> **已移出的子專案**：`鋁擠型框架設計器.html`（40 系列型材框架，單位 mm）
> 已於 2026-08-20 移出資料夾，**不在版控內、資料夾裡也沒有這個檔案**。
> 下方「子專案 2」整節僅供日後參考，不要當成現存的工具。

---

## 日常工作流程

| 步驟 | 誰做 |
|---|---|
| 1. 改檔案 | Claude |
| 2. 雙擊 `版控工具\1-看差異.bat` | kang |
| 3. 讀結果檔確認沒動到不該動的地方，並寫 `commit-msg.txt` | Claude |
| 4. 雙擊 `版控工具\2-存檔.bat` | kang |
| 5. 雙擊 `版控工具\3-上傳網路.bat` | kang |

推送後 GitHub Pages 約 1～2 分鐘重建，之後所有裝置重新整理即為最新版。

**先看再存。** 步驟 2 是唯讀的，誤按最多覆蓋一個文字檔。

---

## 部署設定（一次性，已完成）

- GitHub 帳號：`kangman1977-cloud`
- Repository：`space-design`（**Public**，免費帳號的 Pages 需要公開）
- Pages 設定：Settings → Pages → Deploy from a branch → `main` / `/(root)`
- 遠端：`https://github.com/kangman1977-cloud/space-design.git`
- `.nojekyll`：停用 Jekyll 處理，避免中文檔名被吃掉
- `index.html`：入口頁，**三個工具都在上面**（建模器於 `0428b14` 補上）。
  因為檔名是中文，網址會被編碼成一長串百分號，平板上打不出來，
  所以用入口頁讓使用者用點的。建模器更是只能從這裡點進去 ——
  它由多個 ES 模組組成，雙擊檔案開不起來
- 三個工具各有一份 `manifest-*.webmanifest` 與 `icons/*`，都能「加到主畫面」

**所有路徑都是相對路徑**，沒有任何以 `/` 開頭的絕對路徑，
因此放在 `/space-design/` 子目錄底下也能正常運作。改動時務必維持這一點。

**建模器不能雙擊開啟（重要）**
建模器拆成多個 ES 模組，瀏覽器的 CORS 政策會擋掉 `file://` 底下的模組載入。
因此只有兩種開法：

| 場合 | 網址 |
|---|---|
| 本機／平板測試（先跑 `版控工具\平板測試-啟動伺服器.bat`） | `http://<內網IP>:8080/建模器/` |
| 線上 | `https://kangman1977-cloud.github.io/space-design/建模器/` |

前兩個工具是單一 HTML 檔，不受影響，照樣可以雙擊開。

**離線與 PWA**：只做了 manifest（加到主畫面、全螢幕），**沒有做 Service Worker**。
原因是實際情境「平板就算沒 SIM 也會接熱點」，離線快取價值不高，
卻會帶來版本陳舊的風險。日後若需要離線，採「網路優先、失敗才用快取」策略。

---

# 子專案 1：空間平面規劃器

## 專案目的
網頁式的空間配置工具，讓非設計軟體使用者能在瀏覽器上自由擺放物件、
用真實尺寸預覽廠房／辦公室配置，並可切換 3D 檢視感受空間感。

## 確定狀態
- **主檔案**：`空間平面規劃器.html`（單一檔案，雙擊即可用瀏覽器開啟）
- **技術**：原生 JS + Canvas 2D；3D 使用 three.js r128（CDN 載入，需網路）
- **單位**：全部以 **cm** 為內部單位
- **座標**：2D 的 (x, y) → 3D 的 (x, z)；物件 `z` 欄位＝高度

## 已完成功能

### 2D 平面
- 物件庫點擊新增、自訂尺寸新增
- 拖曳移動、四角把手縮放、旋轉把手（Shift 鎖 15°）
- 網格吸附（5 / 10 / 25 / 50 / 100 cm 可切換）
- 框選多選、靠左／靠上對齊、水平／垂直均分
- 屬性面板可精確輸入 X / Y / 寬 / 深 / 高 / 角度 / 顏色 / 類型
- 量測工具（量走道寬度）、底圖比例校正工具
- Undo / Redo、JSON 存讀檔、PNG 匯出、localStorage 自動暫存

### 3D 預覽
- 環繞檢視（左鍵轉、右鍵平移、滾輪遠近）
- 第一人稱漫遊（點畫面鎖定滑鼠，WASD 移動、Shift 快走、Q/E 升降）
- 牆體碰撞（房間只擋牆、可走進室內；名稱以「門」開頭的物件不擋路；可勾「穿牆」關閉）
- 底圖可貼在地板上、外牆、名稱標籤、陰影，皆可開關

## 分層造型（tiers）
物件可由多層方塊堆疊，做出「上寬下窄」等造型（如工作桌桌面外伸、機台底座／機身／機頭）。

```
o.tiers = [ {name, w, h, z0, z1, dx, dy, color}, ... ]   // 沒有此欄位＝單一方塊
```
- `z0/z1`＝該層離地的下緣／上緣；`dx/dy`＝相對物件中心的水平偏移
- `o.w / o.h / o.z` 永遠是所有層的**外框**（由 `fitTiers()` 自動重算並重新置中）
- 改主面板的寬／深／高 ＝ 等比縮放所有層（`setObjSize()`），只在 change 時觸發避免逐字累積誤差
- 內建範本：`desk`(工作桌) / `machine`(機台) / `cabinet`(櫃體) / `platform`(平台)
- 只有 `kind==='solid'` 支援分層；改成房間或區域會清掉 tiers

## 從「組裝系統結構說明表」匯入（kind: system）
說明表按「⇪ 匯出給平面圖」產生交換 JSON，平面規劃器按「⇩ 匯入組裝系統」讀入。
**是加入現有平面圖，不會清空既有物件**；匯入後自動選取新物件，可直接拖曳定位。

交換格式：`{type:'assembly-system', v:1, unit:'cm', head, systems:[...]}`
每個 system 帶 `w/h/d`、`ps`、`postXs`（立柱中心線 X）、`bays`、
`levels:{front,back,depth}`（橫料各層中心高度）、`seats`、`expr`、`bom`。

物件對應：`w`＝系統寬、`h`＝系統深、`z`＝系統高，結構存在 `o.sys`。
`qty` 為 N 時會產生 N 個獨立物件（名稱加 `#1 #2`），依序往後錯開 60cm。

- **2D**：矩形＋各支立柱的俯視腳位（白色小方塊）
- **3D**：依 `postXs` 與 `levels` 建出真實骨架（立柱／前後橫料／深度料）。
  已驗算 272×200×34 的系統會產生 22 支桿件，與說明表備料明細完全一致
- **尺寸鎖定**：`hitHandle()` 對 system 不回傳縮放把手，避免拉伸後圖面與實際用料不符。
  要改尺寸得回說明表重新排料再匯出

## 物件類型（kind）
| 類型 | 值 | 3D 呈現 |
|---|---|---|
| 實體 | `solid` | 一整塊量體（桌、機台、柱、牆） |
| 房間 | `room` | 四面牆＋地板，中空可走進去 |
| 區域 | `zone` | 貼地薄片，用於標示走道／成品區 |
| 人形 | `person` | 比例參考人偶。2D＝肩橢圓＋頭＋面向三角形；3D＝腿/身/手臂/頭組成的人形。寬＝肩寬、深＝身體厚度、高＝身高（預設 45×35×170）。不參與漫遊碰撞、不支援分層 |

## 資料格式（存檔 JSON）
```
{ v:2, nextId, objects:[{id,name,x,y,w,h,z,rot,kind,color}],
  roomW, roomH, roomZ, bg:{src,x,y,w,h}, bgAlpha, view }
```
- `x,y` 為物件**中心**座標；`rot` 為度數
- `bg.src` 是 base64 DataURL，所以存檔會把底圖一起帶著

---

# 子專案 2：鋁擠型框架設計器　⚠ 已移出，僅供參考

> **這個工具已經不存在於資料夾中，也不在版控裡。**
> 2026-08-20 移出。以下內容保留是因為配件推算規則與驗證過的切料數字有參考價值，
> 建模器做鈑金／型材件時可能用得到。**不要把它當成可以開啟或修改的檔案。**

**原主檔案**：`鋁擠型框架設計器.html`（獨立網頁，單位 **mm**）

## 用途
用 40 系列鋁擠型搭工作桌架／料架／防護框，3D 即時預覽，自動產生切割料表與配件清單。

## 座標與資料模型
X＝長、Z＝深、Y＝高，原點在框架**底部中心**。

```
member = { id, prof:'4040', p1:[x,y,z], p2:[x,y,z], joint:'corner'|'butt', name }
```
桿件用起點／終點表示，長度自動算，因此斜撐也能做。

## 兩個獨立選項（重要，別混淆）
| 選項 | 影響 | 說明 |
|---|---|---|
| **框架接法** `post` / `beam` | **切料長度** | `post` 立柱通長＝H，橫梁扣兩端立柱寬；`beam` 橫梁通長＝L，立柱扣上下梁高 |
| **接合方式** `corner` / `butt` | **只影響配件** | 角件外掛 vs 端面內接。兩者桿件切料長度相同 |

驗證過的數字（1800×750×800、4040）：
- 柱通長 → 立柱 800×4、長梁 1720×4、短梁 670×4
- 梁通長 → 立柱 720×4、長梁 1800×4、短梁 670×4
- 換成 4080（斷面寬 80）→ 長梁 1640、短梁 590

## 型材資料（`PROF`）
4040 / 4040輕型 / 4080 / 40120 / 8080，各含斷面尺寸與 kg/m。
**kg/m 為市售概略值**，要精確請依供應商規格改 `PROF` 這個物件。

## 配件推算規則
桿件端點若落在另一支桿件的斷面範圍內（半寬＋3mm）即算一個**接點**，其餘為**外露端**。
- 角件外掛：每接點 N 個角件（預設 2，可調），每角件配 2 支 M8 螺栓 + 2 個 T 型螺帽
- 端面內接：每接點依斷面模組數配內接組（4040＝1、4080＝2、8080＝4）
- 外露端各 1 個封蓋；位於最低處的外露端另計調整腳杯

## 功能
- 4 個範本：箱型框架 / 工作桌架 / 多層料架（層數可設）/ 防護圍籬
- 3D：真 T 溝斷面（ExtrudeGeometry，可關成方管加速）、等角/前/側/俯視、陰影、外框尺寸標註
- 點選 3D 桿件即選取，右側可改型材、接合、起訖點、長度；可複製／刪除／加單支桿件
- 切割料表（含可調鋸切裕度）＋ 配件清單，可匯出 CSV
- JSON 存讀檔、PNG 截圖、localStorage 自動暫存（key: `alu_frame`、`alu_panels`）

## 待辦 / 可延伸
- [ ] 桌面板／壓克力面板（含開孔）
- [ ] 斜撐快速加入
- [ ] 單價設定與報價單輸出
- [ ] 標準長度套裁最佳化（6m 原料如何切最省）
- [x] 匯出到「空間平面規劃器」當成物件（已完成，見子專案 1 的「從組裝系統結構說明表匯入」）

---

# 子專案 3：組裝系統結構說明表

**主檔案**：`組裝系統結構說明表.html`（獨立網頁，單位 **cm**）

## 用途
取代目前用 Illustrator 手工畫的「組裝系統結構說明表」。輸入需求尺寸 → 自動排料 →
產生展開計算式、正面／側面圖、備料明細，可列印或存 PNG。

## 標準料（公司實際規格，cm）
`26 / 34 / 40 / 46 / 60 / 94 / 150 / 200 / 292 / 300`
- 預設 **292 與 300 只當立柱、不當橫料**（由實際案例回推的慣例），可用勾選調整
- 立柱斷面預設 4cm

## 資料結構（v2：一個案件多張說明表）
```
DOC = {
  head: { date, no, name },        // 全案共用表頭
  sheets: [ sheet, ... ],          // 多張說明表
  cur: 0                           // 目前編輯第幾張
}
sheet = { name, qty, req:{w,h,d}, ps, lv:{f,b,d},
          bays:[{len,pieces}], postH, postHPieces, depth:{len,pieces}, pickSig }
```
- `qty` ＝這張說明表要做**幾套**（同一組配置重複製作）。圖形不變，備料自動乘上去。
  `computeBOM()` 每個品項同時回傳 `per`（單套）與 `qty`（＝per×套數），
  `cut`／`seats`／`bars` 皆已含套數。已驗算：一張 3 套 ＝ 三張各 1 套。
- `S` 是模組層變數，永遠指向 `DOC.sheets[DOC.cur]`
- 左側欄位是「編輯介面」：切換說明表時 `syncToDOM()`，欄位改動時 `syncFromDOM()`
- `withSheet(i, fn)` 可暫時把 `S` 切到第 i 張執行 fn 再切回（批次匯出／合併備料用）
- 標準料勾選與排料條件（容許差、每組、最多座數、單跨接料）是**全案共用**
- 讀檔相容舊版單張格式（`v` 不等於 2 時自動包成一張）

## 核心邏輯
```
系統寬 = 4 + 跨距1 + 4 + 跨距2 + 4 + … （n 支立柱、n-1 個跨距）
系統深 = 4 + 深度跨距 + 4
系統高 = 立柱長
```
- 括號外＝客戶需求尺寸，括號內＝標準料湊出的實際組裝尺寸
- 跨距可由 1～3 支標準料接起來，寫成 `(150+26)`
- 「前3後2」＝正面 3 層橫料、背面 2 層 → **每個跨距 5 支橫料**
- 深度料：每座前後連接，層數可設（預設 2）

## 演算法
- **跨距排列**：分層 DP，`dp[k][w]` ＝ 恰好 k 個跨距、佔用 ≤ w 的最大佔用量；
  另有可達性表 `reach[k][w]` 用來列出每個座數的前 N 名方案
- **自動選用偏好**：差距 ≤ 容許差（預設 30cm）之中，取「**接料最少 → 座數最少 → 差距最小**」
- **立柱／深度選料**：單支優先；單支差距超過容許差才考慮接料

## 驗算結果（對照公司實際兩張表）
| 案例 | 需求寬 | 公司原表 | 程式自動選用 |
|---|---|---|---|
| ABA-0107 | 290 | `4+60+4+200+4=272` | 相同 ✓ |
| ABA-0125 | 400 | `4+200+4+(150+26)+4=388` | `4+34+4+150+4+200+4=400`（差 0、單支料） |

高 220 → 立柱 200 ✓　深 36 → 34 ✓（皆與原表一致）

ABA-0125 程式給的方案客觀上更好（差 0 且不需接料），公司原排法可能有現場或庫存因素。
把「每組顯示」調大即可在 3 座那組看到 388 的排法。**若之後確認有其他排料偏好規則，改
`autoPick()` 的排序條件即可。**

## 功能
- **一個案件多張說明表**：上方分頁列切換，可新增／複製／改名／前後移動／刪除；
  表上右上角標示「名稱（第幾張/總數）」
- **每張可設套數**：分頁標籤顯示 `×3`，表上右上角標「×3 套」，
  備料明細框加註單套用量，右側面板以 `(單套×套數)` 呈現，CSV 有「單套／套數／總數量」三欄
- **備料可切換「這張」與「全案合計」**
- **批次輸出**：列印全部（A4 橫向、一張一頁）、PNG 一次匯出全部、CSV 含分張明細＋全案合計
- **可指定存放位置**：工具列「指定存放位置」勾選後，單檔匯出會跳「另存新檔」；
  「全部 PNG」則先選一次資料夾再整批寫入。用 File System Access API
  （`showSaveFilePicker` / `showDirectoryPicker`），偵測不到或被擋時自動退回一般下載並提示一次。
  相關函式：`saveBlob()` / `downloadBlob()` / `dataURLtoBlob()`（同步轉檔，避免 await 吃掉使用者手勢）
- 表頭欄位（進場日期／客戶單號／案件名稱）為全案共用
- 排料方案依座數分組，每組列出前 N 名，點選即套用
- 逐跨距手動改（下拉選標準料或接料組合）、可增刪跨距
- 說明表 Canvas 輸出（虛線格、正面圖、側面圖、展開式、備料明細框），PNG／列印／PDF
- 備料明細 + 實際切料統計，可匯出 CSV
- 3D 立體預覽（等角／前／側／俯視）
- JSON 存讀檔、localStorage 自動暫存（key: `sys_sheet`）

## 已知細節
- **立柱斷面寬**（`pS`，預設 4）＝展開式裡的「4」，每支立柱佔掉的寬度
- 說明表的紅線用**固定線寬 9px** 繪製，但側視圖比例被放大約 3 倍，
  若橫線畫到外緣（0～總深）會超出立柱線。因此橫線一律畫到**頭尾立柱的中心線**為止，
  端點由接點圓標蓋住。正面圖同樣處理。

## 平板支援（進行中，分支 `touch`）
**設計原則：事件入口分開，動作邏輯共用。** 桌機的 `mouse*` 路徑保持原行為，
觸控是另外新增的入口，兩者最後呼叫同一組動作函式，所以計算結果保證一致，
未來要改邏輯也只改一處。

- 3D 視角：共用 `orbitBegin / orbitMove / orbitEnd / orbitZoom / orbitPanBy`
  - 滑鼠：左鍵轉、右鍵平移、滾輪縮放（與原本完全相同）
  - 觸控：單指轉、雙指捏合縮放＋雙指拖曳平移
- 側欄分隔線：共用 `grabStart / grabMove / grabEnd`，滑鼠與觸控各接一組入口
- 說明表預覽：新增 `SV{z,x,y}` 縮放狀態，雙指捏合以手指中心為基準縮放
  （已驗算按住的點不會位移），單指拖曳平移，右下角有 −／⤢／＋ 按鈕
- CSS 隔離：
  - `@media (pointer:coarse)` 加大點擊目標 → 只對手指生效，滑鼠桌機不受影響
  - `@media (max-width:1100px)` 側欄改抽屜（含遮罩、點擊關閉）→ **1100px 以上完全維持原版面**
  - `touch-action` 只設在 3D 畫布、分隔線、說明表預覽，其餘保留瀏覽器預設
  - `overscroll-behavior:none` 擋掉下拉重新整理搶走拖曳

**iPad 實測（2026-08-20）**：單指旋轉、雙指縮放平移、說明表放大讀字、
抽屜側欄開關、iOS 鍵盤不擋輸入框 — 全部正常。

### 空間平面規劃器的觸控（同一套原則）
- 2D 畫布共用 `canvasBegin / canvasMove / canvasEnd / canvasZoomAt / canvasPanBy`
  - `canvasBegin(sx,sy,opt)` 的 opt：`pan`（強制平移）／`shift`（加選）／`marquee`（強制框選）
  - 滑鼠：中鍵右鍵空白鍵平移、左鍵拖曳、Shift 加選、滾輪縮放（與原本相同）
  - 觸控：**單指在物件／把手上＝移動縮放旋轉；單指在空白處＝平移畫布；
    空白處按住約 0.45 秒＝轉為框選（會跳提示）；雙指＝捏合縮放＋平移**
- 3D 共用 `orb3Begin / orb3Move / orb3Zoom / orb3PanBy`
- **第一人稱漫遊在觸控裝置隱藏**（`#mWalk` 於 `pointer:coarse` 設 display:none），
  因為需要滑鼠鎖定，iPad 不支援
- 把手尺寸依裝置調整：`HS` 桌機 7px／觸控 12px，命中寬容值 `HGRAB` 2px／14px

**尚未完成**：three.js 內嵌（離線）、PWA。

## 待辦 / 可延伸
- [ ] 確認並加入其他排料偏好規則（例如背板分割、單跨最大容許跨距）
- [ ] 表單版面微調到與現行 AI 檔完全一致（字級、位置）
- [ ] 多系統（一個案子多組）合併備料
- [ ] 庫存扣帳／可用料查詢

---

# 子專案 4：建模器

**主檔案**：`建模器/index.html`（多檔案 ES 模組 ＋ WebAssembly，單位 **cm**）
**開啟方式**：**不能雙擊**，必須走伺服器
（本機 `http://localhost:8080/建模器/`、平板 `http://<內網IP>:8080/建模器/`）
**目前進度**：第 3.5 期**已完成**（2026-08-21，`4530697`）。全案共 8 期。
**已經可以投入生產**：治具樣品印 3D、箱體護罩料槽出圖送雷切。
下一步是第 4 期：草圖擠出。

> **本機請用 `localhost` 開，不要用內網 IP。**
> 「指定存放位置」用的 File System Access API 只在**安全環境**可用，
> https 與 localhost 算，`http://192.168.x.x` 不算。
> 用 IP 開會自動退回一般下載（勾選框會淡化並說明原因）。
> 內網 IP 那條路是給 iPad 用的。

## 目標與範圍（重要，避免走偏）

### 做這套的用意（kang 2026-08-21 說明，優先於底下的一切規格）

**建好的模型要能實際做出東西，兩條路並列，沒有主次：**

| 路徑 | 用途 | 需要的出口 |
|---|---|---|
| **建模 → 3D 列印** | 治具、樣品、小零件 | **STL**（切片軟體吃這個） |
| **建模 → 展開 → 2D 切割** | 紙材、發泡板、珍珠板、木板、壓克力 | **DXF**（雷切／水刀／2D CNC） |

「CNC」在這個專案裡指的是**雷切／水刀／2D 切割**，不是 3 軸銑削。
所以**不需要 STEP** —— 那是 B-rep 曲面格式，不是網格，
自己寫等於做半個 CAD 核心，不在範圍內。

### 範圍

專攻**建模**與**展開**，**不做 Blender 那種全能軟體**。
判準：Blender 做不好、但公司天天要用的事才做；純多邊形雕塑不是重點。

要能涵蓋三類對象：機構件／治具（精確件）、設備量體、自由造型。
輸出要有：**STL（3D 列印／CAM）**、可列印展開圖（含尺寸標註）、DXF（2D 切割）。

**展開要對應的材料**（2026-08-22 由 kang 說明後改寫）：

| 材料 | 折得起來？ | 捲得起來？ |
|---|---|---|
| 紙材 | 壓線／壓痕 | **可以** |
| 發泡板、珍珠板、薄木板 | 銑 45 度 V 溝（留薄皮） | 不行 |
| 木板、壓克力 | 不行，切開再接 | 不行 |
| （帆布軟膜） | 可以 | 可以 |
| （鐵板、不鏽鋼、鋁板） | 可以 | 可以 |

括號裡的是**目前用不到的**，規則保留著以防日後外發或混用。

> **這一段被改寫過兩次，兩次都是同一個教訓。**
>
> 2026-08-21：原本只寫了展開那一條路，八期路線圖裡完全沒有 3D 列印的
> 出口 —— 照原規劃走完八期，東西還是印不出來。（坑第 13 條）
>
> 2026-08-22：原本寫「四種材料：**鈑金**、帆布軟膜、壓克力木板、紙模打樣」，
> 把鈑金排第一。實際上一種金屬都不用。**AI 從第一句就假設這是鈑金工具，
> 照著文件推了整整一場，直到 kang 主動說明材料才發現。**（坑第 14 條）
>
> 兩次的共同點：**規格寫得再詳細，也不等於目的。**
> 而「目的」包含用什麼材料、給誰做、做出來怎麼組。

## 八期路線圖

| 期 | 內容 | 狀態 |
|---|---|---|
| 0 | 技術驗證：3D 點選、三軸 gizmo、布林、觸控手感 | **完成**（放 `XXX/`，不進版控） |
| 1 | 核心：半邊網格、邊角色、平面區域合併、Undo、存讀檔、基本體 | **完成** `2b030f0` |
| 2 | 參數體、布林運算、陣列鏡射 | **完成** `8d2de61` |
| 3 | 展開 v1：平面與折彎件 → 列印圖 ＋ DXF | **完成** `77f386b` |
| 3.5 | **STL 匯出 ＋ 列印前檢查**（3D 列印那條路的出口） | **完成** `4530697` |
| 5(A) | 算不準的展開要講出來（止血） | **完成** `766a3fd` |
| 5(B) | 展開改走外皮弦長 ＋ 生成樹先走最平的邊 | **完成** `7c4fa25` |
| 5(C) | 實體可直接展開 ＋ STL 頂點共用修正 | **完成** `91a7f03` |
| 4 | **草圖擠出** | 未開始 |
| 5 | 展開 v2：圓錐的極座標展開 | **只剩紙材需要**，見下 |
| 6 | 自由造型：點／邊／面編輯 | 未開始（kang 確認用得到） |
| 7 | 展開 v3：自由曲面切分近似 ＋ 失真標示 | 未開始 |
| 8 | 互通：匯出 `model-mesh`，平面規劃器加 `kind:'mesh'` | 未開始 |

第 3.5 期是對焦之後插進來的；5(A)(B)(C) 三輪則是**原路線圖上完全沒有的**，
起因是實測與 kang 說明材料之後才發現的問題。

**第 8 期是唯一會動到 `空間平面規劃器.html` 的一期**，其餘都不碰現有檔案。

## 路線圖現況（2026-08-22）—— 需要重排，還沒排

八期路線圖是在「這是鈑金工具」的假設下訂的。材料一釐清，有兩項變了：

**一、原訂第 5 期（圓錐的極座標展開）從「必要」變成「只對紙模有用」。**

板材捲不起來，圓錐一定是切成平板再接，所以**弦長就是正確答案**，
不需要修正。只有紙材捲得起來才需要真弧長。
目前的行為：捲得起來的材料展開錐面會跳「請勿據以下料」警告
（因為修正做不到，數字偏短）；捲不起來的材料不跳，因為數字是對的。

**二、路線圖裡沒有 kang 真正最需要的那件事：指定分片。**

kang 的原話：「立體造型我們必須要做規劃，**如何分配展開的區域**，
能夠依序讓 CNC 切割後再組裝結合。」

舉例：一個立方體可以六面展成一片（銑 45 度 V 溝折起來），
也可以拆成三片（上下各一片、側邊四面一片），也可以六片全分開。
**選哪一種是製造決定，不是計算結果** —— 少一片就少一道對位誤差，
但要多一道銑溝工序。

程式目前只決定「哪些邊可以折」，剩下的由攤平生成樹的走訪順序決定 ——
**接縫位置是走訪順序的副產品，不是任何人的判斷。**
底層其實已經支援了（`EDGE_ROLE.CUT`，而 `edgeIsCut()` 第一件事就是
尊重使用者標記的切割線），缺的只是讓 kang 標的介面。

這件事跟第 6 期（點／邊／面編輯）共用同一套「點選邊」的介面，
所以合理的做法可能是把它跟第 6 期合併，或把第 6 期提前。

**下一輪開始前要先決定這個**，不要照舊的路線圖往下推。

## STL 匯出（第 3.5 期，`js/out/stl.js`）

**匯出時自動處理四件事，每一件不做都會出錯：**

| | 為什麼 |
|---|---|
| cm → mm 換算 | STL **沒有單位欄位**，切片軟體一律當 mm 讀 |
| **Y 軸向上 → Z 軸向上** | 建模器沿用 three.js 的 Y 向上，但列印平台是 XY 平面。不轉的話模型會**躺著**進切片軟體。轉換 `(x,y,z) → (x,−z,y)`，行列式 ＋1，右手系不變，所以繞向與法向朝外自動保持正確 |
| 整組落到平台 Z=0 | 多物件合成一個檔時整組一起落下，相對位置不變 |
| 板件自動加厚 | 板件在資料層是中性面（開放曲面、零厚度），直接匯出切出來是空的 |

單位可切換（跟 DXF 一致），但選 cm 時面板常駐警告 —— 那會讓模型小十倍。

**列印前檢查才是這個功能真正的價值。** 匯出 STL 本身三十行就寫得完。
3D 列印最常見的失敗是「模型不封閉」，切片軟體通常只會說「模型有問題」，
或乾脆默默印出空殼，等發現已經浪費幾小時料與機時。

這個建模器答得出來，而且答得出**是哪裡**的問題（有幾條邊界邊）——
因為第 1 期就是半邊結構、布林走 Manifold、第 1 期就在算尤拉數。
**這是前兩期打的地基第一次直接變成使用者看得到的價值。**

檢查項目：封閉性、法向朝外（體積正負）、獨立塊數、退化三角形、
外框是否超過常見成型範圍、模型是否小到像單位選錯。
分 bad／warn 兩級，**warn 只提醒不擋** —— 程式沒資格替人決定做不做得出來。

## 五個關鍵決定（第 3、5 條在 2026-08-22 修訂，其餘不變）

**1. 資料結構用半邊（half-edge），不用面清單**
展開引擎最常問「這條邊隔壁是哪個面」。面清單每問一次要翻遍全部的面，
半邊是 O(1)。第 6 期的面編輯也非它不可。

**2. 存參數，不存三角形**
方塊存 `{type:'box',w:60,h:45,d:40}`，不是 12 個三角形的座標。
檔案小、看得懂、能回頭改尺寸重新生成。
只有被布林或面編輯改過、回不去參數的物件才存網格（`bake()` 之後）。
這對應 Blender 的 `.blend` 與 `.obj` 的差別，主要走前者。

**3. 物件分「實體 solid」與「板件 sheet」**
　　⚠ **「只有 sheet 能展開」已於 2026-08-22 推翻（`91a7f03`）**

兩者的精確意思：

| | 網格代表什麼 | 板厚的作用 |
|---|---|---|
| **實體 solid** | 封閉外殼，**就是物件的外表面** | 對幾何完全沒有作用 |
| **板件 sheet** | 開放曲面、零厚度，代表**板厚的正中間（中性面）** | 顯示與 STL 加厚時才用到 |

原本規定只有 sheet 能展開，理由是「要展開的東西一定有板厚，
而板厚會改變展開尺寸（折彎時中性層才是不變的）」。**那是鈑金的道理。**

但這套工具是切開再接合、接縫銑 45 度斜接，**板厚由斜面自動吸收**，
每一片就是切到外緣尺寸 —— 而實體的網格本來就是外表面。
直接展開它就是正確答案。

證據：同一個 60×45×40 的方塊，標成實體與標成板件，展開結果**完全一樣**
（1 片、面積 13800 cm²、展開長 145、展開寬 210）。
擋著的時候使用者只能手動改標籤繞過去，而繞過去拿到的是同一組數字 ——
**證明擋的只是標籤，不是幾何。**

連帶：「要不要加厚」的判斷也從 `kind` 這個標籤改成**網格開不開放**
（`mesh.isClosed()`）。理由見坑第 8 條 ——
kind 是使用者隨手可改的標籤，封閉與否才是幾何事實。

**板件現在只剩一種用途**：單純一片平的東西（背板、擋板）。
那種情況中性面跟外皮沒差別，不會出問題。

**4. 布林運算用 Manifold，而且只有 `bool.js` 一個檔案碰得到它**
（2026-08-21 定案，**推翻了原本「換成 BVH 加速版」的規劃**）

原訂要用 `three-bvh-csg`。實測同一題（方塊 60×45×40 挖 ⌀20 貫孔）：

| | three-bvh-csg | Manifold |
|---|---|---|
| 耗時 | 40 ms | 0 ms |
| 三角形 | 376 | 144 |
| 尤拉數（應為 0） | −113 ✗ | 0 ✓ |
| 網格封閉 | 否 | 是 |
| 結構問題 | 244 項 | 0 項 |

兩邊體積都對、畫面也一樣，差在結構：`three-bvh-csg` 把原三角形切開再拼回去，
**切完之後鄰居關係斷了 506 處**。而展開引擎問的第一個問題就是
「這條邊隔壁是哪個面」，斷掉的地方答不出來，第 3 期會直接失效。
那是它的設計取捨，不是等人修就會好的 bug。

Manifold（`elalish/manifold`，Apache-2.0）的設計目標就是保證輸出封閉可接。
**它不是 Google 的產品** —— 作者 Emmett Lalish 寫它時任職 Google（20% 專案），
現已離職至 Wētā FX，程式碼在他個人帳號下。使用者含 OpenSCAD、Blender、
Godot Engine、BRL-CAD、Babylon.js、trimesh。

**風險控管**：它只被 `js/build/bool.js` import，其他程式碼一律透過我們自己的
介面呼叫。要換掉它就改那一個檔，運算樹格式、面板、Undo、存讀檔都不用動。
這是「規則分開、幾何共用」原則套用在外部相依上的版本。

**5. 折彎存「內側圓角 R ＋ K 因子」，不存中性層半徑**
（2026-08-21 第 3 期定案）
　　⚠ **這條只對「捲得起來的材料」成立**（2026-08-22 補上適用條件）

　　整套 K 因子／中性層是**金屬折彎**的模型：材料被拉伸，所以要算中性層。
　　板材不是這樣做的 —— 它是銑 45 度 V 溝再折，**外皮長度不變**，
　　沒有拉伸也就沒有中性層。差別見下一節「板材與金屬的差別」。
　　珍珠板那筆材料的 K 值留 0.5 只是因為欄位要有值，實際用不到。

折彎時外側被拉長、內側被壓縮，中間有一層長度不變，叫**中性層**，
下料長度必須照它算。中性層位置由板厚與 K 因子決定：

```
中性層半徑 rn = 內側R + K × 板厚
一道折彎的展開長 BA = θ × rn
```

**為什麼存內側 R 而不是中性層**：中性層是算出來的，現場量不到。
師傅講「折 R3」、圖面標的、模具標的，全部都是內側圓角。

**差多少**（3mm 鐵板、內 R3、折 90 度、四道）：
照內側 R 算 18.8mm，照中性層算 26.4mm，**差 7.6mm**，折完裝不上去。

K 是**材料屬性**（軟鋼 0.40、不鏽鋼 0.45、鋁 0.35、帆布與紙 0.50），
所以預設值放在 `unfold/rules.js` 的材料表，面板可覆寫。
幾何那邊只負責照公式算 —— 又是同一條「規則分開、幾何共用」。

## 板材與金屬的差別（2026-08-22 新增，這一節最容易被忽略）

kang 的原話：「跟鋁與鐵**只是接合方式不同**而已」、
「像**摺紙**一樣把展開圖組合起來」。

兩者都能讓材料保持一整片，但**展開長度的算法完全不同**：

| | 金屬折彎 | 板材（銑 45 度 V 溝） |
|---|---|---|
| 長度基準 | **中性層**（板厚中間某一層） | **外皮**（外側表面） |
| 公式 | `BA = θ × (ri + K×t)` | 各面外緣尺寸直接相加 |
| 板厚的作用 | 決定中性層位置 | 決定 V 溝寬度（90 度時 ＝ 2t） |
| 進不進展開圖 | **進** | **不進** |

外皮在折的時候長度不變，所以 100mm 的立方體、10mm 板，
四面側板攤開就是**剛好 400mm**，在 100／200／300 的位置各銑一道
20mm 寬的 V 溝（尖端落在外皮上）。

**板厚不進展開圖**，因為 45 度斜接的本質就是每一片都切到外緣尺寸，
斜面自動把板厚吃掉。板厚在這個專案只剩三個用途，全部在展開圖之後：
畫面上看起來像真的、STL 匯出加厚、V 溝要銑多寬（而最後那個是現場的事）。

### 邊的處理方式（kang 說明，程式**不管**這一層）

| 處理 | 結果 |
|---|---|
| 45 度**切到底** | 兩片完全分開，斜面對接 |
| V 溝**不切到底**（留薄皮） | 保持一片，折得起來 |
| 壓痕／壓線（紙材） | 保持一片 |
| 黏貼邊 | 某片邊上額外加料 |

kang 明確劃了界線：「**只要做到準確地展開，邊的處理方式是 CNC
與實際操作的範圍**」。所以程式不做 V 溝深度、留幾 mm 薄皮、
壓痕工具、黏貼邊寬度這些 —— 那些是現場決定的。

### 捲得起來 vs 捲不起來（`canRoll`）

網格上的一段「圓弧」是一圈平面小面。這些面到底代表
「真的被捲圓的曲面」還是「一圈平板拼出來的多邊形」，
**幾何本身分不出來**（坑第 10 條）。分得出來的是材料：

- 紙、帆布 → 捲得起來，小面是離散化的產物，真長度是**弧長**
- 板材 → 捲不起來，小面**就是實際要切的平板**，長度是**弦長**

所以 `rules.js` 的材料規則多回答第五個問題 `canRoll()`，
幾何核心只多 6 行（`arcCorrection()` 裡決定要不要 `applyStretch()`）。

拉伸是 `flatten.js` 裡**唯一會破壞精確性的一步** ——
攤平本身是剛體運動，尺寸完全不變。所以對板材而言，**不拉伸就是精確**。

## 檔案結構

```
建模器/
  index.html              介面骨架、importmap、樣式
  js/core/mesh.js         半邊網格結構（703 行，整個專案的地基）
  js/core/region.js       平面區域合併與曲面分類
  js/core/history.js      Undo / Redo（快照式）
  js/core/io.js           文件模型 Doc / ModelObject、存讀檔格式 v3
  js/build/prim.js        九種帶參數的基本體（見下方「參數體一覽」）
  js/build/bool.js        布林運算。**唯一接觸 Manifold 的檔案**
  js/build/array.js       陣列與鏡射（不直接碰 Manifold，走 bool.js 開出的介面）
  js/unfold/rules.js      材料規則。六種材料，各自回答四個問題
  js/unfold/flatten.js    展開幾何核心。**完全不知道材料是什麼**
  js/unfold/part.js       ModelObject 與展開引擎之間的轉接層、備料 CSV
  js/out/sheet.js         展開圖。一份繪圖程式，Canvas 與 SVG 共用
  js/out/dxf.js           DXF R12 輸出
  js/out/save.js          存檔（做法沿用組裝系統結構說明表那一套）
  js/out/stl.js           STL 匯出（3D 列印／CAM）＋ 列印前檢查
  js/view/scene.js        three.js 場景（只負責把文件畫出來，不改文件）
  js/view/select.js       選取與 gizmo
  js/ui/toolbar.js        右側面板、結構分析、布林運算樹、陣列、折彎序列
  js/ui/unfoldPanel.js    展開視窗
  js/ui/exportPanel.js    3D 匯出視窗
  js/main.js              接線
  lib/three/              three.js r185（ESM）＋ Orbit / Transform 控制器
  lib/manifold/           Manifold 3.5.1（WASM）＋ LICENSE ＋ 來源說明.txt
  tests/run.mjs           回歸測試，593 項。跑法見下方「回歸測試」
```

`js/unfold/` 與 `js/out/` 是第 3 期新增的兩層：
**前者算「攤平後長什麼樣」，後者管「怎麼把它畫出去」**，兩者互不知道對方。
所以之後換掉展開演算法不必動出圖，加一種輸出格式也不必動幾何。

`建模器/node_modules/` 是跑測試時自動產生的墊片，已在 `.gitignore` 內，
不是專案的一部分。

## 參數體一覽（九種）

| 類型 | 標籤 | 種類 | 備註 |
|---|---|---|---|
| `box` | 方塊 | 實體 | |
| `cylinder` | 圓柱 | 實體 | `openEnded` 可只留側面（側面才是可展的） |
| `cone` | 錐體 | 實體 | 上下半徑可不同，做錐形接頭 |
| `sphere` | 球 | 實體 | 不可展，展開一定要切分近似 |
| `prism` | 角柱 | 實體 | 分段數少的圓柱。六角、八角在鈑金常見 |
| `tube` | 管 | 實體 | 中空。**χ＝0**（中間通了）。自己接半邊結構，不經三角形 |
| `roundBox` | 圓角方塊 | 實體 | 只倒四條垂直邊。**圓角是可展的圓柱面** |
| `plate` | 平板 | **板件** | 開放的單面，沒有厚度 |
| `bend` | 折板 | **板件** | 折彎序列，見下 |

**板件（sheet）是開放的面，不是實體。** 厚度是 `ModelObject.thickness`，
畫面上的厚度由 `mesh.shell()` 在**顯示時**加上去，文件裡不存。
`isSheetPrim(type)` 決定新增時的 kind。

### 折板的折彎序列

```
{ type:'bend', w, first, arcSeg, k,
  bends: [ {angle, ri, len}, ... ] }    // 轉幾度、內側圓角、轉完再走多長
```

一道＝L 型；兩道同向＝U 型；兩道一正一負＝Z 型。想折幾道就加幾道。
**寫死 L/U 只能做兩種，序列涵蓋全部，而且展開讀的就是它。**

`ri` 是**內側圓角**（師傅與圖面講的那個 R），`k` 是 K 因子。
中性層半徑 `rn = ri + k × 板厚` 由 `neutralRadius()` 算，
展開長由 `bendAllowance()` / `bendDevelopedLength()` 算 ——
**這三個函式是展開長度的唯一權威來源**，見「關鍵決定」第 5 條。

板厚不存在 src 裡（同一份參數換個板厚就是另一件事，不該有兩份
各自為政的板厚），而是由 `buildSrc(src, t)` 從 ModelObject 傳進來。
遞迴時用閉包帶下去，所以 `bool.js` 與 `array.js` 完全不必知道有這回事。

折彎圓弧被切成 `arcSeg` 段，段與段不共面，`autoMarkFolds()` 會自動
標成折線 —— 這是半邊結構的「邊角色」欄位第一次真正派上用場。
**`arcSeg` 下限是 2**，理由見「踩過的坑」第 10 條。

**資料流是單向的**：`操作 → 改 Doc → hist.commit() → view.sync(doc) → 畫面更新`
畫面永遠是「文件的投影」，自己不存狀態。
所以 Undo 只要把文件換回舊快照，畫面自然跟著回去，不必為每個功能寫反向操作。

核心邏輯完全不依賴 three.js 以外的東西，也不碰 DOM，
**因此可以在 Node 環境下直接跑測試**（第 1 期的驗證就是這樣做的）。

## 半邊結構速查

```js
he.v      這條半邊的起點
he.face   所屬的面；null ＝ 邊界半邊
he.next   同一面上的下一條（繞一圈回到自己）
he.twin   反向的那一條；一條「邊」＝ 兩條半邊
he.role   'free' | 'fold'（折線）| 'cut'（切割線）
```

- 走訪不重複的邊用 `mesh.edges()`（只回傳 id 較小的那條半邊）
- **設定邊的角色一定要用 `mesh.setRole()`**，它會同時設兩條半邊。
  只設單邊的話，`edges()` 可能剛好讀到另一條，看起來就像沒設（實際踩過）
- `mesh.dihedral(he)` 回傳轉折角：0 ＝ 共面、正 ＝ 凸（山折）、負 ＝ 凹（谷折）
- 開放網格（板件）的邊界會自動補上 `face === null` 的邊界半邊並串成迴圈，
  且自動標記為 `cut`

## 展開架構：規則分開，幾何共用

**第 3 期已照這個分層實作完成**（`unfold/rules.js` ＋ `unfold/flatten.js`），
沿用專案既有原則「入口分開、動作邏輯共用」。

```
┌─ 材料規則（各自獨立，互不相干）────────────┐
│  鈑金規則   帆布規則   板材規則   紙模規則  │
│  哪裡能折？補償多少？餘量？檢查什麼？        │
└────────────────┬───────────────────────────┘
                 │ 只把「規則」傳下去
┌────────────────▼───────────────────────────┐
│  幾何核心（完全不知道材料是什麼）            │
│  區域合併 → 曲面分類 → 攤平 → 重疊偵測 → 分片│
└────────────────────────────────────────────┘
```

**判準**：這段邏輯需不需要知道材料是什麼？需要就獨立，不需要就共用。

| 需要知道材料 → 獨立 | 不需要 → 共用 |
|---|---|
| 哪條邊能折、哪條必須切 | 把相鄰的面攤平 |
| 折彎補償、縫份、黏合片 | 偵測攤平後有沒有重疊 |
| 檢查規則（最小折邊等） | 重疊了要怎麼切分 |
| 輸出圖面樣式 | |

每個規則模組只需回答**五個**問題（2026-08-22 從四個增為五個）：
`canFold(角度)` / `canRoll` / `allowance(角度, 內側R)` / `margin()` / `validate(片)`
**新增材料 ＝ 在 `MATERIALS` 多加一筆，幾何核心一行都不用動。**

已實作的七種。**預設是珍珠板**（2026-08-22 從 `steel` 改過來）：

| 材料 | 折？ | 捲？ | K | 特性 |
|---|---|---|---|---|
| **珍珠板／發泡板／薄木板** | ✔ | ✘ | — | **預設**。銑 45 度 V 溝可折，展開走外皮弦長 |
| 紙模打樣 | ✔ | ✔ | 0.50 | 黏合片 1.0cm |
| 壓克力／木板 | ✘ | ✘ | — | 所有折線強制轉切割線，各片分開下料 |
| 帆布／軟膜 | ✔ | ✔ | 0.50 | 縫份 1.5cm |
| 鐵板／鍍鋅鋼板 | ✔ | ✔ | 0.40 | 目前用不到，規則保留 |
| 不鏽鋼 | ✔ | ✔ | 0.45 | 同上 |
| 鋁板 | ✔ | ✔ | 0.35 | 同上。內R 小於板厚會裂（會警告） |

**珍珠板那一筆填的是前六種都表達不出來的格子：折得起來，但捲不起來。**
金屬與帆布兩者皆可，壓克力／木板兩者皆否。少了這一筆，
就沒有東西能描述「銑 45 度 V 溝折起來的珍珠板」——
而做角柱會被當成滾圓，展開長往多錯 4～11%。
測試有一條專門盯著它（「可折不可捲」目前只有這一種）。

`canFold` 只問尖角折。**圓弧折彎不問** —— 它是滾出來的，
總角度 360 度的圓筒照樣做得出來，拿「折不折得起來」去問它會誤報。

**尚未實作、但規劃時就想清楚的兩件差異**（第 5 期以後再補）：
帆布可以「少切幾刀」（軟料能硬拗，不必每個轉折都切開）；
紙模的黏合片目前只在標題欄提醒「圖上未含，下料時另加」，
還沒有真的畫出黏合片的形狀。

**最難的是「重疊偵測與自動分片」**，佔整個引擎約 70% 的工作量，而且是純幾何。
第 3 期只做到**偵測與告知**，自動分片排在第 7 期。
有成熟啟發式解法，但不保證每次都給出最漂亮的分片，
所以一定要做成「自動分片 ＋ 手動調整切線」，不強迫接受電腦的判斷。

### 圓弧修正：這一期真正的重點

攤平是剛體運動，對可展曲面是精確解。但圓弧在網格上被切成直線段，
一段一段攤開只會得到**弦長總和**，永遠比真正的弧長短（坑第 6 條）。

所以攤平之後一定要做圓弧修正：辨識「哪幾條折線其實同屬一段圓弧」
（**等寬 ＋ 等角**的連續段），沿垂直於折線的方向做一維拉伸，
把那一段拉長到 `r × Θ`。折線本身與平行於折線的尺寸都不動 ——
這正是「捲起來的紙攤開」在做的事。

**驗證方式是交叉對答案**：`flatten.js` 從網格辨識、
`bendDevelopedLength()` 從參數套公式，兩條完全獨立的路，
`arcSeg` 2～24 全部一致到 1e-14。跟第 2 期「管 vs 布林」同一招。

## 為什麼一定要切分（數學根據）

高斯絕妙定理：曲面展開成平面時高斯曲率不變。所以：

| 類型 | 例子 | 能否無失真展開 |
|---|---|---|
| 可展曲面 | 平面、圓柱、圓錐、稜柱、稜錐 | 能，有精確解 |
| 不可展曲面 | 球面、環面、自由曲面 | **數學上不可能** |

不可展的只有三條路：切分成近似可展的小片、允許材料變形、加剪口。

程式用**角虧**（angle defect ＝ 360° 減去頂點周圍所有夾角）判斷：
≈0 攤得平、>0 像球面外鼓、<0 像馬鞍。這是離散版高斯曲率。
**封閉曲面的角虧總和恆等於 4π ≈ 12.5664**（高斯–博內定理），
接錯一條邊就湊不出這個數字，所以它同時是最強的拓撲驗證工具。

## 交換格式規劃（第 8 期用）

主要走**自訂 JSON**（長期穩定的關鍵是格式在自己手上）。

> **修正（2026-08-21）**：原本這裡寫「glTF 當附加的單向出口
> （給外部軟體、3D 列印看）」—— **glTF 不是 3D 列印的格式**，
> 切片軟體吃的是 STL 或 3MF。3D 列印那條路已經由第 3.5 期的
> STL 匯出解決。glTF 若要做，用途是「給別人看 3D 模型」，不是列印。

```
{ type:'model-mesh', v:1, unit:'cm', head:{...},
  models:[ { name, qty, w,h,z, mesh:{pos,idx,col}, src:{...} } ] }
```

**三個約定必須對齊平面規劃器，否則匯入會錯位**：
單位 cm（不是 mm）／原點在物件中心、`z` 從地面起算／建模器 (x,y) → 平面規劃器 2D 的 (x,y)。

**效能是真限制**：iPad 上 r128 同時畫多個模型，面數必須控制。
匯出時要提供精細度選項並顯示面數，超過門檻就警告。

## 回歸測試（改動核心模組後一定要跑）

```
node 建模器/tests/run.mjs
```

**593 項全部是用數學對答案的**，不是「看起來沒錯」。對不上就是改壞了。
第一次跑會自動建立 `建模器/node_modules/three` 墊片，讓 Node 也能解析
`import 'three'`（瀏覽器是靠 importmap）。

沙箱下載不到瀏覽器，所以「畫面有沒有出來」永遠只能靠 kang 實際開啟確認。
但**能用數學驗的部分就一定要驗** —— 建模器的核心刻意跟 three.js 場景與 DOM
切開，就是為了這件事。第 3 期的出圖也照這條做：`drawProgram()` 只產生
一串純資料的繪圖指令，Canvas 與 SVG 兩個算繪端各自照著畫，
**所以「圖上標了什麼、有沒有疊在一起」在 Node 裡測得到**。

### 已驗證的基準數字

| 項目 | 結果 |
|---|---|
| 尤拉數 V−E+F | 方塊／圓柱／球皆為 2，開放平板為 1 |
| 方塊 60×45×40 | V8 E18 F12，合併成 **6 片**（各片外輪廓 4 點），體積 108,000 cm³／面積 13,800 cm² |
| 圓柱 ⌀50×70（32邊） | V66 E192 F128，合併成 **34 片** |
| 角虧總和 | 封閉曲面精確等於 **2πχ**；χ=2 時就是 4π = 12.5664 |
| 可展性判定 | 圓柱**側面**、圓錐、錐形接頭 → 可展開；半球、環面 → 不可展開 |
| 貫孔（60×45×40 挖 ⌀20） | V72 E216 F144、**χ=0**、38 片、角虧總和 0、體積 95,514.219 cm³ |
| 盲孔 | χ=2（沒挖穿就還是 2），體積正確 |
| 聯集 | 分開兩塊 χ=4；重疊兩塊體積只算一次 |
| 巢狀布林 | 先挖孔再聯集，體積與 χ 皆正確 |
| 存讀檔往返 | 體積相同、深拷貝有效、v1 與 v2 舊檔都讀得動 |
| 管 ⌀50/⌀40×70 | 體積精確、**χ＝0**（中間通了）、66 片；內半徑 0 會退化成實心 |
| 圓角方塊 60×45×40 R6 | 體積精確、22 片；圓角半徑 0 會退化成普通方塊（6 片） |
| 折板（40＋R2 的 90°＋30） | 開放面 χ＝1、折線 5 條、切割線 14 條 |
| 折板 展開長度 | **73.14 cm（真弧長）≠ 73.12 cm（網格弦長）**，見踩過的坑第 6 條 |
| 板件加厚 | 體積**精確等於面積×厚度**；折板 2/3/8 段 × 45/90/135 度全數成立 |
| 陣列 | 線性／網格／環形／鏡射的體積、元件數、折線數皆正確 |
| 板件陣列 | 直接拼接不聯集；5 片折板 ＝ 25 條折線 |
| 散熱板 8×4 | 貫穿孔數 32、χ＝−62（＝2−2×32）、體積誤差 0.1 cm³ 內 |
| **交叉驗證** | 參數體「管」vs 布林「大圓柱減小圓柱」算出同一個體積（相對誤差 7.5e−8） |
| 效能 | 16,128 面的球：曲率 7ms、區域合併 25ms、連通元件 6ms；單次布林 2.7ms；128 個孔的散熱板 187ms |

第 3 期新增的基準：

| 項目 | 結果 |
|---|---|
| **展開長度 交叉驗證** | 網格辨識 ＝ 參數公式，`arcSeg` 2/3/4/6/8/12/24 全部差 **1e−14** |
| 修正真的有在動 | 修正後的長度必須明顯大於弦長總和（否則測不出有沒有修正） |
| 中性層 | R2＋K0.4×t0.3 ＝ **2.12**；板厚 0 時等於內側R；尖角折（R0）不補 |
| BA 90° R3 t0.3 K0.4 | **4.9009 cm**；若誤用內側R 直接算會短少 0.18 cm 以上 |
| L／U／Z／四道混合 | 展開長、板寬、折彎道數全對；辨識回推的內側R 與角度都等於輸入值 |
| 折彎區寬度 | 展開圖上每一道折彎佔的寬度 ＝ 那一道的 BA |
| 圓筒側面 8～128 段 | 展開長 ＝ **2πr ＝ 157.0796**，誤差 1 微米內（自動剪開接縫，兩端仍算在弧內） |
| 平板 100×60 | 精確。曾拿三角化的對角線當基準而歪成 116.6×102.9 |
| 尖角折 | 展開長 ＝ 直線相加，折彎區寬度 0 |
| 六種材料 K | 鋁 < 鐵 < 不鏽鋼 < 帆布；壓克力把折板拆成多片並說明原因 |
| **v3→v4 舊檔** | 網格面積與展開長**完全不變**；轉換會鑽進布林樹與陣列裡 |
| 展開圖分段尺寸 | 各段相加 ＝ 總長（師傅照這些數字畫線） |
| 標註不重疊 | 四種件自動偵測，全部 **0 處**（含 21 道折彎的圓角方塊） |
| DXF 結構 | 四區段順序、三張表順序（LTYPE 必須在 LAYER 前）、成對閉合、全 CRLF、全 ASCII |
| SVG 相容 | XML 宣告、UTF-8、不用 `dominant-baseline`、線寬 ≤ 0.6mm |
| 檔名 | Windows 不合法字元、空字串、長度上限、中文保留 |
| 效能 | 128 段圓柱展開 2ms |

**注意「封閉圓柱」與「圓柱側面」不一樣**：封閉圓柱整體是**不可展**的
（上下緣 64 個頂點是折角、有角虧），可展的是側面。展開時本來就會把
上下蓋當成另外的片。這一點寫測試時搞錯過一次。

## 這個子專案踩過的坑

1. **半邊的 role 只設單邊** — 邊界切割線標記後顯示 0 條，
   因為 `edges()` 讀到的是另一條半邊。**一律用 `setRole()`**
2. **gizmo 初始化順序** — `setSnap()` 在 `this.tc` 指派前被呼叫，直接爆掉。
   建構子裡要先 `this.tc = tc` 再做其他設定
3. **曲率計算是 O(頂點×面)** — 上萬面會卡死。改成掃一遍 O(面)，
   16,128 面從卡住變成 7ms。凡是「對每個頂點都查一次全部的面」都要警覺
4. **three.js 的事件名稱搜尋不到** — `dragging-changed` 是執行時用
   `屬性名 + '-changed'` 組出來的，字面搜尋會誤判成不存在。
   要找 `defineProperty( '屬性名'` 才對

5. **把只對特例成立的假設寫死** — 第 1 期的結構分析面板寫死
   「封閉 → 尤拉數一定是 2、角虧總和一定是 4π」。那時只有基本體，
   做不出有洞的東西，所以從沒出事。第 2 期一挖貫穿孔就變成 χ=0、
   角虧總和 0，面板把**完全正確的結果打了兩個紅叉**。
   正確的式子是 `χ = 2×實體數 − 2×貫穿孔數`、`角虧總和 = 2πχ`。

   → 這類 bug 不會讓程式當掉，但會讓人**不敢相信工具給的任何數字**，
     而那個面板存在的意義就是讓人相信數字。
   → **規則**：寫「應該是多少」的判定時，先問「這條規則的成立條件是什麼」，
     把條件寫進註解；而且判定邏輯要抽成不碰 DOM 的純函式，才測得到。
     `topologyCheck()` 就是這樣抽出來的。

6. **展開長度不能用網格量 —— 這條會讓實際下料出錯**　⚠ 第 3 期必讀

   圓弧在網格上被切成 `arcSeg` 段直線，**弦長永遠比弧長短**，
   所以拿網格去量會**往少的方向錯**，而且每道折彎都少一點。

   | 同一片折板（40 ＋ R2 的 90° 彎 ＋ 30） | 長度 |
   |---|---|
   | 網格量到的（弦長） | 73.12 cm |
   | 真正的展開長（弧長 r×θ） | 73.14 cm |

   差 0.03%，折三道就差到零點幾公分。雷切下去才發現就來不及。

   → **規則**：展開長度一律用 `bendDevelopedLength()`（真弧長），
     **絕對不要用 `mesh.area()` 或量網格頂點距離去反推**。
     第 3 期的展開圖、DXF、備料長度都走這個函式。
     測試裡刻意把「網格面積用弦長對」與「展開長度用弧長對」分成兩項，
     就是為了讓這個差別一直看得見。

7. **加厚時折彎處會被削薄**　⚠ 第 3 期做折彎補償時會再遇到同一件事

   `shell()` 第一版沿頂點法向推半個板厚，體積少了 0.076%。
   頂點法向是**角平分線**，只推 t/2 的話折角處實際厚度變成
   `t × cos(半夾角)` —— 90 度折就只剩 0.71 倍。

   → 正確做法是除以那個餘弦（鈑金與繪圖裡講的「尖角接合 miter」）。
     修正後 1／3／8 段 × 45／90／135 度全部精確到浮點誤差。
   → **更一般的教訓**：凡是「沿法向偏移」的運算，在不共面的地方
     都要想一下有沒有這個係數。肉眼看不出來，只有算體積才抓得到。

8. **板件不該做布林聯集 —— 那是語意問題，不是函式庫限制**

   陣列原本一律走布林聯集，板件（開放的面）整個失敗。
   當下的直覺是「想辦法讓布林吃下去」，但那是錯的方向：

   | | 該怎麼做 | 為什麼 |
   |---|---|---|
   | 實體 | 布林聯集 | 兩份重疊處只能算一次，接縫要真的縫起來 |
   | **板件** | **直接拼接，不合併**（`Mesh.merge()`） | 12 片一樣的側板就是 12 片，展開要分開出圖。黏成一體反而讓第 3 期分不出來 |

   → 判斷依據用**網格封不封閉**，不是物件的 `kind` ——
     kind 是使用者隨手可改的標籤，封閉與否才是幾何事實。
   → **拼接時折線一定要跟著搬過去**，掉了折線第 3 期就展不開。
     測試有一項專門盯這個（5 條折線的折板做成 5 片必須是 25 條）。

9. **規格上合法 ≠ 別人的軟體讀得動**　⚠ 凡是要送出去的格式都適用

   第一版 DXF 只寫了 HEADER／TABLES(只有 LAYER)／ENTITIES 三段。
   完全符合 R12 規格，AutoCAD 讀得動，**Illustrator 直接跳錯誤視窗**。
   缺的是：BLOCKS 區段（空的也要有）、LTYPE 表、STYLE 表、圖層 0。
   另外 `$INSUNITS` 是 R14 才有的變數，寫在 AC1009 裡是時代錯亂。

   SVG 那邊沒寫 `<?xml ... ?>` 宣告，Illustrator 根本沒認出那是 SVG，
   **當成純文字檔開**，畫面一堆標籤原始碼，中文還變亂碼。
   另外它不支援 `dominant-baseline`，所有標註會往上跳半個字高；
   線寬寫成 0.35 個使用者單位（1 單位 ＝ 1cm）＝ 印出來 3.5mm 粗，
   製圖標準大概 0.5mm，粗了七倍。

   → **規則**：要送出去給別的軟體開的格式，只能用最保守的寫法。
     不用選用屬性、不用新版才有的欄位、骨架寧可多寫幾十行。
   → **更重要的規則**：這類東西**沙箱驗不了**。
     語法檢查過、瀏覽器看起來對，都不算驗證過；
     **一定要 kang 用真正的目標軟體開過**。這條推翻了原本
     「沙箱能做靜態檢查就算驗證」的假設。

10. **圓弧至少要切兩段才認得出來**

    切一段的「圓弧」在網格上就是一個平面四邊形，跟一個倒角
    **完全一樣**，沒有任何幾何資訊能分辨。
    這不是演算法保守，是資訊真的不存在。
    → `prim.js` 的 `arcSeg` 下限設成 2，並在面板提示裡寫明理由。
    → 同樣的道理：等寬等角的連續折面會被判定成圓弧，
      因為它幾何上就是。要表達「刻意做成折面板」得等第 6 期的面編輯。

11. **「產生 `<a download>` 然後 click」有三個地雷**

    實測存出來的檔案叫「尚未確認的 636844.DXF」—— 那是 Chrome
    自己的暫存名，副檔名還被改成大寫，所以 Illustrator 認不得。
    **看起來像匯出功能壞掉，其實檔案內容完全正確。**
    另外還留下一堆沒下載完的 `.crdownload`。三個原因：

    | 地雷 | 後果 | 修法 |
    |---|---|---|
    | `<a>` 沒掛進 DOM | Chrome 忽略 `download` 檔名 | appendChild 再 click，click 完移除 |
    | blob URL 1 秒就 revoke | 下載被中斷 | 改成 5 秒 |
    | 連續下載多個檔案 | 只放行第一個 | 每個錯開 250ms |

    → 修法直接沿用**組裝系統結構說明表**已經驗證過的那一套，
      抽成 `js/out/save.js` 共用，兩個工具的存檔行為因此完全一致。
      **踩過的坑要往回看自己的專案，不要重新踩一遍。**
    → 順帶補上「指定存放位置」（File System Access API）。
      它只在**安全環境**可用：https 或 **localhost**，
      `http://192.168.x.x` 不算。用 IP 開會自動退回一般下載，
      勾選框淡化並在提示裡講清楚原因 ——
      **沉默地退回是最糟的做法**，使用者會以為勾選壞掉了。

12. **讀不出尺寸的圖，比沒有圖更糟**

    折彎多的件（圓角方塊 21 道）分段尺寸全擠在同一排，
    變成一團「0.9787.05.24」。看的人會以為自己讀到了。

    → 但也**不能因為放不下就不標**：折彎區只有 3.33cm，
      那個數字正是師傅畫線要用的，丟掉等於這張圖不能下料。
      改成錯開兩排，兩排都塞不下才放棄（並到備料明細去查）。
    → 測試加了一組**自動偵測標註重疊**的檢查。
      這件事測得到，是因為出圖刻意拆成「決定畫什麼」（純資料）
      與「怎麼畫出來」兩層 —— 又是同一條分層原則的回報。

13. **開新階段前要先確認「目的」，不是先確認「規格」**　⚠ 這條最貴

    第 3 期整期做完之後，kang 才說明做這套程式的用意是
    「建模拿去 3D 列印、展開拿去 CNC」。

    而原本的八期路線圖**完全沒有 3D 列印的出口**。
    「目標與範圍」只寫展開，唯一相關的一句是第 8 期順帶提到的 glTF ——
    而 glTF 根本不是 3D 列印的格式。
    **照原規劃走完八期，東西還是印不出來。**

    我從第 1 期做到第 3 期，一次都沒問過「這套做出來要拿來幹嘛」，
    只照著既有文件推進。文件寫得再詳細，也只是「當時想到的規格」，
    不等於「真正要達成的目的」。

    → **規則**：開新階段（或接手一個專案）之前，先問一句
      「這個做出來要拿來做什麼、給誰用、下一步會接到哪裡」。
      規格是從目的推出來的，順序不能反。
    → 補救很便宜（STL 匯出半天就做完，因為地基早就在了），
      但如果拖到第 8 期才發現，中間好幾期的設計取捨可能都要重來。
    → 這條也解釋了為什麼「目標與範圍」那一節現在最上面放的是
      **kang 的原話**，而不是規格 —— 規格會過期，目的不會。

14. **「目的」包含用什麼材料**　⚠ 這條跟第 13 條一樣貴，而且是同一個病

    第 13 條之後，AI 仍然從第一句就假設這是**鈑金工具** ——
    因為日誌通篇是鈑金語言（K 因子、中性層、折彎補償、最小折邊），
    材料表六種有三種是金屬。**沒問過實際用什麼材料，就照著文件往下推。**

    整整推了一場：花大量工夫追「圓錐展開偏短 0.16～2.5%」，
    直到 kang 主動說明「我們用紙材、發泡板、珍珠板、木板、壓克力，
    一種金屬都不用」，才發現**那個弦長本來就是對的**，
    而程式真正的錯是反過來的 —— 把正確的弦長拉成錯的弧長，
    4 角柱因此長了 **11.07%**。

    → **規則**：第 13 條要補一句 ——「目的」包含**用什麼材料、
      給誰做、做出來怎麼組**。這三件事沒問清楚，規格一定是猜的。
    → 這也是為什麼「開新對話時先讀這段」現在第四條就是材料，
      而且刻意寫成警告。

15. **弦長還是弧長，是材料問題，不是幾何問題**

    網格上的一段「圓弧」是一圈平面小面。它到底代表
    「真的被捲圓的曲面」還是「一圈平板拼出來的多邊形」，
    **幾何本身分不出來**（這就是坑第 10 條的另一面）。

    第 3 期把「一律當成捲圓的」寫死在幾何核心裡，等於替所有材料
    做了同一個假設。對板材而言那個假設一直是錯的：

    | | 程式給的 | 外緣相加（真值） | 誤差 |
    |---|---|---|---|
    | 4 角柱 | 188.50 | 169.71 | **＋11.07%** |
    | 6 角柱 | 188.50 | 180.00 | ＋4.72% |
    | 8 段圓柱 | 188.50 | 183.69 | ＋2.62% |

    → 修法是讓**材料規則**回答（`canRoll`），幾何核心只多 6 行。
      這是「規則分開、幾何共用」原則第 N 次派上用場。
    → **判準**：一段邏輯需不需要知道材料是什麼？需要就別寫在幾何核心裡。
      第 3 期問過這個問題，但漏掉了「圓弧修正」這一段。

16. **攤平的生成樹要先走最平的邊，否則共面的東西會被拆散**

    圓錐加底蓋展開後是一張**從中心炸出去的星芒**，尖刺互相交疊，
    外框 220.56×182.05，完全不能用。

    成因不是切割規則，是**走訪順序**：底蓋由 32 個共面三角形組成，
    先進先出的佇列會讓每個底蓋三角形各自從**旁邊那片側面**跨輪圈接上去，
    而不是接到隔壁的底蓋三角形，底蓋因此被拆散、散落在扇形四周。

    → 改成生成樹**優先跨過轉折角最小的邊**（共面的鄰居其實是同一片板子）。
      外框變成 126.33×149.41，星芒消失。
    → **不改變任何「哪些邊可以折」的規則，只改變先攤哪一條。**
      攤平是剛體運動，順序不影響每一片自己的形狀與尺寸。
    → 副產品：圓角方塊的四個圓角**終於被辨識成圓弧帶**（先前是圓弧 0），
      球冠的重疊偵測也開始生效。

    ── 這條之前繞了很久，值得記下來 ──
    一開始判斷是「輪圈是封閉的圓，不該當折線」，想從幾何推論出規則。
    **試了五種判準，五種全部誤傷合法的件**（方塊被拆成六片、
    折板 L 被拆散、底蓋跟每片側面都只共用一條邊……）。
    五次撞牆之後才回頭看真正的成因，而它跟切割規則無關。
    → **教訓：連續幾種「聽起來很對」的規則都失敗時，
      多半是問錯問題了，不是還沒想到第六種規則。**

17. **「中途對、最後錯」的 bug，只測中途抓不到**　⚠ 這條是送出去的檔案錯了

    100×60 的平板加厚後放在 Y=44cm 處匯出 STL：

    | | 外框 | 體積 |
    |---|---|---|
    | `dropToBed` 前 | 1000×600×**2** mm | +1,200,000 ✓ |
    | `dropToBed` 後 | 1000×600×**439** mm | **−86,600,000** ✗ |

    原因：`triangles()` 做扇形三角化時，`p0` 每個面只算一次，
    而 `out.push({ a, b: b.clone(), c: c.clone() })` —— **`a` 沒有 clone**。
    一個四邊形切出來的兩個三角形因此共用同一個 `Vector3`，
    而 `dropToBed()` 是就地平移，共用的頂點被**減了兩次**。

    → 修法：三個頂點都 clone。
    → **為什麼一直沒被發現**：三角形面（方塊、圓錐、球）每個面只產生
      一個三角形，共用不到。這個 bug 只在**有四邊形面**的東西上發作
      —— 平板、折板、管、圓柱側面、圓角方塊，而那正是板件。
      而且物件**在原點時不發作**（位移是 0，直接 return）。
    → **第 3.5 期的外部驗證用的是錐體 —— 剛好是不受影響的那一類。**
      所以那次「外部驗證通過」其實完全沒有涵蓋板件。
    → 折板其實也一直是壞的（外框 1030.8mm 應為 321.8mm），
      只是體積剛好還是正的，沒被判「印不出來」，所以沒人注意。

    ── 三條規則 ──
    → **多段轉換的輸出路徑，驗證要放在最末端**，而且要用
      「跑完全程之後仍然守恆的量」去對（這裡是體積與外框）。
      原本的測試量的是 `mesh.volume()` 與加厚後的體積 —— 那兩個一直都對。
    → **外部驗證挑樣本要涵蓋不同的「網格結構」，不只是不同的形狀。**
      錐體與平板在使用者眼中是兩個形狀，在這個 bug 眼中是
      「全三角形」與「有四邊形」兩類 —— 只驗了前者等於沒驗。
    → **要測病因，不只測症狀。** 症狀（體積變負）會變，
      病因（任兩個三角形共用頂點物件）不會。
      現在兩者都測，日後有人為了省一次 clone 又改回去會立刻被擋下。

18. **誤報比漏報更糟**

    「算不準的展開要講出來」第一版的判準是「折線沒有全部互相平行」，
    結果**鋼板方塊**攤成十字型時 5 道折線分兩個方向，
    於是在**最常見的件**上跳出「請勿據以下料」——
    可是方塊根本沒有圓弧（圓弧 0、尖角 5），那張圖完全正確。

    → 判準改成直接問「**這是不是錐面**」：折線是否放射狀匯聚到同一點。
      分離度很寬：圓錐（seg 4～64）匯聚比例一律 100%，
      方塊 2/5、圓角方塊 2/21、角柱 2/7、球 127/511 —— 非錐面最高 25%。
    → **測試那一節寫了六個正面案例卻漏掉鋼板方塊**，因為只測了
      壓克力方塊，而壓克力的折線全被切開、等於沒有折線 ——
      **看起來測過了，其實測了個空的。**
    → 誤報會讓人學會忽略整個警告欄，那警告欄就等於不存在。
      這跟坑第 5 條（讓人不敢相信工具給的數字）是同一種傷害。

## 待辦 / 可延伸

- [x] **第 2 期全部完成**（`8d2de61`）：布林運算、陣列鏡射、九種參數體
- [x] **第 3 期全部完成**（`77f386b`）：展開 v1、K 因子、DXF／SVG／列印／備料 CSV

- [x] **第 5 期(A)(B)(C) 完成**（`766a3fd` / `7c4fa25` / `91a7f03`）：
      止血、外皮弦長、生成樹先走最平的邊、實體可直接展開、STL 修正

- [ ] **指定分片的介面**　★ kang 實際最需要、但原路線圖裡沒有的一項

      「立體造型必須做規劃，**如何分配展開的區域**，能夠依序讓 CNC
      切割後再組裝結合。」一個立方體可以六面一片、三片、或六片全分開，
      **選哪一種是製造決定，不是計算結果**。

      底層已經支援（`EDGE_ROLE.CUT`，`edgeIsCut()` 第一件事就是尊重
      使用者標記的切割線），缺的只是點選邊的介面 ——
      而那正是第 6 期的地基，兩者應該合併或把第 6 期提前。

- [ ] **第 4 期：草圖擠出**

      一併做「擠出多邊形」—— 原本排在第 2 期，理由是角柱已涵蓋正多邊形，
      任意斷面需要畫草圖的介面，那正是第 4 期整期在做的事，先做半套等於白做。

- [ ] **第 5 期：圓錐的極座標展開**　**只剩紙材需要**
      板材捲不起來，圓錐一定是切成平板再接，**弦長就是正確答案**。
      只有紙材捲得起來才需要真弧長。目前捲得起來的材料展開錐面
      會跳「請勿據以下料」警告，捲不起來的不跳。
- [x] **主入口頁加上建模器的按鈕**（`0428b14`）
      連帶做了圖示（`icons/modeler-*.png`）與 `manifest-modeler.webmanifest`，
      所以建模器也能「加到主畫面」了。
      manifest 的 `scope` 是 `./建模器/`（另外兩個是 `./`）——
      從主畫面開建模器時視窗只圈住建模器，點回首頁會用一般瀏覽器開，
      不會把 PWA 視窗帶走。
- [ ] 實體「抽殼」轉板件　**優先度已大幅下降**
      原本是「做了才能展開實體」的前提，但實體現在直接展開外表面就對了
      （關鍵決定 3 修訂）。抽殼只在「真的要做一個有內壁的空心件」時才需要，
      而且真正麻煩的是「哪幾個面要開口」，那需要點選面的介面（第 6 期）。
- [ ] 展開片的排料與材料利用率（可接上說明表既有的備料邏輯）
- [ ] 展開圖加「摺線方向」的視覺區分（目前只有文字標 ↑上折／↓下折）

---

## 協作規則

### 版本控制與檢查流程
- 本資料夾已納入 Git。**Claude 一律不執行任何 git 指令，連唯讀的 `git status` 也不行。**
  掛載點不支援刪除 `.git` 內的檔案，唯讀指令建立的 `index.lock` 會留下來，
  導致 kang 下次存檔 fatal 失敗（2026-08-20 實際發生過）。
  Claude 只用 Read／grep 讀檔案內容；所有 git 動作由 kang 在 Windows 端執行。
- **操作流程與工具見 `版控工具\README.md`**：
  `1-看差異.bat`（唯讀）→ Claude 讀結果檔確認 → Claude 寫 `commit-msg.txt` → `2-存檔.bat`（純加法）
- 切分支、退回**不做成一鍵**，由 Claude 當場給指令
  （判準參考 ERP 專案：一鍵只給唯讀與純加法，會改變現況的動作不給捷徑）
- `.bat` 內容必須**純 ASCII + CRLF**（中文會在 Big5 環境亂碼；LF 會讓 `goto` 標籤失效），
  已用 `.gitattributes` 鎖住 `*.bat text eol=crlf`
- **改完檔案後一定要跑 `git diff` 自我檢查**，特別注意有沒有 `-` 掉不該刪的行。
  教訓：2026-08-20 插入 CSS 時誤刪 `@media print{` 開頭，導致列印規則全域生效、
  側欄工具列全被隱藏。這種錯誤**語法檢查抓不到**（大括號仍然平衡、JS 也沒問題），
  只有 diff 看得出來
- CSS 大量改動後，額外檢查：大括號配對、每個 `@media` 是否正確閉合、
  `display:none!important` 這類破壞性規則是否確實被包在該有的 media query 內

### 這個專案踩過的坑（照時間，都是實際發生過的）
1. **沙箱跑 `git status` 也會鎖住 repo** — 唯讀指令一樣建立 `index.lock`，
   而沙箱刪不掉，導致 kang 存檔 fatal 失敗。→ 沙箱一律不執行任何 git 指令
2. **CSS 插入時誤刪 `@media print{` 開頭** — 大括號仍然平衡、JS 也沒問題、
   瀏覽器不報錯，但列印規則變成全域生效、整個介面消失。→ 只有 diff 抓得到
3. **`.bat` 內容含中文會亂碼、LF 換行會讓 `goto` 失效** — 檔名可中文，內容不行；
   已用 `.gitattributes` 鎖 CRLF
4. **轉檔腳本把檔案寫成 0 bytes** — 寫成 `open(p,'wb').write(txt.encode(...))`，
   `wb` 先清空檔案，`encode` 才拋例外。→ 必須「先編碼驗證成功，才開檔寫入」。
   這也是 `1-看差異.bat` 第 [5] 段「各檔絕對行數」存在的理由
5. **flex 項目預設會被壓縮** — 工具列空間不足時按鈕被壓到比文字還窄，
   文字直向折行。→ `flex:none` + `white-space:nowrap`
6. **小物件的縮放把手會蓋住物件本身** — 45×35cm 的「人」在常用比例下只有 14×11px，
   四角把手加起來遠大於它，一拖就變成改尺寸而不是移動。
   → 物件太小時不提供把手，且**命中判斷與繪製必須用同一個函式**，畫面才不會騙人
7. **iPad 全螢幕時內容跑到狀態列底下** — `black-translucent` 造成。
   → 用 `env(safe-area-inset-*)`，桌機這些值是 0 不受影響
8. **`localhost:8080` 被別的專案的 Service Worker 佔住** — 2026-08-21 發生。
   本機開入口頁跑出完全不相干的舊專案畫面，iPad 卻正常。
   原因是 Service Worker 綁在「**網址來源**」（`http://localhost:8080`）上，
   不是綁在資料夾，它會攔截該來源的所有請求並回自己的快取，
   **不管實際上是哪個伺服器在聽這個埠**。iPad 走內網 IP 是不同來源，所以沒事。
   → 清法：網址列左邊 ⓘ →「網站設定」→「刪除資料」→ `Ctrl+Shift+R`。
   → 本專案**刻意沒有 Service Worker**（見「部署設定」），所以不會反過來害到別人。
   → 若日後又常撞到，根本解法是把測試伺服器的埠改掉
     （`版控工具\平板測試-啟動伺服器.bat` 的 `set PORT=`），
     不同的埠就是不同的來源，永遠不會打架。

### 沙箱能做什麼、不能做什麼（影響每次交付的驗證方式）

- **不能開瀏覽器實測** — 沙箱下載不到 Chrome，無法跑 headless 測試。
  所以「畫面有沒有出來」永遠只能靠 kang 實際開啟確認，AI 不能宣稱已驗證。
- **不能驗「別的軟體讀不讀得動」**　⚠ 2026-08-21 實際踩到（坑第 9、11 條）
  匯出的 DXF／SVG 能不能在 Illustrator 開、下載下來的檔名對不對，
  沙箱一律驗不了。**格式合法、語法檢查過、內容正確，都不代表對方打得開。**
  → 凡是交付「要送出去給別的軟體開的東西」（DXF、SVG、PDF、下載檔），
    **交付時就要明講「這部分我驗不了，請你用真正的軟體開一次」**，
    不要讓 kang 以為已經驗過了。
  → **外部驗證挑樣本時，要涵蓋不同的「網格結構」，不只是不同的形狀。**
    2026-08-22 實際踩到（坑第 17 條）：第 3.5 期用錐體做外部驗證並通過，
    但錐體全是三角形面，剛好避開了「四邊形面頂點共用」那個 bug ——
    而板件（平板、折板、管、圓柱側面）全都中招，STL 一直是壞的。
    **只驗了一類等於沒驗。** 現在的分類至少要涵蓋：
    全三角形面／有四邊形面、封閉／開放、在原點／不在原點。
- **能做的驗證**：JS 語法檢查、跨檔匯入匯出對照、HTML 元素 id 對照、
  CSS 大括號與 media query 檢查、外部網路依賴掃描、函式庫 API 逐項確認、
  匯出檔案的**結構**（DXF 區段與表格是否齊全成對、SVG 是否良構）
- **能在 Node 裡跑真正的邏輯測試** — 只要模組不碰 DOM。
  建模器的核心刻意跟 three.js 場景切開，就是為了這件事。
  **凡是能用數學對答案的就一定要對**（尤拉公式、角虧總和、體積面積、展開長度），
  比「看起來沒錯」可靠得多。
  → 建模器已有現成的測試腳本：`node 建模器/tests/run.mjs`，**593 項**。
    **改過 mesh / region / prim / bool / io / toolbar / unfold / out 就跑一次。**
    新功能請順手把「能用數學驗的部分」加進去，不要另外開檔案。
  → 連「圖上標了什麼、有沒有疊在一起」都測得到 ——
    因為出圖拆成「決定畫什麼」（純資料）與「怎麼畫出來」兩層。
    **要讓東西測得到，就把不碰 DOM 的那一半抽出來。**
- **npm 可用**（能抓函式庫原始碼），但一般 CDN 被擋。
  要用外部函式庫時：npm 取得 → 複製進 `lib/` → 連授權與版本說明一起放，
  跟 three.js 與 Manifold 同一套做法。**執行時不可以連外。**

### 一般規則
- 回答用繁體中文、簡潔
- 多檔案修改前先列計畫並提關鍵疑問
- 修錯連續失敗超過 3 次就停下回報

## 子專案 1（空間平面規劃器）：使用流程建議

> 這兩節在檔案順序上被排到「協作規則」後面，但內容屬於子專案 1。

1. Illustrator 平面圖 → 輸出 PNG
2. 右側「上傳底圖」
3. 工具列「⚖ 校正比例」沿一段已知長度（如 618cm 柱距）拉線，輸入實際 cm
4. 從左側物件庫擺放物件、調高度
5. 切到「◈ 3D 預覽」用第一人稱走一遍確認走道與空間感
6. 「💾 存檔」存成 JSON，下次「📂 讀檔」續做

## 子專案 1（空間平面規劃器）：待辦 / 可延伸
- [ ] **新增 `kind:'mesh'` 物件類型，匯入建模器的模型**
      （`model-mesh` JSON。行為比照現有的 `system`：加入現有平面圖、不清空，
      2D 畫俯視輪廓、3D 畫真網格。預設鎖定縮放，解鎖後名稱旁標示縮放比例。
      **排在建模器第 8 期**，屆時會是唯一一次動到這個檔案）
- [ ] 圖層與群組
- [ ] 門開口（讓房間牆真的開洞）
- [ ] 列印排版（A3 含比例尺與圖例）
- [ ] 材質貼圖（地板、牆面）
- [ ] 家具 3D 模型匯入（glTF）
- [ ] 走道淨寬自動檢查（低於設定值時警示）

## 進度紀錄
- 2026-08-20　v1 完成：2D 平面編輯器（拖拉、吸附、物件庫、存讀檔、匯出 PNG）
- 2026-08-20　v2 完成：加入物件高度與類型、3D 環繞檢視與第一人稱漫遊、牆體碰撞
- 2026-08-20　v2.1 介面：左右側欄改為可拖曳調寬（150–620px，雙擊還原）、
  可一鍵收合、寬度記憶於 localStorage（key: `floorplan_panels`）；
  修正欄位文字截斷（物件庫名稱過長改省略號、屬性面板標籤縮窄）
- 2026-08-20　v2.2 分層造型：物件可堆疊多層方塊做出上寬下窄等形狀，
  含 4 組快速範本與逐層編輯器；2D 用外廓實線＋內層虛線呈現；
  物件庫的工作桌／機台／CNC／台車／料架已預掛範本
- 2026-08-20　v2.3 新增「人」物件（kind: person）作為空間比例參考，
  身高／肩寬可調，角度＝面向方向
- 2026-08-20　開啟子專案 2「鋁擠型框架設計器」v1：
  40 系列型材、4 範本、T 溝斷面 3D、桿件點選編輯、切割料表與配件清單、CSV 匯出
  （後來移出資料夾，未納入版控）
- 2026-08-20　開啟子專案 3「組裝系統結構說明表」：取代 Illustrator 手工表單，
  標準料排料 DP、多說明表、套數、備料與批次匯出，並與平面規劃器互通

### 平板化與上線（2026-08-20，分支 `touch` → 已合併回 `main`）
- `ae07255` 說明表加入觸控支援，並建立版控工具
- `3c5c0b3` 版控工具：平板測試伺服器 + stale lock 偵測
- `354da13` 空間平面規劃器加入觸控支援
- `c10b80a` 修正平板工具列文字擠成直排
- `e8080fe` 修正小物件難以點選與移動
- `3a2910a` 修正 iPad 全螢幕被狀態列蓋住
- `dcc84e4` three.js 本地化 + 加到主畫面
- `a4ec883` 入口頁與上傳工具
- 上線 GitHub Pages，iPad 實測通過

**設計原則貫穿全部觸控改造**：事件入口分開、動作邏輯共用。
桌機 `mouse*` 路徑行為不變，觸控是另外新增的入口，兩者呼叫同一組動作函式，
因此計算結果保證一致，未來改邏輯也只改一處。
已用「抽出計算邏輯比對 SHA-256」與「同尺寸實跑比對排料備料結果」驗證過。

### 建模器啟動（2026-08-21）

- **第 0 期技術驗證通過** — 桌機與 iPad 上 3D 直接編輯（點選、三軸 gizmo、
  布林挖孔、觸控）手感都可接受，架構**不必**改成 2D 主導。
  驗證檔留在 `XXX/建模器驗證/`（不進版控），日後正式版手感變差時可回頭比對
- `2b030f0` 建模器第 1 期：半邊網格核心與參數化基本體（15 檔、2,746 行自寫程式碼）
  - 純加法，未修改任何既有檔案
  - kang 已在瀏覽器實際開啟確認：畫面正常、60 FPS，
    面板數字與沙箱算出來的完全一致

**同一條原則延續到建模器**：`select.js` 的滑鼠與觸控走同一組動作函式；
第 0 期在 iPad 實測過的「移動 8px 內、450ms 內算點選」「拖 gizmo 時關掉視角旋轉」
「手指裝置 gizmo 放大 1.5 倍」三項設定原封不動搬進正式版。

### 建模器第 2 期（上）：布林運算（2026-08-21）

- `e7cff61` 布林運算：差集／聯集／交集，存運算樹而不是存三角形
  - 挖完孔還能在面板上回頭改孔徑與孔的位置，另有「凍結成網格」
  - 檔案格式 v1 → v2；v1 舊檔仍讀得動
  - **推翻原訂的 BVH 方案，改用 Manifold**（理由見「關鍵決定」第 4 條）
  - 建立回歸測試 `tests/run.mjs`，67 項
- `386cef3` 修正結構分析對「有洞」與「分成多塊」物體的誤判
  - 見「踩過的坑」第 5 條。測試 67 → 89 項
  - `mesh.js` 新增 `componentCount()`，`validate()` 多回傳 `components`

kang 已在本機與 GitHub Pages 兩邊實測確認：挖孔正常、改半徑孔跟著變、
復原可一路退回、線上版 WASM 正常載入。

### 建模器第 2 期（中）：陣列與鏡射（2026-08-21）

- `e47bcd8` 線性／環形／鏡射三種排列，做成**修飾器**而不是複製成 N 個物件
  - 理由是備料：12 支一樣的橫料，展開要出「一張圖 ×12」，不是 12 張一樣的圖。
    跟說明表的「套數」是同一個概念。`ModelObject.copies` 帶著這個數字
  - 線性可吃兩個方向 → 散熱孔的網格就是這樣排的
  - 資訊只能往下走，所以預設留在修飾器，另外提供「打散」
  - 檔案格式 v2 → v3；測試 89 → 153 項
  - 順帶把布林面板的「可展開子項」抽成共用的 `_itemBox` / `_srcBody`

### 建模器第 2 期（下）：管、圓角方塊、折板（2026-08-21）

- `8d2de61` 三個新參數體，加上板件在畫面上顯示厚度
  - 折板做成**折彎序列**，L／U／Z／多折用同一套；面板可加減道數
  - 板件顯示加厚（`mesh.shell()`），資料層仍是「中性面 ＋ 厚度屬性」
  - 測試 153 → 235 項
  - **這一期學到三件會直接影響第 3 期的事，寫在「踩過的坑」第 6、7、8 條**
  - kang 實測時抓到「板件不能做陣列」，修正後一併補上測試

**第 2 期到此完成。**

### 建模器第 3 期：展開 v1 ＋ DXF／SVG 出圖（2026-08-21）

- `77f386b` 板件可以攤平成下料圖了。新增 7 檔 2,466 行，測試 235 → 407 項
  - **折彎半徑改成「內側 R ＋ K 因子」**，見「關鍵決定」第 5 條。
    檔案格式 v3 → v4，舊檔轉換後形狀與展開長完全不變
  - 展開幾何核心 `unfold/flatten.js` 完全不知道材料是什麼；
    六種材料規則各自獨立在 `unfold/rules.js`
  - **圓弧修正**：從網格辨識圓弧折彎帶，把弦長換成真弧長。
    與參數公式交叉驗證，`arcSeg` 2～24 全部一致到 1e-14
  - 出圖拆成「決定畫什麼」（純資料）與「怎麼畫出來」兩層，
    Canvas 預覽、SVG、列印畫的內容保證一致，而且**測得到**
  - DXF R12（圖層分切割線／折線／折彎區／尺寸／文字）、SVG、
    A4 橫向列印、備料 CSV、指定存放位置

**kang 實測回報了四個沙箱驗不出來的問題，全部修正並補進測試：**
DXF 在 Illustrator 打不開（缺骨架）、SVG 被當成純文字開（缺 XML 宣告）、
下載檔名變成 Chrome 的暫存名、折彎多的件標註擠成一團。
四條都寫進「踩過的坑」第 9～12 條。

**這一輪最重要的教訓是第 9 條**：規格上合法不等於別人的軟體讀得動，
而這類問題**沙箱一律驗不了**，只能靠實際開啟。以後凡是要送出去的格式，
交付時就要明講「這部分我驗不了，請你用真正的軟體開一次」。

### 建模器第 3.5 期：STL 匯出（2026-08-21）

- `4530697` 3D 列印那條路的出口。新增 2 檔 543 行，測試 407 → 461 項
  - 這一期是**對焦之後補的**：kang 說明用意是「建模拿去 3D 列印、
    展開拿去 CNC」，而原本的八期路線圖完全沒有 3D 列印的出口。
    教訓寫在「踩過的坑」第 13 條 —— **這條最貴**
  - 匯出自動處理四件事：cm→mm、Y 軸向上→Z 軸向上、落到平台 Z=0、
    板件自動加厚。詳見上方「STL 匯出」一節
  - **列印前檢查**才是重點：封閉性、法向、獨立塊數、尺寸。
    第 1、2 期打的地基（半邊結構、Manifold、尤拉數）第一次直接
    變成使用者看得到的價值

**外部驗證（這次特別重要，因為坑第 9 條說過沙箱驗不了）**

1. 另寫一支獨立的 Python 解析器讀回檔案（不碰本專案程式碼）：
   結構、體積、外框、貼平台皆正確；**每條有向邊只出現一次且都找得到
   反向邊** → 封閉且流形。
2. kang 用第三方線上工具（`iamrapid.com/tools/stl-analysis`）實測自己
   匯出的模型，判定「可直接列印 － 防水且多歧管」。
   它報的 498 三角形／251 頂點代回尤拉公式：`251 − 747 + 498 = 2`，
   拓撲完美。那個數字跟本專案毫無關係，對得上就表示一條邊都沒接錯。

kang 目前沒有切片軟體與 3D 列印機。但 STL 的風險遠低於 DXF ——
它就是「一個檔頭 ＋ 一串三角形」，沒有 DXF 那種可以缺的結構區段。

> ⚠ **這次的外部驗證後來被證明有漏**：兩項驗的都是**錐體**，
> 而錐體全是三角形面，剛好避開了「四邊形面頂點共用」那個 bug。
> 板件（平板、折板）的 STL 從第 3.5 期起就一直是壞的，
> 直到 2026-08-22 才被發現。見「踩過的坑」第 17 條。

**兩條生產路徑到此都通了**：
建模 → STL → 3D 列印；建模 → 展開 → DXF → 雷切。

### 第 5 期(A)：算不準的展開要講出來（2026-08-22）

- `766a3fd` 止血。測試 461 → 492 項

  圓錐展開走**弦長相加**，而當時以為那是錯的（對捲得起來的材料確實是）。
  真正的修法是極座標展開，但那是大工程，所以這一輪只做
  「算不準的片一定要標出來」。

  - `nonDevelopable` 維持 `false` 但補上一大段「**為什麼做不出來**」：
    用角虧判定會給錯答案，而且錯在「哪些頂點算內部」這個定義上，
    兩種定義各錯一半（圓錐與球冠剛好相反）。
    寧可留一個誠實的 false，也不要放一個會在圓錐上說「可以下料」的判定
  - 一度加了「封閉實體一律拒絕展開」，**被測試打回來** ——
    那會打死壓克力箱體（六片分開下料）這條天天在用的路
  - 警告合併：31 道折彎的錐面原本吐 31 行一模一樣的字，改成講一次

### 第 5 期(B)：展開改走外皮弦長（2026-08-22）

- `7c4fa25` 這一輪推翻了 (A) 的部分前提。測試 492 → 548 項

  kang 說明材料之後才發現：**板材捲不起來，弦長本來就是對的**，
  而程式反過來把它拉成弧長，4 角柱錯了 11.07%。見坑第 14、15 條。

  - 材料規則多回答 `canRoll()`，新增「珍珠板／發泡板／薄木板」，
    預設材質從 `steel` 改成它
  - 生成樹改成**先走最平的邊**，修掉封閉件攤成星芒的問題（坑第 16 條）
  - 警告判準從「折線不平行」改成「**是不是錐面**」——
    前者在鋼板方塊上誤報（坑第 18 條）
  - 3D 列印不受影響（實測：三種情況匯出的 STL 位元組完全相同）

### 第 5 期(C)：實體可直接展開 ＋ STL 修正（2026-08-22）

- `91a7f03` 測試 548 → 593 項

  - 拿掉「不是板件就拒絕展開」那道門（關鍵決定 3 修訂）
  - 「要不要加厚」的判斷從 `kind` 標籤改成 `mesh.isClosed()` 幾何事實
  - **修好 `triangles()` 沒有 clone `a` 的 bug** —— 板件的 STL
    從第 3.5 期起就一直是壞的（坑第 17 條）

  **外部驗證（這次刻意挑有四邊形面的）**：kang 用同一個線上工具
  驗平板與折板，兩個都判定「可直接列印 － 防水且多歧管」，八項檢查全綠。
  它報的表面積 12,064.00 cm² 可手算對答案：
  `2×(100×60) + 2×(100×0.2) + 2×(60×0.2) = 12064`，一分不差；
  尤拉數回推也都是 2（平板 8−18+12、折板 28−78+52）。

**下一步還沒定** —— 路線圖需要重排，見「路線圖現況」。
