# 網頁空間設計程式 — 現況（STATUS）

> 🔴🔴 **新對話只讀這一份就能開始。** 上限 **276 行**（超過就是有東西漏進來了）。
>
> ⚠ **本檔⛔ 不自存事實**。每一行都是「**狀態 → 指標**」——
> 「**為什麼**」的家是 `規格\` 與 `PROJECT_LOG.md`，「**經過**」的家是 `HISTORY.md`。
> **在這裡讀到任何解釋性的長段落，就是它跑錯地方了，請搬走。**
>
> 〔2026-08-29 建立。病因量過：`PROJECT_LOG.md` 2791 行、**說明文字佔 51%**，
> 　而它的定位卻是「每次從頭讀完」—— **參考書被規定要從頭讀**。
> 　結果是最該執行的規則沉底（我讀到第 713 行被截斷），當天犯了四次同類錯。〕

---

## 🔴 動手前檢查表（**每次動工前填，填完貼進 `版控工具\commit-msg.txt`**）

> ⚠ **這一張是紀律，⛔ 不是機制 —— 它會再破。** 老實寫在這裡。
> ⭐ 唯一的防線是**填的內容要進 commit-msg**，而 kang 每輪都會看到。
>
> 🔴 **為什麼做成「要填的表」而不是「要讀的規則」**（2026-08-29 量到的）：
> 同一天，四道**機械清單**我執行率 **100%**（模組圖／測試／id 對照／檔尾），
> 而「動工前標記事實假設」那條**寫成一段話的規則，執行率 0%** —— 四次全破。
> **⛔ 差別不是重要性，是形狀。**

```
【這次要做什麼】

【依賴哪些事實】⛔ 每條都要填「出處在哪一行」，填不出來就是還沒查
  1. ……………………………  【已查證 · 檔名:行號】 or 【還沒查】
  2. ……………………………  【已查證 · 檔名:行號】 or 【還沒查】

🔴 有任何一條是【還沒查】→ ⛔ 不准動手，先去查。

【會動到哪幾個檔】

【怎麼知道做對了】⛔ 要能對答案的量，不是「看起來對」
```

---

## 現況（走到哪了）

- **三個工具**：空間平面規劃器／組裝系統結構說明表／建模器。前兩個完成上線，**主力是建模器**。
- **三條生產路徑全通**：STL 列印／展開 DXF／剖面分切疊層。→ `PROJECT_LOG.md`「目標與範圍」
- **編輯能力對齊 Blender 進行中** → `外部參考-Blender編輯.md` 第 10 節
  - ⚠ **看那張表前先確認它開頭的「最後回寫」日期⛔ 不比最後一個 commit 舊**
  - 現算還剩幾項：`awk '/^### 10\.1/,/^### 10\.7/' 外部參考-Blender編輯.md | grep -c '^| .*| ⭐'`
- **測試**：`node 建模器/tests/run.mjs`　⛔ 總項數不記在任何文件（跑完自己會印）
- **線上**：https://kangman1977-cloud.github.io/space-design/

## ⏳ 待實測（🔴 **開新功能前先看這裡，有未結案的就先問 kang**）

- 🔴 **鋼筆物件的右側面板（2026-08-30，四項）** —— kang 問出來的缺口

  **前置**：用鋼筆畫一個形狀 → 收尾 → **點選那個物件**，看右側面板。

  | # | 做什麼 | 應該看到 |
  |---|---|---|
  | 1 | 看右側面板 | 多了「**鋼筆 參數**」那一區，裡面有 **`高 Y`** 欄位 ＋ **`轉成可編輯網格`** |
  | 2 | 把 `高 Y` 從 3 改成 6 | ⭐ **東西變兩倍高**，體積也**剛好兩倍**（結構分析對得起來） |
  | 3 | 看「新增」那個下拉 | 🔴 **裡面⛔ 不可以出現「鋼筆」**（它要有人先畫，⛔ 不能從零生成） |
  | 4 | 按 `轉成可編輯網格` | 提示講「錨點已經不在了」；**`編輯路徑` 那顆消失**；⭐ 但**形狀一格都沒變**，而且 `拉點線面` 現在進得去了 |

  ⚠ **第 4 項是不可逆的** —— 要驗就用一個**畫來測試的**物件，
  ⛔ 不要拿你正在做的東西按。

✅ **鋼筆 第 3 階段（二）做洞 八項全過並結案**（kang 2026-08-30：「驗收OK」）。
　　⭐ **⛔ 幾何一行都沒新寫** —— 只接了三個接點（`classify()` export／
　　補算 `area`／組成 `{pts, holes}`）。判準是**幾何**，⛔ 不看繞向也⛔ 不看順序。
　　⭐ `加一條` 這顆鈕**畫的時候與事後進 `編輯路徑` 按，走的是同一支**。
　　🔴 經過（含「我把第 4 項設計壞了」與「快取規則被量掉一列」）
　　逐字搬進 `HISTORY.md`「鋼筆 第 3 階段（二）」那一節（md5 對過）。
　　⏳ **平板仍然沒驗**。

✅ **鋼筆 第 3 階段（一）加點／刪點 十項全過並結案**（kang 2026-08-30：「我也驗收OK」）。
　　⭐ 加點 ＝ de Casteljau 分割（形狀等價）／刪點 ＝ 最小平方擬合
　　（誤差比「直接接」小將近 90 倍）。
　　🔴 **這一輪照出我寫錯一句話**：「形狀一格都沒變」⛔ 是錯的 ——
　　拉直成折線的**取樣會變**，體積差千分之幾。經過與三次「診斷也錯了」
　　逐字搬進 `HISTORY.md`「鋼筆 第 3 階段（一）」那一節（md5 對過）。
　　⏳ **平板仍然沒驗**。

✅ **內建鋼筆 第 2 階段十二項全過並結案**（kang 2026-08-29：「我驗收OK」）——
　　`改點`（選／拖錨點、拖把手、尖角⇄圓滑）＋ 右側面板 `編輯路徑`
　　（攤平、半透明、完成後旋轉與體積**一格不差**）。
　　⭐ **⛔ 這一件一次就過** —— 完整清單與「AI 上線前先驗過十項」的經過
　　逐字搬進 `HISTORY.md`「內建鋼筆 第 2 階段」那一節（md5 對過）。
　　⏳ **平板仍然沒驗**（跟刀具那三項卡在同一件事：平板不在身邊）。

✅ **鋼筆工具列標籤一項通過並結案**（kang 2026-08-29 實測，附截圖）——
　　`尖角` 後面是 **`厚度` 3**、`鎖角度` 後面是 **`度` 45**。
　　⚠ 但截圖照出**版面換行把 `度` 與 `45` 拆開**了 ——
　　kang 決定**併進之後的「功能介面」討論**，見真待辦「功能」那一格。

✅ **鋼筆「直線接曲線」三項全過並結案**（kang 2026-08-29 實測，附截圖：
　　畫出**曲線→直線→曲線→直線**的混合路徑）。
　　⭐ 至此**鋼筆第 1 階段的所有操作都通了**。

〔上一批：**內建鋼筆 第 1 階段十九項全過** —— ⚠ 連續退回**八輪**才通過，
　完整清單與八輪的病因逐字搬進 `HISTORY.md`
　「內建鋼筆 第 1 階段：十九項實測全過」那一節。〕

〔⏳ 待實測本來 2026-08-29 第一次全部清空過，這是清空後的第一批。〕

## 真待辦（⛔ 只放沒做完的；推導與規格都在別處）

**下一件**

- 🔴 **③ 開放與封閉：甲 加厚成板 ／ 乙 立成牆**（kang 2026-08-30 拍板）
  —— ✅ **做洞 2026-08-30 八項全過並結案**，這一件**可以開了**。

  ✅ **決定都拍板了，⛔ 不必再問**：
  兩顆開關 —— **`不封口`**，以及**只在它開著時才出現的 `往上長`**
  （開 ＝ 牆，欄位叫`高度`；關 ＝ 板，欄位叫`寬度`）。
  ⭐ 甲乙做成**板件 sheet**，厚度走既有的板厚機制，⛔ 不必新欄位。
  ⭐ **閉合路徑的行為一格不變**（還是實心塊）。

  🔴 **這一件要新的幾何**：開放的線**走不了 `extrudeMany`**（那要封閉輪廓）——
  乙是「沿線長出一片垂直的帶」，甲是「沿線往兩側偏移成封閉輪廓」。

  ⏭ **丙 只當參考線：排到後面**（kang 2026-08-30 決定）。
  ⚠ **⛔ 不是因為沒用，是因為它要一個新概念**：
  `Doc` 只有 `objects`、`KIND` 只有 solid／sheet，**兩種都會產生網格**
  【已查證 · `io.js:37-40, 618-622`】。
  🔴 而且有一個隱患：⛔ 不排除它的話，**3D 列印檢查會說「這個物件不封閉」**
  —— 那是誤報。

- ⏭ **⛔ 順手掉出來但這一輪刻意不做**：**閉合 ＋ 往兩側 ＝ 一圈框**。
  ⚠ 它跟 `內縮` 做得出來的東西重疊，而且⛔ 不是 kang 要求的
  〔功能之間的定位不可以互相模糊〕。

- ⚠ **第 2 階段留下的一個已知限制**（⛔ 不是 bug，⛔ 不要當成 bug 去修）：
  物件**非等比縮放**（`scale.x ≠ scale.z`）時，`編輯路徑` 裡的
  **`鎖角度` 度數跟畫面上量到的角度會差一點** —— 那是座標系本身的事。
  出處：`io.js` 的 `penPathToWorld()` 檔頭。

⏭ **`增長／縮減選取` 2026-08-29 收起來**（kang 決定）——
　　🔴 **選取那一格因此全部結束了，⛔ 下一件要從對照表重挑，而且要問 kang。**
　　理由與實測數字在 `PROJECT_LOG.md`「✘ 明確不做」。

**文件與工具**

- 🔴 **`HISTORY.md`「這個子專案踩過的坑」574 行、0 個 `###` 錨點** ——
  日誌到處寫「坑第 N 條」而**索引跳不進去**。修法＝那 34 條改成 `###`。
- ⚠ **`規格\建模器-點線面編輯.md` 的目次是手抄的，已經漂了**
  （寫「刀具三項輔助 105 行」，實際 **212 行**）。修法＝產生器加「保留人工欄」。
- **`重複偵測.js` 還沒搬** —— ERP 有現成的（`D:\彩藝通ERP系統\文件工具\`）

**功能**

- ⏭ **`鎖角度` 的 `度` 與數字被換行拆開** —— kang 2026-08-29 決定**併進之後的
  「功能介面」討論**一起處理，⛔ 不單獨開一輪。
  ⚠ **標籤本身是對的、已結案** —— 剩下的只有「那三個東西要綁成一組不換行」。
  ⭐ 標 ⏭ ⛔ 不標 ⏳：這是**他指定要延後的**，不是沒發現的問題。
- **瓣片展開 A＋B** ⏭ kang 2026-08-27 排到後面，**AB 要一起做** → `瓣片展開與省道-設計方向.md`
- **量測第 2 步的「每一個」** ⏭ 等 kang 想呈現方式 → `規格\建模器-量測.md`
- **合併頂點** ⚠ 最貴，會打破「既有頂點保持原索引」的契約
- **圓錐環切不出東西**（側面是三角形，`edgeRing` 只穿得過四邊形）→ 要動核心
- 其餘 → `PROJECT_LOG.md`「待辦 / 可延伸」

---

## 穩定參考（⛔ 這些不會變）

- 🔴 **絕對不要執行任何 git 指令**，連 `git status` 也不行 → 理由見 `PROJECT_LOG.md`「協作規則」
- 🔴 **這不是鈑金工具**。材料是紙材、發泡板、珍珠板、木板、壓克力，**一種金屬都不用**
- **建模器不能雙擊開啟**（ES 模組），要走伺服器。本機用 `localhost`，⛔ 不要用內網 IP
- **改過建模器核心就跑** `node 建模器/tests/run.mjs`
- 🔴 **動過 `js/` 底下任何檔案，再跑一次「模組圖真的載入」** ——
  `node --check` **對 ES 模組是瞎的**（抓不到重複宣告，2026-08-28 整個網頁掛掉）
  → 指令在 `PROJECT_LOG.md`「沙箱能做什麼」
- **DXF／SVG／下載檔沙箱驗不了** —— 一定要 kang 用真的軟體開過才算數
- 🔴 **上傳之後 AI 可以自己開線上版先驗一遍**（內建瀏覽器，⛔ 開不了本機
  `localhost`）。⚠ **驗之前一定要強制重抓整個模組圖**，否則會看到舊的
  `main.js`，症狀跟「條件寫錯」一模一樣（2026-08-29 實撞）。
  ⚠ **AI 驗過 ≠ kang 驗過** → 規則全文見 `PROJECT_LOG.md`「沙箱能做什麼」

### 日常流程

| 步驟 | 誰做 |
|---|---|
| 1. 改檔案 ＋ 寫 `版控工具\commit-msg.txt` | Claude |
| **2. 🔴 跑三道機械檢查**（見下）| Claude |
| 3. `1-看差異.bat` → Claude 逐行確認沒動到不該動的 | kang → Claude |
| 4. `2-存檔.bat` → `3-上傳網路.bat` | kang |
| 5. 🔴 **實測 → 回報 → Claude 當場回寫並結案** | kang → Claude |

**第 2 步的三道**（⛔ 存檔前一定要跑完）：

```
node 建模器/tests/run.mjs                    改過建模器核心
node 文件工具/目次產生器.js --all             改過任何 ## 節
cd 建模器 && node --input-type=module -e "import('./js/main.js').catch(e=>{if(e instanceof SyntaxError)process.exit(1)})"
                                             改過 js/ 底下任何檔案
```

⭐ **目次產生器要在寫完文件之後、存檔之前跑** ——
它會改 `PROJECT_LOG.md`／`HISTORY.md`，先存檔就得多開一個 commit 補目次。
〔ERP 2026-08-12 實撞過同一件事〕

---

## 落點規則（新東西該寫去哪）

> 每次做完一件事先問：**這是現況、規格、還是流水帳？**

| 這種東西 | 家 |
|---|---|
| 現況／待辦／⏳ 待實測 | **`STATUS.md`**（本檔·**替換式，結案就移出**）|
| 一個功能的規格、紅線、否決紀錄 | **`規格\` 一功能一檔**（替換對應段落，⛔ 不堆疊舊版）|
| 鐵律、協作規則、部署、尺寸的依據 | **`PROJECT_LOG.md`** |
| 已結案的流水帳、坑的完整經過 | **`HISTORY.md`**（append，⛔ 只搬不改事實）＋ **重跑目次產生器** |
| 動工前的推導、外部調查、被否決的方向 | **專屬文件**（如 `編輯功能設計方向.md`）|
| 這一輪做了什麼 | **`版控工具\commit-msg.txt`** ⛔ 不放日誌檔頭 |
| 行號、測試總項數 | **⛔ 不記** —— 會過期，要用時現算 |

### 三條鐵律（文件的）

1. 🔴 **同一條規則只寫一次**，別的檔只寫「這條在那邊」，⛔ 不抄第二份
2. 🔴 **指標一律用「名字」，⛔ 不用「位置」**（「見上一項」必然會斷）
3. 🔴 **搬移一律逐字搬 ＋ 機械斷言**（行數／md5），⛔ 不做順手精簡改寫

---

## 鐵律索引（⛔ 內容在 `PROJECT_LOG.md`「鐵律」那一節，動手前讀對應的那一組）

| 組 | 一句話 |
|---|---|
| **一、動手之前** | 先確認**目的**不是規格；**推論不是權威事實**；🔴 **⛔ 不給止血方案**——推論的方向要朝向找出**權威原因** |
| **二、寫幾何演算法** | 「一個面」＝共面區域；連續幾種規則都失敗多半是**問錯問題**；容許值要有物理意義；🔴 **性質由「兩端」決定時，只改一端 ＝ 沒改** |
| **三、做介面與回饋** | **誤報比漏報更糟**；讓兩個數字互相對得起來；🔴 **問「這個數字在這個狀態下有沒有意義」** |
| **四、效能** | 每幀迴圈裡的東西會不會隨模型大小成長 |
| **五、查證與交付** | 規格上合法 ≠ 別人的軟體讀得動；**開清單前問「使用者驗得出這個結果嗎」** |
| **六、關於日誌本身** | 跨功能的原則⛔ 不能只留在第一次遇到它的功能底下；**⛔ 不要寫一個不存在的退路** |

### 跟 kang 講話（⛔ 這幾條破過很多次）

- **⛔ 不用程式圈的行話** —— 他驗的是畫面，⛔ 不是原始碼。欄位名去 `index.html` 抄
- **講數量與形狀**，而且**單位要講清楚是點、邊還是面**
- 🔴 **實測清單裡的數字⛔ 不准寫死一個值** —— 要寫成**他算得出來的式子**
  （前置參數照 `js/build/prim.js` 的 `PRIM_DEFAULTS` 抄，⛔ 不照測試、⛔ 不自己填）
- **他說 OK 就當場回寫**，⛔ 不要等到下次有東西改才順手做

---

## 這個專案有哪些文件

> ⭐ **⛔ 不在這裡列規格檔清單** —— 那會漂。要看有哪些規格：**`Glob 規格\*`**（由檔案系統產生）

| 檔 | 做什麼用 | 怎麼讀 |
|---|---|---|
| **`STATUS.md`**（本檔）| 現況／待辦／落點規則／鐵律索引 | **每次開場讀完** |
| `PROJECT_LOG.md` | **參考書**：鐵律全文、協作規則、部署、尺寸的依據、目標與範圍 | ⛔ **不從頭讀**，看目次取用 |
| `規格\*.md` | 一個功能一檔的完整規格 | 需要時精準取一份 |
| `HISTORY.md` | 已結案流水帳、34 條坑的完整經過 | **只被 Grep**，目次由程式產生 |
| `外部參考-Blender編輯.md` | 能力對照表（第 10 節）＝**決定下一件做什麼的依據** | 動編輯前 |
| 其餘專屬文件 | 尺寸依據的推導／編輯功能設計方向／瓣片展開與省道／外部參考三份 | 動那一塊之前 |

**文件工具**：`node 文件工具/目次產生器.js <檔名>`（改完 `##` 節要重跑）
